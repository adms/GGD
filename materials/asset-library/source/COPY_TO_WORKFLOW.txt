GGD 素材庫：其他工作流從這裡開始

唯一共編說明：https://github.com/adms/GGD/blob/codex/hero-model-library-options/materials/asset-library/README.md
全角色模型盤點：https://github.com/adms/GGD/blob/codex/hero-model-library-options/materials/hero-model-library/全角色模型盤點.md
Repository：adms/GGD
共編分支：codex/hero-model-library-options
PR：https://github.com/adms/GGD/pull/1152

在含此分支變更的 GGD checkout 根目錄：
python3 tools/hero-model-library/query.py 莉娜
python3 tools/hero-model-library/query.py b2-popp --json
python3 tools/hero-model-library/query.py 拳四郎 --downloads
python3 tools/hero-model-library/sync.py

第一守則：所有工作流取得的模型、貼圖、動作、特效、音效與語音完整保留，免費／付費版本均須整合後台選項。
第二守則：手動指定模型 > 原著模型 > MOD社群修改 > 相似模型貼圖修改 > 相似模型 > 300英雄 > MBA > 原版 > 借用 W3X 選用。原著模型指原作遊戲直接擷取；300／MBA 保留第 6／7 位。
第三守則：成品一律進 Git；半成品、原始來源、準備材料等進 S3；本機全部保留。
每批完成後重建全角色模型盤點.md，來源與產物同批 commit + push。查詢只讀 Git，不依赖這台 Mac 的 outputs 或 intake。
來源網址／改造備註改 download-sources.json；第二批／舊英雄配對改 pairing-inputs.json；獨立副本改 derivatives.json。詳細來源表都在唯一共編說明。

成品固定入口：materials/asset-library/git-release.json；成品檔案：materials/asset-library/releases/<版本>/
以同一 Git commit 的成品與 default-policy.json 為準；release.json 保留既有 S3 副本位置，勿用 S3 latest 覆蓋 Git 版本。
原始／半成品備份在 legacy/，僅人工指定許可使用，不得自動取用或混入可用候選。
AWS 只用 vibe-coding profile、ap-east-2 region；不讀憑證、不切換 profile、不改 IAM，AccessDenied 回報 action/resource。

整庫儲備規則：不依角色新舊、上架狀態或既有 GGD ID 篩選；不同世代、平台、作品、版本與配色全部保留。先取得原始整庫／整包並保存逐檔 SHA-256，按需配對轉換。原始儲備存 S3 legacy 並全留本機；驗收成品進 Git，再加入後台獨立選項。同級候選按來源遊戲發售日由新到舊，手動指定及九級順位保留。
查詢尚未配對與下載中來源：python3 tools/hero-model-library/query.py --downloads gitlab-ssbu-models
完整規則：materials/hero-model-library/全角色模型盤點.md；機器規則：download-sources.json 的 ingestionPolicy、consoleSourceScope，以及 default-policy.json 的 sameClassPriority。

角色語音索引：materials/hero-model-library/角色語音索引.md；voice-index.json／voice-files.jsonl.gz 保存角色與逐檔 SHA-256，供合成素材挑選、聽審及轉錄。查詢：python3 tools/hero-model-library/query_voice.py 莉娜

語音逐檔索引以 voice-index.json.sourceFileManifest 為準；目前 Git 儲存完整 voice-files.jsonl.gz，query_voice.py 自動驗證及解壓，本機另保留未壓縮 JSONL。音樂、音效及含合成播報來源保留並標記 excludedFromSpeechInput，不直接混入角色語音輸入。
