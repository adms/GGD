import { z } from "zod";

export const LOBBY_RALLY_DOC_ID = "lobby-rally";

export const zConfigLobbyRallyDoc = z
  .object({
    id: z.literal(LOBBY_RALLY_DOC_ID),
    schema: z.literal("config.lobby-rally@1"),
    note: z.string().optional(),
    /**
     * 總開關。⛔ 關掉 = 建房不再廣播、一鍵開打回到「立刻開」。
     *
     * ⭐ 預設 **on**，因為第〇·六守則說「優先權大的更新後都是預設啟動」——
     * 開關存在是為了**回頭**，不是為了觀望。
     */
    enabled: z.boolean().describe(
      "@zh 集合令總開關\n" +
      "@note ⛔ 關掉 = **一鍵 rollback**：建立房間不再對大廳廣播、一鍵開打回到 2026-08-21 之前那條「立刻開，不等人」的路。⚠️ 關掉之後主揪仍然可以用既有的一對一邀請（房間裡點名字送邀請碼），那條路完全沒有變。",
    ),
    /**
     * 集合令的倒數秒數。⭐ owner 明說 **10**。
     *
     * 上界 120 是傳輸柵欄（伺服器同界）：再長的等待不是「拉人」，是把主揪關在
     * 一個他自己按不掉的畫面裡。下界 3 是「來得及看清楚視窗上寫什麼」。
     */
    waitSeconds: z.number().min(3).max(120).describe(
      "@zh 倒數幾秒後開打\n" +
      "@note ⭐ owner 明說 **{{出貨值}}**（2026-08-21：原始規格 10 → 早上「改成五秒」→ 晚上「5->10秒」改回）。這是**期限**不是共識：時間到就開，沒趕上的位子由 BOT 補。調大＝更容易湊到真人，代價是每一場開打前都要空等；調小＝幾乎等於立刻開。⚠️ 上界 120 由 schema 給（伺服器的傳輸柵欄同界），⛔ 那不是政策，是防止一個打錯的數字把主揪關在自己按不掉的畫面裡。",
    ),
    /**
     * ⭐ owner 明說的「**包含 vs bot**」：一鍵開打也走同一條集合令
     * （建 listed 房 → 廣播 → 等 → bot 補位開始），⛔ 不是兩條流程。
     *
     * 關掉 = 一鍵開打退回 `POST /rooms/solo`（不列房、不等人、立刻開）。
     * ⚠️ **練習模式永遠走 solo 那條路**，跟這一格無關：練習房是測試碼的鑰匙，
     * 一間有旁人的練習房＝作弊房（見 `room.Create()` 的檔頭）。
     */
    includeBotMatch: z.boolean().describe(
      "@zh 一鍵開打也走集合令\n" +
      "@note ⭐ owner 明說的「**包含 vs bot**」。開著（出貨值）＝ 按下一鍵開打會先建一間**列在大廳**的房、廣播、等倒數，誰來了誰上。關掉＝一鍵開打回到不列房、不等人、立刻對 11 隻 bot 開場。⚠️ 練習模式不受這一格影響（見上面的說明）。",
    ),
    /**
     * 倒數到期時，⛔ 不管有沒有人按過「準備」就開始。
     *
     * 決策點：集合令是**期限**不是共識。關掉它的話，一個從房間列表走進來、
     * 從不按準備的路人就能讓主揪的倒數永遠開不了場，而畫面上只會顯示
     * 「按了開始，什麼都沒發生」。
     */
    startIgnoresReady: z.boolean().describe(
      "@zh 倒數到期就開，不管有沒有人按準備\n" +
      "@note 開著（出貨值）＝ 期限到就開場。關掉＝照一般房間的規則，**每一個非主揪成員都要按過準備**才開得了 —— ⚠️ 那表示一個從房間列表走進來、從不按準備的路人，可以讓主揪的倒數永遠開不了場，而畫面上只會顯示「按了開始，什麼都沒發生」。⛔ 一般的「開始遊戲」按鈕**不受這一格影響**，它永遠要求全員準備。",
    ),
    /**
     * ⭐ **集合令是「預設加入」還是「預設不加入」** —— owner 2026-08-21 逐字：
     * 「**預設是加入，五秒是讓人按否定的**」。
     *
     * | 值 | 倒數結束時 | 視窗的主要按鈕 |
     * |---|---|---|
     * | `opt-out`（出貨） | **自動加入** | 「不要」 |
     * | `opt-in` | 什麼都不做（視窗關掉） | 「加入」 |
     *
     * ⛔ 這不是措辭差異：opt-in 要人**主動點同意** ——
     * opt-in 幾乎等於沒有人會加入，而這張票的目的是「拉人進來」。
     * ⇒ `opt-in` 存在只為了**回頭**（第〇·六守則），⛔ 不是為了觀望。
     */
    joinMode: z.enum(["opt-out", "opt-in"]).describe(
      "@zh ⭐ 倒數結束是「加入」還是「放棄」\n" +
      "@note ⭐ owner 2026-08-21 逐字：「**預設是加入，五秒是讓人按否定的**」。**opt-out（出貨值）**＝ 視窗跳出來，**倒數結束前沒按「不要」就進房**，主要按鈕是「不要」。**opt-in** ＝ 回到 2026-08-21 早上那一版：要**按下「加入」**才進房，沒按就什麼都不會發生。⛔ 這不是措辭差異：opt-in 要人**主動點同意**，而這張票的目的是「拉人進來」。⚠️ 窗口長度看**上面那一格**（「倒數幾秒後開打」），⛔ 這裡不複述數字 —— 複述就是第四個住處。⇒ opt-in 是**一鍵 rollback**，⛔ 不是觀望用的。\n" +
      "@opt opt-out opt-out（出貨）＝ 沒按「不要」就進房\n" +
      "@opt opt-in opt-in ＝ 要按「加入」才進房",
    ),
    /**
     * opt-out 的**提前量**：倒數在截止前這麼多秒就自動加入，⛔ 不是掐在同一刻。
     *
     * ⚠️ 這一格是承重的，不是裝飾：主揪的客戶端在 `expiresAt` 那一刻按下開始，
     * 一間**已經開打**的房會把同一刻送出的加入請求拒掉（409/404）——
     * 於是「預設加入」會變成「預設加入失敗」，而畫面上什麼都不會說。
     * 提前量就是那趟 click→request→SADD 的來回餘裕。
     *
     * ⚠️ 上界 10 大於 `waitSeconds` 的下界 3，所以「提前量吃掉整個窗口」是**設定得出來**的
     * ⇒ `autoJoinAt()` 另外夾住「提前量不得超過窗口的一半」，⛔ 讓「不要」永遠按得到。
     */
    autoJoinLeadSeconds: z.number().min(0.2).max(10).describe(
      "@zh 自動加入要比開場早幾秒\n" +
      "@note ⚠️ 承重，⛔ 不是裝飾：主揪的畫面在倒數**歸零那一刻**按下開始，而一間已經開打的房會把同一刻送出的加入請求拒掉 —— 於是「預設加入」會變成「預設加入失敗」，而畫面上什麼都不會說。這一格就是那趟來回的餘裕（出貨 {{出貨值}} 秒＝ 5 秒的窗口裡，玩家有 3.5 秒可以按「不要」）。調大＝更保險但可以反悔的時間更短；調小＝網路慢的人會被關在門外。⚠️ 上界 10 大於倒數秒數的下界 3，所以填得出「提前量吃掉整個窗口」—— 程式另外夾住「不得超過窗口的一半」，⛔ 讓「不要」永遠按得到。",
    ),
    /**
     * ⭐ **掛機的人不要被拉進去**：這麼多秒沒有任何輸入（或分頁在背景）的人，
     * 集合令**不會**替他自動加入 —— 視窗留著，他回來仍然可以自己按「加入」。
     * `0` = 關掉這道閘（誰都會被自動拉進去）。
     *
     * ⚠️ 為什麼判斷在**瀏覽器**而不是伺服器：平台的 presence 只有「連線活著」
     * （`lobby/ws.go` 的 heartbeat 是**計時器**送的，⛔ 不是使用者動作），
     * 所以伺服器分不出「盯著大廳的人」和「開著分頁去睡覺的人」。真正的輸入事件
     * 只有收件人自己的分頁看得到 —— 而自動加入本來就是他那一台發出的請求。
     * ⛔ 不要為了這一格在伺服器上造第二套 presence。
     */
    idleExcludeSeconds: z.number().int().min(0).max(3600).describe(
      "@zh ⭐ 掛機幾秒以上就不自動加入\n" +
      "@note ⭐ 這是「預設加入」帶來的新問題的答案：**這麼多秒沒有任何滑鼠／鍵盤動作（或分頁在背景）的人，不會被自動拉進去** —— 視窗留著，他回來仍然可以自己按「加入」。⛔ 少了它，掛機的人會被拉進一場比賽然後整場不動，而那對其他九個人**比少一個人更糟**。填 **0 ＝ 關掉這道閘**（誰都會被自動拉進去）。⚠️ 判斷發生在**收件人的瀏覽器**，⛔ 不是伺服器：平台的 presence 只知道「連線還活著」（heartbeat 是計時器送的），分不出「盯著大廳」和「開著分頁去睡覺」。",
    ),
    /**
     * 加入房間（**自動或手動都算**）＝同時標記準備好（同一個 request）。
     *
     * 決策點：關掉的話玩家要在倒數剩不到幾秒時再按一次準備，而那一趟來回
     * 正好是開場會把他丟下的那個窗口。
     * ⚠️ 2026-08-21 從 `readyOnAccept` 改名：opt-out 之下**沒有人按過「同意」**，
     * 舊名字描述的動作不存在了（第一守則：語意改了，舊文案就是謊話）。
     */
    readyOnJoin: z.boolean().describe(
      "@zh 進房就等於準備好（自動加入也算）\n" +
      "@note 開著（出貨值）＝ 進房與標記準備是**同一個 request**，不管那一次進房是倒數自動送出的還是玩家自己按的。關掉＝玩家進房之後還要自己再按一次準備，而那一趟來回正好落在倒數剩不到幾秒的時候 —— 開場會把他丟下。⚠️ 這一格 2026-08-21 從 `readyOnAccept` 改名：預設加入之下**沒有人按過「同意」**，舊名字描述的動作已經不存在。",
    ),
    /**
     * 場上要有幾個**真人**才顯示玩家名冊。⭐ owner 的條件句是
     * 「**若有其他玩家**一起進入房間遊戲」—— 2 就是那句話的直譯。
     *
     * 填 1 = 連單機 vs bot 也永遠顯示（除錯時有用）。
     */
    rosterMinHumans: z.number().int().min(1).max(12).describe(
      "@zh 場上至少幾個真人才顯示名冊\n" +
      "@note ⭐ owner 的條件句是「**若有其他玩家**一起進入房間遊戲」，2（出貨值）就是那句話的直譯。填 1 ＝ 連單機打 BOT 也永遠顯示（除錯時有用）。⚠️ 數的是「這個位子屬於真人」的**座位數**，⛔ 不是「現在還連著的人」—— 不然一個玩家斷線的瞬間名冊就會自己消失，而那正是最需要它的時候。上界 12 就是一場比賽的座位數。",
    ),
    /**
     * ⭐ owner 明說的「**每回合結算也都要特別再提示一次**」。
     * 他自己給了理由：「因為**有可能斷線離開或連線回來房間繼續遊戲**」——
     * ⛔ 這不是裝飾，是斷線重連之後「我現在跟誰在打」的唯一資訊來源。
     */
    showRosterInSettlement: z.boolean().describe(
      "@zh 每回合結算顯示玩家名冊\n" +
      "@note ⭐ owner 明說「**每回合結算也都要特別再提示一次**」，理由是他自己給的：「因為**有可能斷線離開或連線回來房間繼續遊戲**」。⛔ 這不是裝飾 —— 斷線重連的玩家沒有事件歷史，這份名冊是他「我現在跟誰在打、誰還在線上」的唯一資訊來源。關掉＝回合結算與中場商店都不再顯示。",
    ),
    /** 選角階段（＝一起進場的那一刻）也顯示一次名冊。 */
    showRosterInChampSelect: z.boolean().describe(
      "@zh 選角畫面顯示玩家名冊\n" +
      "@note 一起進場的那一刻也報一次姓名／積分／所選英雄。⚠️ 選角當下多數人還沒鎖英雄，所以那一欄會先寫「未選角」再一個一個亮起來 —— 那是對的，⛔ 不是漏資料。",
    ),
    /**
     * ⭐ **邀請時可以選「隊友」還是「敵對方」**（GH#655）—— owner 2026-08-24 逐字：
     * 「大廳邀請對象進房間應該要能選擇是**隊友**還是**敵對方**」。
     *
     * true（出貨）＝ 好友面板的邀請鈕變成**兩顆**（同隊 / 對面），意向跟著邀請碼
     * 一路帶到房間，落座時**優先**照它走。
     * false ＝ 一顆沒有陣營的邀請鈕，也就是這張票之前的行為 ⇒ **一鍵 rollback**。
     *
     * ⚠️ 意向是**偏好，⛔ 不是硬性**（owner 同一則：「⭐ 建議偏好（滿了就讓位）」）：
     * 想要的那一隊滿了就落到下一隊，⛔ 而且不是靜靜地換 —— 房間列表上每個人的
     * 隊伍是**由落座那支函式本身**算出來的（`room.PlanSeats`），所以換邊在開打
     * **之前**就看得到。
     *
     * ⚠️ 這一格為什麼住在集合令這份文件裡：它是**大廳的邀請行為**，而且它的消費端
     * 就在同一支 `apps/client/src/ui/platform/lobbyRally.ts` 旁邊。⛔ 另開一份
     * config 文件會多一份 session-gate 與導覽列接線，而那兩行是全 repo 已知
     * **唯一真正共用**的檔（CLAUDE.md 第〇·七守則）—— 為了一格布林去動它划不來。
     */
    inviteSideChoice: z.boolean().describe(
      "@zh 邀請可指定隊伍（同隊／對面）\n" +
      "@note ⭐ owner 2026-08-24：「大廳邀請對象進房間應該要能選擇是**隊友**還是**敵對方**」。開著（出貨值）＝ 好友面板的邀請鈕變成**兩顆**（同隊／對面），意向跟著邀請碼一路帶到房間，落座時優先照它走。關掉＝ 一顆沒有陣營的邀請鈕，也就是這個功能出現之前的行為 ⇒ **一鍵 rollback**。⚠️ 意向是**偏好，⛔ 不是硬性**（owner 同一則：「建議偏好（滿了就讓位）」）：想要的那一隊滿了就落到下一隊 —— ⛔ 而且不是靜靜地換，房間列表上每個人的隊伍徽章由**落座那支函式本身**算出來，換邊在開打**之前**就看得到（沒滿足意向的那一顆會變紅）。",
    ),
  })
  .strict();

// ─────────────────────── 大廳集合令（GH#492）────────────────────────────

export type ConfigLobbyRallyDoc = z.infer<typeof zConfigLobbyRallyDoc>;

/** 去掉 id/schema/note 的殼之後,程式真正讀的那一份。 */
export type LobbyRallyPolicyDoc = Omit<ConfigLobbyRallyDoc, "id" | "schema" | "note">;

/**
 * 出貨預設（＝內容載不到時的保險絲）。
 *
 * ⭐ `waitSeconds: 10` 是 owner 明說的那一格。⚠️ 它當天改過**兩次**：
 * 原始規格「最多等 10 秒」→ 早上「改成五秒」→ **晚上改回 10**
 * （「調整戰鬥開始的拉人時間 5->10秒」）。⛔ 中間那一版已被取代，⛔ 不要再改回 5。
 * `joinMode: "opt-out"` 是他同一天說死的第二格（「**預設是加入，五秒是讓人按否定的**」）；
 * 其餘各格是決策點，預設值選的是
 * 「照 owner 的話做」的那一邊（第〇·六守則：優先權大的更新預設啟動）。
 *
 * ⚠️ 每一格都必須和 `apps/client/src/ui/platform/lobbyRally.ts` 的
 * `DEFAULT_LOBBY_RALLY` 一字不差 —— 那一份才是畫面真的在用的。
 * `apps/admin/src/laneConfigDocs.test.ts` 逐格比對兩邊,差一格就紅。
 */
export const DEFAULT_LOBBY_RALLY_POLICY: LobbyRallyPolicyDoc = {
  enabled: true,
  waitSeconds: 10,
  includeBotMatch: true,
  startIgnoresReady: true,
  // ⭐ owner 2026-08-21:「預設是加入，五秒是讓人按否定的」。
  // ⚠️ 那句話裡的「五秒」是當時的窗口值,同日晚上改成 10 —— ⛔ 裁決講的是**預設方向**,不是秒數。
  joinMode: "opt-out",
  autoJoinLeadSeconds: 1.5,
  idleExcludeSeconds: 120,
  readyOnJoin: true,
  rosterMinHumans: 2,
  showRosterInSettlement: true,
  showRosterInChampSelect: true,
  // ⭐ GH#655 —— owner 2026-08-24 明說要這個功能,所以它出貨就是 on
  // (第〇·六守則:優先權大的更新後都是預設啟動)。false 是一鍵 rollback。
  inviteSideChoice: true,
};

/**
 * 文件 → 政策。缺席／壞掉一律回退到出貨預設。
 *
 * ⚠️ 這裡**沒有**「載不到就關掉功能」這個選項:一份載不到的內容文件是
 * 2026-08-01 骨架事故那一條路,而在那條路上把集合令靜靜關掉,會讓「內容全毀」
 * 長得跟「owner 昨天關掉了集合令」一模一樣 —— 兩個都不會有人看見。
 */
export function resolveLobbyRally(
  doc: ConfigLobbyRallyDoc | null | undefined,
): LobbyRallyPolicyDoc {
  if (!doc) return DEFAULT_LOBBY_RALLY_POLICY;
  return {
    enabled: doc.enabled,
    waitSeconds: doc.waitSeconds,
    includeBotMatch: doc.includeBotMatch,
    startIgnoresReady: doc.startIgnoresReady,
    joinMode: doc.joinMode,
    autoJoinLeadSeconds: doc.autoJoinLeadSeconds,
    idleExcludeSeconds: doc.idleExcludeSeconds,
    readyOnJoin: doc.readyOnJoin,
    rosterMinHumans: doc.rosterMinHumans,
    showRosterInSettlement: doc.showRosterInSettlement,
    showRosterInChampSelect: doc.showRosterInChampSelect,
    inviteSideChoice: doc.inviteSideChoice,
  };
}
