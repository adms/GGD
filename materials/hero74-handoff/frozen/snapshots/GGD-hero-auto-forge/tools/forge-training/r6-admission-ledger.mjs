/** Deterministic data-admission ledger; never changes training data or labels. */
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {fileURLToPath} from 'node:url';import {isDeepStrictEqual as equal} from 'node:util';import {digest} from './dataset.mjs';import {fileHash} from './precision-diagnostic.mjs';
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){
 const root=path.resolve(process.argv[2]),read=p=>JSON.parse(fs.readFileSync(p)),manifest=read(path.join(root,'dataset-manifest.json')),old=read(path.join(manifest.parent,'cases.private.json')).filter(c=>c.split==='train'),train=read(path.join(root,'cases.private.json')).filter(c=>c.split==='train'),excluded=read(path.join(root,'excluded-legacy-mechanism-training.json')),rows=[];
 const reviewedSources=new Set(['hero-source','owner-mechanism']);
 for(const c of old){const next=train.find(n=>n.id===c.id||n.id===c.id+'-wording-v2'),omission=excluded.find(e=>e.id===c.id);assert(!!next!==!!omission);const status=omission?'excluded-superseded-catalog':next.id===c.id?'retained-prior-reviewed':'reworded-same-label';if(next){assert(equal(c.target,next.target));if(status==='retained-prior-reviewed')assert(equal(c.messages,next.messages));}
  rows.push({originalId:c.id,trainingId:next?.id??null,status,task:c.task,sourceId:c.sourceId??null,sourceVersion:reviewedSources.has(c.task)?'historical-source-snapshot':'historical-classification-catalog',oldRequestDigest:c.requestDigest,newRequestDigest:next?.requestDigest??null,labelChanged:false,input:next?JSON.parse(next.messages[1].content):null,acceptedTargets:next?.acceptedTargets??c.acceptedTargets,reviewReason:omission?.reason??next.reviewReason??next.proof??null,fullSourceRereadThisExtension:reviewedSources.has(c.task),ownerGold:false});
 }
 for(const c of train.filter(c=>!old.some(o=>c.id===o.id||c.id===o.id+'-wording-v2')))rows.push({originalId:null,trainingId:c.id,status:'new-personally-reviewed',task:c.task,sourceId:c.sourceId??null,sourceVersion:c.task==='owner-mechanism'?'historical-source-snapshot':'pinned-main-4793eaaa-review-v4',newRequestDigest:c.requestDigest,input:JSON.parse(c.messages[1].content),acceptedTargets:c.acceptedTargets,reviewReason:c.reviewReason,fullSourceRereadThisExtension:c.task==='owner-mechanism',ownerGold:false});
 const counts=Object.fromEntries(Object.entries(Object.groupBy(rows,r=>r.status)).map(([k,v])=>[k,v.length]));assert.deepEqual(counts,{'retained-prior-reviewed':240,'reworded-same-label':24,'excluded-superseded-catalog':170,'new-personally-reviewed':202});assert.equal(rows.filter(r=>r.trainingId).length,466);
 const ledger={createdAt:new Date().toISOString(),counts,trainingRows:466,ledgerRows:636,trainingCasesSha256:manifest.casesSha256,rows,oldArtifactsModified:false,modelImprovementProven:false,limitations:['The ledger includes 170 excluded old rows, not 636 training examples.','24 rewrites do not mean 24 proven mislabels; original label values stay fixed, some propositions changed.','58 current needs appear in single and multi-plan contracts; two presentations are not independent needs.','54 retained VFX rows inherit previous review; they were not newly visually validated.','All labels assistant reviewed, not independent human Gold.']};
 fs.writeFileSync(path.join(root,'DATA_ADMISSION.json'),JSON.stringify(ledger,null,2)+'\n',{flag:'wx'});
 const lines=['# R6-v2 資料清單與採用狀態','','實際訓練 **466題**。JSON清單有636列，是因為包含170列被排除的舊資料，不可把清單列數當訓練數。每列有舊ID、新訓練ID、狀態、來源、完整模型輸入、答案及理由。','','| 狀態 | 筆數 | 說明 |','| --- | ---: | --- |','| 保留既有已審查題 | 240 | 英雄80、Owner106、VFX54；輸入與標籤不變。 |','| 同標籤措辭改寫 | 24 | 英雄10、Owner14；不是24個已證實錯標。 |','| 排除舊能力目錄 | 170 | 不再訓練過期wave/sweep敘述；不是170答案全錯。 |','| 新增已親自審查 | 202 | Owner未知54、目前單卡58、多卡90。 |','','新增多卡90中58是相同單卡訓練需求的另一公開契約，只有另外32個新寫需求；不能把重複展示當獨立來源。英雄與Owner完整來源已重讀，特效沿用既有分類審查，不做視覺驗證。','','資料採用清單：`DATA_ADMISSION.json`。Owner新增54題完整原文／理由：`owner-unknown-review.json`。多卡新需求train/dev/test逐題理由：`stack-new-review.json`。排除170列ID：`excluded-legacy-mechanism-training.json`。舊資料與測試不修改。','','資料品質改善不等於模型已改善；模型結果仍需看同條件dev/test/post及真入口。',''];fs.writeFileSync(path.join(root,'DATA_ADMISSION.md'),lines.join('\n'),{flag:'wx'});
 console.log(JSON.stringify({root,counts,trainingRows:466,ledgerRows:636,ledgerSha256:fileHash(path.join(root,'DATA_ADMISSION.json')),rowsDigest:digest(rows)}));
}
