import { z } from "zod";
import { zId } from "../common";

/* ══════════════════════════════════════════════════════════════════════════
 * config.replay@1 —— 對戰錄影政策 (owner 2026-08-02)
 * ══════════════════════════════════════════════════════════════════════════
 *
 * owner 2026-08-02：「請幫我預設打開，就算玩到一半就離開也應該有 replay 才對」
 *
 * ── 為什麼這份文件存在（＝在此之前的狀態）────────────────────────────────
 * #175 的錄影從頭到尾**沒有開關**。`MatchRoom.onCreate` 無條件呼叫
 * `MatchRecorder.open()`，落地間隔是 `Recorder.ts` 裡的 `const FLUSH_MS = 500`，
 * 保留量是 `store.ts` 裡的 `RETAIN_MAX_FILES = 200` / `RETAIN_MAX_AGE_DAYS = 30`。
 * 三個都是 CLAUDE.md 第一守則點名的那種寫死：**每一個都是一個「A 還是 B」的
 * 決定**，而它們一個都不在後台，改任何一個都要 rebuild 映像 + 重啟容器。
 *
 * ⚠️ 「預設打開」在程式上一直是真的（沒有開關 ⇒ 永遠開）。owner 之所以覺得它
 * 是關的，是因為 GH#170：正式機的 `/data/replays` 是 root 的、容器跑 uid 1000，
 * `createWriteStream` **非同步**吃到 EACCES，於是每一場都「開了錄影、零位元組
 * 落地」。那個病灶的解法在 `replayHealth.ts` + `docs/replay-observability.md`，
 * 不在這份文件裡 —— 這份文件解的是「開關本身不存在」。
 *
 * ── 這份文件**不**收什麼 ──────────────────────────────────────────────────
 * `GGD_REPLAY_DIR` / `GGD_REPLAY_REQUIRED` / `GGD_REPLAY_UNHEALTHY_AFTER` /
 * `GGD_REPLAY_HEALTHZ_STATUS` 留在環境變數，**刻意的**：那四個是「這台機器的
 * 監控行為」，一台一個值，而且 `/healthz` 要在內容樹載入之前就答得出來。
 * 這份文件收的是「錄影政策」——每一場比賽都適用、owner 會想改的那些。
 */
export const zConfigReplayDoc = z
  .object({
    id: zId,
    schema: z.literal("config.replay@1"),
    note: z.string().optional(),
    /**
     * 要不要錄影。**出貨值 true**（owner 2026-08-02「請幫我預設打開」）。
     *
     * 關掉之後這台 shard 上的每一場都完全不開錄影檔：後台「對戰回放」不會再有
     * 新的一列，`/healthz` 的 `replay.opened` 停在原地。留這一格是因為錄影是
     * best-effort 的旁路 —— 磁碟快滿、或某一場出了會讓錄影器自己爆掉的內容時，
     * 這是唯一不用重新 build 映像就能止血的閥。
     */
    enabled: z.boolean().describe(
      "@zh 錄影總開關\n" +
      "@note 出貨**開著**（owner 的裁決）。關掉之後這台 shard 上的每一場都完全不開錄影檔：後台「對戰回放」不會再有新的一列，也就沒有任何一場可以回放。留這一格是因為錄影是旁路 —— 磁碟快滿、或某一場的內容讓錄影器自己爆掉時，這是唯一不用重建映像就能止血的閥。⚠️ 讀不到內容文件時它**仍然是開的**（fail-open）：內容載入失敗不可以順手把錄影關掉，那正是 2026 年 8 月「一場都沒錄到」沒有人發現的形狀。",
    ),
    /**
     * 緩衝的錄影行多久交給檔案串流一次（毫秒）。**出貨值 500**。
     *
     * 它決定的是「**中途離開最多丟掉幾秒**」：程序被 `kill -9`（容器重啟、OOM）
     * 時，還沒交出去的那一段就沒了。調小 = 掉的秒數變少，代價是每分鐘多幾次
     * write syscall；調大 = 反過來。**不可以調成 0** —— 那等於每 tick 寫檔，
     * 而錄影器的第一條契約是「不准在 tick 路徑上做同步磁碟 I/O」。
     *
     * 上界 10000：再大的話一次容器重啟就會吃掉十秒以上的比賽，而那正是這張單
     * 要修的東西。下界 50：低於這個值只是在燒 syscall，換不到有意義的秒數。
     */
    flushIntervalMs: z.number().int().min(50).max(10_000).describe(
      "@zh 多久把錄影寫進磁碟一次（毫秒）\n" +
      "@note 它決定的是「**程序被硬砍時最多丟幾秒**」—— 容器重啟、部署、OOM 被殺的那一刻，還沒交給磁碟的那一段就沒了。出貨 {{出貨值}}＝最多丟半秒。調小＝丟得更少，代價是每分鐘多幾次寫入；調大到 5000＝一次重啟可能吃掉五秒的操作。⚠️ **不可以是 0**：那等於每一個 tick 寫一次檔，而錄影器的第一條規矩是不准在 tick 路徑上做同步磁碟 I/O —— 那會直接讓場上的人卡頓。",
    ),
    /**
     * 磁碟上最多留幾份錄影（新的贏）。**出貨值 0 ＝ 不限份數**（GH#498）。
     *
     * 影響的是「多久以前的那一場還找得回來」與磁碟佔用。實測一場 4 分鐘 12 人
     * 的比賽壓縮後約 60 KB。調到 1 等於「只留最新一場」。
     *
     * ⚠️ **這是一條和天數獨立的刪除規則。** owner 2026-08-21 說的是「超過幾天的
     * 錄影一律刪掉 預設不刪除」—— 只把天數改成不刪，第 201 場仍然會把第 1 場刪掉，
     * 而那正是他會說「我明明設了不刪」的那個結果。所以兩格一起以 **0 = 不限**
     * 出貨。⭐ ROLLBACK：這一格填回 200 就是舊行為。
     */
    retainMaxFiles: z.number().int().min(0).max(5_000).describe(
      "@zh 磁碟上最多留幾份錄影（0 = 不限）\n" +
      "@note **出貨 {{出貨值}} ＝ 份數不限**（GH#498）。填非 0 的話超過的從最舊的開始刪（正在錄的那一場永遠不會被刪）。實測一場 4 分鐘 12 人的比賽壓縮後約 60 KB，所以 200 份約 12 MB。調成 1＝只留最新一場，昨天那一場明天就找不回來了。⚠️ 這是**和下面那格獨立的第二條刪除規則** —— owner 2026-08-21 說的是「超過幾天」，但只把天數設成不刪的話，第 201 場照樣會把第 1 場刪掉，所以兩格一起以「不限」出貨。⭐ 想回到舊行為：這一格填 200。",
    ),
    /**
     * 超過幾天的錄影一律刪掉（與上面那條取先觸發的）。**出貨值 0 ＝ 永不刪**。
     *
     * owner 2026-08-21：「對戰錄影 超過幾天的錄影一律刪掉 **預設不刪除**」(GH#498)
     *
     * 影響的是「上個月那一場還在不在」。錄影檔帶著玩家顯示名稱，所以這一格
     * 同時是個資保留期限，不只是磁碟策略 —— 0 代表**永久保留**，這是 owner 的
     * 明確選擇。⭐ ROLLBACK：填回 30 就是舊行為。
     *
     * ⚠️ 兩格都 0 ＝ 磁碟**無限成長**，而正式機的 docker data-root 和錄影目錄在
     * **同一顆碟**（2026-08-16 那次 80GB build cache 把碟塞爆、網站 502）。所以
     * 後台「對戰回放」頁首會印出目前佔用與剩餘空間 —— ⛔ 那不是裝飾，它是這個
     * 預設值唯一的煞車。
     */
    retainMaxAgeDays: z.number().int().min(0).max(3_650).describe(
      "@zh 超過幾天的錄影一律刪掉（0 = 永不刪）\n" +
      "@note owner 2026-08-21：「對戰錄影 超過幾天的錄影一律刪掉 **預設不刪除**」(GH#498)。**出貨 {{出貨值}} ＝ 不因為年齡刪除任何錄影**。填非 0 的話和上面那格取**先觸發**的。⚠️ 錄影檔帶著每一位玩家的顯示名稱，所以 0 代表**永久保留個資**，那是 owner 的明確選擇。⚠️ 兩格都 0 ＝ 磁碟無限成長，而正式機的 docker 和錄影目錄在**同一顆碟**（塞爆過一次，網站 502）——「對戰回放」那一頁的頁首會印出目前佔用與剩餘空間，那是這個預設值唯一的煞車，請偶爾看一眼。⭐ 想回到舊行為：這一格填 30。",
    ),
  })
  .strict();
/** 對戰錄影政策（`content/config/replay.json`）。 */
export type ConfigReplayDoc = z.infer<typeof zConfigReplayDoc>;
