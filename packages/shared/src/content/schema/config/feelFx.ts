import { z } from "zod";
import { zId } from "../common";

/**
 * config.feel-fx@1 —— **爽度**那一層 (`content/config/feel-fx.json`, GH#494)。
 *
 * owner 2026-08-21 逐字：
 *   「殭屍死掉後**掉落小金幣**停留 **1秒**後**動畫效果軌跡(貝茲曲線加速)吸回到
 *     擊殺的英雄**搭配**輕音效** **提高爽度 模仿肉鴿遊戲的氛圍感**」
 *   「**連擊也會有像 candy crush 類似連段音階升高的音效**刺激玩家帶來獎勵感」
 *   「你在施展技能的時候會釋放一個粒子特效，最後會飄散到天空，這個**特效存活
 *     時間真的太長了，請你砍半，不需要後半段飄到天空**」
 *
 * ⭐ 這一份文件裡**沒有一格會改變任何人拿到的金幣**。擊殺賞金今天就已經在
 * `sim/systems/MobSystem.ts` 發完了（`grantGold` / `payMobBounty`），這裡描述的
 * 是那一刻**看得到、聽得到**的部分。`goldPickup.enabled = false` ⇒ 逐位元回到
 * 這一版之前：錢照給，只是不畫不響。
 *
 * ⚠️ 為什麼是自己一份文件而不是塞進 `config.vfx-cleanup@1`：那一份管的是
 * **回收**（池子留幾個、發射器上限），這一份管的是**手感**（飛多久、彎多高、
 * 音階升幾階）。兩者一起調的機會是零，而混在一起會讓「關掉爽度」這個止血閥
 * 順手把回收也關掉。
 *
 * ⚠️ 每一格都是 number / boolean，⛔ 沒有 enum：後台通用表單引擎的
 * `deriveFields` 只認得這兩種，一個 enum 會被歸進 `unsupported` 而那一頁會紅。
 * 「用哪一種緩動」因此是 `easePower` 這個**連續**的數字，而不是三選一 ——
 * 好處是 owner 可以把它調到「剛剛好」，⛔ 不必在三個別人挑好的名字裡選。
 */
export const zConfigFeelFxDoc = z
  .object({
    id: zId,
    schema: z.literal("config.feel-fx@1"),
    note: z.string().optional(),
    /** 殭屍死了掉一枚小金幣、停一下、彎著飛回擊殺者身上的那一段。 */
    goldPickup: z
      .object({
        /**
         * 總開關。false = 完全不畫金幣、不播吸取音效，玩家拿到的錢**一毛不差**
         * （賞金在 sim 裡早就發完了）。這是止血閥：畫面太吵或平板掉幀時一鍵回到
         * 這個功能存在之前。
         */
        enabled: z.boolean().describe(
          "@zh 金幣吸取特效總開關\n" +
          "@note 關掉＝殭屍死了不畫金幣、不播落袋音效。⭐ 玩家拿到的錢**一毛不差**（賞金是伺服器發的，跟這一層無關），所以這是純粹的止血閥：畫面太吵、平板掉幀、或這個特效出了任何問題時，關它就回到這個功能存在之前。",
        ),
        /**
         * 金幣落在屍體上之後，**停在原地閃多久**才開始飛（秒）。owner 指定 1 秒。
         * 調小 = 錢一掉就被吸走，節奏更快但看不清楚掉了幾枚；調大 = 戰場上會同時
         * 躺著更多枚金幣，畫面更「肉鴿」但也更亂。
         */
        hoverSeconds: z.number().min(0).max(5).describe(
          "@zh 金幣落地後停留幾秒\n" +
          "@note 金幣掉在屍體上、開始飛之前先在原地閃多久。owner 指定 1 秒。調小＝錢一掉就被吸走，節奏更快但看不清楚掉了幾枚；調大＝戰場上會同時躺著更多枚金幣，更有肉鴿味但也更亂。",
        ),
        /**
         * 從起飛到被吸進英雄身上要**飛多久**（秒）。這一格決定「吸力」有多強：
         * 短 = 啪一下就進口袋（爽但幾乎看不到軌跡），長 = 看得清楚它繞過來，
         * 但太長會讓死亡與獎勵之間斷開。
         */
        flightSeconds: z.number().min(0.05).max(3).describe(
          "@zh 金幣飛回來要幾秒\n" +
          "@note 從起飛到沒入英雄身體的時間，也就是「吸力」有多強。短＝啪一下就進口袋（爽，但幾乎看不到軌跡）；長＝看得清楚它繞過來，但太長會讓「殭屍死掉」和「拿到獎勵」在感覺上斷開。",
        ),
        /**
         * **加速的力道**。1 = 等速直線（owner 明說⛔ 不要的那個）；越大越像被磁鐵
         * 吸走 —— 起步慢慢飄、末段暴衝進身體。這一格就是 owner 說的「貝茲曲線加速」
         * 裡的**加速**那一半（彎度是下面那一格）。
         */
        easePower: z.number().min(1).max(6).describe(
          "@zh 飛行加速力道\n" +
          "@note owner 特別點名要「加速」而不是等速直線。1＝等速（回到他不要的那個）；越大越像被磁鐵吸走 —— 起步慢慢飄、末段暴衝進身體。這一格只管步調，弧彎多少是下面那格。",
        ),
        /**
         * 貝茲曲線的**控制點抬多高**（世界單位）。0 = 退化成直線（⛔ owner 不要的
         * 那個）；越大金幣拋得越高、弧越誇張。太大會讓金幣飛出畫面上緣再掉回來。
         */
        arcHeight: z.number().min(0).max(8).describe(
          "@zh 飛行弧線抬多高\n" +
          "@note 貝茲曲線的控制點往上抬幾個世界單位。0＝退化成直線（同樣是 owner 不要的那個）；越大拋得越高、弧越誇張。太大金幣會飛出畫面上緣再掉回來。抬高而不是往側邊偏，是因為戰鬥相機固定 68 度俯角，側偏在螢幕上幾乎看不出來。",
        ),
        /**
         * 同時最多讓幾枚金幣在**飛**。超過的直接算成已吸取（錢本來就已經到手了），
         * 只是不畫那一段軌跡。這一格是畫面預算，⛔ 不是掉落上限。
         */
        maxConcurrent: z.number().int().min(1).max(256).describe(
          "@zh 同時最多幾枚在飛\n" +
          "@note 超過的直接算成已經吸走（不畫那一段軌跡）。⭐ 這是畫面預算，**不是掉落上限**，更不是金錢上限 —— 被略過的那幾枚，錢一樣早就到手了。",
        ),
        /**
         * 兩發吸取音效之間至少隔幾毫秒。一次 AoE 掃掉一整排殭屍時，這一格決定
         * 你聽到的是「叮」還是一團糊掉的噪音。⛔ 被擋掉的那幾發是**不播**，
         * 不是排隊等一下再播（排隊只會把噪音延後）。
         */
        sfxThrottleMs: z.number().int().min(0).max(2000).describe(
          "@zh 落袋音效最短間隔（毫秒）\n" +
          "@note 兩聲「叮」之間至少隔多久。一次範圍技掃掉一排殭屍時，這一格決定你聽到的是清脆的幾聲還是一團糊掉的噪音。⛔ 被擋掉的那幾發是**不播**，不是排隊等一下再播（排隊只會把噪音往後挪）。0＝完全不節流。",
        ),
        /**
         * 吸取音效的音量倍率（乘在 `audio-map` 那一格自己的 gain 上）。owner 要的是
         * 「**輕**」音效 —— 一場幾十隻殭屍，這一格開太大就會蓋掉技能與打擊聲。
         */
        sfxVolume: z.number().min(0).max(2).describe(
          "@zh 落袋音效音量倍率\n" +
          "@note 乘在這個音效自己的音量上。owner 要的字是「**輕**」—— 一場幾十隻殭屍，開太大就會蓋掉技能聲與打擊聲。0＝靜音（金幣照飛）。",
        ),
      })
      .strict(),
    /** 連段音階：連擊越長，吸金幣的「叮」越高，⭐ 到頂就不再升（⛔ 不刺耳）。 */
    comboPitch: z
      .object({
        /**
         * 總開關。false = 每一枚金幣都用**同一個**音高（連擊照樣算、HUD 照樣顯示，
         * 只是聽不出高低）。
         */
        enabled: z.boolean().describe(
          "@zh 連段音階總開關\n" +
          "@note 關掉＝每一枚金幣都用同一個音高。⭐ 連擊本身照樣算、HUD 上的連殺數字照樣顯示，只是聽不出高低 —— 這一格碰不到任何機制。",
        ),
        /**
         * 連擊每前進一段，音高升幾個**半音**。1 = 半音階（candy crush 的那種爬升），
         * 2 = 全音階（更明顯但爬得更快就到頂）。0 = 等於關掉。
         */
        semitonesPerStep: z.number().min(0).max(4).describe(
          "@zh 每連一段升幾個半音\n" +
          "@note 1＝半音階（candy crush 那種一階一階爬上去的感覺，出貨值）；2＝全音階，更明顯但很快就到頂。0＝等於關掉。",
        ),
        /**
         * 最多升到第幾段就**停住**。這一格是「不刺耳」的保證：12 段 × 1 半音 = 剛好
         * 一個八度。越大越尖，⛔ 到某個點之後聽起來就只是壞掉。
         */
        maxSteps: z.number().int().min(0).max(24).describe(
          "@zh 最多升到第幾段\n" +
          "@note ⭐ 這一格就是「不刺耳」的保證：升到這一段之後就停住，再連下去也不會更高。12 段 × 1 半音＝剛好一個八度。調更大會越來越尖，過了某個點聽起來就只是壞掉。",
        ),
        /**
         * 多久沒有再擊殺就把音階**歸零**（秒）。⚠️ 這是**聲音**的記憶，與 sim 的連擊
         * 視窗（`sim/combat/killCombo.ts` 的 5 秒）是兩件事：畫面上的連殺數字可以還
         * 亮著，而音階已經回到起點 —— 反過來也行。出貨值刻意設成一樣，所以耳朵與
         * 眼睛預設是同步的。
         */
        resetAfterSeconds: z.number().min(0.5).max(30).describe(
          "@zh 多久沒擊殺就把音階歸零（秒）\n" +
          "@note ⚠️ 這是**聲音**的記憶，和畫面上那個連殺數字是兩件事（那個由伺服器用 5 秒視窗決定）。出貨值刻意設成一樣，所以耳朵和眼睛預設是同步的；設小＝音階更容易回到起點，設大＝聲音會記得比畫面久。",
        ),
      })
      .strict(),
    /**
     * 施法光柱那一圈**往上飄的粒子**（`vfx/castPillar.ts` 的 `moteSpec`）。
     *
     * owner 2026-08-21：「特效存活時間真的太長了，請你砍半，不需要後半段飄到天空」。
     * ⚠️ 只砍壽命是不夠的 —— 那會變成「飄到一半**突然消失**」。所以上升本身也要在
     * 壽命結束**之前**收斂（`gravityY` 小一點、`drag` 大一點），粒子才會自然停住。
     */
    castMotes: z
      .object({
        /** 這一圈粒子最短活幾秒。與下面那格一起決定「餘燼在畫面上留多久」。 */
        lifetimeMinSec: z.number().min(0.05).max(3).describe(
          "@zh 施法餘燼最短壽命（秒）\n" +
          "@note 施法光柱腳邊那圈往上飄的粒子活多久。owner 2026-08-21 要求「砍半」，所以出貨值是原本的一半。⚠️ 只調這兩格會讓粒子在還往上衝的時候被剪掉（看起來像破圖）—— 要一起看下面的重力與阻力。",
        ),
        /** 最長活幾秒。調大 = 回到 GH#494 之前那種「一路飄到天上」的畫面。 */
        lifetimeMaxSec: z.number().min(0.05).max(3).describe(
          "@zh 施法餘燼最長壽命（秒）\n" +
          "@note 同上的另一端。調大＝回到「一路飄到天空」的舊畫面，那正是 owner 說「太長了」的那個。",
        ),
        /**
         * 往**上**的重力（全 repo 唯一一處重力是反的）。它就是「飄到天空」那個
         * 動作本身：越大爬得越高越久，0 = 粒子原地擴散不上升。
         */
        gravityY: z.number().min(0).max(20).describe(
          "@zh 施法餘燼往上的力道\n" +
          "@note 全遊戲唯一一處重力是往上的地方 —— 它就是「飄到天空」那個動作本身。越大爬得越高越久；0＝粒子原地擴散不上升，光柱會失去「能量被吸進去」的讀法。",
        ),
        /**
         * 空氣阻力（每秒保留幾成速度）。越小煞得越快 —— 這是讓上升在**還看得見的
         * 時候**就停下來的那一格，⛔ 不是靠壽命把它切掉。1 = 完全不減速。
         */
        drag: z.number().min(0.1).max(1).describe(
          "@zh 施法餘燼空氣阻力\n" +
          "@note 每秒保留幾成速度。越小煞得越快 —— ⭐ 這是讓上升在「還看得見的時候」自己停住的那一格，也就是不靠壽命硬切掉粒子的正解。1＝完全不減速（衝上天）。",
        ),
      })
      .strict(),
    /**
     * ⭐ GH#788 —— **蓄力集氣特效**：吟唱夠長的每一次施放，多道細光束從四周
     * 向施法者身體**內縮**（owner 附圖：莉娜集氣），顏色＝**隊伍顏色**
     * （wire 的 teamId → 客戶端隊色盤，⛔ 不寫死紅藍）。
     *
     * owner 2026-08-27 逐字：
     *   「所有吟唱時間超過0.3秒以上都要有蓄力特效（粒子特效從外往身體內縮
     *     多道小光束像集氣一樣如圖但顏色是隊伍顏色光芒）」
     *
     * ⭐ 一個通用模組（第〇·五守則）掛在施法窗口事件上，⛔ 不逐技能加 effects。
     * 與施法光柱（CastPillarFx）疊配：光柱＝讀秒，集氣＝蓄力感。
     *
     * ⚠️ `.optional()` 的理由與 `config.vfx-cleanup@1` 那幾族逐字相同：
     * 線上已經有 feel-fx 的耐久覆蓋層，缺這一塊的舊 override 必須照樣過驗。
     * 消費端（`apps/client/src/vfx/CastChargeFx.ts` 的 `readCastCharge`）
     * 逐格退回出貨值。
     */
    castCharge: z
      .object({
        /**
         * 總開關（GH#788 的一鍵 rollback）。false = 完全不畫集氣光束 ——
         * 施法光柱、吟唱條、技能本身一格都不動，逐位元回到這個功能存在之前。
         */
        enabled: z.boolean().describe(
          "@zh 蓄力集氣總開關\n" +
          "@note 關掉＝吟唱時完全不畫集氣光束，逐位元回到 GH#788 之前。⭐ 施法光柱、吟唱條、技能本身一格都不動 —— 這一層純粹是演出，碰不到任何機制，所以這是一鍵 rollback 的止血閥。",
        ),
        /**
         * 吟唱至少幾秒才配集氣（owner 明說的門檻）。量的是 sim 夾完
         * `config.cast-time@1`（floor/cap/multiplier）之後的**最終**吟唱窗口，
         * ⛔ 不是技能文件裡的原始值。上界對齊 cast-time 的 capSec。
         */
        minCastSec: z.number().min(0).max(4).describe(
          "@zh 吟唱幾秒以上才配集氣（秒）\n" +
          "@note owner 指定的門檻（出貨 {{出貨值}}）。⚠️ 量的是吟唱規則頁（floor/cap/倍率）夾完之後的**最終**吟唱窗口，不是技能文件裡的原始值 —— 所以全域把吟唱調快之後，剛好掉到門檻以下的技能會自動失去集氣。0＝每一次有吟唱的施放都畫。",
        ),
        /** 同時幾道光束在內縮。越多越像原圖的滿天集氣，也越吃畫面預算。 */
        beamCount: z.number().int().min(1).max(24).describe(
          "@zh 同時幾道光束在內縮\n" +
          "@note 越多越像參考圖那種滿天集氣，也越吃畫面預算。⭐ 畫質自動降級（AdaptiveQuality）會按比例少畫幾道，這一格是**滿畫質**時的上限；不足此數的部分永遠不會分配網格，所以調小也是省資源的一格。",
        ),
        /**
         * 一道光束從外圈飛進身體要幾秒（**內縮速度**的倒數）。調小＝吸得更急
         * 更有磁鐵感；調大＝緩慢匯聚。光束到達就從身體那一點被吸收並在外圈
         * 重生，整段吟唱連續循環。
         */
        convergeSec: z.number().min(0.1).max(2).describe(
          "@zh 一道光束飛進身體要幾秒\n" +
          "@note 內縮速度的倒數：調小＝吸得更急、磁鐵感更強；調大＝緩慢匯聚。光束到達身體那一刻被吸收並在外圈重生，整段吟唱連續循環 —— 所以短吟唱至少看得到一輪，長吟唱是持續的能量流。",
        ),
        /** 一道光束的長度（世界單位）。越長越像「光的線」，越短越像「光點」。 */
        beamLength: z.number().min(0.2).max(4).describe(
          "@zh 一道光束的長度（世界單位）\n" +
          "@note 越長越像「光的線」被抽進身體，越短越像一群光點。光束在接近身體時會自己縮短（被吸收的讀法），這一格是它剛出生時的長度。",
        ),
        /**
         * 亮度倍率（乘在加法混合的 alpha 上）。0 = 看不見（等於關掉但保留循環
         * 成本，⛔ 要關請用 enabled）；夾在轉不透明之前的上限，隊色永遠蓋不住
         * 施法者本體。
         */
        brightness: z.number().min(0).max(2).describe(
          "@zh 集氣亮度倍率\n" +
          "@note 乘在加法混合的透明度上。0＝看不見（循環還在跑 —— 要真正關掉請用總開關）；上限被夾在轉為不透明之前，所以再亮也蓋不住施法者本體，受害者永遠讀得出是誰在施法。與施法光柱視覺打架時，先把這格調亮、光柱那頁調暗。",
        ),
        /** 光束從離身體幾個世界單位外開始飛進來（外圈半徑）。 */
        startRadius: z.number().min(0.5).max(6).describe(
          "@zh 光束從離身體多遠開始（世界單位）\n" +
          "@note 外圈半徑：光束出生的距離。調大＝集氣範圍更壯觀、但單道光束在畫面上的速度感更快；調小＝貼身的緊湊蓄力。⚠️ 它只是演出半徑，跟技能的施法距離、命中範圍完全無關。",
        ),
      })
      .strict()
      .optional(),

    // ── GH#741／#725（lane FEEL 2026-08-27）三格 ─────────────────────────
    // ⚠️ 三格**全部 optional** ⇒ 缺席時的行為與今天逐位元相同（rollback ＝刪掉它）。
    /**
     * 命中煙的**壽命倍率**。1 = 出貨（light 0.3s / heavy·ex 保留 ~0.6s）。
     * ⭐ 它乘在 `IMPACT_TUNING` 的分級上，⛔ 不是一個絕對秒數 ——
     * 那樣就會把三檔壓成一檔（而「輕重分得出來」正是這一批要的東西）。
     * 調大 = 尾巴更久更黏；調小 = 收得更乾淨但打擊感變薄。
     */
    impactSmokeLifeScale: z.number().min(0.1).max(4).optional().describe(
      "@zh 命中煙的尾巴長度倍率\n" +
      "@note 1＝出貨（輕擊 0.3 秒收乾淨、重擊與 EX 保留約 0.6 秒）。⭐ 它是**乘在分級上**的倍率，⛔ 不是一個絕對秒數 —— 寫成絕對值會把三檔壓成一檔，而「輕重分得出來」正是這一批要的東西。調大＝尾巴更久更黏、畫面更髒；調小＝收得乾淨但打擊感變薄。",
    ),
    /** EX 施放時的**畫面壓暗／去飽和**（GH#725 舊 #42）。⛔ 缺席 = 完全不畫。 */
    /**
     * ⭐⭐ GH#725 AC⑤ —— 命中／死亡的**體素碎塊**層。
     *
     * ⚠️ ⭐ 它與火花是兩件事：火花是**光**（additive、亮、瞬間），
     * 碎塊是**物質**（standard blend、被重力拉下去、看得出是方的）。
     * ⛔ 把它調成 additive 就只是多一層火花 —— 那是這一層存在的反面。
     *
     * ⭐ 顆數**跟著打擊重量走**（乘上該 tier 的火花數比例）——
     * ⛔ 每一擊都噴同樣多會讓「這一下重不重」整個訊息消失。
     *
     * `enabled: false` ＝ 整層不生（＝這張票之前的行為，⭐ 一鍵 rollback）。
     */
    impactDebris: z
      .object({
        enabled: z.boolean().describe(
          "@zh 命中/死亡噴體素碎塊\n" +
          "@note ⭐ **這是 GH#725 AC⑤ 的 rollback 開關**。開著＝打中的時候噴出會被重力拉下去的方塊。⚠️ ⭐ 它與火花是**兩件事**：火花是**光**（additive、亮、瞬間），碎塊是**物質**（standard blend、落地、看得出是方的）—— ⛔ 把它調成 additive 就只是多一層火花，那是這一層存在的反面。⭐ 顆數**跟著打擊重量走**（乘上該 tier 的火花數比例）⛔ 不是每一擊都噴同樣多。",
        ),
        /** 幾顆（heavy 級的基準；其他 tier 按火花數比例縮放）。 */
        count: z.number().int().min(1).max(64).describe(
          "@zh 碎塊顆數（重擊基準）\n" +
          "@note heavy 級噴幾顆；其他 tier 按火花數比例縮放。⚠️ 調太大在多人混戰時是**粒子預算**的問題，⛔ 不只是視覺。",
        ),
        /** 活多久（秒）。⚠️ 太長會讓地上一直有東西在跳。 */
        lifeSec: z.number().min(0.1).max(4).describe(
          "@zh 碎塊活多久（秒）\n" +
          "@note ⚠️ 太長會讓地上一直有東西在跳，而那會吃掉「剛剛發生了什麼」這個訊息。",
        ),
        /** 方塊多大（世界單位）。 */
        size: z.number().min(0.02).max(1).describe(
          "@zh 碎塊多大（世界單位）\n" +
          "@note ⭐ 要看得出**是方的**，這一層才成立 —— 太小就跟火花分不出來，等於白噴一層粒子；太大會蓋住命中點本身，讓玩家看不到自己打中了哪裡。",
        ),
        /** 噴多快。 */
        speed: z.number().min(0.5).max(24).describe(
          "@zh 碎塊噴多快\n" +
          "@note 初速。⚠️ 重力固定 -9.8，所以速度同時決定飛多遠與滯空多久。",
        ),
      })
      .strict()
      .optional(),
    exDim: z
      .object({
        /** 總開關。false ＝ 回到「只有推鏡、沒有壓暗」的今天。 */
        enabled: z.boolean().describe(
          "@zh EX 施放時畫面壓暗總開關\n" +
          "@note 關掉＝只剩推鏡、沒有壓暗，逐位元回到這個功能存在之前。⭐ 這是一鍵 rollback 的止血閥：壓暗是純演出層，碰不到任何機制。",
        ),
        /** 最暗那一刻的黑幕不透明度。太高會蓋掉自己的特效。 */
        peakAlpha: z.number().min(0).max(0.8).describe(
          "@zh 最暗那一刻的黑幕不透明度\n" +
          "@note 0＝完全不暗。⚠️ 調太高會把 EX 自己的特效一起蓋掉 —— 這一層的用意是讓 EX 跳出來，⛔ 不是讓畫面變黑。",
        ),
        /** 去飽和強度（0 = 不動色，1 = 全灰）。⭐ 它讓 EX 的顏色跳出來。 */
        saturate: z.number().min(0).max(1).describe(
          "@zh 去飽和強度\n" +
          "@note 0＝不動顏色，1＝全灰。⭐ 這一格才是讓 EX 的顏色「跳出來」的主力：周圍褪色而技能本身不褪。壓暗（上一格）負責亮度，這一格負責彩度，兩者獨立。",
        ),
        /**
         * 整段持續幾毫秒。⭐ 出貨值**逐字等於** `EX_PUNCH_MS` ——
         * ⛔ 兩者不一致時，壓暗會比推鏡早收或晚收，而那看起來像掉幀。
         */
        durationMs: z.number().min(50).max(2000).describe(
          "@zh 壓暗整段持續幾毫秒\n" +
          "@note 出貨值**逐字等於**推鏡的時長（EX_PUNCH_MS）。⚠️ 兩者不一致時，壓暗會比推鏡早收或晚收，而那在畫面上看起來像**掉幀**，⛔ 不像演出。要改就連推鏡一起想。",
        ),
      })
      .strict()
      .optional(),
    /** 冷卻圈的**本地預測**（GH#725 舊 #119）：按下的當幀起轉，⛔ 不等一趟 RTT。 */
    cooldownPredict: z
      .object({
        /** 總開關。false ＝ 回到「等權威快照才起轉」的今天。 */
        enabled: z.boolean().describe(
          "@zh 冷卻圈按下當幀就起轉\n" +
          "@note 關掉＝回到「等一趟 RTT、伺服器回來才起轉」的今天。⭐ 這一格只影響**你自己畫面上的圈**，⛔ 不影響任何實際冷卻 —— 權威快照一到就以權威為準。",
        ),
        /**
         * 預測**最多**先轉幾毫秒；權威快照一到就以權威為準。
         * ⛔⛔ 它只能**提前起轉**，⛔ 永遠不可以**延長**冷卻 ——
         * 那會變成一個玩家看得到、伺服器不知情的作弊面（結構保證在 `ui/cooldownPredict`）。
         */
        graceMs: z.number().min(0).max(5000).describe(
          "@zh 預測最多先轉幾毫秒\n" +
          "@note 超過這個時間權威還沒回來，圈就回到未起轉的樣子（避免斷線時圈一直在轉而技能其實還能按）。⛔⛔ 它只能**提前**起轉，永遠不會**延長**冷卻 —— 那會變成一個玩家看得到、伺服器不知情的作弊面。",
        ),
      })
      .strict()
      .optional(),
    /**
     * ⭐⭐ GH#734 —— **右鍵指令按下去的那一刻的回饋**。
     *
     * owner 2026-07-29 逐字：「點右鍵攻擊會讓目標物**閃紅圈圈** 並且玩家角色
     * 發出**攻擊語音**；取消...等其他動作也是播對應音效」
     *
     * ⚠️ ⭐ 在此之前 `GameApp.ts` 的 `onOrder` **三個呼叫點都是直通**
     * `setOrder(order)` —— ⛔ 零個回饋呼叫，而 `resolveTargetMarker()` 那一套環
     * 早就存在、⛔ 只有**手把**那條路在用。
     *
     * ⭐ **兩個軸分開**（⛔ 不是一個總開關）：有人只想要環、有人只想要聲。
     * ⭐ 出貨三格全 true —— ⛔ 一個預設關著的回饋等於沒做（第〇·六守則：
     * 「優先權大的更新後都是預設啟動」）。
     */
    orderFeedback: z
      .object({
        /** 總開關。false ＝ 逐位元回到「按下去什麼都不發生」的今天。 */
        enabled: z.boolean().describe(
          "@zh 🖱 右鍵指令按下當下的回饋（總開關）\n" +
          "@note owner 2026-07-29：「點右鍵攻擊會讓目標物**閃紅圈圈** 並且玩家角色發出**攻擊語音**；取消...等其他動作也是播對應音效」。⭐ 關掉＝逐位元回到「按下去什麼都不發生」的今天。⚠️ 在這一格出現以前，`GameApp` 的三個 `onOrder` 呼叫點**都是直通** `setOrder` —— 零個回饋呼叫，而目標環那一套 decal 早就存在、只有**手把**那條路在用。⭐ 本地零延遲播放（你自己的指令，⛔ 不等伺服器）。",
        ),
        /** 目標腳下閃一下（複用 `targetMarker` 那套 decal，⛔ 不新做一層）。 */
        ring: z.boolean().describe(
          "@zh 🖱 指令當下目標腳下閃一圈\n" +
          "@note 複用瞄準/範圍那一套地面環（⛔ 不是新做一層）。⭐ 只有**指定目標**的指令會閃 —— 右鍵地面／A 鍵點地不閃（那時候還沒有目標）。關掉＝只留聲音。",
        ),
        /** 指令當下的語音/音效（選既有池，⛔ 不錄新的）。 */
        voice: z.boolean().describe(
          "@zh 🖱 指令當下的音效（攻擊/移動/停止/取消）\n" +
          "@note 選**既有**語音池，⛔ 不錄新的。⭐ 右鍵地面**也會出聲** —— ⚠️ 沉默與「按鍵沒吃到」在玩家手上長得一模一樣。⭐ A 鍵點地播的是**移動**音，⛔ 不是攻擊音：那時候他還沒有指定目標，攻擊音會讓他以為鎖到人了。關掉＝只留紅圈。",
        ),
      })
      .strict()
      .optional(),
  })
  .strict();
/** 爽度特效（`content/config/feel-fx.json`，GH#494）。 */
export type ConfigFeelFxDoc = z.infer<typeof zConfigFeelFxDoc>;

/**
 * 出貨預設 —— `content/config/feel-fx.json` 讀不到（舊部署 / 內容掛掉 / 被存壞的
 * override）時，`readFeelFx` 回退到的就是這一份。
 *
 * ⚠️ 每一格都要和 `content/config/feel-fx.json` 一字不差 ——
 * `apps/client/src/vfx/feelFx.test.ts` 的 drift 斷言在守。
 *
 * ⭐ 為什麼保險絲是**開著**的（`enabled: true`）：這一層碰不到任何一塊錢，
 * 它只決定「看不看得到」。讀不到內容就靜音掉 owner 明說要的爽度，是把一個
 * 內容故障翻譯成一個設計倒退（`arenaFire` 那格是相反的方向，因為 owner 明說
 * 要「全部場地都去掉」—— 保險絲要站在 owner 說過的那一邊）。
 */
export const DEFAULT_FEEL_FX: ConfigFeelFxDoc = {
  id: "feel-fx",
  schema: "config.feel-fx@1",
  goldPickup: {
    enabled: true,
    hoverSeconds: 1,
    flightSeconds: 0.42,
    easePower: 3,
    arcHeight: 1.9,
    maxConcurrent: 32,
    sfxThrottleMs: 55,
    sfxVolume: 0.32,
  },
  comboPitch: {
    enabled: true,
    semitonesPerStep: 1,
    maxSteps: 12,
    resetAfterSeconds: 5,
  },
  castMotes: {
    lifetimeMinSec: 0.175,
    lifetimeMaxSec: 0.35,
    gravityY: 3,
    drag: 0.7,
  },
  // ⭐ GH#788 蓄力集氣。minCastSec 0.3 = owner 2026-08-27 逐字「超過0.3秒以上」；
  // 其餘五格是實作方挑的預設（每一格都是後台可調的回頭路）。
  castCharge: {
    enabled: true,
    minCastSec: 0.3,
    beamCount: 10,
    convergeSec: 0.45,
    beamLength: 1,
    brightness: 1,
    startRadius: 2.4,
  },
  // ⭐ GH#734 —— 三格全 true。⛔ 一個預設關著的回饋等於沒做
  //   （第〇·六守則：「優先權大的更新後都是預設啟動」）。
  orderFeedback: { enabled: true, ring: true, voice: true },
};
