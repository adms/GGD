# GGD 前門 —— 2026-08-28 從 Caddy 自己的 autosave.json 重建。
#
# ⚠️ 這個檔在 2026-08-28 之前**從來沒有進過 git**，而它是 ggd.adms.ai 的 TLS 前門。
# 它在某個時間點從磁碟上消失了（很可能是一次 git 操作），而**跑著的容器沒有受影響**
# —— bind mount 一旦建立，來源檔被刪也照樣服務。於是它安靜地壞了幾週，
# 直到 2026-08-28 的 containerd 搬遷第一次重啟容器才爆出來（Exited 127）。
# ⇒ 這一版**必須進版控**。
#
# 內容逐項對照 /data/docker/volumes/ggd_caddy-config/_data/caddy/autosave.json：
#   listen :443 · h1+h2 · host ggd.adms.ai · reverse_proxy edge:8080 · 自動 TLS
# ⭐ 主機名可切換,⛔ 而預設**就是正式站現在的值** —— 正式機吃這個檔,
#   ⛔ 一個會改變它行為的預設值是不可接受的。
#
# ⚠️⚠️ 為什麼要可切換（2026-08-29 踩到的）:
#   Caddy 一啟動就會為**它列出的每一個主機名**去要 Let's Encrypt 憑證，走 HTTP-01
#   ⇒ 那個名字必須**已經指向這台**。
#   ⛔ 在 mini 上掛 `ggd.adms.ai`（它還指著正式站）⇒ 挑戰打到正式站 ⇒ 失敗重試
#     ⇒ ⚠️ **Let's Encrypt 的失敗有速率限制,撞到要等一週**。
#   ⇒ 搬遷期間 mini 用 GGD_SITE_HOST=mini.adms.ai,切換那一刻才改成正式名字。
# ⭐ 可以列**多個**主機名（空白分隔）—— 切換期間同時服務臨時名字與正式名字,
#   ⇒ 切 DNS 的那一刻憑證**已經在手上**,⛔ 不是切完才開始要（那會有一段空窗）。
# ⚠️ 但 Caddy 會為**列出的每一個**名字去要憑證 ⇒ 每一個都必須**已經指向這台**,
#   ⛔ 否則挑戰失敗、累積速率限制（Let's Encrypt:每小時 5 次/每個名字）。
#   ⇒ 所以是 `GGD_SITE_HOSTS`（複數）,由部署腳本按當下的 DNS 狀態決定要列哪些。
{$GGD_SITE_HOSTS:{$GGD_SITE_HOST:ggd.adms.ai}} {
	reverse_proxy edge:8080
}
