// Adapted from batch2-37 at cd5e98945; source SHA-256 fd82af476e68a6d04a5d0c91496400db030927472ae4b0bcc4989d9975a79d01.
// Authoring conveniences only. All cards/effects resolve through the shipped schema.
export const clone=x=>structuredClone(x);
export const tier=(damageTier='小')=>({damageTier});
export const dmg=(t='小',extra={})=>({kind:'damage',damageType:'magic',amount:tier(t),...extra});
export const heal=(n=120,applyTo='target',extra={})=>({kind:'heal',amount:{flat:n},applyTo,...extra});
export const mana=(p=.08,applyTo='self',extra={})=>({kind:'restore',manaPct:p,applyTo,...extra});
export const shield=(n=160,duration=3,extra={})=>({kind:'shield',amount:{flat:n},duration,absorbs:'all',stackKey:'b2-protection',onExisting:'keepLarger',...extra});
export const status=(id,flags={},duration=3,applyTo='target')=>({kind:'applyStatus',statusId:id,duration,applyTo,...flags});
export const has=(statusId,subject='target')=>({kind:'status',statusId,subject});
export const recent=(slot,withinSec=4)=>({kind:'recentCast',subject:'self',slot,withinSec});
export const low=(value=.5,subject='self')=>({kind:'stat',subject,stat:'hp',mode:'percent',op:'<',value});
export const distance=(op,value)=>({kind:'distance',op,value});
export const when=(effect,condition)=>({...effect,condition});
export const buff=(stat,value,duration=3,extra={})=>({kind:'applyBuff',applyTo:'self',duration,modifiers:[{stat,op:'flat',value}],...extra});
export const consume=(statusId,effects,subject='target',extra={})=>({kind:'consumeStatus',shape:'single',statusId,count:'all',subject,onConsumed:effects,...extra});
export const cd=(slot='Q',amount=1,extra={})=>({kind:'modifyCooldown',shape:'single',who:'self',slot,mode:'reduceFlat',amount,...extra});
export const cleanse=(extra={})=>({kind:'dispel',shape:'single',polarity:'debuff',pools:{status:true,dot:true,buffs:true},count:3,order:'newest',...extra});
export const push=(distance=3,from='caster')=>({kind:'knockback',distance,speed:12,from,subtractGap:false,uncontrollable:false});
export const blink=(extra={})=>({kind:'blink',shape:'single',to:'point',applyTo:'self',...extra});
export const dot=(extra={})=>({kind:'dot',amountPerTick:tier('極小'),damageType:'magic',intervalSec:1,durationSec:3,stacking:'refresh',onCasterDeath:'stop',...extra});
export const delayed=(effects,delaySec=.6,extra={})=>({kind:'delayed',shape:'single',delaySec,effects,stopOnCasterDeath:true,...extra});
export const aoe=(effects,radius=3,extra={})=>({kind:'damageArea',radius,includeOrigin:true,amount:tier('極小'),damageType:'magic',maxTargets:6,...(effects.length?{onHitTargets:effects,onHitTargetsMode:'perTarget'}:{}),...extra});
export const summon=(extra={})=>({kind:'summon',body:'self',count:1,durationSec:4,damageMult:.3,hpMult:.3,formation:'ring',spread:2,maxAlive:2,onOwnerDeath:'despawn',...extra});
export const evasion=(extra={})=>({kind:'evasion',chance:1,durationSec:1.8,applyTo:'self',dodgesAbilities:false,...extra});
export const hook=(on,effects,extra={})=>({on,effects,internalCooldown:2,target:'self',...extra});
// Main's typed template slots supply their own discriminant on expansion.
export const card=(ref,params={})=>({ref,params:Object.fromEntries(Object.entries(params).map(([k,v])=>[k,['status','dot','onHitVfx'].includes(k)&&v?.kind?Object.fromEntries(Object.entries(v).filter(([p])=>p!=='kind')):v]))});
export const seq=(effects,castType='targeted',side='enemies')=>card('tpl-effect-sequence',{effects,castType,side,castTimeSec:.1,radius:3});
export const move=(text,cards,vfx='fx.prim.arcane.pulse-sm',extra={})=>({text,cards:Array.isArray(cards)?cards:[cards],vfx,...extra});
export const passive=(text,hooks)=>move(text,card('tpl-event-passive',{hooks}),null);
export const strike=(text,extra={},tail=[])=>move(text,[card('tpl-single-strike',{damage:tier(),damageType:'physical',castTimeSec:.1,...extra}),...(tail.length?[seq(tail)]:[])],'fx.prim.arcane.slash');
export const self=(text,effects,vfx)=>move(text,seq(effects,'self'),vfx);
export const ally=(text,effects,vfx='fx.prim.wind.pulse-sm')=>move(text,seq(effects,'targeted','allies'),vfx);
export const ground=(text,effects,vfx='fx.prim.arcane.nova')=>move(text,seq(effects,'ground'),vfx);
export const healing=(text,target='ally',tail=[])=>move(text,[card('tpl-heal',{target,amount:{flat:120},castTimeSec:.1}),...(tail.length?[seq(tail,target==='self'?'self':'targeted',target==='self'?'enemies':'allies')]:[])],'fx.prim.wind.pulse-sm');
export const nova=(text,tail=[])=>move(text,[card('tpl-ground-nova',{damage:tier('中'),damageType:'magic',radius:200,castTimeSec:.1}),...(tail.length?[seq(tail,'ground')]:[])],'fx.prim.arcane.nova');
export const line=(text,extra={})=>move(text,card('tpl-line-strike',{damage:tier('中'),damageType:'magic',length:9,width:1.5,aim:'target',castTimeSec:.1,...extra}),'fx.prim.arcane.bolt');
export const mobility=(text,tail=[])=>move(text,[card('tpl-blink',{castTimeSec:.1}),...(tail.length?[seq(tail,'ground')]:[])],'fx.prim.arcane.dash');
export const form=(text,to='alternate',extra={})=>move(text,card('tpl-transform',{to,durationSec:4,castTimeSec:.1,...extra}));
export const edge=(source,target,actor,field,remove,extra={})=>({source,target,metric:{actor,field},expect:'increase',waitSec:.8,observeSec:1.2,remove,...extra});
export const kit=(signature,closest,difference,moves,combos,limitations=[])=>({signature,closest,difference,moves,combos,limitations});
