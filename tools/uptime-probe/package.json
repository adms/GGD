{
  "name": "@ggd/uptime-probe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "GH#124 — off-box uptime probe for ggd.adms.ai that actually notifies a human",
  "bin": {
    "uptime-probe": "./src/cli.ts"
  },
  "scripts": {
    "probe": "tsx src/cli.ts",
    "probe:dry": "tsx src/cli.ts --dry-run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
