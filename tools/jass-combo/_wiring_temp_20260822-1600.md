# #541 —— 需要主 session 接線的三處（⛔ lane 的柵欄外）

`content/config/combo-strikes.json` 已經產出，schema tag 是 `config.combo-strikes@1`。
⚠️ **`zConfigDoc` 是 `z.discriminatedUnion`** —— 一個沒註冊的 tag 會讓 `ContentLoader`
**整份**內容驗證失敗（＝ 2026-08-02 那次「網站開得起來但只剩 2 隻骨架」的形狀）。
⇒ 下面三步在同一個 commit 裡做完，⛔ 不要只做前兩步。

## ① 新檔 `packages/shared/src/content/schema/comboStrikesDoc.ts`

（草稿在 `tools/jass-combo/_comboStrikesDoc_draft.ts`，可以直接 `cp` 過去。）

## ② `packages/shared/src/content/schema/config.ts` 兩行

```ts
import { zConfigComboStrikesDoc } from "./comboStrikesDoc";   // ← 與第 187 行那一族並列
...
export const zConfigDoc = z.discriminatedUnion("schema", [
  ...
  zConfigComboStrikesDoc,        // ← 與第 8333 行那一族並列
]);
```

`packages/shared/src/content/schema/index.ts` 再加一行 `export * from "./comboStrikesDoc";`。

## ③ admin 兩行（`configDocCoverage` 那條閘要的）

```ts
// apps/admin/src/store.ts  —— 頁面 union + 清單（對照 "practice" 那兩處：279 / 705 行）
| "combo-strikes"
"combo-strikes",

// apps/admin/src/ui/App.tsx —— 導覽列（對照 354 行）
{ page: "combo-strikes", label: "連段節奏", emoji: "🥁", section: SEC_SYS },
```

⚠️ **這份表是唯讀資料，不是可調參數** —— 後台那一頁只要看得到就好。
⭐ 真正該可調的是「動畫節奏要不要照原作」那個決策，而它屬於技能側，⛔ 不在這張票裡。

## ④ 產物是產生的，⛔ 不可手改

```bash
python3 tools/jass-combo/extract.py           # 重生成
python3 tools/jass-combo/extract.py --check   # 唯讀，過期回非零
```
守衛 `packages/shared/src/ops/jassComboTable.test.ts` 真的把它跑起來。
⚠️ 記得把 `jass-combo:check` 加進 `pnpm skills:check` 的聚合，
否則 `skillsSyncCoversGenerators.test.ts` 會紅（package.json 裡每一支 `*:check`
都要嘛在聚合裡、要嘛在豁免表裡帶一個能被反駁的理由）。
