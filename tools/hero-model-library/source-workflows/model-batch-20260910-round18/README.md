# 本批來源轉換程式

此處保留 Ash 初版轉換、莉娜跨遊戲重綁、Fate 貼圖恢復及 Malzahar PCM16 恢復時實際使用的程式。各子目錄的 `files.sha256.json` 對應本機凍結交付；包含當時的絕對輸入路徑，重跑前應讀取並指定新的輸出版本，不能覆寫原包或已凍結候選。

原始模型、貼圖、音訊、中間產物與大量逐幀分析資料仍在各 `download-sources.json` 來源的本機 intake，備份位置另見 `public-source-files.json`。這些程式與結構檢查不代表模型已在 GGD 後台驗收。
