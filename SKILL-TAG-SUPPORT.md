# GGD 技能標籤 — 引擎支援狀況

> 給技能／特效編輯器實作參考。**這一份是自足的**：不需要讀遊戲原始碼，也不需要問人。

| 項目 | 值 |
|---|---|
| **產出日期** | **2026-08-09（rev C）** |
| 對應契約 | `ggd-runtime-capabilities@1` |
| **契約指紋** | **`ef984bcc`** — 指紋變了，這份文件就過期了（rev B 同指紋，見下） |
| 量測日期 · 引擎版本 | 2026-08-09 · HEAD `4e3ebc88` **＋ 尚未 commit 的工作區**（GH#296 小怪死亡順序 · GH#298 · 格擋四授權面 · 三軸守衛）|
| 取代 | 同日 **rev B**（量在 `8c54fdb0` ＋工作區，四態 64 / 52 / 23 / 0）· 再前面是同日 **rev A**（`d199d3eb`，60 / 49 / 28 / 2）· 再前面是 2026-08-05（三態 52 / 46 / 41）|
| 機器可讀版 | `skill-tag-manifest.json`（同目錄）—— **下拉選單請由那個檔驅動**，不要把清單硬寫進程式碼 |

> ⚠️ **rev C 是為了收掉 owner #299 剩下的四條裁決（衝擊波 / 暴擊 / 反彈 / 格擋）而重出的。**
> rev B 把那四條標成「待實作 / 待確認」，rev C 逐條量過之後 **四條全部翻盤**：
> 一條是 rev B 自己**誤判**（衝擊波 —— 只讀了模板註解，沒有讀投射物文件），
> 兩條**已經做完**（暴擊相乘 GH#302、格擋授權格放寬），一條**量過而且三個問題全部通過**（反彈）。
> 另外反映三筆事實：小怪死亡（修了一半，成因換了）· 疊層三條軸（GH#304）· 層數已經上線路。
>
> ⛔ **rev C 一筆 state 都沒有換**（64 / 52 / 23 / 0 與 rev B 逐格相同）——
> 變的是**那七格裡寫什麼**。所以如果你已經接了 rev B，**下拉選單的四態不用動，但那七格的文案一定要換**：
> 其中四格 rev B 叫你**繞路**（待實作／待確認／「HUD 會顯示層數是謊話」），而今天繞路是白繞。
> ⛔ 契約指紋沒有變（`ef984bcc`）—— 那一批**沒有新增** effect kind 或 hook event，
> 改的是既有機制的**行為**與**授權格**，而指紋只推導名詞。⚠️ 這正是本文件開頭那條教訓的實例：
> **指紋一樣不等於裁決一樣**，指紋只能告訴你「詞彙表沒變」。

## 怎麼重新產生這份文件

三份輸入，缺一不可：

1. **標籤與順序的權威來源** —— owner 的原始標籤清單（8 組、139 個）。分組與順序都以它為準，
   ⛔ 不要重排、不要合併、不要自己補標籤。
2. **引擎能力的唯一真相** —— `docs/editor-contract/ggd-runtime-capabilities.json`。
   它從出貨的註冊表**推導**（不是手寫的），欄位有 `fingerprint` · `effectKinds`(**35**) · `hookEvents`(**19**) ·
   `conditionLeafKinds`(5) · `hookFields`(13) · `templateFamilies`(17) · `planned`(26) · `unsupported`(**6**) ·
   **`knownBroken`(1)**。⚠️ 檔頭那一格指紋與這份契約的 `fingerprint` 不同 = 引擎已經動過，本文件的裁決要整批重跑。
   （rev A 時是 34 / 15 / 7 / 3 —— 這一批加了 `blink`、四個觸發事件，並把 `condition.stack-count@1`
   從 unsupported 移出、把 `onRevive` 與 `onDeath` 的英雄那一半移出 knownBroken。）
3. **逐標籤的裁決** —— 在該 HEAD 上逐檔讀程式 ＋ 執行探針 ＋ 真的跑被宣稱的守衛 ＋ 對出貨內容做全樹解析。
   ⛔ 不採信程式註解、不採信任何前一版清單的結論（這一輪就抓到對外契約有兩句過期的 caveat，方向是**低報**自己的能力）。

   ⚠️ **rev C 加一條，因為 rev B 在這一步上真的踩到了**：**「讀程式」不等於「讀對了那一份檔」。**
   rev B 的【衝擊波】判成「引擎沒做」，唯一根據是**模板的一句註解**（「per-step collect 收合成一發」）；
   而那支技能會不會分段推進，答案根本不在技能那一側 —— 在它引用的**投射物文件**的 `pierce` 上，
   而出貨的 9 份全是 `true`。⇒ **一個機制的答案常常住在被引用的那份資料裡，不在引用它的程式裡。**
   對每一個「引擎做不到」的裁決，多問一句：**這件事有沒有可能是一格資料，而我只讀了程式？**

`skill-tag-manifest.json` 與本文件是**同一份資料的兩個視圖**，改一個就要改另一個。

## 這份在回答什麼

設計端使用一套中文標籤描述技能（【暈眩】【破盾】【擊殺時】…）。
這份表回答：**每一個標籤，編輯器今天能不能讓作者選？選了會不會真的生效？**

⚠️ 這不是一份「願望清單」。表上有五分之一的標籤引擎今天做不到，
而它們在資料結構上**看起來完全正常** —— 存得起來、卡片上顯示得出來、只是遊戲裡什麼都不會發生。
編輯器的狀態閘就是為了擋住這件事。

**假的 ❌ 只是害你白白繞路；假的 ✅ 會害你照著設計一張卡，而它在遊戲裡安靜地什麼都不做**
—— 沒有錯誤訊息、沒有紅字，畫面上跟正常一模一樣。所以這份文件的偏誤方向是固定的：
寧可把一格標成「不可選取」，也不把沒把握的東西標成「可用」。

## 四個狀態 = 四種下拉行為

| state | 編輯器該怎麼做 | 數量 |
|---|---|---:|
| `allowed` | 正常選項。⚠️ 若該筆帶 `authorNote`，把它收在展開區——**可以收起來，不要整個丟掉** | **64** |
| `allowed_with_warning` | 可選，但 **`authorWarning` 必須常駐顯示在作者眼前**（不是 tooltip） | **52** |
| **`broken`** | ⛔ **可以顯示、但要像 `blocked` 一樣不可存。** 文案不一樣：`blocked` 是「引擎沒有」，`broken` 是「**引擎有，但今天是壞的，issue 在追**」。帶 `issue` 與 `workaround` 兩格 | **0** |
| `blocked` | ⛔ **不可選取**。可顯示成灰階「開發中」，但**不可以存得起來** | **23** |

⚠️ **`broken` 這一格本版是 0 筆，但列舉值要留著。** rev A 的兩筆（【死亡時】【復活時】）
都在 2026-08-09 修掉了。⛔ 不要因為今天是 0 就把這個分支從 switch 裡拿掉 ——
下一次出現「引擎有、今天壞掉」的機制時它會直接被用上，而那正是最需要擋住作者的一種。

⛔ `blocked` 與 `broken` 一定要真的擋。這個專案已經發生過「選項在下拉裡、引擎零實作」，
結果是作者做了一整批卡片，上線後才發現完全沒有效果。

⚠️ **`broken` 比 `blocked` 更危險**，因為它**會通過驗證**：枚舉裡有、schema 收得下、後台選得到。
它唯一的破綻是遊戲裡什麼都不發生，而那一刻沒有人看得出來。

## 這一版（rev C）改了什麼

### 計數

**計算基礎**：分母是 owner 原始清單的 **139 個標籤**（8 組）。下面每一格都是逐標籤數出來的，不是估的；
四格相加 = 139。**新增的 14 個標籤不進這個分母**，另列一節。

| 狀態 | 2026-08-05 | rev A | rev B | **rev C** | rev C − rev B |
|---|---:|---:|---:|---:|---:|
| `allowed` 可直接使用 | 52 | 60 | 64 | **64** | **±0** |
| `allowed_with_warning` 可用但要警告 | 46 | 49 | 52 | **52** | **±0** |
| `blocked` 不可選取 | 41 | 28 | 23 | **23** | **±0** |
| `broken` 已知壞掉 | — | 2 | 0 | **0** | **±0** |
| 合計 | 139 | 139 | 139 | **139** | |

算式（rev B → rev C）：

```
allowed  64 ± 0   = 64
warning  52 ± 0   = 52    ← 七格改寫全部落在這一格內部，沒有跨格
blocked  23 ± 0   = 23    ← 【格擋時】仍然在這裡（格擋不是一個時機，實測零個派發點）
broken    0 ± 0   =  0
                          合計 = 139
```

⭐ **「一筆都沒換」本身就是這一版要講的話。** rev C 動的七格**全部原本就是 `allowed_with_warning`**，
翻盤的是它們的**理由**：從「引擎沒做／沒查」變成「引擎做了／查過了」。⛔ 所以下拉選單的**狀態**不用改，
但**警告文字一定要換** —— rev B 那四格的警告是在叫作者繞路，而繞路今天是白繞（假的 ❌，見本文件開頭）。

### rev C 的異動總帳（7 筆改寫，0 筆換 state，逐筆可對）

| # | 標籤 | rev B 說 | **rev C 量到** | 這一格為什麼會錯 |
|---:|---|---|---|---|
| 1 | 【衝擊波】 | 「引擎不是分段推進」→ **待實作** | ⭐ **是分段推進**（owner 對）。9 份 `imported.wave*` 投射物**全部 `pierce: true`**，波沿路每個敵人各打一次；**76 份出貨技能**在用 | ⛔ **只讀了模板的註解，沒有讀投射物文件。** 那句註解描述的是技能文件，不是投射物。第三守則的教科書案例 |
| 2 | 【暴擊】 | 「合成一個數字擲一次骰」→ **待確認** | ⭐ **已落地**（GH#302）：每條來源各自擲、倍率**相乘**；`critStrike` 授予**四個授權面**；三格後台參數 | 量在 rev B 之前的 HEAD；那一批當天稍晚才進 `4e3ebc88` |
| 3 | 【反彈】 | 「沒有逐支對照過」→ **待確認** | ⭐ **三個問題全部通過**：事件真的發、效果真的跑、**對象就是原攻擊者**且觸發傷害傳得到 | rev B 沒有實際去量，只承接了更舊一版的推測 |
| 4 | 【格擋】 | 「主動技／增益卡／限時授予三格缺」→ **待實作** | ⭐ **授權格已放寬**：一份定義展開**四個**授權面；限時授予同時就是主動技那一格的答案 | 這一批做完了，rev B 出版時還在做 |
| 5 | 【死亡時】 | 「小怪的屍體太早被銷毀」 | ⚠️ **那個成因修好了，但出貨路徑仍然關著，理由換成另一個**：小怪**沒有屬性表**，身上掛不住任何卡片 | 修對了一半；⛔ 別把「已修」讀成「可以用」 |
| 6 | 【層數累積】 | 「層數怎麼送到客戶端還沒有答案 → HUD 文案都是謊話」 | ⭐ **層數已經上線路**（每座位一組「(計數器 id, 層數)」，HUD 讀的就是它）；⭐ 疊層**三條軸**全部落地（GH#304） | 那句話從 `4e3ebc88` 起自己才是謊話 |
| 7 | 【反彈成功時】 | 只寫了改名與判準 | ⭐ 補上量測結果（同 #3） | — |

### rev B 的異動總帳（8 筆換 state，逐筆可對，保留供對照）

| # | 標籤 | rev A | **rev B** | 為什麼 |
|---:|---|---|---|---|
| 1 | 【瞬移】 | warning | **allowed** | ⭐ owner #299-4：**真瞬移**落地（獨立的 `blink`，不再是極短平移）。GH#301-2 |
| 2 | 【死亡時】 | **broken** | warning | ⭐ **英雄那一半修好了**（GH#293）；小怪那一半仍發不出去（GH#296）→ 降成警告不是解除 |
| 3 | 【復活時】 | **broken** | **allowed** | ⭐ 復活圈那條實戰路徑修好了（GH#294，取錯一格） |
| 4 | 【護盾產生時】 | blocked | **allowed** | ⭐ GH#300：發射點接上（`addShield` 是唯一的護盾生成點） |
| 5 | 【護盾破碎時】 | blocked | warning | ⭐ GH#300：接上了，但**到期消失**與**被【破盾】拆掉**都不算破碎 |
| 6 | 【隊友陣亡時】 | blocked | **allowed** | ⭐ GH#300：死亡事件多一種收件人範圍（活著的隊友） |
| 7 | 【狀態被套用的當下】 | blocked | warning | ⭐ GH#300：接上了，但**續期不重發**、被免控拒絕的不算 |
| 8 | 【驅散免疫】 | blocked | warning | ⭐ GH#295：三個 authoring 入口多了「這一份可不可以被拔」，明寫 `false` 就拔不走。rev A 說「授權格還是沒有」，那句是假的 |

算式（rev A → rev B，兩個方向都對得起來）：

```
allowed  60 + 1（瞬移）+ 1（復活時，從 broken）+ 2（護盾產生時 / 隊友陣亡時，從 blocked）   = 64
warning  49 − 1（瞬移升走）+ 1（死亡時，從 broken）
            + 3（護盾破碎時 / 狀態被套用的當下 / 驅散免疫，三筆都從 blocked）              = 52
blocked  28 − 5（四個新觸發事件 + 驅散免疫，全部離開）                                     = 23
broken    2 − 2（死亡時降成 warning、復活時升成 allowed）                                  =  0
                                                                                 合計 = 139
```

### ⚠️ rev B 另有 **13 筆沒有換 state、但內容整段改寫** —— 照 rev A 抄會做出錯的卡

| 標籤 | 改了什麼 | 來源 |
|---|---|---|
| 【擊飛】 | ⭐ **落點可以指定了**（四檔 short/default/long/toEdge）。rev A 寫「作者無法精確指定落地位置」，那句從這一批起是假的 | owner #299-3 · GH#301-1 |
| 【混亂】 | ⭐ **語意整個換掉**：從「自動選目標會挑到隊友」變成「**完全無法指定目標 + 亂走路**」。⛔ 照 rev A 設計的卡在新語意下不成立 | owner #299-9 · GH#301-3 |
| 【虛弱】 | ⭐ **有專屬機制了**（攻速減半 + 造成傷害減半，倍率在後台）。⚠️ 但今天**沒有任何狀態文件帶那個標籤**，所以機制是惰性的 | owner #299-7 · GH#301-4 |
| 【層數累積】 | ⭐ 多了**狀態層數**（`applyStatus.stacks`）而且**條件問得到**；⭐ 「每層 +X%（乘算）」也修好了。⚠️ **rev C 又整段改寫了一次**：三條軸全部落地、層數已經上線路 —— 見上面 rev C 總帳第 6 列 | owner #299-8 · GH#301-5 · GH#286 · **GH#304** |
| 【破甲】【破防】【破魔】 | ⭐ 屬性對小兵仍然無效，**但狀態掛得上、條件查得到** —— 追加效果對小兵發得動了 | owner #299-10 · GH#301-6 |
| 【身上有某狀態時】【標記】【標記引爆】【裝備了某類道具時】 | ⭐⭐ **條件不再只能掛在觸發時機上** —— 每一個效果自己有一格 `condition`。rev A 那句「條件只能掛在 hook 上」是假的 | owner 2026-08-09 裁決 |
| 【淨化】【驅散】 | ⭐ **增益池不再是死開關**：三個 authoring 入口多了「這一份可不可以被拔」 | GH#295 |
| 【免疫】【免死】【火圈點燃】 | ⭐ **火圈燒傷不再繞過防禦鏈**：無敵擋得住了；免死是一格後台開關（⛔ 出貨預設關） | GH#287 |

### ✅ rev B 標成「待實作 / 待確認」的那 4 筆 —— rev C 全部收掉了

⭐ **四筆的 `issue: "GH#299"` 已經從 manifest 拿掉。** 逐筆的量測結果：

| 標籤 | owner 說（#299） | **rev C 量到的引擎事實** | 怎麼量的 |
|---|---|---|---|
| 【衝擊波】 | **是分段推進傷害沒錯** | ⭐ **owner 是對的，rev B 誤判。** 模板產出的投射物是 `imported.wave`，而 **9 份 `imported.wave*` 文件每一份都 `pierce: true`** —— 波不會停在第一個人身上，沿路每個敵人各跑一次命中效果 | 掃 18 份出貨投射物文件（9 pierce / 9 不 pierce）＋讀投射物系統的 `!pierce → 銷毀` 分支；**76 份技能文件**在用這一族 |
| 【暴擊】 | 兩軸、**像多個獨立技能各自判斷** | ⭐ **已落地（GH#302）**：每條來源各自擲骰、倍率**相乘**；`critStrike` 授予四個授權面；`stackMode` / 總倍率上限 / 來源上限**三格後台參數** | 讀出貨的 `content/config/crit.json` ＋ Zod ＋ 後台表單三處對齊 |
| 【反彈】 | **有許多角色靠「反彈成功」發動連續效果** | ⭐ **三個問題全部通過**：① 事件真的發（獨立派發系統）② 掛上去的狀態／標記／傷害真的跑 ③ **對象就是原攻擊者**，觸發的那一發傷害傳得進去 | 跑兩條出貨守衛（`reflectHook` / `reflectSuccessProvenance`，17 條全綠）＋讀派發點 |
| 【格擋】 | 授權格要放寬 | ⭐ **已放寬**：一份定義展開**四個**授權面（道具／技能被動／增益卡／限時授予）；⭐ 限時授予同時就是「主動技怎麼給格擋」的答案 | 跑 `sourceGrants.test.ts`（四個授權面，四個突變都驗過會紅） |

⛔ **但有一格 owner 要的東西仍然沒有，而且它不在上面四條裡**：
**「格擋的那一刻」不是一個時機** —— 擋下來不會觸發任何效果（整支格擋程式**零個** hook 派發點，實測）。
所以【格擋時】那一格仍然是 `blocked`，「格擋成功時反擊」今天寫不出來。
⚠️ 這一格**沒有任何 issue 在追**（GH#301 的標題與它自己的條列不一致 —— 標題寫「第 6 條」但條列沒有列格擋）。

### rev A 的異動總帳（相對 2026-08-05，16 筆，保留供對照）

| # | 標籤 | 舊 | 新 | 為什麼 |
|---:|---|---|---|---|
| 1 | 【死亡時】 | blocked | **broken** | 時機接上了，但出貨路徑上**一次都不會發**（實測）。GH#293 |
| 2 | 【復活時】 | blocked | **broken** | 只有道具復活那條路會發；復活圈（實戰唯一路徑）打空（實測）。GH#294 |
| 3 | 【淨化】 | allowed | allowed_with_warning | 四池只有兩池通：增益池是**死開關**、護盾池在預設極性下整池跳過。GH#295 |
| 4 | 【切換】 | allowed | allowed_with_warning | 變身式切換本身沒問題，但要跟新的【切換型技能】分清楚——後者玩家看不到它開著 |
| 5 | 【免死】 | allowed_with_warning | **allowed** | ⭐ 技能也寫得出來了（具名標記的免死規則），不再只限道具 |
| 6 | 【恐懼】 | blocked | **allowed** | ⭐ 整支機制出貨並已接線（丟指令、逃離、不攻擊） |
| 7 | 【反彈・分型別】 | blocked | **allowed** | ⭐ 走的是「受到傷害時 + 傷害型別過濾」，不是反彈自己的欄位 |
| 8 | 【標記】 | blocked | **allowed** | ⭐ 「貼在敵人身上給別人讀」成立了（狀態條件葉） |
| 9 | 【身上有某狀態時】 | blocked | **allowed** | ⭐ 條件樹讀得到狀態了（問 id 或問類） |
| 10 | 【裝備了某類道具時】 | blocked | **allowed** | ⭐ 裝備條件葉出貨（比對道具標籤或指名一件） |
| 11 | 【守衛塔倒下】 | blocked | **allowed** | ⭐ 世界時機出貨（打倒守衛塔不發【擊殺時】，這是唯一的路） |
| 12 | 【殭屍王出現】 | blocked | **allowed** | ⭐ 世界時機出貨 |
| 13 | 【火圈點燃】 | blocked | **allowed** | ⭐ 世界時機出貨（只在開始那一刻發一次） |
| 14 | 【抵擋致命傷時】 | blocked | **allowed** | ⭐ 走的是具名標記的免死規則，**不是**一個通用時機 |
| 15 | 【延遲落地(通用排程)】 | blocked | allowed_with_warning | ⭐ 排程器出貨且已接線，但**落點一定是隨機的** |
| 16 | 【迴避時】 | blocked | allowed_with_warning | ⭐ 時機出貨，但**分不出真閃避與攻擊者揮空** |

rev A 的算式（2026-08-05 → rev A，保留供對照）：

```
allowed  52 + 1（免死升上來）+ 9（從 blocked 升，扣掉轉成 broken 的 2 筆）− 2（淨化 / 切換降級）= 60
warning  46 − 1（免死升走）+ 2（延遲落地 / 迴避時，兩者裁決是「有限制」）+ 2（淨化 / 切換）      = 49
blocked  41 − 13（翻盤 11 + 轉 broken 2）                                                    = 28
broken    0 + 2（死亡時 / 復活時）                                                            =  2
                                                                                  合計 = 139
```

⚠️ **rev A 的第 1、2 列（死亡時 / 復活時 → broken）在 rev B 已經失效**，見上面 rev B 的總帳。
其餘 14 列到今天仍然成立。

### ⚠️ 另外三筆**沒有換狀態、但引擎名字或事實變了**，照 2026-08-05 版抄會壞

| 標籤 | 變了什麼 |
|---|---|
| **【反彈成功時】** | ⛔ **引擎名字改了**：舊名 `onReflect` 已從枚舉裡拿掉，照 2026-08-05 版抄的文件會在**載入時被拒收**。新名 `onReflectSuccess`。 |
| **【驅散】** | ⛔ **2026-08-05 版的文案是錯的**：它寫「其餘三池正常」。⭐ rev B 起增益池已經有 authoring 欄位（GH#295），護盾池仍要手動放行極性——看那一列。 |
| **【升級時】** | ⚠️ 理由**反方向**過期：它已經從枚舉裡**刪掉**，所以今天根本選不到（不再是「選得到但沒有用」）。已裁決不做。 |

### ⚠️ 綠燈的可信度不是均勻的（量到的）

2026-08-08 與 2026-08-09 兩批新增的機制，**出貨內容採用率全部是 0**：
6 個新世界時機全 0、2 個新條件葉全 0、2 個新傷害過濾器全 0、10 個新效果種類有 9 個是 0
（只有淨化 3 次，全在道具上）、切換 0、跨技能強化 0、恐懼 0；具名標記 1 份。
⭐ **rev B 這一批也一樣全 0**：`blink` 0 · `launchDistance` 四檔 0 · `applyStatus.stacks` 0 ·
`condition.status.minStacks` 0 · 四個新觸發事件各 0 · **帶 `weakness` 標籤的狀態文件 0 份**
（所以【虛弱】這個機制今天一場比賽裡一次都不會發生）· 效果上的 `condition` 0 份。
⭐ **rev C 這一批也一樣**（2026-08-09 重掃 2,042 份出貨文件，逐鍵數的）：
`onReflectSuccess` **0** · `applyStatus.stacks` **0** · `mark.roundDelta` **0** · `condition ⋯ minStacks` **0** ·
`critStrike` 授予 **1**（一件道具）· `block` 授予 **4**（四件道具，另外三個新授權面各 **0**）·
具名標記 **1** 份（十二道試煉）。

⭐ **但 rev C 有一個例外，而且是好消息**：【衝擊波】那條路**採用率不是 0，是 76** ——
出貨技能文件裡有 **76 份**在用會穿透的 `imported.wave*` 投射物。
⛔ 這也正是 rev B 誤判的代價：它把一個**已經有 76 份內容在跑**的機制寫成「引擎沒做」。

對照組——**今天真的在跑的**時機只有七個：
普攻時 68 · 受到傷害時 19 · 擊殺時 13 · 週期 9 · 技能命中時 6 · 施法時 5 · 被暈眩時 2。

⛔ **對你的具體後果：這一版每一個新翻成綠燈的格子，你的第一張卡就是那個機制的第一次上線測試。**
而 rev A 的【死亡時】【復活時】兩個缺陷能帶著全綠的測試出貨，正是因為沒有任何內容跑過它們。
每一筆的採用狀況寫在 manifest 的 `hasShippedExample` 與各列的補充說明裡。

---

# ⛔ 已知壞掉 / 半壞（排在最前面）

⭐ **rev A 列了四筆，rev B 修掉了兩筆半。** 契約 JSON 的 `knownBroken[]` 從 3 筆降到 **1 筆**。

⚠️ **rev C 補一句：那唯一的 1 筆（`hook:onDeath`）在契約 JSON 裡的敘述已經過期了。**
它寫的成因（小怪的屍體太早被銷毀）**已經修掉**；⛔ 但**結論沒有變** —— 小怪那一半仍然不能用，
理由換成了「小怪身上掛不住任何卡片」。⇒ **以本頁 A 段為準，不要照抄契約 JSON 的那段散文。**
（同一份契約檔的散文欄位在 rev A 就撒過謊了 —— 見文末「已知缺口」。**枚舉欄位可靠，散文欄位不可靠。**）

**下面四段：A / C / D 三筆仍然要注意**（全部是 `allowed_with_warning` —— 機制可用，但有一格是
死的、一半玩家看不到、或只對某一種身體成立）；**B 已經修好**，留著是因為它示範的形態你自己
也會踩到。⛔ **`broken` 這一版是 0 筆。三筆的失效方式仍然相同：通過驗證，玩家看不到。**

## A ⚠️【死亡時】`hook:onDeath` — 英雄那一半修好了，**小怪那一半沒有**（state = `allowed_with_warning`）

**是什麼** —— 一個觸發時機，語意是「這張卡的持有者被打倒的那一刻」。

**rev A 說什麼** —— 「出貨路徑上一次都不會發」。派發函式的第三行擋掉已死的持有者，而死亡系統
**先**寫「不活著」**才**發事件。⭐ **那一半 2026-08-09 修好了（GH#293）**：修法沒有在派發函式裡
替 onDeath 寫一個 if，而是讓廣播表自己說話 —— 只有【死亡】那一列標記為「允許死者收到」。
所以「自己被打倒的那一刻 ⋯」現在真的會發。

**⛔ 小怪那一半：rev C 修了一半，而且成因換了一個 —— 兩句要一起讀**

⭐ **已修（GH#296）** —— rev B 寫的那個成因（小怪系統在廣播系統**之前**就把屍體銷毀，廣播跑到時
查不到那個實體，在「缺資料」那一行 return）**這一批修掉了**：屍體改成排進一個佇列，在**同一個 tick
的更後面一格**才真的清掉，所以廣播跑的時候屍體還在。守衛跑的是**完整的出貨 `step()`**，不是手寫的
預期狀態。

⛔ **但出貨路徑仍然是關的，理由換成另一個** —— **小怪身上掛不住任何卡片**。出貨的殭屍只建了
**狀態欄**，沒有**屬性表**（刻意的，owner 裁決過），而「把卡片掛到某個實體上」與「派發那個實體的卡片」
**兩支函式的第一行都是「沒有屬性表就 return」**。⇒ 「小怪死亡時 ⋯」今天**仍然寫不出來**，
只是從「屍體太早消失」變成「牠身上沒有地方放你的卡」。

⚠️ **這一半沒有自己的 issue。** GH#296 追的是**已修**的那一半（順序），
而「小怪能不能攜帶 ModifierSource」是一個**還沒有人開票**的結構問題。
⚠️ 也請注意這正是本文件反覆講的形狀：**一句「已修」在這裡會害你做出一張永遠不會發動的卡** ——
修的是機制，不是**你寫得出來的東西**。

**你該怎麼繞過** —— ① 「**殺死**小怪時 ⋯」→ 用【擊殺時】（會發，掛在擊殺者身上，13 份內容在用）。
② 「**小怪死亡時** ⋯」（不論誰殺的、含環境傷害與火圈）→ 今天寫不出來，
⛔ **不可以用【擊殺時】近似** —— 沒有擊殺者的死亡它一次都不會發。
③ 「**我死的時候給隊友** ⋯」→ ⭐ 現在有路了：用【隊友陣亡時】（本版新開，見下面的完整清單），
但注意方向是反的 —— 卡片要掛在**活著的那個隊友**身上。

## B ✅【復活時】`hook:onRevive` — 已修（rev B 起 state = `allowed`）

rev A 量到「實戰上唯一的復活路徑打空」：**復活圈**（隊友站進去頻道 5 秒）發出的事件裡
「是誰」放的是**圈圈自己的實體 id**，而廣播表讀的就是那一格 → 拿著一個圈圈去找英雄的資料，
第一行就 return。⭐ **2026-08-09 修好了（GH#294）**，改讀另一格，兩條復活路都會發。

⚠️ 留著這一段是因為它示範了一種你自己也會踩到的形態：**用道具復活測會過，實戰是零。**
今天仍然 **0 份技能內容**在用這個時機（唯一的 1 份是道具）。

## C ⚠️【淨化】的「護盾」那一池 — 預設極性把整池跳過（state = `allowed_with_warning`）

**是什麼** —— 淨化效果上的四個池開關：狀態、持續傷害、護盾、增益。四個都是布林欄位。

**rev A 說什麼** —— 「增益與護盾**兩個**都是死開關」。⭐ **增益那一池 2026-08-09 修好了
（GH#295）**：`applyBuff` / `applyStatus` / 持續傷害三個 authoring 入口都多了一格
「這一份可不可以被拔」（`dispellable`），作者標了就拔得掉 —— 那個「全專案沒有任何欄位」
的說法從這一批起是假的。

**⛔ 但要兩邊一起做，而且護盾那一池仍然要手動放行** —— ① 出貨設定的「增益預設可否被拔」
仍然是**否**（刻意的：沒有人預期自己買的裝備效果可以被敵人剝掉），而淨化的**增益池預設也是關的**
⇒ 要拔增益，卡片上要勾「拔增益」**而且**來源那一邊要標成可拔。② **護盾池**：預設極性是
「只拔負面」，而護盾**沒有極性**，所以在預設下整池被跳過 —— 要「淨化順便破盾」得把極性寫成
「不分正負」並勾護盾池。③ ⛔ 仍然沒有「此狀態不可被淨化」（驅散免疫）這個形狀。

**你該怎麼繞過** —— ① 「狀態」與「持續傷害」兩池預設就通。② 要打掉護盾，**【破盾】**仍然是
比較穩的路（它不看極性也不看可否被拔，而且可以只打掉一層）。③ 出貨的 3 件淨化道具那句
「移除目標身上的增益」今天仍然碰不到增益 —— ⭐ 但那現在是**內容可以自己修好**的，不再是引擎缺一格。

## D ⚠️【切換型技能】`ability@1.toggle` — 機制在，但玩家看不到它開著（新標籤，state = `allowed_with_warning`）

**是什麼** —— 「按一次開、再按一次關，開著期間持續付魔力，關的時候跑一段收尾效果」。機制真的出貨了，而且關閉出口只有一個函式（手動關與魔力不足自動關都走它），所以「關閉時釋放一發」不可能只發生一半。⚠️ 它**不是** 139 個標籤裡的【切換】——那一個指的是變身式切換（換形態、換模型，玩家看得到）。這是**另一條**機制，列在「14 個新標籤」那一節。

**實測到什麼** —— **兩個缺口，都會被玩家撞到。** ① **開關狀態不在任何送給客戶端的欄位裡**，開／關兩顆事件被明確列為伺服器內部事件，所以客戶端**沒有任何欄位**能畫出「它現在是開的」——玩家只看得到魔力在掉；斷線重連、觀戰切換、中途加入的人一律看成關的。（結構原因：實體旗標的位元已經一格都不剩，補這一半需要一個通訊協定決定。）② **關閉分支排在驗證順序最前面**——在暈眩、沉默、擊倒、施法中、冷卻**全部之前**。那個位置是為了「開了以後關不掉」而刻意選的，副作用是被暈眩、被沉默的人照樣關得掉，而關閉會跑收尾效果。⚠️ 另外屍體關不掉它、也不收維持費、也不自動關，復活後按鈕還開著、扣款接著跑。

**issue** —— 沒有獨立 issue（可見性那一半是 GH#285）· 修起來中等；「被控時能不能關」可以是一格後台欄位。

**你該怎麼繞過** —— ① **今天 0 份出貨內容用切換技**，所以你寫的第一張就是第一次上線測試。② 如果「玩家要看得見它開著」是這張卡的重點，改走**變身**那條路（【變身】／【切換】）——換身體會換模型，客戶端看得到。③ ⛔ **不要把收尾效果設計成一發強力傷害**——被控的人可以立刻按掉來觸發它，那是一個可以被玩家利用的出口。

## ⚠️ 附帶：火圈（⭐ 這一段 rev A 寫錯了一半，2026-08-09 更正）

rev A 寫「火圈燒傷**直接扣血**，不經過傷害結算，所以免死、無敵、魔力屏障一律擋不住」。
⭐ **那句話從 GH#287 起只剩一半是真的。** 火圈現在走一條與傷害佇列**共用同一組判定函式**
的路，所以：

- ⭐ **無敵 / 免疫傷害擋得住火圈了。** 內容側的開關就是無敵卡上那一格「擋不擋真傷」。
- ⛔ **護盾仍然不吸火圈**，**魔力屏障也吃不到它**。
- ⚠️ **免死能不能救，是一格後台開關**（`火圈 → 免死是否適用`）—— ⛔ **出貨預設是關的**
  （＝維持今天「火圈無視免死」的行為，等 owner 裁決）。所以**今天**被火圈燒死仍然不會被救起來，
  但那不再是一個結構性的洞。
- 對照：持續傷害（DoT）的最後一跳一直都**走**傷害結算，所以免死**擋得到**燒傷致死。

⚠️ 對你的後果沒有變：任何以「絕對不會死」為賣點的卡，文案上**今天**仍然不能承諾涵蓋火圈 ——
但理由從「引擎做不到」變成「後台那一格預設關著」。
這是一個玩家一定會在對局後段撞到的情境 —— 火圈正是為了收尾而存在的。

⚠️ 同一個形狀的第二個實例（不同機制，同一種「繞過共用管線」的錯）：**【吞噬】／【處決】**
先把傷害推進結算佇列、再**無條件**替施法者回血；封包要更後面才會因為目標無敵而被整包丟掉，那時回血早就付了。
→ 「處決敵人回復生命」對無敵目標照樣回滿。

---

## ⚠️ 六個最容易做出啞卡的地方

這六個是後果最嚴重的，建議在編輯器裡做成**硬性驗證**或**表單結構**，而不只是文字警告：

1. ⭐⭐ **條件現在有兩個住處：觸發時機**上**，以及每一段效果**上。**（2026-08-09 owner 裁決）**

   ⛔ **這一整條在 rev B 反過來了 —— rev A 寫「條件只能掛在 hook 上，效果陣列裡沒有這一格，
   寫了會被 schema 拒絕」。那句話從這一批起是假的，而且是在最貴的方向上假：
   照 rev A 的形狀做表單，作者寫不出一半合法的 JSON（rev A 警告的正好相反）。**

   今天的事實：**每一個效果種類**（全部 35 個）都有一格 `condition`，
   而它與 hook 上那一格是**同一個型別、同一個求值器、同一組葉子** —— ⛔ 不是第二套條件系統。
   所以編輯器可以用**同一個條件編輯元件**渲染兩個位置。

   - ✅ **寫得出來（新）**：在一支主動技的效果清單上直接掛
     「若目標身上有〔恐懼〕則這一段才生效」。省略 = 無條件執行（＝今天所有內容的行為）。
   - ✅ **寫得出來（舊，沒有變）**：「**當**我普攻命中時，**若** ⋯ 則額外造成 X」
     → 一個【普攻時】／【技能命中時】的觸發，帶條件。

   **新的表單形狀：條件是「觸發時機」卡片與「效果」卡片**共用**的一個子區塊。**
   兩邊各一格，內容物完全相同。

   ⚠️ **效果上的條件，語意要講清楚（三條）：**

   | 情況 | 行為 |
   |---|---|
   | 這一段效果**有**目標 | **逐一目標**判斷 —— 範圍技裡沒有通過的人不會被算進去 |
   | 這一段效果**沒有**目標（自我增益／落點特效） | **整段成立或整段不發生** |
   | 一個目標都沒通過 | 那一段的處理器**不會被呼叫**（不是打 0 傷害） |

   ⛔ **一個一定要在編輯器裡擋下來（或至少警告）的組合：**
   **自己解目標**的效果 —— `damageArea`（範圍傷害）／`damageLine`（直線傷害）／
   `randomArea`（隨機落點）／`leap.onLand`（落地） —— 在條件求值的那一刻**還沒有目標**，
   所以 `subject: "target"`（問「目標身上有沒有 ⋯」）在那一刻讀 **FALSE** ⇒ **整段永遠不發**。

   > **⛔ 換句話說：「範圍內只打身上有某狀態的人」這個形狀今天仍然不存在。**
   > 它不會報錯、schema 收得下、卡片描述還會畫成「目標帶有【…】」——
   > 然後遊戲裡那張卡一次都不會發動。這是本文件開頭講的「假的 ✅」的教科書例子。
   > 要做那件事，今天仍然得繞回 hook（例如【技能命中時】配槽位過濾器）。

   ⚠️ **推導時的新預設**：文案只說「若敵人具有〔恐懼〕則額外追加傷害」而沒說什麼時候判定
   → ⭐ 現在預設把條件掛在**那一段效果**上（形狀與作者腦中的一樣），不必再繞成
   「命中時機 + 只有這一支技能的槽位過濾」。⚠️ 例外正是上面那個陷阱：
   那一段如果是 `damageArea` / `damageLine` 這種自己解目標的效果，就仍然要繞回 hook。

   五個條件葉（機率／裝備／實體種類／屬性／狀態）在兩個位置都可用，**沒有例外**。

2. **指定友軍的技能放不出去。** 施放方式選【指定】時，若目標陣營是友方，玩家按下去不會有任何反應
   （四條輸入路徑都只餵得出敵方目標，沒有目標時連封包都不會送）。出貨內容已經有 13 份技能文件踩到。
   → 建議：選了【指定】就強制目標為敵方，友方效果引導作者改用【輔助】（自身）。

3. **指向技拿不到目標清單，而且這個洞在 2026-08-08 擴大了一倍。** 施放方式選【指向】時，
   任何「對目標做某事」的效果（傷害／狀態／治療／護盾）會完全落空；
   ⚠️ 七個 2026-08-08 的新效果種類（改冷卻／加權隨機分支／資源互換／事件數值轉換／隨機落點／
   魔力屏障／延長增益）**外加 rev B 的【瞬移】`blink`**，全部帶同一個「形狀」欄位，
   在指向技上「單體」解出來是空陣列、「圓形」退回**以施法者為圓心**。
   → 建議：選【指向】時，效果清單只允許投射物與直線傷害。

4. **有些標籤只有特定文件類型寫得出來**（manifest 的 `authorableIn`，本版有異動）：
   - 【真視】【隱身(常駐)】【飛行】—— 只限**天生技與道具**。
   - 【格擋】—— ⭐ **rev C 起四個授權面全開**：道具 · 技能的被動區塊（含天生技，可配形態閘）·
     三選一增益卡 · **限時授予**（`applyBuff.block`）。⭐ 最後那一個就是「**主動技能**怎麼給格擋」的答案
     —— 效果清單裡放一段限時增益授予即可，⛔ **不需要新的效果種類**。
     ⛔ 仍然缺的只剩「**格擋的那一刻**」這個時機（見【格擋時】，仍然不可選取）。
   - 【免死】—— **道具 + 技能**（具名標記的免死規則）；⛔ 三選一增益卡仍然沒有這一格。
   寫在沒有授權的文件類型上會完全無效，而且沒有任何錯誤提示。→ 建議：依文件類型過濾選項。

5. **七個標籤的「屬性那一半」對小兵永久無效**（manifest 的 `noEffectOnMinions`）：
   【破甲】【破防】【破魔】【易傷】【凋零】【層數累積】【輪替增益】。
   根因只有一個：小兵沒有屬性元件，`applyBuff` 那條路第一行就 return，**而且是靜默的**。
   第三場之後場上多數敵人是小兵，所以純屬性的技能會「前兩場有用、之後幾乎是空的」。

   ⭐ **rev B 有兩處更正（owner #299 第 7、10 條）：**
   - **【虛弱】離開這一組** —— 它從 rev B 起不是屬性，是一個讀取時的倍率。
     「造成傷害減半」那一半**對小兵完全成立**；只有攻速那一半仍然無效。
   - **【破甲】【破防】【破魔】的意思變了** —— 屬性照樣無效，
     ⭐ **但狀態掛得上小兵，而且條件葉查得到**，所以「若目標有〔破甲〕則追加 X」對小兵
     **是發得動的**。⛔ 寫法要分兩段：減護甲的數字用 `applyBuff`（對小兵沒效果），
     要讓後面的卡讀得到就**另外**用 `applyStatus` 掛一份帶那個標籤的狀態。
     ⚠️ 只寫 `applyBuff` 的話，小兵身上什麼都不會留下。

   ⚠️ 另外兩個同樣「對殭屍無效」但**根因不同**的：
   【迴避】（殭屍的攻擊根本不擲閃避骰）與【混亂】（殭屍的目標指派住在另一支系統，
   而它跑在混亂那一步**之後**、也從不問這一格 —— ⛔ **rev B 沒有複驗這一項**）。
   → 建議：選取時顯示這句提醒。

6. **「首次跌破血量」做不到。** 只能表達「現在低於 N%」，每次受傷都會重新成立 → 會反覆觸發。
   → 建議：搭配觸發的「內部冷卻」欄位，並在卡面文案上避免承諾「只有第一次」。

---

# 完整清單

### ✅ 可直接使用（64）

**啟動方式**　主動 · 被動  
**施放方式**　範圍 · 輔助  
**數值縮放**　AP加成 · AD加成 · 衍生屬性(把 A 的 X% 加到 B)  
**位移控制**　衝刺 · 擊退 · 跳躍 · **瞬移**⭐  
**效果／狀態**　暈眩 · 減速 · 變身 · 免疫 · 加速 · 吸血 · 治療 · 純演出 · 回復 · 吸收(護盾) · 暴走 · 燃燒 · 燒魔 · 真傷 · 吞噬 · 復活 · 恐懼 · 沉默 · 發錢 · 睡眠 · 護盾 · 重創 · 免死 · 反彈・分型別 · 嘲諷 · 標記 · 汲魔 · 破盾 · 魔免  
**觸發條件**　機率 · 屬性門檻 · 某事之後的 N 秒內  
**觸發事件**　普攻時 · 週期 · 受到傷害時 · 擊殺時 · 技能命中時 · 施法時 · 身上有某狀態時 · 反彈成功時 · 被暈眩時 · 裝備了某類道具時 · 守衛塔倒下 · **復活時**⭐ · 抵擋致命傷時 · 暴擊時 · 殭屍王出現 · 火圈點燃 · 被小兵傷害時 · 被英雄傷害時 · **護盾產生時**⭐ · 造成傷害時 · **隊友陣亡時**⭐  

⭐ = **rev B 新翻成綠燈（4 筆）**：瞬移（真瞬移落地）· 復活時（GH#294 修好）·
護盾產生時 / 隊友陣亡時（GH#300 接上發射點）。
⚠️ 這 4 筆的出貨內容採用率**全部是 0** —— 你的第一張卡就是那個機制的第一次上線測試。
（前一版翻綠的 10 筆採用率也仍然是 0 或 1。）

⚠️ **rev C 這一格一筆都沒有加也沒有減。** rev C 翻盤的七格全部原本就是 `allowed_with_warning`，
所以它們留在下一節；⛔ 但那七格**不是**「本來就知道可以用」——
其中四格 rev B 明確叫作者不要用。**這一節的清單沒有變，不代表那七格沒有變。**

#### ✅ 但帶補充說明的 20 筆（可以收起來，**不要整個丟掉**）

| 標籤 | 分類 | 補充說明 |
|---|---|---|
| **主動** | 啟動方式 | ⚠️ 2026-08-08 在施法的驗證順序**最前面**插了一格：如果這支技能是【切換型技能】而且正開著，按下去就是**關閉**，而且這一格排在暈眩／沉默／擊倒／施法中／冷卻**全部之前**（刻意的：否則 60 秒冷卻的切換技會關不掉）。可觀測後果：被暈眩、被沉默、被擊倒的人**仍然關得掉**切換技，而關閉會跑收尾效果。⛔ 所以不要把切換技的收尾效果設計成一發強力傷害——那是一個被控時仍然打得出來的出口。非切換技的驗證順序逐字未變。 |
| **被動** | 啟動方式 | ⭐ 2026-08-08 多了一條「被別的技能改寫」的路（【跨技能強化】），但它**唯一**的接口就掛在被動上，而且只改得到被動裡的**觸發區塊**：屬性數字、光環、視野、飛行、格擋五種內容一個都改不到，主動施放路徑也完全問不到它。⚠️ 被強化的節點名是自由字串，**打錯字那條操作會靜默無效，不會報錯**。 |
| **瞬移** | 位移控制 | ⭐ 2026-08-09（owner #299 第 4 條 / GH#301-2）**改成真瞬移**：舊版是 `leap` 配極短飛行時間的平移（最短 2 tick，身體真的存在於中間位置），現在有獨立的 `blink` 效果種類，同一個 tick 換座標。⚠️ 四個欄位要一起讀：`shape`（誰是候選）／`applyTo`（`self` 預設，或每一個解出來的目標）／`to`（指向點／貼上目標／集結到施法者）／`stopShortUnits`（落在目的地前面多少單位）。⛔ `applyTo:"target"` 配 `to:"targetUnit"` 是沒有意義的組合（每個目標瞬移到它自己身上），引擎**跳過**那一格而不是報錯。⭐ 想「瞬移過去再打」一定要用 `onArrive`：寫在同一層 `effects[]` 的下一格會在**起跳點**解算（同 `leap.onLand` 的理由）。⚠️ 落到被別人佔住的位置是允許的，之後由碰撞的軟分離推開。0 份內容在用 —— 你寫的第一張就是這個機制的第一次上線測試。 |
| **免疫** | 效果／狀態 | ⚠️ 四件要知道的：① **【淨化】拔不掉無敵**（無敵不在淨化的四個池裡）；② **【吞噬／處決】對無敵目標照樣讓施法者回滿血**（傷害封包被整包拒收，但回血在那之前就付了）；③ **【免控】只有一個讀取點** —— 它擋得掉暈眩／定身／減速／恐懼，但擊退、嘲諷、拉扯這三種不問它；④ ⭐ **2026-08-09 起無敵擋得住火圈燒傷**（GH#287，火圈改走與傷害佇列同一組判定）—— 舊版寫的「火圈直接扣血、無敵也擋不住」從這一批起是假的。傷害結算的順序是：無敵 → 減傷 → 格擋 → 護盾 → 魔力屏障 → 免死 → 扣血，所以無敵一票否決魔力屏障與免死（正確：被無敵擋掉的那一發不該燒魔力也不該燒免死層數）。 |
| **吞噬** | 效果／狀態 | ⚠️ **對無敵／免傷目標，傷害被靜默拒收，但施法者照樣回滿血。** 60 秒冷卻 + 140 魔力換一發「他沒事、我滿血」是一個玩家看得出來的怪行為。0 份內容在用。 |
| **恐懼** | 效果／狀態 | ⭐ 2026-08-08 落地並已接線：被恐懼的人會丟掉玩家指令、遠離此刻最近的敵人、而且不攻擊；到期由狀態到期系統管，結構上不可能永久。兩件要寫進卡面的事：① 它**只管腳不管手**——技能照放，要「連技能都放不出來」得同時寫【沉默】；② 它**算控場**，會被【免控】擋下來（跟【暴走】相反）。⚠️ 出貨內容今天 0 份在用。 |
| **護盾** | 效果／狀態 | ⚠️ ① 「魔力先付還是護盾先付」今天**寫死成護盾優先**，不是欄位。② 【破盾】是獨立的效果種類（不是淨化的一格），它只清護盾、不看「可否被拔」、可以只打掉一層；省略打誰時**打敵人**。③ 【淨化】理論上也能清護盾，但那一池今天是死的（見【淨化】）。④ 護盾**不在屬性表裡**（它是一個陣列），所以任何「把護盾寫成一條屬性修飾」的設計都不成立。 |
| **免死** | 效果／狀態 | ⭐ 技能也寫得出來（具名標記帶一條免死規則），不再只限道具；攔截點在扣血的前一行，所以「這一發會不會殺死我」在那裡是字面意思，而且「救完剩多少血」「真傷救不救」「內部冷卻」三個都是欄位。三個要寫進卡面的邊界：① **火圈燒傷**：2026-08-09 之後這是一格**後台開關**（火圈 → 免死是否適用），⛔ **出貨預設關** —— 所以今天被火圈燒死仍然不會被救起來，但那不再是一個結構性的洞，而是一個 owner 打得開的欄位。⭐ 同一批起**無敵／免疫已經擋得住火圈**（GH#287）。持續傷害的最後一跳一直都擋得到。② 標記只裝在**擁有那支技能的人**身上，沒有「把免死送給隊友」。③ **三選一增益卡仍然沒有這一格**。 |
| **反彈・分型別** | 效果／狀態 | ⚠️ 路徑跟名字不一樣：**沒有「反彈型別」這種欄位**。做法是在【受到傷害時】上加一格**傷害型別過濾**，再掛一段反彈；引擎真的在過濾（不是啞欄位），讀的是最後一次型別轉換之後的型別，跟護甲／魔抗吃到的那一個相同。⚠️ 它過濾的是**觸發的那一發**；反彈出去那一發自己的型別是另一格。⚠️ 反彈鏈要一起寫「最大連鎖深度 1」，而且單發反彈比例的上界是 5 倍（要更高得靠固定值補）。0 份內容在用。 |
| **標記** | 效果／狀態 | ⭐ 「A 技能在敵人身上蓋一份狀態 → B 技能讀它並引爆」成立。⭐⭐ **2026-08-09 起那個「讀」不再只能掛在觸發時機上** —— 主動技的效果清單裡每一格自己就有 `condition`，所以「這一發只對帶著記號的人多打一段」直接寫得出來。⭐ 而且**讀得到層數了**（`applyStatus.stacks` ＋ `condition.status.minStacks`），「疊到 3 層才引爆」寫得出來 —— ⚠️ 但只有作者**明寫 `stacks`** 的狀態才會累加。⛔ 條件仍然讀不到 `ability@1.marks` 那一套**具名標記**的層數（那是另一個池子）。⚠️ 既有的兩格「用記號加傷」（`comboBonus` / `bankedBonus`）沒有動，仍然只讀施法者自己身上的記號。0 份內容在用。 |
| **身上有某狀態時** | 觸發事件 | ⭐ 條件樹讀得到狀態：可以問「這一份狀態」（`statusId`）或「這一類狀態」（`tag`），主體可選自己或目標；標籤來源是文件宣告的標籤 **OR** 從欄位推導的標籤（作者漏標也查得到）。⭐⭐ **2026-08-09 起條件不再只能掛在觸發時機上**（owner 裁決）—— 每一個效果自己都有一格 `condition`，所以「這一段效果只對身上有〔恐懼〕的人生效」直接寫得出來了，**同一個型別、同一個求值器、同一組葉子**，不是第二套條件系統。⭐ 也問得到層數了（`minStacks`，見【層數累積】）。⚠️ 四件仍然要小心：① 狀態 id 是軟引用，打錯不會在載入時被擋，只是永遠不成立（用「類」比對比較安全）；② id 與標籤同時寫是**解析錯誤**，不會安靜地選一邊；③ 效果上的條件是**逐一目標**判斷的（範圍技裡沒有那個狀態的人不會被算進去）；④ ⛔ **沒有解出目標的效果**（`damageArea` / `damageLine` / `randomArea` / `leap.onLand`）會退化成「整段成立或整段不發生」，而 `subject:"target"` 在那一刻讀 **FALSE** —— 所以那些效果配 `subject:"target"` 是**永遠不發**，⛔ 不要用它假裝有做「範圍內只打身上有某狀態的人」。0 份內容在用。 |
| **反彈成功時** | 觸發事件 | ⛔ **這一格的引擎名字改了**（舊名 `onReflect` 已經從枚舉裡拿掉），照 2026-08-05 版抄的文件會在載入時被拒收。⭐ **rev C 量過了**（GH#303）：事件真的發、掛在上面的效果真的執行、而且**事件的對象就是原攻擊者**（追加傷害打得到他），觸發的那一發傷害也會傳進去。⚠️ 判準只有變嚴：反彈量 ≤ 0、鏈到底、目標死了／免疫／被迴避，這幾種都不發。⚠️ 反彈鏈要寫「最大連鎖深度 1」，單發反彈比例上界 5 倍。0 份內容在用。 |
| **裝備了某類道具時** | 觸發事件 | 「某**類**」比對的是道具自己的標籤（從登錄表讀），「某一件」走道具 id；兩格同時寫是解析錯誤。⚠️ 道具 id 是**軟引用**，打錯不會在載入時被擋，只是永遠不成立。⚠️ 「類」讀的是標籤不是價格帶，也沒有「幾件」。⭐ **2026-08-09 起這個條件在效果上也掛得了**（不再只能掛在觸發時機上）。0 份內容在用。 |
| **守衛塔倒下** | 觸發事件 | ⭐ 打倒守衛塔**不發【擊殺時】**（獎勵由守衛塔系統自己付），所以這是唯一接得到的路，而它現在接上了。世界作用域的兩條語意要寫進卡面：① **沒有目標**——任何「對目標⋯」的條件一律讀否，效果一律落在持有者自己身上；② **不發給死人**——持有者那一刻正好躺著就是零。0 份內容在用。 |
| **復活時** | 觸發事件 | ⭐ 2026-08-09 修好了（GH#294）：舊版只有「復活」效果種類（技能／道具復活，全庫 1 份道具）那條冷門路會發，**復活圈**（隊友站進去頻道 5 秒，實戰上唯一會發生的復活）永遠打空 —— 廣播表取到的是圈圈自己的實體而不是英雄。現在兩條路都會發。⚠️ 0 份**技能**內容在用（唯一的 1 份是道具），所以你寫的第一張仍然是第一次上線測試。 |
| **抵擋致命傷時** | 觸發事件 | ⭐ 「擋下致命一擊的那一刻跑效果」今天寫得出來，但走的是**具名標記**不是一個時機：救活的同一刻可以在**被救的人**身上跑一批效果（例如無敵 1.5 秒 + 回復 50%），也可以在**周圍敵人**身上跑一批（例如擊退／暈眩）。⛔ ① 它**不是**通用的「擋下致命傷」時機——只有身上帶免死規則的標記救起來的那一發才跑，沒有標記就是嚴格的什麼都不做。② **火圈燒傷攔不到**（持續傷害的最後一跳擋得到）。③ 道具側「擋下致命傷」的格擋仍然不觸發任何效果。1 份技能內容在用。 |
| **殭屍王出現** | 觸發事件 | 世界作用域的兩條語意同【守衛塔倒下】：**沒有目標**、**不發給死人**。⚠️ 別跟【對 BOSS】混：這是「王出現的那一刻」，不是「打王時多打幾成」（後者仍然寫不出來）。0 份內容在用。 |
| **火圈點燃** | 觸發事件 | 只在火圈開始的那一 tick 發**一次**，不是每 tick。世界作用域兩條語意同【守衛塔倒下】（沒有目標、不發給死人）。⭐ **2026-08-09 更正一句舊文案**：火圈燒傷不再繞過整條防禦鏈（GH#287）—— **【免疫】／無敵擋得住火圈了**。⛔ 仍然擋不住的：**護盾不吸火圈**、**魔力屏障吃不到它**；**免死**能不能救是後台一格開關（火圈 → 免死是否適用），⛔ **出貨預設關**（＝維持今天「火圈無視免死」的行為，等 owner 裁決）。0 份內容在用。 |
| **護盾產生時** | 觸發事件 | ⭐ 2026-08-09 落地（owner GH#300：「這個使用率超高請一定要實作」）。持有者＝**拿到護盾的人**，事件裡的對手＝給盾的人。⚠️ 口徑是「**新出現一片盾**」不是「這個人身上的盾變多了」：一發 AoE 給三個人 → 三則（三張卡各響一次）；身上已經有兩片再拿第三片 → 仍然一則。⛔ 不要寫「護盾產生時給自己護盾」這種卡 —— 那是一條每次都真的多一片盾的自迴圈；引擎有一個「每 tick 最多四輪」的界會把它截斷，但那是保險絲不是設計。0 份內容在用。 |
| **隊友陣亡時** | 觸發事件 | ⭐ 2026-08-09 落地（GH#300）。⚠️ **方向與【死亡時】是相反的**：持有者是**還站著的隊友**，不是死掉的那個人 —— 掛在死者身上的卡什麼都不會發生。死者的每一個活著的隊友各收到一次，他自己不收。⚠️ 一次團滅時誰也不觸發誰（那一刻大家都已經躺下了）。⚠️ 它吃的是與【死亡時】**同一則**死亡事件，所以小怪死亡同樣發不出去（GH#296）—— 「隊友」指的是英雄隊友。0 份內容在用。 |

### ⚠️ 可用，但必須顯示警告（52）

| 標籤 | 分類 | 給作者看的警告 |
|---|---|---|
| **指定** | 施放方式 | 只能指定敵人，而且這道限制在 2026-08-08 之後**變嚴也變大**了。把技能設成指定友軍（治療／增益）時，玩家按下去不會有任何反應——滑鼠／手把／觸控／機器人四條輸入路徑仍然只餵得出敵方目標，沒有目標時連封包都不會送出去；引擎端現在還多了一道明寫的排除。⚠️ 出貨內容裡已經有 **13 份獨立技能文件**踩到這一格（全部是「按了什麼都不會發生」）。友方效果請改用「輔助（自身）」或帶友方範圍的效果。 |
| **指向** | 施放方式 | 指向技**不會產生目標清單**，任何「對目標做某事」的效果掛上去會完全落空；指向技只能靠投射物或直線傷害來結算。⚠️ 這個洞在 2026-08-08 **擴大了一倍**：七個新的效果種類（改冷卻／加權隨機分支／資源互換／事件數值轉換／隨機落點／魔力屏障／延長增益）全部被強制帶一個「形狀」欄位，而在指向技上「單體」形狀解出來是空陣列、「圓形」形狀因為沒有落點而退回**以施法者為圓心**。所以「指向技 + 任何新效果種類」今天一律是安靜的空砲。 |
| **解鎖上限** | 數值縮放 | 目前只有攻擊速度與吸血兩條屬性真的能突破上限，其他屬性寫了不會生效。 |
| **擊飛** | 位移控制 | ⭐ 2026-08-09（owner #299 第 3 條 / GH#301-1）**落點可以指定了**，簡化成四檔：一小段 `short` ／ 預設 `default` ／ 一大段 `long` ／ 到底部 `toEdge`。四檔的實際距離是後台 `config.combat-feel@1` 的欄位，不是寫死的數字。⛔ 兩件一定要知道：① **選了 `default` 以外的任何一檔，`distance`／`impactPower`／「減去雙方距離」三格全部不看** —— 那一檔就是落點。想要系統推算的長度請寫 `default`（或整格省略），行為與這一格出現之前逐字相同。② **`toEdge` 不是一個很大的數字**，是一條沿著推的方向打在決鬥區邊界圓上的射線；被推的人已經在圈外（火圈縮過頭）時它回 0＝不推。⚠️ 飛行時間仍然是「距離 ÷ 速度」，所以弧線一定跟你選的那一檔對得上，但它**不是**一個可以獨立填的秒數。0 份內容在用。 |
| **飛行** 🔒 | 位移控制 | 沒有限時飛行。只有天生技與道具能給常駐飛行，主動技能無法給「飛 N 秒」。 |
| **後撤** | 位移控制 | 沒有獨立的「後撤」選項，要用位移並把方向設成反向。 |
| **拉扯** | 位移控制 | 位移距離會先扣掉與目標的間距，所以實際拉近的距離比填的數字短。設計時要把這一段算進去。 |
| **擊倒** | 位移控制 | 擊倒期間目標完全無法行動也無法轉身，時間由位移距離推算，不能單獨指定。 |
| **鉤索** | 位移控制 | 沒有拉繩表現，也不是「拉到腳邊」——實際是把目標拉一段固定距離。 |
| **召喚** | 效果／狀態 | 機制完整但目前沒有任何已上線的內容使用過，第一張卡等同新功能，建議先小規模驗證。 |
| **迴避** | 效果／狀態 | 屬性形式（提高閃避率）可用且有 15 份內容在用；⭐ 觸發側也補上了（見【迴避時】）。⛔ 但「這一發必定閃避」的效果形式仍然 0 份內容用過。⛔ 而且**對整波殭屍完全無效且沒有錯誤訊息**：殭屍沒有屬性元件，更徹底的是**殭屍的攻擊根本不擲閃避骰**。 |
| **定身** | 效果／狀態 | 擋得住普通移動，但擋不住衝刺、擊退、跳躍。有位移技的角色被定身後照樣衝得出去。 |
| **旋轉** | 效果／狀態 | 可用，目前僅兩支技能使用。 |
| **切換** | 效果／狀態 | 這個標籤指的是**變身式切換**（切形態、換模型），它本身可用，而且因為換的是身體，玩家看得到它切過去了。⚠️ 不要跟 2026-08-08 新增的**【切換型技能】**（開／關按鈕 + 開著時每秒扣魔 + 關閉時跑一段收尾效果）混為一談：那是**另一條**機制，它的開關狀態**不在任何送給客戶端的欄位裡**（玩家只看得到魔力在掉，斷線重連／觀戰／中途加入一律看成關的），而且被暈眩、被沉默、施法中**都關得掉**，關閉會跑收尾效果。見 newTags 裡的【切換型技能】。 |
| **反彈** | 效果／狀態 | ⭐ **rev B 的「待確認」撤回 —— 這一批真的量了（GH#303 的三個問題全部通過，兩條守衛都跑過）。** owner #299 第 5 條「有許多角色靠『反彈成功』發動連續效果」在引擎這一邊**成立**：① 事件真的發（獨立的派發系統，排在死亡結算**之前**、傷害排空迴圈**之外**，所以被同一發打死的人不會「死後才反彈」，而 A 彈 B、B 又打 A 的鏈每 tick 只推進一步）；② 掛在它上面的【狀態】【標記】【傷害】真的執行；③ ⭐ **追加傷害打在原攻擊者身上** —— 事件的對象就是攻擊者，不填目標就是他；而且**觸發的那一發傷害會一路傳進去**，所以「反彈成功時再追加剛剛那一下的 N%」寫得出來（少了這一半，那種卡會靜默變成 0）。⛔ 仍然要注意三件：① **「反彈成功」的判準比直覺窄** —— 反彈量算出來 ≤ 0、反彈鏈到底、以及目標死了／無敵免疫／被迴避，這幾種**都不算成功，一次都不發**（刻意的：一個「其實沒彈到也照發」的事件會讓「反彈時回血」實際上變成「被打時回血」，而畫面上看不出差別）；② 反彈自己**沒有型別維度**，掛上去物理與魔法一律照彈，要「只彈物理」得走【反彈・分型別】；③ 反彈鏈要一起寫最大連鎖深度，單發反彈比例上界 5 倍。⚠️ 出貨內容**0 份**在用這個時機 —— owner 說的那些角色還沒重製，所以你寫的第一張就是它的第一次上線測試。 |
| **暴擊** | 效果／狀態 | ⭐ **owner #299 第 2 條 2026-08-09 落地（GH#302），rev B 的「待確認」撤回。** 今天的規則就是 owner 講的那一條：**每一條暴擊來源各自擲骰，抽中的倍率依序相乘**（舊的「只算最強那一條、整發只擲一次」已經不是預設了）。⭐ 而「一條自己的機率＋自己的倍率＋自己的吸血」這種來源（`critStrike`）**四個授權面都寫得出來了** —— 道具、技能被動（含天生技）、三選一增益卡、以及限時授予（`applyBuff`）。rev B 寫「暴擊吸血只掛得到道具」，那句從這一批起是假的。⛔ **但出貨設定會夾掉 owner 自己那個例子**：`config.crit@1` 的**總倍率上限出貨是 100**，所以「1% × 100 倍 ＋ 10% × 2 倍」兩條都中的那個 **200 倍在出貨設定下會變成 100 倍**。⚠️ 另外兩格也在同一頁後台：**合成方式**（相乘／取 max／相加，出貨相乘 —— 切成取 max 的世界裡玩家的第二張暴擊卡就是廢牌）與**最多算幾條來源**（出貨 5，超過的按期望值排序丟掉，同時也是每一發的亂數預算上界）。⇒ ⛔ **卡面文案不要承諾一個具體的總倍率**，那個數字由後台三格決定；要承諾就只承諾「這一條自己的倍率」。⚠️ 屬性形式（提高暴擊率／暴擊傷害）照舊可用（36 份內容在用），但它是**聚合**的：加下去之後這位英雄**每一次**暴擊都變成那個倍率，「6% 的那一次是 10 倍」要走 `critStrike` 那條路。⚠️ 採用：`critStrike` 出貨只有 1 件道具在用，另外三個授權面各 0 份。 |
| **格擋** 🔒 | 效果／狀態 | ⭐ **owner #299 第 6 條「授權格要放寬」這一批做完了，rev B 的「待實作」撤回。** 授權面從兩個變成**四個**，而且是**同一份定義展開四次**（不是抄四份）：**道具**、**技能的被動區塊**（含天生技，可配「只有在某形態下才格擋」的閘）、**三選一增益卡**、以及 ⭐ **限時授予**（`applyBuff.block`）。⭐ 最後那一個同時就是「**主動技能**要怎麼給格擋」的答案 —— ⛔ **不需要一個新的效果種類**，主動技的效果清單裡放一段限時增益授予即可，「接下來 5 秒內格擋」現在有形狀了。四條路寫進的是同一個來源欄位（減傷那一側完全不看來源種類），所以鏈式獨立判定、傷害型別過濾、致死判定逐條相同。⛔ 仍然缺的**只剩一格**：**「格擋的那一刻」不是一個時機** —— 擋下來不會觸發任何效果（整支格擋程式零個派發點），所以「格擋成功時反擊」今天寫不出來，【格擋時】那一格仍然不可選取。⚠️ 內部冷卻的記帳住在**那一個來源**上，而限時授予每次施放都是一個**新的**來源 ⇒ 那一格在限時授予上的正確讀法是「**這一次施放最多擋幾次**」，不是「每 N 秒一次」。⚠️ 採用：4 件道具在用，另外三個授權面各 0 份。 |
| **處決** | 效果／狀態 | 不是「直接判定死亡」，是「血量低於門檻時打一筆大傷害」。血量高於門檻的目標不會被秒。⚠️ 如果走的是【吞噬】那條路：對無敵／免傷目標傷害會被靜默拒收，**但施法者照樣回血**。 |
| **衝擊波** | 效果／狀態 | ⭐ **rev B 這一格是錯的，rev C 更正（第三守則：註解會說謊，去驗證）。** rev B 寫「引擎把整支收合成一顆投射物＋命中傷害，不是分段推進」，而那個結論的唯一根據是**模板原始碼的一句註解** —— 那句註解描述的是**技能文件**長什麼樣，它沒有描述**投射物文件**。owner #299 第 1 條「**是分段推進傷害沒錯**」是對的：模板產出的投射物是 `imported.wave`，而**出貨的 9 份 `imported.wave*` 投射物文件每一份都是 `pierce: true`** —— 波不會停在第一個人身上，它繼續前進，沿路掃到的每一個敵人**各跑一次完整的命中效果**，直到射程用完或撞到場地邊界。⭐ 而且這不是理論：**76 份出貨技能文件已經在用這一族投射物**。⛔ 三件仍然要注意：① **同一個身體只會被打到一次**（引擎記著這一發打過誰），所以「對**同一個目標**連打三段」寫不出來 —— 分段指的是**沿路不同的身體**；② ⛔ **`pierce` 住在投射物文件上，不在技能上** —— 同一支技能換一顆非穿透的投射物（`imported.bolt*` 那 6 份全部沒有 pierce）就會變成「打到第一個人就停」，而**卡面上看不出任何差別**；③ 命中順序是沿路先撞到誰就先打誰，不是同時結算。 |
| **延遲落地(通用排程)** | 效果／狀態 | ⭐ 「N 秒後在一片區域跑這串效果」今天真的落得了地（排程綁在絕對時間上，重播重現得出同一組落點）。⛔ 但**落點一定是隨機的**：只能給一個散佈半徑，**沒有「就在這個座標」**；錨點只能是自己或目標，而且在施法那一刻就固定。⛔ 節奏只有「每隔 X 秒、共 N 次」一種，寫不出「這一發 0.3 秒、下一發 2 秒」。⛔【陷阱】（踩到才觸發）與【引導】仍然拿不到。0 份內容在用。 |
| **淨化** | 效果／狀態 | ⭐ 2026-08-09（GH#295）**【增益】那一池不再是死開關**：`applyBuff` / `applyStatus` / 持續傷害三個 authoring 入口都多了一格「這一份可不可以被拔」（`dispellable`），作者標了就拔得掉。⚠️ 但要兩邊一起做：出貨設定的「增益預設可否被拔」仍然是**否**（刻意的 —— 沒有人預期自己買的裝備效果可以被敵人剝掉），而淨化的**增益池預設也是關的**。⛔ **【護盾】那一池仍然要手動放行**：預設極性是「只拔負面」，而護盾沒有極性，所以在預設下整池被跳過 —— 要「淨化順便破盾」得把極性寫成「不分正負」並勾護盾池（或直接改用【破盾】，它不看極性也不看可否被拔，而且可以只打掉一層）。⛔ 仍然沒有「此狀態不可被淨化」（驅散免疫）這個形狀。⚠️ 出貨的 3 件淨化道具都沒明寫要拔哪幾池，吃的就是那些預設；其中一件的卡面寫「移除目標身上的增益」，今天仍然碰不到增益 —— ⭐ 但現在那是**內容可以自己修好**的，不再是引擎缺一格。 |
| **減傷** | 效果／狀態 | 沒有「受到傷害降低 N%」這一格：屬性表裡沒有這條屬性，唯一的比例減免仍然是護甲／魔抗的公式（對真傷無效）。⭐ 2026-08-08 多了一種「整包吃掉」的形狀：**魔力屏障**（用魔力抵傷，必須明列擋哪些傷害型別，可設保留魔力下限），它是真的會扣傷害的。所以今天能做的近似有三種：護盾、免疫、魔力屏障。⚠️ 魔力屏障 0 份內容在用。 |
| **石化** | 效果／狀態 | 可以做出「不能動」，但沒有專屬圖示與驅散分類，玩家在畫面上分不出它跟暈眩的差別。 |
| **虛弱** | 效果／狀態 | ⭐ 2026-08-09（owner #299 第 7 條 / GH#301-4）**有專屬機制了**：虛弱＝「攻速減半 **＋ 造成的傷害**減半」。兩個倍率住在後台 `config.weakness@1`（可調），⛔ 不是卡片上的數字 —— 兩張不同的虛弱卡是同一個強度，而且**疊兩層不會變成 0.25**（有沒有虛弱是布林）。⚠️ 「造成傷害減半」砍的是**打出去的傷害封包**不是 AD/AP 屬性，所以**固定值傷害也減半**；代價是屬性面板不會顯示 AD/AP 掉一半（虛弱不是一條屬性，它是身上的一個減益）。⛔ **哪一筆狀態算虛弱由狀態文件的標籤決定**（`tags` 帶 `weakness`），引擎裡沒有寫死的狀態編號 —— 而**今天出貨的 28 份狀態文件一份都沒有帶這個標籤**，所以這個機制此刻是**惰性的**：你要先建一份帶 `weakness` 標籤的狀態文件，技能再 `applyStatus` 套上它，不用改任何一行程式就會生效。⚠️ 對小兵是**一半有效**：造成傷害減半成立（傷害封包不看屬性元件），攻速減半不成立（小兵沒有屬性元件）。 |
| **輪替增益** 🧟 | 效果／狀態 | 對小兵無效——小兵沒有屬性元件，輪替的增益不會掛上去。 |
| **中毒** | 效果／狀態 | 引擎**不區分傷害屬性**：中毒、流血、燃燒共用同一套持續傷害。⭐ 但「兩張不同名字的卡在機制上完全一樣」這句 2026-08-08 之後**不再成立**：狀態標籤變成開放的跨技能介面，別的技能真的讀得到，所以正確寫法是「持續傷害付傷害 **＋** 另外掛一份帶 `poison` 標籤的狀態」，兩張卡從此分得開。⛔ 三條代價：持續傷害**自己不會**掛那份狀態（作者要寫兩段，忘了就退回舊行為）；今天內容裡**沒有** poison／bleed 狀態文件；淨化仍然只能整個持續傷害池一起清。 |
| **免控** | 效果／狀態 | 叫「免控」但擋的仍然是一小部分控場。⭐ 2026-08-08 多擋了一種（**恐懼**）。⛔ 仍然照樣穿透的：**擊退**、**嘲諷**、**沉默**、**混亂／暴走**、**重創**、**持續傷害**。設計時請逐項對照，不要當成「免疫所有負面」。 |
| **冷凍** | 效果／狀態 | 沒有專屬機制，是「減速 + 定身」兩段組合。 |
| **凋零** 🧟 | 效果／狀態 | 沒有專屬機制，是「週期觸發 + 疊層負面增益」的組合寫法。 |
| **層數累積** 🧟 | 效果／狀態 | ⭐ **owner #304 的三條軸 2026-08-09 全部落地，而且與既有的具名標記合併成 _一套_，不是第二套。** 一個計數器的身分是**借來的**（一個技能編號 或 一個狀態 id），三條軸改的都是**同一個**數字：① **隨觸發 ±N** —— 把「計數器 ±N」這一段效果掛在**任何一個觸發時機**上（普攻時 +1、受到傷害時 −1 ⋯）；② **隨時間 ±M** —— 掛在【週期】上，節奏用那一格**內部冷卻**表達（每 N 秒一次）。⛔ **不要漏掉它** —— 沒填內部冷卻就是**每 tick 一次**（一秒 30 層），而畫面上只看得出「怎麼一瞬間就空了」；③ **隨回合 ±N** —— 標記文件上的一格，回合邊界結算。⚠️ 它與「每回合補回初始值」**互斥**（schema 會擋下來），因為那是同一根軸的兩種政策。⚠️ 三條軸都是**撞到 0 或撞到上限就停**，不會變成負數，而且**掉層會記帳**（「每失去一層永久 +X%」讀的就是那個帳），補滿不記帳。⭐ 讀取端也接上了：條件葉問得到層數（≥N），⭐ 而且**層數真的送到客戶端了** —— 每個座位一組「(計數器 id, 層數)」，HUD 讀的就是它。⛔ **rev B 那句「任何『HUD 會顯示第幾層』的文案今天都是謊話」，從這一批起自己才是謊話** —— 照它繞路是白繞。⭐ 同一批也修好了舊那一套的坑：**「每層 +10%（乘算）」現在真的乘層數**（舊版永遠只算一層，而且面板／商店／圖鑑一致地報那個錯數字）。⛔ 仍然要注意五件：① **只有作者明寫層數的狀態才會累加** —— 沒寫就只是續期（相容性閘），所以對出貨的既有狀態問「≥2 層」永遠是 false，那不是壞掉；② 只有「≥」一種比較，「**剛好 N 層**」寫不出來；③ 「某一**類**狀態合計幾層」沒有人定義過那個語意，⛔ 別那樣讀；④ 線上每個座位最多送 16 個計數器（超過的按 id 字典序丟掉，決定性的）；⑤ **增益疊層**（`applyBuff.stackKey`）那一套對小兵仍然永久無效 —— ⭐ 但**計數器與狀態層數對小兵是成立的**（殭屍有狀態欄）。⛔ 具名標記仍然只裝在**擁有那支技能的人**身上，貼不到敵人身上（要貼敵人請用狀態層數）。⚠️ 採用：具名標記 1 份（十二道試煉），隨回合 0、狀態層數 0、層數條件 0 —— 三條軸的第一張卡都還沒有人寫。 |
| **易傷** 🧟 | 效果／狀態 | 只放大物理與魔法傷害，對真實傷害無效。 |
| **替身** | 效果／狀態 | 同上：機制完整但沒有任何已上線內容用過。 |
| **標記引爆** | 效果／狀態 | ⭐ 「在敵人身上標記後引爆」寫得出來：A 技能 `applyStatus` 貼一份狀態，B 技能用 `condition.status{subject:"target"}` 讀它。⭐⭐ **2026-08-09 起引爆那一段可以直接掛在主動技的效果清單上**（每個效果自己有一格 `condition`），不必再繞去一個 hook。⛔ 既有的兩格「用記號加傷」欄位（`comboBonus` / `bankedBonus`）**沒有動**，它們仍然只讀得到**施法者自己**身上的記號。⛔ 一個一定要避開的陷阱：`damageArea` / `damageLine` 這種**自己解目標**的效果配 `subject:"target"` 的條件時，條件在**沒有上游目標**的那一刻讀 FALSE ⇒ 整段不發。「範圍內只引爆帶記號的人」今天寫不出來。⚠️ 狀態條件葉 0 份內容在用。 |
| **流血** | 效果／狀態 | 引擎**不區分傷害屬性**：中毒、流血、燃燒共用同一套持續傷害。⭐ 但「兩張不同名字的卡在機制上完全一樣」這句 2026-08-08 之後**不再成立**：狀態標籤變成開放的跨技能介面，別的技能真的讀得到，所以正確寫法是「持續傷害付傷害 **＋** 另外掛一份帶 `bleed` 標籤的狀態」，兩張卡從此分得開。⛔ 三條代價：持續傷害**自己不會**掛那份狀態（作者要寫兩段，忘了就退回舊行為）；今天內容裡**沒有** poison／bleed 狀態文件；淨化仍然只能整個持續傷害池一起清。 |
| **混亂** | 效果／狀態 | ⭐ 2026-08-09（owner #299 第 9 條 / GH#301-3）**語意整個換掉**：舊版是「自動選目標時會挑到隊友」，現在是**完全無法指定目標 ＋ 亂走路**，跟【恐懼】同一族。⛔ 照舊版設計的卡（「混亂的人會打隊友」）在新語意下**不成立** —— 他誰都打不到。三件要知道的：① **不用再配【暴走】**（這一格自己就丟指令＋亂走；兩個一起寫時混亂贏）；② 混亂期間**技能照放** —— 要「連技能都放不出來」得同時寫【沉默】；③ 亂走的方向每 0.5 秒換一次，走的是模擬層的亂數（重播重現得出同一條路）。⛔ **對殭屍的行為這一批沒有複驗**：殭屍的目標指派住在另一支系統，而它跑在混亂那一步**之後**、也從不問這一格 —— 所以「對整波殭屍下混亂」今天請當成不成立。⚠️ 今天 0 份內容在用。 |
| **狀態被套用的當下** | 效果／狀態 | ⭐ 2026-08-09 落地（GH#300）：狀態被掛上去的那一刻。持有者＝**被掛上狀態的人**，對手＝施加的人。⛔ 三種情況**刻意不發**：① **續期不重觸發**（否則一支每 tick 續期的減速會每秒發 30 次）；② 被【免控】拒絕掛上的那一筆不算；③ 疊層時**只有層數真的變高**那一次算 —— 而層數只有在作者明寫 `stacks` 時才會累加，所以對出貨的既有狀態它等於「只有第一次」。⚠️ 別跟【身上有某狀態時】混：那一個是**條件**（2026-08-09 起效果與觸發器上都掛得了），這一個是**時刻**。0 份內容在用。 |
| **真視** 🔒 | 效果／狀態 | 只有天生技與道具寫得出來。寫在一般技能或增益卡上會完全無效。 |
| **破甲** 🧟 | 效果／狀態 | **屬性那一半對小兵永久無效**（小兵沒有屬性元件，`applyBuff` 那條路第一行就 return，而且是**靜默**的）；第三場之後場上多數敵人是小兵。⭐ 但 2026-08-09（owner #299 第 10 條 / GH#301-6）補上並證實了另一半：**狀態掛得上小兵，條件葉也查得到** —— 所以「若目標身上有〔破甲〕則追加 X」這種後續效果，對小兵**是發得動的**。⛔ 寫法要分成兩段：減護甲／減魔抗的數字用 `applyBuff`（對小兵沒有效果），要讓後面的卡讀得到就**另外**用 `applyStatus` 掛一份帶〔破甲〕標籤的狀態。⚠️ 一段效果只寫 `applyBuff` 的話，小兵身上什麼都不會留下。 |
| **破防** 🧟 | 效果／狀態 | **屬性那一半對小兵永久無效**（小兵沒有屬性元件，`applyBuff` 那條路第一行就 return，而且是**靜默**的）；第三場之後場上多數敵人是小兵。⭐ 但 2026-08-09（owner #299 第 10 條 / GH#301-6）補上並證實了另一半：**狀態掛得上小兵，條件葉也查得到** —— 所以「若目標身上有〔破防〕則追加 X」這種後續效果，對小兵**是發得動的**。⛔ 寫法要分成兩段：減護甲／減魔抗的數字用 `applyBuff`（對小兵沒有效果），要讓後面的卡讀得到就**另外**用 `applyStatus` 掛一份帶〔破防〕標籤的狀態。⚠️ 一段效果只寫 `applyBuff` 的話，小兵身上什麼都不會留下。 |
| **破魔** 🧟 | 效果／狀態 | **屬性那一半對小兵永久無效**（小兵沒有屬性元件，`applyBuff` 那條路第一行就 return，而且是**靜默**的）；第三場之後場上多數敵人是小兵。⭐ 但 2026-08-09（owner #299 第 10 條 / GH#301-6）補上並證實了另一半：**狀態掛得上小兵，條件葉也查得到** —— 所以「若目標身上有〔破魔〕則追加 X」這種後續效果，對小兵**是發得動的**。⛔ 寫法要分成兩段：減護甲／減魔抗的數字用 `applyBuff`（對小兵沒有效果），要讓後面的卡讀得到就**另外**用 `applyStatus` 掛一份帶〔破魔〕標籤的狀態。⚠️ 一段效果只寫 `applyBuff` 的話，小兵身上什麼都不會留下。 |
| **繳械** | 效果／狀態 | 實際是提高攻擊失手率，不是真的讓對方無法攻擊——所以不會有傷害封包、不觸發吸血或普攻連動。 |
| **血量首次低於** | 效果／狀態 | 只能表達「現在低於 N%」，不能表達「首次跌破」。每次受傷都會重新成立，所以會反覆觸發。 |
| **詛咒(失手)** 🧟 | 效果／狀態 | 只影響英雄的攻擊。小兵的攻擊完全不經過失手判定，所以對小兵無效。 |
| **限時隱身** | 效果／狀態 | 沒有直接的限時隱身，目前只能透過變身間接達成。 |
| **隱身(常駐)** 🔒 | 效果／狀態 | 只有天生技與道具寫得出來。一般技能與增益卡寫不出常駐隱身。 |
| **飢餓** | 效果／狀態 | 只會流失生命，不會流失魔力。 |
| **驅散** | 效果／狀態 | ⭐ 2026-08-09（GH#295）**【增益】那一池不再是死開關**：`applyBuff` / `applyStatus` / 持續傷害三個 authoring 入口都多了一格「這一份可不可以被拔」（`dispellable`），作者標了就拔得掉。⚠️ 但要兩邊一起做：出貨設定的「增益預設可否被拔」仍然是**否**（刻意的 —— 沒有人預期自己買的裝備效果可以被敵人剝掉），而淨化的**增益池預設也是關的**。⛔ **【護盾】那一池仍然要手動放行**：預設極性是「只拔負面」，而護盾沒有極性，所以在預設下整池被跳過 —— 要「淨化順便破盾」得把極性寫成「不分正負」並勾護盾池（或直接改用【破盾】，它不看極性也不看可否被拔，而且可以只打掉一層）。⛔ 仍然沒有「此狀態不可被淨化」（驅散免疫）這個形狀。⚠️ 出貨的 3 件淨化道具都沒明寫要拔哪幾池，吃的就是那些預設；其中一件的卡面寫「移除目標身上的增益」，今天仍然碰不到增益 —— ⭐ 但現在那是**內容可以自己修好**的，不再是引擎缺一格。 |
| **驅散免疫** | 效果／狀態 | ⭐ 2026-08-09（GH#295）**寫得出來了**：`applyStatus` / `applyBuff` / 持續傷害三種效果都多了一格「這一份可不可以被拔」，明寫 `false` 的那一筆淨化就拔不走。rev A 寫「授權格還是沒有，今天唯一的槓桿是後台三個全域預設」—— 那句話從這一批起是假的。⛔ 但它**不是**一個「這個人免疫驅散」的狀態，三件要知道的：① 它是**逐一來源**的旗標 —— 你只保護得了自己這一段掛上去的東西，保護不了別人給的；② **回合重置與復活不看它**（那兩條是「這一局結束了」不是「有人對你放淨化」）⇒「永遠不會消失」仍然寫不出來；③ **【破盾】不看它**（護盾池根本沒有這一格），所以護盾擋不了破盾。⚠️ 出貨的 28 份狀態文件一份都沒有寫這一格，所以吃的仍然是後台的三個全域預設（狀態／持續傷害＝可拔，增益＝不可拔）。 |
| **死亡時** | 觸發事件 | ⭐ **英雄那一半 2026-08-09 修好了**（GH#293）：「自己被打倒的那一刻 ⋯」現在真的會發（廣播表那一列明確放行死者）。⚠️ **小怪那一半這一批修了一半，但出貨路徑仍然是關的，而且成因換了一個** —— 這兩句要一起讀：**已修**：小怪的屍體不再在廣播跑起來之前就被銷毀（改成同一個 tick 的更後面一格才清），所以一隻**身上掛得住卡片**的小怪，牠自己的【死亡時】真的會派發（守衛跑的是完整的出貨 tick）。⛔ **仍然不行**：**小怪身上掛不住任何卡片** —— 出貨的殭屍只建了狀態欄，**沒有屬性表**，而「把卡片掛上去」與「派發卡片」這兩支函式**第一行都是「沒有屬性表就 return」**。⇒ 「**小怪死亡時** ⋯」今天**仍然寫不出來**，只是理由從「屍體太早消失」換成「牠身上沒有地方放你的卡」。⚠️ 這一半**沒有自己的 issue**（GH#296 追的是已修的那一半）。⇒ 繞法沒有變：「**殺死**小怪時 ⋯」用【擊殺時】（會發，掛在擊殺者身上，13 份內容在用）；⛔ **不可以用【擊殺時】近似「不論誰殺的」** —— 沒有擊殺者的死亡（環境傷害、火圈）它一次都不會發；「我死的時候給隊友 ⋯」用【隊友陣亡時】（掛在**活著的那個隊友**身上）。⚠️ 0 份內容在用：你寫的第一張是這個時機的第一次上線測試。 |
| **迴避時** | 觸發事件 | ⛔ **分不出「我真的閃掉了」與「對方自己揮空」**：攻擊者因為被下【詛咒(失手)】而揮空時，這個時機**也會發動**（兩者共用同一個發射點，引擎自己的註解就寫著「這個事件的名字現在涵蓋兩個不同的原因」）。所以「閃避時反擊」的卡會在不該發的時候發。⚠️ 兩個角色是反的：持有者是**閃掉的那個人**，效果的目標是**攻擊者**——照直覺抄會掛到攻擊者身上。0 份內容在用。 |
| **護盾破碎時** | 觸發事件 | ⭐ 2026-08-09 落地（GH#300）。持有者＝**盾被打破的那個人**，對手＝打破它的人。⛔ 但它是一個**時刻**不是一個動作，三種情況**不算**破碎、一次都不會發：① 被打到但護盾還有剩；② 護盾**到期消失**；③ 【破盾】主動打掉別人的盾（那是「我去拆別人的」，不是「我的破了」）。⚠️ 第 ③ 項是**已知的缺口不是漏掉的** —— 要不要讓它也算是 owner 的裁決。所以「盾破反擊」寫得出來，「盾一沒了就 ⋯」寫不出來。0 份內容在用。 |
| **連續第 N 次** | 觸發事件 | 計數只在「屬性成長」這一種效果上可用，不能拿來做「每 N 次普攻強化一次」。 |

🧟 = 對小兵無效　🔒 = 只有特定文件類型寫得出來

### ⛔ 已知壞掉：可以顯示、不可以存（**0**）

⭐ **本版這一格是空的。** rev A 的兩筆都在 2026-08-09 處理掉了：

| rev A 的 broken | 現在 | 為什麼 |
|---|---|---|
| **死亡時** `hook:onDeath` | `allowed_with_warning` | 英雄那一半修好（GH#293）；⚠️ **rev C：小怪那一半的成因換了** —— 屍體順序已修（GH#296），但小怪身上掛不住任何卡片，所以仍然寫不出來 → 見上面「已知壞掉」A 段 |
| **復活時** `hook:onRevive` | `allowed` | 復活圈那條實戰路徑修好（GH#294）→ 見上面 B 段 |

⛔ **但這個 state 值一定要留在你的實作裡。** 它今天是 0 筆純屬運氣好 ——
下一次出現「引擎有、schema 收得下、後台選得到、遊戲裡什麼都不發生」的機制時，
它是唯一擋得住作者的閘。把它從 switch 裡拿掉，那一天你會收到一批安靜失效的卡片。

### ⛔ 不可選取（23）

| 標籤 | 分類 | 原因／替代 |
|---|---|---|
| 牆 | 效果／狀態 | 沒有「站著不動的召喚物」這種形式（召喚物一律自動索敵追擊），也沒有「擋住路徑」的碰撞語意。 |
| 環繞衛星 | 效果／狀態 | 缺少掛在宿主身上、每 tick 判定接觸的軌道實體。既有的「環形陣列」是一個展開成一圈射線的模板，不是繞著人轉的東西。 |
| 魔力耗盡 | 效果／狀態 | 沒有「資源跨越門檻的那一刻」這種時機。只能用【週期】輪詢近似，拿不到邊緣語意，也做不到只算一次。 |
| 韌性 | 效果／狀態 | 全遊戲唯一的持續時間倍率只作用於嘲諷。控場時間在套用當下就換成絕對時間，中間沒有任何人可以乘一個倍率。 |
| 對 BOSS | 觸發條件 | 身分在世界裡是有的，但條件葉與傷害過濾兩邊的清單都沒有 BOSS 這個成員，沒有任何葉子讀得到。 |
| 移動 N 距離 | 觸發條件 | 沒有位移累積器，也沒有讀差值的條件葉。 |
| 脫離戰鬥 N 秒 | 觸發條件 | 沒有「最後一次進出戰鬥是什麼時候」這種世界狀態。⚠️ 別拿 hook 的內部冷卻近似——那是「這條 hook 發動後隔多久能再發」，不是「我多久沒打架」。 |
| 附近敵人達 N 人 | 觸發條件 | 五顆條件葉沒有任何計數葉。光環是「對圈內每一個人投影一份」，數量從來沒有被算出來給任何人讀。 |
| 靜止 N 秒 | 觸發條件 | 引擎唯一的「有沒有在動」讀的是**當下速度**，沒有持續時間累積，也沒有葉子讀得到。 |
| 任務完成 | 觸發事件 | 戰場任務系統本身還不存在，連可以掛的事件都還沒有。 |
| 全場勝負 | 觸發事件 | 勝負判定住在遊戲伺服器、不在模擬層，而所有 hook 的派發點都在模擬層裡。模擬層也沒有發任何比賽結果事件。 |
| 升級時 | 觸發事件 | ⚠️ 理由反方向變了：它已經從枚舉裡**刪掉**，今天後台根本選不到它（不再是「選得到但沒有用」）。已裁決不做。 |
| 回合勝負 | 觸發事件 | 同【全場勝負】：回合結算是由外層驅動的，模擬層沒有發回合結果事件。 |
| 回合第 N 秒 | 觸發事件 | hook 沒有任何「起始延遲」欄位，【週期】的第一次觸發一定落在戰鬥第 0 秒。 |
| 格擋時 | 觸發事件 | 格擋是一條**減傷通道**不是一個事件，整支格擋程式零個 hook 派發點。 |
| 殭屍波開始 | 觸發事件 | 模擬層只發「每一隻怪各一次」與「王出現」兩種事件，**沒有波次層級的事件**——這正是【殭屍王出現】做得成而它做不成的原因。 |
| 每回合只發動一次 | 觸發事件 | 內部冷卻的範圍只有「這個來源」與「這個技能槽」兩種，而且單位是秒（上限 300 秒），蓋不住一個回合加中場。 |
| 治療他人時 | 觸發事件 | 治療只送畫面用的通知，不派發任何 hook。⚠️ 而且它還缺一個**決策**：持有者是治療者還是被治療者（那正是它與【被治療時】的差別）。 |
| 第 N 回合起 | 觸發事件 | 可比較的屬性表裡沒有「回合」這一項。世界裡有回合數，但沒有任何葉子讀得到。 |
| 被治療時 | 觸發事件 | 同【治療他人時】的另一半，受方視角同樣不存在。 |
| 購買道具 | 觸發事件 | 購買發生在中場、在模擬層之外，而所有 hook 派發點都在模擬層裡。道具帶的觸發會在裝備時掛上，但「買下去那一刻」觸發不了東西。 |
| 進入範圍時 | 觸發事件 | 沒有進入／離開偵測（光環是逐 tick 無狀態重建的），也沒有「誰在誰的圈裡」的側表。【陷阱】那一族卡就卡在這裡。 |
| 選到增益卡 | 觸發事件 | 同【購買道具】：發牌在模擬層之外。增益卡被選中後會掛成一個來源（它帶的觸發之後照常發），但「選中那一刻」本身不是事件。 |

⚠️ **這 23 個的成本不是均勻的。** 由便宜到貴大致三層（給排序用，不是承諾）：

1. **只差一個廣播器** —— 【治療他人時】【被治療時】（治療事件已經在發了，只差派發）。
   ⭐ 這一層 rev A 有三筆，【隊友陣亡時】已經在 2026-08-09 做掉了 —— 它真的只差一種收件人範圍。
2. **差一顆條件葉或一個枚舉成員** —— 【對 BOSS】【第 N 回合起】【每回合只發動一次】【附近敵人達 N 人】【回合第 N 秒】。
3. **差一整根世界狀態或一個子系統** —— 【移動 N 距離】【靜止 N 秒】【脫離戰鬥 N 秒】【進入範圍時】【韌性】
   【環繞衛星】【牆】【任務完成】【全場勝負】【回合勝負】【購買道具】【選到增益卡】【殭屍波開始】
   【格擋時】【魔力耗盡】。

⭐ **rev A 在第 3 層列的五筆已經不在這張表上了**：【護盾產生時】【護盾破碎時】【狀態被套用的當下】
（GH#300 全部接上）、【隊友陣亡時】（同上）、【驅散免疫】（GH#295 開了 authoring 欄位）。
⚠️ 這說明第 3 層那個「差一整根世界狀態」的估計**偏保守** —— 五筆裡有四筆其實只差一列廣播表。

⚠️ 其中【全場勝負】【回合勝負】【購買道具】【選到增益卡】是同一個結構原因：
那些事情發生在**模擬層之外**（遊戲伺服器那一側），而所有觸發的派發點都在模擬層裡面。

---

# 14 個新標籤（⚠️ **不在上面 139 的分母內**）

這 14 個是 2026-08-08 那一批引擎詞彙帶進來的機制，owner 的原始清單裡沒有對應的名字。
⚠️ **rev B 沒有新增任何一筆** —— 2026-08-09 那一批的新機制全部落在既有標籤上（`blink`→【瞬移】· `launchDistance`→【擊飛】· `applyStatus.stacks`→【層數累積】·`config.weakness@1`→【虛弱】· 四個新觸發事件→四個既有標籤 · `dispellable`→【驅散免疫】），所以 `newTagCount` 仍然是 14、`tagCount` 仍然是 139。
**計算基礎**：可直接使用 7 · 可用但要警告 7 · 合計 14，與上面的 139 分開計。
在 manifest 裡它們住在獨立的 `newTags` 陣列（不是 `tags`），所以 `tagCount` 仍然是 139。

⚠️ 其中三筆是**同一件事的另一個名字**，不是新能力：【免死（標記版）】是【免死】的新路徑、
【迴避（主動授予）】是【迴避】的效果形式、【吞噬／處決】就是【吞噬】。列出來是因為引擎那一側是獨立的東西。

| 標籤 | state | 引擎裡叫什麼 | 限制／補充 |
|---|---|---|---|
| **改冷卻（指名一支技能）** | ✅ 可用 | `modifyCooldown` | 指名方式二選一且**至少要填一個**：技能槽位，或精確引用某一支技能。三種模式：按比例縮短／固定秒數縮短／直接重置。⚠️ 「基準」是一個真的決策欄位：以**剩餘**冷卻為基準（預設）vs 以**基礎**冷卻為基準——「這一招冷卻縮短 50%」在一次性效果裡只有後者是誠實的。⚠️ 它調的是既有的冷卻計時器，跟全域冷卻縮減是**兩條不互通的路**。0 份內容在用。 |
| **加權隨機分支** | ⚠️ 有限制 | `weightedBranch` | ⛔ **規格還沒定案**：機率表的語意（1/6 是「每次獨立擲」還是「六發彈倉」）尚未裁決，在定案之前寫下去的卡，語意會在定案那天靜靜地改變。0 份內容在用。 |
| **資源互換** | ✅ 可用 | `swapResource` | 在結算的同一刻原子交換**資源**（生命／魔力）。⚠️ 它**不是**換位置，也**不是**偷三圍——那兩件今天都寫不出來。0 份內容在用。 |
| **事件數值轉換** | ⚠️ 有限制 | `eventValueConversion` | ⛔ **規格還沒定案**：轉換的基數（原始傷害／減免後傷害／實際失去的血量）尚未裁決，定案那天既有的卡會靜靜地換一個意思。0 份內容在用。 |
| **魔力屏障（以魔力抵傷）** | ✅ 可用 | `manaBarrier` | 用魔力抵傷，必須**明列**擋哪些傷害型別，可設「魔力保留下限」。⚠️ 它排在護盾**之後**、免死**之前**，而「魔力先付還是護盾先付」今天不是欄位。⛔ 對外契約檔（與程式註解）曾寫「接線還沒接」——**那句是假的**，接線在那裡。0 份內容在用。 |
| **延長增益（受傷延長）** | ✅ 可用 | `extendBuff` | 0 份內容在用——你寫的第一張就是這個機制的第一次上線測試。 |
| **隨機落點（空間排程器）** | ✅ 可用 | `randomArea（+ randomAreaSystem）` | 「N 秒後在一片區域跑這串效果」。⚠️ 限制與【延遲落地(通用排程)】那一列完全相同：落點一定是隨機的、錨點只能是自己或目標且在施法那一刻固定、節奏只有等間隔一種。⛔ 對外契約檔曾寫「接線還沒接」——**那句是假的**。0 份內容在用。 |
| **發屬性（力／敏／智）** | ✅ 可用 | `grantAttribute` | ⚠️ 這個機制**早於** 2026-08-08 那一批就存在，只是 2026-08-05 版沒有給它一個獨立標籤（當時只收了它的「每 N 次」那一格當【連續第 N 次】）。給的量必須是正數，而且只認力／敏／智三條。8 份內容在用。 |
| **具名標記（層數）** | ✅ 可用 | `ability@1.marks（zMarkSpec）` | 第二套層數機制：可扣減、可設成永不過期、「什麼時候歸零」是獨立的第二根軸（整場／每回合／從不），「累計失去幾層」不會因為補回層數而倒退。⛔ 只裝在**擁有那支技能的人**身上，**不能貼在敵人身上** —— ⭐ 要貼在敵人身上並問層數，改用 rev B 的**狀態層數**（`applyStatus.stacks` ＋ `condition.status.minStacks`）。⚠️ 層數**不在送給客戶端的欄位裡**，只靠事件推播，所以中途加入／重連／觀戰切換的人在下一次層數變動之前看不到還剩幾層。⚠️ 條件葉**仍然**讀不到具名標記的層數 —— `minStacks` 讀的是**狀態**那個池子，兩者不通。1 份內容在用。 |
| **免死（標記版）** | ⚠️ 有限制 | `ability@1.marks[].lethal（MarkLethalRule）` | ⚠️ 這是 139 個標籤裡【免死】那一列的新路徑（同一件事，不同寫法）——看那一列的完整邊界：**火圈燒傷**（⭐ rev B 起是一格後台開關，⛔ 出貨預設關 ⇒ 今天仍然攔不到）、只保護自己、三選一增益卡沒有這一格。 |
| **切換型技能（開／關按鈕）** | ⚠️ 有限制 | `ability@1.toggle（enterToggle / exitToggle / toggleUpkeepSystem）` | 「按一次開、再按一次關，開著期間持續扣魔，關的時候跑一段收尾效果」。機制真的出貨了，而且關閉出口只有一個（手動關與魔力不足自動關都走它），所以「關閉時釋放一發」不可能只發生一半。⛔ 兩個玩家一定會撞到的缺口：① **開關狀態不在任何送給客戶端的欄位裡**，玩家看不到它是開的（只看得到魔力在掉），斷線重連／觀戰／中途加入一律看成關的；② **被暈眩、被沉默、施法中都關得掉**，而關閉會跑收尾效果——所以不要把收尾效果設計成一發強力傷害。⚠️ 另外屍體關不掉它，也不收維持費，復活後按鈕還開著。0 份內容在用。（複製那一半是 GH#285） |
| **跨技能強化** | ⚠️ 有限制 | `ability@1.augment（collectAugmentOps / applyAugmentToHooks）` | ⛔ **只改得到另一支技能的「被動觸發區塊」**：屬性數字、光環、視野、飛行、格擋五種內容一個都改不到，主動施放路徑也完全問不到它——所以「強化一支主動技的傷害」寫不出來。⛔ 不遞迴、沒有巢狀。可改的操作只有四種且兩端有界：觸發機率、持續秒數、AP 係數、門檻百分比。⚠️ 指向的技能指不到會在**載入時**報錯（好事），但**節點名打錯字是靜默無效，不會報錯**。0 份內容在用。 |
| **迴避（主動授予）** | ⚠️ 有限制 | `evasion` | ⚠️ 這是 139 個標籤裡【迴避】那一列的效果形式（不是新東西，2026-08-05 版就有這個 token）。0 份內容用過效果形式；屬性形式有 15 份在用。⛔ 對殭屍完全無效。 |
| **吞噬／處決** | ⚠️ 有限制 | `devour` | ⚠️ 這是 139 個標籤裡【吞噬】那一列（不是新東西）。⛔ **對無敵／免傷目標，傷害被靜默拒收，但施法者照樣回滿血。** 0 份內容在用。 |

⚠️ **這 14 個裡有 11 個的出貨內容採用率是 0**（三個例外：【發屬性】8 份文件；【具名標記（層數）】與【免死（標記版）】各 1 份，而且是**同一份**技能文件）。

⚠️ 兩筆的**規格還沒定案**（【加權隨機分支】的機率語意、【事件數值轉換】的基數），
在定案之前寫下去的卡，語意會在定案那天靜靜地改變。

---

## 標籤推導規範（規範，不是參考）

前面回答的是「這個標籤引擎做不做得到」。這一節回答另一半：**拿到一段技能文案，該標哪些標籤？**

### 1 · 主動／被動

**優先序（高 → 低）**

1. **文案第一行的 `[被動]` 或 `[主動]`** ← 最高
2. 使用者提供的新文案
3. 既有的文案
4. runtime / `castType` / `innateKind`

⛔ **這一條最容易被實作反：文案凌駕引擎欄位。**
被動技能**即使**資料裡是 `castType: "self"` 或 `"targeted"`、**即使有冷卻時間**，仍然是 `[被動]`。
→ **編輯器不可以用 `castType` 反推主動／被動。** 那個欄位只描述「怎麼瞄準」，不描述「誰按的」。

以下**一律是被動**：

- 每秒、每 N 秒自動觸發
- 普攻時自動觸發
- 技能命中後自動觸發
- 受到傷害、閃避、反彈成功後自動觸發
- 其他技能造成的被動強化
- 生命門檻達成後自動發動

**只有**玩家按下技能按鈕、主動切換、主動變身或主動施法，才是 `[主動]`。

### 2 · `[輔助]`

**兩個條件都成立**才可以標：① 技能是 `[主動]`　② 效果作用於我方英雄或友方單位

⛔ 不能標的：被動技能 · 自身被動強化 · 自身護盾／吸血／反彈／變身 · 自身切換或 self-cast · 敵方傷害／控制／反彈效果

### 3 · 施放方式

| 文案描述 | 標籤 |
|---|---|
| 單一敵方目標 | `[指定]` |
| 指定地點施放 | `[指定][範圍]` |
| 方向、直線、投射物 | `[指向]` |
| 方向性技能同時作用多個敵人 | `[指向][範圍]` |
| 周圍或區域影響多個單位 | `[範圍]` |

⛔ 被動技能不加這三個當施放方式——除非它描述的是**效果作用範圍**（例：被動光環的半徑）。

### 4 · 事件與效果標籤

依文案**明確語意**補，不要推測：

| 文案語意 | 標籤 |
|---|---|
| 普攻觸發 | `[普攻時]` |
| 技能命中觸發 | `[技能命中時]` |
| 受到傷害觸發 | `[受到傷害時]` |
| 生命／魔力門檻 | `[屬性門檻]` |
| 週期觸發 | `[週期]` |
| 機率觸發 | `[機率]` |
| 狀態條件 | `[身上有某狀態時]` |
| 反彈成功後觸發 | `[反彈成功時]` |
| 裝備條件 | `[裝備了某類道具時]` |

⚠️ `[迴避]` 與 `[格擋]` 本身已代表「受到攻擊時的防禦判定」，**不重複**加 `[受到傷害時]`——除非效果鏈需要區分事件。

### 5 · ⭐⭐ 條件要掛在哪裡（**rev B 整條反過來了**，這一條會改變表單結構）

⛔ **rev A 這一節寫的是「條件必須落在一個觸發時機上，不能落在效果清單上」。那已經是假的。**

`[身上有某狀態時]` `[裝備了某類道具時]` `[機率]` `[屬性門檻]` 這一類**條件**，
在導出 JSON 時**兩個位置都合法**：觸發時機上，或**那一段效果自己**上。

- 文案只說「若敵人具有〔恐懼〕則額外追加傷害」而沒說什麼時候判定
  → ⭐ **預設掛在那一段效果上**（形狀與作者腦中的一樣）。
- ⛔ **唯一的例外**：那一段如果是 `damageArea`（範圍傷害）／`damageLine`（直線傷害）／
  `randomArea`（隨機落點）／`leap.onLand`（落地）這種**自己解目標**的效果，
  效果上的 `subject:"target"` 條件會**整段不發** —— 那一種仍然要繞回一個觸發時機
  （【技能命中時】+ 只有這一支技能的槽位過濾）。

詳見「六個最容易做出啞卡的地方」第 1 條（含語意表與陷阱）。

> 機器可讀版本在 `skill-tag-manifest.json` 的 `derivationRules.conditionPlacement`
> —— ⚠️ **rev B 把那個物件整個換掉了**（多了 `semanticsOnEffects` 與 `defaultForDerivation`，
> `rule` / `formShape` / `why` 三格的內容與 rev A **相反**）。照 rev A 的值做表單會是錯的。

---

## 附帶的機器可讀檔：`skill-tag-manifest.json`

下拉選單**請由這個檔驅動**，不要把清單硬寫進程式碼 —— 引擎補上功能時只要換檔案，編輯器不用改。

```json
{
  "schemaVersion": 1,
  "generated": "2026-08-09",
  "supersedes": "2026-08-05",
  "engine": {
    "contract": "ggd-runtime-capabilities@1",
    "contractFingerprint": "ef984bcc",
    "head": "8c54fdb0 ＋ 未 commit 的 2026-08-09 引擎批次（GH#299/#300/#301）",
    "measuredOn": "2026-08-09"
  },
  "tagCount": 139,
  "newTagCount": 14
}
```

⚠️ **`generated` 三個 rev 都是 `2026-08-09`**（同一天出了三版）。
⛔ **rev B 與 rev C 的 `engine.contractFingerprint` 也一樣**（都是 `ef984bcc`）——
指紋只推導**詞彙表**，而 rev C 改的是既有機制的行為與授權格，所以指紋分辨不出來。
⇒ **要分辨手上是哪一版，看 `supersedes` 與 `engine.head`**：
`supersedes: "2026-08-05"` ＋ head `d199d3eb` = rev A（指紋 `e6843214`）·
`supersedes: "2026-08-05"` ＋ head `8c54fdb0 ⋯` = rev B ·
`supersedes: "2026-08-09 rev B ⋯"` ＋ head `4e3ebc88 ⋯` = **rev C**。

每一筆 tag 的欄位：

| 欄位 | 說明 | 本版 |
|---|---|---|
| `tag` | 中文標籤名（設計端使用的詞） | |
| `group` | 分類：activation / cast / scaling / movement / effect / condition / event | |
| `state` | `allowed` / `allowed_with_warning` / `blocked` / **`broken`** | ⭐ 多一個值 |
| `engineToken` | 對應的引擎欄位或列舉值，可用來產生輸出的 JSON | ⚠️ rev B 改了 13 筆，**rev C 再改 5 筆** |
| `authorWarning` | **必須常駐顯示**的警告（`allowed` 時為 null；`broken` 時是「壞在哪裡」；`blocked` 時是「為什麼沒有」） | |
| **`authorNote`** | `allowed` 但仍有非阻斷補充說明時的文字。可以收在展開區，**不要整個丟掉**。沒有時為 null | ⭐ 新欄位 |
| **`workaround`** | `broken` 專用：今天該怎麼繞過。沒有時為 null | ⭐ 新欄位 |
| **`issue`** | 追蹤編號（例：`GH#293`）。沒有時為 null | ⭐ 新欄位 |
| `hasShippedExample` | false = 引擎支援但目前沒有任何已上線內容用過，第一次使用建議小規模驗證 | |
| `authorableIn` | 有此欄位代表只有列出的文件類型寫得出來 | ⚠️ 多兩個值 |
| `noEffectOnMinions` | true = **屬性那一半**對小兵永久無效 | ⚠️ rev B 語意收窄了，見下 |

⚠️ **`noEffectOnMinions` 的語意在 rev B 收窄**：它現在只說「這個標籤的**屬性**那一半對小兵無效」。
【破甲】【破防】【破魔】仍然是 `true`，但它們的**狀態**那一半掛得上小兵、條件葉也查得到（owner #299-10）。
⭐【虛弱】從 `true` 改成 `false`（它有一半對小兵成立）。⛔ 不要把這一格顯示成「這個標籤對小兵完全沒用」。

`authorableIn` 的值：`item`（道具）· `innate`（天生技）· `ability`（技能，含天生技）·
`abilityPassive`（技能的被動區塊，含天生技）· ⭐ **`augment`**（三選一增益卡）·
⭐ **`applyBuff`**（限時授予 —— 主動技能透過它給出一個有期限的來源）。
⚠️ 後兩個是 **rev C 新增**的值，今天只出現在【格擋】那一筆；`ability` / `abilityPassive` 是 rev B 加的。

頂層另有：

- `states` —— 四個 state 各自的編輯器行為（**多了 `broken` 一格**）。
- `stateOrder` —— 建議的顯示順序：allowed → allowed_with_warning → broken → blocked。
- `counts` —— 四態計數 + 上一版計數（`counts.previous`），計算基礎寫在 `counts.basis`。
- `notes` —— 讀這份檔之前要知道的 **10** 條（rev C 多一條、改寫第 9 條）。
- **`newTags`** —— 上面那 14 個新標籤，欄位形狀與 `tags` 完全相同。**它們不在 `tags` 裡**，
  所以 `tagCount` 仍然是 139，既有的「139 列」假設不會壞。
- `derivationRules` —— 上一節那份規範的機器可讀版本。⛔ **rev B 把 `conditionPlacement` 整個換掉了**
  （`rule` / `why` / `formShape` 的內容與 rev A **相反**，另外多了 `semanticsOnEffects` 與
  `defaultForDerivation`）。

## ⚠️ breaking change

### 0 · rev B → rev C（**如果你已經接了 rev B，只需要讀這一段**）

**結構沒有變，`state` 也一格都沒有變**（64 / 52 / 23 / 0）。變的只有**七格的文字與五格的 `engineToken`**
—— 但其中**四格是「rev B 叫你繞路，而繞路今天是白繞」**，那是最貴的一種假訊息，所以這一段不可以跳過。

| # | 改了什麼 | 誰會壞 | 怎麼改 |
|---|---|---|---|
| 1 | **7 筆 `authorWarning` / `authorNote` 換值**（衝擊波 · 暴擊 · 反彈 · 格擋 · 死亡時 · 層數累積 · 反彈成功時） | 把警告文字**複製進自己程式**的編輯器（rev B 那四句在叫作者避開一個其實可用的機制） | 一律由 manifest 驅動，⛔ 不要快取警告字串 |
| 2 | **4 筆 `issue` 從 `"GH#299"` 變 `null`**（衝擊波 · 暴擊 · 反彈 · 格擋） | 拿「有沒有 issue」當「這一格能不能用」的 UI | `issue` 只是註記，能不能用永遠看 `state` |
| 3 | ⭐ **【格擋】的 `authorableIn` 從 2 個變 4 個**（＋`augment`、＋`applyBuff`） | 依文件類型過濾選項的表單 —— 它今天會把三個**其實合法**的位置擋掉 | 重讀 `authorableIn`，不要硬寫 |
| 4 | **5 筆 `engineToken` 換值**（見下表） | 用舊 token 產 JSON 的程式 | 重讀 manifest |
| 5 | ⭐ 【層數累積】**「HUD 顯示不了層數」那句話反過來了** | 照 rev B 把「層數」從卡面文案裡拿掉的內容 | 層數現在真的送到客戶端，文案可以講 |

**rev C 換掉的 5 筆 `engineToken`：**

| 標籤 | rev B | **rev C** |
|---|---|---|
| 【衝擊波】 | `tpl-traveling-wave` | **`spawnProjectile ＋ projectile@1.pierce`**（穿透住在**投射物文件**上，不在技能上） |
| 【暴擊】 | `⋯ / damage.canCrit / HookDef.damageCrit` | ＋ **`critStrike` 授予（四個授權面）＋ `config.crit@1`**（三格後台參數） |
| 【反彈】 | `damage.incomingPct（＋新的 onReflectSuccess 時機）` | **`damage.incomingPct ＋ hook:onReflectSuccess`**（量過：對象＝原攻擊者） |
| 【格擋】 | `item@1.block ＋ ability@1.passive.ranks[].block` | **`BlockGrant`（一份定義，四個授權面）** |
| 【層數累積】 | `⋯ ＋ applyStatus.stacks（＋ condition.status.minStacks）` | ＋ **`ability@1.marks.roundDelta`** ＋ **`SeatState.counterIds/counterCounts`**（層數上線路） |

### A · rev A → rev B（**如果你已經接了 rev A，這一段也要做**）

**結構完全沒有變**：沒有新增／刪除任何欄位、沒有改型別、沒有改頂層 key、
`tags` 仍然 139 列、`newTags` 仍然 14 列。壞的不是形狀，是**值**：

| # | 改了什麼 | 誰會壞 | 怎麼改 |
|---|---|---|---|
| 1 | ⭐⭐ **`derivationRules.conditionPlacement` 的三格內容與 rev A 相反** | 照 rev A 建表單的編輯器 —— 它會把「條件」只做成 hook 卡片的子區塊 | 條件改成 **hook 卡片與效果卡片共用**的子區塊。⛔ 這一項的成本最高：不改的話，作者寫不出一半合法的 JSON |
| 2 | **8 筆 `state` 換了**（見上面 rev B 總帳） | 硬寫了「哪些標籤不能選」的程式 | 一律由 manifest 驅動，不要快取 |
| 3 | **`broken` 現在是 0 筆** | 拿 `broken` 陣列長度當「有沒有壞掉的東西」的程式 | 保留分支，不要移除 —— 見上面「已知壞掉：0」那一節 |
| 4 | **13 筆 `engineToken` 換了值**（見下表） | 用舊 token 產生 JSON 的程式 | 重讀 manifest |
| 5 | **9 筆 `allowed` 的 `authorWarning` 從一句過期的「尚未實作 ⋯」改成 `null`** | 只看「有沒有 authorWarning」而不看 state 的 UI | 用 `state` 判斷該不該常駐顯示，不要用「欄位有沒有值」 |
| 6 | **`noEffectOnMinions` 的語意收窄**（只指屬性那一半） | 把它顯示成「對小兵完全沒用」的 UI | 改文案；【虛弱】那一筆從 `true` 變 `false` |

**rev B 換掉的 13 筆 `engineToken`：**

| 標籤 | rev A | **rev B** |
|---|---|---|
| 【瞬移】 | `leap` | **`blink`**（effects/blink.ts ＋ movement/blink.ts） |
| 【擊飛】 | `knockback.launchHeight` | ＋ `knockback.launchDistance`（四檔） |
| 【混亂】 | `applyStatus.targetsAllies` | 同名，⛔ **語意整個換掉** |
| 【虛弱】 | `applyBuff{stat:"ad",…}` | **`config.weakness@1` ＋ 狀態文件的 `tags:["weakness"]`** |
| 【層數累積】 | ⋯ ＋ `ability@1.marks` | ＋ `applyStatus.stacks`（＋ `condition.status.minStacks`） |
| 【身上有某狀態時】 | `condition.status{subject, statusId\|tag}` | ＋ `minStacks`，而且 **hook 上與效果上都掛得了** |
| 【標記】 | `condition.status{…}` | `applyStatus`（貼）＋ `condition.status{…}`（讀） |
| 【淨化】 | `dispel{pools:{…}}` | ＋ 來源側的 `dispellable` |
| 【驅散】 | `dispel（＋config.dispel@1）` | ＋ 來源側的 `dispellable` |
| 【驅散免疫】 | `null` | **`applyStatus/applyBuff/dot 的 dispellable`（寫 false）** |
| 【護盾產生時】 | `null` | **`hook:onShieldGained`** |
| 【護盾破碎時】 | `null` | **`hook:onShieldBroken`** |
| 【隊友陣亡時】 | `null` | **`hook:onAllyDeath`** |
| 【狀態被套用的當下】 | `null` | **`hook:onStatusApplied`** |

（表上是 14 列，其中【身上有某狀態時】那一筆嚴格說是**同一個 token 加欄位**，
所以「換了值」算 13 筆。）

### B · 2026-08-05 版 → rev A（如果你還在 2026-08-05 那一版，這一段也要做）

結構是**向後相容**的：沒有刪掉任何欄位、沒有改任何欄位的型別、沒有改頂層陣列 `tags` 的長度。
但有四件事會讓「嚴格」的實作紅：

| # | 改了什麼 | 誰會壞 | 怎麼改 |
|---|---|---|---|
| 1 | **`state` 多一個值 `broken`** | 對 state 做**窮舉** switch / 用三值 enum 驗證的程式 | 加一個分支：顯示得出來、但**不可存**，文案用「引擎有，但今天是壞的」 |
| 2 | **`authorableIn` 多兩個值**（`ability` / `abilityPassive`） | 把該欄位當成「item \| innate」二值的程式 | 當成開放字串集合處理 |
| 3 | **多三個欄位**（`authorNote` / `workaround` / `issue`），而且**每一筆 tag 都有這三格**（沒有值時是 `null`） | 用 `.strict()` / `additionalProperties: false` 驗 tag 物件的程式 | 放寬，或把三個欄位加進 schema |
| 4 | **多一個頂層陣列 `newTags`** | 假設頂層只有既定 key 的程式 | 忽略它，或一起讀（欄位形狀與 `tags` 相同） |

⛔ **另外有 11 筆 `engineToken` 改了值**（另有 13 筆從 `null` 變成有值）：

| 標籤 | 舊 | 新 |
|---|---|---|
| 【反彈成功時】 | `onReflect` | `onReflectSuccess` |
| 【迴避】 | `evasion / Stat.Evasion` | `evasion / Stat.Evasion（＋新的 onEvade 時機）` |
| 【反彈】 | `damage.incomingPct` | `damage.incomingPct（＋新的 onReflectSuccess 時機）` |
| 【暴擊】 | `Stat.CritChance / Stat.CritDamage / damage.canCrit` | `⋯ / HookDef.damageCrit` |
| 【格擋】 | `item@1.block` | `item@1.block ＋ ability@1.passive.ranks[].block` |
| 【淨化】 | `dispel` | `dispel{pools:{status,dot,shields,buffs}}` |
| 【減傷】 | `Stat.Armor / Stat.MagicResist` | `⋯ ／ manaBarrier` |
| 【免死】 | `item.block` | `ability@1.marks[].lethal ＋ item@1.block.lethalOnly` |
| 【層數累積】 | `applyBuff.stackKey / maxStacks / stackVisual` | `⋯ ＋ ability@1.marks` |
| 【標記引爆】 | `damage.comboBonus / damage.bankedBonus` | `⋯ ＋ condition.status{subject:"target"}` |
| 【驅散】 | `dispel` | `dispel（＋後台可調的 config.dispel@1）` |

⛔ **`onReflect` 是唯一會讓文件載入直接失敗的**：它已經從枚舉裡刪除。其餘是「多了一條新路」，
舊值仍然有效，但只寫舊值會拿不到新能力。

## 已知缺口（不隱藏）

- ⚠️ **owner 的原始清單其實有 140 個標籤，這份表只有 139。** 差的那一個是**【離開範圍時】**——
  2026-08-05 版沒有收它，本版也仍未個別查證。它與【進入範圍時】同屬尚未實作的範圍事件，
  實際上應該一樣是 `blocked`，但因為沒有查證，這份文件**不替它背書**，也不把它算進任何一格計數。
  ⛔ 編輯器如果照 owner 的清單建下拉，這一個會落在表外 —— 請當成 `blocked` 處理。
- **對外契約檔（指紋 `e6843214` 的前一版）曾經有兩句過期的 caveat**，方向是**低報**自己的能力
  （魔力屏障與隨機落點兩個機制寫「接線還沒接」，實際上接線就在那裡）。本版的裁決以**讀程式**為準，
  不以契約的 caveat 為準。⚠️ 意思是：**契約檔的散文欄位不是可靠來源，枚舉欄位才是。**
- **兩筆規格未定案**（【加權隨機分支】的機率語意、【事件數值轉換】的基數）。
  它們今天是 `allowed_with_warning`，但語意會在定案那天改變 —— 那**不是**引擎壞了，是規格動了。
- **採用率 0 的「可選」標籤有 51 個**（rev B 是 52，**−1**）。
  **計算基礎**：`hasShippedExample === false` 且 `state` 不是 `blocked` 的筆數，直接從 manifest 數出來 ——
  139 個標籤裡 **40** 個（`allowed` 22 ＋ `allowed_with_warning` 18）＋ 14 個新標籤裡 **11** 個
  （`allowed` 5 ＋ `allowed_with_warning` 6）= **51**。
  ⭐ **少的那一個是【衝擊波】** —— rev B 把它記成 `hasShippedExample: false`，
  而實際上有 **76 份**出貨技能在用那條路。⛔ 這是同一個誤判的第二個後果：
  一個錯的「引擎沒做」會連帶把採用率也記成 0。
  ⚠️ rev A 這一行寫「29 個（18 ＋ 11）」，那個 18 只數了 `allowed` 而漏了 `allowed_with_warning` 那一半 ——
  **rev B 起用的是全部可選的口徑**，所以數字變大不代表採用率變差。
  引擎支援 ≠ 有人跑過。rev A 的【死亡時】【復活時】就是這樣帶著全綠的測試出貨的。
- **rev B 沒有重跑全表。** 它重跑的是：owner #299 點名的 10 條、GH#293/#294/#295/#287/#286 的
  受影響列、以及 GH#300/#301 新落地的機制。⛔ 其餘的列沿用 rev A 的裁決，
  而 rev A 對 2026-08-05 那 52 個「可直接使用」只做過**抽查**。⚠️ 抽查不是全查。
- ⛔ **rev B 明確沒有查證的一項**：【混亂】對**殭屍**的完整行為。混亂的接線在英雄那條路上有守衛，
  但殭屍的目標指派住在另一支系統、跑在混亂那一步之後、也從不問這一格 ——
  所以這份文件把「對整波殭屍下混亂」當成**不成立**，而不是宣稱它成立或不成立。
- ✅ **owner #299 的第 1、2、5、6 條（衝擊波 / 暴擊 / 反彈 / 格擋）rev C 收掉了**，
  四筆的 `issue` 已經清空。⚠️ 但**剩兩件沒有任何 issue 在追**，兩件都是 rev C 才看清楚的：
  ① **「格擋的那一刻」不是一個時機**（【格擋時】仍然 `blocked`，實測零個派發點）；
  ② **小怪身上掛不住任何卡片**（沒有屬性表）—— 這是【死亡時】小怪那一半**真正**的成因，
  而 GH#296 追的是**已經修好**的那一半（屍體銷毀順序）。⛔ 別把 GH#296 讀成「小怪死亡時可以用了」。
- ⛔ **rev C 沒有重跑全表。** 它重跑的是：owner #299 剩下的 4 條、以及 GH#296 / GH#302 / GH#304
  的受影響列（共 7 格）。其餘沿用 rev B。⚠️ **而 rev C 抓到 rev B 有一格是純誤判（衝擊波）** ——
  誤判的成因是「讀了註解沒讀資料」，⛔ 所以請把「沿用」讀成「還沒有人重新量過」，不是「已驗證」。
- ⚠️ **契約檔（`docs/editor-contract/ggd-runtime-capabilities.json`）的 `knownBroken[0]` 這一刻是過期的**：
  它逐字寫著小怪的死亡「在 `worldHookSystem` 之前就 `destroy` 掉屍體」，而**那個成因已經修掉了**。
  結論（小怪那一半不能用）仍然成立，但**理由已經換人**。⛔ 這份文件以本頁為準；
  那個檔不在本文件的維護範圍內。
  今天這四筆的追蹤點只有 GH#299 的表格本身 —— ⚠️ 如果你要排這四件的實作順序，**先開 issue**。

