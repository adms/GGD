# 🎯 Codex 編輯器的 goal（2026-09-01）

> ⭐ 這一份給的是**驗收標準**，⛔ 不是工作清單。
> 「往前怎麼走」在 `CODEX_COORDINATION_20260831.md`，「差多少」在 `CODEX_CATCHUP_20260830.md`。

---

## ⭐ 0. 一句話

**做出一個「玩家能自己設計英雄／技能／特效」的 no-code 介面 ——
⭐ 而它的正確性由 main 的契約定義，⛔ 不由你我的判斷定義。**

⇒ ⭐ 判準只有一條：**契約裡的每一格，編輯器都碰得到；編輯器碰得到的每一格，契約裡都有。**

---

## ⭐ 1. 驗收標準（五條，全部可機械檢查）

| # | 標準 | 怎麼驗 |
|---:|---|---|
| **1** | ⭐ **契約指紋對得上** | `ggd-runtime-capabilities.json` 的 `fingerprint` ＝ 你那邊存的那一份 |
| **2** | ⭐ **雙向零缺口** | 契約有而編輯器碰不到 ⇒ 紅；編輯器碰得到而契約沒有 ⇒ 也紅 |
| **3** | ⛔ **`_ownerOnly` 那 39 格不做欄位** | 它們是 owner 的人工旋鈕。⭐ 編輯器**只編原始資料（五級距）**，⛔ 不需要知道那些值（owner 2026-08-23：「避免雙重編輯」） |
| **4** | ⭐ **產出的 JSON 通得過出貨驗證** | 丟進 `content:build` 不紅（Zod strict ＋ `noOpModifierClaims`） |
| **5** | ⭐ **驗收招式做得出來** | owner 點名的三招，特別是 **20-002 理想鄉EX**（`anim.on = reflectSuccess` 的第一份內容**刻意留給你**） |

---

## ⭐ 2. 今天的契約規模（main 的現值）

| | |
|---|---|
| `ggd-editor-coverage.json` `_total` | ⭐ **4,840 格** |
| 其中 owner 專屬（⛔ 不做） | `_ownerOnly` **39** |
| effect kinds · config 欄位 | 46 · **3,741** |
| `ggd-runtime-capabilities` fingerprint | 見該檔（⭐ 每次 `git pull` 後重讀） |

⚠️ ⭐ **你的分支上那份是 1,096 格** —— 它是**四個 merge 之前**的快照。
⇒ ⛔ 第一件事不是寫功能，是**追上契約**。

---

## ⭐ 3. 分工紅線（⛔ 兩邊都不要越過）

| 誰 | 做什麼 | ⛔ 不做什麼 |
|---|---|---|
| **main** | 引擎機制 · 契約產生器 · 出貨內容 · 玩家那一層 | ⛔ 不碰 `apps/editor/**` |
| **Codex** | `apps/editor/**` 的一切 | ⛔ 不改引擎 schema／registry —— ⭐ **缺機制就開票給 main**，⛔ 不要自己加一個 |

⚠️ ⭐ 你的分支現在動了 **17 個 `apps/editor` 以外的檔**（`packages/shared` 6 · `apps/client` 5 ·
`apps/content-api` 4 · `tools` 1 · `docs` 1）。
⇒ 那些**要分開評審**：它們是引擎面，⛔ 不是編輯器面。

---

## ⭐ 4. 合併前的四道閘（⛔ 缺一條就不合）

```bash
git merge origin/main          # ① 先追上 —— 你落後 main 59 個 commit
pnpm typecheck                 # ② 全綠
pnpm test                      # ③ 全綠（⭐ 你帶了 21 個測試檔，很好）
bash scripts/genrun.sh editorcov:build && git diff --exit-code docs/editor-contract/
                               # ④ ⭐ 契約重生成之後**逐位元組不變**
```

⚠️ ④ 是最重要的一條：⭐ 它證明你**沒有偷偷改契約來配合編輯器** ——
契約只能因為**引擎長出新機制**而變，⛔ 不能因為編輯器想要而變。

---

## ⭐ 5. 引擎缺機制時怎麼辦（⛔ 不要自己加）

開一張票給 main，帶五件：
`[優先級 tag]` `[類型 tag]` `驗收標準` `[思考策略]` `[解決模板]`

⭐ **而且要寫「這個缺口擋住幾支技能」** —— main 照**擋住的支數**排序，
⛔ 不照票號、⛔ 也不照誰先開。

---

## ⭐ 6. 現在最該做的三件（我的建議，⛔ owner 有最終決定權）

1. ⭐ **追上契約**（1,096 → 4,840）—— ⛔ 在這之前做的每一個欄位都可能是舊的
2. ⭐ **把那 17 個引擎面的檔拆成獨立 PR** —— 它們與編輯器混在一起無法評審
3. ⭐ **做出 20-002 理想鄉EX** —— 那是 owner 點名的驗收招，而機制側四段接縫已經全通
