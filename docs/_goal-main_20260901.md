# 🎯 main 的 goal（2026-09-01 v2）—— **最快關票**

> ⭐ 大目標（owner 逐字，⛔ 不可偏離）：
> 「**開放讓玩家自己設計 英雄、技能、特效**，不是靠 AI 無止境的逼近太沒效率」
> 「所以**後台編輯器的抽象化、完整性、視覺化可操作性**很重要，
>  因為所有功能都要**可 JSON 操作設定**，並且也有 **no code 遊戲引擎等級的操作介面**」

⇒ ⭐ **main 只負責三件**：①引擎每個功能可 JSON 操作 ②契約說得出真話 ③玩家那一層。
⛔ 其餘（特效移植、內容批、視覺驗收）**全部給 Codex 編輯器** —— owner 2026-09-01 逐字。

---

## ⭐ 0. 硬規則（⛔ 這兩條是 owner 親自改的）

| # | 規則 |
|---:|---|
| **1** | ⭐ **每一段落都要 BMPNDD** —— owner：「**這才是硬規則**」。⛔ 而且要跑 `bash scripts/bmpndd.sh "<一句話>"`，⛔ **不是手打重組**（我漏掉七版 Discord 就是這樣來的） |
| **2** | ⭐ **關鍵票照開**（⛔ 擋住連環問題的要開）；非關鍵的**註記在現有票裡就好**，先不開 |

---

## ⭐ 1. 我今天該做的只有四張（owner 逐字點名）

| 票 | owner 說什麼 | 狀態 |
|---|---|---|
| **#730** | 「**do it quick**，這是你少數分配要做好的事情，**專心做好**」 | ✅ **關了**（v0.34.21）—— ⭐ 上一次卡在「要不要部署寫入 API」，而量下去發現那**不是翻旗標**，是拆掉一道刻意的安全邊界（`NODE_ENV=production` 直接 throw）。⛔ 不做；⭐ 而正確的那條路（durable overlay）早就在正式後台上 |
| **#841** | 「**快做阿**」 | ✅ **關了**（v0.34.5） |
| **#863** | 「你真靠北 **拖到現在還沒做完**」 | ✅ **關了** —— ⚠️ 五條 AC **本來就全滿**，票文過期 |
| **#813** | 「**看不懂拉幹**」 ⇒ ⭐ 我要先用白話重寫這張票，⛔ 不是直接做 | ✅ **關了** —— 白話版在票上；⭐ 而「一輪真遊玩」那條路**走不通**（owner：你不用玩遊戲測試），⇒ 違規清單改成**推導**：三類全 0，那個「已知必爆」的 iframe 檔頭自己寫著 production 永遠不會產出它 |

⭐ **四張之外收掉的**（都在同一個 goal 底下：讓引擎的每一格都可 JSON 操作）：

| 票 | 做了什麼 | 狀態 |
|---|---|---|
| **#761** | `vfx@1` 長出 KP2* 八條時間軌 · `spawnModelFx` 收得下骨頭掛點（詞彙照抄 `spawnVfx`）· `extra` 表歸零的**輸入**變成內容 | ✅ 六 AC 全綠 |
| **#473** | 後台「為什麼這一格開不了」改讀**預先算好的**稽核表（725 id · 2,175 列） | ✅ |
| **#327** | 外部匯入包走 `parseWithUnknownFieldReport` ⇒ 多餘欄位**說得出名字** | 🟡 鏈路已接上⛔未驗收（要一份真的外部包，那是 Codex 的產出） |
| **#629** | 怪物聚仇的候選索引提到迴圈外 ⇒ **28.5ms → 0.6ms（47.7×）**；⭐ 空間雜湊換扁平 CSR ⇒ **1.34×** | ✅ **關了**（v0.34.20）—— ⛔ 票文的 3.4× 沒達到，⭐ 而原因量到了：瓶頸在**查詢**不在建表 |

⭐ **#629** owner 提示：「你不是要做一個**超屌的新的資料結構**可以超大幅解決」
⇒ 那是我自己提過的方案，⭐ 要把它寫進票再做，⛔ 不是重新想。

---

## ⛔ 2. 交給 Codex 的（⭐ 我只寫進 goal md，⛔ 不做）

`#547`（特效移植 342/384）· `#623`（變身態逐對退場）· `#664`（HITL Phase 2）
· `#650` `#803` `#736` `#734`（🟡 只差終端證據那一批）· `#244`（JASS 模板，⭐ 要先分工）

⇒ ⭐ 全部寫進 `docs/editor-contract/GOAL_CODEX_20260901.md`，⛔ 我不碰。

---

## ⭐ 3. 每張票七步（⛔ 不跳不加）

```
① 前提回驗   ticket-progress.sh read <N> ⭐ 先讀進度標記，⛔ 不是票文
② 量         一個指令量出現況（⛔ 不憑印象）
③ 做         三個住處 ＋ 決策住 game/ 或 sim/
④ 守         一條承重守衛 ＋ 一次突變（⭐ 要真的紅過）
⑤ 產物       editorcov:build → caps:export → content:build（⭐ 順序固定）
⑥ 三處紀錄   ticket-progress.sh（⭐ **一定帶 --commit 與 --player**）· ruling.sh · 計畫表
⑦ BMPNDD     ⭐ bash scripts/bmpndd.sh "<一句話>" —— ⛔ 就這一行
```

⚠️ ⭐ **⑥ 的 `--player` 是玩家公告的唯一來源** —— 沒寫就退化成「系統優化更新」。
⛔ 而且不可以講實作細節（檔名／欄位／票號／**後台** ⇒ 閘會擋）。

---

## ⛔ 4. 五個我這輪踩出來的坑（⭐ 每一個都有閘了）

1. ⛔ **手打重組 BMPNDD** ⇒ Discord 七版全漏。閘：`everyTagAnnounced.test.ts`
2. ⛔ **`ticket-progress.sh` 整段覆寫** ⇒ 第二次不帶 `--player` 就抹掉它。已改成**沿用**
3. ⛔ **python 編輯腳本累積到最後才寫檔** ⇒ 中間失敗丟掉前面成功的編輯**而它印過 ✓**
4. ⛔ **改動打壞既有測試就改測試** ⇒ 先問「那是**設計**還是過期」
5. ⛔ **憑印象填值** ⇒ 寫之前先算（讀型別／算排序／查合法值）

---

## ⭐ 5. 節奏

一張票收乾淨才開下一張 · 每段落一次 BMPNDD · context 95% 存快照 · 收工 `noStrandedLaneCommits`

---

## 🎯 main 三件責任的**量尺**（2026-09-01 補上 —— ⭐ 在此之前它們是三句話）

| 責任 | 量尺 | 今天的數字 |
|---|---|---|
| ① ⛔ 沒有只能改程式才碰得到的角落 | `ops/codeOnlyKnobs.test.ts`（棘輪） | 母體 **132**，其中「⛔ 待判」**70** |
| ② 契約說得出真話 | `content/editorCapabilities.test.ts`（雙向）＋ `ops/editorCoverageFresh.test.ts`（新鮮度）＋ ⭐ **新** `ops/editorContractOnlyGrows.test.ts`（棘輪） | **4,895 格**，只准往上 |
| ③ 玩家那一層 | ⛔ **沒有量尺** —— 它還沒有東西可以量 | 見下 |

### ⭐ ①：那 70 個「待判」是什麼
每一個都是一個「owner 想改的時候**要改程式**」的角落。
⚠️ 而豁免是**規則**（誤打守衛／上下界柵欄／數值容差／協定位元），
⛔ 不是一張 132 列的名單 —— 名單會過期，規則不會。

⚠️ ⭐ 這支量尺**自己被灌大過兩次**（296 → 149 → **132**），
而我是**逐個抽驗其中三個**才發現的。⇒ 那次抽驗被釘成測試的第 ③ 條。

### ⭐ ②：這個 session 證明了那條鏈是真的
`vfx@1` 長出 KP2* 八軌 ⇒ `vfxField` **53 → 86**；
`spawnModelFx` 收得下骨頭掛點 ⇒ **4,873 → 4,874**；
`damageTierPerRank` ＋ `summon-agent` 家族 ⇒ **4,895**。
⇒ ⭐ 引擎長出機制、契約自動變長，⛔ 而在此之前**沒有東西擋得住它變短**。

### ⛔ ③：玩家那一層量到的缺口（GH#908）

| 四塊 | 今天 |
|---|---|
| **身分** | ⭐ 有（`internal/auth` 31 檔 ＋ `account` 3 檔） |
| **審核** | ⭐ 有骨架（`internal/curation` 14 檔）⚠️ 而它審的是**出貨白名單**，⛔ 不是玩家投稿 |
| **投稿** | ⛔ **零** |
| **發現** | ⛔ **零** |

⭐ 而它**不是從零開始**：投稿包的格式（`zEditorImportPackage`／`parseImportPackage`，#327）、
有驗證的寫入路（durable overlay，#730）、人審流水線（`tools/review/`，#664 Phase 1）、
契約（4,895 格）**四個零件都在** ⇒ ⭐ 缺的是**把它們串起來的那條線**。
