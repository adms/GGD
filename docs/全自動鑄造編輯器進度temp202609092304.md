# 全自動鑄造編輯器進度 temp202609092304

核對日期：2026-09-09（Asia/Taipei）。本次為 Main 整合段落的差異快照；前份為 `全自動鑄造編輯器進度temp202609091000.md`（實際核對 22:01）。MVP 仍未完成；本文件不啟動 Phase 2／3。

## 已完成

- 第一批 PR [#1135](https://github.com/adms/GGD/pull/1135) 與第二批 PR [#1153](https://github.com/adms/GGD/pull/1153) 都已整合固定 Main `0de629f9e`／`v0.42.22`；沒有追後续僅公告的版本。
- 第一批兩次合併的衝突已解決；原 37 名 projects／refinements 對 `2cdc3f902`、第二批 37 名 projects／source-lock 對 `66172c84` 均無差異。74 名／444 槽原有內容、名稱與模型綁定保留。
- 編輯器契約、素材索引與版本文件沿既有產生器同步。素材清單只補 Main 已有的 4 個模型索引，原 1650 筆不變；未為四筆新增索引下載模型、重新配對或生成圖片。第一批既有 timeStop 的中文標籤補用 UI 原有「局部時停」，不改機制。
- Main 未入 Git 的當日帳本與原文封存已原樣恢復到功能分支；第二批缺少的當日戰情文件也恢復原檔。第一批原有戰情文件保留，由既有產生器更新七天窗。Main 工作樹未改動。
- [#1155](https://github.com/adms/GGD/issues/1155) 已實作：一般房間舊測試要求排除已核准英雄，與新預設相反；只修斷言與過期說明，兩個分支均四項通過。待 Main 合併才符合該票關閉條件。

## 驗證結果與限制

| 項目 | 本次結果 |
|---|---|
| 第一批 Editor release | 通過：604 項、型別檢查、production build |
| 第一批既有角色連動／阿薩謝爾 | 3 檔、107 項通過；包含極大範圍嘲諷、先強化再進賢者時間的既有機制 |
| 一般房間 | 修正前 3 過／1 失敗；修正後兩分支均 4 過；無遊戲行為變更 |
| 第二批 Editor release | 原聚合停在 README 指紋舊值；修正後該單項通過、build 通過。其他既有通過結果沿用，未宣稱修正後重跑整個聚合 |
| 真正收件路由與政策來源 | 3 個指定 Go 測試通過；不等於正式服務設定已配置 |
| coord | 兩分支各 27 份 packet、0 缺件；歷史收據仍保留其原提交與適用範圍 |
| skills | 分段接續原清單，已處理實際過期的產物。**仍非全綠**：`iconplan:check` 缺本機 `data/curation/whitelist.json`；保留 Main 原始 icon-plan，沒有把本機缺檔狀態當出貨配置，也沒有建立或修改白名單 |
| 訊息帳本 | 既有非嚴格檢查通過；仍有當日未對票紀錄，不能稱全部訊息已完成對票 |

逐步原始日誌、退出狀態與摘要，位於兩個 PR 各自的 `materials/community-{hero-forge,batch2-integration}/receipts/main-v04222-integration/`；各自目錄只使用對應實際名稱。`logs.json` 帶 SHA-256，`summary.json` 保存基準與 target 資料。這些是本地整合證據，不能充當新共同服務的發布收據。

## 做中、待整合與阻擋

| 狀態 | 項目／下一步 |
|---|---|
| 待審查／合併 | 先審 PR #1135 的既有必要執行機制；第二批分支本身不包含第一批所有 runtime，不能只搬 74 份 JSON 就視為完成 |
| 待共同服務確定 | 最後上架服務的問題仍待回覆。現有各 37 名隔離發布證據沿用，沒有把两個平台當成單一平台 |
| 待配置核對 | 固定 Main 原檔 `ugc.enabled=true`、`communityRoomOnly=false`，但 `ui-cues.playerContent.submit/discover=false`；出貨限額仍每日 20／待審 5。共同服務須讀實際 overlay，對齊 Owner 已接受的 100／50。含模型 ZIP 上限在 Go 缺欄時回到 256 KiB，不能因 TS default 較大便認為服務已可收大包 |
| 待建包／投稿／上架 | 合併與共同服務版本確定後，取得其真正 target，重建受影響包，再走既有投稿、管理員審查、正式發布；不移除審查或版本固定條件 |
| 待必要功能確認 | 74 名在同一服務可選、可進場，技能／機制／特效能運作；沿用仍適用的證據，只補整合影響的部分，沒有新增逐招錄影或回放矩陣 |

## 未做、待改善與延後

本輪未改模型配對、沒有擴充模板／機制、沒有調整特效或平衡；沒有操作正式服務或 AWS。模型配對等待 Owner 後續整理。Phase 2／3 及額外事項仍未啟動，其狀態沿用前份快照，不因這次 gate 修正而重新展開。

## 提交與交接

修正分別在 `codex/community-hero-forge-release-materials`（實作收尾 `01c52633c`）與 `codex/community-batch2-integration`（`251785352`）；本快照、原始日誌與摘要隨後追加提交並推送原 PR，遠端是否成功以此次 PR 留言及分支 HEAD 為準。Main 只做審查與合併；未直接修改或推送 main。#1147／#1150 仍保留未完成的正式整合範圍，不以本次本地結果關閉。
