# 技能 AP 換算計畫（指標）

⛔ **內容不在這一份。** 這一份只是 owner 2026-08-21 那則裁決的**入口指標**。

| 是什麼 | 在哪 |
|---|---|
| **計畫全文**（規則 · 錨點 · 逐支換算表 · 爆掉分析 · stage 2 規格） | [`docs/技能AP換算計畫.md`](技能AP換算計畫.md) |
| 機器可讀（JSON） | `docs/_data/ap-conversion-plan.json` |
| 機器可讀（CSV） | `docs/_data/ap-conversion-plan.csv` |
| 產生器 | `tools/ap-conversion/gen.py`（`pnpm apconv:plan` / `pnpm apconv:plan:check`） |

⚠️ **為什麼正文不用 `_temp_<時間戳>` 這個名字**：檔名裡帶時間戳 ⇒ 每跑一次就是一個
新檔案 ⇒ `--check` 的**逐位元組比對永遠不可能相等**，那條閘只能被放寬成模糊比對，
而一條被放寬的閘等於沒有閘（CLAUDE.md 記過兩次：GH#389 · #426）。
⭐ 而且 `_temp_` 的判準是「**它會不會過期成沒有人要讀的東西**」—— 這一份**不會**：
它接在 `skills:sync` / `skills:check` 上，技能一動它就跟著動、過期就紅。
⇒ 依 CLAUDE.md 的命名規則，正文用**正常命名**才是對的。
