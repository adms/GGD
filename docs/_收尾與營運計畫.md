# 專案收尾 · 專注營運計畫

> owner 2026-07-27：「我們差不多要讓這個專案告一段落，將開發重心放到後台編輯器了。」
>
> 這份文件的用途只有一個：**讓你敢停手。**
> 所以它不是許願清單，是一份「這些補完 → 剩下的放掉 → 之後靠編輯器」的判斷。

---

## 〇、這份文件的可信度（先講清楚）

寫這份文件時**撞到週用量上限**（`weekly limit · resets Jul 29 7am Asia/Taipei`），
所有背景 agent 全部失敗。所以它是**單人靜態分析**寫的，不是多 agent 普查。

| 標記 | 意思 |
|---|---|
| ✅ **已實查** | 我親手 grep / 讀過原始碼，附 file:line |
| ⚠️ **待查** | 從既有文件與任務清單推得，**還沒實查** |

⚠️ 標記的項目在動手前**必須先實查一次** —— 本專案已經有 10 次「東西做了但玩家拿不到」，
清單上的狀態不可信。下面第一節就是三個例子。

---

## 一、清單在說謊 —— 實查後的三個修正

只查了三項就翻掉三項。這說明**任務清單的 pending/in_progress 狀態整體不可信**。

### ✅ #241「`/editor/` 烤進正式映像且完全沒有驗證」→ **已經修好了**

`docker/edge.Dockerfile:93-109`：

```
# Default OFF. See the header.
... echo "edge build: content editor OMITTED (task #241).
    Pass --build-arg GGD_INCLUDE_EDITOR=1 for a dev image."
```

預設不打包。要進去得明確傳 build arg，而且那條路徑會印
「this image must NOT be deployed publicly」。
`:23` 還記著一條更重要的事實：**它本來也不是寫入漏洞** ——
`apps/editor/src/api/client.ts` 在非本機環境會 dead-fold 掉寫入路徑。

→ **這一項可以關掉。** 它原本是我打算列進「停手前必補」的第一名。

### ✅ #243 備份 →「機制齊全，但沒有人在按」

`apps/platform/internal/platformarchive/` 是完整的一套：
`backup.go` · `export.go` · `restore.go` · `retention_test.go` ·
`freespace_unix.go` · `hygiene_test.go`，還有獨立執行檔 `cmd/platformarchive/main.go`。

但 —— **全 repo 找不到任何排程**。
`grep -rniE "cron|schedule|ticker" apps/platform/internal/platformarchive/` 零命中；
platform 唯一的 ticker 是 `server.go:712` 的 `pendingSweepInterval`（掃待審註冊，不是備份）。

→ 備份是**手動**的。這一項進「停手前必補」，但只需要一顆排程，不是重寫。

### ✅ 資料住在哪 → **一台 VM 的磁碟，沒有離機副本**

`docker/compose.yaml:69-72`：

```yaml
volumes:
  # durable truth — survives container restarts, lives in the repo
  - ../data:/data
```

家人的帳號、藍水晶、排行榜、邀請碼全在 `ggd.adms.ai` 那台機器的 `<repo>/data/`。
容器重啟活得下來 ✓ —— **但那台 VM 掛掉就全沒了。**

→ 這是整份文件裡**唯一一條「不補會出人命」的項目**。詳見第二節。

---

## 二、停手之前一定要補完的（六項）

判準只有兩條：**不補家人玩不下去**，或**不補有資料/安全風險**。
不符合這兩條的一律進第三節。

### T1 · 離機備份排程 —— ✅ 已實查 · 小

現況：機制齊全（`platformarchive`），零排程，靠後台手動按。
要做：一顆每日排程把 `data/` 打包丟到機器外（GCS bucket 最省事，同一個 GCP 專案）。
保留天數 `retention.go` 已經有了，直接用。

**不做的後果**：VM 一掛，家人所有帳號、水晶、排行榜歸零，而且**你不會事先知道**。
這是唯一一條「補起來只要幾十行、不補是不可逆損失」的項目。

### T2 · 出事了要有人知道 —— ⚠️ 待查 · 小

現況：`server.go:471` 有 `/healthz`。⚠️ **但沒查到有誰在打它。**
要做：一個外部探測（UptimeRobot 之類）打 `/healthz`，掛了發訊息給你。
`#209` 已經有 Slack 通知管道，接上去就好。

**不做的後果**：服務掛了，你要等家人跟你說才知道。

### T3 · 收尾中的七項功能 —— 進行中，不算新待辦

這七項是你這兩天親口點的，全部在 batch 3 之前就開了工作流；
週上限一解（Jul 29 7am）就繼續。**不要重複列進待辦。**

| 項目 | 狀態 |
|---|---|
| #283 團隊生命可見 + 十回合上限 | 診斷完成、spec 完整（見下），實作待重跑 |
| #278 開房拉好友一起玩 | 待重跑（改靜態追鏈路） |
| #282 隱形技能/道具沒作用 | 待重跑（普查階段） |
| #279 死亡提示 | 待重跑 |
| 作弊碼不計分不發水晶 | 待重跑 |
| #230 VFX 真實引用普查 | 待重跑（唯讀） |
| 四分支驗證（262b/277/280/281）| **驗證全部失敗於週上限，`verdicts: []` 是空的不是通過** |

### T4 · 四條未合併分支的驗證 —— ✅ 已實查 · 中

`git rev-list --count main..<branch>`：

```
feat/262b-vfx-gaps            2 commits ahead
feat/277-mouse-target-assist  1
feat/280-round-end            1
fix/281-firering-mobs         1
```

四條都還在半空中。**這四條擋住其他批次**，而且 `fix/281`（火圈燒殭屍）與
`feat/280`（回合結算 WIN/LOSE + MVP 分數）都是你直接點名的體驗問題。
驗證方式改成**純靜態**（見第五節），不要再起環境打一場。

### T5 · #283 的完整 spec —— ✅ 已實查 · 中

上一輪實作 agent 中途斷在額度，交付從未 commit、worktree 已刪。
但驗證者在它消失前抓到 diff 並做完靜態分析，**診斷是完整的，直接照做即可**：

**錯的（不要重犯）**
1. 客戶端一行沒改 —— owner 要求的「結算/商店看得到」完全沒做
2. 結算：`TeamLivesBar` 在 `HUD_Z.slot = 25`，`MatchEndPanel` 是 `inset:0` + 不透明 + `HUD_Z.screen = 40`
   → **有渲染、被蓋死**。repo 自己的普查表 `VersionBadge.tsx:41` 早就寫了 `settlement → COVERED`
   → **正解是把生命值畫進 `MatchEndPanel` 內部**，不是調 z-index
3. `MatchEndPanel` 從頭到尾沒印過生命值；`settlementModel.ts:314` 的 `lives` **只**被排序讀
   → **用於排序 ≠ 畫在畫面上**
4. 商店：`hudLayout.ts:194-207` 的 `displaced: "hide"` 原封不動
5. `resolveMaxRounds()` **零呼叫點**。`MatchRoom.ts:395` 傳 12 個位置參數止於 `ownership`，
   `maxRounds` 被加在第 13 個 → 永遠吃預設值。
   上限「碰巧」在 10 生效純粹因為預設等於出貨值 —— **你把轉盤轉到 6，什麼都不會發生**。
   而 `MatchRoom.ts:381-384` 就在同一個函式裡寫著同一個 bug 的墓誌銘：
   「the owner held the match-length dial and turning it did nothing」
6. 「共用比較器」沒共用：`settlementModel.ts:341` 的私有副本沒被 import，
   且兩份階數已不一致（server 多一個 `roundWins` 階）→ 上限收場時兩邊名次會不一樣
7. 零測試 —— `roundCapReached()` 改成 `return false`，測試照樣全綠

**對的（保留）**
上限時序沒有 off-by-one，驗證者特地推過：
`PhaseMachine.ts:45/56/66` + `maybeFinish()` 三個呼叫點全在 `case "resolution":` 內
→ 第 N 回合 resolution 期間 `phase.round === N`，`>= maxRounds` 在**第 10 回合結束時**才閂上。
真的打滿十回合。

### T6 · 倉庫清乾淨 —— ✅ 已實查 · 小

```
分支總數 ~180，其中 ~140 是 worktree-wf_* 殘留
仍在的 worktree：9
0 commits ahead 的分支：~60（已合併或空的）
```

`feat/279-death-notice` 與 `fix/282-invisibility` 都是 **0 ahead 的空分支** ——
開了名字沒做事，會誤導下一個 session 以為有東西。

**不做的後果**：不是技術風險，是**認知風險**。你之後要靠這個 repo 做營運，
一個有 180 條分支、其中 140 條是垃圾的 repo，找不到東西。

---

## 三、明確放掉的

放掉不等於刪掉 —— 每一項都寫了「想撿回來從哪開始」。

### 3-1 全部的資產債（#81 · #178 · #116 · #72）

| 項 | 規模 |
|---|---|
| #81 退役 Blizzard 資產 | 26 模型 + 511 語音 + 863 圖示 |
| #178 補完缺的圖示 | 602 個 |
| #116 Sketchfab 換模 | 逐件找 + 逐件授權證明 |
| #72 圖示覆蓋 | 695 + 168 |

**為什麼放掉**：這是**私有部署給家人玩**，不是上架商店。版權閘已於 2026-07-26
由你決定全開（#239）。圖示缺了會顯示替身，家人不會因此玩不下去。
而這四項加起來是整個清單裡**最貴的一塊**，做完了體感增益接近零。

**想撿回來**：從 #101 的圖示生成頁開始（那頁是活的，能看進度）。

### 3-2 i18n（#19）· 名言 VO（#139 #142）· 手把全 UI（#197）· 無障礙名稱（#252）

**為什麼放掉**：使用者是**你的家人，講中文，用鍵鼠或手把打**。
三語 UI 沒有讀者；113 位英雄的日文名言 VO 是一個純加分項目乘上 113；
手把已經能打戰鬥（#222 選取框也做了），只是選單要用鍵鼠。

**想撿回來**：#19 的 i18n 要先有框架才談得上翻譯，先做框架再說。

### 3-3 內容保真度的長尾（#56 #113 #144 #119/#249 #236 #230 的執行半）

| 項 | 內容 |
|---|---|
| #56 | importer 漏掉 150/180 個 w3u 欄位碼 |
| #113 | 14 對英雄文件數值全同 —— 是重複還是不同角色 |
| #144 | 逐英雄的移速/攻速/回復從 w3x 重抓 |
| #119/#249 | 變身系統 |
| #236 | 喪標麥可補滿 5 格技能 |
| #230 執行半 | 把 106 支孤兒特效接回技能 |

**為什麼放掉 code 這條路**：這些**全部是內容問題，不是程式問題**。
而你的新重心正是內容編輯器 —— 這些應該變成「在後台裡改」，不是「再寫一輪匯入器」。
→ **整批搬到第四節。**

**想撿回來**：先確認編輯器能不能改那個欄位。不能 → 那才是要寫的 code。

### 3-4 效能長尾（#63 #115 #99 #98 · 延遲計畫的第二、三層）

**為什麼放掉**：`docs/改進延遲.md` 的第一節已經指出主因很可能**已經修掉**了
（殭屍模型從 5,288 tris 換成 168 tris，尖峰 60 隻 = 317,280 → 10,080 tris）。
在你回報「還是卡」之前，這一整層是在修一個沒有證據的問題。

⚠️ **例外**：`docs/改進延遲.md` 第二節指出伺服器可能在**無聲地丟 tick**，
而 `plan.dropped` 為 true 時唯一輸出是一行 `console.warn`（`MatchRoom.ts:519-523`）——
沒有計數、沒有指標。這一條併進 T2（可觀測性），因為它同時是營運問題。

### 3-5 表演層的加分項（#143 #147 #148 #149 #234 #250 #251 #232 #74 #41）

回合勝利者 3D 展示 · 戰鬥泥土塵埃 · 店員輪播提示 · 強化三選一池 ·
擊殺語音歡呼 · 令咒節奏表演 · 大廳英靈殿 · 商店評價欄 · 登入轉場 · 懸停播報。

**為什麼放掉**：這些都是「有了更好」，沒有一項是「沒有就不能玩」。
遊戲的表演層已經非常厚（#93 煙火、#92 浮動傷害、#33 特效、#123 特效庫、
#135 每首 BGM 專屬前奏、#184 語音辨識度…）。**再加只會邊際遞減。**

**想撿回來**：#149（強化三選一池）是這一組裡唯一會影響**玩法深度**的，
如果家人開始覺得「每場都差不多」，先撿它。

---

## 四、後台編輯器時代：營運要靠什麼

⚠️ **這一整節是 ⚠️ 待查。** 我沒有親手點過後台，週上限也不允許再開 agent 去點。
**下一個 session 的第一件事就是把這節實查一遍。** 下面是「該問的問題」，不是「答案」。

### 4-1 主線問題：**不寫 code 能不能出一支新技能？**

這一題的答案決定「重心搬到編輯器」是真的還是願望。要驗的四段：

1. **後台能不能新增一支技能文件？** —— #96/#102 說能，⚠️ 待實查
2. **存下去會不會進遊戲？** —— #189「durable content overlay in data/」是 in_progress。
   ⚠️ **這是致命的一題**：如果後台改的東西**部署一次就被蓋掉**，
   整個編輯器路線是假的。分支 `feat/189-durable-content-overlay` 是 **0 commits ahead**（空的）。
3. **能不能綁特效？** —— #205 特效編輯器 in_progress
4. **能不能配圖示？** —— #186「後台建內容時自動生圖示」pending；
   ⚠️ #112「AI 圖片路徑對現行所有 provider 都是壞的」pending —— **這兩條是連著的**

→ **建議下一步順序就是 2 → 3 → 1 → 4。** 先確認存得住，再談編輯什麼。

### 4-2 從第三節搬過來的（改由編輯器做，不寫 code）

- #113 十四對重複英雄 → 後台開兩個頁面對照著看，直接決定留哪個
- #144 逐英雄移速攻速 → 後台改數值
- #236 喪標麥可 5 格技能 → 後台配技能
- #230 特效配對 → #205 特效編輯器（**這正是它存在的理由**）
- #108 傳說池策展 / 描述與 modifier 不符 → 後台改
- #149 強化三選一池 → 後台加內容

**這一組是「編輯器好不好用」的驗收清單。**
如果編輯器做完了，這六項應該一個下午能全部處理掉；如果不能，就是編輯器還沒做完。

### 4-3 營運日常 —— ⚠️ 多數待查

| 要回答的 | 現況 |
|---|---|
| 出事怎麼知道 | ✅ 有 `/healthz`（`server.go:471`），⚠️ 沒查到有誰在打它 → **T2** |
| 資料會不會不見 | ✅ 在 `<repo>/data/`，容器重啟活得下來，**沒有離機備份** → **T1** |
| 怎麼上版 | ✅ ssh → git pull → `docker compose build && up -d`，⚠️ 手動、無回滾 |
| 家人怎麼回報 bug | ⚠️ #175 match replay 就是為此做的（`data/replays` 有掛載），**⚠️ 沒查過真的能不能重播** |
| 有沒有定時炸彈 | ⚠️ 憑證（Caddy 自動續，應該沒事）· 磁碟（`data/replays` 只進不出？**待查**）· token 過期 |

⚠️ **`data/replays` 只進不出這一條要特別查** —— 每場比賽都寫一個 `.jsonl.gz`，
如果沒有清理，那是**慢速的磁碟炸彈**，而磁碟滿了 platform 就寫不進資料。
`platformarchive/freespace_unix.go` 存在說明有人想過磁碟，但那是備份的空間檢查，不是 replay 清理。

### 4-4 安全（維持現狀，不要動）

這幾條已經做完並驗證過，**進入營運期後不要為了方便而放鬆**：

- #126 私有部署閘：註冊 → 待審 → 管理員核准才能玩
- #210 已修：被拒玩家持有效 token 仍能進大廳 WS（**不要重新打開**）
- 邀請券：TTL + 單次 + 綁收件人
- #117 已修：裸奔的 redis
- #162 loopback = admin（本機免登入）—— ⚠️ 這條在對外環境的行為值得再確認一次
- #241 `/editor/` 預設不打包（`edge.Dockerfile:93-109`）

---

## 五、方法論的改變（owner 2026-07-27）

> 「我們開始盡量使用邏輯驗證，而不是大量模擬驗證來節省時間與 token fee」

**這個改變是對的，而且今天就有證據。**

#283 的驗證者**完全沒有起 dev server、沒有開瀏覽器、沒有跑測試套件**，
純靠 `grep` 找呼叫點、讀 `HUD_Z` 常數、`git reflog`、比對兩份比較器的階數，
就抓出**七條致命傷**，包括「`resolveMaxRounds()` 零呼叫點」這種
起環境打一場**反而看不出來**的缺陷（因為預設值碰巧等於出貨值）。

### 之後的預設做法

| 先用 | 只在必要時用 |
|---|---|
| grep 找真實呼叫點（排除註解） | 起 dev server |
| 讀常數、比 z-index / 座標算重疊 | 開瀏覽器截圖 |
| 單元測試 + **變異測試**（改壞關鍵行看紅不紅） | 打一場完整比賽 |
| `git log` / `reflog` / `diff --stat` 驗證交付真的存在 | 兩個真帳號端到端 |
| 型別與 schema 追資料流 | |

**變異測試不能省。** 那是唯一能分辨「守衛有效」與「守衛是裝飾」的手段，
而且它是靜態的 —— 改一行、跑測試、看紅燈數，不需要環境。

⚠️ **例外**（這些靜態真的答不出來，要起環境）：
- 「畫面上到底看不看得到」的最終確認（但**先靜態排除**掉 z-order/座標/掛載問題）
- 兩個真帳號的多人流程（#278）
- 手機實機上的幀率

---

## 六、額度狀況（2026-07-27）

**週上限已滿，`resets Jul 29 at 7am (Asia/Taipei)`。**

今天連續撞到兩種：先是月額度（你提高後恢復），接著是週上限。
週上限**不是提高就能解**的，只能等重置。

**規則**：撞到 `You've hit your ... limit` 時**不要重試工作流** ——
agent 會立刻失敗並回傳空結果，而空結果長得跟「驗證通過」一模一樣
（今天四分支驗證就回了 `verdicts: []`，那不是四條都通過，是四條都沒跑）。
**看到空的 verdicts 要先確認是不是額度。**

---

## 七、下一個 session 的前三件事

1. **T1 離機備份排程** —— 唯一不可逆的風險，先做
2. **實查第四節** —— 親手點一次後台，回答「不寫 code 能不能出一支技能」，
   特別是 #189「後台改的東西部署一次會不會被蓋掉」
3. **T5 #283 照 spec 實作** —— 診斷完整，是最省力的一項

T3 的其餘六項與 T4 的四條分支，等週上限重置（Jul 29 7am）後用**靜態驗證**跑。
