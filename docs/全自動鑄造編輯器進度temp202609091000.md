# 全自動鑄造編輯器進度 temp202609091000

## 2026-09-11 活動快照

目前目標是讓 **126 名新角色**全部經過社群英雄全自動鑄造編輯器的建立／匯入、六槽編譯、完整 ZIP、投稿、管理員審查、發布與單英雄版本回復，並完成 Phase 2／3。本文只記錄能由目前 Git、收據或實際操作證明的狀態；「編譯通過」「本機有模型」「ZIP 可產生」「PR 已建立」均不等於正式上架。

### 角色批次

| 批次 | 數量 | 目前狀態 | 尚缺 |
|---|---:|---|---|
| 第一批社群英雄 | 37 | 已有 Editor 重建、222 槽、投稿、固定候選、隔離管理員發布、遊戲名單與獨立版本回復收據 | Main 整合後在預定正式服務確認當前發布指標；不是重新設計技能或重拍逐槽畫面 |
| 第二批社群英雄 | 37 | 已有 Editor 建包、222 槽、投稿、隔離管理員發布及對局抽樣收據 | 與第一批在同一正式服務 target 的發布結果仍需 Main 整合確認 |
| LoL 第一批 | 7 | 已有 Editor 專案、42 槽、模型綁定、編譯／模擬與內容名冊 | 當前正式服務的投稿／審查／發布指標尚未在本分支重驗 |
| LoL 第二批 | 11 | Draft PR #1193；QWER、重施放與共用機制接縫已有提交 | PR 尚未合併，因此尚未正式發布 |
| 已取得模型／重上架舊角 | 34 | 本分支集中重驗 **34／204／34 ZIP 全通過，0 阻擋、0 失敗**；Astralym 與 8 名核准替代模型另有服務往返增量證據 | 本分支尚待 Main PR 審查與正式服務投稿／核准／發布 |
| **合計** | **126** | 內容與程式工作分散於已合併 Main、#1193 與本分支 | 只有全部依賴合併並在同一正式平台發布後，才能逐名標記「已上架」 |

126 名逐名狀態、ID 與批次歸屬另見 `docs/editor-contract/社群英雄126名上架狀態.md`。目前可證明的是 **115 名待上架、11 名實作中、0 名有 Main 當下正式服務發布證據**；第一、二批的隔離發布證據仍保留，但不冒充正式站現況。

34 名集中報告在 `docs/_reports/community-acquired-heroes/acceptance-summary.json`。本次 rebase 後重新建立，目標 catalog 為 `sha256:310c2990327f885ca0f90d9e10b94359e4ce5083dbc5c2852f1897faada2ff4f`，processor fingerprint 為 `7fa275f09510`。完整本機執行輸出保存在 `/private/tmp/ggd-acquired-report-05-rebased`，Git 只保存可審查摘要。

### 模型與素材

- 34 名皆有可建包的模型與六用途動作映射；26 名使用本尊／已取得轉換模型，8 名使用 Owner 核准的明示替代預設。替代者為 Zero、拉姆、碧翠絲、Mario、Mewtwo、Pokémon Trainer、Ryu、Steve／Alex，日後取得本尊可新增模型版本切換，不覆寫舊版。
- Astralym 原始 GLB 超過現有單檔 8 MiB 上限；已用可重建腳本移除不可達 buffer/accessor，保留取樣後相同的骨架、六動作與渲染資料。最終 GLB 為 3,889,188 bytes。
- 3,454 個唯一來源／中間素材已按 SHA-256 封存至既定 S3；Git 保留成品 GLB、設定、生成／壓縮程式、版本清單、SHA-256 與收據。本機原始檔未刪除。
- S3 收據：`docs/_reports/community-acquired-heroes/source-archive-receipt.json`。未讀取、輸出或保存 AWS 憑證。

## Phase 2

| 編號 | 狀態 | 本輪結論 |
|---|---|---|
| P2-01 保存、離線、復原、跨裝置衝突 | 程式已實作；代表情境已有驗收 | IndexedDB、自動保存、桌面備份、雲端 CAS、完整差異與採本機／採遠端／另存新作均保留；126 名正式服務整合只補受影響情境 |
| P2-02 複製、授權改作、退回再投稿 | 程式已實作；代表流程已有驗收 | 改作為獨立作品、來源可追溯、退回修訂與重送不覆寫舊候選 |
| P2-03 後台比較、審查中續編、發布恢復 | 程式已實作；代表流程已有驗收 | 審查綁定不可變候選，作者續編隔離，CAS 與冪等發布保留；正式服務結果仍依 Main 合併後 target |
| P2-04 模型／動作／icon／資產版本 | 程式已實作；34 名本輪全通過 | 每名獨立 modelKey、版本選單、保存、ZIP 還原與單英雄回復；不宣稱任意 FBX 或任意骨架自動重定向 |
| P2-05 macOS／Windows 桌面交付 | 程式、建置與更新驗證流程已實作 | macOS universal DMG／ZIP、Windows NSIS／portable、保存後更新、摘要與發布者驗證均在。正式簽章、Team ID、Windows publisher 與 stable feed 是外部發行設定，未偽造通過 |

Phase 2 的程式範圍已完成；正式簽署及發布設定只能由持有憑證與 feed 的 Main／Owner 提供。這項外部條件不影響 Web／AI-off 的英雄建立、投稿與審查。

## Phase 3

| 編號 | 狀態 | 本輪結論 |
|---|---|---|
| P3-01 視覺／操作 | 依核准範圍完成 | 沿用既有 43 主題／47 文件與角色畫面收據；本輪修正 Hero Forge 冷啟動 PBR 載入停滯並實際確認畫面恢復。不新增錄影矩陣 |
| P3-02 一致性／故障情境 | 依核准範圍完成 | 沿用既有確定性、固定版本、CAS、回復與故障注入收據；Owner 已排除全量回放／逐 tick 擴驗，不倒掛為上架門檻 |
| P3-03 效能／環境 | 依 Owner 指定範圍完成 | 目標是平板、iPad mini A17 Pro 近似等效能、30 fps；排除手機，不要求額外 A17 Pro 真機認證 |
| P3-04 視覺精修 | 功能阻擋範圍完成 | 只修會使模型、機制或特效失效的重大問題；原作特效微調、鏡頭精修與逐角色美術調整留在 `docs/額外事項待確認.md` |
| P3-05 本機 AI／E8 | runtime 與 gate 已整合；模型發行 gate 尚未通過 | 桌面版已有固定 manifest、續傳下載、容量／SHA 驗證、IPC、硬體政策、無洩漏 llama runner、216 案例評分與封裝 gate；目前 manifest 明示 `released=false`／`LOCAL_AI_E8_GATE_PENDING`，因此不啟用推論，AI-off 路徑完整可用 |

P3-05 的未完成部分是核准模型在 macOS Metal 與 Windows CUDA 的實際 E8 收據及可發布 runtime artifact；目前本機沒有能合法推翻 `LOCAL_AI_E8_GATE_PENDING` 的成對收據。程式會因此保持關閉，不會下載模型權重進 Git，也不會把未量測結果寫成通過。

## 本分支已完成與待 Main

已完成：

- 34 名獨立 Editor 配方、屬性、出身、定位、204 槽、既有模板／機制、VFX／SFX、模型與六動作映射。
- 34 名集中 factory→compile→dependency→asset→ZIP→readback→inspection；34 全通過。
- 模型版本下拉、legacy canonical ID、8 名核准替代版本、Astralym 安全壓縮與可重建工具。
- Phase 3 本機 AI 桌面 runtime／release gate 整合；24 項桌面 AI／封裝測試、Editor 23 項與 Shared 6 項定向測試通過，三套 TypeScript typecheck 通過。
- Skill Forge 功能驗收目錄擴到 60 份文件／56 個主題，補齊 Main 已出貨的 `consumeStatus`、`evasion`、`spendHealth`、`pull`、`chance` 與 8 個被動事件；視覺收據基線仍維持 47 份文件／43 個主題，沒有把功能覆蓋擴張成額外錄影工作。
- 技能形狀閘已覆蓋 907 支技能／58 份模板；訊息帳本也已修正同文不同時間被錯誤去重，2026-09-10 的 85 則訊息全部列出並對票。
- Editor 完整測試已在目前分支重跑：91 個檔案通過，595 項通過、1 項依既定條件跳過。驗收同時移除技能總數 421 的過期常數，改以當下 `content/abilities/_index.json` 對帳 907 份技能。
- `models:check` 揭露的 14 筆既有模型幾何已逐項分類：`ou99.495015` 與其不可變版本的 prim1 為實際血泥，兩份 model doc 均宣告隱藏；`ou99.458777` 的手持燈具、`ou99.474258` 的細長配件，以及哆啦A夢 `Spell One` 的任意門／道具群均以真選角與施法預覽確認為合法幾何，加入帶尺寸、骨架及反駁條件的豁免。現在掃描 259 顆 GLB、22 處已宣告殘留幾何與 7 處已宣告離地分身，結果為 0 問題。
- 原始／中間素材 S3 封存及 Git 成品／收據分流。

待 Main／正式環境：

- 審查並合併本分支與 LoL 第二批 #1193。
- 在當下正式 content target 重建必要 ZIP，讓尚未正式發布的角色經投稿、管理員核准與發布，確認正式名單可選及單英雄 rollback。
- 提供 P2-05 的正式簽章／發布 feed；若要啟用本機 AI，再提供兩個核准硬體 target 的 E8 收據與 runtime artifact。

目前集中閘剩下一項不屬於本分支角色內容的外部依賴：`combat:check` 精確回報 74 筆缺少核准語音 donor WAV（第一批 37 名＋第二批 37 名），屬 #1170 的外部語音材料，不能在本分支以假檔或跳過檢查偽造通過。完整 `pnpm skills:check` 已一路通過 `models:check`、`coord:check` 與 `voices:check`，只在這 74 筆 donor 缺件退出；`pnpm editor:accept:release` 與獨立 `pnpm coord:check` 均通過。原先 `models:check` 的 14 筆模型幾何已在本分支完成逐項分類並通過；其餘抽出的 29 個模型閘後檢查已有 24 個通過。`review:check` 的既有 HITL／視覺待審依 Owner 核准範圍不作為本輪新增工作。

停止規則：不再追加錄影、全量回放、特效微調、新模板、新機制標籤或角色平衡重設。發現會阻止建立、建包、投稿、審查、發布、遊戲解析或回復的缺陷才開票修正；其餘只記入 `docs/額外事項待確認.md`。
