# v0.39.1 —— 🧮 AP 係數公式**接上載入層**：125 個係數從此由級距推導

> owner 2026-09-06 逐字：「do A, 全部技能接上公式」

── ⛔⛔ 在此之前 ──
公式（#942）與「載入時套到文件上」那一層（#945 的 `resolveApCoeffOnDoc`）都在，
⛔ 但 **production 呼叫點 0 行** ⇒ 出貨的 `ap-coefficient.enabled = true` 是一格沒有消費端的開關，
玩家拿到的是樹上手填的 125 個字面值（失敗形態⑧ 第二次；#945 的 docstring 自己在講上一代的同一個病）。
owner 今早問「不是有公式嗎」—— 這一條就是答案。

── ⭐ 接線 ──
· `registries.ts`：`withTiers = withApCoeff(withTiersCore(d))` —— standalone／英雄卡內嵌／模板展開／道具
  四條路只有這一個接縫；包在**最外層**是承重的（它讀 cooldown/range/castTimeSec/radius 解析**之後**的值）。
· 逐節點版載入層 `resolveApCoeffOnDocWithTiers()`：冷卻中位／秒數由**帶 ratios 的那個節點**判形狀。
  ⚠️ 第一版以文件判形狀 ⇒ 4 份混形文件（edem.w · emfr.e · emns.e · etyr.r）5 個值錯（範圍表 60s 取代單體表 30s）
  —— 逐支比對量到後改逐節點；報表 `gen.ts` 與 runtime 共用同一支 `apCoeffCooldownFor()`，
  重生成後與 HEAD 逐位元只差 06-02 那一列 tie 排序 ⇒ **報表上的公式值＝場上跑的值**。
· rollback：後台 `ap-coefficient.enabled = false` ⇒ `resolveApCoeffOnDocWithTiers` 逐位元 no-op（回到手填）。
  ⚠️ config 在註冊時讀 ⇒ 翻回去要重啟 shard。

── 守衛（靈魂層：一條承重 ＋ 一次突變）──
`apCoeffWired.test.ts`：載入出貨內容、跑真的 `registerAll`、讀註冊表 —— 開關開 ⇒ coeff＝公式值（⛔ 不等於字面值）；
開關關 ⇒ 回到字面值。突變（`withApCoeff` 拿掉）⇒ 紅：expected 0.158, received 1。還原後 sha 相同。

── 裁決 A ──
06-02 山形修煉-變（`ucrl.w`／`u034.w`）手填改回 **1.0**（同編號兩形態一致）；`apCoeffDeviation` 棘輪 22→23 帶理由
—— 接上之後它量的是「開關關掉時玩家會落到哪裡」，⛔ 不再是場上的值。

── 連鎖修的（都是「公式現在在註冊時改寫 ap coeff」的正確副作用）──
· `loader.test`：`ratios[].coeff`（ap）進「被授權分歧」名單，與 `castTimeSec` 同形（第四個）
· `skillCostTiers.test`：夾具關掉 ap-coefficient，隔離傷害級距機制（同 G12 的手法）
· 變身對子基準線重 dump（06-02 兩形態一起動，看過 diff）
· `fieldAdoption.exemptions.json`：`onGuardianDown` 的 landing 到期 31 天 ⇒ 誠實改 debt（與接線無關的定時炸彈）
· `apps/client/src/build/buildStamp.test.ts`（2026-07-24，task #66）**六週沒進過 git** —— `.gitignore:20 build/` 吞掉原始碼目錄；
  Lane13 的閘在 CI 上抓到（本機綠、CI 紅）⇒ 搬到 `apps/client/src/buildstamp/`（開 #1038，與 #1013 同形）

── ⚠️ 部署前要 owner 看的 ──
接上之後 125 個係數同時變（公式較高 73／較低 52）。倍率最極端的 14 支列在 `docs/editor-contract/ggd-ap-coeff-before-after.md`
與 #1035 的留言（15-02 疾風迅雷 0.1→2.27 ×22.7 …）——#945 判它們多半是**級距標籤判錯**。

驗：`skills:sync`＋`skills:check` 0 · `pnpm typecheck` 0 · shared／game-server／client／admin／content-api 全綠 · 樹乾淨閘綠。


## ⚠️ 部署前 owner 要看的：接上之後這 14 支變最多（手填 → 公式）

| 技能 | 手填 | 公式 | 倍率 |
| `godie-emfr.w` 15-02 疾風迅雷 | 0.1 | 2.2746 | **22.75×** |
| `godie-etyr.passive` 14-00 召喚式神 | 0.6 | 0.0327 | **0.05×** |
| `godie-o00l.e` 53-03 破法對咒 | 0.6 | 0.0413 | **0.07×** |
| `godie-n01g.q` 42-01 凍結的大地 | 0.6 | 0.0426 | **0.07×** |
| `godie-e007.ex` 12-002 仙氣發勁 | 6 | 0.5644 | **0.09×** |
| `godie-ewar.ex` 12-002 仙氣發勁 | 6 | 0.5644 | **0.09×** |
| `godie-u010.w` 38-02 邪王炎殺煉獄焦 | 0.5 | 0.0491 | **0.10×** |
| `godie-uvng.w` 38-02 邪王炎殺煉獄焦 | 0.5 | 0.0491 | **0.10×** |
| `godie-h020.e` 04-03 龍破斬 | 1.8 | 0.1815 | **0.10×** |
| `godie-hjai.e` 04-03 龍破斬 | 1.8 | 0.1815 | **0.10×** |
| `godie-hart.r` 01-04 超究武神霸斬 | 0.3 | 2.1354 | **7.12×** |
| `godie-efur.r` 13-04 龍星群 | 0.4 | 2.8424 | **7.11×** |
| `godie-u034.w` 06-02 山形修煉-變 | 1 | 0.158 | **0.16×** |

⭐ 倍率極端的多半是**級距標籤判錯**（#945）。rollback：後台 `ap-coefficient.enabled = false`（逐位元回到手填；要重啟 shard）。

## 🎮 玩家看得到的

- 技能的法強（AP）加成改由公式決定：冷卻越短、範圍越大的技能每一發拿得越少，冷卻長的單體技拿得越多 —— 整體強度不變（公式的基準是校準過的），變的是技能之間的相對強弱。
- 系統優化更新：穩定性與速度的例行維護。
