# v0.28.6 —— 後台 121 頁第一次有搜尋（#776）＋ 三張移植對照表變成 live

## 逐句對票（docs/_daily/2026-08-26.md，284 則、未對票 0）

| owner 說的（逐字） | 票 | 狀態 |
|---|---|---|
| 「**改 claude.md 前記得都要備份喔** 這是我們開發守則之一」 | — | ✅ 已確認：`rules-backup/2026-08-26_211239` 是**改動前**那一份（還寫著舊的 `331`） |
| 「後台左側選項**已經太長 不容易尋找、閱覽及管理** 你有什麼好建議做**資訊視覺化改善**嗎」 | #776 | ✅ ⌘K 指令面板＋🗺 導覽地圖＋🏷 標籤資料層 |
| 「**w3x jass, mdl, 蝗蟲群 球體 都已經建表對照移植完了嗎？**」 | #777 | ⚠️ **建表 ✅ · 移植 ❌**（數字在下面）＋ 普查快照過期另開 #777 |
| 「push + note + deploy」 | — | 本版 |

## 🔍 #776：診斷是「沒有搜尋」，⛔ 不是「分組太粗」

量到：**121 頁 · 11 組**，而 `App.tsx` 裡「搜尋/filter/search」的字串數 = **0**。
分組今天已拆過第二輪（31→16、36→23）—— ⭐ 每次只買到一點點而清單持續變長，
那是**第〇·七守則「一行接線」病長在導覽上**：每加一頁就多一列要用眼睛掃過去。

| owner 的動詞 | 做了什麼 |
|---|---|
| **尋找** | ⌘K 面板：模糊比對 標題／page id／分組名／**18 組同義詞**；空 query 顯示最近用過 |
| **閱覽** | 🗺 導覽地圖：11 組 treemap，寬度 `round(√(頁數/最大)×12)`（⭐ 取平方根才讓**面積**∝頁數） |
| **管理** | 地圖上的**覆蓋層生效中**徽章；無 session 誠實降級，⛔ 不編假狀態 |

實測：「傷害」跨五組一次撈出 · `cd` → **冷卻規則**（同義詞）· `vfx` → 四頁跨四組 · `zzzzz` → 空。
地圖 124/124 頁。⭐ 接線當下**兩條既有閘正確叫住新頁**（navSections 基準線＋navTags 覆蓋率）。

## 🧭 三張移植對照表的**誠實現況**（`/__live` 當場算的，⛔ 不是印象）

| 軸 | 建表 | 移植 |
|---|---|---|
| **JASS 特效** | ✅ 421 支逐支 | match **77** · replaced 202 · **jassOnly 10（原作有、GGD 零表達）** · none 21 · notInCensus 18 · unlinked 8 |
| **MDL 家族** | ✅ 178 顆／321 dummy／269 生成點 | resolved 78 · partial 8 · queued 9 · **noEvidence 83** · **零生成點 dummy 163** |
| **蝗蟲群/球體** | ✅ 236 隻逐隻 | 已落地 **64** · **缺 113（48%）** · 有模型無引用 19 · 隱形承襲 40 |

⚠️ 而且 JASS 那張的來源是 **2026-08-02 的一次性快照**（當時 662 份、現行 421）——
259 份退休、18 份新增沒有 join ⇒ ⭐ **只要它不自我更新，「移植完了嗎」就永遠有一段答不出來的區間**。
⇒ 另開 **#777**（快照↔現況對帳閘）。

## 出貨數值的裁決歸屬

**owner 原話**：本版零格新裁決。**我挑的（可回頭）**：⌘K 的 18 組同義詞與 13 個標籤詞彙
（回頭＝把 App.tsx 三行接線與 navMap 那一列拿掉，一個 commit；⛔ 這條路徑沒有遊戲 config —— 玩家看不到）。

## 這一輪順手開的票（等你勾要不要併進下一版）

| 票 | 是什麼 | 不修玩家會遇到什麼 |
|---|---|---|
| **#777** | JASS 普查快照過期（662→421） | 「移植完了嗎」永遠答不準；新技能在對照表上是 `notInCensus` |
| #768 | audition 量尺沒有自證 | 特效驗收的結論可能是假的（readPixels 在滿版亮幀回 0） |
| #769 | `--dry-run` 寫下假的轉檔紀錄 | 紀錄說裝好了而磁碟沒動 |
| #770 | 25 顆角色模型 44 個 TeamGlow 面全透明 | 隊色光暈看不見，還付 draw call |
| #771 | 戶籍/隔離區（本輪已大修，尚有 in-file 標記待做） | 586/640 產物檔內無標記，打開像手寫的 |
| #772–#774 | ggd-assets 靜默漏檔 · 後台 dev 連結沒有閘 · kill-bounty 標頭沒有閘 | 部署期靜默/正式站外洩 dev 頁/文件說謊 |
