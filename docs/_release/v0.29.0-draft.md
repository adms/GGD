# v0.29.0 —— 你的七項現場缺陷全修 ＋ 六項指示落地 ＋ 批核頁真的進了後台

> ⭐ 用詞紀律：像素驗收未完成的一律寫「**鏈路已接上，⛔ 未驗收**」。

## 逐句對票（`docs/_daily/2026-08-27.md`，未對票 0）

| 你說的（逐字） | 票 | 狀態 |
|---|---|---|
| 復活火圈太大 **減少 40%** | #778 | ✅ 4 → **2.4**，判定與視覺**同一格**一起縮 |
| 莉娜龍破斬沒有特效只剩文字了 **你竟然還跟我說你比照JASS了？** | #779 | ✅ 根因是我 v0.28.5 的常數；爆點亮像素 **0 → 3,052** |
| 拳四郎變身閃電**沒有去背** 變成黑色閃電 | #780 | ✅ `BJS_ALPHA_ONEONE = 0` 其實是 **ALPHA_DISABLE**，真值 6 |
| 閃電演算法太過耗效能 **請深入分析原因** | #781 | ✅ `arcs.update` **10.8ms → 3.8ms**、垃圾 −97%＋後台上限一格 |
| 場地特效又沒乾淨…**你不是有在監控嗎？** | #782 | ✅ 漏在 retire 陪葬共用材質；監控**看不到軌**（軌不是節點）已補 |
| 林克又在倒著走路攻擊了 **你不是說已經修正了嗎？** | #783 | ✅ 取消前搖不收面向鎖；#216 的守衛**對面向零斷言** |
| 地上殘留好多**紫色圈圈**特效 | #784 | ✅ 圈型 **mesh** 不在粒子登錄表裡 ⇒ 壽命閘結構性失明 |
| **我在後台審查頁面根本找不到** | #785 | ✅ 誠實答案：它**本來不在後台**。現在是真的一頁＋13 頁批核條 |
| **給人看的話不能只有ID 還要有名稱** | #786 | ✅ 排行榜四類 ID 全補中文名（載入時 join） |
| 把所有詠唱超過一秒的**都調整至一秒 但在後台留下記錄** | #787 | ✅ 載入時夾（⛔ 沒動 95 份 JSON）＋清單頁三欄記錄 |
| 吟唱 ≥0.3 秒都要有**蓄力特效**（隊伍顏色內縮光束） | #788 | ✅ `CastChargeFx`，通用模組（⛔ 不逐技能） |
| 移速加成**%轉五級距，上下限 0.1~4** | #789 | ✅ 31 列級距化 ⚠️ 並修掉它引發的回歸（下詳） |
| 後台 **每級加成 這頁無法顯示** | #790 | ✅ 專頁＋「點下去空白」這一族的覆蓋率閘 |
| 這些設定頁面**都會有批核頁面的部分** | #669 | ✅ `ReviewStrip` 一元件 × 一格參數掛滿 13 頁 |
| **守則犯錯記成一張表**，統計頻率及原因 | — | ✅ `docs/守則犯錯.md` ＋ `rule-slip.sh` ＋ 閘（已寫進 CLAUDE.md） |

## ⭐ 兩項「我說過修好了」的真相

| | 真相 |
|---|---|
| **龍破斬** | ⭐ **是我 v0.28.5 弄壞的**。`BJS_ALPHA_ONEONE = 0` —— 而 Babylon 的 0 是 **ALPHA_DISABLE**（關混合＋寫深度）。⚠️ 上次的守衛綠著，因為測試把**同一個錯字面值**抄成第二個住處 ⇒ **斷言的正是缺陷本身**（第〇·四守則的測試版）。而 #767「驗收過」是因為量到的亮度來自不透明畫出 —— **量尺數字對、機制結論錯**。 |
| **林克** | ⭐ **#216 的守衛驗錯了東西**：它的檔頭自己寫著「斷言讀的是**傷害事件**」—— 對**面向零斷言**。而缺陷自 #264 就潛伏，是 08-24 把打帶跑變成核心循環之後才每場發作。 |

## ⚠️ #789 上線後被**六條閘接力**抓到回歸（誠實記錄）

**逐階曲線被級距的網格壓平**：鬼隱之擊 `perRank` 0.5/0.5/0.5/**1.5**/**3.0** → 中/中/中/大/極大
＝ 0.5/0.5/0.5/**1**/**4**（rank4 從 +150% 變 +100%）。

⭐ 規則早就寫過只是沒有閘 —— `cooldownTier` 的契約逐字：「**填了級別 ＝ 每一階同一個值**」。
六條閘：`nativeFidelity` → `perRankNotTierified`(新) → `moveSpeedTiers` → `abilityCodeParityForms`
→ `fieldAdoption` → `templateFailSoft`。全部收乾淨，10 份產物從 `095e27fe` 還原。
⚠️ `tiers:apply` 是**冪等的** —— 值被吃掉重跑補不回來，只能從 git 取。

## 出貨數值的裁決歸屬

**你的原話**：火圈 −40% · 詠唱上限 1.0 · 蓄力門檻 0.3 · 移速級距 0.1~4。
**我挑的（都有一格可回頭）**：移速中三格 0.2/0.5/1 · 蓄力五格外觀 · `maxConcurrentArcs` 32（＝原寫死值）。

## 🧑‍⚖️ 一頁批次後台驗收 —— ⭐ 等你裁決

**待裁決 21 批** · ⚠️ **其中 4 批自己承認有問題（已置頂）** · ⛔ 未登記 4 批（沒有 rollback 開關）

> owner 的定義（2026-08-24 逐字）：「**先上線成果**，但是在後台可以**一鍵否決還原**，**追加原因的 HITL**，但**預設是直接上線**」。
> ⇒ 這是**事後否決權**，⛔ 不是上線前審批門。否決＝翻下表那一格開關，⛔ 不是 revert commit。

| 批次 | 票 | 連續圖片 | 一鍵還原 |
|---|---|---:|---|
| ⚠️ 光束砲家族按 JASS 真相重建：count 6→1、逐支穿回原作模型（⚠️ 像素驗收未過， | #721 #702 #767 | 7 | `tpl-beam-roll` › `params.clip.default` |
| ⚠️ 光束砲對擷圖驗收（20-03/09-04/59-04 逐幀＋形狀分析器）—— ⚠️ 三項 1 | #721 #767 | 40 | `tpl-beam-roll` › `params.count.default` |
| ⚠️ 04-03 龍破斬：火球出膛 → 飛行（⚠️ 0 亮像素）→ 落點爆炸 | #695 | 6 | `vfx-families` › `families.dissipate.enabled` |
| ⚠️ 01-04 超究武神霸斬：七連斬 ＋ 收尾閃光（⚠️ 模型層冷快取 0 頂點） | #695 | 7 | `vfx-families` › `families.shockwaveRing.enabled` |
| 光束砲一族：原地開火（path:static） | #673 #649 | 19 | `tpl-beam-roll` › `params.path.default` |
| …另外 16 批 | | | |

📍 **在後台哪裡看**：左欄 **營運 › 🧑‍⚖️ 批次驗收（連續圖片）**（每一個對照/設定頁的頂端也各有一條與它相關的批核區）。

## 這一輪順手開的票（等你勾要不要併進下一版）

| 票 | 是什麼 | 不修玩家會遇到什麼 |
|---|---|---|
| **#792** | 12 支技能說明寫「吟唱2秒」而實際被夾成 1 秒 | **卡面說謊**（第一·五守則） |
| #793 | MatchesPage 的 mapId 還是裸 ID | 同 #786 的病，另一頁 |
| #791 | genguard hook 把 heredoc 內文的 `>` 當重導 | 擋下合法寫檔（**我今天自己踩到**） |
| #777 | JASS 普查快照過期（662→421） | 「移植完了嗎」永遠答不準 |
| #768–#774 | 量尺沒自證 · `--dry-run` 假紀錄 · TeamGlow 全透明 · 戶籍 · 三個沒有閘的洞 | 見各票 |
