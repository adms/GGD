# v0.29.3 —— 🧹 #300–#899 第二輪（十三條 lane 一天）＋ 拆掉「331 份檔的死路」

> owner 2026-08-27：「**清理完 #300~#899為止** … **別問我了自己判斷** 但是留後台開關可以簡易 rollback
>  … 盡量**平行多工作流**最有效率」

---

## ⭐⭐ 這一版最重要的一件事：**一道牆擋住了三條 lane，而它是一句宣告寫錯**

`genguard` 與 PreToolUse hook 把 **`skillremake:provenance`** 當成
`content/{abilities,champions}/*.json`（**492 份**）的**作者** ⇒ 對每一份都回 `AUTHOR`
⇒ 「改產物被擋／改來源沒有來源」的**死路**。

⛔ 而它不是作者。**逐行讀程式**（⛔ 不是信檔頭）：`stamp_provenance.py` 全檔只寫
`out["provenance"]` 一格 —— 純就地改欄位 ＝ **正規化器**。

| | |
|---|---:|
| 母體 | **492** 份 |
| 改前判 AUTHOR | **492**（全部） |
| 改後判 AUTHOR | **161** |
| ⭐ **從死路解開** | **331** |

⭐ 真的有作者的那些**仍然被擋** —— 這一格只放行「**只有** provenance 認領」的。
⚠️ 它在同一天內擋掉三條 lane（#648 的 39 支 · #745 的 29 隻 · lane C 的內容修復），
而**三條 lane 都正確地拒絕**用 python 檔案 API 繞過唯讀位元 —— 它們是對的，擋錯人的是那張表。

⚠️ 順帶更正 CLAUDE.md 一句話：它寫著「`content/abilities` **422/422**，⭐ 這兩個目錄**零例外**」
—— ⛔ **那句話是錯的**，它被同一條 glob 灌大了。
⭐ **一個被 glob 灌大的統計，讀起來跟真的一模一樣。**

---

## 🚨 差點出貨的靈魂層回歸（第一輪抓到）

#755 讓 `facingLock` 量到 `alignAtHit = **-0.73**` ——
⭐ **攻擊朝著目標的反方向出手**，也就是你抱怨過**兩次**的「林克倒著走路攻擊」。
A/B 證明：換回舊 `combatFeel.ts` ⇒ 6/6 綠。
**處置**：預設翻回 `legacyAbsoluteClosing: true`（欄位拆分留著，只是預設不啟用）。#755 重開。

---

## 🔐 批核頁鎖上了（#796）—— ⭐ 我沒有建立任何憑證

線上實測（匿名）：`/__review/features` **401** · `/__live/sfx-map` **401** ·
`POST feature-verdict` **401** · `/healthz` **200**（刻意匿名，部署後置條件要用）。
用你**已經有的**後台登入（token 轉給平台自己的 admin-only 端點驗）。
🔁 `GGD_REVIEW_REQUIRE_ADMIN=0` 一鍵回到全開。

⭐ 順帶修掉一句過期的散文：那 13 頁對照/設定頁**正式 build 根本不載入**。拿掉了。

---

## 🧹 十三條 lane 的成果

**關掉（帶證據）**：#538 #713 #718 #733 #758 #760 #765 #767 #772 #773 #774 #776
#791 #793 #794 #796 #798 #802 —— ⭐ 其中 **#767 帶終端像素證據**（長寬比 2.28 → 10.23–53.69）。

**⚠️ 沒關的都留了逐項現況**，四個最值得你看的：
- **#765**：那個缺陷**從來沒被修掉** —— 守衛一上就量到 `88×38`，**逐位元等於**當初的量測 → #800
- **#792**：⛔ 票上寫的兩條修法**都會靜默改動遊戲行為**（`castTimeSec` 的來源就是描述裡那個秒數）
- **#538**：⭐ **票的前提是我自己的推測** —— 當日帳本逐字證明「大混戰」是**一張地圖的名字**，⛔ 不是名單
- **#803**：光束砲**永遠不可能是金橘色** —— 出貨 `.glb` 的 5 張貼圖**有 4 張無彩度**，金橘色住在轉檔器沒處理的 `.mdx` PRE2 chunk

---

## 📊 誠實記帳

| | |
|---|---:|
| 今天進場 open（#300–#899） | **106** |
| 現在 open | **98** |
| 新開（都是**查到**的真缺陷） | #797–#804 |

⛔ **「清理完」還沒到。** 剩下的多數是真的還沒做 —— 兩條唯讀稽核 lane 逐張讀碼確認過。
⭐ 但這一天換到的三件事比「數字變小」更有用：
**① 一道擋住 331 份檔的牆拆了** · **② 一個靈魂層回歸被擋在出貨前** · **③ 積壓變成一份會自己更新的索引**。

---

## ⚠️ 未驗收（用詞紀律）
- 像素驗收（火圈 A/B · 集氣特效 · 林克面向序列）：⛔ 仍未補拍
- **#724** 平台安全七項收 5 項（CSP 等一輪線上量測、client 側 refresh token 另一條 lane）
- ⚠️ **這一版動了 Colyseus 的 view tag**（#760 快照剔除，實測實體剔除 50%、位元組省 47.8%）
  ⇒ ⛔ **必須完整重建映像**，煙霧測試要確認**實體真的收得到**
