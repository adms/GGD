# v0.28.5 —— 「改產生物」根治（#771）＋ 後台 13 頁 LIVE 對照·視覺化（#775）＋ additive 修正（#767）

> ⭐ 用詞紀律：玩家看得到的改動只在有終端證據時說「已修」。

## 逐句對票（docs/_daily/2026-08-26.md，282 則、未對票 0）

| owner 說的（逐字） | 票 | 狀態 |
|---|---|---|
| 「go on and **fix it all**」＋「如果有**改產生物而不是改產生器**的情形，你一定要去**追誤會的多個源頭**修正」 | #771 | ✅ 六軸稽核＋複驗（49 筆成立/T0×23）→ 三路修正全落地（下詳） |
| 「GGD 戰情版.md **應該在我本機端阿** 並且我也要看到 對話開票 **逐訊息對應開的票號 全記錄在裡面**」 | #669 | ✅ 根目錄 `GGD戰情版.md`（symlink 永指今天）＋ 282 則逐字內嵌（⛔ 不再是指標） |
| 「請你將以下內容製作成**後臺頁面**，並且**適當分類**，目前後台左測有些**分類已經過長**」 | #775 | ✅ 13 頁全上＋左欄拆兩組（31→16、36→23） |
| 「這些後台頁面的內容都要 **script 實時動態產生**，**不是靜態內容**喔」 | #775 | ✅ /__live request-time 計算（mtime 鍵快取＝與磁碟一致；dataset 自動發現） |

## 🏠 #771：一個自我增強迴圈，斷了

量測沙盒保留 444＋量測工具零解鎖 ⇒ 條件寫入端量到 0 寫 ⇒ 戶籍 0 ⇒ genrun 解鎖不到 ⇒
EACCES ⇒ 補寫入端自解鎖（治症狀，一天第三處）。

**斷點三件**：①量測前整棵解鎖沙盒 ②戶籍重量測 621→**640**（零宣告產生器 3→0；日期帳本
改 **glob**；「語意級條件寫入端」用 `// ggd:writes` 靜態宣告收割）③裁決層 fail-closed
（「無主」三態分流：鎖著無主 🚫 · 檔頭自宣告 🚫 · 真無主 ✓；quarantine 打錯步驟名 exit 2）。

**誤導源**：CLAUDE.md 六處（最貴的：產物底數「331/56」→ **422/422、72/72 零例外** ——
舊數字會讓下一輪算出「91 支技能是手編的」）＋記憶兩份＋6 lane（gen_spec「⛔手寫」假標記 ·
README:696 假宣告 · MIRROR RULE 散文副本 · 兩支閘母體擴到 packages/apps/docs · 44 筆存量進棘輪）。

**端到端**：今天三次 EACCES 的原路（contract:numbers · msgledger:build · speedtiers:build）
genrun 實測全 ✓。新閘 `syncIoDeclaresWrites`（突變驗過）。

⚠️ 量測層自己也犯了一次錯並被**自己的閘**抓到：收割 regex `\S+` 在含空白檔名上
截出幽靈鍵 —— guardMessages 閘當場紅（「主人是 contract:numbers」而真相是兩個主人）。

## 🖥️ #775：後台 13 頁，全部「每次請求當場算」

🌪MDL特效家族 🧭平行處理盤 🎬JASS特效對照 🦗蝗蟲群對照 🧩機制模板·範圍(SVG 俯視圓盤)
🧬特效模板·視覺 🔊技能音效對照 📡出身屬性雷達(SVG) 📈技能級距雷達(421 支五軸 SVG)
✍️說明→JSON(POST；⭐先剝「」對白＝第〇·六守則②) 🧿EX根源三選一 🗡️寶具三選一 📐90支重製對照

實測：13 GET 全 200、第二發 cache hit；MDL 頁與雷達頁截圖驗過。
左欄：新組「五級距·數值」15、「狀態規則」13 —— ⛔ 一頁都沒刪（navSections 23 tests 綠）。

## 🔆 #767：additive 常數修正 —— 驗收④（亮度）**過了**

`ALPHA_ADD` 是錯的（仍乘 src alpha，而 luma-key 把亮度搬進了 alpha ⇒ 加上來的又被乘掉）。
改 **`ALPHA_ONEONE`**（SRC+DEST＝WC3 additive 的翻譯）：亮度中位 **75 → 238**
（owner 原作擷圖 246–254）。⚠️ 驗收③（飽和度）仍沒過：0.24–0.27 vs ≥0.47 ——
加法疊多變白，是**另一個**根因；探針證明動 emissiveStrength ×2 沒有用（本版不動）。

## 出貨數值的裁決歸屬

**owner 原話**：本版零格新裁決。**我挑的（可回頭）**：`stockGlowAdditive=true`（#767 批已登記
rollback）；左欄兩個新分組（回頭＝把 APPROVED_MOVES 兩列刪掉、section 改回去，一個 commit）。
