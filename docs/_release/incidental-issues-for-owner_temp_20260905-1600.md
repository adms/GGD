# 🎫 這一輪順手開的 12 張票 —— ⭐ 要不要併進 v0.39.0，**由 owner 勾**

> CLAUDE.md 第零守則⑧ + 部署協定第 2 步：
> 「順手發現的缺陷一律 `gh issue create`，**不當場修**」＋「deploy 新版的時候要**拿出來問我**檢驗是否要跟這一版修完一起上」
>
> ⚠️ 沒有這一步，⑧ 就從「把排序權還給 owner」變成「把缺陷埋進 issue 堆」。

## ⭐ 玩家看得到的（3 張）—— 建議優先勾

| 票 | 是什麼 | 多嚴重 | 修起來多大 | ⛔ 不修玩家會遇到什麼 |
|---|---|---|---|---|
| **#1002** 🚨 | 跑一次 `pnpm -r test` 會**改掉出貨內容**：68 份技能掉 `castTimeSec` ＋ 一份產生器來源被改，而**三個閘的訊息都指著錯方向** | **緊急** | 中（元兇已點名：`editorSourceSurvivesSync.test.ts` 逾時後留下髒樹） | ⭐ 那個髒狀態一旦被 commit，**68 支技能的施法時間整個消失** |
| **#1005** 🚨 | **7 支技能**在**全新 clone** 上載入後 `castTimeSec` 變 `undefined` —— 而 JSON 與英雄卡鏡射**兩份都有值** | 重要 | 未知（11 個假設已實驗排除，票裡逐條列了） | ⭐ 那 7 支的施法時間在乾淨環境（＝**部署的那台**）上是 undefined |
| **#1000** | Codex `35b231ef3` 刪掉了 71 行「有 vfx-script ⇒ **預設演出讓路**」，並在**同一個 commit** 裡把守著它的東西也拿掉了 ⇒ 光柱／粒子／螢幕提示／浮動文字**不讀** `channelTakeover` | 重要 | 中 | ⭐ authored cast 的演出被預設演出蓋住 —— **兩層特效一起放** |

## ⭐ 閘壞掉（玩家看不到，但它們讓 CI 說謊）—— 5 張

| 票 | 是什麼 | 多嚴重 | 修起來多大 | ⛔ 不修會怎樣 |
|---|---|---|---|---|
| **#998** | collection 清單有**兩個住處**（TS `COLLECTIONS` ↔ Go `KnownCollections`）—— ⭐ **同一個形狀壞過 3 次**，第 3 次是今天 | 重要 | 中（產生器＋閘） | ⭐ 第 4 次：新 collection 的內容在 platform 側被拒 ⇒ 內容驗證整份失敗 |
| **#995** | `docs:readme:check` 的產生器吃 **git-ignored 的營運狀態**（whitelist）⇒ 同一個 commit 在兩台機器上兩個「正確」輸出 | 重要 | 小 | ⭐ 一條**結構上不可能綠**的閘（失敗形態⑨） |
| **#1004** | `editor-target-profile.json` 把**機器狀態**烘進對外契約（本機 213,250 B vs CI 213,115 B） | 重要 | 小 | 同上：兩台機器兩個答案 |
| **#1003** | `/private/tmp` 與 `mktemp -t` 是 **macOS 專屬**，還有 **6 支腳本**寫死它 | 重要 | 小 | ⭐ 那 6 支在 Linux／容器／CI 上直接壞（這個教訓已經用散文寫過**兩次**） |
| **#1006** | 「兩個併發的第一次註冊都變成管理員」——⭐ **修法已落地**，缺的是**決定性守衛**（60 次本機重跑零重現） | 重要 | 中 | 修法沒有守衛看著 ⇒ 下一次改動可以無聲撤銷它 |

## ⭐ 債（可以排到下一版）—— 4 張

| 票 | 是什麼 | 多嚴重 | 修起來多大 |
|---|---|---|---|
| **#999** | `autoAcquireWhileMoving` 的 IDLE 控制組量不到它想量的東西（⭐ 而它刻意留紅擋掉整個 `unit` job —— 今天已繞過） | 重要 | 小 |
| **#1001** | 36 格手寫標籤搬回 Zod 的 `@zh`（棘輪 963 → 999 的那一筆帳） | 一般 | 中 |
| **#997** | `AGENTS.md` 檔頭的**基準 sha 沒有守衛** —— 一份**對外契約**上會過期的數字（Codex 拿它算 `baseCommit` 祖先關係） | 一般 | 小 |
| **#996** | `cover("ping-band-gutter")` 全 repo **零命中** —— 一條宣告了卻沒有人量的覆蓋軸 | 一般 | 很小 |

---

## ⭐ 我的建議（⛔ 你可以整個推翻）

| 併進 v0.39.0 | 理由 |
|---|---|
| ⛔ **一張都不併** | ⭐ 這一版的目的是 owner 逐字要的「**整理乾淨一個版本**」—— 它是一次**收斂**。⛔ 塞新修補進來就不再是「乾淨」，而是「乾淨＋三個新變數」 |

⇒ ⭐ 建議把 **#1002 · #1005 · #1000** 排成**下一版的第一批**（它們三張都是玩家看得到的），
⛔ 而不是塞進這一版。**要塞的話請直接說哪幾張。**
