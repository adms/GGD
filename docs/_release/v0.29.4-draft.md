# v0.29.4 —— 🧱 牆的另一半拆了（387/492 份全開）＋ 第三/四輪清理

> owner 2026-08-27：「**清理完 #300~#899為止** … **別問我了自己判斷** 但是留後台開關可以簡易 rollback」

---

## ⭐⭐ 這一版的頭條：**我第一次只修了一半，而是別的 lane 撞到才發現**

v0.29.3 修好了 `skillremake:provenance` 的 glob 誤判（331 份檔解開）。
⛔ 但 **55 份英雄卡仍然打不開** —— `speedtiers:build` 是**完全同一個形狀**：
一條 glob 認領 `content/champions/*.json`（71 份），而逐行讀
`tools/speed-growth/gen.ts` 的 `withTierLines()`：它只在**原始字串**上刪/插 tier 那幾行，
⛔ 其餘位元組一個都不碰 ⇒ **正規化器，不是作者**。

| | 今天進場 | v0.29.3 | ⭐ v0.29.4 |
|---|---:|---:|---:|
| 判 AUTHOR（改不動） | 492 | 161 | **105** |
| ⭐ 可以改 | 0 | 331 | **387** |

⭐ **一條可以當場檢查的新規矩**（已寫進 CLAUDE.md）：
> **修好一個 glob 誤判之後，去把那張表裡每一條 glob 都問一次** ——
> 「這一支真的**產生**這些檔，還是只**就地改欄位**？」

⚠️ 而發現它的 lane **拒絕用 `GGD_GENGUARD_OFF=1` 繞過** —— 那個判斷是對的：
繞過會把誤判藏起來，下一輪就沒有人會發現它。

---

## 🧹 這兩輪關掉的

**#325**（部分）· **#635** · **#671** · **#716** · **#723** · **#799** · **#805**

三個值得你看的：
- **#671 部署 3 分鐘達成** —— 實測 `全量 146s · 其中 build 118s`（目標 ≤180s）。
  ⛔ 而這張票的 Scope ① 是**假前提**：layer 早就分好了，build 只花 45 秒⛔ 不是瓶頸。
- **#325** —— ⭐ 在今天之前，「**用後台新建一張場地**」產出的就是一張壞的場地：
  schema 說它合法，而消費端讀 `spawns[side][slot % TEAM_SIZE]` ⇒ **slot 1/2 拿到 `undefined`**。
  三處骨架（後台 · 編輯器 · 夾具）都修了，全部從 `TEAM_SIZE` 推導⛔ 不抄字面 3。
- **#716** —— `GameApp.ts` 4,362 → **3,970**。⚠️ 而票上「`main.tsx` 是撞車重災區」的前提
  **不成立**：實測那個檔裡 config doc id 是 **0 個**（表面命中是 `bindGoreToSettings` 裡的
  `gore` 這種子字串）。真正的逐 config 接線在 admin `store.ts` + `ui/App.tsx` → #807。

---

## 🔊 打擊音終於分得開（#763）

12 點的刺拳與 400 點的大絕不再播同一顆 `hit`：`light`(0.55) / `medium`(0.72) / `heavy`(0.95)。
⭐ 選了「搬到雙生 `hitImpact` 事件」而**不是**「客戶端自己推 tier」——
`deriveTier()` 沒有 export，抄一次門檻就是第二個住處，而它會用
「音效跟震動對不起來」這種**認不出來的方式**過期。
🔁 rollback 做成**資料**：三顆 key 任一顆從 `audio-map` 拿掉就逐位元回到今天（不必重建映像）。

---

## ⚠️ 三件我做錯、修好、並記進帳本的

1. ⭐ **在 lane 還在改 content 的時候跑 `ship:check`** ⇒ 撈到的 11 個紅全是它未完成的工作樹改動。
   守則明寫「閘跑的時候不動任何檔」，⛔ 而我從**另一端**犯了它。
2. ⭐ **一次 vitest 只跑了 27 個檔裡的 2 個卻 exit 0** —— 是從摘要行看出來的⛔ 不是離開碼。
   **「離開碼 0」不等於「跑到了」。**
3. ⭐ `model-budget` 的「11 個零幾何 emitter」寫死在測試裡，光束重建修好 10 個之後它紅了，
   而訊息讀起來像「東西不見了」。已改成**只能變少的棘輪**。

---

## 📊 誠實記帳

**今天進場 106 → 現在 95。** 新開 #797–#807（都是**查到**的真缺陷）。
⛔ **「清理完」還沒到**，而那是誠實的：剩下的多數是真的還沒做。

⭐ 但今天換到的是**結構性**的：
**① 一道擋住 387 份檔的牆拆了** · **② 一個靈魂層回歸被擋在出貨前** ·
**③ 積壓變成一份會自己更新的索引** · **④ 批核頁真的到得了你面前而且鎖上了**。

## ⚠️ 未驗收（用詞紀律）
- 像素驗收（火圈 A/B · 集氣特效 · 林克面向序列）：⛔ 仍未補拍
- **#745** `blunt` 類做不了：它是 `BasicAttackSystem.ts` ＋ `combatSfx.ts` 的兩檔契約，
  ⭐ 而**只做 content 那一半會靜默落回 sword**
