# v0.29.1 —— 🧑‍⚖️ 批核頁真的上線了（＋材料/結果分署）

> owner 2026-08-27 逐字：
> 「⚠️ 讀不到批次 Error: 回的不是 JSON（HTTP 200）—— dev server 沒掛 /__review …
>  ⇒ **請同步到線上**，並且**線上批核的結果也同步到本機端**，為避免**讀寫混淆**，
>  請將**批核材料跟批核結果分署不同資料夾**，用**特定存取script的特定存取權限**
>  來管理**避免錯改**」

---

## 📋 逐句對票（⛔ 不是憑印象重寫 —— 從 session transcript 撈的）

| owner 說的（逐字） | 票 | 狀態 |
|---|---|---|
| 「dev server 沒掛 /__review ⇒ **請同步到線上**」 | **#794** | ⭐ 已上線（`5389179a`）⚠️ 線上端待你按一次才算驗收 |
| 「**線上批核的結果也同步到本機端**」 | **#794** | ✅ `bash scripts/review-sync.sh` |
| 「為避免**讀寫混淆**，**批核材料跟批核結果分署不同資料夾**」 | **#794** | ✅ 材料 444 / 結果 644，欄位互不相交 |
| 「用**特定存取script的特定存取權限**來管理**避免錯改**」 | **#794** | ✅ `scripts/review-access.sh`（三層權限） |
| 「(analyze & fix or close issues of #300–#799)」 | — | ✅ 106 張逐張查證（下表） |
| 「整理對話開票進 GGD戰情版 跟 平行批次盤 issue」 | #754 · 戰情表 | ✅ 待裁決批次進第一屏 |

---

## 🧑‍⚖️ 你現在真的看得到那一頁了 —— 去哪裡看

**後台左欄 › 營運 › 🧑‍⚖️ 批次驗收（連續圖片）**
（另外 13 個對照/設定頁的**頂端**也各有一條只顯示該頁相關批次的批核區）

現況：**25 批全部待你裁決 · 0 批未登記**（每一批都寫得出自己的 rollback 開關）。

⚠️ 其中**帶紅旗的已置頂** —— 標題自己就寫著有問題的那幾批，例如
`⚠️ 04-03 龍破斬（⚠️ 0 亮像素）`。⭐ 那一批 **08-25 就在帳本裡標紅了**，
而你 **08-27 才在遊戲裡撞到**。這一版把那條路打通，就是為了不要再有下一次。

**否決 = 翻那一批登記的後台開關**（每一列都印出 config id ＋ 欄位），
⛔ 不是 revert commit。否決**必填原因**，進帳本。

---

## 🔐 材料與結果分署（＝你說的「避免錯改」）

在此之前兩者住同一個檔，所以我每一次登記都會**讀出你的裁決欄位再寫回去**。

| | 落點 | 誰寫 | 權限 |
|---|---|---|---|
| 📦 **批核材料** | `docs/_review/material/batches.json` | 我（`review:register`） | **444**（寫入端自解鎖→寫→重鎖） |
| 🧑‍⚖️ **批核結果** | `docs/_review/verdicts/{local,live}.json` | **你**（本機／線上各一個檔） | 644 |

⭐ 兩份的**欄位集合刻意不相交** ⇒ 寫錯邊是 **schema 擋**，⛔ 不是紀律。
⭐ 本機與線上**各一個檔**（同名 = 同步變成盲目覆蓋）；合併是**讀的時候算的**。

**同步的方向不對稱，而那個不對稱就是安全機制**：
材料**走 git** 上線；結果用一支只搬**一個檔**的腳本回流。⛔ 不 rsync 整棵樹。

---

## 🌐 順帶：13 頁對照/設定頁在線上也活了

它們的實時資料面 `/__live/**` 同病 —— 也只掛在 dev server 上，
所以**線上打開全部是空的**。同一台 sidecar 一起接（純計算、零寫檔）。

---

## 📅 新守衛：一份**過期的證據**比沒有證據更危險（#795）

稽核量到：#721 / #767 的連續圖片報告，**比它要驗的重建早了 89 分鐘** ——
而這件事只寫在檔名裡。

> 沒有證據時我會說「未驗收」；**有一份格式完美但過期的證據時，我會說「已修」**。

現在批核頁會當場指名 `⚠️⚠️ 證據比修復早，請重拍`。
⭐ **它上線十分鐘就抓到我自己的一筆錯誤登記。**
⚠️ 誠實的現況：25 份報告只有 2 份寫了自己的 HEAD ⇒ 另外 23 批**判不出來**
（那是 `ℹ️ 判不了`，⛔ 不是綠燈）。

---

## 🔢 #300–#799 逐張查證（106 張）

| 段 | 關閉 | 補「現況」註解 | 未做 |
|---|---:|---:|---:|
| #700–#799（62 張） | **1**（#776 後台搜尋） | 9 | 52 |
| #300–#699（44 張） | 0 | 4 | 40 |

⚠️ #300–#699 那 44 張在 08-26 已被逐張讀碼過，今天重驗 24 條機器可判斷言
**24/24 一致** ⇒ 0 關閉不是漏查（74 個新 commit 全落在 #700–#790）。

---

## ⭐ 兩件要你勾的

1. **`configForms.ts` 4,978 → 5,783**（一天 +113）—— 約 **11 天**撞第〇·七守則的
   7,000 強制拆線，而它同時是撞車重災區。要不要現在排？
2. **順手開的票**（#791 hook 誤判 · #792 12 支技能卡面說謊 · #793 裸 mapId ·
   #795 證據時間身分）—— 要不要併進下一版？

---

## ⚠️ 未驗收（用詞紀律）

- **線上批核頁**：鏈路已接上，⛔ 但要你在線上真的按一次「保留/否決」才算驗收
- **像素驗收**（火圈 A/B · 集氣特效 · 林克面向序列）：⛔ 仍未補拍
