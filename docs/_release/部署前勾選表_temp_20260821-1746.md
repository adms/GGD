# 🗳 部署前勾選表 — 2026-08-21 17:46 (GMT+8)

> 部署協定**第 2 步**（owner 2026-08-05：「deploy 新版的時候要**拿出來問我**檢驗是否要跟這一版修完一起上」）。
> ⚠️ 少了這一步，第零守則⑧ 就從「把排序權還給 owner」變成「把缺陷埋進 issue 堆」。
>
> **本文件唯讀盤點，⛔ 沒有改任何程式或內容。**

| | |
|---|---|
| 全部開著的票 | **326** |
| **這一輪**（2026-08-19 起，＝上次真的部署的 v0.21.3 之後）開的票 | **78**（開 48 / 關 30） |
| 其中**仍然開著** | **48** |
| ⭐ 其中**已經落地、只是票沒關** | **13**（實測驗過，見 §3） |
| ⇒ **真正還沒做、要 owner 排序的** | **35** |
| 🔴 其中 **owner 早就答過、而票上仍寫「待裁決」** | **2**（#423 · #448，見 §2 紅字） |
| ⭐ **建議併進這一版** | **13**（見 §2） |
| 待部署的 commit | `v0.21.3..HEAD` = **57** 個（`v0.23.0..HEAD` = 11 個） |
| 其中 subject 沒有真票號的 | **25 / 57**（`v0.23.0..HEAD` 是 **7 / 11**）—— 實質的 8 個，見 §4 |

⚠️ **本文件的「量到的」= 我讀了出貨檔／程式碼確認過；「未驗」= 只讀了票的內文。** 兩者分開標。

---

## 🔴 §0 這一版**部署當下**必須做的兩件事（⛔ 不是「可以排」）

| # | 是什麼 | 為什麼是現在 | ⭐ 我的建議 |
|---|---|---|---|
| **A** | **部署後按後台「🧹 清理變身態」那顆按鈕** | 量到的：本機 `data/curation/whitelist.json` 已經是 **49**，⭐ 但它在 `.gitignore:34` 裡 ⇒ **`git push` 與 `deploy` 一個位元組都碰不到線上那份**。線上仍是 **59**。⇒ **玩家在選人畫面照樣選得到 10 個變身態**（它們沒有完整技能組、有的沒有 description），而這一版所有平衡量測的母體都是 49。 | ✅ **一定要做**。這正是 `f0779b70` 那顆按鈕存在的理由；不按＝這一版的白名單改動對玩家等於沒發生 |
| **B** | ⛔ **不可以用 `--content-only` 部署** | 這一版動了 `packages/shared/src/content/schema/config.ts`（新增 `speed-growth-tiers` 等 config tag）＋ sim 的位移碰撞。`content/` 是 live bind-mount 而映像是 build 時烘的 ⇒ 「新內容 + 舊映像」正是 2026-08-02 生產故障的組合（內容載入整份失敗 → fail-open 退回 2 隻骨架） | ✅ 走完整重建（`host-deploy.sh` 預設），煙霧測試**開全新分頁**讀 `[client] content loaded: N champions` |

---

## 🎯 §1 排序表 —— 按「**玩家看不看得出來**」，⛔ 不是按票號

> 🟥 = 玩家一定會遇到　🟧 = 玩家看得出來　🟨 = 要留意才看得出來　⬜ = 玩家看不到（工程／流程／契約）

### 🟥 A 級：玩家一定會遇到

| 票 | 是什麼 | 嚴重度 | 修起來多大 | **不修玩家會遇到什麼** | ⭐ 我的建議 |
|---|---|---|---|---|---|
| **#472** | 白名單收斂到「上架中」＝ 59 → 49 | 🟥 T0 | **零**（程式已完成，只差按鈕） | **選人畫面看得到 10 個變身態**：技能組是 w3x 殘骸、部分沒有 description、選了以後那一場就是壞的 | ✅ **併這一版** — 見 §0-A，只是一個部署後動作 |
| **#469** | `roster.json` 的 `hiddenChampions` 是 `[]` | 🟥 高 | 小（填一個陣列 + 一輪驗證） | 五位隱藏英雄（死之王 · **飛鼠先生** · 喪標麥可 · 黑人牙膏 · 黑化Saber）**玩家永遠隨機不到** ⇒ #336 的彩蛋機制上線了但沒有內容。⭐ **而且它讓另一個裁決的前提落空**：owner 2026-08-21 說「飛鼠先生本來就會變成隱藏角色，所以強一點合理」⇒ 天譴不壓數值 —— 而他今天**不是**隱藏角色，那條豁免現在沒有依據 | ✅ **併這一版**：要嘛填名單、要嘛把天譴的豁免理由改掉。⛔ 兩件事不能只做一半 |
| **#461** | 卡面幾何有三種打架的來源；冷卻卡面秒 vs 實際秒 | 🟥 高 | 中（文案規則 + 產生器 + 兩個顯示點） | 同一張技能卡上，**文案寫「120 秒」而正下方的數字欄寫「24.0s」**（`combat-env.cooldown=0.2`）。44-01 死神之眼卡面寫「施法距離 2」而引擎跑 **12**（`rangeTier:極大` 蓋掉 `range`）。⇒ 玩家**沒有辦法用卡面做取捨判斷**，而 owner 立這條規則的理由正是「不然很難讓玩家判斷取捨」 | ✅ **併這一版**：這一版剛把 420 份說明改成從 JSON 推導（`68682b43`），⭐ **卡面現在最接近可信，這是收尾的最便宜時機** |
| **#429** | 回合結束沒有真正清乾淨 | 🟥 高 | 大（中場生命週期重構） | owner 原話：「你**每次都偷懶有奇怪的東西跑到下一場**」。特效／英雄狀態／座標帶進下一場（#416 的根因是 `lastPos` 停在中場的 x=−24），而且**一進戰鬥就 LAG**（沒有預載） | ⚠️ **拆一半併**：本版先做「清特效 + 清狀態」那半（便宜、玩家立刻看得到）；預載那半排下一批 |

### 🟧 B 級：玩家看得出來（靈魂層 —— 技能行為／特效／遊戲機制）

| 票 | 是什麼 | 嚴重度 | 修起來多大 | **不修玩家會遇到什麼** | ⭐ 我的建議 |
|---|---|---|---|---|---|
| **#439** | 動地跺 `WarStompCaster` 三樣（模型／音效／地裂）都沒落地 | 🟧 高 | 中（一個資產 + 一族綁定） | **66 支技能**播的是**程序塵環**，而原作那個特效是全 repo 引用第 1 名（150 個引用點）。⇒ 玩家看到的「震擊」是一圈灰塵，沒有地裂也沒有撞擊音 | ✅ **併這一版**：#443 剛把 Blizzard 資產全面解禁 ⇒ **擋住它的那條規則今天已經不存在了**，一支技能落地帶動 66 支 |
| **#452** | 場地之外是全黑（13 張圖） | 🟧 中 | 中（13 張 2D 背景，owner 說可地端生成） | 每一場比賽的視野邊緣都是一片近黑色（`scenery.palette.void`，例 `#150a10`）——**13 張主題差很多的圖，場外長得一模一樣**。⇒「無限城」與「櫻花神社」的天空是同一種黑 | ⬜ **不併**：純美術增益，量大，且 #453（FATE 風格統一）會重做一次 ⇒ 兩張一起做才不會白工 |
| **#425** | `wave.*` 9 份裡 5 份與別的彈道共用拖尾 | 🟧 中 | 小（5 筆綁定） | **龜派氣功波跟氣彈長得一模一樣**；`wave.arcane` 有 4 份彈道共用同一個 vfx。⇒ 玩家分不出對面丟過來的是哪一招 | ✅ **併這一版**：5 筆綁定，靈魂層，CP 值最高的一張 |
| **#431** | 家族特效綁定沒有「owner 設計覆寫」那一層 | 🟧 中 | 中（schema 加一層 + 產生器） | owner 點名「飛鼠天譴要改成立起來的光柱」—— 引擎**有** `lightColumn`，內容側**無處可寫** ⇒ 那一招今天改不動 | ✅ **併這一版**：它是**擋路的機制缺口**（第〇·五守則：機制在引擎、技能在 JSON），不修的話之後每一次特效覆寫都要改程式 |
| **#423**<br>**#404** | 千年練成／樹海降臨要真的召喚樹精 | 🟧 中 | 中（一具身體，⛔ 已經不是裁決） | 卡片寫「招喚樹精 4/6/8 棵」，**一棵都沒召喚**。原作證據鏈三段完整（`A0GN`→`A0GO`→`n00Q`）。⚠️ 而且「改描述」**不是零代價的解法** —— 買了召喚流派道具的玩家，這招一個單位都不加 | 🔴 **併這一版 —— owner 早就裁決過了，⛔ 我差一點又問一次**（見 §2 底下的紅字） |
| **#417** | 同一個技能編號在兩份文件裡數值不一樣 | 🟧 中 | 小（對帳 + 一條閘） | **04-03 龍破斬**：`godie-h020.e` 半徑 **8.25**、`godie-hjai.e` 半徑 **6.0**。⇒ 同一招在兩位英雄身上大小不同，而編號是 JASS 對照的 join key（⛔ 不可浮動） | ✅ **併這一版**：一條閘 + 一次對帳，而且它會擋住之後每一次同型漂移 |
| **#441** | 2 位英雄沒有戰鬥語音包 | 🟧 中 | 中（兩個語音包） | **白木老樹精**與**傑·富力士**：不會喊技能名、沒有受傷聲、沒有擊殺／死亡／被控語音（點自己與名言仍在）。⇒ 玩到這兩位會覺得「這角色是啞的」 | ⬜ **不併**：兩位都不在核心體驗上，語音批次一起做比較划算 |
| **#464** | 冷卻逐階不同的 24 支：**5 支疑似資料錯** | 🟧 中 | 小（5 筆數值 + 一條閘） | Avalon 12→**60**（升級冷卻變 5 倍）· 界王拳**升級反而變慢** · 暗步滿階 **0.2 秒**（等於沒有冷卻）。⇒ 玩家升級技能之後**變弱**，或拿到一個沒有冷卻的技能 | ✅ **併這一版**：owner 已裁決（「附錄 A => yes, yes」）。⭐ 5 支資料錯是**玩家立刻感覺得到的**，而它們只是 5 個數字 |
| **#420** | 44-01 死神之眼：施法距離「極大」、冷卻 60→30 | 🟧 中 | 小（兩格） | owner 已裁決的設計改動沒落地 ⇒ 夜神月今天弱於 owner 想要的樣子（⚠️ 落地後傷害至少翻倍） | ✅ **併這一版**：#414 的級距表這一版已經做完了 ⇒ **擋住它的東西沒了** |
| **#448** | 30-00 攝影機重做：標記 → 空放瞬移 | 🟧 中 | 中（一個新機制 + 兩份英雄卡） | owner 的新設計沒落地；臭作那一招今天還是舊的偵查技（`range` 已從 29.33 收成 12.0，但**兩段式行為不存在**） | ✅ **併這一版**：⛔ 這**不是**一個待裁決 —— owner 2026-08-19 已經把設計逐字給完了（「給予指定敵方英雄標記，之後施展若無指定敵方英雄單位代表順移至敵方身邊」）。做不完就做到能收的地方，⛔ 不要排到「未來版次」（owner 2026-08-20：那等於丟棄） |
| **#451** | 連鎖閃電是 O(N²) 機制 | 🟨 中 | 中 | 皮卡丘打雷 + 飛鼠天譴：「範圍內每個單位各觸發一次連鎖閃電」。⇒ 人多的時候**掉幀**，而且傷害是天文數字（owner 已裁決按 `decay` 算總計 ≈ 12,700 ⛔ 不是 24,000） | ✅ **併這一版**：分級定義那半 owner 已裁決「只改定義⛔不動數值」＝ 很便宜；O(N²) 那半排下一批 |
| **#462** | 「描述↔JSON 一致性」從來沒有被量過 | 🟨 中 | 中（一支掃描器） | 這一版剛把 420 份說明改成推導（`68682b43`）——⭐ **但沒有任何東西在量它是不是真的一致**。⇒ 下一次有人手改一句文案，卡片又開始說謊，而**沒有東西會紅** | ✅ **併這一版**：⭐ 它是 §1-A 的 #461 與這一版 420 份改動的**守衛**。⛔ 做了改動不做閘＝第零守則的元規則（判準 0/4 全破） |

### ⬜ C 級：玩家看不到（工程／流程／契約）—— ⛔ 這一版建議**不併**

| 票 | 一句話 | ⭐ 我的建議 |
|---|---|---|
| **#438** | 技能設計模板化（四軸五級距 → 自動生成） | ⚠️ **這一版其實已經做掉大半**（`5eb4384c` / `4c4fb939` / `7bd78fa3`）⇒ **重新盤點它剩什麼**，⛔ 不要當成沒開始 |
| **#447 #445 #446 #414** | 傷害／冷卻／耗魔／施法距離四軸級距 | 同上：這一版大量落地，⭐ owner 2026-08-21 又給了新公式（`傷害 × (1 + AP × 0.5%)`、`intToAbilityPower → 4`）⇒ **要確認新裁決有沒有全部落地再決定關不關** |
| **#444** | 級距的錨點作廢（場地半徑 24 → 24–42） | 不併：它是 #414 的地基，而級距已改成推導 ⇒ 先確認還成不成立 |
| **#443** | 版權規則變更（Blizzard 全面解禁 + 資產優先序） | ⭐ **規則本身要今天寫進 CLAUDE.md／記憶**（零成本），資產落地跟著 #439 走 |
| **#449** | 資訊統一原則：清單一律 JSON → script 產生 | 不併：README 場地過期不影響玩家 |
| **#450** | 特效還原的**驗收標準**（龍破斬 · 世界終結 · 超究武神霸斬） | ⭐ 不併，但**要記住**：它定義了「特效做好了」這句話憑什麼可以講。⛔ 這一版不要宣稱特效還原度 |
| **#453 #457** | FATE 視覺識別統一 · 產圖器支援 LoRA/SDXL | 不併：純產能工具 |
| **#454** | 大廳「宿敵排行榜」 | ⏸ 卡在一個 owner 一句話的定義（(a) 交手次數／(b) 恩怨值／(c) 苦主）⇒ **順便問掉**，⛔ 不要等下一輪再問 |
| **#460** | 四軸級距的五個貫穿性實作約束 | 不併：檢查清單性質，跟著四軸走 |
| **#467** | 拆檔案讓平行化不卡住 | ⚠️ owner 明說「**建議你優先做因為關鍵**」，⭐ 而這一版已落地大半（`234d96b9` / `c14f9de2`）⇒ **重新盤點剩什麼** |
| **#473** | 「啟用上架」當下自動跑稽核 | ✅ **建議併**（唯一的 C 級例外）：它把第零守則的「判準」換成「閘」，而 §0-A 那顆按鈕已經是同一頁 ⇒ 邊際成本很低 |
| **#475 #480 #484 #430** | 逐支級距裁決回寫 · 創建新英雄自動化 · 逐則對票自動化 · 版本 NOTE | 不併：流程／後台，玩家零感知 |
| **#427 #468 #458 #459 #421 #422 #440 #474 #482 #483 #485 #419 #433** | — | ⭐ **這 13 張已經做完了，見 §3。建議直接關票** |

---

## ✅ §2 建議併進這一版的：**13 張**（逐張一句話）

| 票 | 一句話理由 |
|---|---|
| **#472** | 部署後按一顆按鈕，否則玩家照樣選得到 10 個壞掉的變身態（⛔ 白名單住 gitignore，deploy 碰不到） |
| **#469** | 隱藏英雄名單是空的，而**另一個裁決（天譴不壓數值）的理由建立在它不是空的之上** |
| **#461** | 卡面寫 120 秒、數字欄寫 24.0s —— 這一版剛把說明改成推導，現在是最便宜的收尾時機 |
| **#462** | 給上面那 420 份推導出來的說明**配一條會紅的閘**，否則下一次手改就又開始說謊 |
| **#464** | 5 支冷卻資料錯（升級變慢、滿階 0.2 秒），owner 已裁決，只是 5 個數字 |
| **#420** | owner 已裁決的死神之眼改動，擋住它的 #414 級距表這一版已經做完了 |
| **#425** | 龜派氣功波跟氣彈長一樣 —— 5 筆綁定，靈魂層 CP 值最高 |
| **#439** | 66 支技能播程序塵環，而 #443 剛把擋住它的版權規則解掉 |
| **#431** | 「天譴改光柱」今天無處可寫 —— 這是機制缺口，不修的話每次覆寫都要改程式 |
| **#417** | 同一個技能編號兩個半徑（8.25 / 6.0），編號是 join key ⛔ 不可浮動 |
| **#473** | 「啟用上架」當下跑稽核 —— 判準換成閘，而它跟 §0-A 那顆按鈕同一頁 |
| **#423 #404** | 千年練成召喚樹精 —— ⭐ **owner 已裁決**，見下面紅字 |
| **#448** | 30-00 攝影機標記→瞬移 —— ⭐ **owner 已把設計逐字給完**，不是待裁決 |

### 🔴 ⛔ 我在寫這份表的過程中差一點違反第零守則 —— 記在這裡當證據

我原本把 **#423**（千年練成要不要真的召喚樹精）與 **#448**（攝影機兩段式）列成「要問 owner」。
跑了 `bash scripts/asked-before.sh` 之後：**兩張他都已經答過了**，而**兩張票的 body 裡都還寫著「待裁決／需要平衡裁決」**。

| 票 | owner 原話（逐字，⛔ 不是我的摘要） | 在哪 |
|---|---|---|
| **#423** | 「**yes, 跟殭屍一樣只是不會移動加上屬於施展技能方，加上有生命週期時限**」 | `docs/_daily/2026-08-19.md:117`（⚠️ 那一列的票號寫成 `#421`，是重編號前的） |
| **#448** | 「因為**已經不是 dota 大地圖**，請你幫我這招改成**給予指定敵方英雄標記**，之後施展**若無指定敵方英雄單位代表順移至敵方身邊**，記得修改說明、JSON 跟編輯器契約、後台設定」 | `ledger-source_temp_20260820-0135.md:1285` |

⇒ **第零守則的那個迴圈今天仍然是活的**：答案在帳本裡，而票上寫著「待裁決」。
⭐ **建議**：這一版部署前用 `bash scripts/ruling.sh 423 448` 把這兩則逐字寫回票 body。
⚠️ 另外 **#433 的 body 掛的是別張票的裁決**（牆/移速/白名單三件，那是 #487/#486），
`ruling.sh` 把裁決灑到多張票上時會產生這種錯配 —— 讀票的人會以為那就是這張票的答案。

⏸ **真正還沒答過的只有一張**：
- **#454** — 「宿敵」用哪一種定義？（(a) 交手次數／(b) 恩怨值／(c) 苦主）
  ⭐ 我的建議：**(a) 排序 + 顯示 (b) 的戰績**。⛔ 但它是體驗層，可以不併這一版，只要**現在問掉**就好

---

## ⭐ §3 已經落地、只是**票沒關**的 13 張（⛔ 它們不是缺陷）

> ⚠️ 這一節的存在本身就是一個發現：**開票數量在誇大待辦**。
> 逐張都是我讀出貨檔／程式碼確認的，⛔ 不是看 commit message 猜的。

| 票 | 量到的證據 |
|---|---|
| **#419** | `content/abilities/godie-e00r.q.json` → `castType:"self"` + `passive` 區塊，描述已改成「不必施放，也不耗魔」。⭐ **它是 CLOSED 的 #489 的重複票** |
| **#421** | `GameApp.ts:3593` → `occludeArgsFor(this.arenaDef.zones, viewerZone, center)` —— viewerZone 已經傳進去了（`87061c8c`） |
| **#422** | `apps/game-server/src/match/lobbySpawnSpread.test.ts` 存在；`arenaRules.test.ts` 的註解逐字記著「GH#422 開場擺位改成四隊分開站」 |
| **#427** | `generateFamilyContent.ts:154` → `if (!owned.has(k)) out[k] = v;`，`owned` 已從整塊物件改成 **key 集合**；:171 的註解記著被吃掉的 197 格 |
| **#433** | `godie-orkn.passive` 的 `range` 已從 29.33 收成 **12.0** + `rangeTier:"極大"`。owner 已裁決「yes, yes」。⚠️ **抽驗 1/14**，關票前建議跑一次離群普查 |
| **#440** | `GameApp.ts:2277 pushVfxSound()` 改走 `vfxSoundCues(...)` → `spatialSourceFor`，檔頭註解逐字記著 GH#440 |
| **#458** | `abilitySystem.ts:185-193 groundTargets()` 已經讀 `def.targetsEnemies`，兩個方向都擋 |
| **#459** | ① `godie-hvsh.e` 的效果樹已完整（結界 buff × `onInterval` 範圍傷害 × 吸血 heal × 攻速/移速 −50% × `onKill` grantAttribute）② `godie-efur.w` 已有 `resourcePct.perRank [0.06,0.08,0.10,0.12]` |
| **#468** | `descriptionClaims.ts:255-260` 已**結構化**處理 `resourcePct`（讀 `resource` + `basis`），⛔ 不是把名字塞進 `MAX_HP_PCT_KEYS` |
| **#474** | 出貨 **71/71** 位英雄的 `buildPriority` 全部是空陣列；後台三處欄位已拔（`contentFields.ts` · `NewHeroPage.tsx` · `heroTemplate.ts`）；12 條梯子已另存 legacy |
| **#482** | `content/config/form-visuals.json:16` → `attachScale: 0.4161`（舊 0.3221），三個住處 + `attachmentScale.test.ts` 都到位 |
| **#483** | `arenaRules.test.ts` 已改成掃 seed **1200–1219**、斷言「至少 3 個命中」，實測 8/20（2.7× 餘裕）；`toBeGreaterThan(0)` 沒被拿掉 |
| **#485** | `e75231a1` 把母體改成 49 位可選本體；`e3c5743b` 把手寫的「53」換成推導 |

---

## 🧾 §4 沒有票的 commit

### `v0.23.0..HEAD`（11 個）→ **7 個 subject 裡沒有真票號**

| commit | subject | 判定 |
|---|---|---|
| `f0779b70` | 一鍵清理變身態 | ⚠️ 對應 **CLOSED #486**，但 subject 沒引用 |
| `5ea04900` | 位移的終點必須落在牆的這一邊 | ⚠️ 對應 **CLOSED #487**，但 subject 沒引用 |
| `00935377` | 鑄技工坊預覽讀死了 TS 常數 | ⚠️ 對應 **CLOSED #488**，但 subject 沒引用 |
| `948c8f29` `fd7026ec` | 逐則對票 + 作戰板重生 | ⬜ 文件例行，實質屬 #484 |
| **`bedfcfc5`** | 移速／攻速每級成長五級距 | 🔴 **`#speed-growth` 不是一個 issue** —— 它是一個看起來像票號的字串。**沒有任何票** |
| **`7bfd74bb`** | 三圍成長歸 0 補完 20 個變身態 | 🔴 **完全沒有票**（owner 口頭「所有角色」） |

### 🔴 ⭐ 真正的問題不是「沒有票」，是**兩個決策只活在 commit message 裡**

> 第零守則：「⛔ 只活在留言裡的更正等於不存在」。這裡是它的變體 —— **只活在 commit body 裡的待裁決**。
> ⚠️ commit message 是 `git log` 才看得到的東西；owner 不會去 `git log` 找他要裁決的事。

| 出處 | 逐字（從 commit body 抄出來） | 為什麼要 owner 看 |
|---|---|---|
| `bedfcfc5` | 「🔴 owner 那一則的『**49 位全部給中**』與『維持今天的值 / 零平衡改動』**打架**（梯子上的中是 ms 0.02 / as 0.03，全給中 = 49 位一起變快，**LV99 移速 5.8→7.76**）」 | 實作照第〇·六守則①「內文修正標籤」選了**零平衡改動**。⇒ **owner 說的那個結果沒有發生**，而他不知道 |
| `7bfd74bb` | 「⭐ `as` 那一軸**凍結未動** —— 它不在 `appliesTo`⋯重推導會做出一次無聲的攻速平衡改動。**三條出路已具名待裁決**」 | 「待裁決」在 commit body 裡＝**沒有人在等它** |

⭐ **建議**：這兩條**現在**用 `bash scripts/ruling.sh` 開票（一行，⛔ 不要等下一輪）。

### `v0.21.3..HEAD`（57 個，＝這一版真正要部署的範圍）→ **25 個沒有票號**

其中 **17 個是文件／作戰板／規則例行**（⬜ 不需要票）。**實質的有 8 個**，除了上面 6 個還有：

| commit | subject | 判定 |
|---|---|---|
| **`68682b43`** | **420 份技能說明改成從 JSON 推導** —— 卡面上一個手打的機制數字都不剩 | 🔴 **這一版對玩家影響最大的一個改動，而它沒有票**（相關 #461/#462 都沒被引用） |
| `49caceb8` | 卡面值 ↔ 實際值：`{{cd!}}` 讓卡片說得出「你其實只等 9 秒」 | ⚠️ 這是 **#461 的一半**，沒引用 |
| `e3c5743b` | 手寫的「可選本體 53」擋住 content:build → 三個數字全部改成推導 | ⚠️ 這是 **#485 的一半**，沒引用 |

⭐ **`68682b43` 值得單獨拿給 owner 看**：420 份卡面文案換了來源，是這一版**玩家第一眼會看到的東西**，
而 release note 的「逐句對票」表上沒有它 ⇒ 部署後如果文案有問題，沒有人查得到它是哪一次改的。

---

## 📌 §5 給 owner 的三個勾選格

```
[ ] ① §2 的 13 張全部併進這一版
[ ] ② 只併 §2 前 6 張（#472 #469 #461 #462 #464 #420），其餘排下一批
[ ] ③ 我自己挑（在上表逐張打勾）

[ ] ④ 唯一還沒答過的一張，現在回答：
      · #454 「宿敵」用哪一種定義？(a) 交手次數 / (b) 恩怨值 / (c) 苦主
        （⭐ 我建議 (a) 排序 + 顯示 (b) 的戰績）

[ ] ⑤ §4 那兩個「只活在 commit message 裡」的決策，現在開票：
      · 移速五級距：要「49 位全部給中」（LV99 移速 5.8→7.76）還是「零平衡改動」？
      · 攻速成長那一軸要不要一起重推導？（三條出路已具名）

[ ] ⑥ §3 的 13 張直接關票（我已逐張驗過落地）

[ ] ⑦ 部署前跑 `bash scripts/ruling.sh 423 448` —— 把你早就給過的兩則裁決寫回票 body
      （⛔ 今天它們還寫著「待裁決」，見 §2 的紅字）
```

---

<sub>⚠️ 本文件是 `_temp_` 暫存產物（第零守則的命名慣例）。過 7 天由 `bash scripts/temp-sweep.sh --move` 搬進 `docs/legacy/_temp-retired/`。</sub>
