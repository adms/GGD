# 🎛 開關列表 —— 2026-09-06 夜間批次

> owner 2026-09-06 逐字：「盡量把所有票解決完我要去睡了 **有問題做成開關 不要卡在我這裡 我醒了你可以回我開關列表**」

⭐ 每一格都是**我挑的預設**；要翻就翻，⛔ 不必問我。「住處」是你要去哪裡翻。

## 🎮 玩家看得到的（後台三個住處，存檔當下生效）

| 開關 | 預設 | 我為什麼這樣挑 | 住處 | 票 |
|---|---|---|---|---|
| `one-shot-clamp.enabled` | **true**（原 false） | #928 的止血；`maxFractionOfMaxHp` 維持 1.0。量到：3 場／30 回合／5,224 發裡 **93 發（1.78%）會被夾、平均削 40.9%**，最大單發 `godie-nbbc.r` 2153% | 後台「一擊必殺夾限」☠️ 頁。⚠️ 翻回去要重啟 shard，⛔ 不可 `--content-only` | #1017 |
| `ugc.digestRecompute` | **on** | 投稿的 digest 改由伺服器重算比對，對不上 400 指名檔；off ＝ 回到「客戶端自己說的」 | 後台 UGC 頁 | #1022 |

## 🔧 CI／腳本（環境變數；不設＝預設）

| 開關 | 預設 | 意思 | 票 |
|---|---|---|---|
| `GGD_SELF_CAST_DAMAGE_GUARD` | strict | `castType:self` 的 damage 沒明寫 `applyTo` ⇒ 內容閘紅；`warn` 只印警告不擋。⚠️ 原本做成後台 config `damage-rules.abilitySelfDamageGuard`，被 `configDecorationCensus` 棘輪擋下（玩家翻了什麼都不會變的欄位＝裝飾）⇒ 改環境變數 | #1019 |
| `GGD_WORKSPACE_TEST_CONCURRENCY` | 1（pnpm 原本 4） | CI 逐包跑測試 —— 4 vCPU × 4 包 × 16 forks ＝ 11.6× 超額訂閱造成 worker RPC 逾時 | #1014 |
| `GGD_VITEST_FORKS_PER_PACKAGE` | 空＝nproc | 每包 forks 上限 | #1014 |
| `GGD_VITEST_MAX_FORKS` | min(16, 核數) | game-server 本機 forks | #1014 |
| `GGD_VERDICT_MIN_SUMMARIES` · `GGD_VERDICT_STRICT_RPC` | — | `vitest-verdict-check.sh`（已裁決＋已跳過＝＝總檔數）的嚴格度 | #1014 |
| `GGD_GENINPUT_OFF=1` | 閘**開** | commit 時「改了產生器的輸入就得帶著新產物」 | #1026 |
| `GGD_GENINPUT_MAX_READERS` | 22 | 「專屬輸入」的分母 | #1026 |
| `GGD_LEDGER_NO_REGEN=1` | 重生成**開** | `ruling.sh` 收工自帶 `board:roll`＋`board:build` | #1026 |
| `GGD_RULING_MSGTIME_OFF=1` | 用**訊息時間** | `ruling.sh` 帳本列鍵；=1 退回執行時間 | #1028 |
| `GGD_PLAYERNOTE_SCOPE` | commits | 玩家公告只算區間內有 commit 指名的票；`updated` ＝ 舊行為 | #1021 |
| `GGD_REFERENCE_CURATION` | snapshot | README 開放名單讀版控快照；`live` ＝ 讀本機白名單 | #995 |
| `GGD_EDITOR_PROFILE_CURATION` | placeholder | 對外契約不烘本機白名單；`live` ＝ 舊行為 | #1004 |
| `GGD_CURATION_WHITELIST` · `GGD_ABILITY_PROSE` | 未設 | 產生器的測試接縫 | #995 #1004 |
| `GGD_TESTRUNNER_PYTHON`（Dockerfile ARG） | 1 | testrunner 映像裝 python3/Pillow/mpyq；`--build-arg …=0` 回舊映像 | #1013 |
| `GGD_PYTHON` | 未設 | 覆蓋 `findPython()` 候選清單（仍要過 sentinel） | #1013 |
| `GGD_CONTENT_API_URL`（platform） | 空 ⇒ **503 fail-loud** | Submit（開關 on）與 Promote 都要它；本機 `http://127.0.0.1:8787` | #1022 |
| `TMPDIR` · `GGD_LIVE_CACHE_DIR` · `--sandbox` | os.tmpdir() | 13 支腳本不再寫死 macOS 的 `/private/tmp` | #1003 |
