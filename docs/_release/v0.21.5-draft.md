# v0.21.5 —— 級距名改回 owner 的五個字 · 連鎖閃電逐跳 · 兩條判準換成兩個閘

> ⏸️ **草稿。** owner 說「修正完今天我們談話所開的票，我們再 push note deploy」——
> 兩個工作流還在跑（12 條 lane），完成後這一份會補上它們的結果再發。

---

## ⭐ 主體：級距名 `小/中/大/超大/極大` → **`極小/小/中/大/極大`**（GH#463）

owner：「明明就是 **極小 小 中 大 極大** 五級距怎麼又變成六了，**沒有超大這種東西**哪裡來的？」

⚠️ 「超大」**不是憑空發明的** —— owner 2026-08-11 給 AoE 級距時親口說過，當時是**四級**。
而他在 **2026-08-19 同一天**給冷卻級距時已經改口成「一樣是**極小小中大極大**」。
⇒ 那天有**兩份 owner 規格並存**，而合併時挑了**舊的那一份**。照第〇·六守則新的贏 ——
**錯的不是造字，是沒發現同一天已經有更新的詞彙。**

### 遷移：505 筆內容同批重寫，**值一個都沒動**

| 橫木 | 值 | 舊名 | 新名 | 筆數 |
|---:|---:|---|---|---:|
| 1 | 3 | 小 | **極小** | 47 |
| 2 | 4.5 | 中 | **小** | 44 |
| 3 | 6 | 大 | **中** | 77 |
| 4 | 8 | **超大** | **大** | 85 |
| 5 | 12 | 極大 | 極大 | 252 |

**253 筆換位置，零手感變更。**

### ⛔⛔ 為什麼這次遷移特別危險
「小」在改名前後**都是合法值**，只是從第 1 格變成第 2 格。
⇒ 只改 enum 不重寫內容的話，**253 筆會被靜默重新解讀成大一級**，
而 Zod 過、`content:build` 過、全套測試綠 —— 因為每一個零件都是對的。

### ⭐ 遷移過程中守衛抓到三次「差一點就靜默壞掉」
1. **`fieldAdoption` 棘輪** —— 抓到漏改 `distanceTier`
2. **新寫的 `skillTierNames.test.ts`** —— 抓到重生成把「超大」**寫回** 3 個檔
   （`batch1.py` 的 `TIER_R` 與 **19 處 `radiusTier=` kwarg** 還是舊名字）
3. **`skillTierLadder` 的兩份文件對帳** —— 抓到 **77-01** 從一致（6 vs 6）變成不一致（8 vs 6）。
   ⭐ `radiusTier="大"` **一個字都沒動**，但它從第 3 格變成第 4 格 —— 那正是「詞不變、意思變了」

### ⭐ typecheck 順手抓到一個**既有**缺陷
`authoringRules.ts` 的 `aoe.max-radius` 註解寫「= **最大的**那一級距」，
而它索引 `["超大"]` —— **第 4 格不是第 5 格**。**上限一直是 8 而不是 12。**

### 守衛
`skillTierNames.test.ts`（3 條，突變驗過）· `configForms.ts` 的 AoE 頁從**手打五格**
改成 `SKILL_TIER_NAMES.map`（第零守則⑨）

---

## GH#408 —— 吞噬 `victim: "champion"` → `"any"`

owner：「he can kill enemy below 3% hp left, **including zombies. boss**」
⇒ 這是**回答平衡疑慮的方式**而不是放寬：問題是「rank1 的 3% 處決線幾乎沒用」，
而答案不是調高數字，是**目標池本來就該更大**。一場 60 隻殭屍 + 殭屍王，3% 每回合都會觸發。
⭐ 比調數字好：數字會再被改，目標池是**設計**。卡面同步改成「任何敵方單位（含殭屍與殭屍王）」。

---

## GH#453 —— FATE 圖示風格：三個住處統一 + 產圖戳記含風格

- 風格字串有**三個住處**（config JSON / Zod DEFAULT / `keywords.py` 的 fail-open 退路），
  其中**兩份已經漂移**而**零個守衛** → `iconStylePromptHomes.test.ts`
- 產圖器判斷「這張還新不新」只看 `METHOD_VERSION`（**程式**常數），而風格住在**資料**裡
  ⇒ 改風格一張都不會失效、1010 張全被 skip → 戳記改成 `METHOD_VERSION + 風格 digest`
- 全部 1158 張圖示已備份（`/Users/Takuro/GGD-backups/icons-20260819/`，2168 檔 sha256 驗過）

⚠️ **重畫尚未執行** —— 量到 PASS-2 的文字提示詞不是夠力的槓桿（改完前後幾乎逐像素相同），
真正的槓桿是 **PASS-1 的配色**，而它會連帶改變主體。等 owner 裁決走哪一條。

---

## 第一次量到的兩件事

### 「描述 ↔ JSON 一致」（GH#462）
owner 問「上次不是 100% 通過了嗎」——那個 100% 問的是「**是不是純 JSON**」，
⛔ 不是「說明與 JSON 一致」。後者**從來沒有被量過**。

| | 通過率 |
|---|---:|
| 回魔 | **0%** |
| 最大生命% | 40.0% |
| 持續時間 | 57.7% |
| 標籤 | 63.8% |
| 傷害 | 64.4% |
| 冷卻 | 84.4% |
| 耗魔 | 95.9% |

**161/461 支至少一處不一致，共 202 處。**
⭐ 分佈很清楚：**owner-spec 幾乎全對**（冷卻 67/67、耗魔 47/47），
不一致幾乎全在 **w3x-import** ⇒ 是匯入時文案與數值各走各的。

### 兩支技能其實是空的（GH#451）
- **86-04 打雷絕招**：`effects` 是 **空陣列** ⇒ 按下去什麼都不會發生（冷卻與魔力照扣）
- **65-04 天譴**：只有一個 `dash` ⇒ 沒有傷害、沒有閃電

⭐ 原作設定查到了：**彈跳 16 次**（⛔ 不是通例的 12）。
天譴還有兩件先前不知道的：**250 點法力損失** + **指定目標額外 501~2500 傷害**。

---

## 驗證
`pnpm typecheck` EXIT=0 · `pnpm test` EXIT=0（全 package）·
七支產生器全部重跑並 commit 產物 · 測試/實作比 **0.10×**

## ⚠️ 部署注意
級距名是 **Zod enum 變更** ⇒ 內容與映像必須一起動。
⛔ **不可以 `--content-only` 部署**，必須完整重建映像。

---

## ⭐ GH#451 —— 連鎖閃電：**逐跳有時間差**（owner 2026-08-20 裁決）

owner 逐字：

> 「**DECAY 0.9**，但這個技能的重點在於**隨機選擇單位遞減時間差的閃電特效與傷害**
>  （**每個閃電有極小的時間間隔播放閃電動畫與傷害才到下一個**，剛好可以避免你說的計算上限），
>  **有其特殊性與純範圍直接給傷害區別很大**」

⚠️ 這推翻了同一天稍早做出來的第一版 —— 它把**所有鏈、所有跳在一個 tick 內結算完**。
一格閃完，玩家看到的跟一發 AoE 沒有分別，正是 owner 說「區別很大」的那個錯誤形態。

| 要求 | 落在哪 |
|---|---|
| `decay = 0.9` | 內容側填（`decay` **維持必填無預設**） |
| ⭐ 逐跳極小時間間隔（動畫**與傷害**都跟著走） | 引擎：pending 佇列 + **絕對 tick**，照 `delayed.ts` / `randomArea.ts` 同一個形狀 |
| 隨機選下一跳的單位 | 決定性 rng，Map/Set 迭代先排序 |

⭐ 逐跳間隔**同時是效能設計** —— owner 明說它「剛好可以避免計算上限」。
改前實測：60 隻 / 100 隻身體 = **4.23 / 6.83 ms**（30Hz 一格的 13% / 20%）。

### 層級衝突的處置（第〇·六守則）

| 層 | 說什麼 |
|---:|---|
| **1 · owner 新版說明** | 「周圍敵方單位**逐個傷害遞減**」 |
| 5 · w3x 原始設定 | `A04H` DataC 等級 1–3 **全是 0.0** ＝ 原作**根本沒有遞減** |

⇒ 層 1 贏。被取代的原作行為（decay 1.0，總量 16× 首跳 = 48,000 點）已記進 #451，
⛔ 不會無聲消失。

### ⭐ JASS 逐行讀完，它證實了**卡面**而不是 w3a

`raw/war3map.j:40489` `Trig_PIKACHU_Actions` →
`ForGroupBJ(GetUnitsInRangeOfLocAll(100+200*level, 施法者位置), …)`，
**對圈內每個單位各造一隻 dummy 施放 chainlightning 指向它自己**。
⇒ 半徑 `100+200×lvl` = **300/500/700**，**卡面的 300 是對的**，
w3a 的 `area` 450/650 **沒有被 JASS 讀取**。先前標記的「卡面 vs w3a 打架」就此解掉。

---

## ⭐ 兩條判準換成了兩個閘

這個 repo 已經記錄了**五次**「要記得⋯」失效。這一版把兩條再換掉。

### ① context 快滿之前先存快照（owner 2026-08-20）

> 「每次 **context 要滿的時候**先開一個 **`memory_temp_{timestamp}.md`** 先存進去吧 **避免意外要找回**」
> 「界線在 **95%** 快滿的時候，**最多到 98%**」

```bash
bash scripts/memory-temp.sh          # 寫快照到 docs/_daily/
bash scripts/memory-temp.sh --check  # 只看 transcript 多大、上一份存在哪
```

掉的**不是程式碼**（程式碼在工作樹裡），掉的是**只存在於對話裡的那一半**：
owner 的裁決原話、我撤回過哪個錯誤結論、兩個工作流為什麼不能同時跑、哪個數字沒有來源。
腳本只抓機械那一半，另外留**三節 `（待填）`** 當場補
（⏸ 卡在 owner 的決定 · 🧠 裁決逐字 · ➡️ 下一步含排序理由）。

⚠️ 老實說：**腳本讀不到 context 百分比**，它只印 transcript 的 MB。95/98 這一格的觸發責任在我身上。

守衛：`packages/shared/src/ops/memoryTempScript.test.ts`（真的把腳本跑起來）。

### ② GH#466 —— 技能改動 → 一條指令重生成所有衍生資料

> owner：「⋯球體綁定位置、特效 pitch/scale/color/透明度、特效音效綁定、五級距、
>  說明↔實際實作 JSON⋯都請整理更新到 **JSON** 並讓 **script 動態更新**所有相關文件與
>  codex 編輯器契約文件、後台設定參數與介面⋯**避免資訊不同步造成的錯誤**」

**根因**：`content:build` 只涵蓋三分之一。

| 產生器 | 原本在 `content:build` 裡？ |
|---|---|
| shared content:build · `spec:build` · `overview:build` · `tiers:build` | ✅ |
| `caps:export`（Codex 能力契約） · `refresh_docs.py`（90 支 + 引擎須知 §13.10） | ❌ |
| `contract:numbers` · `atlas` · `treasure:csv` · `docs:readme` · `docs:status` | ❌ |

⇒ 在此之前那是**八個要記得的指令**。現在是一條：

```bash
pnpm skills:sync     # 重生成全部八支
pnpm skills:check    # 全部驗一次（唯讀）
```

⭐ **聚合指令自己也會過期，所以它也有閘**：
`packages/shared/src/ops/skillsSyncCoversGenerators.test.ts` ——
package.json 裡每一支 `*:check` 都必須要嘛在 `skills:check` 裡、
要嘛在豁免表裡帶著一個**能被反駁的**理由。加第 15 支產生器而不做選擇 → 紅。
第二條斷言另外抓「驗得到卻**重生成不了**」—— 閘紅了沒人知道要跑什麼。

⚠️ 寫這條的當下它就抓到我自己的 bug（子字串比對把 `docs:readme` 當成一支 check）。
突變驗過：把 `caps:check` 從聚合裡拿掉 → 紅，訊息指名它。

---

## ⭐ GH#467 —— 四個「單一寫入者」拆掉了（owner：**優先做因為關鍵**）

owner 2026-08-20：檔案拆分策略，**讓大量改技能時的平行化不卡住**。

⚠️ 這一票不是重構潔癖，是**併行度的硬上限**：一個檔同一時間只能有一個 lane 在寫。
上一節那六條工作流之所以要按「檔案領域互斥」切，正是因為有四個檔誰都繞不開。

### 量到的四個（＝為什麼是這四個，⛔ 不是憑感覺挑的）

| 檔 | 原本 | 誰在排隊 | 現在 |
|---|---:|---|---|
| `tools/skill-remake/batch1.py` | **3,354 行**，擁有 20 個 id / 113 份 JSON | 任何改到那 20 位英雄的 lane | 393 行殼 + `common.py` + **`heroes/<英雄 id>.py` × 15** |
| `packages/shared/src/content/schema/effect.ts` | **4,754 行 / 40 個 kind 一個 union** | 任何**新機制** | 299 行門面 + **`schema/effects/<kind>.ts` × 40** |
| `packages/shared/src/sim/effects/effect.ts` | **2,751 行**（同一個 TS union） | 同上 | 430 行 + **`sim/effects/variants/<kind>.ts` × 40** |
| `descriptionClaims.baseline.ts`（181）· `abilityCodeParity.baseline.ts`（342） | 兩條全域棘輪 | **每一條修卡面的 lane** | 兩支只剩 loader（50 / 48 行）+ **60 / 22 份逐英雄 JSON** |
| ✅ `content/abilities/` | 462 檔，一技能一檔 | — | ⭐ 本來就切好了 —— 瓶頸**不在內容** |

**併行度**：三條軸的上限從 **1** 變成 **15 位英雄 / 40 個 kind / 60 份卡面棘輪**。
⛔ 這不代表可以開 60 條 lane（那有別的成本），它代表**再也不用為了一個檔排隊**。

### ⭐ 驗收標準只有一條：**分片是搬家，不是改行為**

安全網 `tools/shard/snapshot_generated.py` 先從**凍結的 commit** 存基準
（⛔ 不是會被別條 lane 邊改邊動的工作樹），改完逐位元組比對：

| 量什麼 | 結果 |
|---|---|
| 產生器產出（20 個 id / 113 份 JSON） | 新漂移 **0**；唯一一筆 `godie-e010.r.json` 是**別條 lane 的內容修正**，與分片無關 |
| 產生器的規格表 `T`（含順序與每列鍵序） | 前後 dump `diff -q` **逐位元組相同**（8,447 行） |
| `batch1.py --check` · `refresh_docs.py --check` · `emit_spec_md.py` → 暫存目錄 | 三者皆 0；90 支的並排文件**逐位元組相同** |
| Zod 對 `content/**/*.json` | 前後都是 **1,721 通過 / 5 拒**，而且 5 筆的 issue **逐字相同** |
| 38 條負向探針（每一條跨欄位規則 + 每一道界 + `.strict()`） | issue 陣列 code/path/message/**順序**逐條相同 |
| 40 個 union 成員的 `Object.keys(shape)` 與 `options` 順序 | 逐格相同 |
| 兩條棘輪的豁免集合 | desc **163 → 163**、parity **326 → 326**，對稱差 **∅**，重複 0 |
| `packages/shared` typecheck（各量兩次，先刪 tsbuildinfo） | 7.87 / 8.04 s → 8.03 / 8.04 s —— 落在雜訊內，**沒有變慢** |

⭐ **安全網當場抓到兩次真的漂移**（⛔ 不是測試抓的，是這張表抓的）：
① refiner 派發表第一版只搬了 10 個 kind，漏掉 `refineDispelShape` 的 10 個消費端與
`shield` / `grantAttribute` 兩段 inline 規則 → **7 條負向探針從「拒絕」變成「通過」**
（＝一批壞內容會靜默載進去）；
② 對外的 `zEffectDef` 一度從 `ZodLazy` 變成 `ZodEffects` —— 解析結果一樣，但
`walkZod` / `fieldAdoption` / `editorCapabilities` 三個 walker 在拆包裝的型別上分岔，
**而它們都不會報錯**（失敗形態②）。兩處都已還原。

### 三條會紅的閘（⛔ 不是三條要記得的判準）

| 閘 | 關掉哪一種靜默腐爛 | 突變 |
|---|---|---|
| `batch1.py::load_heroes()` | `heroes/` 多一個檔沒登記 / `HERO` 有一列沒有檔 / 一個檔放了別人的技能 | 兩個方向各埋一次 → 都 EXIT=1 並指名 |
| `schema/effects/effectShardWiring.test.ts` | 兩個目錄 × `zEffectDefUnion.options` × `EFFECT_HANDLERS` **四個方向**任一邊分家；有 `refine` 卻沒接進派發表 | 拿掉 `shieldBreak` 的 refiner → 紅 |
| `baselineShards.ts` 的 loader | 檔名↔內容錯位 · `[]` 空殼 · 同一鍵出現在兩個檔 · 孤兒檔 | 孤兒檔 + 空殼各埋一次 → 都紅，訊息**指名要刪哪個檔** |

### 對外落點（owner：「優化機制與結果要寫到⋯」）

| 落點 | 做了什麼 |
|---|---|
| README §9 | 新增「大量改技能時的檔案分片」一節：新目錄結構 + **三條規則** + `tools/` 表補上 `skill-remake` / `shard` |
| Codex 契約 `ggd-runtime-capabilities.md` | §3 多一張**每次匯出時數出來的**覆蓋表（40 / 40 個 kind 兩個目錄都有檔）；5 處指向舊 `schema/effect.ts` 分支的佐證改成分片後的檔 |
| `docs/技能標記機制與效果規則.md` | §5 每一個 kind 多一行 **定義檔**（去磁碟上找，找不到就不印） |
| 兩者都是**改產生器**（`tools/capability-export/export.ts` · `tools/skill-spec/gen_spec.ts`） | ⛔ 沒有手改任何一份產生的文件 |
| `package.json` | ⛔ **不需要改**（理由見下） |
| JSON / 後台設定 | ⛔ **不需要改** —— 分片沒有新增任何開關，出貨數值一個都沒動 |

⭐ **為什麼 `package.json` 不加一支 `shard:check`**：`heroes/` 的雙向閘寫在
`load_heroes()`，而它在 **import `batch1.py` 的當下**就跑 —— `refresh_docs.py` 是
`import` 它的，所以 `pnpm skillremake:docs:check`（已經在 `skills:check` 裡）
就會執行那道閘；`skillRemakeJsonFresh.test.ts` 另外再跑一次 `batch1.py --check`。
再加一支 script 只是同一件事跑第三遍（第零守則）。
`snapshot_generated.py` 是**遷移期**的安全網（基準綁一個凍結的 commit），
⛔ 不是常設的新鮮度閘，所以它刻意不進 `skills:check`。

---

## 🔀 這一版的工作方式：**六條工作流併行**

owner：「要快點打開更多同步不衝突工作流來完成，不然趕不上七夕情人節比賽進度了」

切分依據是**檔案領域互斥**，⛔ 不是主題相近 —— 兩條 lane 只要會編到同一個檔就不能併行。

| run | 領域 | 票 | 持有的鎖 |
|---|---|---|---|
| `wf_0e721af0` | **#451 連鎖閃電逐跳** | 451 | ⭐ `content/` · `schema/effect.ts` · client `render/vfx/` |
| `wf_3439ab5b` | W1 平台與後台旋鈕 | 349 352 350 355 360 376 410 | Go · admin · `schema/config.ts` |
| `wf_c93877d0` | W2 場地幾何與出生點 | 364 422 421 363 345 409 398 | arena 幾何 · match 擺位 |
| `wf_3c43693e` | W3 音效與語音接線 | 402 436 403 440 441 | 音訊層 · 空間音場政策表 |
| `wf_9ab443dc` | W4 測試基建與閘 | 428 359 334 351 353 374 407 412 | `vitest.config.ts` · replay digest · castability 普查 |
| `wf_1407b978` | W5 文件與產生器一致性 | 449 389 426 381 417 335 | `docs/` · `tools/` |

每條的內部形狀都是**實作 lane 併行 → 一個對抗複驗 + 一個全域檢查**。
⭐ 全域檢查（typecheck / 新鮮度閘 / 卡面說謊閘）**收成一個 agent 跨全部 lane 做一次** ——
六條各做一遍就是同一件事做六遍。

### ⚠️ 併行時會咬人的四件事

1. **假紅** —— GH#359：`gameAppFacingWiring` 在多 package 併行時撞 5000ms 逾時（單獨跑 3.3 秒過）。
   六條併行時它**每分鐘都在製造假紅**。⇒ W4 被指定**最優先修它**；在那之前任何紅燈
   都要**先單獨重跑那一支**再下結論。
2. **工作樹是唯一副本** —— 78 個已追蹤改動 + 27 項未追蹤，全部來自這個 session，**沒有備份**。
   所有 lane 明令禁止 `git checkout <檔>` / `restore` / `stash` / `add -A`
   （2026-08-18 一次 checkout 洗掉三條 lane）。
3. **`content/` 是全域鎖** —— `skills:sync` 會寫 `bundle.json`，同時跑會產出壞掉的 bundle
   （2026-08-01 讓選人畫面整個空掉的形態）。⇒ 只有 #451 那條能跑，其餘全部禁止。
4. **subagent 紅線** —— 每條 lane 都明令：⛔ `gh issue create`、⛔ ssh/curl 正式站、⛔ `content:build`。

### ⛔ 因此這一輪開不了的 31 張

技能內容 24 張（#333 #356 #358 #369 #373 #375 #400 #401 #404 #414 #419 #420 #424 #438
#442 #444 #445 #446 #447 #448 #459 #460 #461 #465）
＋ 特效家族 7 張（#378 #384 #392 #425 #427 #431 #439）—— 全部等 `content/` 鎖釋放。

---

## 🧾 逐句對票（從 session transcript 撈，⛔ 不是憑印象）

owner 2026-08-19：「這是很好的開發守則，避免遺漏又可以揭露足夠版本更新資訊」

| owner 說的（節錄） | 票 | 狀態 |
|---|---|---|
| 「明明就是 極小 小 中 大 極大 五級距⋯**沒有超大這種東西**」 | #463 | ✅ 505 筆同批重寫 |
| 「he can kill enemy **below 3% hp**⋯including zombies. boss」 | #408 | ✅ `victim:"any"` |
| 「**極大跟極小都是卡上下限的例外**而非線性規則」 | #445 | ✅ 我的建議是錯的，已撤回；線性段 3.0/2.0/2.0，表不用改 |
| 「同一個技能 **升級冷卻通常不會變化** 有變化就是特例 一樣要給我審查列管」 | #464 | ⏳ W 外，24 支列管中 |
| 「連鎖閃電⋯**預設是 12**⋯但打雷跟天譴範圍內的確**沒有**⋯上限」 | #451 | ✅ 查到原作是 **16**；鏈數無上限 |
| 「**DECAY 0.9**⋯重點在於**隨機選擇單位遞減時間差**的閃電特效與傷害」 | #451 | ⏳ `wf_0e721af0` 實作中 |
| 「**通道多好 因為跑才是重點**」 | #409 | ⏳ 已進票，W2 執行中 |
| 「please **fix all** even after that」 | #374 | ⏳ 已進票；⚠️ 推翻第零守則⑧在這批的適用 |
| 「只要做**有開放的角色技能及隨機三選一**就好，沒開放的別浪費 token」 | #462 | ✅ 範圍限縮成 70 英雄 / 414 技能 / 91 增益 |
| 「其他票盡量開**不衝突的獨立工作流**⋯測試不要重複做」 | — | ✅ 六條併行，全域檢查收成一個 |
| 「**逐句對票**⋯要持續更新阿，並且寫到 NOTE」 | — | ✅ 已寫進 CLAUDE.md 部署協定第 1 步 + 本節 |
| 「每次 **context 要滿**⋯先開一個 `memory_temp_{timestamp}.md`」 | — | ✅ `scripts/memory-temp.sh` + 守衛 |
| 「界線在 **95%**⋯最多到 **98%**」 | — | ✅ 寫進 CLAUDE.md，數字不是判準 |
| 「每一次更動技能相關機制或內容⋯讓 **script 動態更新**所有相關文件」 | #466 | ✅ `pnpm skills:sync` + 聚合自己的閘 |
| 檔案拆分策略（讓大量改技能時的平行化不卡住）—— 「**優先做因為關鍵**」 | #467 | ✅ 四個單一寫入者全拆；產出逐位元組相同，新漂移 0 |
| 「優化機制與結果要寫到 **github readme, release note, codex編輯器契約與相關說明文件、相關 script、JSON 及後台設定機制**」 | #467 | ✅ 六個落點逐一處理（兩個「不需要改」有寫理由，⛔ 不是漏掉） |
| 「你是否能**盤點現在 0.21.4 107 所有票**盡量獨立不同工作流加速」 | — | ✅ 107 張逐張讀完，切成 5 條新工作流 |
| 「更新 **GGD作戰版**跟 release note，包括你工作流分工的方式跟群組現況、注意事項」 | — | ✅ 作戰板原地更新 + 本節 |


## 🧾 逐句對票 · 2026-08-20（⛔ 從 session transcript 撈，不憑印象）

| 時間 | owner 說的 | 票 | 狀態 |
|---|---|---|---|
| 16:03 | 「每次 context 要滿的時候先開一個 `memory_temp_{timestamp}.md`」 | CLAUDE.md 第零守則 | ✅ `scripts/memory-temp.sh` + 95%/98% 界線 |
| 16:31 | 連鎖閃電層級衝突 ⇒「**DECAY 0.9**…重點在於**隨機選擇單位遞減時間差的閃電特效與傷害**…**有其特殊性與純範圍直接給傷害區別很大**」 | #451 | ✅ 獨立 `chainLightning` kind（`jumpIntervalSec` 讓整條鏈不在同一 tick 結算） |
| 16:58 | `RANKED_MIN_APEX_LADDER`「**看不懂，請你說明的更簡單易懂**」 | #4xx | ✅ 已重寫說明 |
| 17:05 | 「**先留 0**。理由是這是家人小圈子」 | 同上 | ✅ 出貨值 0 |
| 17:32 | 「備份規則都是 **`{用途}_temp_{timestamp}.md`**…方便被認出是否已經過時要放到 legacy」 | CLAUDE.md | ✅ `scripts/temp-sweep.sh`（⛔ 只搬不刪） |
| 17:40 | 「我說過你要做**取代**這種事情以前都要**備份**…**這一定要放到開發守則嚴守**」 | CLAUDE.md | ✅ **PreToolUse hook** `preserve-before-overwrite.py`（⛔ 永不擋工具） |
| 17:43 | 「**如何拆檔案** 讓獨立平行化不會那麼容易卡住…**建議你優先做因為關鍵**」 | **#467** | ✅ 四個瓶頸拆掉（4,754→299 · 2,751→430 · 3,354→393 · 兩支 baseline→79 份 JSON） |
| 17:53 | 「回溯到 v0.18.1…**訊息開票對應，沒開票要補開票，票已完成要去註記現況後關閉**」 | reconcile lane | ✅ 關 29 張 · 補開 9 張（#469–#477） |
| 20:13 | 「**fix all, fix gen_board.py**」 | — | ✅ 拿掉三格時鐘欄位 + `board:check` 進聚合閘 |
| 20:13 | 「**創建新英雄** 十一種屬性、技能說明、施展距離、範圍、傷害、冷卻、耗魔的**生成代入與檢查跳警示**…特別是 **script 程式自動化跟警示**」 | **#480** | ✅ 六欄從 420 支語料取中位數 + 六條警示 + `newhero:check` |
| 20:13 | 「優化機制與結果要寫到 **github readme, release note, codex編輯器契約與相關說明文件、相關 script、JSON 及後台設定機制**」 | #467 擴散 | ✅ README §9 · 契約第 15 章 + 第 10 節 · 8 支產生器補 npm script |
| （多則） | 「注意有時候你會**改錯有變身檔的英雄技能，只改了其中一個**…**godie-e00u Sakuya…要移到 legacy 不要再被掃到**…**請你想個一勞永逸的方法**」 | **#479** | ✅ 26 組對子雙向對帳 + 7 位英雄進 `_legacy` ⇒ 編號碰撞 2→0 |
| （多則） | 「**每次我回答你以後，就馬上去更新 github issue**…避免我要重複講」 | CLAUDE.md | ✅ `scripts/ruling.sh` + `scripts/asked-before.sh` |
| 15:37 | 「**只要做有開放的角色技能及隨機三選一就好，沒開放的別浪費 token**」 | #472 | ✅ 範圍收斂；退場物進 `_legacy` 後自動不被掃到 |
