# GGD 執行批次計畫（Execution Batches）

> **⚠️ 2026-07-30 全面重寫。** 上一版寫著「main = `a6b1609d`，線上 **v0.7.0**」——
> 那是**八個版本前**的狀態。這份檔案自己就是「註解會說謊」的案例：
> 它被當成計畫來讀，但沒有人在每次 deploy 後更新它。
> **新規則：每一次 push 都要更新這一份的「現況」段，否則下一個人會照過期的東西做。**

---

## 📌 現況（2026-07-31 清晨，v0.9.16 出貨前）

| | |
|---|---|
| 線上 | `cba64c28` = **v0.9.15** —— ⛔ **v0.9.16 沒能部署**：主機無私鑰、本機 agent 無 identity，`git pull` 認證不了。詳見 `_session-handover-0731.md` §1b |
| main | **v0.9.16 已合併** —— `pnpm typecheck` EXIT=0；`pnpm test` 有 **3 條已知紅**（下表逐條交代） |

### 🎮 v0.9.16 localhost 實打（2026-07-31，協定第 3 步）

在 `client-playtest`(:5205) + `game-server-mobile`(:2599) 上真的打了一場：

| 檢查 | 結果 |
|---|---|
| 登入場景 | ✅ 兩條龍、競技場島、英雄輪播、版本徽章 `3e9634e8` 正確 |
| 一鍵開打 → 選角 | ✅ 93/93 英雄、12 個席位、倒數 10 秒、逾時自動選角有作用 |
| 中場商店 | ✅ 商人 3D、三選一（SILVER AUGMENT）、屬性面板、裝備欄 0/6 |
| 屬性數值 | ✅ 生命 **8009**（maxHealth 倍率 9 生效）、魔力 511、護甲 6.2 |
| 戰鬥 | ✅ Round 1 跑起來、**184 fps**、小地圖、敵方三人 HP/MP 面板、技能列六格、首回合操作說明 |
| Console | ✅ 零錯誤 |

⚠️ 沒測到的：揍敵客的新 EX（選角逾時被自動選成魯夫）。存款加成的行為由
`efurKit.test.ts` 的兩條守衛 + 三個突變驗證涵蓋，但**沒有人在畫面上看過那個數字**。
下一次實打請手動選揍敵客按 F。

---

### ⚠️ 出貨時的 3 條紅燈 —— 逐條交代，沒有一條是「不知道為什麼」

| 測試 | 判定 | 為什麼還是出貨 |
|---|---|---|
| `settlement.test.ts` 贏家收到淘汰廣播 | **HEAD 就有** —— 在 `1dddf5c9` 的乾淨 worktree 上跑出**一模一樣**的 `expected [1, +0] to not include +0` | 線上 v0.9.15 已經帶著它。**它是真缺陷**（贏的隊伍可能看到淘汰畫面），但不是這一批造成的，修它要動 #193 的名次判定，屬於獨立一票 |
| `autoAcquireWhileMoving` IDLE hits=0 | **前一條 lane 已診斷並刻意留紅** —— 檔頭寫著「整場 2,410 tick 裡最近的敵方英雄從來沒有靠近到 14.95 單位以內，而 Saber 的索敵半徑是 `MELEE_ACQUIRE_FLOOR = 6`……**留紅在這裡，不要用放寬期望的方式蓋掉（e34339b7 就是那樣被 revert 的）**」 | **玩家的索敵是對的，是沒有東西可以打**。缺口是「**bot 會不會主動去打站著不動的玩家**」，那是 bot AI 的題目 |
| 同檔 `frozenTicks=0` | 同一個父因 —— stick 情境同樣 0 命中，沒有命中就沒有 hitstop 可以凍結 | 同上 |

⚠️ **這一輪順手發現的不對稱，值得 owner 看一眼**：`OrderSystem.ts` 的索敵半徑是條件式的 ——
**走位卡住**的玩家拿 `ae.seekRadius`（48，掃全場），**站著不動**的玩家只拿近戰地板 **6**。
也就是說「卡住」比「站著」更容易索到敵。這是 GH#216 為了修卡死而加的，
但它讓 idle 變成全場索敵半徑最小的狀態。**要不要讓 idle 也吃 seekRadius，是一個決策點 → 應該做成欄位。**


| 六步協定 | 第 1–2 步完成；第 3 步（localhost 實打）在 v0.9.15 做過並記在下方；**第 5–6 步就是這次** |
| owner 授權 | 2026-07-31 醒來後明說「準備 git push + note + deploy」→ **這一版要上線** |

### v0.9.16 這一版真正做完的事

| 主題 | 內容 | 守衛 |
|---|---|---|
| **owner 七條修正全部落地** | 見 `docs/_owner-corrections-0731.md` 的逐條勾稽 | 每一條都有突變驗證 |
| **13-002 揍敵客 EX「燒越多打越痛」** | 新機制:`spendMana.bankAs` 存款 → `damage.bankedBonus` 提款。三個數字全是**有上下界的欄位**(係數 / 上限 / 存款壽命) | `efurKit.test.ts` 兩條;三個突變(拿掉 bankAs / 拿掉 bankedBonus / 拿掉 `Math.min` 上界)全部紅 |
| **勇者小呆 08-002 防禦 ×2 改成真的 ×2** | 從 `pctAdd` 改成 **`pctMult`**。`statPipeline` 是 `(base+flat)×(1+ΣpctAdd)×Π(1+pctMult)`,只有 `pctMult` 會**跟著裝備一起乘** —— 這是 owner 2026-07-31「裝備或升級提升了也會跟著 ×2」的唯一正確形狀。基礎防禦 0 → 7(本體與變身態都是) | 索隆 11-002 的 AD ×1.5 同樣改成 `pctMult` |
| **59-00 初號機暴走 吸血 10% → 50%** | 而且**明確不是無敵**:`berserk` 只拿走方向盤(`sim/berserk.ts`),不碰生命 | `laneA.innates.test.ts` 斷言寫死 0.50,文件改回 0.10 就紅 |
| **變身持續時間終於有守衛** | 20 秒 / 15 秒過去**只在到期後才被檢查**,把 `durationSec` 改成 8 / 5 仍然 7/7 全綠 | 實測前搖 11 tick,常數化為 `EX_FORM_WINDUP_TICKS`;改短就紅(已驗) |
| **四條假守衛/假註解修掉** | `abilityScaling` 的 fx-16 只認 `kind==="damage"`,把 `damageLine` 的物理傷害誤判成違規;`fieldAdoption` 的 GUARD-THE-GUARD 因為 `zStatModifier` 長出 `from` 而**整條變成空集合**(自己的失敗形態被自己抓到) | 兩條都改成「錨在真正的 zod 站點 + 成員資格」 |
| **27 個零採用欄位逐條交代** | 條件系統的比較運算子與屬性軸、擊退的六個子欄位、`whileForm`、`spendMana.applyTo=target` … 每一條都寫了 status 與**為什麼**,沒有一條是為了消紅燈 | `fieldAdoption.test.ts` 9/9 |

⚠️ **這一版沒有做的**（誠實記錄，不要當成做完了）：
- **模板複數套用**（owner 2026-07-31 全域要求）——「一支技能同時套多張模板卡」還沒開工，見下方第一批
- **icons-pixel/ 142 個零引用檔**沒有刪 —— 它不進 bundle，刪它是淨整理但需要 owner 一句話
- **`godie-h01o.passive`** 仍然是孤兒(變身不會換 `passiveSlot`),那是**功能缺口**不是垃圾檔,不該用刪除來「修」

---

## 🔍 這一輪五條工作流的收成（2026-07-30）—— 三條被自己的駁斥者推翻

**六個 lane 裡有三個宣稱「已落地」但被複驗推翻。** 這個比例跟上一輪一樣
（八條裡六條的「已量測」被駁）。**不要把 lane 的自評當結果讀。**

| lane | 判定 | 真正的狀況 |
|---|---|---|
| 遙測出貨守衛 #207 | ✅ CONFIRMED | 把 `MatchRoom` 那行換成 `= null`（線上等於完全不記錄）→ 新守衛 3/3 紅，而既有 `analytics.test.ts` 10/10 全綠。**這就是它存在的理由。** |
| #223 拳四郎骨架 | ✅ CONFIRMED（但只修一半） | 骨架洩漏是真的（往返 7 次累積 7 具，修後恆為 1）。**但孤兒骨架不渲染任何東西** —— owner 看到的「兩個模型骨頭一起動」不是它。 |
| #221 智慧→魔抗 | 🔴 REFUTED | 見下方「第一守則的失敗」 |
| #262 特效洩漏 | 🔴 REFUTED | 見下方「修洩漏反而製造第①種故障」 |
| UI 三處遮擋 #219 | 🔴 REFUTED | 幾何是對的，**守衛是假的**：它掃原始碼字串（第⑥種），保留 `hudSurfaceStyle(...)` 再補一行 `top:` 就能把橫幅釘回原位而 34/34 全綠。另外 COMPACT 分級整個刪掉，1894 條 ui 測試照樣全綠（第③種）。 |
| 名單 51→53 | 🔴 REFUTED | 工程是對的，但改動連帶弄紅兩個**硬寫 51** 的釘子（`castabilitySweep.test.ts` / `combatVoiceCoverage.test.ts`），而它宣告「這是唯一由我造成的紅」。 |

### 🔴 #221：第一守則的失敗，而且被一句假註解蓋住

`intToMagicResist` 進了 `content/config`、Zod schema 與 sim，**但沒進
`apps/platform/internal/combatenv` 的 Go 鏡像**。那不是「少一個欄位」——
`sanitizeFrom` / `sanitize` / `baseDoc` / `DefaultDoc` 全部是
`for _, k := range Keys` **重建整張 map**，所以不在 `Keys` 裡的 key 會被平台
從它服務的每一張表**丟掉**。結果：**後台看不到、改不了，對操作者等同寫死。**

更糟的是它在測試裡留了一句解釋：「APPENDED, not inserted —— apps/platform
compares its mirror positionally」。**那句話是假的**（`keysync_test.go:52` 用的是
`assert.ElementsMatch`，順序無關），而它讓人以為平台鏡像已經同步。
第三守則的教科書案例：一句假註解 + 一條建立在它上面的斷言。

同一個檔還把 `maxHealth === 6.0` 釘進「智慧→魔抗」的守衛，理由是「同一批決定」——
於是隔壁 lane 把它調成 9 的當下，這個檔就以 `expected 9 to be 6` 紅了，
而錯誤訊息完全沒提平衡。**已修：Go 鏡像補上、假註解撤回、無關的釘子拿掉。**

### 🔴 #262：修洩漏反而製造第①種故障（算出來但畫不出來）

回合邊界清池用了 `m.dispose(false, true)`。Babylon 的**第二個參數**會一路傳到
`material.dispose(false, true)`，把材質引用的每一張貼圖也 dispose —— 而 fill 的
emissive/opacity 兩個槽指的都是 `sharedFor()` **每個 scene 只建一次的共用貼圖**。

於是第一個回合邊界就殺掉它，而 `trimTelegraphPools` 不刪 `sharedByScene` 的條目，
第二回合的新材質又指回那張死的 → `isReadyOrNotBlocking()` 恆 false →
`isReadyForSubMesh` 直接 return false → **那個 submesh 一張 frame 都不會被畫**。
症狀是 #228 的核心元件（隨吟唱填滿的魔法陣圓盤）在**出貨預設**下從第 2 回合起消失。

**而它新增的 5 個 suite、33 條測試全綠** —— 因為那些斷言量的是
`scene.meshes.length`（**屬性**：網格在不在場景圖上），缺陷卻是「材質永遠 ready
不了」（**行為**：畫不畫得出來）。一個 ready 不了的 mesh 確實還在 `scene.meshes` 裡。

**已修**：`disposeButKeepSharedTextures`（網格 `dispose(false,false)` + 材質
`dispose(true,false)`），加一條**只讀 `isReadyOrNotBlocking()`** 的守衛，兩個突變都紅。

### 🟡 選角重名：我把問題答錯了兩次

owner 說「角色可選名單重複太多」。我第一次掃 `modelKey` 得出「26% 模型共用」——
走 voxelSkin 真實路徑後是 **119/119 零重複**，那是第⑦種故障，而我已經把它發成
issue 和 hosted 頁。owner 澄清是**名字**之後才問對問題：

```
全部 champion doc 119 份 · 重名 19 組（38 份）· 19 組全部是變身對
出貨 starterChampions 53 位 · 重名 0 組 · 變身態外洩 0 個
```

本體與變身態在 w3x 的 `unam` 上一字不差（只有匯入器不讀的 `unsf` 才分得出來）。
**兩次都是同一個錯：先假設問題是什麼，再去找支持它的數字。**

修法依 owner 2026-07-30「不要出現讓人解鎖變身後的英雄吧」與 2026-07-26
「換成本體」：閘移到白名單**之外**（`enforced === false` 分支原封不動回傳全部，
離線/dev/平台連不上都走那條，所以任何寫在白名單裡的濾除結構上是 no-op），
而且是 **alternate → base 代換**不是丟棄 —— 線上 operator 手動勾了十個變身態，
單純 filter 會讓那十位英雄靜悄悄消失（#55 黑化Saber 的形狀）。

⚠️ 順帶推翻一句寫在 `valhalla.ts` 註解裡的理由：「濾掉會刪掉十位英雄」——
實測 26 對變身的**本體 doc 一份都不缺**，那十個的本體也全在出貨名單裡。

---

## 🎮 v0.9.15 本機實打（2026-07-30，headless TTK sweep，119 位英雄 × 40 場/值）

**這是 owner 睡覺期間我能做的實打** —— 線上 offline-vs-bots 是關的，瀏覽器對戰需要 owner。
headless sweep 跑的是**真的 sim**（`tools/ttk-sim`，跑 `SimWorld.step()`），不是估算。

### ✅ 你擔心的「生命倍率 ×6 會讓回合太長」—— 沒有發生

出貨值 `maxHealth = 6`：

| | 自然淘汰 | 出貨路徑（含火圈） |
|---|---|---|
| 最短 | 50 s | 50 s |
| p10 | 83 s | — |
| **中位** | **132 s** | **160 s（2 分 40 秒）** |
| 平均 | 161 s | 156 s |

#153 的目標是「最短 ≥2 分、平均 ~3 分」。**中位 160 秒落在目標區間內**，
而且火圈把長尾收得很好（自然淘汰平均 161s，加上火圈之後 156s）。

### 🔴 第二輪實打（goldCurve，6 seed）—— 抓到一個死內容

```
seed 1000: 6 rounds   1007: 5   1014: 5   1021: 5   1028: 5   1035: 6
```

**4/6 場在第 5 回合就結束。** 而 `CAPSTONE_ROUND_GATE = 6`（`statPath.ts:62`）——
**#82 的 7,500 金頂點路線「傳說·萬象強化」在多數場次裡閘門從來沒開過。**

⚠️ 它不是壞掉，是**兩個各自合理的數字乘起來變成不可能**：
#104 把閘門移到 `round >= 6` 保護 owner 說的「大約是第五場之後」，
但**沒有人量過一場實際有幾回合**。

⚠️ 而且 `CAPSTONE_ROUND_GATE` 與 `STAT_TICK_TARGET`（20）**都是程式常數，後台改不了** ——
這是「寫死才需要理由」的反例，而且沒有理由。

→ 已開單。要先量 50+ seed 的分佈再決定改閘門 / 改比賽長度 / 改層數門檻，三者連動。

⚠️ 順帶：`goldCurve.ts` 叫 goldCurve 但**沒有輸出任何 gold curve**，只印回合數。

### ⚠️ 但量到三件該記下來的

**① `stall = 50%` —— 一半的對決不會自然分出勝負，靠火圈收尾。**
這不是 bug，但它意味著**火圈是主要的結束機制，不是保險**。
所以 #218「殭屍王在場時延後火圈」的風險比我原本估的高：延後火圈 = 延後**一半回合**的唯一結束方式。

**② 模擬器建議 `maxHealth ≈ 9.0`，而我們出貨 6。**
換句話說：以「平均 3 分鐘」為目標，**現在的回合是偏短的，不是偏長**。
owner 的體感（「玩家太容易死」）與這個數字一致。⚠️ 但 6 → 9 會讓 stall 更高（x8 已經 53%，x20 是 68%），
**先看 owner 線上實打的體感再決定**，不要憑模擬器直接調。

**③ 模擬器與出貨設定漂移：`combat-env.json cooldown = 0.2`，但 harness base 用 0.25。**
模擬器自己印出這行警告。**所有以它為基準算出來的 TTK 都偏保守**（冷卻越長，TTK 越長）。
→ 開一張單：把 harness 的 base 對回出貨值，重跑 sweep。

---

## 🌙 2026-07-30 深夜 · owner 睡前裁決 + 我的 localhost 實打

> owner：「我要去睡了，請你好好合併之後再 push + note … **直到我醒來再決定要不要 deploy**」
> → **push + note 是授權的；deploy 不是。** 這一輪不碰 ggd.adms.ai。

### owner 這一輪的裁決（已全部套用或已排進工作流）

| 裁決 | 狀態 |
|---|---|
| **僵屍王 `n002` 不移植不上架** —— 它是 w3x 的任務角色 | ✅ 工作流已停，不做 |
| `godie-e00u` 十六夜Sakuya **下架，連帶技能也不處理** | ⏳ 尚未執行（見下方試玩發現） |
| `godie-u01f` 萬夫莫敵 **從英雄名單下架就好** | ⏳ 尚未執行 |
| **天堂之劍改 6% × 10 倍暴擊**（原本 3% × 50 倍太誇張） | ✅ 已改：`critChance 0.06` / `critDamage 8.25`（基礎 1.75 + 8.25 = 10.0），**描述同步改** |
| **on-attack 條件系統一定要實作，編輯器也要配合** | 🔄 工作流進行中（最高優先） |
| **傳說武器擴充也要優先實作完** | 🔄 工作流進行中（最高優先） |
| 殭屍上限**後台欄位上界設 500**（不是寫死 500） | ⏳ 待做 |
| 尖峰重量模擬**停止**，資料已夠 | ✅ 已停 |
| **天生技是原生的，不必偵測有沒有作用；JASS 有時直接偵測角色** | ✅ 這句話直接推翻了一條 lane 的結論 —— 見下 |

### 🔴 owner 那句話救了三支技能

天生技 lane 寫：「war3map.j 對這 24 支**技能 rawcode 零引用**（英雄 rawcode 的命中是
選角/計數管線），**所以沒有一支是 A 類**」—— 它把 owner 指名的那條路當雜訊排除了。
駁斥者去讀，結果 **`godie-hpb1` 07-00 獸化心靈是完整實作的**：
`j:14163` 判 `killUnit%8==0 && GetHeroStatBJ(AGI)<120 && GetUnitTypeId(GetKillingUnitBJ())=='Hpb1'`
→ `j:14226` `ModifyHeroStat(AGI, killer, ADD, 1)`。就是「每殺 8 個 +1 敏」逐字，
**外加一個 120 敏的隱藏上限，tooltip 沒寫、報告也沒寫**。
同樣被埋掉的還有 `godie-e00t` 66-00 恐懼（j:48838）與 `godie-u00k` 71-00 暗夜契約（j:48004）。
**那條 lane 判 REFUTED，要重做。**

### 🆕 今天抓到一個 `_false-completions.md` 沒有的失敗形態

> **測試是從內容掃描生成的，所以刪掉內容等於刪掉測試。**

`invulnerableBinding.test.ts` 用 `it.each(從磁碟掃出來的 13 支)` 生成逐支斷言，
唯一的下限是 `toBeGreaterThan(0)`。實測把 13 支刪到剩 1 支 →
**檔案從 29 條測試縮成 5 條，EXIT=0 全綠**。內容消失時測試不是失敗，是**不存在**，
而 vitest 不會抱怨測試變少。已改成明文名單 + 集合相等（少一支多一支都紅）。
**這一種要加進 `_false-completions.md`。**

### 🎮 我的 localhost 實打（協定第 3 步，v0.9.16 候選）

環境：`game-server-mobile` :2599 + `client-playtest` :5205，離線對 bot。

**正常的**
- 進得去、選角→戰鬥→商店全程沒卡住；**206–221 fps**
- `ws compression: ON permessage-deflate`（今天的改動是活的）
- 內容載入 119 英雄 / 219 道具 / 31 augment / 3 loot table
- **HP 9014/9014** —— 生命倍率 9 確實生效
- 選角 18 秒逾時**自動隨機**（#207）正常；第一回合操作說明浮層（#187）正常
- 敵方三人 HP/MP 面板、小地圖、裝備 0/6、金幣 600g、Lv3 全部正常

**⚠️ 發現**
1. **`完全而瀟灑的女僕 - 十六夜Sakuya` 還在選角畫面裡** ——
   owner 已裁定下架，但工作流被停掉所以還沒執行。**下一輪要做。**
2. **選角卡片不在無障礙樹裡** —— `read_page` 只看得到搜尋框，93 張英雄卡一張都沒有。
   跟 #252（三選一卡片沒有無障礙名稱）是同一個病，範圍比記錄的大。
3. 登入頁英雄跑馬燈**左側數格會先空白再填入** —— 是載入順序不是缺 icon，觀感問題。

**❌ 我差點誤報的（記下來當教訓）**
我一度以為「角色沒有 3D 模型」，因為畫面上只有選取圈和血條。查下去發現
`canvas` 的**背景緩衝是 300×150** 被拉伸到 1440×900 ——
那是 headless 環境開場 `innerWidth=0` 造成的，**不是遊戲缺陷**。
⚠️ **教訓：在 headless 裡看到「東西沒畫出來」，先查 canvas 尺寸再下結論。**

### 天堂之劍：改完數值之後仍然缺它的招牌機制

原作 `I01N` base = **`azhr` 復活聖典**，`class: Charged`，三支能力：
`AIrc`（**死亡時原地復活**）· `AIlz`（生命 −500）· `AIcs`（暴擊）。
**「魂藏」就是這張地圖給復活取的名字**，「起死回生 3 次」是三次充能。
現況 = **代價在（−500 生命），回報不在（沒有復活）**，而且 `craftRole=quest`，
**玩家在三選一真的抽得到一件純負面道具**。
GGD 現有的復活是 #84 的「隊友到復活圈引導」，跟「自己死亡原地復活×N」不是同一個原語。
→ 已排進傳說武器收尾 lane：**先做成 `draftEligible` 欄位排出 quest 池**（可一鍵放回），
不硬造半吊子的復活。同型的還有 `godie-i01s 仙后座`（tier1，抽得到但完全沒東西）。

---

## 🎯 接下來的批次（2026-07-31 重排 · 相依性優先，其次檔案領域）

> **分批依據有兩層**：
> ① **相依性** —— 同一批的任務彼此有前後關係，或必須一起改才不會互相打架；
> ② **檔案領域互斥** —— 不同批之間不共用檔案，所以**批與批之間可以同時開跑**。
>
> ⚠️ **`content/` 一次只給一條 lane**（`content:build` 讀整棵樹，另一條的未進版檔會被烘進 bundle）。
> ⚠️ **共用型別會穿透目錄** —— 改 `packages/shared/src/content/schema/**` 就會波及 admin / editor / game-server 三處鏡像。

---

### 🥇 第一批 · 模板複數套用（owner 2026-07-31 全域要求，**擋住其他技能工作**）

> owner 原話：「我們討論的技能記得都要能用**編輯器編輯模板**跟**複數選取**」
> 呼應 2026-07-30：「模板**複數可被套用**於技能中」

**為什麼整批綁在一起**：這四件事改的是**同一條資料路徑**（schema → expand → 編輯器 →
寫回），拆開做會出現「schema 支援多張卡但 expand 只吃第一張」這種半成品。

| 序 | 任務 | 檔案領域 | 相依 |
|---|---|---|---|
| 1-A | `ability@1` 的 `template` 從**單一 ref** 變成**有序陣列**（`template: {ref,params}[]`），並定義**衝突規則**（後蓋前？還是同欄位重複即拒？——這是決策點，做成欄位） | `packages/shared/src/content/schema/template.ts` · `ability.ts` | 無 |
| 1-B | `expand()` 依序套用多張卡，產生**可診斷的展開紀錄**（哪一格來自哪張卡） | `packages/shared/src/content/template/` | 1-A |
| 1-C | 鑄技工坊 UI：多選卡片、排序、每張卡自己的參數面板、即時看到疊起來的結果 | `apps/editor/src/forge` | 1-B |
| 1-D | 把這一輪做的 **12 支天生技 + 3 支變身/法球 + 揍敵客六技**改寫成「模板 + 參數」，證明路徑通到底 | `content/abilities`（獨佔） | 1-C |

⚠️ **1-D 是驗收**，不是額外工作。`fieldAdoption` 目前把 `abilities.template` 記成
「landing」——**沒有任何一支出貨技能是用模板表達的**，所以這條路徑今天在內容上是 0 採用。

---

### 🥈 第二批 · on-attack 條件系統的**內容採用**（機制已上線，內容是 0）

> owner 2026-07-30：「on-attack 條件系統 **要優先實作**」「>=< 某個常數或某個數值條件…
> 當然機率也是 condition 甚至可以組合技」

機制在 v0.9.16 已經真的會動（`sim/content/condition.ts` + 鑄技工坊的下拉編輯器，
59-00 暴走與 52-00 十二道試煉是活的採用者）。**缺的是內容**：`fieldAdoption` 列出
**13 個條件成員零採用**（三個比較運算子 + 十個屬性軸）。

| 序 | 任務 | 檔案領域 |
|---|---|---|
| 2-A | 從 w3x 挑出**最像條件觸發**的一批技能（「血量低於 X 時」「對防禦高於 N 的目標」），逐支改寫成 condition | `content/abilities`（獨佔，**與 1-D 互斥，要排在它之後**） |
| 2-B | 組合技（`all` / `any` 巢狀）至少要有一支出貨卡 —— 目前 `any` 分支只有測試在走 | 同上 |
| 2-C | 條件的 `level` 軸接上「英雄等級」而不是「技能等級」，並確認兩者在 UI 上不會被混淆 | `packages/shared/src/sim/content/condition.ts` |

---

### 🥉 第三批 · 傳說武器擴充（owner 2026-07-30 明列的優先項，**與一、二批完全不共用檔案**）

| 序 | 任務 | 檔案領域 |
|---|---|---|
| 3-A | 四類定位補完：近戰專用擴散 / 法師保命 / 坦克衝刺 / 射手百分比傷害 | `content/items`（**與 `content/abilities` 不同目錄，但 `content:build` 仍是整棵樹 → 要排隊**） |
| 3-B | 白名單同步（`apps/platform/.../starter.go`）—— 已經踩過一次：`MatchController.ts:1036` 是**先抽再過濾**，漏一列就等於抽到空卡 | `apps/platform/internal` |
| 3-C | `damageArea` 近戰擴散的內容採用（機制在，radius 已從 ability 搬到 effect） | `content/items` |

---

### 4️⃣ 第四批 · 後台三洞 + 兩個上限（純 admin，**可與前三批完全平行**）

| 序 | 任務 | 為什麼綁在一起 |
|---|---|---|
| 4-A | #277 基礎加成**零驗證**（負值會讓全英雄一開場就死） | 三條都在同一個表單元件與同一條寫入路徑上 |
| 4-B | #278 存檔不是「下一場生效」，要重啟 shard | 同上 |
| 4-C | #279「清除」語意誤導 + clamp 靜默吃掉數字 | 同上 |
| 4-D | 殭屍上限做成後台欄位，**上限值 500**（owner 2026-07-30 裁定） | 同一頁（#287 殭屍波後台頁） |
| 4-E | #286 攻速上限 10.0：`ModOp.CapRaise` + `config.stat-caps@1` + 後台頁 | 同一組「上限類」欄位 |

⚠️ 4-A/B/C 是**同一個 bug 家族**：後台寫得進去、但寫進去的東西沒有被驗證、
不會立刻生效、而且清除的語意跟玩家想的不一樣。分開修會各修一半。

---

### 5️⃣ 第五批 · VFX 真實引用普查 #230（**owner 很在意的保真度層**）

> 93% 的技能用同一個通用替身，106 支已經抽出來的原作特效**閒置在硬碟上**。

| 序 | 任務 | 檔案領域 |
|---|---|---|
| 5-A | 逐支對照：這支技能在 w3x 用的是哪一個 effect，我們綁的是哪一個 | `content/vfx` · `apps/client/src/render/vfx` |
| 5-B | 把 106 支閒置的接上去，並把**綁錯的**列成清單 | 同上 |
| 5-C | `heightY` 的視覺修正（**229 支畫在錯的高度**）—— ⛔ **等 owner 裁決**，因為它會改變 258 支技能的觀感 | 同上 |

---

### 6️⃣ 第六批 · 延遲與效能（獨立計畫書：`~/.claude/plans/jiggly-percolating-thacker.md`）

**⚠️ 這一批多了一個 v0.9.16 量出來的具體項目：跟隨鏡頭跟不上瞬移。**
13-01 暗步。極限之圓在 **0.067 秒（2 個 tick）內位移 7.2 單位**。
`leapFraming.test.ts` 實測：朝畫面深處 **44% 的取樣被上緣切掉**，
朝畫面前方 **33% 的取樣完全不在畫面內**。
⚠️ 那條守衛量的是**拋物線構圖**，而暗步沒有拋物線 —— 所以我把 apex 0 且
飛行 ≤ 0.1 秒的瞬移從它豁免掉了（豁免條件寫死在測試裡，會跳起來的技能一支都放不過）。
**但問題本身沒有消失**，只是換了歸屬：它是**鏡頭跟隨**（#268）的題目，
要用「鏡頭插值速度 vs 位移速度」去測，不是用弧線取樣。

那份計畫的**第一層**（量測 + 客戶端 zone 剔除 + 接上已存在的 83 個 LOD 變體 + 連線資訊）
成本低、不管真兇是誰都不會白做。**它與上面五批完全不共用檔案**，可以整批平行開。

---

### 7️⃣ 第七批 · 戰場任務（#262 殭屍王 / #263 mini dota）

owner 2026-07-30 已明確：**殭屍王是 w3x 的任務角色，不移植也不上架**，
所以 #262 要重做成「跨回合累積 100 隻 → 召喚一個**我們自己的** boss」。
#263 mini dota（塔 HP 100,000，拆掉即勝）是獨立玩法，動 `sim/` 與 `arena-rules`。

---

### ⛔ 仍擋在 owner 那裡的裁決

1. **模板覆蓋率的分母**：696 份 vs 去重後的 592 份 —— 影響「95.5%」這個數字要怎麼報
2. **`autoEngage` 出貨預設**：目前是 `true`（會搶玩家的方向盤）
3. **#230 的 `heightY` 視覺改動**：229 支技能會同時改觀感
4. **火圈要不要吃護盾**：目前 `FireRingSystem.ts:98` 直接寫 `hp.hp -= dmg`，**繞過整條傷害佇列**
5. **icons-pixel/ 142 個零引用檔要不要刪**
6. **#209 魔法老師是哪一隻**：編號 15 vs 82

---

## ✅ owner 已裁決（2026-07-26）—— 這些不用再問

| # | 裁決 | 要做什麼 |
|---|---|---|
| **1** | **等級上限維持 18。** 地圖寫 40，但先不動 | 「先記錄戰況，後面再參考分析要不要調整」→ **純記錄，不排工**。要做的是讓戰況資料存得下來，日後才有東西可分析 |
| **2** | **`godie-h02n` 打我阿笨蛋不開放** | **從清單移除，不要再列。** 它的 0 成長不是待修項目，因為它根本不上架 |
| **3** | **克勞薩先生的數值要修成合理值** | 等級 1 只有 79 點生命是真的會被秒殺。修 → 見下方工作項 |
| **4** | **三條描述↔JASS 衝突一律照 JASS 修** | `A0JD` 補敏捷係數 · `A091` 補吸引 + 2×等級顆 / 250+100×等級 · `A0L6` 補擊退，且距離用 JASS 的 **800**（不是描述的 1000） |
| **5** | **節拍疊層：(a) 與 (b) 兩個都做** | 給小怪 `StatsComp`，**並且**把節拍改掛在攻擊者身上。owner：「以後還是有可能會用到」。→ **批次 1-1 解除阻擋** |

**第 5 條為什麼兩個都要，講清楚語意差別**（這是它值得兩條軸的原因）：
- 掛在**受擊者**身上 ＝「**這個敵人**被連續打了幾下」→ 對單一目標的連段、破防、處決線。
- 掛在**攻擊者**身上 ＝「**我**連續打了幾下」→ 節奏、攻速累積、四拍令咒的音樂節拍。
四拍令咒要的是後者（音樂跟著**你**的手速走，不是跟著某個倒楣鬼）。而小怪拿到 `StatsComp`
之後，前者也終於能對殭屍波生效。兩條軸都存在，未來的技能可以各取所需。

---

## ⚠️ 等 owner 裁決

| # | 問題 | 我沒有自己決定的原因 |
|---|---|---|
| **1** | **43 位英雄要不要各自捏一具體素模型？** 目前共用四具（法師 18 / 騎士 10 / 蠻族 9 / 盜賊 6），靠執行期換膚取得辨識度 | 各捏一具＝**+2.18 MB**（現在全部 255 KB），8.5 倍回歸。⚠️ 但你對體素貼圖的新指示（本地生成麥塊式四面貼圖）可能讓這題的答案改變 —— 先做貼圖，再回頭看還缺不缺剪影 |

---

## 📌 owner 累積裁定（不要再問，也不要自己改回去）

1. **版權閘退役** —— 已有邀請＋審查，不需要卡版權。已合併。
2. **變身前後共用語音** —— 省掉 460 個音檔的生成。已做完。
3. **描述與 JASS 衝突要先問 owner** —— 因為 JASS 可能是刻意的隱藏機制。
4. **`AgiMoveBonus` 不補** —— 維持沒有這根軸。
5. **保留 `growth` 區塊** —— 屬性層與 growth 相加不是重複計算，是兩個來源。
6. **生命全域倍率** —— ⚠️ **這一條在 2026-07-31 之前寫著「×8 → ×4」，那是假的。**
   owner 之後又推翻過兩次，而這一行沒有跟上：**×3 → ×4 → ×6 → ×9**。
   出貨值以 `content/config/combat-env.json` 的 `multipliers.maxHealth` 為準（**今天是 9.0**），
   由 `tools/todo-check` 的 `docEnvTruth` 守衛釘住 —— 這一行再腐爛一次會直接紅。
   （2026-07-30 的 ×6 → ×9 是 owner 依 TTK sweep 的二次裁決，commit `2eb157b4`。）
7. **喪標麥可**：力量 15 / 敏捷 8 / 智慧 0；毒與吸血改吃最大生命%；四拍＝統一 4 秒 +40% 視窗。
8. **回合結束全部歸零**，但吞噬層數是刻意例外。
9. **藍水晶不砍** —— 實測 21 分鐘剛好回到原本調校點。
10. **熊貓照自己的說明成長** —— 原作三個成長全是 0.0，跟它自己的介紹矛盾；取文案區間中點。
11. **投射物命中時才算傷害** —— 施法條件在施法時判，量值與減免在命中時算。

---

## 這份檔案的使用規則

1. **相依 = 同批，沒有例外。** 兩件事碰同一份檔案，或一件需要另一件的 schema／資料，就是同一批。跨批只允許一種關係：**前批的產出被後批消費**。
2. **Lane 是檔案領域，不是主題。** 每個 lane 標明它**獨佔哪些路徑**，lane 之間不共用任何一個檔案。批內若有先後（例如 B 要等 A 的常數），會在描述裡寫明。
3. **排序依據是「玩家按下去會不會覺得被騙」**。唯一的例外是**衛生工作解鎖整個叢集**時（單一寫者、產生器、活頁報表），那種會被提到最前面。
4. **規模**：S ≤ 半天 · M ≈ 1–2 天 · L ≈ 3–5 天 · XL = 自成一波。
5. 每批收工，回寫 `docs/_requirements-audit-gaps.md`、三張活頁、以及本檔的〈附錄：已驗證完成〉。

---

## 平行度與殘局回收（owner 2026-07-27 訂正）

**機器是 M5 Max / 128 GB / 18 核。owner 明確表示：20 個 agent 同時跑全套測試套件都沒問題。**
**不要自我設限降低平行度 —— 那不是瓶頸。**

### 真正的瓶頸是「沒人收殘局」

一次把機器壓到 owner 連字都打不了的事件（5 分鐘負載平均 **104**），根因不是當下跑幾條，而是累積：

| | 累積量 |
|---|---|
| 死掉的工作流留下的 worktree | **81 個 / 52 GB** |
| 孤兒 dev server（vite / game-server / wsfeed）still watching | **12 個** |
| `trustd` 對那幾十萬個 node_modules 二進位檔反覆驗簽章 | 常駐 ~25% |

**工作流被停掉或隨 session 死掉時，它的子行程與 worktree 都不會自動收。**

### 規則

1. **每個 agent 的交辦都要寫明：做完要 `git worktree remove` 自己的 worktree、殺掉自己起的 dev server。**
2. **長 session 中主動收殘局**（尤其每次停掉工作流之後）：
   `git worktree list` · `ps -eo pid,command | grep -E "vite|wsfeed|tsx src/index"`
3. **診斷卡頓看 `uptime` 的三個負載平均**，不要看瞬時 CPU%。
   1 分鐘遠低於 5/15 分鐘 = 正在恢復；反過來 = 還在惡化。
4. `ecosystemanalyticsd` / `trustd` / `WindowServer` 衝高**是結果不是原因**。

### ⛔ 另一個真正的上限：帳號月度用量

2026-07-27 撞到過一次 —— 四個驗證 agent 同時回
`You've hit your monthly spend limit`，**一個都沒跑成**。
出現這個時**不要重開工作流**（每次重試都再燒一點），先告訴 owner。

---

## 這個專案一直在犯的同一個錯（先讀這段，再讀批次）

到今天為止有 **八個**確認案例：功能寫完了、測試全綠、但**在執行期不可能發生**。

| 形狀 | 案例 |
|---|---|
| **accessor 沒有呼叫者** | `StatusAuraFx`（`VfxSystem.ts:407` 的註解把缺的那一行原字寫出來）、`loadVictoryTaunts()`（351 支 taunt / 5.28 MB 因此在必須發聲的當下才冷抓） |
| **權威欄位沒有讀者** | `SeatState.undoDepth`（每 tick 廣播，client 0 讀者）、`ENTITY_FLAG.CASTING`（每 tick 寫，沒人讀） |
| **事件沒有訂閱者** | 8 個 `guardian*` + 3 個 `fireRing*` 事件全部死在 server process 內 |
| **註解活得比事實久** | `ChampionMarquee.tsx` 檔頭斷言「registry 在 mount 前已載入」—— #170 讓這句話變成假的，登入頁跑馬燈永久空白 |
| **模組被出貨內容遮蔽** | #79：champion 文件的內嵌 QWER 副本蓋掉 standalone 文件，460/554 技能落回火焰 placeholder |
| **正確綁定但抵達不了** | menuNocturne 劫持 `lobby.mp3`（已修） |

> **⛔ 本計畫自己踩到同一個坑，已訂正（留著當教訓）。**
> 前一稿把 `frameDataAudition.ts` 列為「第八個案例：364 行、0 個 import」。**那是錯的，而且錯法正是本節在警告的形狀 —— 用 grep 找不到就宣告死亡。**
> 實際上 `apps/client/public/frame-data.html:117` 有 `await import("/src/render/frameDataAudition.ts")`：一個**字串裡的動態 import**，
> 任何以模組名做的 identifier grep 都看不到它。檔案是 **534 行不是 364 行**，且它的檔頭第 27-29 行**明講**「`public/*.html` 不是 build entry，所以永遠不進 bundle」——
> **不進 bundle 是設計，不是缺陷。** 把它掛成 AppRoot 路由會把 `NullEngine` + glb 解析拉進正式包。
> **教訓：宣告死碼之前，要對「呼叫點可能是字串」的路徑（動態 import、`public/**`、HTML、設定檔）各查一次。**

**驗收方法論的結論：任何一項「完成」都必須有一個執行期證據 —— 實跑註冊表、實跑 snapshot、或一張截圖。**
本計畫每一條驗收標準都照這個標準寫。

---

### 第九個案例 · 鏡頭（#235 挖到，最值得記）

**任何視覺效果的驗證畫面，必須用遊戲真正在用的鏡頭去拍。**

#93 的回合煙火從來沒有被任何人看到過，根因是 `SMALL_DISTANCE = 22` 把整輪煙火放在攝影機前方 22 公尺 ——
而 #161 把戰鬥鏡頭從 55° 拉到 **68°**、眼睛只在 9.27 公尺高，那個點**落在地板下面 9.3～14.8 公尺**。
粒子有深度測試、地板不透明，所以 **209 顆粒子活著、零像素**。
而它當初為什麼綠燈：試聽頁用 `(0, 6.5, -13)` ≈ **21°**，單元測試用 `(0, 6, -12)` ≈ **24.6°** ——
**兩個都是遊戲裡不存在的鏡頭**。烤雞之所以看得到純粹是因為它是 mesh 且被設了 `renderingGroupId = 1`
（Babylon 會在 group 之間清深度緩衝），運氣好，不是設計。
**#161 動了鏡頭角度，沒有人回頭重驗那些在舊角度下框好的特效。**

同一個形狀後來又出現三次：#247 的跳躍 77% 在畫面外、蒼月潮連段在真實遊戲裡永遠算不到、
**大廳公告後台寫得出來但玩家從來沒看過**。這已經不是個案，是這個專案的預設失敗模式。

---

## 刻意保留、不要「修」的東西

| 項目 | 為什麼 |
|---|---|
| SELA / THORNE 的 TS fixture 保持 `castTimeSec 0` | `combatTiming.test.ts` 的 `ct-04` 需要一個零前搖技能；這個分歧在 `content/loader.test.ts` 裡被**明確斷言**。game-server 永遠載 `content/` |
| `RoundWinnerStage` 與 `MatchEndPanel` 共用同一個 process-wide `victoryTaunts` player | 共用實例**正是**兩個聲音不會疊在一起的保證。安全性目前靠 `roundEndQuoteChampion` 在最終回合回 null —— **要拆之前必須先換一個等價保證** |
| `hitImpact` 沒有 profile 時完全沒有模型反應 | `EntityViewRegistry.ts:279-281` 刻意的防禦性 no-op；已記錄以免被重新「發現」一次 |

## #249 變身系統 — owner 決定（2026-07-26）

掃描結論：**26 支變身技能，100% 都是 WC3 Metamorphosis 模式**（技能上帶 `Eme1` 本體 / `Emeu` 變身體兩個單位 rawcode）。所以第二形態的完整數值、模型、技能列全部免費附贈，一個都不用重建。26 / 101 個本體角色 = **26%**，不是「幾乎大部分」。

**匯入器一直丟掉 `Eme1`/`Emeu`（#56 的 150/180 欄位碼）**，所以這件事從來沒被發現。掃描器直接讀 `raw/war3map.w3a` 才找到。

**#113 的判斷是錯的。** 那 14 對「數值完全相同的英雄」全部是本體↔變身態。它比對的名字/模型/baseStats/QWER 正好是 WC3 變身態被要求保持一致的欄位；真正的差異在它沒比的 `uabi`（球體掛件住這）、`uspe`、`usca`、`umdl`。它當時寫「那是匯入器讀到同一個英雄的兩個單位實例」——那句是誤診。**但它的建議「只報告、不要刪」救了整個資料集**：照 dedup 做下去，26 個第二形態現在已經不存在。

### owner 的三個決定

1. **開放名單的 10 個變身態 → 全部換成本體**，第二形態只能靠變身技能進入。這同時修掉：草泥馬是移速 0 的臥草態、妙蛙種子其實是 scale 3.0 的妙蛙花（**很可能才是 #32/#150 那串大小喬不好的真正根因**）、拳四郎是北斗之鼠、悟空已經是超賽所以他的 R 變成空技能。曹操只有變身態、本體 `O02N` 從沒匯入，要補。
2. **4 支掛在天生技欄位的變身（魯夫二檔、克勞薩、飛影邪眼全開、卡迪那紮根）→ 做成條件自動觸發的被動。** w3x 沒有這個設定，觸發條件要自己設計。
3. **推翻 2026-07-22 的「疑慮一律判斷寬鬆為多英雄」在這三組上的適用**：25 拳四郎 / 58 皮卡丘 / 61 克勞薩 改以 w3x 的 `Eme1`/`Emeu` 為準，判為本體↔變身態。原規則保留為預設，「有 w3x 欄位證據」是明確的例外條款。

### 待處理的阻擋項

- 30 臭作、70 白木卡迪那：帶 `Eme1`/`Emeu` 的技能**不在本體的技能列上**（`A0YT`/`A0O6` 只掛在變身體），war3map.j 也沒引用。變身沒有可達的觸發點，要先解決。
- 球體掛件是 `Asph` 物件表機制，76 個物件，**完全沒接線**。變身態的視覺簽名就是它（`球體(武裝霸王)` 掛索隆變身態、`球體(悟空超3)` 換掉本體的 `球體(悟空正常)`）。五個模型其實**已經在 content 裡**，缺的是掛點通道，不是粒子抽取。

### #249 換名冊的連帶損傷 —— 藍水晶經濟被無聲改掉（已修，`fix/249-store-alignment`）

換 10 格名冊時 **`content/config/store.json` 的 `championPrices` 沒有跟著換**，而且沒有任何測試釘住這層關係，所以全部閘門都是綠的。

**沒有價格 = 免費，兩邊都是**：client `lockStateOf` 看到 `price === undefined` 回 `"free"`；server `wallet.OwnsChampion` 看到 `!priced` 直接回 `true`。後果有兩個，第二個比第一個嚴重：

1. 換進來的 10 個本體完全跳過 300 水晶解鎖 → 免費英雄 **12 → 19**、水晶去化池 **38 → 31**。
2. `FreeChampions()` 用 `price == 0` 種新帳號的 `OwnedChampions`，所以**每一個新註冊的帳號都還在被贈送 `godie-h020` / `godie-o00x` / `godie-u01u`** —— 三個現在根本選不到的 id。大廳商店也照樣為這 10 個孤兒各畫一列死掉的商品。

**修法：形狀原封不動。** 每個換進來的本體**繼承它取代的變身態的價位**，10 個舊 id 移除。前 / 壞掉 / 修好 = `12 免費 · 38 付費` → `19 · 31` → `12 · 38`，價格一律 300。owner 已明講藍水晶不動（「藍水晶本來就是獎勵 有人抱怨我們再來改」），所以移動的只有 id，不是數字。副作用：免費那 3 格從變身態換成本體，所以現在免費的是莉娜因巴斯 / 悟空 / 索隆本人。

**守衛（這比修本身重要）**：`TestStarterRosterMatchesChampionPrices`（Go，`whitelist-store-prices`）＋ game-server 的 `curationVsContentModel.test.ts` 鏡像檢查。兩個方向都驗，而且**失敗訊息直接列出漂掉的 id**，不是只報數量。連 `12 / 38` 的形狀也一起釘住，下一個動名冊的人會被逼著明講他要改經濟。

### 其餘 roster id 消費端的普查結果

| 消費端 | 有沒有漂 |
|---|---|
| `championPrices`（商店 / 解鎖 / 新帳號種子 / 大廳商店列） | **漂了** —— 就是上面那條，已修＋已守 |
| `FreeChampions()` / 新帳號種子 | 沒有自己的表，跟著 `championPrices` 一起修好 |
| 錢包最愛（favourites） | 舊資料可能留下死 id，改成**讀取時過濾**（見下） |
| `content/config/champion-voices.json` | 沒漂（113 位全覆蓋） |
| `content/config/victory-taunts.json`、`quotes.json` | 沒漂（111 / 112 位全覆蓋） |
| `content/assets/vfx/w3x-ability-provenance.json`（VFX 綁定表） | 沒漂（109 位、639 支技能全覆蓋，本體與變身態都在） |
| 英雄頭像覆蓋率 | **完全沒變**：換出去缺圖的是 `h02r` / `h02u`，換進來缺圖的是 `hgam` / `h02v` —— 同樣兩位英雄（#90 妙蛙種子、#92 草泥馬），名冊缺圖數維持 8 |
| champ-select / codex / 白名單 | 一律吃 `starterChampions`，自動跟著走 |
| **戰鬥語音包** `content/assets/audio/voices/lines/` | **漂了，本批沒修** —— 50 個目錄正好是**舊**名冊，換進來的 10 個本體一個語音包都沒有（每位 46 clip）。要跑 CosyVoice 產檔，是資產產線的工作不是本批的；**沒有加守衛，因為現在加就是紅的**。歸 #142／語音批次 |

### owner 會在畫面上看到的一件事：拳四郎變醜了

`godie-u00l` 北斗之鼠穿的是**真的 `imported.heropikachu`**；換回本體 `godie-umal` 拳四郎之後，他穿的是 CC0 替身 `champ.skin.barbarian`。**這是修正本身的必然結果，不是 bug**：地圖給拳四郎本體的模型是 Blizzard 內建的 VillagerMan1，本來就拿不到，而 `starter.go` 早就寫明「共用替身模型＝美術還沒補，不是同一個英雄」。**不要為了好看把名冊換回變身態。** 另外 `content/skins/skin.godie-u00l.heropika.json` 這件造型現在掛在一個選不到的英雄身上（大廳商店會用 `deriveStoreRows` 的 fallback 幫它補一列），要不要改掛到 `godie-umal` 是設計題 —— 但那等於用造型系統做變身，跟 owner「變身態改由技能觸發」的裁示牴觸，所以本批**不動**，留給 #119／#116。

### 最愛（favourites）：選擇「讀取時過濾」而不是資料遷移

`ToggleFavourite` 本來就擋不存在的英雄，所以**寫不進**壞資料；壞掉的方式是名冊在合法的 pin 底下被換走。改在 `overlayMeta` → `liveFavourites` 過濾（`apps/platform/internal/wallet/meta.go`）。

選它不選遷移的理由：遷移要**永久刪掉**耐久紀錄裡的那筆，而英雄是會回來的（白名單可編輯、#119 之後變身態也可能重新可選）；過濾是冪等的、不需要遷移步驟、不會弄丟一筆之後會再度合法的 pin，而且掛在所有介面都會經過的那一條讀取路徑上。空 catalog（平台沒掛 content 也會開機）**不當成證據**，原樣放行，否則一個設定錯誤就會清空所有人的置頂。測試：`TestFavouritesDropChampionsTheCatalogLost`。

## 三個結構性發現（2026-07-26，來自 #247 / 抓取系普查）

這三個都不是「某支技能修好了」，而是「我們一直用錯了一把尺」。

### 1. 垂直軸一直用錯尺 —— 一直有兩把

#247 用平面匯入尺 `GGD_PER_WC3 = 11/600` 去換算 `SetUnitFlyHeightBJ` 的振幅。但**平面尺是由場地幾何決定的**（763 → 14.00u，正確）；**垂直尺是由鏡頭決定的**，而 GGD 的鏡頭不是 WC3 的（WC3 約 30° / 1650u / ~70° fov；GGD 68° / 10u / 0.8 rad）。

解「離鏡頭目標多高還留在視錐內」：WC3 約 950 單位頂空，GGD 只有 5.51u → **每 1 GGD 單位 ≈ 172 WC3 單位，不是 54.5**。平面尺把每條弧在畫面上放大了約 3.2 倍，蒼月潮 77% 的飛行時間在畫面外。

新增 `GGD_APEX_PER_WC3 = 1/250` 與獨立的 `wc3h` 單位。階層完全保留（1000>600>400>300>250 → 4.00>2.40>1.60>1.20>1.00），而且查證到 A0RZ（A=1000）**在 WC3 自己的頂空之外**，也就是那條弧在原作裡本來就會探出畫面——`1/250` 就是選來讓剛好只有那一條也還是會探出去。

**刻意不加執行期 clamp 或 zod `.max()`**：靜默夾限才是忠實匯入規則真正禁止的東西；紅色測試才是「知情地抬高守衛」的機制。

### 2. 地圖本來就有共用的位移引擎

`Set_Move_Value(unit, Distance, Angle)`，`war3map.j:4785`，區段標題「**共同擊退觸發**」。`Move_Func`（j:4745-4783）0.04 秒週期、每 tick 走 50 wc3；**實際位移 < 10 wc3 就判定撞牆**——彈回、生 ThunderClap、砸掉 150 內可破壞物、中止。

散落在各技能裡的碰撞判定（大熊 `>8` j:51210、Berserker `>=5` j:52127、皇者 `>8` j:54373）**是這個函式的複製變體，門檻不同是抄寫失誤而不是設計**。

意義：抓取系的拋出階段**要移植這個原語，不要自己發明**；`collisionEps` 統一成引擎自己的 10 wc3。而且原作分層很清楚——**地面走 `Set_Move_Value`、空中走 `SetUnitFlyHeightBJ`，沒有任何抓取 trigger 自己寫位移數學**。這也要重新檢視 `SIM_CAPABILITIES.knockback` 那條「刻意不提供」。

### 3. 施法時算死 vs 落地才算 —— 一整類的時序錯誤

蒼月潮的連段加成實作了、測試綠了、**在真實遊戲裡永遠不會生效**。

JASS 在 `Trig_Jump_Start_Actions`（j:34211-34216）就把 `+5×敏捷` 加進 `udg_MoonDamage`，41 tick 的弧線跑完後落地發的是**早就凍結的數字**。所以 1 秒的連段視窗在飛行途中過期，在 WC3 裡無所謂。

GGD 反過來，`comboBonus` 在**傷害套用當下**才解算，而那是在**落地**——飛行 1.435 秒 > 視窗 1.00 秒，**任何時序都算不到**。

之前的測試會過，是因為它**在隔離環境測效果，沒有真的飛完整條弧**。這是「完成宣告要問這條觸發在正常一場遊戲裡真的會發生嗎」的又一次。要當成一個**類別**掃：任何施法與結算之間有時間差的效果（跳躍、飛彈、延遲 AoE、引導），只要它的傷害項是條件式的，都可能有同一個病。

## 傷害結算時機：一條標準規則（owner 2026-07-26）

**關於「施法這個動作」的條件 → 施法當下判定。**
**傷害的量值與減免 → 命中當下計算。**

owner 的原話：「通常攻擊與技能都是命中時才看實際情況計算傷害（例如對方看到飛過來前開了防禦提升等）」。

兩者不衝突，因為它們問的是不同的問題：

| | 何時 | 依據 |
|---|---|---|
| 蒼月潮 07-03 的連段視窗 | **施法當下** | 條件是「在者、皆、陣發動後 1 秒內**施展**」——問的是按鍵那一刻。飛行 1.435 秒 > 視窗 1.00 秒，在命中時問永遠是「否」，所以那是 bug（已修，`bakeCastTimeConditionals`） |
| AD/AP 係數、目標的護甲/魔抗/護盾 | **命中當下** | 攻守雙方在飛行途中做的事都應該算數 |

### 這代表 9 條技能刻意不忠於 JASS

原作在施法時就用 `GetHeroStatBJ` 把係數讀死，GGD 讓它到命中才讀，所以施法者在飛行途中被削弱時，投射物會跟著變弱。**這是 owner 知情的選擇，不是遺漏。**

受影響：`godie-hapm.w`、`godie-hart.w`、`godie-u00n.r`、`godie-u00o.r`（onLand）；`godie-u00n.e`、`godie-u010.e`、`sela.q`、`thorne.e`、`augments/storm-arrow`（onHit）。

**下一輪「JASS 為準」的稽核不要把這 9 條當成 bug 修回去。** 已登記在 `docs/todo/abilities.md` 的 `jass-247-14`。

---


---

## 工作流交辦的一個結構性錯誤（我犯了第三次）

**勘查／量尺階段的 agent 沒有 `isolation: 'worktree'`，所以它們在主 worktree 裡跑。**

已發生三次：
1. `tools/w3x-import/_wk73_load.mts` 被我的 `git add -A` 掃進 main
2. 一個 scout agent 在主 worktree 裡 `git checkout` 了自己的分支，
   我接下來一整段工作（殭屍熱修、#259 合併、守衛、骷髏退場）全落在錯的分支上，
   而 `git push origin main` 一路回報「Everything up-to-date」
3. **這一輪**：#268 v2 的量尺 agent 在主 worktree 建了 `apps/client/src/render/aimReach.measure.test.ts`；
   另一條工作流在我跑 client 全套的**同時**建立又刪除了 `src/render/__dbg.test.ts`，
   造成一次假的 FAIL（vitest 收集到檔案、執行時檔案已消失）

**規則（從現在起每一份工作流交辦都要帶）：**
- **每一個會寫檔的 agent 都要 `isolation: 'worktree'`**，不只是實作 agent。
  「唯讀勘查」不是理由 —— 勘查 agent 會寫探針、會寫暫存腳本、會 checkout 分支。
- docs commit **一律逐檔列出路徑**，永遠不用 `git add -A`。
- 主 worktree 的測試結果，若失敗的是一個**不存在的檔案**，先懷疑是併行工作流的競態，
  再重跑一次確認。

---
