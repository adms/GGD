# 📋 守則犯錯帳本

> owner 2026-08-27（逐字）：
> 「你要把**每次開發守則上規則犯的錯記成一張表 守則犯錯.md** 用來**統計每次犯錯的頻率及原因**，
>  日後可以用來**改進反思**，請你記到開發守則」

⭐ **兩欄是重點**，⛔ 不是「記一句話」：
**守則代號**（犯的是哪一條 → 頻率）＋ **成因代號**（**為什麼**會犯 → ⭐ 改進的抓手）。
兩欄都只准用封閉詞彙表（`tools/rule-slip/ledger.py`）—— 打錯字的代號會讓統計**靜默分裂**成兩列，
而一份會靜默分裂的統計比沒有統計更糟：它看起來是對的。

**記一筆（⛔ 不要手改這個檔）：**

```bash
bash scripts/rule-slip.sh 技術-離開碼 疲勞重複 "又用 | head 讀 tsc 的離開碼"
```

`--check` 是閘（統計區過期或代號打錯 ⇒ 非零）；`--stats` 只重算統計區。

<!-- SLIP_STATS_BEGIN -->

## 📊 統計（18 筆 · ⭐ 自動重算，⛔ 不要手改這一區）

### 依守則

| 守則 | 次數 | 是什麼 |
|---|---:|---|
| `2-守衛` | **3** | 第二：改壞不會紅（沒守衛／斷言方向錯／被測的不是出貨的） |
| `0.4-二住處` | **3** | 第〇·四：同一個值有第二個住處（算得出來的數字被烘進文件/測試/註解） |
| `0-產物` | **2** | 第零：改產生器的產物而不是來源 |
| `0-順序` | **2** | 第零：順序相依沒排對（產生器／閘的先後） |
| `技術-離開碼` | **2** | 硬約束：`| head` / `| tail` 吞掉離開碼，或背景任務的 exit 誤讀 |
| `技術-shell` | **2** | 硬約束：shell 轉義吃掉內容（反引號／錢字元；訊息沒走 --body-file） |
| `0-備份` | **1** | 第零：覆蓋／刪除前沒留底（含改 CLAUDE.md／記憶前沒快照） |
| `👁-未驗收` | **1** | 👁：沒有終端證據就說「做完／已修」 |
| `3-註解說謊` | **1** | 第三：註解／文件的宣稱沒去驗（引用的檔或測試不存在） |
| `1-推測當需求` | **1** | 第一：把我的推測寫成事實／需求（票、模板預設值、註解） |

### 依成因（⭐ 改進的抓手）

| 成因 | 次數 | 是什麼 |
|---|---:|---|
| `沒有閘` | **5** | 這條規則當時只是散文，沒有會紅的東西 |
| `憑印象` | **4** | 沒有打開來源就下結論（記憶／推測代替量測） |
| `疲勞重複` | **4** | 同一個坑今天已經踩過，仍然再踩 |
| `順手` | **3** | 為了快，跳過一個已知的步驟 |
| `工具騙我` | **1** | 工具／訊息／文件給了錯的資訊（誤導源） |
| `抄字面值` | **1** | 把一個值複製到第二個地方，而那裡沒有守衛 |

### 依日期

  2026-08-27 18 筆

⭐ **最常犯的守則**：`2-守衛`（3 次）—— 第二：改壞不會紅（沒守衛／斷言方向錯／被測的不是出貨的）
⭐ **最常見的成因**：`沒有閘`（5 次）—— 這條規則當時只是散文，沒有會紅的東西

<!-- SLIP_STATS_END -->

## 📒 逐筆（append-only，⭐ 最新在最上面）

| 日期 | 守則 | 成因 | 我做了什麼 | commit |
|---|---|---|---|---|
<!-- SLIP_ROWS -->
| 2026-08-27 | 2-守衛 | 沒有閘 | 把 /__live 接上 sidecar 卻漏了 nginx 的 location，而我自己的 /healthz 只驗「模組載得起來」（名詞）沒驗「nginx 送得到」（關係）—— 失敗形態⑧，而我在同一個檔的檔頭剛寫過這條教訓 | `7acdc0ce` |
| 2026-08-27 | 0-備份 | 順手 | 拿一筆真的批核登記（firering）當測試跑 register.mjs，把它的 title/family/issues/rollback 全蓋掉 —— 已從 HEAD 逐欄還原 | `5389179a` |
| 2026-08-27 | 2-守衛 | 憑印象 | 同一段還寫了 sudo，而 hostDeployScript.test.ts 早就明令禁止（容器內 root 才是）—— 閘抓到了，但那代表我沒讀既有形狀就寫 | `e77ef0c4` |
| 2026-08-27 | 0-產物 | 憑印象 | 在 host-deploy.sh 插入新段落時引用了一個不存在的變數 $REPO_DIR —— 沒先讀那支腳本自己的慣例（它從 repo 根跑，用相對路徑） | `e77ef0c4` |
| 2026-08-27 | 0-順序 | 疲勞重複 | board:build 又排在 docs 編輯之前（今天第四次）—— 它讀 docs/_daily 與 _release 必須最後跑 | `2c9badc1` |
| 2026-08-27 | 0.4-二住處 | 順手 | 還原 perRank 時把 value 寫回帶級別的節點，造出雙住處（templateFailSoft 抓到） | `ca132fa9` |
| 2026-08-27 | 技術-離開碼 | 疲勞重複 | 同日第二次：pnpm tsc / head 讀離開碼拿到 head 的 0，真相是紅的 | `2f17c899` |
| 2026-08-27 | 0.4-二住處 | 沒有閘 | #789 級距化把 perRank/ranks 的升階曲線壓平（鬼隱之擊 +150%→+100%）—— cooldownTier 早就寫過『填級別＝每階同值』而沒有閘 | `2f17c899` |
| 2026-08-27 | 技術-shell | 順手 | 自己加的 genguard hook 把 heredoc 內文的 > 當重導，解析出 / 而擋下合法寫檔 | `2fafa1ce` |
| 2026-08-27 | 2-守衛 | 沒有閘 | #216 的面向守衛只數傷害事件、對面向零斷言 ⇒ 林克倒著走而它綠著（#783） | `2fafa1ce` |
| 2026-08-27 | 0-產物 | 工具騙我 | sync-io 戶籍漏登 ⇒ 三處 EACCES 我用寫入端自解鎖繞過（治症狀），根因是量測沙盒保留 444（#771） | `2fafa1ce` |
| 2026-08-27 | 0-順序 | 沒有閘 | board:build 讀 docs/_daily 與 _release 卻先跑，同一個閘連紅三次 | `2fafa1ce` |
| 2026-08-27 | 👁-未驗收 | 沒有閘 | 龍破斬宣稱比照 JASS，而 08-25 的證據自己寫著飛行段 0 亮像素、且驗收頁 21 批零裁決沒人看見（#779 #785） | `2fafa1ce` |
| 2026-08-27 | 3-註解說謊 | 憑印象 | 看 A/B 圖就斷言「FragDriller 岩塊原作沒有」，而 JASS 與產生器來源早就逐行寫明它是 h008 | `2fafa1ce` |
| 2026-08-27 | 1-推測當需求 | 憑印象 | #244 寫「擋住 44/18/13 支」當成量到的數字，實際產物寫的是軸維度 43/38/17/14/6 | `2fafa1ce` |
| 2026-08-27 | 0.4-二住處 | 抄字面值 | stockGlowAdditive.test.ts 把 ALPHA_ONEONE=0 抄成第二個住處 ⇒ 斷言的正是缺陷本身（#780） | `2fafa1ce` |
| 2026-08-27 | 技術-shell | 疲勞重複 | gh issue comment 用反引號寫檔名，被 shell 當命令替換吃掉（同日第二次，訊息應一律走 --body-file） | `2fafa1ce` |
| 2026-08-27 | 技術-離開碼 | 疲勞重複 | 用 pnpm tsc / head -4 讀離開碼，拿到的是 head 的 0；同一個坑 CLAUDE.md 記著三種變形 | `2fafa1ce` |
