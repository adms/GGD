# 🦗 蝗蟲群保真移植計畫（GH#688 · 追蹤到做完為止）

> owner 2026-08-25（逐字）：「所有JASS技能、部隊單位是蝗蟲群 都請你各自建表建md檔
> 互相對應參數跟演出時序，才能做到完美的技能特效移植，**不能靠盲猜**，
> 請你列為一個大計畫md持續追蹤直到做完為止」
>
> 驗收起點：悟空 09-04 —— 「特效完全沒看到最重要的蝗蟲群」
> （`CreateNUnitsAtLoc(1,'h007',…)` · h007=特效龜派 · ReviveHuman.mdl · scale 1.25）

## 資料源（全部已在 repo,⛔ 不用重解 w3x）

| 檔 | 是什麼 |
|---|---|
| `tools/w3x-import/out/GoDieEX22s-src/OBJECTS.json` | 461 隻自訂部隊的欄位（model/scale/…；⚠️ **頂點色/alpha 未浮出** → Phase 0 要補抽取器） |
| `tools/w3x-import/out/vfx-census/MODEL_USAGE.json` | 模型使用普查 |
| `war3map.j`（GoDieEX22s-src） | 每個 CreateUnit/CreateNUnitsAtLoc 生成點＋時序 |

## Phase 表（做完一格勾一格）

| # | 交付物 | 閘 | 狀態 |
|---|---|---|---|
| **0** | 抽取器補**頂點色/alpha** 欄（unit w3u 的 tint 三欄+alpha）——「大小顏色透明度」的顏色那 2/4 沒有它就是盲猜 | 抽出 h00S=[255,100,100] 對上模板檔頭既有紀錄 | ✅(tint)：閘達成——`UNIT_TINTS.json` h00S=[255,100,100] ✓，236/236 全覆蓋；抽取器**要加的行數=0**（src_objects.py:53/:176 已會抽），只差重跑產生器刷新過期的 OBJECTS.json ⸱ ⛔(alpha)：w3u **結構上無此欄**（ucua 0 次）→ ✅(alpha) 2026-08-25：JASS 掃描落地於 `locust:build`（`census.json` runtimeAlpha：57 呼叫點回溯 —— resolved 26 · event-unit 25 · unresolved 6 **誠實標不猜**） |
| **1** | **蝗蟲群單位普查表**（產生的）：dummy 子集逐隻 id·名·model·scale·tint·alpha | `locust:check` 逐位元組 | ✅掃描完成：`docs/_reports/locust_scan/units.md`+`units.json`（236 隻；判準修正——**Aloc 承重**、base 橫跨 19 個 rawcode；非白 tint 133；alpha 全 null=w3u 無欄位）⸱ ✅產生器化 2026-08-25：`pnpm locust:build` → `docs/蝗蟲群對應表.md`＋`tools/locust-census/census.json`（正式版,判準逐字沿用）；`locust:check` 已進 `skills:check`,`locust:build` 已進 `skills:sync` 尾端 |
| **2** | **常用 MDL 參數 md**（owner 點名 7 支＋普查高頻）：發射器/材質/大小/顏色/alpha/壽命 | 同上 | ✅點名 7 支 7/7：`mdl-params.md`（alpha 病族 3 貼圖=LUMA-KEY 候補＋出生 alpha=0 病 3 例；拿不到清單**空**）⸱ 缺口：普查高頻族（TornadoElemental×9、MonsoonBoltTarget×5…）未掃；⚠️ `locust:check` 已建但**不含 MDL 參數**（mdl-params 仍是一次性掃描） |
| **3** | **JASS×dummy 交叉表**：每隻 dummy 每個生成點（技能・位置・facing・sleep 時序・移動・壽命・Scale/VertexColor 呼叫） | 同上 | ✅掃描完成：`jass-sites.md`+`jass-sites.json`（644 生成點/263 rawcode/Trig 內 411；09-04 逐行；⭐前段 hfoo+ogru+hkni=65 支是**隱形隱藏施法**→零視覺工作）⸱ ✅產生器化 2026-08-25：交叉表併入 `蝗蟲群對應表.md` §三＋runtime alpha §四（`locust:build`/`locust:check`）；合成見 `synthesis.md` |
| **4** | 參數對應機制落地：unit scale/tint/alpha → modelFx 逐 key 映射；static `count` 沿線（#673-④ 併入） | 守衛+突變 | ✅(①static count×spacing 沿線：sim+schema+preset+模板 count:6/spacing:2 預設,一格改回 1=rollback；突變「沿線迴圈改單具」→紅已驗)⸱ ✅(②`model@1.fxAlpha` 引擎+client 材質 alpha 乘法,缺席=1=今天;⚠️ 57 個 SetUnitVertexColorBJ 的 per-cast 接線是後續批,fieldAdoption landing 豁免在案)⸱ ⬜(③glb 動畫剪輯播放/timeScale：**評估完成⛔未實作**,接縫與擋的支數見 `docs/_reports/M4_temp_20260825.md` §機制③) |
| **5** | **pilot：悟空 09-04**（h007×ReviveHuman）連續圖片驗收 | beam-audition 亮像素 | ✅ stock MPQ→glb 第一支落地（`w3x.stock.revivehuman`＋h006 的 `w3x.stock.flamestrike1`,luma-key 路徑,gate⑥⑦綠）；09-04 兩份 doc＋champion 鏡射綁 主體×6 沿線＋火柱×6；連續擷圖+逐張亮像素 `docs/_reports/goku_visual-proof_20260825-0200/`（基線 0 → 出生+載入 **1384 亮/3060 lit** → 到期回 34）⸱ ⚠️ 待主 session `skills:sync`（bundle/_index 是產物）；h008 外層凍播＝機制③後續 |
| **4c** | ⭐ **模板化**（owner 2026-08-25：「所有這些球體、蝗蟲群特效都要變成模板，可以被編輯器複用、成為 JSON 設定模板標籤」）—— census 推導 ≤6 個 `tpl-locust-*` 家族，吃得下五欄，進 Codex 契約 | paramsSchema/stack + census --check（#693） | ✅ 2026-08-25：**4 個**家族（2×2＝位移 × 多具，從 census 推導：static-single 165 · static-line 15 · travel-single 12 · travel-line 4）；⛔ 刻意不用 timedLife（GGD 這側同一格 lifeSec）與 anim/timeScale（機制當時未落地）當第三、四軸。四族共用**同一支** `modelFxFamily`（零行新程式）；`onTouch` 改由「有沒有宣告 `touchDamageTier`」推導 ⇒ ⛔ 純演出模板不塞傷害。新機制：**節點級 `spawnModelFx.tint`/`.alpha`**（schema+sim+client 一條線，rig 的 free-list 按外觀分家）＋ 新 slot 型別 `rgb`。**10 個既有節點**（77-01/14-01/79-03/11-04/09-04×2/38-03×2/38-002×2）改成引用模板並**逐鍵等價**（golden 取自 `git show HEAD:`）。建議表 → `蝗蟲群對應表.md` §五（196 隻逐隻）。守衛 `templates/locustTemplates.test.ts`（突變驗過：tint 從展開器拿掉 ⇒ 紅）⸱ ⚠️ 待主 session `caps:export` + `content:build`（`_index`/`bundle` 不含四份新模板）⸱ 報告 `docs/_reports/T6_temp_20260825.md` |
| **6** | 逐技能鋪開（按普查支數排序）＋ **一頁式連續圖片批核**（owner 2026-08-25：「所有球體、蝗蟲群都要進後台一頁式連續圖片批核**但預設先上線**」）—— 每列一個家族、附逐幀序列＋亮像素、事後否決（#669） | 天譴式擷圖 | 🔄 **進行中**：① MonsoonBoltTarget（o00E 一族 12 支，v0.27.0）② **TornadoElemental ×9**（v0.27.5，aa5e776c）：模型 War3x.mpq→glb 轉換 gate 全綠；綁定 6/9（e00Y/e016→emfr.py · e013→hart.e · h01S→udre.r＋u01u.r · o01P→u00v.e · o01H→i01v）；**無落點 3 誠實列**（h027 原作 α=0.01% 刻意隱形、u00A/u00Z sites=0，`SHARED_MODEL_UNADOPTED` 釘住）；#669 已登記（rollback＝tpl-locust-orb.params.alpha.default 1.0→0）；audition 基線 0→419 lit→到期 0。⸱ **下一族**（建議表支數序）：crescent ×5 · ReviveHuman 殘餘 ×4 · TomeOfRetrainingCaster ×4 · WarStompCaster ×4 · Meteor ×4 |

## 已知風險（開工前寫下,⛔ 不是事後補）

- ReviveHuman/Awaken 這族 stock MDL 可能與 netherstrike 同為 alpha 病族（形狀住 alpha）⇒ LUMA-KEY 前例在 `gltf.py`/`extract_particles.py`
- 悟空「看不到蝗蟲群」的已知成分：①static 單具 vs 原作十具（#673-④）②h007 的 ReviveHuman 模型未進 beam 綁定（模板預設 netherstrike=h00X 的模型,而悟空的 dummy 是 h007）③頂點色未映射

## 對話↔票同步

#688（本 epic）· #673（beam 驗收,④併入 Phase 4）· #674（球體蝗蟲群 epic,其驗收併入 Phase 6）
