# 🦗 蝗蟲群保真移植計畫（GH#688 · 追蹤到做完為止）

> owner 2026-08-25（逐字）：「所有JASS技能、部隊單位是蝗蟲群 都請你各自建表建md檔
> 互相對應參數跟演出時序，才能做到完美的技能特效移植，**不能靠盲猜**，
> 請你列為一個大計畫md持續追蹤直到做完為止」
>
> 驗收起點：悟空 09-04 —— 「特效完全沒看到最重要的蝗蟲群」
> （`CreateNUnitsAtLoc(1,'h007',…)` · h007=特效龜派 · ReviveHuman.mdl · scale 1.25）

## 資料源（全部已在 repo,⛔ 不用重解 w3x）

| 檔 | 是什麼 |
|---|---|
| `tools/w3x-import/out/GoDieEX22s-src/OBJECTS.json` | 461 隻自訂部隊的欄位（model/scale/…；⚠️ **頂點色/alpha 未浮出** → Phase 0 要補抽取器） |
| `tools/w3x-import/out/vfx-census/MODEL_USAGE.json` | 模型使用普查 |
| `war3map.j`（GoDieEX22s-src） | 每個 CreateUnit/CreateNUnitsAtLoc 生成點＋時序 |

## Phase 表（做完一格勾一格）

| # | 交付物 | 閘 | 狀態 |
|---|---|---|---|
| **0** | 抽取器補**頂點色/alpha** 欄（unit w3u 的 tint 三欄+alpha）——「大小顏色透明度」的顏色那 2/4 沒有它就是盲猜 | 抽出 h00S=[255,100,100] 對上模板檔頭既有紀錄 | ✅(tint)：閘達成——`UNIT_TINTS.json` h00S=[255,100,100] ✓，236/236 全覆蓋；抽取器**要加的行數=0**（src_objects.py:53/:176 已會抽），只差重跑產生器刷新過期的 OBJECTS.json ⸱ ⛔(alpha)：w3u **結構上無此欄**（ucua 0 次）→ 要新開 JASS 掃描（57 個 SetUnitVertexColorBJ 呼叫點），見 synthesis §1 |
| **1** | **蝗蟲群單位普查表**（產生的）：dummy 子集逐隻 id·名·model·scale·tint·alpha | `locust:check` 逐位元組 | ✅掃描完成：`docs/_reports/locust_scan/units.md`+`units.json`（236 隻；判準修正——**Aloc 承重**、base 橫跨 19 個 rawcode；非白 tint 133；alpha 全 null=w3u 無欄位）⸱ 缺口：報告尚未產生器化，`locust:check` 閘未建 |
| **2** | **常用 MDL 參數 md**（owner 點名 7 支＋普查高頻）：發射器/材質/大小/顏色/alpha/壽命 | 同上 | ✅點名 7 支 7/7：`mdl-params.md`（alpha 病族 3 貼圖=LUMA-KEY 候補＋出生 alpha=0 病 3 例；拿不到清單**空**）⸱ 缺口：普查高頻族（TornadoElemental×9、MonsoonBoltTarget×5…）未掃；`locust:check` 未建 |
| **3** | **JASS×dummy 交叉表**：每隻 dummy 每個生成點（技能・位置・facing・sleep 時序・移動・壽命・Scale/VertexColor 呼叫） | 同上 | ✅掃描完成：`jass-sites.md`+`jass-sites.json`（644 生成點/263 rawcode/Trig 內 411；09-04 逐行；⭐前段 hfoo+ogru+hkni=65 支是**隱形隱藏施法**→零視覺工作）⸱ 缺口：`locust:check` 未建；合成見 `synthesis.md` |
| **4** | 參數對應機制落地：unit scale/tint/alpha → modelFx 逐 key 映射；static `count` 沿線（#673-④ 併入） | 守衛+突變 | ⬜ |
| **5** | **pilot：悟空 09-04**（h007×ReviveHuman）連續圖片驗收 | beam-audition 亮像素 | ⬜ |
| **6** | 逐技能鋪開（按普查支數排序）＋ #669 一頁批次驗收 | 天譴式擷圖 | ⬜ |

## 已知風險（開工前寫下,⛔ 不是事後補）

- ReviveHuman/Awaken 這族 stock MDL 可能與 netherstrike 同為 alpha 病族（形狀住 alpha）⇒ LUMA-KEY 前例在 `gltf.py`/`extract_particles.py`
- 悟空「看不到蝗蟲群」的已知成分：①static 單具 vs 原作十具（#673-④）②h007 的 ReviveHuman 模型未進 beam 綁定（模板預設 netherstrike=h00X 的模型,而悟空的 dummy 是 h007）③頂點色未映射

## 對話↔票同步

#688（本 epic）· #673（beam 驗收,④併入 Phase 4）· #674（球體蝗蟲群 epic,其驗收併入 Phase 6）
