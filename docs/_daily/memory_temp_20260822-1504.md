# memory_temp 2026-08-22 15:04

> ⚠️ 這是 **context 溢出前的保命快照**，不是交付文件。
> 下一輪接手時：先讀這一份，再讀 `docs/_session-handover.md`。
> 機械欄位由 `scripts/memory-temp.sh` 產生；**「只有我知道」那幾節要手填**，
> 留著 `（待填）` 的欄位代表這一份快照是不完整的。

| | |
|---|---|
| transcript | 1408 MB |
| 上一份快照 | memory_temp_20260821-1949.md/Users/Takuro/GGD/docs/_daily/memory_temp_20260821-1949.md |

## 機械狀態

```
HEAD      565a94be docs(#450)(#539)(#540): owner 的四份原作截圖入庫 —— ⛔ 而我把 EVA01 的兩個特效講反了
branch    main
未 push   34 個 commit（vs origin/main）
工作區    已追蹤改動 1 檔 / 未追蹤 1 項
```

<details><summary>工作區逐檔</summary>

```
 M README.md
?? docs/_daily/memory_temp_20260822-1504.md
```

</details>

## 工作流（這個 session 派出去的）

| run | 進度 | 狀態 | agents |
|---|---|---|---|
| `wf_9f3fcaee-a88` | 6/6 | ✅ 完成 | — |
| `wf_2a4a0cfc-97d` | 25/25 | ✅ 完成 | — |
| `wf_9ae095a0-d3d` | 4/4 | ✅ 完成 | — |
| `wf_9a75263d-0cc` | 6/6 | ✅ 完成 | — |
| `wf_b9c38ba5-e90` | 9/9 | ✅ 完成 | — |
| `wf_a4d13e5f-5ca` | 0/7 | ⏳ **還在跑** | — |

## 今天開/動過的 issue

- #501 open — 🧾 330 張開票全面盤點：已完成的關掉、剩下的排成可平行批次
- #519 closed — 🎮 指定型技能沒有目標指示 —— 手把玩家不知道這一發會打誰
- #535 closed — 📱 手機註冊 QR —— 全新玩家在掌機上開帳號，⛔ 全程不打字
- #536 closed — 🖤 芙莉蓮迷宮的地板全黑
- #537 closed — 👥 朋友清單：上線的沒有排到最上面，而且讀取太慢（商店沒有預先載）
- #539 open — 🔮 EX／已學習技能的**常駐特效**全樹沒有還原 —— 莉娜腳下的魔法陣是活標本
- #540 open — 🧟 揍敵客／拳四郎在戰鬥與英靈殿都拖著一具殘留屍體 —— 機制早就有，那三份 model doc 一格沒填

## ⏸ 卡在 owner 身上的決定

<!-- 一行一個：是什麼 · 選項 · 每個選項的後果。⛔ 不要寫「等回覆」就算了 -->
| 題 | 選項與後果 | 為什麼非 owner 不可 |
|---|---|---|
| **push 授權** | 33+ commit、四閘全綠，⛔ 未 push | owner:「**下次 PUSH 前 讓我確認就好**」·確認頁 https://claude.ai/code/artifact/b0db0efb-c022-4794-9b14-c19f8003bc78 |
| **#533 `intToAbilityPower 4`** | 要不要也折進 AP 係數（`attackDamage`、`strToAttackDamage` 都已折） | 折了倍率才乾淨，⛔ 但這是平衡改動 |
| **HitStop 五個常數** | 門檻 12 · 凍結 2–6 tick · 每 55 傷害 +1 · 暴擊 +2 —— **全寫死在 TS**（違反第一守則） | 要不要做成後台一頁（S 級） |
| **#450 / #539 誰先** | 我建議先做 #450 的 **schema 層**（四段演出）—— #539 就是它的第 ① 段，兩張同一個機制 | 排序是 owner 的權力（第零守則⑧） |
| **#441 兩位缺戰鬥語音** | `godie-e00s` · `godie-ucrl` **都在上架 49 位裡**，⛔ 補它要 repo 外 ~31GB CosyVoice 3 | 一般 session 產不出來 |
| **#108 平台那一半** | rig 已落地（credited=4/4），⛔ 「去主機讀數字」是 owner-only | 禁 ssh 正式站 |

## 🧠 owner 今天的裁決 / 更正

<!-- ⚠️ 逐字引用。這一節是最容易在 compaction 掉的東西，而且掉了會做錯方向 -->
### 架構（最重要的三則）
> 「你的傷害要**從生命反推我沒意見**，但**不能把系統倍率乘進去再反推**啊，這樣我用系統倍率就**沒意義了**」
> 「技能 普攻 生命 五級距公式**可以互相影響帶入公式作為參數**，但是**系統倍率不能放在裡面**」
> 「那為何**所有技能不動態讀取共用同一份公式表**呢？」「那你**只要改公式表 等於全改完**」
> 「不然英雄、技能、道具、特效這些堆積起來會**指數爆炸**」「**再多 token 都改不完**」
⇒ 已落地 #532/#534 + **CLAUDE.md 第〇·四守則**。

### 工作方式
> 「拜託**不要再犯相同錯誤** 不要**重複測試小地方** 請盡量**批次錯** 寫到開發守則」（#500）
> 「我看起來**大部分根本不需要我裁決，你直接做就好了**，你的尺度未免太低了?」
> 「越老的票越要小心考古，**傾向關閉 (<300)**」
> 「請記得把這些**透過讀取JSON到script處理更新到文件、後台設定及codex編輯器**也要更新」
> 「有什麼問題就**記錄在 NOTE 更新對話開票 然後做成開關** 下次PUSH前 讓我確認就好」

### 優先序階梯（他重述了兩次）
> 「我們的決策順序明明就是 **重製新說明 > JASS > w3x技能說明 > w3x技能設定**」

### 平衡
`maxHealth` **6.0** · `attackDamage` **1.0**（0.6 已折進卡片）· `lifesteal.base` **2.0** ·
59-001 完全暴走門檻 **20% → 50%** · 五級距 **200/500/1000/1500/2000**

### #450 的真正意義（⭐ 我先前理解錯了）
> 「這三支**為何作為驗收標準**，是為了建立**特效正確性、動畫正確性、傷害正確性 三大標準檢驗**
>  ⋯製作而成的 **script 來套驗檢驗其他所有的技能**⋯並且讓 **codex 編輯器也能依照合約做出類似的模板**」

### ⛔ 我被更正過的（不要重犯）
1. **EVA01 兩個特效講反** —— 「**粉紫色是 EX，橘黃色才是 AT 力場**，請你把所有圖片**按照編號串起來**判斷」⇒ 編號＋HP 串起來才對。
2. **#450 量錯檔** —— 讀成下架分身的 EX（`godie-h020.ex`/`n01g.ex`），真正出貨是 `godie-hjai.e`/`n003.r`/`hart.r`。
3. **時區** —— transcript 是 UTC、帳本是 GMT+8，直接比會誤報 12 則疏漏。
4. **工作流 args** —— 傳空陣列／欄位名不合 ⇒「completed」但 **0 個 agent**。⭐ 一定要讀 `<result>` 的計數。

## ➡️ 下一步（含排序理由）

1. **等 owner 授權 push** —— 33+ commit、四閘全綠。⛔ 這是唯一的阻塞點。
2. **#450 的 schema 層**（四段演出）⭐ **排第一**，因為：
   · 四支原作截圖讀出來的共同結構是**四段**（常駐/吟唱/發射/命中），
     而 `ability@1` 只有**一個** `vfxKey` ⇒ **schema 表達不了原作的形狀**，那是移植不完整的根因
   · **#539（EX 常駐特效）就是它的第 ① 段** —— 兩張票同一個機制，⛔ 分開做會做兩次
   · 它同時是 **Codex 對外合約**（owner:「讓 codex 編輯器也能依照合約做出類似的模板」）
3. **#540 殘留屍體** —— ⭐ 真正的產出是那條**反向守衛**（帶 gore geoset 卻沒填 `hiddenPrimitives` 要紅），
   ⛔ 不是修那兩位。124 份只有 2 份填了，16/40 帶那種 geoset。
4. **批 2 的 47 張** —— 批 1 已收（97 張 → 關 29 + 15 裁決）。

## ⚠️ 併行工作流的硬規矩（這個 session 踩了四次才寫下來）

⭐ commit 唯一正解，**全程不 stage**：
```
printf '%s\n' "$MSG" > /private/tmp/msg-<lane>.txt
git commit -F /private/tmp/msg-<lane>.txt -- <逐檔列名>
```
⛔ `git add` 與 `git commit` 之間別的 lane 會把你的檔掃走（程式不丟，**票號與突變紀錄會丟**）。
⛔ `git commit --amend` 已入禁令 —— 它動的是 **HEAD**，而 HEAD 在併行時不屬於任何 lane。
⚠️ 並行上限 `min(16, CPU−2)` = **16**：開 100 條只是排隊，⭐ 優化的是**每條的含金量**不是條數。
⚠️ `pnpm skills:sync` / `content:build` 寫 `bundle.json` ⇒ **全域鎖，只有主 session 能跑**。
