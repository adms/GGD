# 2026-08-21

## 逐則對票 · 每一則訊息（`scripts/message-ledger.sh` 維護）

> ⭐ 這張表由 `scripts/message-ledger.sh` 從 session transcript **逐則**撈出來對帳，
> `scripts/ruling.sh` 收到裁決時也寫進同一張表。
> 表格那一格是**截斷**過的；⛔ 全文沒有被壓縮取代，它在 `ledger-source_temp_*.md`。
> 票號那一格 ⛔ 不可以留空：對不到票就寫 `⏸ 未對票`，`--check` 會紅。

| 時間 | owner 說了什麼（逐字） | 票 |
|---|---|---|
| 00:32 | owner 2026-08-21（最後四題全部裁決）： ## A · LV50/LV99 缺口 → **暫時這樣（走 B：先量 AP）** > 「B：先去量 AP 到底貢獻多少，再決定要不要動 => **暫時這樣吧**」 ⇒ ⛔ 不動任何出貨值。先量 AP 在 LV50 的貢獻；若 ≥51% 這題自己消失。 ## B · 137(實為 142)支靠攏的「高/低」界線 → ⭐ **用傷害級距推導，⛔ 不用全庫中位** > 「應該是用**我們算出來的 600/1500/3000/4500/6000 作為基礎來算**吧， > 記得要 **script 自動化**」 ⇒ ⛔ 廢掉「全庫中位 462/500」那個作法（那是一個與級距無關的第四個住處）。 ⭐ 我的解讀（要在報告裡驗證）：**逐支比較它自己的「傷害級距」與「冷卻級距」**—— 傷害級距 < 冷卻級距 ⇒ **低** ⇒ 往短靠；傷害級距 > 冷卻級距 ⇒ **高** ⇒ 往長靠。 ⭐ 這樣界線是**推導的、逐支的**，級距表一改它自己跟著動，⛔ 不是一個全域常數。 ⚠️ 若這個解讀與 owner 的意思不符，要把「差在哪」列出來。 ## C · #446 耗魔怎麼收 → **C：只夾上下限** > 「C 上下限」 ⇒ ⛔ 不建第三張表。現況 220 vs 規格反算 247（差 12%）⇒ 只夾兩端。 ## D · #433 · #464 → **yes, yes** > 「附錄 A => **yes, yes**」 ## ⭐ 以上做完要再更新一次兩份 MD > 「以上做完請再更新一次」 | #447 #445 #446 #433 #464 |
| 00:40 | owner 2026-08-21： > 「**錯誤的母體資料 下架沒使用的英雄 技能 說明 道具 請都整理到 legacy** 好嗎」 ## ⛔ 執行前量到的關鍵矛盾（⚠️ 照錯的那份搬會炸線上） \| 來源 \| 數量 \| \| \|---\|---:\|---\| \| `content/champions/*.json` \| **71** \| ⛔ **我先前用錯的母體**（檔案數，含骨架佔位） \| \| `data/curation/whitelist.json`（本機） \| **46** \| ⛔ **落後 13 隻** \| \| 線上 `/api/v1/curation/whitelist` \| **59** \| ✅ **權威** —— 玩家現在選得到的 \| 線上是本機的**嚴格超集**（本機沒有任何線上沒有的）。 線上多出來的 13 隻：godie-e00s · efur · ewar · h02v · hgam · hjai · nbbc · nsjs · ogrh · ucrl · udre · umal · uvng ⇒ **搬移的權威 = 線上那 59 隻**，⛔ 不是本機 46、⛔ 不是檔案數 71。 ## ⭐ sela / thorne 不在任何白名單裡 它們是 `main.tsx` 內容載入失敗時的**骨架佔位**，⛔ 不是遊戲內容。 Scorch Ring（`sela.e`）排進平衡清單就是因為母體用了檔案數。 ## ⚠️ 木乃香 `godie-etyr` —— 記錄裡查不到退場裁決，而且**它在線上那 59 隻裡** 掃過 `docs/_daily`、既有 issue、transcript：**零命中**。⛔ 不擅自搬，等 owner 一句話。 ## 連帶要重算 600 這個數字來自 71 隻的中位 ⇒ 母體換成 59 之後**必須重算**。 `mana:audit` 的「70/71 隻超過 20 秒」同樣是錯的母體。 | #479 #472 #447 |
| 00:56 | owner 2026-08-21（⭐ 這一則解釋了今天一連串的錯）： > 「**上架不能包含變身態 我們討論過了 之前就是這樣才沒改到正確的英雄技能**」 ## 量到的 線上白名單 **59** 隻裡有 **10 個是變身態** ⇒ **真正的上架本體 = 49**。 變身態（⛔ 不該可選）→ 本體： e007→ewar · h020→hjai · h02r→hgam · h02u→h02v · n00p→nsjs · n01c→nbbc · o00x→ogrh · u00l→umal · u010→uvng · u01u→udre ## ⭐ 拔掉是安全的（已驗證，⛔ 不是推測） `whitelist.allowsChampion()` **只在 `MatchController.selectChampion:1230` 被呼叫一次** ＝ 它只擋「選人」，⛔ **變身時不查它**。⇒ 拔掉 10 個變身態，變身不會壞。 ## ⇒ 三個數字，三個用途，⛔ 今天全部混在一起用 \| 數字 \| 是什麼 \| 誰該用 \| \|---:\|---\|---\| \| **49** \| **可選本體** \| ⭐ 選人畫面 · **平衡母體** · 上架清單 \| \| 59 \| 可選本體 + 變身態 \| ⛔ 今天的白名單（錯的） \| \| 71 \| `content/champions/*.json` 檔案數（含骨架佔位） \| ⛔ 我今天所有量測用的（最錯的） \| ⇒ **600 這個數字要用 49 重算**（它是 71 隻的中位算出來的）。 | #479 #472 #447 |
| 00:57 | owner 2026-08-21（補充，⭐ 這一條適用**所有**量測）： > 「並且我們**查所有屬性級距等 都是不考慮變身態的**」 ⇒ **平衡母體 = 49 位可選本體**，⛔ 一律排除變身態、⛔ 排除骨架佔位（sela/thorne）、⛔ 排除退場。 ⇒ 適用範圍是**全部**，⛔ 不只傷害級距： `anchors:build`（三個空間的中位）· `mana:audit`（滿魔時間）· `lowdmg:build`（傷害偏低）· `hero-archetypes`（屬性定位）· `statCaps`（7 條上限的母體）· `newHeroDefaults`（六欄中位）· 冷卻/耗魔/距離/範圍五級距的一切統計。 ⚠️ 今天量到的數字**全部要重算**（它們用的是 71）： - 傷害五級距 **600**（來自 71 隻 LV30 中位 2,808.6） - `mana:audit` 的「70/71 隻超過 20 秒」 - `statCaps` 七條上限的母體（build.ts 用 74 濾 transform 得 53、再扣退場得 49 —— ⭐ 這一支**剛好已經是對的**，因為它有 `r.group !== "transform"` 那道濾網） | #447 #445 #446 |
| 01:00 | owner 2026-08-21： > 「好 **請你做 A** 之後**重算 全部重新檢查 重新生成** 後更新 **GGD作戰表 對話開票 及 平衡數值決策建議**」 ## A = 保留內容，只從白名單拔掉 ⇒ 10 個變身態的**內容檔一份都不搬**（它們是上架中的變身目標，只是不可選） ⇒ 白名單 59 → **49** ⇒ 代價 **零**：已驗證 `whitelist.allowsChampion()` 只在 `MatchController.selectChampion:1230` 被呼叫一次 ＝ 只擋選人，⛔ 變身時不查它 ## ⛔ 為什麼 B/C 被否決（量到的，⛔ 不是推測） - **B（真的搬進 _legacy）**：變身態自己的技能 **60 份**失效 · 帶 `championForm` 的 **23 支** 技能目標消失 ⇒ **10 位英雄的變身技同時變成空的** · 引用變身態 id 的出貨檔 **80 份**全部斷掉 · 還會踩到 #479 已知的死法（本體還在出貨時單獨搬 ⇒ snapshot builder 拋錯） - **C（連本體一起下架）**：上架從 49 掉到 39 - ⭐ **替代方案不存在**：`applyBuff` 只能疊 modifier，而變身要換**整張英雄卡** （模型/技能組/屬性）—— `championForm.ts` 檔頭已明寫「never a modifier stack on the first」 ## ⭐ 根本上這是一個**分類錯誤** 「不可選」與「退場」被當成同一件事，而它們是兩件事。 ## 然後要做的 ① 全部量測母體改成 **49 位可選本體** ② **600 用 49 重算** ③ 全部重新檢查、重新生成 ④ 更新三份：**GGD 作戰板 · 逐則對票 · 平衡數值決策建議** | #479 #447 #472 |
| 01:17 | 2026-08-21 稽核結果（⛔ 停手，等 owner 裁決）—— 「420 支全轉五級距」的前提是錯的 ## ⛔ 三個否決理由（量到的） ① **`content/abilities/` 是產生器的輸出**：`common.py:52` 逐字「⛔ 不讀工作區，這支產生器自己會 覆寫 content/abilities/」。實跑 `batch1.py` → **90/420 檔被逐位元組重寫**。 ⇒ 寫進去的 tier 會在下一次 `skills:sync` **靜默消失**。 ② 另一條 lane 已經把它做成**機制**（`tierSnap.ts`，註冊時靠攏，內容不動）。 ③ **(a) 刪掉原始值直接出局**：`cooldown` 是 `.min(1)` 必填，而 `enabled:false` 是**止血閥** —— 刪掉之後拉止血閥 = 420 支技能**沒有冷卻**。schema 自己寫「要留特例就不要填級別」 ⇒ **原始欄位就是特例機制**。 ## ⇒ 正解是 (c)：**註冊時靠攏，內容不動** ## 🔴 但 (c) 有一個洞，而且**已經在出貨內容裡發作** 已填 `rangeTier` 的 183 支裡 **19 支** raw 值與級距不符、`radiusTier` 31 支裡 1 支 ⇒ **20/214 = 9.3% 已漂移**，而**沒有任何守衛**。 最嚴重：**`godie-emns.q` 44-01 死神之眼 —— 級距 12、JSON 寫 2、差 6 倍**。 ⭐ 20/20 全部落在那 90 支產生器檔裡，而 `heroes/godie-emns.py:31-39` 的註解**自己承認**： 「卡面說 2、引擎跑 12⋯只改卡面那一行，⛔ 不動 rng=2」（GH#414/#463 已記帳、刻意延後）。 ## 🔴🔴 最重要的一件：**填 damageTier 會廢掉升階回報** `damageTiers.ts` 的規則：填了 `damageTier` 就**交出 `flat` 與 `perRank`**。 而 420 支裡有大量**逐階遞增**的陣列（例 `60-04 完美盾反` manaCost:[120,150,180]）。 ⛔ 級距是**一支技能一格** ⇒ 全面轉換 = **技能升階在傷害／耗魔／冷卻三軸上不再有任何回報**， 只剩 `ratios`/`attrRatios`。 ⚠️ 這不是某幾支的 ±30%，是**整個升階系統的語意改變** ⇒ **必須 owner 裁決**。 ## 覆蓋率真相：⛔ 420/420 做不到也不該做到 cooldownTier 適用 **358**（62 支純被動無冷卻）· manaCostTier **342** · rangeTier **191** （229 支 range=0，其中 209 是 self）· radiusTier **93** · damageTier **204** （216 支沒有可換算的基礎傷害葉：只有 ratios/attrRatios、純輔助、或 flat:1 的載體節點） ## 傷害那一軸的實況 169/204 **全部往上，⛔ 一支都沒往下**。極端：`28-03 分身` 5→600 = **+11900%**。 ⭐ 級距分佈 極小 120 / 小 75 / 中 9 ⇒ **大與極大零支** —— 出貨技能沒有一支撐得到 4500。 ## 順手發現（⛔ 未修） **20 支 ground 技的 `range = 0`** —— 「在指定地點施放」卻只能放腳下。是**作者漏填**不是不適用。 | #447 #445 #463 |
| 01:33 | owner 2026-08-21（⭐ 六個裁決 + 一個質疑）： ## ① 升階：**B 全轉** + ⭐ 一個我漏掉的設計事實 > 「**我們也有升階公式討論過不是嗎 除了冷卻以外 傷害跟耗魔是一起變動的** > 應該是比較接近 **B 全轉，接受升階只剩 ratios 成長**」 ⇒ 選 **B**。⭐ 但要記住那句事實：**傷害與耗魔是一起變動的，冷卻不是** —— 所以耗魔的 perRank 應該跟著傷害走，⛔ 不是各自獨立。 ## ② 44-01 死神之眼 → **A：以級距為準，JSON 填 12** > 「**這個我們討論過了 請你把 json 填 12** 你是不是**沒用對話開票 記錄跟解**」 ⇒ ⛔ 我沒有查記錄就把它當成「未決的債」報上去。那 20 支全部照 A（級距為準改 JSON）。 ## ③ 冷卻兩端 → **A：都夾** > 「[v] A 都夾」 ⇒ 24 支超上限 + 27 支低於下限，全部夾。 ## ④ 耗魔：**免費技就不要調** > 「manaCostTier 342 / 78 支免費技 => **那就不要調耗魔阿**」 ⇒ 78 支 manaCost=0 的維持 0，⛔ 不要為了「覆蓋率」硬塞級距。 ## ⑤ ⛔ 我的被動分類錯了 > 「cooldownTier 358 / 62 支純被動，沒有冷卻這件事 => **不對 有些被動是有冷卻的 > 例如 初號機吞噬**」 ⇒ **重新判定**：被動**可以**有冷卻（內部冷卻）。62 這個數字要重算。 ⚠️ 初號機吞噬 = `godie-e00r.q`（59-01），owner 2026-08-19 已裁決改成被動且 「內部冷卻跟主動一樣就好」。 ## ⑥ 距離／範圍：⭐ **要查舊時代的幾百範圍，甚至查 JASS** > 「rangeTier 191 / 229 支 range=0 · radiusTier 93 / 327 支不是 ground > => **請你實際判斷說明是否有包含舊時代的幾百範圍來轉換 甚至查 jass**」 ⇒ ⛔ 不可以把 `range=0` 一律當成「不適用」。要查原作是不是有幾百單位的射程 而在移植時掉成 0。**查 JASS**（第〇·六守則第 3 層：程式不會說謊）。 ## ⑦ 非主動傷害技 → **免魔力** > 「damageTier 204 / 216 支沒有可換算的基礎傷害葉 => **若不是主動傷害技能 就免魔力吧 乾脆點**」 ⇒ 那 216 支裡**不是主動傷害技**的，`manaCost` 直接設 0。 ## ⏸ 他的質疑（要回答） > 「上限 maxMana / manaRegen / armor 348050 / 3276 / 8160 → 345050/3271/7939 > **這什麼鬼阿 太誇張了吧 你沒算錯嗎**」 | #447 #445 #463 #414 #485 |
| 01:48 | owner 2026-08-21： > 屬性上限 => 「**[v] A 維持現狀**（餘裕 17–69× 亂跳，但擋得住打錯）」 ⇒ ⛔ 不動七條上限。它們是**防打錯的柵欄**不是平衡數值， 餘裕不一致（16.9×–68.7×）是「×200」這個倍數本身沒有理由造成的， 但 owner 選擇接受 —— 擋得住打錯就夠了。 ⇒ ⛔ 以後不要再提議改這七條，除非有內容真的撞到上限。 | #485 #447 |
| 00:35 | 14-04 聖夜降臨 14-00 召喚式神 的木乃香 應該整支都下架移到 legacy 了吧 為何又出現在 平衡數值決策建議？Scorch Ring | #485 #472 |
| 00:37 | 我們應該討論過上架英雄還剩哪些吧 你查就知道 | #485 |
| 00:54 | 你列給我看目前還上架的角色有哪些 | #485 |
| 01:50 | 請你更新 GGD作戰版 跟 平衡數值決策建議 ，我已經被搞倒好亂了，我想看到乾淨的五級距、公式及範例，另外我們有提到其他一堆票要更新，你做了哪些 哪些做到一半 哪些還沒做 都要更新上去 別忘了 對話開票 | #484 #447 |
| 02:17 | owner 2026-08-21： > 「**我們架構已經改過了 你要同步開最大限度工作流來檢查跟更新** 應該沒問題吧」 ⇒ ① **核准開最大限度平行工作流**做檢查與更新 ⇒ ② ⭐ 他點出的架構事實**改變了風險判斷**： 靠攏發生在**註冊時**（`tierSnap.ts`），內容不動，而且有 `enabled` 開關 ⇒ 套用級距是**一個開關就能回頭**的動作，⛔ 不是不可逆的全域重平衡。 ⇒ 所以 A/B/C 那一題**不該擋住工作** —— 照第〇·六守則做成開關， 預設走「量到的最不傷害現況」那一邊，owner 事後一鍵切換。 ⚠️ 但 76 支全部不達相稱下限這件事仍要如實報告（可能是**級距訂太高**而不是 76 支都錯）。 | 447 445 414 |
