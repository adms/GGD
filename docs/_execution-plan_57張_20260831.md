# 57 張票執行計畫（2026-08-31）—— ⭐ Codex 編輯器獨立作業最優先

> owner 2026-08-31 逐字：
> 「我要你**做完目前 緊急 重要 優先的票**，並且**能讓 codex 編輯器獨立作業為最優先**」
>
> 目標（owner 的兩句話，⭐ 這是排序的唯一依據）：
> 「**開放讓玩家自己設計 英雄、技能、特效**，⛔ 不是靠 AI 無止境的逼近太沒效率」
> 「所以**後台編輯器的抽象化、完整性、視覺化可操作性很重要**，
>  因為**所有功能都要可 JSON 操作設定**，並且也有 **no code 遊戲引擎等級的操作介面**」

---

## 0. ⭐ 排序的判準（⛔ 不是優先級 tag）

⚠️ ⭐ owner 說「Codex 獨立作業最優先」⇒ **排序的第一問不是 tag，是**：

> ⭐ **這張票不做，Codex 會不會卡？**

| 答案 | 波次 |
|---|---|
| ⭐ **會卡，而且 Codex 做不了**（機制／schema／契約在 main） | **W1** |
| ⭐ **會讓他做出來的東西是死的**（管道不通） | **W2** |
| ⛔ 不卡他 | W3–W5 |

⇒ ⭐ 一張 `[重要]` 若擋住 Codex，它排在 `[緊急]` 前面。⛔ 反之亦然。

---

## W0 · 前提回驗（⭐ **先做，⛔ 不可跳過**）

⚠️ ⭐ **已證實兩張緊急票的程式早就做完了**：

| 票 | 證據 | 動作 |
|---|---|---|
| `#842` 動畫消失 | `VfxSystem.ts:1110` 碼表歸零已落地；**3 支守衛 8 條全綠**（`refireClockRollback` · `refireSecondPerformanceRenders` · `hardCapRefire`） | ⭐ **關票** |
| `#721` 光束錯誤 | `tpl-beam-roll` `count=1` `path=static`，`origin` 指得到 `j:26790` 等八個行號 | ⭐ **關票或只補驗收** |

⇒ ⭐ **57 張裡有多少是這種，今天沒有人知道。** W0 就是去知道。

**做法**：逐票問**一個**問題 —— 「票文主張的**現況**今天還成立嗎？」
⛔ 不是「做完了沒」（那一軸 2026-08-27 量過，17 張讀碼三次，**0 張可關**）。

**⭐ 產出**：一張「真正還沒做的」清單 ＋ 一批當場關掉的票。

---

## W1 · ⭐⭐ 解鎖 Codex（**最高優先，⛔ 他做不了這些**）

⭐ 判準：這些全是 **main 的 schema／機制**，Codex 在他的分支上碰不到。

| 票 | 擋住什麼 | 為什麼是 main 的 |
|---|---|---|
| ⭐ **新** `defenseSuccess` 觸發器 | **理想鄉EX**（三招之一，由反彈成功觸發） | ⛔ 不在 `vfxScript.ts` 的 `on` 列舉裡 |
| ⭐ **新** M1 逐刀瞬移 · M3 升空曲線 · M4 逐段加速 | **超究武神霸斬** | `clipWindowMs` 是固定值，⛔ 表達不了逐段加速 |
| ~~**#809**~~ ✅ **已關**（AC 三條逐條驗過）| `spawnVfx at:"bone"` **只錨定施法者**，原作另有 **124 次**掛在受擊者 | ⭐ sim 機制 |
| ~~**#761**~~ ✅ `0c259fe35` | 「一次施法有幾個發射器」住在**渲染層硬寫死表**；`ability@1` 無附著模型／飛行物／閃電光束三圖層 | ⭐ schema |
| ~~**#650**~~ ✅ `51e55d0eb`+`ee933030d` | BlockGrant 缺一道特效軸 | ⭐ 與 `defenseSuccess` **同一個 emit 站設計** |
| **#841** 🔄 `7c9db754a`（Scope④ 要重寫：重跑會掉隊伍色）| mdx→glb filter-mode 只有 4/7 條分支 ⇒ 56 個 MASK 切不掉 | ⭐ 轉檔器 |
| ~~**#803**~~ ✅ `d35b6fb9c`+`6d7f78ab7` | 光束砲貼圖 4/5 張無彩度 ⇒ 顏色住在沒有顏色的地方 | ⭐ 資產 |
| ~~**#771**~~ ✅ **已關** | 4 支產生器宣告零產物 ⇒ `genrun.sh` 解鎖不了 ⇒ **改產物被擋、改來源沒來源**的死路 | ⭐ 基建 |
| ~~**#879**~~ ✅ **已關** | `genrun skillremake:json` 在乾淨 HEAD 上就重算 ⇒ **一條永遠不會綠的閘** | ⭐ 基建 |
| ~~**#768**~~ ✅ **已關** | `beamAudition` 量尺沒自證（滿版亮幀回 0） | ⭐ **驗收工具** —— Codex 要用它交驗收圖 |
| ~~**#715**~~ ✅ **已關** | audition 台子三個盲區沒守衛 | 同上 |
| ~~**#686**~~ ✅ **已關** | 開票規格 catalog 機讀化 ＋ Files→柵欄推導 | ⭐ 讓 Codex 開票時**寫得出前置** |

⭐ **W1 完成的定義**：Codex 拿得到「三招都寫得出來」的機制 ＋ 一把會自證的量尺。

---

## W2 · ⭐ 管道（⛔ 沒有它，編輯器做出來的東西是死的）

| 票 | 內容 |
|---|---|
| ~~⭐ **#736**~~ 🔄 `acc70563c`（段②③已驗，段①＝#730）| admin 編 → server 熱載 → client 看得到。⭐ 三個接線點票裡逐行指名：`contentBus.ts:94` 的 `CONTENT_KINDS` 加一個字串 · client 合成 overlay · admin 走 `putOverlayDoc` |
| **#730** 🔄 `40562597a` | 生產頁 PUT 前先驗（`validateOverlayDoc` 在此之前**零呼叫端**，而 `shared/overlay.ts` 對外承諾過會驗）。⛔ 9 頁仍在 DEV chunk |
| ~~**#820**~~ ✅ **已關**（複驗發現）| 後台 14 頁「對照表」做完了，⛔ 而 **13/14 頁沒有寫入端** |
| ~~**#775**~~ ✅ **已關**（複驗發現）| 後台 13 頁對照·視覺化·設定（⭐ 全部 script 實時產生） |
| **#244** | JASS 技能模板計畫 → 模板總類表 → UI 編輯器 |
| **#473** 🔄 `6dff1f120` | 啟用當下真的跑稽核 —— ⭐ **三態**：跑不到 ≠ 通過 |
| **#664** 🔄 `26ec72ab6` | HITL 流水線 ＋ ⭐ **第二軸**：渲染層漂移也讓核准過期 |
| **#327** 🔄 `c9250791a`+`8a00f6879` | tokenizer（允許清單在**形狀**）· checksum 政策 · 未知欄位訊號 |

⭐ **W2 完成的定義**：⛔ 玩家做的東西**進得了遊戲**。

---

## W3 · 內容批（⭐ 可大量平行，⛔ 互不相干）

`#882` 卡面說謊 · `#840` 天譴 dash · `#648` 43 支迴圈宣稱 · `#401` 6 支沿線推進 ·
`#547` 投射物/衝擊波移植 · `#751` Asph 球體 11 筆 · `#623` 變身態逐對退場 ·
`#423` 樹精召喚 · `#817` fist/claw 解鎖 18 隻 · `#443` 版權解禁後的資產優先序

---

## W4 · 語音與回饋

`#441` 語音普查 · `#743` T3 狀態語音 · `#731` T4 comms/ping · `#734` 右鍵攻擊回饋 ·
`#756` 語音分離度漏斗 · `#725` 戰鬥視覺四項 · `#722` 友軍指定技能四條死路徑 · `#330` 技能點回饋

---

## W5 · 系統與玩法

`#884` 部署韌性 · `#865`/`#862` 搬遷收尾（⭐ **09-05 前**）· `#813` CSP/token ·
`#629` 1000 隻殭屍 · `#382` VRAM · `#626` `configForms.ts` 拆檔 ·
`#752` mini dota · `#737` 回合分數 · `#502` 手把全流程 · `#863` 手把 v4 ·
`#448` 30-00 攝影機 · `#747` 節拍兩軸 · `#873`/`#800` 觸控 HUD（⚠️ owner：**手機低優先**）·
`#878` bot 守衛前提消失 · `#869` 批次盤

---

## ⛔ 不在這份計畫裡

| 票 | 為什麼 |
|---|---|
| **#838** 特效工坊 | ⭐ owner 2026-08-31：「**給 codex 編輯器做**，你到時候參考就好」 |
| `#861` 搬遷主票 | ⭐ 已完成（v0.33.0 在 mini 上），留著追觀察期 |

---

## ⭐ 平行柵欄（⛔ 逐檔列名，不是目錄）

| lane | 領地 | ⛔ 不碰 |
|---|---|---|
| **A** | `apps/admin/src/**` | client · sim · tools |
| **B** | `packages/shared/src/content/schema/**` · `apps/client/src/vfx/**` | admin · tools |
| **C** | `tools/**` · `content/**` | admin · client |
| **D** | `apps/game-server/**` · `apps/platform/**` | admin · client |
| **E** | `apps/client/src/ui/**` · `render/**` | vfx · admin |

⚠️ ⭐ **共用檔只有兩個**（CLAUDE.md 逐字）：`apps/admin/src/store.ts` 與 `ui/App.tsx` **各一行**
⇒ ⭐ 由主 session 最後統一加，⛔ lane 不碰。

⚠️ `pnpm skills:sync` 是**全域鎖** ⇒ ⛔ 平行時全部禁跑，主 session 最後統一跑一次。

---

## ⭐ 每一批的收工條件（⛔ 不是「commit 了」）

1. ⭐ **留後台開關可簡易 rollback**（三個住處：`content/config/*.json` ＋ Zod `DEFAULT_*` ＋ admin `SHIPPED_*`）
2. ⭐ **一條承重守衛 ＋ 一次突變驗證**（⛔ 不是每個分支各一條）
3. ⭐ 玩家看得到的功能：⛔ 沒有終端證據之前只准說「**鏈路已接上，⛔ 未驗收**」
4. ⭐ commit 一律 `git commit -F msg -- <逐檔 pathspec>`（⛔ 不 stage、⛔ 不 `--amend`）
5. ⭐ 收工跑 `noStrandedLaneCommits.test.ts`（⛔ 別讓 commit 擱在 worktree 分支上）

---

## ⭐⭐ 這份計畫怎麼**撐過 context 重置**（owner：「理論上是可以自動跑三天以上的工作量」）

⚠️ ⭐ 三天 ＝ **會經過好幾次 context 重置**。⇒ ⛔ 進度**不可以活在對話裡** ——
⭐ 而 CLAUDE.md 已經記過這個病：「一項工作被排進未來的版次，實際發生的事情是**它消失了**」。

### ⭐ 三個住處，缺一個就會重做已經做好的

| 住處 | 記什麼 | 指令 |
|---|---|---|
| **① GitHub issue** | ⭐ 每一票的**進度標記**（做了什麼 · commit sha · 玩家那一句） | `bash scripts/ticket-progress.sh write <票號> … --commit <sha> --player "<一句>"` |
| **② 本檔** | ⭐ 波次層級的勾選（見下方進度表） | 直接改這一份 |
| **③ 當日帳本** | owner 的裁決（⭐ 逐字，⛔ 不改寫） | `bash scripts/ruling.sh <票號>` |

⚠️ ⭐ **只寫一個地方等於沒寫**：只寫票 ⇒ 下一輪不會去翻 57 張；
只寫本檔 ⇒ GitHub 上看不到；只寫帳本 ⇒ 找不到對應的票。

### ⭐ 每一次接手（context 重置後）的**四行**

```bash
sed -n '/## 📊 進度/,/^---/p' docs/_execution-plan_57張_20260831.md   # ① 走到哪一波
gh issue list --state open --limit 400 --json number,title | head    # ② 還剩幾張
npx vitest run packages/shared/src/ops/noStrandedLaneCommits.test.ts # ③ ⭐ 有沒有 commit 擱在 worktree 分支
git log --oneline -12                                                # ④ 上一輪做了什麼
```

⛔ **不要**重新盤點 57 張票 —— ⭐ 那是 W0 的產出，它寫在下面的進度表裡。

### ⭐ context 到 95% 就存快照（⛔ 硬上限 98%）

```bash
bash scripts/memory-temp.sh     # ⚠️ 三節「（待填）」一定要當場補完,留著等於沒存
```

⭐ 掉的**不是程式碼**（那在工作樹裡）—— 掉的是**只存在對話裡的那一半**：
owner 的裁決 · 我撤回過哪個錯誤結論 · 兩個工作流為什麼不能同時跑 · 哪一個數字沒有來源。

### ⛔ 三天期間**每天**要做的

| 何時 | 做什麼 |
|---|---|
| 每天開工 | `bash scripts/board-roll.sh`（戰情表輪替＋七天窗） |
| 每天收工 | ⭐ **一天一次發版**（`bash scripts/release.sh --tag "說明"`）—— ⛔ 不要一天發五次 |
| 撞紅燈時 | ⭐ **批次撈**：三個閘一次跑完寫三個 log 再一起讀，⛔ 不是修一個跑一次 |
| 同一個閘紅第三次 | ⛔ **停** —— 那是迴圈不是壞運氣，找結構性根因 |
| 任何指令 > 5 分鐘沒新輸出 | `ps` 看 CPU%。⭐ **0% ＝ 掛了** ⇒ kill 重跑 |

---

## 📊 進度（⭐ 每完成一項就當場更新這一格，⛔ 不是最後補）

| 波 | 範圍 | 狀態 | 備註 |
|---|---|---|---|
| **W0** | 前提回驗 57 張 | ✅ **完成** | ⭐ 五條 lane 驗完 **54 張** · **~20 張前提失效（37%）** · 已關 **15 張** |
| **W1** | 解鎖 Codex（12 項） | 🔄 **進行中** | ⭐ **12 項裡 8 項已了**（5 張複驗發現**早就關了**：#771 #879 #768 #715 #686）。⛔ 剩 `#809` `#841` |
| **W2** | 管道（8 張） | 🔄 **進行中** | `#736` `acc70563c` · `#730` `40562597a` · `#664` 第二軸 `26ec72ab6` |
| **W3** | 內容批（10 張） | ⬜ 未開始 | 可大量平行 |
| **W4** | 語音與回饋（8 張） | ⬜ 未開始 | |
| **W5** | 系統與玩法（17 張） | 🔄 **進行中** | `#862` log 上限已關（`688ae853a`＋`ab55e1785`）· ⚠️ `#865` 仍 **09-05 前** |

⭐ 狀態符號：⬜ 未開始 · 🔄 進行中 · ✅ 完成 · ⛔ 卡住（**要寫卡在哪**）

---

## 📋 W0 的產出（2026-08-31）—— ⭐ **這是接手時要讀的那一張**

### ⭐ 已關（15 張）

| 票 | 為什麼可以關 |
|---|---|
| **#842** 動畫消失 | 碼表歸零已落地，3 支守衛 **8 條全綠** |
| **#721** 光束形狀 | 長寬比 2.28–3.83 → **10.23–53.69**；家族 9 節點對齊 **0.2%** 內。⛔ 顏色**沒修** ⇒ 移交 #803 |
| **#881** 忍者穿和服 | 詞彙分署落地（`config.voxel-look@1`），9 隻全部脫下和服 |
| **#739** 體素條碼 | owner：「體素計畫已經全面暫停了」 |
| **#768** 量尺沒自證 | `makeCertifiedMeasure` 已接，**12 條綠**，三條 AC 都有斷言 |
| **#715** audition 盲區 | 「四道縫少任何一道 ⇒ 紅」已落地 |
| **#873** 觸控攻擊鈕 | `TOUCH_PLATE_KNOWN` **空了**，24 條綠 |
| **#800** 觸控 HUD 重疊 | 同上 |
| **#817** fist/claw | `NEEDS_CLASS={}` `BLOCKED={}` ⇒ **18 → 0** |
| **#771** 產生器零產物 | 40 步裡零產物剩 **3 支且全部合法** |
| **#879** castTimeSec 重算 | 收斂迴圈 50→4→**0**，parity **5 條綠** |
| **#861** 搬到 Mac mini | ⭐ 已完成，v0.33.0 今天部署驗過 |
| **#502** 手把全流程 | 子票 **closed 25 / open 1**（只剩自己） |
| **#882** 卡面說謊 | ⚠️ ⭐ **我今天開的，而它開的時候就過期了**（見下） |
| **#722** 友軍指定技能 | 四條路徑齊，**8 條綠**；⭐ 順帶更正「5 支」→ **4 支** |

### ⚠️ ⭐ W0 抓到的**三個方法論教訓**

| # | |
|---|---|
| **①** | ⭐ **開票前要重跑那一條閘** —— 我拿 `skills:sync` **之前**的紅燈開了 #882，而同一輪產生器已經修好它。⇒ 帳本已記（`2-守衛 / 憑印象`，第 155 筆） |
| **②** | ⭐ **票文的數字會漂**：#648 43→**37** · #722 5→**4** · #547 80/120→**108/126** · #664 322→**340** · #626 5,665→**243** 行 |
| **③** | ⛔⛔ **這一條我寫錯了 —— 見下方更正** |

### ⛔⛔ W0 的③被推翻了（2026-08-31，`#648` lane 用 commit hash 查證）

我在 W0 寫：
> ⭐⭐ 「掉出清單」⛔ 不等於「修好了」—— 掉出的 6 支**全部 `template: null`**，
> 它們是**宣稱那一側的文案變了**才不再算差集，⛔ 不是被實作。

⛔ **那是錯的。** 逐支查 commit：

| 技能 | 真相 |
|---|---|
| `e00r.q`（59-01 吞噬，⭐ **正是我點名的先導樣本**）· `e00r.e` | ⭐ **真的被實作了** —— `cd921c0eb`（#644 owner 裁決） |
| `emfr.passive` | ⭐ 實作了 —— `3a7fd4867` |
| `huth.r` | ⭐ 實作了 —— `d4f631012` |
| `o02p.r` | ⭐ 實作了 —— `f63cf3263` |
| `e007.r` | ⭐ 卡面**從來沒有**週期宣稱 —— 誤報退場 |

⭐ **我的假前提從哪來**：`effects: []` 被我讀成「空技能」——
⚠️ ⛔ 而對**被動技**那是正常的：機制住在 `passive.ranks[].hooks[].on = "onInterval"`。

⇒ ⭐ **教訓**：`template: null` ⛔ 不等於「沒有機制」。
⚠️ 而我當時是拿它當「證據」寫進 W0 的結論 —— ⭐ 那正是「**我的推測會變成需求**」的形狀。

### 🔄 要改寫票文的（⭐ 前提部分失效，⛔ 不可直接關）

| 票 | 改成什麼 |
|---|---|
| **#736** | ⭐ ①②仍成立（`contentBus.ts:94` 仍 3 項無 `content-overlay`）；⛔ ③已失效（`clientOverlay.ts` 存在） |
| **#730** | 仍成立，行號 505/543 → **610/648**，`CONTENT_ROUTES` 8→**9** |
| **#820** | ⭐ **近乎反轉**：今天 **11 個** dataset 有 `write`，只剩 3 個 `readonlyWhy` |
| **#775** | 只剩 AC3 未達（`SEC_SYS` 仍 **30** > 20） |
| **#756** | 段③已落地（`tools/review/voice.mjs` 152 行）；段①②仍成立 |
| **#686** | catalog 已存在（8,282B）＋ `lane-plan.sh`（19,662B）⇒ 縮成殘留 |
| **#841** | ⭐ **7 條分支全在**（`gltf.py:308`）；只剩 ③ 的雙向 sentinel 閘 |
| **#809** | ⭐ 機制已落地（`boneOn` enum）；⛔ 內容只翻 **1 支**（`godie-hart.r`）⇒ 改成「內容批 8 支」 |
| **#547** | 分母長大：`fx.w3x` vfx doc **126** 份 · 技能只引用 18 ⇒ **108 沒人用** |
| **#737** | server 已上線路；⛔ **client 0 命中** ⇒ 縮成純 client/HUD |
| **#626** | ⭐ `configForms.ts` **5,665 → 243 行**（已拆 17 檔）；只剩 `EXEMPTIONS` **3,020 行** |
| **#443** | ⭐ **阻塞理由換了**：版權已全面解禁，真阻塞是**位元組沒抽取** |
| **#747** | 軸(b) 已落地；只剩軸(a)（mob 無 StatsComp） |
| **#664** | Phase1 失效（`tools/review/` 今 **16 檔**、queue **340**）；Phase2 仍成立（pHash **0 命中**、`approvals.json` 空） |
| **#473** | `enable-audit.mjs` 已存在；⛔ `apps/admin/` 對 `auditPlan` **0 命中** ⇒ 只剩接線 |
| **#330** | AC1 已做（`UnlearnedMark`）；AC3 **三住處零命中** |
| **#725** | 縮成 **#38 #39** 兩項（#41 #119 #43 #44 全已落地） |
| **#743** | ⭐ **消費端存在但消費不到的活標本**：`statusVoiceEdges.ts` 是完整模組**只有自己的測試 import 它** |
| **#731** | ④已由 #441 那批做完 ⇒ 從 Scope 刪掉 |
| **#441** | 缺陷①②③全已修，只剩「2 位補戰鬥語音 + **9** 類無觸發點」 |
| **#884** | ⭐ **環境錯了**：出貨走 `mini-deploy.sh`（`git fetch` + `checkout -f`），⛔ 不是 `git pull` ⇒ 碰撞形狀不同 |
| **#382** | 13 筆 scene（不是 6）；`cef3156af` 逐字說「票上自己提的解法一筆都修不到」 |
| **#869** | 依最新裁決重寫（Electron 已劃給 Codex） |

### ⛔⛔ W0 找到的**新風險**（⭐ 不在任何票上）

> **`#862` 的 log 上限那個洞，在 Mac mini 上是重新打開的。**
> `host-migrate.sh` 15 處 systemd / **0 處 macOS** ⇒ Linux 專用；
> `compose.family.yaml` 的 `logging:` = **0 個**；mac 兩支腳本 **0 處 `max-size`**。
> ⇒ ⭐ 三個「已關」的洞裡至少 log 上限那一個，**在今天的出貨機上沒關**。

---


---

## ⭐ 收尾

**BMPNDD** —— 部署走 `bash scripts/mini-deploy.sh deploy`（⛔ **不是** `host-deploy.sh`，那是 GCP 回滾機）。

---

## 📌 2026-08-31 本輪的結論（⭐ 接手先讀這三條）

### ① `#862` —— 一個「已關」的洞在**另一台機器上**是開的
關它的 `host-migrate.sh` 是 **15 處 systemd / 0 處 macOS** ⇒ Linux 專用。
⇒ ⭐ 修在 **compose**（跟著 repo 走），⛔ 不在 daemon.json（跟著機器走）。
⭐ 並補了**配對式**後置條件：讀跑著的容器的 `HostConfig.LogConfig`，
⛔ 不是讀 compose 檔 —— 因為 `logging:` 只在**建立容器那一刻**生效。

### ② `#803` —— 票文的前提失效，⭐ 而更正之後**要走完它原本要走的那條路**
票文寫「PRE2 沒轉進來」。實際：**轉了**，卡的是一格 `family: null`。
⭐ 金色量到 **sat 0.541**（票文說原作 0.47–0.54，逐位命中）。
⭐ 家族由 **owner 自己的點名理由**指定（「復活家族的金色版」…），⛔ 不是我挑的。

⚠️ 而**只推翻不落地是不夠的**（CLAUDE.md 記過的第三個載體）——
所以同一輪做完了第二半：`model@1.fxEmitters`，因為
⭐ **原作的一顆 .mdx 是「geoset ＋ 它自己的 PRE2」，而轉檔器只轉 geoset**。

### ③ ⚠️ 同一張表有**三個住處**（`w3xArtFamilies.ts` · `build_model_usage.py` · 普查產物）
⭐ drift 閘**已經存在**並當場紅、指名三族 —— ⛔ 不必新寫。
⚠️ 但它只擋得住「TS 抄錯」，⛔ 擋不住「兩份手寫的一起忘了改」。
⇒ 下一輪值得讓 py 那一份**從 TS 推導**（第〇·四守則）。

