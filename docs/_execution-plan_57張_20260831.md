# 57 張票執行計畫（2026-08-31）—— ⭐ Codex 編輯器獨立作業最優先

> owner 2026-08-31 逐字：
> 「我要你**做完目前 緊急 重要 優先的票**，並且**能讓 codex 編輯器獨立作業為最優先**」
>
> 目標（owner 的兩句話，⭐ 這是排序的唯一依據）：
> 「**開放讓玩家自己設計 英雄、技能、特效**，⛔ 不是靠 AI 無止境的逼近太沒效率」
> 「所以**後台編輯器的抽象化、完整性、視覺化可操作性很重要**，
>  因為**所有功能都要可 JSON 操作設定**，並且也有 **no code 遊戲引擎等級的操作介面**」

---

## 0. ⭐ 排序的判準（⛔ 不是優先級 tag）

⚠️ ⭐ owner 說「Codex 獨立作業最優先」⇒ **排序的第一問不是 tag，是**：

> ⭐ **這張票不做，Codex 會不會卡？**

| 答案 | 波次 |
|---|---|
| ⭐ **會卡，而且 Codex 做不了**（機制／schema／契約在 main） | **W1** |
| ⭐ **會讓他做出來的東西是死的**（管道不通） | **W2** |
| ⛔ 不卡他 | W3–W5 |

⇒ ⭐ 一張 `[重要]` 若擋住 Codex，它排在 `[緊急]` 前面。⛔ 反之亦然。

---

## W0 · 前提回驗（⭐ **先做，⛔ 不可跳過**）

⚠️ ⭐ **已證實兩張緊急票的程式早就做完了**：

| 票 | 證據 | 動作 |
|---|---|---|
| `#842` 動畫消失 | `VfxSystem.ts:1110` 碼表歸零已落地；**3 支守衛 8 條全綠**（`refireClockRollback` · `refireSecondPerformanceRenders` · `hardCapRefire`） | ⭐ **關票** |
| `#721` 光束錯誤 | `tpl-beam-roll` `count=1` `path=static`，`origin` 指得到 `j:26790` 等八個行號 | ⭐ **關票或只補驗收** |

⇒ ⭐ **57 張裡有多少是這種，今天沒有人知道。** W0 就是去知道。

**做法**：逐票問**一個**問題 —— 「票文主張的**現況**今天還成立嗎？」
⛔ 不是「做完了沒」（那一軸 2026-08-27 量過，17 張讀碼三次，**0 張可關**）。

**⭐ 產出**：一張「真正還沒做的」清單 ＋ 一批當場關掉的票。

---

## W1 · ⭐⭐ 解鎖 Codex（**最高優先，⛔ 他做不了這些**）

⭐ 判準：這些全是 **main 的 schema／機制**，Codex 在他的分支上碰不到。

| 票 | 擋住什麼 | 為什麼是 main 的 |
|---|---|---|
| ⭐ **新** `defenseSuccess` 觸發器 | **理想鄉EX**（三招之一，由反彈成功觸發） | ⛔ 不在 `vfxScript.ts` 的 `on` 列舉裡 |
| ⭐ **新** M1 逐刀瞬移 · M3 升空曲線 · M4 逐段加速 | **超究武神霸斬** | `clipWindowMs` 是固定值，⛔ 表達不了逐段加速 |
| **#809** | `spawnVfx at:"bone"` **只錨定施法者**，原作另有 **124 次**掛在受擊者 | ⭐ sim 機制 |
| **#761** | 「一次施法有幾個發射器」住在**渲染層硬寫死表**；`ability@1` 無附著模型／飛行物／閃電光束三圖層 | ⭐ schema |
| **#650** | BlockGrant 缺一道特效軸 | ⭐ 與 `defenseSuccess` **同一個 emit 站設計** |
| **#841** | mdx→glb filter-mode 只有 4/7 條分支 ⇒ 56 個 MASK 切不掉 | ⭐ 轉檔器 |
| **#803** | 光束砲貼圖 4/5 張無彩度 ⇒ 顏色住在沒有顏色的地方 | ⭐ 資產 |
| **#771** | 4 支產生器宣告零產物 ⇒ `genrun.sh` 解鎖不了 ⇒ **改產物被擋、改來源沒來源**的死路 | ⭐ 基建 |
| **#879** | `genrun skillremake:json` 在乾淨 HEAD 上就重算 ⇒ **一條永遠不會綠的閘** | ⭐ 基建 |
| **#768** | `beamAudition` 量尺沒自證（滿版亮幀回 0） | ⭐ **驗收工具** —— Codex 要用它交驗收圖 |
| **#715** | audition 台子三個盲區沒守衛 | 同上 |
| **#686** | 開票規格 catalog 機讀化 ＋ Files→柵欄推導 | ⭐ 讓 Codex 開票時**寫得出前置** |

⭐ **W1 完成的定義**：Codex 拿得到「三招都寫得出來」的機制 ＋ 一把會自證的量尺。

---

## W2 · ⭐ 管道（⛔ 沒有它，編輯器做出來的東西是死的）

| 票 | 內容 |
|---|---|
| ⭐ **#736** | admin 編 → server 熱載 → client 看得到。⭐ 三個接線點票裡逐行指名：`contentBus.ts:94` 的 `CONTENT_KINDS` 加一個字串 · client 合成 overlay · admin 走 `putOverlayDoc` |
| **#730** | 9 個內容編輯頁搬上正式 build（⛔ **不是拔 DEV 閘**） |
| **#820** | 後台 14 頁「對照表」做完了，⛔ 而 **13/14 頁沒有寫入端** |
| **#775** | 後台 13 頁對照·視覺化·設定（⭐ 全部 script 實時產生） |
| **#244** | JASS 技能模板計畫 → 模板總類表 → UI 編輯器 |
| **#473** | 內容「啟用上架」的當下自動跑稽核 |
| **#664** | 非結構化資產驗收流水線 Tier0-2 ＋ HITL 批次審查頁 |
| **#327** | 匯入包合法性：strict-by-default · tokenizer · checksum |

⭐ **W2 完成的定義**：⛔ 玩家做的東西**進得了遊戲**。

---

## W3 · 內容批（⭐ 可大量平行，⛔ 互不相干）

`#882` 卡面說謊 · `#840` 天譴 dash · `#648` 43 支迴圈宣稱 · `#401` 6 支沿線推進 ·
`#547` 投射物/衝擊波移植 · `#751` Asph 球體 11 筆 · `#623` 變身態逐對退場 ·
`#423` 樹精召喚 · `#817` fist/claw 解鎖 18 隻 · `#443` 版權解禁後的資產優先序

---

## W4 · 語音與回饋

`#441` 語音普查 · `#743` T3 狀態語音 · `#731` T4 comms/ping · `#734` 右鍵攻擊回饋 ·
`#756` 語音分離度漏斗 · `#725` 戰鬥視覺四項 · `#722` 友軍指定技能四條死路徑 · `#330` 技能點回饋

---

## W5 · 系統與玩法

`#884` 部署韌性 · `#865`/`#862` 搬遷收尾（⭐ **09-05 前**）· `#813` CSP/token ·
`#629` 1000 隻殭屍 · `#382` VRAM · `#626` `configForms.ts` 拆檔 ·
`#752` mini dota · `#737` 回合分數 · `#502` 手把全流程 · `#863` 手把 v4 ·
`#448` 30-00 攝影機 · `#747` 節拍兩軸 · `#873`/`#800` 觸控 HUD（⚠️ owner：**手機低優先**）·
`#878` bot 守衛前提消失 · `#869` 批次盤

---

## ⛔ 不在這份計畫裡

| 票 | 為什麼 |
|---|---|
| **#838** 特效工坊 | ⭐ owner 2026-08-31：「**給 codex 編輯器做**，你到時候參考就好」 |
| `#861` 搬遷主票 | ⭐ 已完成（v0.33.0 在 mini 上），留著追觀察期 |

---

## ⭐ 平行柵欄（⛔ 逐檔列名，不是目錄）

| lane | 領地 | ⛔ 不碰 |
|---|---|---|
| **A** | `apps/admin/src/**` | client · sim · tools |
| **B** | `packages/shared/src/content/schema/**` · `apps/client/src/vfx/**` | admin · tools |
| **C** | `tools/**` · `content/**` | admin · client |
| **D** | `apps/game-server/**` · `apps/platform/**` | admin · client |
| **E** | `apps/client/src/ui/**` · `render/**` | vfx · admin |

⚠️ ⭐ **共用檔只有兩個**（CLAUDE.md 逐字）：`apps/admin/src/store.ts` 與 `ui/App.tsx` **各一行**
⇒ ⭐ 由主 session 最後統一加，⛔ lane 不碰。

⚠️ `pnpm skills:sync` 是**全域鎖** ⇒ ⛔ 平行時全部禁跑，主 session 最後統一跑一次。

---

## ⭐ 每一批的收工條件（⛔ 不是「commit 了」）

1. ⭐ **留後台開關可簡易 rollback**（三個住處：`content/config/*.json` ＋ Zod `DEFAULT_*` ＋ admin `SHIPPED_*`）
2. ⭐ **一條承重守衛 ＋ 一次突變驗證**（⛔ 不是每個分支各一條）
3. ⭐ 玩家看得到的功能：⛔ 沒有終端證據之前只准說「**鏈路已接上，⛔ 未驗收**」
4. ⭐ commit 一律 `git commit -F msg -- <逐檔 pathspec>`（⛔ 不 stage、⛔ 不 `--amend`）
5. ⭐ 收工跑 `noStrandedLaneCommits.test.ts`（⛔ 別讓 commit 擱在 worktree 分支上）

---

## ⭐⭐ 這份計畫怎麼**撐過 context 重置**（owner：「理論上是可以自動跑三天以上的工作量」）

⚠️ ⭐ 三天 ＝ **會經過好幾次 context 重置**。⇒ ⛔ 進度**不可以活在對話裡** ——
⭐ 而 CLAUDE.md 已經記過這個病：「一項工作被排進未來的版次，實際發生的事情是**它消失了**」。

### ⭐ 三個住處，缺一個就會重做已經做好的

| 住處 | 記什麼 | 指令 |
|---|---|---|
| **① GitHub issue** | ⭐ 每一票的**進度標記**（做了什麼 · commit sha · 玩家那一句） | `bash scripts/ticket-progress.sh write <票號> … --commit <sha> --player "<一句>"` |
| **② 本檔** | ⭐ 波次層級的勾選（見下方進度表） | 直接改這一份 |
| **③ 當日帳本** | owner 的裁決（⭐ 逐字，⛔ 不改寫） | `bash scripts/ruling.sh <票號>` |

⚠️ ⭐ **只寫一個地方等於沒寫**：只寫票 ⇒ 下一輪不會去翻 57 張；
只寫本檔 ⇒ GitHub 上看不到；只寫帳本 ⇒ 找不到對應的票。

### ⭐ 每一次接手（context 重置後）的**四行**

```bash
sed -n '/## 📊 進度/,/^---/p' docs/_execution-plan_57張_20260831.md   # ① 走到哪一波
gh issue list --state open --limit 400 --json number,title | head    # ② 還剩幾張
npx vitest run packages/shared/src/ops/noStrandedLaneCommits.test.ts # ③ ⭐ 有沒有 commit 擱在 worktree 分支
git log --oneline -12                                                # ④ 上一輪做了什麼
```

⛔ **不要**重新盤點 57 張票 —— ⭐ 那是 W0 的產出，它寫在下面的進度表裡。

### ⭐ context 到 95% 就存快照（⛔ 硬上限 98%）

```bash
bash scripts/memory-temp.sh     # ⚠️ 三節「（待填）」一定要當場補完,留著等於沒存
```

⭐ 掉的**不是程式碼**（那在工作樹裡）—— 掉的是**只存在對話裡的那一半**：
owner 的裁決 · 我撤回過哪個錯誤結論 · 兩個工作流為什麼不能同時跑 · 哪一個數字沒有來源。

### ⛔ 三天期間**每天**要做的

| 何時 | 做什麼 |
|---|---|
| 每天開工 | `bash scripts/board-roll.sh`（戰情表輪替＋七天窗） |
| 每天收工 | ⭐ **一天一次發版**（`bash scripts/release.sh --tag "說明"`）—— ⛔ 不要一天發五次 |
| 撞紅燈時 | ⭐ **批次撈**：三個閘一次跑完寫三個 log 再一起讀，⛔ 不是修一個跑一次 |
| 同一個閘紅第三次 | ⛔ **停** —— 那是迴圈不是壞運氣，找結構性根因 |
| 任何指令 > 5 分鐘沒新輸出 | `ps` 看 CPU%。⭐ **0% ＝ 掛了** ⇒ kill 重跑 |

---

## 📊 進度（⭐ 每完成一項就當場更新這一格，⛔ 不是最後補）

| 波 | 範圍 | 狀態 | 備註 |
|---|---|---|---|
| **W0** | 前提回驗 57 張 | ⬜ 未開始 | ⭐ 已知 `#842` `#721` 可關 |
| **W1** | 解鎖 Codex（12 項） | ⬜ 未開始 | ⭐ **最高優先** |
| **W2** | 管道（8 張） | ⬜ 未開始 | `#736` 是龍頭 |
| **W3** | 內容批（10 張） | ⬜ 未開始 | 可大量平行 |
| **W4** | 語音與回饋（8 張） | ⬜ 未開始 | |
| **W5** | 系統與玩法（17 張） | ⬜ 未開始 | ⚠️ `#865`/`#862` **09-05 前** |

⭐ 狀態符號：⬜ 未開始 · 🔄 進行中 · ✅ 完成 · ⛔ 卡住（**要寫卡在哪**）

---

## ⭐ 收尾

**BMPNDD** —— 部署走 `bash scripts/mini-deploy.sh deploy`（⛔ **不是** `host-deploy.sh`，那是 GCP 回滾機）。
