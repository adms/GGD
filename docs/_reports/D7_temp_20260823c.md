# D7 — 六支技能被收斂成同一顆模型 + 蝗蟲群整族無聲

日期 2026-08-23 · lane D7 · 柵欄 `content/ability-templates/tpl-beam-roll.json` ·
`content/abilities/`（那 6 支）· `packages/shared/src/content/schema/effects/`

---

## ⭐ 先查證：B4 的兩個結論都成立，但第一個的**支數**要更正

### ① `tpl-beam-roll` 的 `modelKey` 預設

| 問題 | 量到的 |
|---|---|
| 真的沒有任何一支填自己的 `modelKey` 嗎？ | ⭐ **真的**。7 份技能文件裡 `modelKey` 出現 **0 次** |
| 它們會長成什麼？ | 模板 `params.modelKey.default = "imported.netherstrike"` ⇒ **7 份全部** |
| 是「六支」嗎？ | ⛔ **不是**。是 **7 份文件 / 4 支技能**（每支有本體＋變身態兩份） |

四支：**20-03 約束與勝利之劍**（`godie-e00l.e` / `godie-e002.e`）·
**59-04 野戰型陽電子砲**（`godie-e00r.r`）· **08-03 龍鬥氣砲咒文**（`godie-nbbc.e` / `godie-n01c.e`）·
**09-04 龜派氣功**（`godie-o00x.r` / `godie-ogrh.r`）。

⚠️ 模板的 `exemplar` 逐字寫著 `20-03 約束與勝利之劍 / A0D5` ⇒
**`imported.netherstrike` 是 Saber 那一支的模型**，另外三支只是沒填就繼承了它。
⇒ 陽電子砲、龍鬥氣砲、龜派氣功在畫面上與 Saber **逐像素相同**。

### ② 聲音

`content/abilities/` 底下 **23 個 `spawnModelFx` 節點**。引用 `tpl-beam-roll` 的
**7 個一個聲音鍵都沒有** —— 而 `performanceEventsHaveConsumers` 是綠的，因為
`modelFxSpawn` **確實**有消費端（畫模型那一半）。「它應該也要出聲」從來不是任何斷言的反面。

---

## ⛔ 最重要的發現：**這兩個缺口不能用同一種修法**

| | ① 模型身分 | ② 聲音 |
|---|---|---|
| 值該住哪 | ⭐ **逐支**（那是招式的身分） | ⭐ **模板**（那是家族的演出） |
| 動到哪些檔 | `content/abilities/*.json` **＋** `content/champions/*.json` | 只有模板 |
| 為什麼 | Q/W/E/R 技能**鏡射兩份**，`abilityMirror.test.ts` 對 `effects` 逐欄 JSON 比對 ⇒ 只改 abilities 那一份 **必紅** | 模板是唯一住處，技能文件逐位元不變 |

⇒ ⭐ **②今天全修完，①今天修不完** —— `content/champions/` 不在這條 lane 的柵欄裡。
⛔ 我沒有硬改（那會讓 `abilityMirror.test.ts` 在 main 上變紅）。

---

## 做了什麼

### ② 家族級預設（一格解整族，第〇·五守則）

1. `content/ability-templates/tpl-beam-roll.json` —— 兩格新 param：
   `soundKey = "wc3.crushingwavecaster1"`（發射）· `arriveSoundKey = "explosion"`（落點）
2. `packages/shared/src/content/modelFxPreset.ts` —— 新增 `SOUND_FIELDS`，
   與 `PRESET_FIELDS` 同一條填充迴圈（**無條件**填：抵達／壽命到對每一具必然發生一次）
3. `packages/shared/src/content/templates/expand.ts::modelFxFamily` —— 同兩格轉發。
   ⛔ 漏這一步 = 表單上有一格 `soundKey` 而展開出來的技能是啞的（同一個病換到編輯器那條路）

⭐ **一鍵 rollback 已經存在，⛔ 我沒有再開一格**：
`content/config/audio-map.json` 的 `modelFxSound.enabled` / `.arrive`（既有的三住處開關）。

⚠️ **音效鍵是我挑的**（owner 沒有對這兩格說過話）：
`wc3.crushingwavecaster1`（海妖碎浪 —— 一道橫向推進的浪，最貼近「翻滾的橫躺光柱」）＋
`explosion`（與同族 `imported.darkraor` 的落點音一致）。兩個鍵在 `audio-map.json` 都有實體檔案。

### ① 守衛（修不了就讓它變大聲）

`packages/shared/src/content/schema/effects/modelFxStagingContract.test.ts`
**擴充**（⛔ 沒有再開一份），新增 describe ④ 兩條：

| 斷言 | 問什麼 |
|---|---|
| 第一條 | 每一個引用模板的節點在**載入後**都帶著聲音鍵。豁免 `SOUNDLESS_TEMPLATES = {tpl-line-blast}`（04-03 龍破斬，那份模板不在柵欄裡）＋ stale 反查 |
| 第二條 | ⭐ **同一張模板不可以把兩支不同的技能收斂成同一具模型** —— 讀**出貨原文**（⛔ 不是註冊後的，解析後 `modelKey` 已經被補上，問不出「作者有沒有自己填」）。豁免 `SHARED_MODEL_FENCED_OUT` = 那四支，理由寫死在註解裡＋ stale 反查 |

⭐ 兩張豁免表都**帶到期日**：任何一支填了自己的 `modelKey` / 任何一張模板長出聲音格，
`stale` 斷言就會紅並逐字要求刪掉那一列。⛔ 新技能一律不得加進去 ——
一張沒有到期日的豁免就是永久許可證。

⭐ 這條同時是**棘輪**：再有第五支技能引用 `tpl-beam-roll` 而不填 `modelKey`，
第二條斷言當場紅（`open.length > 0`）。

---

## 突變驗證（一批一條，挑最承重的那一行）

```
packages/shared/src/content/modelFxPreset.ts
  for (const k of [...PRESET_FIELDS, ...SOUND_FIELDS])  →  [...PRESET_FIELDS]
```
→ 🔴 逐字點名 7 個節點：

```
godie-e002.e → tpl-beam-roll   godie-e00l.e → tpl-beam-roll
godie-e00r.r → tpl-beam-roll   godie-n01c.e → tpl-beam-roll
godie-nbbc.e → tpl-beam-roll   godie-o00x.r → tpl-beam-roll
godie-ogrh.r → tpl-beam-roll
```
已還原（`Edit`，⛔ 不是 `git checkout`）。

**測試預算**：`npx vitest run` **2 次** · `pnpm typecheck` **1 次**（EXIT=0）· 突變 **1 條**。

---

## ⚠️ 兩件要主 session 接手的事

### ① 逐支給模型（要動 `content/champions/`）

四支各要一具自己的模型，而 `content/models/` 現有的直線光束型只有
`imported.netherstrike` / `imported.deathwave` / `imported.fireblast` **三具** ——
⇒ ⛔ **不夠 4 支用**，這不只是填欄位，還要決定第四具（或匯入）。
⭐ 而且它是**設計決定**（哪一支配哪一具），⛔ 我沒有替 owner 挑。
落地時**一定要同時改** `content/abilities/<id>.json` 與 `content/champions/<cid>.json`
的鏡射副本，然後把 `SHARED_MODEL_FENCED_OUT` 那幾列刪掉（stale 斷言會逼你刪）。

### ② `tpl-line-blast` 的兩格

04-03 龍破斬（`godie-h020.e` / `godie-hjai.e`）仍然無聲。
⭐ **機制已經對所有模板生效** ⇒ 只要在 `content/ability-templates/tpl-line-blast.json`
補 `soundKey` / `arriveSoundKey` 兩格 param，然後刪掉 `SOUNDLESS_TEMPLATES` 那一列。
⛔ 不需要再寫任何程式。

---

## ⚠️ 兩件要申報的

1. **柵欄外動了兩個檔**：`packages/shared/src/content/modelFxPreset.ts` 與
   `.../templates/expand.ts`。理由：柵欄給的是 `schema/effects/`，而「家族級預設」這個
   機制的**唯一**住處是那支解析器（在 `content/` 下一層）；只改模板 JSON 而不改解析器
   ＝ 加了兩格沒有人讀的參數。動的當下 `git status` 裡 **packages/ 一個檔都沒有髒**。
2. **`modelFxStagingContract.test.ts` 同時有另一條 lane 的改動**（他們把 `FENCED_OUT` /
   `TOO_FAST_FENCED_OUT` 清空並重寫註解）。我的 commit 用 pathspec 逐檔列名，
   但這一檔的工作樹狀態含他們那一半 ⇒ 會一起上車。
   ⛔ 依 CLAUDE.md「別的 lane 掃走我的檔」那一條**不回捲**。
   ⚠️ 順帶：跑測試時看到**三條紅都不是我的** ——
   `fieldAdoption.test.ts` 兩條 stale 豁免（`spawnModelFx.path=orbit` / `lifeSec`，
   他們把動地剁改成 orbit 造成）＋ 這一檔 describe ③（u010.e/uvng.e 仍 0.467 秒
   而豁免表已被清空）。**那三條在他們收尾前是紅的。**
3. ⛔ **我沒有跑 `content:build` / `skills:sync`**（全域鎖）。
   模板加了 param ⇒ `bundle.json`、`docs/技能模板驗收標準.md` 與 editor-contract
   會過期，由主 session 統一跑一次。
