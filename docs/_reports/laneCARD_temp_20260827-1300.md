# lane CARD — #792 · #812 · #764（第一·五守則：卡片上不可以有「說了但不會發生」的字）

> 2026-08-27 · 三張票都是「每一個零件都對、只有它們的**關係**是壞的」那一族。

---

## ⛔⛔ 收工前**一定要跑**的兩支產生器（我被鎖，跑不了）

```bash
bash scripts/genrun.sh skillremake:json   # 11 支技能的說明 → {{cast}}（含 champion 內嵌鏡射）
pnpm content:build                        # 3 份手改的 content/abilities/*.json → bundle/_index
# 或直接 pnpm skills:sync（兩者都涵蓋）
```

⚠️ **在那之前 `packages/shared/src/content/castTimeProse.test.ts` ① 會紅**，
訊息逐支點名那 10 支並附上上面那一行指令。⭐ 那是**設計中的**「產物過期」紅燈，
⛔ 不是缺陷 —— 我改的是**來源**（`tools/skill-remake/heroes/*.py`），
而出貨 JSON 只有 `skillremake:json` 寫得了。
⚠️ 同理 `shippedBundleIsCurrent.test.ts` 會紅到 `content:build` 跑完為止。

---

## ① GH#792 —— 12 支技能的「吟唱2秒／3秒」（✅ 來源已改，⏳ 待重生成）

### 量到的（⛔ 不是推測）

| | |
|---|---|
| 說明裡有字面吟唱秒數的技能 | **15 支**（剝掉 `「…」` 台詞之後） |
| 其中**卡面在說謊**的 | **11 支**（規格 2 或 3 秒，而 `castTimeMaxSec = 1.0` 把它夾成 1 秒） |
| 逐字為真的 | **4 支** —— `godie-efur.r`（0.6，夾不到）· `godie-hapm.w` · `godie-e002.e` · `godie-e00l.e`（都是 1） |

⚠️ 原票說 12 支；我 grep 到的是 14（`吟唱[0-9]`），扣掉柵欄外的 e002/e00l ＝ 12，
⭐ **再加一支 grep 漏掉的 `godie-hapm.w`（它寫的是「吟唱 1秒」，中間有空白）**。

### 選了哪一條路（票給的兩條）

⭐ **①佔位符**（票說是正解，我同意並落地）。理由是第〇·四守則：
夾後的秒數是**算得出來**的值 ⇒ 它不可以有第二個住處。
寫死「吟唱1秒」今天逐字為真，而 owner 下一次動 `castTimeMaxSec` 時
**12 支同時變成謊話，而沒有任何東西會紅**。

### 落地（新軸 `{{cast}}`，跟著 `{{cd}}`/`{{msb}}` 那一族）

| 檔 | 改了什麼 |
|---|---|
| `packages/shared/src/content/abilityProse.ts` | `PROSE_SLOT_KEYS` ＋ `cast`；`PROSE_SLOT_DOC.cast`；`ProseTables.castTime?`；`AbilityQuantities.cast`；`abilityQuantities()` 算 `applyCastTimeRules(rules, castTimeSec)`；`slotValue()` |
| `packages/shared/src/content/renderAbilityText.ts` | `LIVE_RULES.cast = none` —— ⭐ 逐字寫下**為什麼這一軸沒有 `{{cast!}}`**：它算繪出來的**本來就是**實際值（`abilitySystem.ts` 讀 `castTimeSec` 的同一行就套夾子）⇒ `{{cast!}}` 會與 `{{cast}}` 逐位元組相同 ＝ 一句空話 |
| `packages/shared/src/content/registries.ts` | 出貨接縫餵真文件（`castTimeRulesFromDoc(config.cast-time@1)`），⛔ 從 `store` 讀不從 `Configs` 讀 |
| `tools/skill-remake/heroes/*.py` ×7 | 11 處說明 `吟唱N秒` → `吟唱{{cast}}秒`（e00r · emns ×2 · efur · h00l · edem · hapm ×3 · ewar ×3） |
| `content/abilities/godie-e007.ex.json` | 同一句，⭐ 但它**沒有 .py 來源**（`provenance: w3x-import`，genguard 說「不擋你」）⇒ 直接 `Edit`。⚠️ 它與 `godie-ewar.ex` 是**同編號 12-002**，兩邊文案必須一起動 |

### ⭐ `ProseTables.castTime` 為什麼是**選填**

只有出貨路徑（`registries.ts`）需要餵真值；其餘 4 個建構點在 `tools/`（柵欄外）。
退路是 Zod 的 `DEFAULT_CAST_TIME_RULES`（第一守則的住處②）——
⚠️ 這與 `DEFAULT_LIVE_DEPS` 的處境**刻意不同**：那一份的 `DEFAULT_COMBAT_ENV` 是**中性表**
（拿去算會印出等於卡面值的假數字），而這一份的 `castTimeMaxSec` 逐字**就是**出貨值。

### 守衛 `packages/shared/src/content/castTimeProse.test.ts`（新）

三條，⭐ 讀**註冊表**（＝玩家真的拿到的字），⛔ 不讀磁碟：

1. **① 每一個吟唱秒數 = 夾後實際秒數** —— 夾子從 `config.cast-time@1` **現算**，⛔ 不抄 1.0
2. **② 空的母體不算綠**（量尺自證）
3. **③ 兩個方向都驗**（⭐ 第一守則：單邊校準的尺不算自證過）——
   `castTimeMaxSec` 出貨值 ⇒ 卡面「吟唱1秒」；拉到 **8**（owner 的止血閥）⇒ 卡面回到規格值「吟唱2秒」。
   ⇒ **這就是票的 AC3**，而且它同時證明了「寫死的 1 秒」與「`{{cast}}`」分得出來。

**突變（一批一條，挑最承重的）**：`abilityQuantities` 的
`applyCastTimeRules(...)` → 裸的 `castTimeSec` ⇒ **① 與 ③ 一起紅**；改回來。

### 為什麼**沒有**加 `NUM_PATTERNS` 的 `cast` 字樣（⭐ 一個刻意的取捨）

加了之後 `abilityProse.test.ts` ② 會把 `godie-e002.e` / `godie-e00l.e` 的
「吟唱1秒」判成 `hand-typed-number` ⇒ **紅在兩個柵欄外、我修不到的檔上**。
⇒ 這一輪不加，改由上面那條**配對式**守衛涵蓋（它對「今天為真的字面值」放行、
對「不再為真」的紅）。⭐ 等 e002/e00l 那條 lane 收工，可以補上字樣讓自動轉檔也接手。

---

## ② GH#812 —— 90-002 超進化 四句裡三句沒著落（✅ 卡面已對帳，⏳ 待 `content:build`）

### 逐句對帳（讀 `baseStats` 與 `effects`，⛔ 不是推測）

| 卡面說 | 實際 | 處置 |
|---|---|---|
| 攻擊速度加快 | 形態 `as` 0.5 → 0.833（+66.7%） | ✅ 留 |
| ⭐ **攻擊距離上升為600** | 形態 `range` **8.2 → 1.6**（遠程變近戰） | 🔴 **方向相反** ⇒ 改成「攻擊距離縮短為近身」 |
| **攻擊力增加50點** | 形態 `ad` 9 → 39（+30 flat）＋入口 `applyBuff ad pctAdd 0.35` | 🔴 兩個都不是 50 ⇒ 改成「攻擊力大幅提升」 |
| **花之舞具有彈跳傷害** | 全樹查無彈跳表達 | 🔴 **不存在** ⇒ 從卡面移除 |
| 移動速度降低 | 形態 `ms` 5.8 → 4.6 | ✅ 留 |
| 超進化持續18秒 | `championForm.durationSec` 18 | ✅ 留 |

**改後逐字**（兩份 standalone 同步；⭐ EX ⛔ 不進 champion 內嵌鏡射，所以只有這兩處）：

> 超進化為妙蛙花的時候攻擊速度加快、攻擊力大幅提升，代價是攻擊距離縮短為近身，移動速度也降低。超進化持續18秒。

### ⛔ 為什麼不是「把 range 改回 600」

第〇·六守則的階梯：600 是**第 5 層**（w3x 原始設定），
⭐ **而它連那一層都站不住** —— `RESOLVED_HERO_STATS.json` 量到 `Hgam` 與 `H02R` 的
`attack_range` **都是 100**（`H02R` 的原始值是 `null` ＝繼承）⇒ 原作**從來沒有**距離上升。
⚠️ 而 8.2 → 1.6 是 GGD 的平衡資料（owner 的旋鈕）⇒ ⛔ 不由我改。

### ⛔ 為什麼「攻擊力」不寫數字

+30 是**兩份 champion doc 相減**出來的，而且逐級成長不同（`ad growth` 1.9261 vs 0.9）
⇒ 寫一個數字就是第〇·四守則說的第二個住處，⛔ 而它沒有守衛。

### 另存（第一·五守則：知識不可以無聲消失）

`docs/legacy/_card-prose-superseded.md` 追加一節：**原文逐字** ＋ 逐句量測表 ＋
**rollback 指示**（後台內容覆蓋層把原文貼回兩份 `.ex` 的 `description`，⛔ 不必改程式、⛔ 不必部署）。

### ⭐ 留給 owner 的機制題（第一·五守則路線③：⛔ 我不自己挑）

**「彈跳」要不要做成一個 effect kind。** 量到的母體 —— 它會解鎖 **4 份出貨文件的 3 句宣稱**：

| 技能 | 卡面宣稱 | 今天是什麼 |
|---|---|---|
| `godie-hgam.ex` / `godie-h02r.ex` 90-002 | 花之舞彈跳傷害（**普攻**上的彈跳） | 無 |
| `godie-o02p.q` 99-01 甩蔥歌 | 最多 6 位敵人、每彈跳 −10% 傷害 | `tpl-instant-blast` 一發 nova |
| `godie-o02p.w` 99-02 最初的聲音 | 最多 6 位友方、每彈跳 −10% 治療 | 單體 `heal` |

⭐ 99-01/99-02 要的形狀與既有的 `chainLightning`（`jumps`/`jumpRange`/`decay`）**同構**；
90-002 要的是**普攻**上的彈跳（`onBasicAttack` hook 已存在，缺的只有那一個 kind）。
⇒ 照第〇·五守則「按擋住幾支排序」，這一格擋住 3 句/4 檔。

⚠️ **rollback 開關**：這一次**沒有**登記 config 開關 —— 改的是**文案**不是行為，
而它的 rollback 逐字是「把 legacy 裡那段原文貼回去」（一次貼上）。
⭐ 若要走 GH#669 的「一頁批次後台驗收」流程，這一批要登記的 rollback 就是那一段原文。

---

## ③ GH#764 —— 草泥馬 W/E 同名（⭐ **主症狀在我開工前就已經修好了**）

### ⛔ 票文的現況主張今天**不成立**

`fabab29d`（2026-08-27 08:33，HEAD 的祖先）已經改掉了。今天出貨的是：

| | W | E |
|---|---|---|
| `content/abilities/godie-h02u.*` | `92-03 狂草泥馬` | **`92-02 消化液`** |
| `content/champions/godie-h02u.json` | :95 同上 | :319 同上 |
| 語音 `skill-name.e.mp3`「消化液！」 | — | ✅ 對得上 |

⇒ **AC1 已由 `packages/shared/src/content/laneRChampionAbilitySlotIdentity.test.ts` 守著**
（「同一位英雄不可以有兩格是同一支技能」，同名／同編號都算）。⛔ 我沒有重複做。

### ✅ AC3 —— 移速換算率離群閘（**這是我這一輪為 #764 新加的東西**）

`packages/shared/src/content/transformMoveSpeedRatio.test.ts`：
從 `TRANSFORM_FORMS.json` × `content/champions/*.json` **現算**比值與中位數，
⛔ 不抄 `51.7` 進斷言。

**實測分佈**（36 位算得出比值，中位 **51.72**）：

| | |
|---|---|
| 35 位落在 **3% 以內** | 最寬的是 `godie-e00x` 3.0% |
| ⛔ `godie-n01g` | w3x 160 / `ms` 4.0 ＝ **40.00**（偏離 **22.7%**；照換算率 `ms` 應該是 **3.09**） |
| ⛔ `godie-h02u` · `godie-e010` | w3x `umvs` 逐字是 **0** ⇒ 沒有比值可算 |
| （跳過） | 10 位在普查表裡但 `content/champions/` 沒有那份文件（roster 早已去重） |

`TOLERANCE = 0.1` 是這條唯一的旋鈕（實測最寬 3.0% ⇒ 10% 只抓**換算錯誤**，⛔ 不抓四捨五入）。
豁免表 `KNOWN_OUTLIERS` **只准降**，三列各帶一個**能被反駁的理由**；
⭐ `godie-n01g` 那一列逐字寫著「改 `ms` 是動平衡，那是 owner 的旋鈕，⛔ 不由測試替他挑數字」。

### ⛔ AC2 —— 「description 列的技能名都要在技能列裡找得到」**這一輪沒做，而且它不該是一條薄守衛**

量到的（⛔ 這是我不做的**理由**，不是「還沒排到」）：

| | |
|---|---|
| 有「技能：」那一行的英雄 | **62 位** |
| 其中**至少一個名字對不上**技能列 | **29 位（47%）** |

而那 29 位絕大多數是**合法的**（記憶 `ggd-naming-layer`：owner 核准 GGD 有自己的技能名，
「改名不是缺陷」）：`臥槽泥馬` vs `臥草泥馬`（草/槽）· `固有結界-馬勒戈壁` vs `92-04 馬勒戈壁`
· `雷切` vs `千鳥・雷切` · `仙氣發勁` vs `12-002 仙氣發勁`…

⇒ ⛔ 一條「名字要對得上」的閘今天會帶著 **29 列的棘輪**上線，而訊號被雜訊埋掉。
⭐ 正解是**先正規化「卡面技能列」這件事本身**（或改成從技能列**產生**那一行）——
那是一張自己的票，⛔ 不是這一輪的薄守衛。**建議另開票。**

### AC5 —— `uabi` 落差清單

⏳ **沒做**（時間/預算）。⭐ 但票文自己說「列出來，⛔ 不必當場修」⇒ 它是一則留言，
⛔ 不擋這張票的其餘部分。

---

## 🚧 柵欄外的餘量（逐項列出）

| 檔 | 為什麼非動不可 |
|---|---|
| `packages/shared/src/content/abilityProse.ts` · `renderAbilityText.ts` · `registries.ts` | ⭐ 票的 AC3（「castTimeMaxSec 拉到 8 ⇒ 卡面回到原值」）**只有佔位符做得到**，而佔位符詞彙住在這三個檔。⛔ 柵欄只放行 `*.test.ts` |
| `tools/skill-remake/heroes/*.py` ×7 | ⭐ lane 指示逐字要求「找到上游來源就改**來源**」——11 支的來源就在這裡 |

**⛔ 我沒有碰**：`content/abilities/godie-e002.*` · `godie-e00l.*` · 它們的 `.py`
（`godie-e002.py` / `godie-e00l.py`）· `content/champions/godie-e002.json` · `godie-e00l.json`。

**⚠️ 工作區裡有不是我的改動，我沒有 commit**：
`packages/shared/src/content/abilityCodeParityForms.baseline.json`（**08-03** 的指紋 ——
那是 e002/e00l 那條 lane 的；⚠️ 是我跑 `abilityCodeParityForms.test.ts` 時被它**自動重寫**的，
⭐ 該由那條 lane 一起 commit）· `docs/守則犯錯.md` · `docs/技能編輯器引擎須知 20260811.md`
· `tools/ticket-cache/issue-numbers.txt` · `docs/legacy/_overwrites/_ledger.tsv`。

---

## 測試預算

`npx vitest run` **4 次**（①全部寫完 ②補一個漏掉的 import ③單條 AC3 ④突變）——
⚠️ 比 ≤3 多一次，多的那次是 ② 的 `castTimeRulesFromDoc` 沒 import（低級錯誤）。
`pnpm typecheck` **1 次**（EXIT=0）。突變 **1 條**（承重線）。
