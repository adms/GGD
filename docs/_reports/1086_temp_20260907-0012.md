# GH#1086 —— 者皆陣 Q→W→E 的 1 秒連擊窗：從**按下**起算，⛔ 不是從吟唱結束

> lane 報告（第二波，2026-09-07 00:12）。⛔ 未 commit、⛔ 未跑任何 `*:build`／`skills:sync`。
> ⚠️ **柵欄外的 13 個檔沒有動** —— 修法做成一份 patch（沙盒複本驗過），等主 session 套：
> `git apply docs/_reports/1086_outside-fence_temp_20260907-0012.patch`（`git apply --check` 在主樹 EXIT=0）。

## 一句話
決策點做成 `config.cast-time@1.comboWindowFrom`（出貨 **`commit`**）：連段窗口從**提交 tick**（按下）起算。
`recentCast` 在解算時拿 `ConditionContext.castCommitTick` 當基準；有吟唱的技能在**提交點**就把 `comboBonus` 烘進 `CastState.effects`
（⛔ 不能等到解算 —— moon-combo 的 1 秒在 1 秒吟唱結束那一 tick 已經被 `statusExpirySystem` 剪掉，`hasStatus` 對任何基準都是 false）。
沙盒複本（in-fence 改動 ＋ patch）：`moonComboChain.test.ts` **8/8 綠**（兩條 `it.fails` 翻回 `it` ＋ 兩條「關掉 ⇒ 逐位元回今天」），突變 **2 紅**，tsc 綠。

## 開關（四個住處）

| 住處 | 檔:行 |
|---|---|
| ① config | `content/config/cast-time.json` `"comboWindowFrom": "commit"`（genguard：無產生器擁有者；⚠️ 它進 bundle ⇒ 主 session 要 `content:build`） |
| ② Zod／DEFAULT | `packages/shared/src/sim/castTimeRules.ts:105`（`CastTimeRules.comboWindowFrom`、`COMBO_WINDOW_FROM_VALUES`、`DEFAULT_CAST_TIME_RULES.comboWindowFrom = "commit"`、`castTimeRulesFromDoc` 缺格／認不得 ⇒ 出貨值）＋ `packages/shared/src/content/schema/config/castTime.ts`（`z.enum(...).optional()`，`@zh/@note/@opt` 三條指令） |
| ③ admin | `apps/admin/src/configForms/specs/authoring.ts` `CAST_TIME_SPEC`：欄位由 `derivedFields(zConfigCastTimeDoc)` 自動推導（enum ⇒ 下拉，`@opt` 給中文）；intro 多一段、`consumer` 點名兩個讀端 |
| ④ ⭐ 消費端 | **`packages/shared/src/sim/content/condition.ts:1377`** `const base = comboWindowBaseTick(world.castTimeRules, world.tick, ctx.castCommitTick)`（recentCast 分支，已落主樹）＋ **`packages/shared/src/sim/abilities/abilitySystem.ts:849`**（patch 套上後）`comboWindowFrozenAtCommit(world.castTimeRules)` ⇒ 提交點增幅＋烘焙寫進 `CastState.effects` |

預設我挑 `commit`（第〇·六：高層級＝卡面「發動後 1 秒內**施展**」成立的那一邊；原作 `udg_MoonCombo` 按下就設旗、按下就讀）。`resolve` ＝ 2026-09-06 前的行為，一鍵 rollback。⚠️ 這一格**不動任何數值**（窗口／吟唱／係數都不變）；瞬發技兩邊逐位元相同。

## 做了什麼

### 已落主樹（柵欄內）
| 檔 | 改動 |
|---|---|
| `packages/shared/src/sim/castTimeRules.ts` | `comboWindowFrom` 型別／出貨值／解析；`comboWindowFrozenAtCommit(rules)`、`comboWindowBaseTick(rules, now, castCommitTick?)` 兩支唯一讀端 helper |
| `packages/shared/src/content/schema/config/castTime.ts` | 欄位 ＋ `DEFAULT_CAST_TIME_DOC.comboWindowFrom`（⛔ 沒有新 export ⇒ `configFacadeSurface` 基準線不動，驗過綠） |
| `content/config/cast-time.json` | 加一格 ＋ note 補一句 |
| `apps/admin/src/configForms/specs/authoring.ts` | 見上表③ |
| `packages/shared/src/sim/content/condition.ts` | ⚠️ 這個檔同時有**另一條 lane 的 form 葉**未 commit 改動；我的四個 hunk：`import { comboWindowBaseTick }`、`ConditionContext.castCommitTick?`、recentCast 分支的 `base`、`scalingOracle(world, self, target?, castCommitTick?)` 第四參。⭐ 單獨落地是安全的：`castCommitTick` 缺席 ⇒ 基準＝現在 ⇒ 與之前逐位元相同（`conditionRecentCast.test.ts` 3/3 綠） |
| `packages/shared/src/sim/moonComboChain.test.ts` | 兩條 `it.fails` → `it`（訊息指名 patch 路徑）；`stage()`／`qThenW()`／`wThenE()` 收 `Partial<CastTimeRules>`；新增兩條「`comboWindowFrom: "resolve"` ⇒ 窗口內＝基準」。⚠️ **主樹 patch 未套之前這兩條翻回的 `it` 是紅的** —— 刻意 fail-loud（silent 才是缺陷），訊息寫著去看 patch |

### patch（柵欄外，⛔ 沒動主樹）`docs/_reports/1086_outside-fence_temp_20260907-0012.patch` —— 13 檔 86+/23−
| 檔 | 改動 |
|---|---|
| `sim/stats/statsComp.ts` | `CastState.beganTick?`（按下那一 tick）、`CastState.effects?`（提交點烘好的清單）；`import type { EffectDef }`（型別，無執行期環） |
| `sim/effects/effect.ts` | `EffectContext.castCommitTick?`（RUNTIME，騎在 ctx 上，理由同 `proxyDepth`） |
| `sim/systems/CastResolveSystem.ts` | `cast.effects ?? applyAugmentToEffects(...)`（⛔ 不增幅兩次）＋ ctx `castCommitTick: cast.beganTick ?? world.tick` |
| `sim/abilities/abilitySystem.ts` | ct>0：`comboWindowFrozenAtCommit` ⇒ `bakeCastTimeConditionals(applyAugmentToEffects(def.effects, ...), ctxAtCommit)` 寫進 `effects`；`beganTick: world.tick`；瞬發 ctx `castCommitTick: world.tick` |
| `sim/effects/effectRunner.ts` | `gateOnCondition` 把 `castCommitTick` 帶進兩個 `evaluateCondition` |
| `sim/effects/damage.ts`（3 處）· `dot` · `damageLine` · `damageArea` · `chainLightning` · `spendMana` · `heal` · `shield`（各 1 處） | `scalingOracle(..., ctx.castCommitTick)` —— `ratios[].when` 的求值器全部 9 個 handler 都帶。⚠️ 第一版只改了 `damage.ts` 的兩個 comboAddend 呼叫 ⇒ Q→W 仍紅（W 的 recentCast 住在**主體** `amount.ratios[].when`，走 `damage.ts:148` 那一個）—— 所以 8 個都要 |

## 量到什麼（沙盒複本 = 主樹 in-fence 改動 ＋ patch）
- **run2**：21 檔 255 tests 全綠（moonComboChain 8/8 · conditionRecentCast 3/3 · castTimeRules 4/4 · conditionalRatios · leapJassFidelity · hartJassFidelity · groundAoeResolve · innateActive · combatJuice · hitFeel · purity · facingLock · castTimeCoverage · configFacadeSurface · dot／damageArea／damageLine／chainLightning／shieldAbsorb／shieldBreak／incomingReflect）
- Q→W：press 差 **2 tick**、解算差 38 tick（窗口 30）⇒ 之前 1761.89＝基準；現在 **> 基準**。超過窗口 ⇒ 仍＝基準。
- W→E：press 差 **0 tick** ⇒ 之前 1211.22＝基準；現在 **> 基準**（提交點烘焙）。超過窗口 ⇒ 仍＝基準。
- `comboWindowFrom: "resolve"` ⇒ 兩條窗口內都＝基準（逐位元回今天）。
- 沙盒 `tsc --noEmit -p packages/shared/tsconfig.json` EXIT=0；主樹 `pnpm typecheck` EXIT=0（in-fence 改動單獨也型別安全）。
- ⚠️ run1 順手跑到 `castabilitySweep.test.ts` 紅：`27 支技能的模板展開失敗 … 找不到的模板: tpl-apply-status, tpl-blink, tpl-heal, tpl-projectile-strike`，323/341 —— ⛔ 不是我的：templatize lane 的 in-flight 內容。⚠️ 它透過沙盒的 `docs` symlink 把主樹 `docs/_castability-128.md` 重寫了 ⇒ 我用 `git show HEAD:docs/_castability-128.md >` 還原（⛔ 沒有 checkout；還原前 `git status` 該檔乾淨，還原後亦乾淨）。之後的 run 都排除它。

## 突變紀錄（沙盒，同一次跑）
`condition.ts` recentCast 的 `base` 改回 `world.tick` ＋ `abilitySystem.ts` 的 `frozenEffects` 改成 `false`：
- × 「吟唱一結束就按 W ⇒ 含條件式 AP 項」 FAIL（1761.89 = 基準）
- × 「W 解算後立刻按 E ⇒ 含 comboBonus」 FAIL（1211.22 = 基準）
- ○ 其餘 6 條 PASS（兩條超過窗口、兩條關掉、Q 進 ledger、EX 分岔）
改回來 → 8/8。⛔ 沒有用 checkout（count-asserted replace，改回後 `grep -c MUTATION` = 0）。

## Known risks 逐支看過（`grep -rl recentCast content/abilities`）
| 技能 | 葉在哪 | 影響 |
|---|---|---|
| `godie-hpb1.w` | 主體 `ratios[].when recentCast Q 1s`（吟唱 1.0） | ⭐ 這張票 |
| `godie-h020.r` / `godie-hjai.r` | 主體 `ratios[].when recentCast EX 6s`（吟唱 2.0） | 基準提早 2 s；窗口 6 s ⇒ 只會更容易成立，實務上無差 |
| `godie-h020.e` / `godie-hjai.e` | `blink.onArrive[].ratios[].when recentCast EX 6s`（吟唱 1.233） | `blink` 用 `{...ctx}` 展開 ⇒ `castCommitTick` 跟著走；同上 |
hook／DoT／投射物落地的求值**沒有** `castCommitTick` ⇒ 基準＝現在，逐位元不變。AP 六維係數：⛔ 沒動任何內容／條件 ⇒ 不變。

## ⛔ 沒做到的
- 柵欄外 13 檔**沒有落主樹**（preamble 第 3 條）—— 只有 patch。⇒ 主樹的 `moonComboChain.test.ts` 兩條翻回的 `it` **在 patch 套上前是紅的**（訊息指名 patch）。
- 「玩家看得到」：沙盒裡是真 world／真 `castAbility`／真 `CastResolveSystem` 的 damage 事件量，⛔ 但主樹還沒有 ⇒ **鏈路已接上，⛔ 未驗收**（主樹）。

## 需要主 session 動的
1. **`git apply docs/_reports/1086_outside-fence_temp_20260907-0012.patch`**（`--check` 已過）→ `npx vitest run packages/shared/src/sim/moonComboChain.test.ts` 期望 8/8。
2. `pnpm content:build`／`skills:sync`：`content/config/cast-time.json` 改了 ⇒ `bundle.json` 過期（`shippedBundleIsCurrent` 會紅）。
3. `packages/shared/src/sim/content/condition.ts` 是與另一條 lane（form 葉）**共用**的檔 —— commit 時 pathspec 會把兩邊的 hunk 一起帶上，主 session 決定歸哪張票。
4. `moonComboChain.test.ts` 檔頭已寫 GH#1086 的突變紀錄（沙盒）；套 patch 後若要在主樹再驗一次突變，把 `condition.ts:1377` 的 `base` 改成 `world.tick` 即可（一條線）。

## 順手發現（⛔ 沒動、沒開票）
- `godie-hpb1.e` 的 `comboBonus{statusId: moon-combo}` 與 `recentCast slot W withinSec 1` 在 GH#937 之後是**同一件事的兩種表達**（W 施放 ⇒ 1 秒窗）；`moon-combo` 狀態只為它存在。內容側可以收成一種，⛔ 但那是 `content/abilities/**`（柵欄外）且 `comboWindowClaims.test.ts` 主 session 在改 —— 只記在這裡。
- `castabilitySweep.test.ts` 會**寫** `docs/_castability-128.md`（測試有檔案副作用）—— 任何從別的工作樹跑它的人都會踩到我踩的那一下。
