# CRESCENT lane 完整報告 — GH#688 Phase 6：crescent（大紅蓮斬）×5（2026-08-25 21:30）

> 任務：census 建議表第三族 —— 5 隻 dummy（u017/u01L/u01M/u01N/u01O）全叫「大紅蓮斬」，
> 共用地圖自帶模型 `crescent.mdl`，params 全同（scale 2 · tint 255,30,30 · lifeSec null）。

## 〇、⭐ 核心發現：這一族的證據形狀與 TORNADO 完全不同

任務簡報的假設（「多半是同一次施放的五道紅月斬 ⇒ orb 或 line」）**被 sites 證據推翻**：

1. **五隻 dummy 是死內容**（三路查證，⛔ 不是猜）：
   - `tools/locust-census/census.json` templateSuggestions：五隻全部 **sites=0、triggers=[]**
   - `war3map.j` grep `'u017'`…`'u01O'`：**零筆**（含地圖擺放的 CreateUnitsForPlayer 函式）
   - OBJECTS.json 全文＋int32 編碼（1966092599…623）：**零引用**（無 summon 欄位、無動態 id）
   - 6 個動態 id 生成點逐一看過：全是 CreateHero/pet 觸發，無關
2. **名字對到的技能不在出貨名冊**：87-01 大紅蓮斬（A0DE，base AOsh）＝ 曹操 `godie-o02o.q`，
   而曹操整包住在 `content/_legacy/`。⇒ dummy 路線**零落點**（誠實列，兩個獨立理由）。
3. **⭐ 但模型本身有真的落點** —— 走 **w3a 證據**（vfx-census `realArt`，provenance
   `w3a-override`，join CONFIRMED）：`crescent.mdx` 是兩支出貨技能的 **Art-Missile**：
   - `A0MY`（80-02 弒鬼神，base **AOsh 衝擊波**）→ `godie-h01u.w`（呂布 W）
   - `A0EZ`（08-04 阿邦快速劍X，base **AUcs 蝗蟲群**）→ `godie-n01c.r`＋`godie-nbbc.r`（小呆 R 兩具身體）
   兩個 base **真的會渲染** missile art ⇒ 原作施放時彎月刃**沿直線飛出**。
   ⇒ preset ＝ **`tpl-locust-travel`（forward）**，⛔ 不是 orb 也不是 line。

## 一、模型（交付 1）——已在 repo，本批做的是**驗**不是**轉**

| | |
|---|---|
| 現況 | `content/models/imported.crescent.json` ＋ `content/assets/models/imported/crescent.glb`（203KB，7/22 匯入）已在出貨 models `_index` —— **零轉換工作**，但**零使用者** |
| ⚠️ 靜態體檢（逐位元讀 glb） | **5 primitive 中 4 個是轉檔器軟刪除**（baseColorFactor [0,0,0,0]＋BLEND —— netherstrike 同款病）。存活的 mat1（18 verts，跨 6.8 單位的刃面）：貼圖 256×256、**75% 亮像素（RGB>32 & A>32）**、maxRGB 255、meanA 222、UV 不退化（12×13 distinct）⇒ **shape-in-RGB，會畫出像素** |
| 音效 | `VFX_SOUND_JOIN` 量到 crescent.mdx 有烘焙 SNDX 事件（`wc3.wandofneutralization`，聲檔已在 audio-map）——但 travel 家族是 SOUNDLESS（staging ④），preset 節點不准帶 soundKey ⇒ **列 owner 裁決項**，⛔ 本批不擅自接 |

## 二、逐支對照（交付 2 · 5）

| 證據源 | 原作 | GGD 落點 | 節點 | 狀態 |
|---|---|---|---|---|
| w3a `A0MY` Art-Missile | 弒鬼神：彎月刃沿直線飛 450 | `godie-h01u.w`（新版＝[周圍] AoE 4.5） | travel forward · **distance 4.5＝技能自己的 damageArea radius**（第〇·六守則第 1 層錨） | ⭐ 改**來源** `tools/skill-remake/heroes/godie-h01u.py` `model_fx=`（產物鎖住，等 skillremake:json） |
| w3a `A0EZ` Art-Missile | 阿邦快速劍X：彎月刃沿線 | `godie-n01c.r`（blink range 12） | travel forward · **distance 12＝技能自己的 range**，節點放 blink **前**（從施放位置沿面向飛，重現原作彈道） | ✅ 手編（standalone＋champion 鏡射） |
| 同上 | 同上 | `godie-nbbc.r`（鏡射身體） | 同上 | ✅ 手編（standalone＋champion 鏡射） |

節點形狀（三落點同款，⛔ 零行引擎程式）：
`{"kind":"spawnModelFx","shape":"single","preset":"tpl-locust-travel","modelKey":"imported.crescent","clip":"idle","scale":2.0,"distance":<上表>}`
- **scale 2.0** ＝ census 這一族唯一量到的 usca（那五隻 dummy 的字面值）。
- **⛔ 不帶 tint** —— w3a missile 未染色；紅 tint（1,0.1176,0.1176）屬於**未接線**的 87-01 dummy，
  拿來染這兩支是發明原作沒有的畫面。
- speed/spin 留模板預設（無逐支證據，家族值不歸我挑）。

### 無落點清單（帶理由，⛔ 不是「還沒做」）

| rawcode | 理由 |
|---|---|
| `u017` `u01L` `u01M` `u01N` `u01O` | **sites=0**（JASS/OBJECTS/int32 三路皆零）＝地圖作者建了但從未接線的死內容；名字對到的 87-01 在 `_legacy`（曹操未出貨）。⭐ 兩個理由都做成**會紅的守衛**（見 §三③）：census 重掃出生成點、或曹操哪天出貨 ⇒ 當場紅、把五道紅月斬（scale 2 · tint 紅）接到它身上 |

## 三、守衛（`packages/shared/src/content/locustCrescent.test.ts`，@visual-proof）

① **glb 靜態可見**：≥1 出生可見 primitive（軟刪除 [x,x,x,0] 判掉）＋可見者必須帶貼圖＋TEXCOORD_0
② **三落點擺得出彎月刃**（standalone＋champion 鏡射 6 處）＋參數釘證據（preset=travel · ⛔ 不得帶 tint · scale=2）＋`validateDoc` 過出貨 schema
③ **無落點理由保持成立**：census sites 仍 0 ＋ `godie-o02o.q` 仍不在出貨 —— 不成立那天紅，訊息自帶下一步

- vitest **2 次**（上限 3）：①七檔批跑（locustCrescent＋tornado＋monsoon＋staging 契約×2＋runtimeAlpha＋noOp）——**唯一紅＝h01u 兩列（設計如此，訊息自帶「跑 pnpm skills:sync」）**，其餘 27 綠 ②突變。
- **突變（承重線）**：刪 `godie-n01c.r.json` 的 spawnModelFx → 紅並逐字指名 `A0EZ → content/abilities/godie-n01c.r.json (standalone)` → 還原（重加節點，⛔ 非 checkout）。
- `pnpm typecheck` **1 次**：EXIT=0（全 repo）。

## 四、視覺證明（交付 3）✅ `docs/_reports/crescent_visual-proof_20260825-2130/`

audition（`beam-audition.html?ability=godie-n01c.r`，5 PNG＋frames.md）：
**calibrate() 自證 173,056** · 基線 **0 lit** → 重放（glb 已快取）**986 lit / 218 bright** →
⭐ **A/B：只關 crescent 節點 ⇒ 420 lit / 0 bright**（566 lit＋全部 bright 來自 crescent）→
重啟用逐數字復原 → 到期 **0 lit**、節點自動 disabled。shot1 目視可辨：白銀彎月刃在飛行中。
⚠️ 誠實限制（frames.md 詳）：**首發串流競速**（glb 首載 ≈1s > 飛行 0.4s ⇒ 一場裡第一發可能看不到，
第二發起可見）；4/5 prim 軟刪除 ⇒ 單刃面無原作光暈；渲染為未染色白銀刃（w3a 證據）。

## 五、#669 批核頁登記（交付 4）✅

- `review:register` 已入帳：批次 `crescent_visual-proof_20260825-2130`，預設 LIVE。
- **rollback 開關**：`tpl-locust-travel` → `params.alpha.default`＝**1.0**（live）⇒ **0**（veto）。
  1.0 與缺席逐位元同義（`modelFxRig.ts:236` 對 alpha≥1 明文跳過乘法）⇒ 出貨畫面不變；
  翻 0 ⇒ travel 整族沒寫自己 alpha 的節點一起隱形。**TORNADO orb 開關同款形狀**（本批把
  travel 的 alpha 補上 default 並同步更正模板 description 那句「刻意沒有預設值」——語意改了
  舊文案就是謊話）。
- `review:check` exit 0（rollback 解析通過；queue.json 已用 `genrun.sh review:build` 重生成）。

## 六、逐檔清單（工作樹，⛔ 本 lane 零 git 寫入）

**改（來源／模板）**
- `tools/skill-remake/heroes/godie-h01u.py` — 80-02 加 `model_fx=` 一列（含證據鏈註解）
- `content/ability-templates/tpl-locust-travel.json` — alpha default 1.0 開關＋description 更正

**改（出貨內容，經 `product-quarantine.sh unlock/lock --step tiers:apply` 正規解鎖，改完已重鎖 402 份）**
- `content/abilities/godie-n01c.r.json` ＋ `content/champions/godie-n01c.json`（R 鏡射）
- `content/abilities/godie-nbbc.r.json` ＋ `content/champions/godie-nbbc.json`（R 鏡射）

**改（帳本／產物）**
- `docs/_review/feature-verdicts.json`（review:register 寫入）
- `docs/_review/queue.json`（`genrun.sh review:build` 正規重生成）

**新**
- `packages/shared/src/content/locustCrescent.test.ts`（守衛＋@visual-proof）
- `docs/_reports/crescent_visual-proof_20260825-2130/`（frames.md＋5 PNG）
- 本報告

## 七、主 session 待收清單 ⚠️

1. **`pnpm skills:sync`**（含 skillremake:json）—— 落地 h01u 的彎月刃節點（standalone＋champion）。
   在那之前 `locustCrescent.test.ts` 第二條**刻意紅兩列**（h01u standalone＋鏡射），訊息自帶修法。
2. **`pnpm content:build`** —— bundle 重打（n01c/nbbc 四檔＋travel 模板已改）。
3. apconv:build / tiers:apply：本批節點不含 tier 欄位 ⇒ 預期 no-op（跟著 sync 走）。
4. **git add 之後 `scripts/visual-proof.sh` 的證據目錄轉為已追蹤**（本 lane 未改 apps/client/src/{vfx,render} ⇒ 該閘不因本批觸發）。
5. ⚠️ 別的 lane 的一筆：`review:check` 點名 `o00e_visual-proof_20260825-1200` **未登記 rollback**（monsoon 族，⛔ 不是本 lane 的，不代辦）。

## 八、owner 裁決項（⛔ 不擋出貨）

1. **首發串流競速**：crescent.glb 首載 ≈1s > 飛行 0.4s ⇒ 每場第一發可能整段看不到。
   選項：(a) 接受（第二發起可見，TORNADO 同性質）(b) modelFx 家族做預載（引擎票，非本批）。
2. **crescent 的烘焙音效**：原作 mdx 自帶 `wc3.wandofneutralization`（聲檔已在 audio-map），
   但 travel 家族是 SOUNDLESS（staging ④ 禁 preset 節點帶 soundKey）。要接的話是**模板層**的
   決定（travel 補 soundKey 格並移出 SOUNDLESS），⛔ 不歸本批替 owner 挑。
3. **曹操（87-01 大紅蓮斬）出貨時**：五道紅月斬（scale 2 · tint [1,0.1176,0.1176]）該接到
   `godie-o02o.q` —— 守衛第三條會在那天自動紅並指路。
