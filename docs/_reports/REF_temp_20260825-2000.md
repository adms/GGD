# REF lane —— 「引用的指令必須真的存在」自動化閘 · 完整報告

- 日期：2026-08-25 20:02
- 交付：`packages/shared/src/ops/referencedCommandsExist.test.ts`（6 條）＋
  `packages/shared/src/ops/referencedCommandsExist.exemptions.json`（豁免表，1 列）
- ⛔ 零 git 寫入、零 gh 寫入、零全域鎖指令；vitest 單檔 3 次（額度內）。

## 閘做什麼

掃**手寫 guidance 檔**的文字 —— `scripts/*.sh` · `scripts/*.py` · `CLAUDE.md` ·
`tools/parallel-gates/*.json`（當純文字掃，行號可指名）—— 抽四種引用並逐一驗證：

| 種 | 形狀 | 驗什麼 |
|---|---|---|
| (a) | `pnpm <name>`（name 含冒號） | ∈ package.json `scripts` |
| (b) | `scripts/<f>.sh` / `scripts/<f>.py` | 檔案存在（前一字元不是路徑字元 ⇒ `docs/legacy/scripts/…` 不算） |
| (c) | `python3 tools/<path>.py` | 檔案存在 |
| (d) | **引號整包**的步驟形 token（`'prose:apply'`） | ∈ package.json `scripts` |

⭐ (d) 是規格外加的一種，但它是**創始案例的形狀**：兩個已知的 `prose:apply` 幽靈
（`genguard.sh` 的 `NORMALIZERS` 集合、`preserve-before-overwrite.py` 的
`NORMALIZER_STEPS`）都是**集合成員**，不帶 `pnpm` 前綴 —— 只做 (a)(b)(c) 會漏掉
催生這條閘的那個幽靈。BARE_RE 要求兩側都是引號、段落全 `[a-z0-9-]`、首段字母開頭
（`'12:30'`、`'width:100'`、`'node:fs'` 都比不中；`node:*` 是 builtin specifier 結構性排除）。

## 誤報防線（寧可漏報）

- 佔位跳過：引用含 `<…>`、`$VAR`、`${…}`、`*` 不算（`bash scripts/genrun.sh <step>` 這種通用寫法）。
- 豁免走 JSON 表（⛔ 不塞測試碼），每列帶理由；**棘輪**：每列必須還罩得住 ≥1 個活的幽靈，
  修好了留著列 ⇒ 紅（同 guard-message-pending 的只准變短）。
- GUARD-THE-GUARD 兩條：①母體下限（實測 24 檔 · pnpm 44 · path 20 · bare 45，門檻取 15/30/12/30）
  ＋兩個哨兵（`pnpm skills:sync`、`scripts/genguard.sh` 一定要被抽到）；
  ②抽取器餵自造 fixture，四種幽靈都要抓到、佔位都要跳過（量尺先自證，同 `calibrate()` 的形狀）。

## 第一次全掃的結果

- (a) 44 個步驟名、(b)(c) 20 條路徑、(d) 45 個 token —— **全部驗證通過，零幽靈**。
- 唯一命中的是 `prose:apply` ×2，而且**NORM lane 已經修掉了**：現在那兩處是
  「2026-08-25 拿掉幽靈名」的**歷史註解**，引用的是幽靈名本身（⛔ 不是叫人跑它）。
  ⇒ 進豁免表 1 列（ref=`prose:apply`，files=那兩個檔，理由寫明是歷史紀錄）。
  ⚠️ NORM lane 若日後改寫或刪掉那兩句註解，棘輪會要求刪這一列 —— 這是預期行為。
- `CLAUDE.md:55` 的 `pnpm tsx tools/card-prose/apply_placeholders.ts`：檔案存在，
  非幽靈（`tsx` 無冒號不進 (a)；`.ts` 路徑不在 (b)(c) 母體 —— 見「已知的漏」）。

## 突變驗證（一條，最承重）

`CLAUDE.md:403` `pnpm echoloop:check` → `echoloop:checkk` ⇒ (a) 紅，訊息逐字指名
`CLAUDE.md:403 引用 \`echoloop:checkk\`` ⇒ 改回 ⇒ 綠。CLAUDE.md diff 乾淨
（動之前先跑了 `scripts/backup-rules.sh`，快照 2026-08-25_200200）。

## 已知的漏（誠實列出，⛔ 不要把綠讀成掃乾淨）

1. `tsx tools/*.ts` / `node tools/*.mjs` 形的引用不在母體（規格只點名 (a)(b)(c)；
   加開要另做誤報評估）。
2. 母體只有四類 guidance 檔 —— `docs/` 的散文、測試訊息裡的指令引用不掃
   （後者部分由 `guardMessagesNameTheGenerator` 的 genrun 步驟比對罩著）。
3. 單字 pnpm script（`pnpm typecheck`）不驗 —— 無冒號誤報面太大，刻意不收。
4. 逐行掃 ⇒ 跨行拼出來的指令字串抽不到。

## 檔案清單（留在工作樹，主 session 統一 commit）

- `packages/shared/src/ops/referencedCommandsExist.test.ts`（新）
- `packages/shared/src/ops/referencedCommandsExist.exemptions.json`（新）

建議 commit 訊息帶突變紀錄：「突變驗過：CLAUDE.md `echoloop:check`→`checkk` ⇒ (a) 紅指名行號 ⇒ 還原綠」。
