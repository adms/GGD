# DOC2 lane 報告 —— docs/開發守則大全.md（2026-08-24）

## 任務

把 CLAUDE.md 的開發守則重新編寫成一份給其他軟體專案參考的通用守則
（`docs/開發守則大全.md`）。抽掉 GGD 專屬內容，留可轉移的原則；
事故匿名化成「案例」框。

## 產出

- `docs/開發守則大全.md`（799 行，符合 500–800 的要求）
- 本報告檔

## 結構

前言＋用詞約定（判準／閘／住處／守衛／突變驗證／lane／owner／靈魂層）→
十五章（每章固定四段：原則 → 為什麼 → 怎麼落地 → 反例）→
附錄 A 採用勾選清單（34 條，含「你的閘」空欄——填不出來的那格就是還沒落地的那條）→
附錄 B 一句話索引（每章一句可背的判準）。

十五章對應指定主題：
1 元規則閘不是判準（含閘的五級強度階梯，整份骨架）
2 值只有一個住處　3 內容＝資料、程式＝機制　4 衝突優先序階梯＋被取代知識另存
5 可調優先於寫死（含 owner 旋鈕、owner 不在時的回捲開關）
6 「說了但不會發生」　7 守衛驗機制不驗數字＋八種失敗形態（⑧單獨展開）
8 看得到才算做完（五層根因＋四個洞＋量尺自證＋用詞紀律）
9 HITL 分層漏斗（Tier 0/1/1.5/2＋三個人力乘數）
10 成本紀律（測試預算分層表＋自檢 awk＋批次撈錯＋5 分鐘閘＋一天一版）
11 併行多 lane（`commit -F msg -- pathspec` 全程不 stage、amend/stash/checkout 禁令、
全域鎖、產物 hook）　12 fail-open＋fail-loud　13 註解會說謊
14 部署協定（名詞 vs 關係、煙霧測試、量最緊的碟、git ls-files）
15 開票規格摘要（指向 docs/開票守則.md——該檔撰寫時尚不存在，屬另一 lane 落點）。

## 匿名化決策

- 專案名、英雄/技能名、主機 IP、具體檔名全部抽掉；保留量到的數字
  （17.5×、245 萬 token、199 節點、25 處空宣稱、14 輪 50 分鐘、一天五次形態⑧等）
  作為「為什麼」的證據力。
- GGD 專屬機制（五級距、Colyseus append-only、ENTITY_FLAG、host-deploy 細節）
  改寫成通用形狀或併入原則；純 GGD 操作細節（skills:sync 指令名等）不收。
- 第 2 章補了一段「三個住處為什麼不是這條的例外」（有 drift 守衛盯的鏡像是接線，
  沒守衛盯的副本才是第二個住處）——原文兩條規則表面打架，通用版必須講清楚。

## 授權遵守

只新增這兩個檔；未動 CLAUDE.md 與任何其他檔；未跑全域鎖、未 gh 寫入、未 push；
commit 用指定的 `git add -N … && git commit -F … -- 逐檔` 形式。
