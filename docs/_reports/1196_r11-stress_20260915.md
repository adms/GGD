# GH#1196 H⑤ 第十一回合 500 隻壓力／清理量測（2026-09-15）

> ⚠️ **量測環境**：開發機 Apple M5 Max（18 核／128 GB）、Node v25.9.0、單一行程、**無其他負載**。
> ⛔ 這**不是**正式站（Mac mini M4）的數字，也⛔ 不是 4 vCPU CI 的數字。
> ⭐ 閘只看「飽和」剖面的 p99；門檻 `GGD_R11_STRESS_P99_MS`（預設 = `TICK_MS` 33.333 ms）。

## 指令（可重跑）

```bash
cd apps/game-server
NODE_OPTIONS=--expose-gc ./node_modules/.bin/tsx scripts/round11-stress.ts --json out.json
```

量尺：`src/match/round11StressHarness.ts`（與 `src/match/round11Stress.test.ts` 共用同一支）。
設定：出貨 `content/config/arena-rules.json` 原樣（`maxAliveZombies` 500 · `durationSec` 600 · `spawnRampSec` 120 · 波次表）。
英雄：**出貨內容**（`ContentLoader` 載入）、12 隻 bot、seed 7。前十回合時序縮短（⛔ 不影響第十一回合長度）。

## ⚠️ 兩種負載 —— 分母不同，⛔ 不要混著讀

| 剖面 | 定義 | 峰值存活 / 出貨上限 | 回合跑了多久 |
|---|---|---:|---:|
| **natural** | 出貨生怪門自己生 | **59 / 500**（⛔ 沒到上限） | 224.9 s（被「活著的英雄歸零」收掉，⛔ 沒跑滿 600 s） |
| **saturated** | 每 tick 用出貨 `spawnMob` 補到那一刻的上限；英雄血量維持滿 | **500 / 500**（第 120 s 起一直滿） | **600 s**（18,000 ticks） |

⭐ ⇒ 「配置上限 500 隻＋完整回合時長」**只有 saturated 量得到**；它是**最壞情況的負載剖面**，⛔ 不是自然會發生的一場。

## tick 成本（ms，nearest-rank，與 `tickHealth.ts` 同定義）

| 剖面 | p50 | p95 | p99 | max | 超過 33.3 ms 的 tick |
|---|---:|---:|---:|---:|---:|
| natural（出貨內容） | 0.086 | 0.581 | 1.959 | 14.458 | 0 |
| **saturated（出貨內容）** | **2.455** | **3.196** | **3.902** | 16.628 | 0 |
| saturated（出貨內容，同日第一次跑） | 2.751 | 4.144 | 5.310 | 116.679 | 4 |
| saturated（骨架英雄） | 2.205 | 3.196 | 4.390 | 55.704 | 2 |

⚠️ max 在不同次之間差很多（16.6 / 55.7 / 116.7 ms）⇒ 單一最慢 tick 受 GC／排程影響，⛔ 不要拿它當穩態成本。
最慢 5 個 tick 在最後一次跑是集中在 262.4–262.6 s 的一段（連續幾個 tick 14–17 ms）。

## 記憶體（heapUsed，取樣前先 GC）

| 剖面 | 進第十一回合 | 比賽結束後 | Δ |
|---|---:|---:|---:|
| natural | 170.8 MB | 172.9 MB | +2.1 MB |
| saturated | 172.8 MB | 174.9 MB | +2.1 MB（期間死掉 9,003 隻殭屍） |

## 清理（兩種剖面都一樣）

| 項 | natural | saturated |
|---|---|---|
| 一般＋特殊超過當下上限的 tick | 0 | 0 |
| 含王超過上限的 tick（王走自己的門、吃每回合配額） | 0 | 3 |
| 殭屍死亡 / 屍體洩漏 / `world.mob` 死列 | 194 / 0 / 0 | 9,003 / 0 / 0 |
| 實體（`world.transform.size`）進場 → 結束 | 13 → 12 | 13 → 12（滿載時 512–518） |
| 結束後：殭屍表 · 規則武裝 · 生怪區 · 王配額 · 結束後預警圈 | 0 · false · 0 · 0 · 0 | 0 · false · 0 · 0 · 0 |
| 結束後：換邊帳本 `round11Possessions` | **12** | **3** |

## 發現（⛔ 本票未改行為）

1. **換邊帳本在比賽結束後不清空**（上表最後一列）。`round11FullFlow.test.ts` 那條
   「換邊狀態留到結算之後 = 0」在它的夾具（`durationSec: 6`，沒有人被換邊）裡是**空斷言**；
   出貨設定跑一場就是 12。⚠️ 但它可能是**刻意的**：`round11ReconnectState` 回「旁觀」那一行的註解寫
   「⛔ 但仍看得到最後結算畫面」、`round11ScoreFor` 算分時讀它，而客戶端 `round11View` 在 `phase !== "combat"` 時整個不畫。
   ⇒ 列為待裁決，見 lane 報告。
2. `MatchController.round11DenyCommand`（GH#922「四種濫用全部撞這一道門」）**零個出貨呼叫端**，只有測試在叫。
3. 出貨波次表的 `reviveCircle`（權重 5）每次抽到都走 `round11EventUnhandled`（已知，`round11WaveEvents.test.ts` 釘住）。
