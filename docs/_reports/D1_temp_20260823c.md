# D1 —— 橫放 beam 從未生效（GH#607）＋ 38-03 落點環

## ① B2 的宣稱：**逐行確認為真**，而且比宣稱多一站

B2 說「`GameApp.ts:867` `modelDocFor` 投影成 `{glbPath,scale}`，丟掉
`fxLongAxis`/`fxSpawnHeight`」。量到的完整鏈路（⛔ 不是推論）：

| 站 | 檔:行 | `fxLongAxis` / `fxSpawnHeight` 在不在 |
|---|---|---|
| 內容 | `content/models/imported.netherstrike.json` | ✅ `fxLongAxis:"y"`（`imported.fireblast` 是 `"x"`）|
| schema | `packages/shared/src/content/schema/model.ts:165,182` | ✅ 兩格都收 |
| ContentDb | `apps/client/src/content/ContentDb.ts:532 modelFor()` | ✅ 回**整份** `ModelDoc` |
| **接縫** | **`apps/client/src/GameApp.ts:867`** | ⛔ **`{ glbPath: doc.glbPath, scale: doc.scale }` —— 兩格在這裡死掉** |
| 型別（第二站，B2 沒點到） | `apps/client/src/vfx/VfxSystem.ts:194` | ⛔ 宣告成 `{ glbPath: string; scale?: number } \| null` ⇒ 就算補進上一行，**object literal 的 EPC 會讓 tsc 拒絕**，逼下一個人再投影一次 |
| rig | `render/modelFxRig.ts:208,220` | ✅ 兩格都讀 —— 讀到的**永遠是 `undefined`** |

⇒ `modelFxAxisCorrection(undefined)` = 恆等 ⇒ **|cos(長軸, 行進方向)| ≡ 0**
（突變跑出來逐字就是 `expected +0 to be close to 1`），而 `fxSpawnHeight` 恆為
`y = 0` ⇒ 每一具移動模型貼地拖行。

⚠️ **每一個零件都是對的，只有它們的組合是空的**（第一·五守則的形狀）。

## ② 修：⛔ 不是「把兩格補回去」，是**不要投影**

`render/modelFxRig.ts` 新增 `modelFxDocFor(doc: ModelDoc | null | undefined)`
—— 出貨路徑上「`model@1` → rig」的**唯一**接縫，body 是 `return doc ?? null`。
`GameApp.ts` 改成 `modelDocFor: (k) => modelFxDocFor(this.contentDb.modelFor(k))`。

⭐ 為什麼是「不投影」而不是「補兩格」：補字面值的話，`model@1` 上加**第三格** fx
欄位時照樣會漏 —— 那是第〇·四守則說的第二個住處。現在「rig 讀得到哪幾格」
只由 `ModelFxModelDoc` 這個**子集型別**說了算，新欄位**零行接線**。

## ③ 守衛：⛔ 不再自己注入 stub

`render/modelFxWireContract.test.ts` 上一版第 88 行是
`resolveModel: () => ({ glbPath: "x.glb" })` —— 一份**測試手寫的模型文件**，
所以 #607 在它眼裡不可能存在（失敗形態⑤，與 #606 同一個病的第二次）。

現在整條線都是出貨的東西：
出貨內容（`ContentLoader` 讀 `content/`）→ 出貨技能（悟空 09-04 龜派氣功
`godie-ogrh.r`，preset `tpl-beam-roll` → `imported.netherstrike`）→ 真的
`SimWorld` 發的 `modelFxSpawn` → **出貨接縫** `modelFxDocFor(Models.tryGet(k))`
→ **真的 `VfxSystem`**（⛔ 不直接戳 rig —— 那會跳過 `VfxContext` 的投影）
→ 出貨場景樹上的 `modelfx-axis-*` 節點世界矩陣。

斷言是**機制**：|cos(宣告的長軸, sim 給的行進方向)| ≈ 1。
⛔ 不驗正負號（長軸是線不是箭頭）、⛔ 不抄任何座標／角度／出貨數值。
第二條釘「接縫⛔ 不投影」本身（`toBe` 同一性），因為 `fxSpawnHeight`
出貨樹一份都沒填、沒辦法用畫面驗。

**突變（一批一條，最承重的那一行）**：把 `modelFxDocFor` 的 body 改回舊投影
`doc ? { glbPath: doc.glbPath, scale: doc.scale } : null`
→ 兩條都紅，訊息逐字：
`⛔ 長軸沒有被擺到行進方向上 —— 這正是 #607 的形狀: expected +0 to be close to 1`。
改回來用 `Edit`（⛔ 不是 `git checkout`）。

## ④ 38-03 落點環 radial → orbit（抄 B2 在 38-002 做的）

`imported.tectonicfury` 那一組原本 `radial/count:12/distance:6.42/speed:30`
⇒ 十二具**共用施法者一個座標**往外飛，6.42/30 ≈ **0.21 秒**噴完就散光。
JASS（`tools/jass-dragon/out/A09I.staging.json`）是**半徑一圈上的十二個位置**，
各站一隻有壽命的傀儡 ⇒ `orbit`（`ringPoints`，每一具各自一個座標、travel 0）。

改：`"path": "orbit"` ＋ `"lifeSec": 3.0`（`zSpawnModelFx.refine` 要求 orbit 必填；
數字**照抄** B2 在 38-002 用的 JASS `timedLifeSec`，⛔ 沒有新編數字）。
⛔ 一個傷害／平衡欄位都沒動 —— 純演出。

### ⚠️ 柵欄外延兩檔，理由寫在這裡

38-03 是 **E 槽**，而 `abilityMirror.test.ts` 對 Q/W/E/R 做**逐欄位**鏡射稽核
（`content/champions/<cid>.json` 的 `abilities.E` 是同一份資料的第二個副本）。
⇒ 只改 `content/abilities/*.e.json` 會讓那條全域守衛**當場變紅**，而它擋的是
每一條 lane 與主 session 的 `skills:check`。B2 自己在 fcabd849 的訊息裡逐字寫過
這件事（「`.e` 一定要連 champions 一起改，而 champions 不在這條 lane 的柵欄裡」），
所以他當時選了只做 `.ex`。

我選擇**把 `content/champions/godie-u010.json` 與 `godie-uvng.json` 一起改**
（各只動 tectonicfury 那一個節點的兩行），理由：留一個紅的全域守衛的成本
遠高於多動兩個檔。`git status` 顯示這兩檔在我開工時是乾淨的，commit 走逐檔
pathspec。⭐ **rollback**：`content/` 是線上 live bind-mount ⇒ owner 在後台
「內容管理」把 `path` 改回 `radial`／刪掉 `lifeSec` 就回原狀，⛔ 不需要重建映像。

## ⚠️ 兩個**不是我造成的**紅（都要主 session 收）

| 紅 | 根因 | 我為什麼沒修 |
|---|---|---|
| `fieldAdoption.test.ts` —— `spawnModelFx.path=orbit` / `.lifeSec` **STALE EXEMPTION** | **HEAD 就已經紅**：`git show HEAD:content/abilities/godie-u010.ex.json` 已含 orbit+lifeSec（B2 的 fcabd849），而 `EXEMPTIONS`（該檔 2797 / 2809 行）沒跟著刪 | 檔案在柵欄外；修法是**刪掉那兩列**（測試自己的錯誤訊息就是這麼寫的）。我的改動只是把 2 份變成 6 份 |
| `modelFxStagingContract.test.ts` —— `SOUNDLESS_TEMPLATES` 有 `tpl-radial-burst` 兩列 stale | 另一條 lane 正在飛：工作區的 `modelFxPreset.ts` 剛加了 `SOUND_FIELDS`、`tpl-beam-roll.json` 剛加了 `soundKey`/`arriveSoundKey` | 那是別人的 lane，⛔ 不碰 |

`abilityMirror.test.ts` ✅ 4/4 綠（鏡射一致）。`pnpm typecheck` EXIT=0。

## ⛔ 沒做（刻意）

- **沒跑 `pnpm content:build`** —— 動了 `content/` 4 個檔，`bundle.json` /
  `_index.json` 現在是**過期的**，請主 session 統一跑一次再 `git add content/`。
- **沒動 `VfxSystem.ts:194`** 那個窄型別（柵欄外）。它現在不擋執行期
  （物件是同一個參考直接傳過去），⛔ 但它仍然是一顆地雷：下一個人照著那個型別
  寫 object literal 就會再投影一次。⭐ 一行的正解是把它換成
  `import type { ModelFxModelDoc } from "../render/modelFxRig"`，建議開一張票。
- **沒填任何 `fxSpawnHeight`** —— schema 的註解寫明 ABSENT = 0 = 今天的行為，
  「哪一根該抬高」是 owner 在編輯器填的數字，⛔ 不是我挑的（第一守則）。

## 🚨 事後：這個 commit **把另一條 lane 的在飛工作一起送上車了**

`c342140f` 的 `apps/client/src/GameApp.ts` 是 **+216 行**，而我只改了那 8 行接縫。
其餘是 **GH#612（分割畫面逐格螢幕演出：`installScreenCues` / `cueLayers` /
`routeScreenCue` / `installSplitScreenCueRouter` / `AdaptiveQuality` 那一段）**
—— 那條 lane 的未提交改動當時就在工作樹裡，而 `git commit -- <pathspec>` 只限制
**哪些檔**，⛔ 擋不住同一個檔裡別人的未提交內容。

⭐ CLAUDE.md 逐字：「pathspec 規則只擋得住我把別人的東西送上車，⛔ **擋不住別人把
我的送上車**」—— 這次是**反方向**，同一個洞。owner 對這個形狀的裁決是
「**沒關係 ⛔ 不要回捲**，但推理要改貼 issue 留言」。

⇒ ⛔ **不要 `git reset` / `--amend` 去「還」那些行** —— 程式碼一個位元組都沒少，
少的是**票號與突變紀錄**。請主 session 告訴 GH#612 那條 lane：
他們的改動已經在 `c342140f` 裡，把他們的推理改貼成 issue 留言即可。
（`pnpm typecheck` 在含他們改動的狀態下 EXIT=0，⛔ 沒有壞掉的東西。）
