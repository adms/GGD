# K7 #642 殭屍王介紹／結算橫幅 —— 半秒淡入半秒淡出

> owner（2026-08-24，逐字）：「殭屍王介紹 特殊殭屍結算 太佔螢幕 說明半秒淡出半秒就好」

## 做了什麼

橫幅／結算面板的時間軸從「hold 4,600ms（橫幅）／8,200ms（結算）＋ 480ms 退場」
換成 **淡入 0.5s → 淡出 0.5s → 消失**，⭐ **淡入＋淡出就是全部壽命**（沒有停住的一段
—— owner 嫌太佔螢幕的正是停住那一段）。兩張橫幅（降臨＋王/特殊殭屍結算）吃**同一對**旋鈕。

## 後台兩格（三住處齊）

| 住處 | 位置 |
|---|---|
| 出貨 JSON | `content/config/world-cues.json` 新 `hud` 塊：`mobBossFadeInSec` / `mobBossFadeOutSec`（各 0.5，Zod 界 0–10 秒） |
| Zod `DEFAULT_*` | `packages/shared/src/content/schema/config/worldCues.ts`：`zHudCues`（doc 上 optional，理由寫在欄位 JSDoc：`readWorldCues` 只重建 point/line，缺塊逐格退回） |
| 後台 | 「世界演出」頁兩列（`apps/admin/src/configForms.ts`）。⚠️ 這兩列已被 lane #643 的 commit `e9179ae1` 掃走（同檔併行，owner 既有裁決「沒關係不回捲」）—— 內容正確在 HEAD |

一鍵 rollback：兩格調大＝拿回停留感（淡出 2 秒 ≈ 舊的存在感）；兩格都 0＝橫幅完全不畫（獎金照發）。

## 讀端

`mobBossModel.ts`：`mobBossFade()`（逐格 typeof、夾 0–10s，⛔ 不整份 parse ——
與 `mobSettlementWordingFromDoc` 同理由）→ `bossLifetime(view, now, fade=mobBossFade())`，
phase `"live"` 退役、改 `"in" | "out"`。`KillCombo` 的讓位讀同一支 ⇒ 讓位窗自動縮成 ~1s。
`MobBossOverlay.tsx` shell 補 `transition: opacity/transform 100ms` —— 100ms poll 在 1s
壽命下會是 5 格階梯，transition 補成連續。

## 被取代的知識（⛔ 不無聲消失）

「橫幅要蓋住自己的 4.40s 恐怖音效」（2026-07-28 規則、`BOSS_BANNER_MS=4600` 釘音效）
被 #642 取代：音效照播，畫面刻意不陪到底。原委記在 `mobBossModel.ts` 檔頭；
`BOSS_BANNER_MS` / `BOSS_SETTLEMENT_MS` / `BOSS_EXIT_MS` / `bossHoldMs` 刪除（零引用驗過）。

## 守衛

`mobBoss.test.ts`：邊界從**出貨 JSON 餵出貨 resolver** 推導（⛔ 不釘 0.5）；
fake timers（`toFake:["performance"]`）走一遍出貨容器 掛載→半亮→淡出→消失。
**突變**（一批一條）：`bossLifetime` 的過期行拿掉 → 4 紅（含掛載時序與 stale-retire）。已還原。

## 測試

client（mobBoss + performanceEventsHaveConsumers）71 綠、admin configForms 20 綠。
`pnpm typecheck`：**唯一紅是 apps/admin `mobWaves*.test.ts` 缺 `normalMobShadow` 標籤**
—— 別條 lane 的 in-flight（`mobWaves.ts` / `arenaRules.mobWaves.ts` 非本柵欄），非本批造成。

## 留給主 session

- `pnpm content:build`（本 lane 禁跑）—— `world-cues.json` 動了，bundle 要重生。
- 柵欄外一行過期註解：`bossIntroModel.ts:425` 仍寫「BOSS_BANNER_MS 4.6s」。
