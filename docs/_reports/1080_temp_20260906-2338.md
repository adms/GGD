# GH#1080 —— `spawnModelFx.anchor` 值域一份 tuple 推導（lane 報告 2026-09-06）

> 狀態：**完成**（兩條 AC 都達成；兩份 sim union 在柵欄外，改由 `tsc` 逼相等，見「需要主 session 動的」）。⛔ 未 commit。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `packages/shared/src/content/schema/effects/spawnModelFx.ts` | ⭐ 新增 `export const MODEL_FX_ANCHORS = ["self","point","target","bone"] as const` ＋ `export type ModelFxAnchor`（緊接 `MODEL_FX_PATHS`，同一個形狀）；`anchor: z.enum(MODEL_FX_ANCHORS)`（⛔ 不再手抄）。說明文字一字未動 ⇒ `spec:build`／`caps:export` 的產物逐位元不變（enum options 相同） |
| `packages/shared/src/content/templates/expand.ts` | 只動兩處：import 多一個 `type ModelFxAnchor`（來自 schema）；拿掉本地 `type ModelFxAnchor = NonNullable<Extract<EffectDef,…>["anchor"]>` 那一行（第五份反推）。`modelFxAnchorsFor()` 本體**不動**，仍讀 `zSpawnModelFx.shape.anchor.unwrap().options`（＝tuple） |
| `packages/shared/src/content/schema/effects/spawnModelFxBone.test.ts` | 重寫（71 行，⛔ 不再 `readFileSync` 掃字串）：① Zod `options` ≡ `MODEL_FX_ANCHORS`、含 bone、bone＋attach＋boneOn 一組 schema 收得下；② **`Same<A,B>` 型別相等**：`SpawnModelFxVariant["anchor"]` 與 `ModelFxPlacementParams["anchor"]` 都 ≡ `ModelFxAnchor` —— 閘在 `tsc`（vitest 不做型別檢查，檔頭寫明）；③ 詞彙同 `spawnVfx`：讀真的 `zSpawnVfx.shape`（`at` 含 bone、`boneOn` options 兩邊相等、`attach` 是 ZodString）；④ 擺位層「bone 當 self」讀真的 `modelFxInstancesFromFrame`（bone ≡ self、≠ point），⛔ 不再找註解裡的「渲染層」三個字 |

## 量到什麼

- `grep -rn '"self", "point", "target", "bone"' packages/shared/src` ⇒ **只剩 tuple 那一處**（AC①）。⚠️ `apps/editor/src/forge/forgeWritebackTemplateGate.test.ts:184` 另有一份同樣的字面值 —— 它是一個**夾具**（把四個值寫進 `tpl-beam-roll.anchor.values` 去驗回寫閘），⛔ 不是值域的第二個住處；柵欄外，⛔ 沒動（可改讀 `MODEL_FX_ANCHORS`）。
- 模板 `anchor.values` 與 Zod 一致：`paramsSchema.test.ts`「表單 enum ⊆ 出貨 Zod」與 `modelFxAnchorBone.test.ts`「anchor.values ⊆ modelFxAnchorsFor(t)」都綠 ⇒ `tpl-beam-roll`／`tpl-locust-strike` 的 anchor 值**沒有**與 Zod 不一致，⛔ 未動任何模板。
- 展開結果逐位元不變：`paramsSchema`（含 35/35 過 `zAbilityDoc`）· `stack` · `expand` · `modelFxAnchorBone` · `spawnModelFxBone` · `templateDefaultsCast` 共 59 條綠；`pnpm typecheck` EXIT 0。

## 突變紀錄

無（票文逐字：體驗層，一條薄守衛，⛔ 不做突變）。

## ⛔ 沒做到的（柵欄）

票的 Scope 要兩份 sim 型別改成 `(typeof MODEL_FX_ANCHORS)[number]`：
- `packages/shared/src/sim/effects/variants/spawnModelFx.ts:102`（`anchor?: "self" | "point" | "target" | "bone"`）
- `packages/shared/src/sim/effects/modelFxPlacement.ts:38`（同上）

兩檔都在本 lane 柵欄外 ⇒ ⛔ 沒動。⭐ 但它們**已經被閘住**：漂了（少一個或多一個成員）`pnpm typecheck` 在 `spawnModelFxBone.test.ts` 的 `simAgrees`／`placementAgrees` 那兩行紅（TS2322）。

## 需要主 session 動的柵欄外檔案（各一行）

```ts
// sim/effects/variants/spawnModelFx.ts（leaf；type-only import 會被抹掉，⛔ 不引入 runtime 環）
import type { ModelFxAnchor } from "../../../content/schema/effects/spawnModelFx";
  anchor?: ModelFxAnchor;
// sim/effects/modelFxPlacement.ts
import type { ModelFxAnchor } from "../../content/schema/effects/spawnModelFx";
  anchor?: ModelFxAnchor;
```

（若不想讓 sim 對 schema 有型別環 —— schema 今天已 `import type { ModelFxPathName }` 自 variants —— 也可以照票文原案把 tuple 搬到 variants 那個 leaf、schema 改成 `import { MODEL_FX_ANCHORS }` 值 import；`Same<>` 閘兩種擺法都成立。）

- 主 session 收工跑 `pnpm skills:check`（本 lane 只准 `*:check`，且 enum options 相同、describe 未動，預期無變）。

## 順手發現但沒動

- 上面那個 editor 夾具字面值（非缺陷，⛔ 未開票）。

## 改了哪些檔

- `packages/shared/src/content/schema/effects/spawnModelFx.ts`（tuple ＋ `z.enum(MODEL_FX_ANCHORS)`）
- `packages/shared/src/content/schema/effects/spawnModelFxBone.test.ts`（重寫）
- `packages/shared/src/content/templates/expand.ts`（import 一行 ＋ 刪本地型別別名）
