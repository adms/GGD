# H5 — 基準線與指紋不再手動（2026-08-23）

## ① parity 基準線：兩邊一起動 ⇒ 自動視為新基準

`packages/shared/src/content/abilityCodeParityForms.test.ts`：

- **兩邊一起動**（`both`／兩邊**同時**新增／兩邊同時消失）＝ 正常技能改動 ⇒
  測試自己把 `abilityCodeParityForms.baseline.json` 改寫成新現況（console 提醒 `git add`）。
- **只動一邊**（`base-only`／`alternate-only`／只有一邊長出新編號）⇒ 照樣紅。
- ⭐ **逐筆合併，⛔ 不整份覆寫** —— 同一輪紅著的單邊改動不會被順手洗白；
  這是「判準不可變弱」的關鍵一手。
- `GGD_FORM_PAIR_DUMP=1` 留著，只給「刻意的單邊形態差異」用。

**突變驗證**（同一輪三個檔一起改，一次 vitest）：
- 04 對子只改本體 `godie-hjai.q.json` ⇒ **紅**，訊息指名「只有**本體**動了」；
  基準線 diff **沒有** 04 的格（沒被吸收）。
- 06 對子兩邊同改 `godie-ucrl.w.json` + `godie-u034.w.json` ⇒ `[auto-baseline]`，
  基準線 diff **只有** `06-02`。
- 全部還原後重跑 ⇒ 綠，基準線位元組不變。

## ② Codex 合約指紋：refresh_docs.py 寫那幾行

`tools/skill-remake/refresh_docs.py` 新增 `build_fingerprint_block()` + `splice_fingerprint()`：

- 檔頭「指紋 + 對帳摘要」**4 行**改由腳本產生，來源是
  `docs/editor-contract/ggd-runtime-capabilities.json`（單一住處；`skills:sync` 裡
  `caps:export` 恰好排在 `skillremake:docs` 前面，順序天然正確）。
- ⛔ 刻意**不寫日期** —— 時鐘欄位會讓 `--check` 逐位元組比對永遠不相等（GH#389/#426 的形狀）。
- `--check` 一併驗指紋行；錨點消失（有人刪了指紋行）會 assert 炸掉，⛔ 不是靜默略過。
- 文件其餘部分不動：`git diff` 恰好 4 行換 4 行。
- `codexContractFresh.test.ts`（直接對引擎現算指紋）不變，仍是第二道獨立閘。

## 驗證

- vitest ×3：①三檔一起（parity + codexContractFresh + skillRemakeDocsFresh）綠
  ②突變紅（見上）③還原後綠。
- `refresh_docs.py --check` EXIT=0。
- tsc（packages/shared）：我的檔 0 錯；**唯一的紅在別條 lane 的在飛檔**
  `packages/shared/src/ops/shipGateScript.test.ts`（工作樹 M，`GGD_DEPLOYED_REF` 欄位），與本 lane 無關。

## ⚠️ 給主 session

- `docs/英雄技能第一批重製-90支.md` 在我跑 `refresh_docs.py` 時被連帶重生成（+44 行，
  `onReflectSuccess`/`screenFlash` —— `batch1.py` 已被別條 lane 更新但文件沒跟上）。
  它在柵欄外，我**沒有 commit** —— 最後統一 `skills:sync` 時會一起收進去。
