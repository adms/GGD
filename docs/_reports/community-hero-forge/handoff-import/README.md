# 完整原稿與模型資料夾匯入驗收

實際 Editor 資料夾選取完成 37 份獨立草稿、222 槽原始描述／requiredRefinement，以及 19 份隨附 GLB 與六用途綁定。`acceptance.json` 逐份核對保存後的完整原文、模板參數、演出資料和模型 SHA。原交接檔及既有作品保留。

`reopen-and-preview.json` 記錄不知火舞的風格替代標示、衛宮士郎的 Archer 互通來源、阿薩謝爾 THE END OF SON 原文及處理說明重開後仍存在。截圖中「六槽編譯與模擬已通過」是基礎模板結果，不是原設計已完成；畫面同時明列待補項。

`missing-models.json` 證明隨附 GLB 缺失時拒絕匯入，38 份既有本機草稿（含 37 份本批作品及先前測試草稿）的 key／token 全部不變。素材先逐一驗證才建立英雄草稿；若儲存本身失敗，介面回報已保存數量，不假裝完整成功。

新增的來源資料與逐槽作者處理說明經共用 schema 驗證，隨既有英雄 ZIP 往返；它們不產生審查裁決。模型來源 SHA 必須對應當前 uploadedModel；改用另一個模型時清除過期來源標示。接入工具不會將先前離線 ZIP 當成可投稿版本。

模型表單改為逐行六用途選單。另修正暫停預覽的粒子暖機檢查：渲染後立即記住實際非零粒子數，再讓出下一個瀏覽器影格，避免 UI seek 清掉已出現的粒子造成假逾時。真正未生成的緩衝仍在相同 750ms 有界期限回報失敗；未放寬期限或視覺標準。

驗證：Editor 86 檔／552 項回歸通過；原稿／ZIP 14 項通過；原稿欄位鎖定及改模型來源清除測試通過；暖機 22 項通過；Editor TypeScript 通過。完整角色機制、原作特效／音效、正式投稿及上線尚未完成。

Content API／Main importer 的 10 項整合測試通過。三項總檢查並行執行，`editor:accept:release` 與 `coord:check` 通過；`skills:check` 因既有 2026-09-06 訊息帳本未對應項失敗，原始記錄保留於本目錄，未製造帳本映射來掩蓋。
