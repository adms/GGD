# lane H（第二輪）明細 —— 光束砲家族 #767 / #721

```
HEAD(進場) = 1ec4d9167cde38d53ebafed54aad8836a2d89481
commit     = 62f75a51
柵欄       = apps/client/src/render/** · apps/client/src/vfx/** · content/ability-templates/**
```

## 一、結論（先講）

| 票 | 結果 | 終端證據 |
|---|---|---|
| **#767** | ✅ **關了** | `docs/_reports/beamshape_visual-proof_20260827-0600/` ＋ 新閘 ＋ 突變 |
| **#721** | 🟡 **留開**（三支答完一支） | 同上；剩 59-03 AT力場（前置 #650）與 42-04 世界終結（另一個家族） |
| **#803** | 🆕 **開了** | 金橘色住在沒轉進來的 `PRE2` 粒子 chunk（轉檔器，柵欄外） |

## 二、這一輪到底做了什麼

兩張票的稽核（2026-08-27）逐字說擋住關票的**只有一件事**：
唯一量過形狀的證據（`beamverify_20260826-1200`）比它要驗的那次重建（`bd24b4af` 13:29）
早 **89 分鐘**，也早於 `a0207a0f`（additive，20:57）。
⇒ **缺的不是工具，是一次在正確版本上的量測。** 這一輪就是那一次。

台子 `beam-audition.html?ability=godie-e002.e`（`client-beam` :39673），
真 `SimWorld` → 出貨 `godie-e002.e` → 真 `modelFxSpawn` → 真 `VfxSystem`/`ModelFxRig` → 真 `.glb`。

### 量尺先自證
`calibrate()` **571,536** 亮像素 · `beamshape.py` 合成「一道」`gaps=0`／「六方塊」`gaps=5` ✅
· ⭐ 凍結動畫後同一設定連拍兩張 **19,630 / 19,630 ＝ 底噪 0**（第一輪沒凍結成功：7,388 → 17,216，已作廢重量）。

## 三、量到的（逐條，⛔ 不是形容詞）

| 性質 | 原作 | 08-26 12:00 | **08-27 HEAD** | 判 |
|---|---|---|---|---|
| 長寬比 | 6.43–20.75 | 2.28–3.83 | **10.23–53.69** | ✅ |
| 暈:核 | 1.27–3.06 | 1.00–1.02 | 1.00–**2.18** | ⚠️ 峰值過 |
| `gaps` | 2–3 | 0–2 | 1–6 | ⛔ |
| sat | 0.47–0.54 | 0.65–0.69 | **0.012–0.061** | ⛔ 白色 |

肉眼（`AB_saber_vs_ggd_20260827.png`）：**修前一顆沿線移動的橘色火球 → 修後一道橫貫敵人線的連續光束。**

## 四、⭐ 三個新發現

1. **白色的根因是貼圖，⛔ 不是層數**（解掉 `stockglow_20260826-1520` 留的兩個候選）。
   直接讀 `.glb` 貼圖像素：`revivehuman` 5 張裡 **4 張 sat 0.000–0.066**，第 5 張 `TeamGlow0` 是**純紅**。
   ⇒ 疊灰只會更亮。**沒有任何混合模式做得出金橘色。** ⇒ #803。
2. **`scaleAxis` 對彩度是承重的**（凍結 A/B/C，逐位元可逆）：sat **0.029 ↔ 0.377（13×）**。
   在此之前它被當成純粹的「長度旋鈕」。
3. **逐層隔離證明 additive 真的在出貨路徑上生效**：5 個 primitive 各自單獨開**都是 0**，
   合起來 **28,923** —— 亮度是疊出來的。

## 五、⛔ 我自己推翻掉的兩個猜測（⛔ 不要再走一次）

| 猜測 | 量到的 |
|---|---|
| 「模型是一疊分離的卡片所以才一排」 | ⛔ **錯**。逐**三角形**投影到長軸做區間聯集：`revivehuman`/`awaken`/`netherstrike`/`flamestrike1` 覆蓋率**全是 1.000、零空隙**。柵欄是**亮度低於門檻的段** |
| 「`scaleAxis` / `spinDegPerSec` 造成一排」 | ⛔ **錯**。3.12→1 之後 gaps 6→**8**（更糟）；roll 掃 0–90° 全在 4–6 |

⚠️ 兩個都是我先寫下推測、再去量才發現錯的 —— 記在這裡是為了讓下一輪讀得到
（CLAUDE.md：模板預設裡的推測會變成「原作的結構」）。

## 六、落地的閘（in-fence）

`apps/client/src/render/beamRenderLengthMatchesReach.test.ts`（`@visual-proof`，233 行含檔頭）

出貨 9 個 `tpl-beam-roll` 節點的 **渲染長度 ÷ 宣告射程**，逐支跑真鏈
（出貨 JSON → 真 sim → 真 `VfxSystem` → 真 `modelFxRig` → 世界跨距，⛔ 不讀 `scaling` 屬性）：

| 技能 | 渲染 | 宣告 | 比值 |
|---|--:|--:|--:|
| 20-03 ×2 | 13.99 | 14 | **1.000** |
| 59-04 | 8.25 | 8.25 | **1.000** |
| 90-04 ×2 | 11.98 | 12 | 0.999 |
| 08-03 ×2 | 12.01 | 12 | 1.001 |
| 09-04 ×2 | 12.02 | 12 | 1.002 |

⭐ **為什麼它不是重複**：`modelFxScaleAxis` 只驗「比值不是方塊」——
`scaleAxis:[1,1,8]`（打 8 畫 36）在那條閘上一樣是綠的。這一條驗**絕對長度**（第一·五守則）。
內含 sentinel（拿掉 `scaleAxis` 必須被抓到 ⇒ 這條閘不可以是空的）。

**突變**：`modelFxRig.ts` `root.scaling.set(s*ax…)` → `setAll(s)`
⇒ 紅，9 支同時掉到比值 **0.141–0.374**，訊息逐支指名技能與兩個數字。改回 → 綠。

## 七、⚠️ 順帶回報（⛔ 不是我造成的，⛔ 也不在我的柵欄）

`pnpm typecheck` 有 **2 個 fail，兩個都在工作樹裡別人未提交的改動上**：
`apps/client/vite.config.ts:741-742` 與 `apps/admin/vite.config.ts:261-262`
—— `Property 'vitest' does not exist on type 'ImportMeta'`（`import.meta.vitest` 少了型別）。
我這一輪加的檔在 tsc 上**零錯誤**。

## 八、59-03 AT力場 —— ⭐ 它不是「缺證據」，是「沒有東西可以拍」

`godie-e00r.e` 是被動：`passive.ranks[].hooks` 的 `onInterval` 裡**只有一個 `shield`**，
全份文件 `spawnVfx` **0** · `spawnModelFx` **0** · `statusId` **0**
（只有一個從不觸發的頂層 `vfxKey`，`castType:self` ＋ cooldown 0）。
⇒ 護盾生出來時畫面上一個像素都不會變 —— 連拍它只會得到一排一模一樣的空白幀。
⇒ 前置是 **#650**（要先有視覺），⛔ 不是「補一次擷圖」。
