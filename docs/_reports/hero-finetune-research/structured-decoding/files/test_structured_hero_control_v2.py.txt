"""Run the same strict schema and resource policy tests on the versioned loader fix."""
import importlib.util
from pathlib import Path
import unittest
import test_structured_hero_control as tests

spec = importlib.util.spec_from_file_location('structured_control_v2', Path(__file__).with_name('structured-hero-control-v2.py'))
control = importlib.util.module_from_spec(spec); spec.loader.exec_module(control)
tests.control = control
SchemaTests = tests.SchemaTests

if __name__ == '__main__': unittest.main()
