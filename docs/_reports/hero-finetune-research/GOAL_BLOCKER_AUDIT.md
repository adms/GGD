# 完整英雄目標：凍結範圍的阻礙稽核

結論：**目標未達成；現行範圍內缺少可用的完整機制訓練目標與獨立最終驗證資料。** 不是 GPU 無法訓練，也不是要把成功標準改成分類準度。

本輪重新執行准入檢查，實際 exit **2**；獨立讀取來源審查旗標、核對凍結分類資料 hash／原文證據／英雄及來源家族切分，並從14份 IR 的動作重新確認整筆排除清單。未改來源、標籤、配方、引擎或准入狀態，未啟動 GPU。

原始收據：[GOAL_BLOCKER_AUDIT.json](GOAL_BLOCKER_AUDIT.json)。這是已有審查與實驗證據的重驗，不是假稱重新人工審查44人，也不表示所有可能配方都不可能。

## 完成條件對照

| 原目標要求 | 本次可核對的證據 | 判定 |
|---|---|---|
| Mac本機12B及可載入微調權重 | 基底／LoRA推論完成；v2八個key與319488參數逐張量相等 | 這項工程相容性已證，非模型品質資格 |
| 完整英雄設定、六槽、原文與跨技能機制监督 | 44人／264槽已有來源記錄；已記錄完整Gold及正式准入0 | 未完成 |
| 問題完整樣本直接排除 | 原14份IR皆含既知點位問題，逐筆重算與排除清單相同，剩餘0 | 排除規則有效；没有可用完整IR訓練樣本 |
| 固定資料訓練與配對比較 | 12名／72個分類訓練判斷、4名／24個開發判斷；hash、逐字引用與家族隔離有效 | 只有來源分類监督，不是完整機制生成监督 |
| 未曝光英雄／機制家族最終留出 | 原清冊所有44人標為歷史已評估；已認證fresh holdout 0 | 未完成；本次不是重建所有歷史模型呼叫的曝光鑑識 |
| 現行能力、schema、編譯與相應行為閘 | 固定引擎及scorer有可重播證據，失敗輸出不放行；不是目前Main完整驗收 | 部分完成，不能宣稱全部機制受支援 |
| 完整支援組英雄級至少95%、危險接受0 | 最新四例可編譯2/4；完整來源涵蓋未認證、局部行為6/7、反例1/3 | 不支持95%或完整安全性；全拒絕不是創建成功 |
| 微調只在完整品質改善且無重大退化才擴大 | 72步可編譯2/4→0/4；低LR維持2/4但開發JSON退步；受限解碼結構改善但機制有進有退 | 尚未有可採用的完整英雄收益，不放大訓練 |
| 特效只推荐既有模板、不得補償機制錯誤 | 本輪機制控制不做VFX生成／視覺驗證 | 沒有完整英雄＋VFX流程的驗收證據 |
| 最佳合格成品及自動創建流程 | 目前只有研究候選，未啟用模型；未經完整來源驗證的輸出保留為未驗證 | 未完成 |
| 腳本、逐例結果、模型收據與保存 | 研究分支已有原始結果、腳本、S3索引及載入勘誤；新67份文字已commit且逐檔驗SHA | 研究交付已有；不代表正式成品達標 |
| 單GPU、接電／記憶體守門、不上雲、不隱藏失敗 | 上輪212快照接電100%、無單段swap增量，終端程序完成；本輪CPU-only | 本輪遵守；不宣稱整機其他程序都停止 |

李星候選的28/28正向與12/12錯誤配方檢出仍保留。但它的准入明確為false，而且現行範圍明確暫停它與IR6；不能為了能開訓練而默默把這份候選改標成Gold。

Main遠端本輪唯讀核對仍為 `5189f41737a54140d20c61297dfa21805bfc4720`，與上一輪查驗相同。既有點位問題沿用 `question.ground-barrage-anchor`，不重複開票、不自行修改引擎、不繞過尚未取得的公開披露批准。

## 為何現在停止追加實驗

重新啟動後的連續三個目標turn都遇到同一組限制：

1. `01a08097-7e0b-7ec2-bfce-499d4f8f4744`：低學習率對照完成；仍只用來源分類，14份完整IR排除。
2. `01a080a2-d420-7512-87d4-6a78176cc5c7`：受限解碼與載入相容修正完成；沒有解除完整機制與獨立資料缺口。
3. `01a080b6-77fd-7142-b078-750d315773a1`：本次准入、資料與完成條件重驗，仍是完整准入0／可用凍結IR0／認證獨立留出0。

前兩輪是有產出與新證據的進展，不是假稱連續無進展；但目前已沒有可在不變更資料／配方範圍下，針對完整機制重新監督並完成獨立驗收的路徑。分類訓練在技術上仍可執行，這不表示繼續任意掃step、LR或格式可以證明完整目標。

## 解除條件（需要使用者決定或外部輸入）

建議保留這個任務只負責模型：**允許接收其他支線已驗收的完整機制訓練資料，另提供未曝光、未參與選模的完整英雄／機制家族留出集。** 每份仍需原文、動作／對象／時序／跨槽關係、能力版本、正反例驗收與hash；有問題就排除，這個任務不修資料或引擎。

這會改變目前禁止新增資料的範圍，尚未得到批准，因此未擅自開始。只修好Main的點位問題亦不會自動產生獨立留出；只增加分類訓練也不會補上完整機制目標。

原本95%完整英雄目標保持不變；本稽核不把它標為完成，不把沒有採用模型說成成功交付。

## 重跑

使用既有Python環境（需psutil）與Node；輸出必須是新檔：

```sh
python tools/editor-acceptance/hero-finetune-scope-audit.py --research-root /absolute/research/outputs/hero-forge-12b-restart-20260908 --output /absolute/new/audit.json
```

工具不連網、不啟動GPU、不更動資料。准入命令的exit2會被保留在JSON；audit本身正常寫出結果可exit0，不能將兩者混作准入成功。
