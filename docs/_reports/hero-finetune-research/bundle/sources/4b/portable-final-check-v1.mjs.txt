/** Actual relocated-bundle inference for all three representations, never expected-answer prompting. */
import fs from'node:fs';import path from'node:path';import assert from'node:assert/strict';import{fileURLToPath,pathToFileURL}from'node:url';import{isDeepStrictEqual}from'node:util';import{fileHash}from'./precision-diagnostic.mjs';
const read=p=>JSON.parse(fs.readFileSync(p)),self=fileURLToPath(import.meta.url);
export const matches=(expectedValues,result)=>result.contractPass&&expectedValues.some(v=>isDeepStrictEqual(v,result.values.map(r=>r.value)));
async function main(bundleRoot,assets,python,runtime,out){
 assert([bundleRoot,assets,python,runtime,out].every(path.isAbsolute));assert(!fs.existsSync(out),'REFUSE_RESTART');assert(!fs.existsSync(path.join(runtime,'gpu.lock')),'GPU_BUSY');const manifest=read(path.join(bundleRoot,'MODEL_BUNDLE.json')),receipt=read(path.join(assets,'ASSET_RECEIPT.json')),expectedPath=path.join(assets,'EXPECTED.private.json'),expected=read(expectedPath);assert.equal(expected.trainingAllowed,false);assert.equal(expected.checkpointSelectionAllowed,false);assert.equal(expected.examples.length,10);
 const variants=Object.keys(manifest.variants);assert(variants.includes('native')&&variants.every(v=>['native','fused-bf16','mlx-8bit'].includes(v)),'UNSUPPORTED_VARIANTS');const entryPath=path.join(bundleRoot,'code/run.mjs'),api=await import(pathToFileURL(entryPath));for(const v of variants)api.verifyBundle(bundleRoot,v);
 for(const e of receipt.examples)assert.equal(fileHash(path.join(bundleRoot,e.path)),e.sha256,'SPEC_CHANGED_DURING_RELOCATION');
 const cutoff=Date.parse('2026-09-06T20:06:00Z');assert(Date.now()<cutoff-180000,'INSUFFICIENT_PORTABLE_CHECK_TIME');fs.mkdirSync(out);const put=(n,v)=>{const p=path.join(out,n);fs.writeFileSync(p+'.tmp',JSON.stringify(v,null,2)+'\n');fs.renameSync(p+'.tmp',p);};
 const pins=[self,expectedPath,path.join(assets,'ASSET_RECEIPT.json'),path.join(bundleRoot,'MODEL_BUNDLE.json'),entryPath].map(p=>({path:p,sha256:fileHash(p)}));put('preparation.json',{preparedAt:new Date().toISOString(),pins,variants,examples:10,defaultVariant:manifest.defaultVariant,selectionAllowed:false,trainingAllowed:false,cutoff:new Date(cutoff).toISOString(),releaseQualified:false});
 const state={status:'running',pid:process.pid,startedAt:new Date().toISOString(),stages:[]};put('state.json',state);let cancelled=false;process.on('SIGTERM',()=>{cancelled=true;});process.on('SIGINT',()=>{cancelled=true;});
 function guard(){assert(!cancelled,'CANCELLED');assert(Date.now()<cutoff,'GPU_CUTOFF');assert(!fs.existsSync(path.join(out,'CANCEL')),'CANCEL');for(const p of pins)assert.equal(fileHash(p.path),p.sha256,'PIN_CHANGED:'+p.path);}
 try{const results=[];
  for(const variant of variants)for(const e of expected.examples){guard();assert(e.semanticGoldAvailable&&Array.isArray(e.expectedValues));const spec=path.join(bundleRoot,'examples',e.id+'.json');state.stage=variant+'-'+e.id;put('state.json',state);
   const r=await api.infer(bundleRoot,spec,python,runtime,path.join(out,state.stage),variant,new Date(cutoff).toISOString());guard();const values=r.values.map(v=>v.value),semanticMatch=matches(e.expectedValues,r);results.push({variant,id:e.id,kind:e.kind,contractPass:r.contractPass,semanticMatch,values,metadata:r.metadata,source:r.modelBundle,seconds:r.values.reduce((s,v)=>s+v.seconds,0)});state.stages.push({stage:state.stage,completedAt:new Date().toISOString(),semanticMatch,contractPass:r.contractPass});put('state.json',state);put('partial-results.json',results);console.log(JSON.stringify(state.stages.at(-1)));
  }
  const byVariant=Object.fromEntries(variants.map(v=>{const rows=results.filter(r=>r.variant===v);return[v,{total:rows.length,semanticPassed:rows.filter(r=>r.semanticMatch).length,contractsPassed:rows.filter(r=>r.contractPass).length,maxPeakMetalBytes:Math.max(...rows.map(r=>r.metadata.peakMetalBytes)),allSemanticsPass:rows.every(r=>r.semanticMatch),allContractsPass:rows.every(r=>r.contractPass)}];}));
  put('summary.json',{completedAt:new Date().toISOString(),bundle:bundleRoot,defaultVariant:manifest.defaultVariant,results,byVariant,variantsTested:variants,fullPrecisionTriadTested:variants.length===3,selectionChanged:false,physical16GBMachineTested:false,releaseQualified:false,activation:false,scope:`${variants.length*10} real public-entry calls against ${variants.join('/')} relocated files, 14 inference requests per representation. Known authored fixtures, not new Owner Gold or complete engine coverage.`});
  fs.writeFileSync(path.join(out,'REPORT.md'),['# 可搬移成品包真實入口檢查','',`${variants.length}種表示法各10次公開入口呼叫，包含完整來源核對及多卡重複／時序；私有答案只在輸出後核對，不進入模型。結果不參與重新選權重或精度。`,'','| 表示法 | 語意符合 | 格式合法 | Metal峰值GiB |','| --- | ---: | ---: | ---: |',...Object.entries(byVariant).map(([v,r])=>`| ${v} | ${r.semanticPassed}/${r.total} | ${r.contractsPassed}/${r.total} | ${(r.maxPeakMetalBytes/1024**3).toFixed(2)} |`),'','12GiB配置上限不等於16GB MacBook實機測試。此處是人造操作案例的有限語意對照，不是全英雄或全機制合格。',''].join('\n'),{flag:'wx'});state.status='complete-research-evidence';state.completedAt=new Date().toISOString();put('state.json',state);console.log(JSON.stringify({status:state.status,byVariant,releaseQualified:false}));
 }catch(e){state.status='failed';state.error=String(e);put('state.json',state);console.error(e);process.exitCode=1;}
}
if(process.argv[1]&&path.resolve(process.argv[1])===self)await main(...process.argv.slice(2));
