import test from 'node:test';import assert from 'node:assert/strict';import {digest} from './dataset.mjs';import {prepare} from './research-inference-v2.mjs';import {verifySerialized} from './r6-entry-recovery.mjs';
test('JSON roundtrip optional keys do not cause a false drift alarm; actual request changes still fail',()=>{const text='生命回復每秒十點。',spec={kind:'source',input:{id:'fixture',task:'owner-mechanism',source:{id:'fixture',name:'fixture',version:'v1',text,sha256:digest(text)},claim:'每秒回復十點生命。'}},built=prepare(spec),saved=JSON.parse(JSON.stringify(built));assert.throws(()=>assert.deepEqual(built,saved));assert.equal(verifySerialized(spec,saved),true);saved.requests[0].requestDigest='changed';assert.throws(()=>verifySerialized(spec,saved),/PUBLIC_PREPARATION_DRIFT/);});
