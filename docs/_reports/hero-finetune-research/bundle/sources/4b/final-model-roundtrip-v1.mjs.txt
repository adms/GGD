/** Conversion + full same-input dev/test measurement. Fixed 8bit, no model training. */
import fs from'node:fs';import path from'node:path';import assert from'node:assert/strict';import{spawn}from'node:child_process';import{fileURLToPath}from'node:url';
import{fileHash}from'./precision-diagnostic.mjs';import{comparePrecisions,choosePrecision,render}from'./final-precision-score-v1.mjs';import{reference}from'./final-reference-v1.mjs';
const self=fileURLToPath(import.meta.url),dir=path.dirname(self),read=p=>JSON.parse(fs.readFileSync(p));
async function main(referenceRoot,evaluationRoot,python,runtime,out,authorization){
 assert([referenceRoot,evaluationRoot,python,runtime,out,authorization].every(path.isAbsolute));assert(!fs.existsSync(out),'REFUSE_RESTART');assert(fs.existsSync(authorization));assert(!fs.existsSync(path.join(runtime,'gpu.lock')),'GPU_BUSY');
 const referencePath=path.join(referenceRoot,'native-reference.json'),validatedReference=reference(referenceRoot),native=validatedReference.native;
 const casesPath=path.join(evaluationRoot,'cases.private.json'),cases=read(casesPath),groups=['dev','test'].map(split=>({name:split,cases:cases.filter(c=>c.split===split),request:path.join(evaluationRoot,split+'-requests.json')}));
 for(const g of groups){assert.deepEqual(read(g.request),g.cases.map(({id,messages,requestDigest})=>({id,messages,requestDigest})));const b=read(path.join(evaluationRoot,g.name+'-token-budget.json'));assert(b.allFit);assert.equal(b.requestsSha256,fileHash(g.request));}
 const baseReceiptPath=path.join(path.dirname(native.base),'NATIVE_REFERENCE.json'),baseReceipt=read(baseReceiptPath);assert.equal(baseReceipt.model,native.base);const baseFiles=baseReceipt.files.filter(f=>f.name.startsWith('base/')).map(f=>({...f,name:f.name.slice(5)}));assert.equal(baseFiles.length,13);
 const cutoff=Date.parse('2026-09-06T20:06:10Z');assert(Date.now()<cutoff-600000,'INSUFFICIENT_CONVERSION_TIME');
 fs.mkdirSync(out);const put=(n,v)=>{const p=path.join(out,n);fs.writeFileSync(p+'.tmp',JSON.stringify(v,null,2)+'\n');fs.renameSync(p+'.tmp',p);};
 const policy={schema:'ggd-final-fixed-precision@1',deadline:'2026-09-06T20:36:10Z',reportReserveSeconds:1800,cloudGpu:false,cloudFallback:false,cloudGpuSpendLimit:0,externalTeacher:false,publish:false,activateInEditor:false,thinking:false,maxMemoryGiB:12,maxSequenceTokens:4096,maxOutputTokens:256,seed:20260906,temperature:0,topP:.8,topK:20,presencePenalty:0,releaseQualified:false};put('eval-policy.json',policy);put('convert-policy.json',{...policy,maxMemoryGiB:64});
 const manifest={base:{path:native.base,revision:baseReceipt.baseRevision,files:baseFiles},adapter:native.adapter,adapterArtifacts:['adapters.safetensors','adapter_config.json'].map(n=>({path:path.join(native.adapter,n),sha256:fileHash(path.join(native.adapter,n))}))};put('conversion-input.json',manifest);
 const pinPaths=[self,...['deploy.py','worker.py','final-reference-v1.mjs','final-precision-score-v1.mjs','r6-score.mjs','r3-score.mjs','classification-score.mjs','composition-client.mjs'].map(n=>path.join(dir,n)),referencePath,...validatedReference.evidencePins.map(p=>p.path),casesPath,baseReceiptPath,...manifest.adapterArtifacts.map(f=>f.path),...groups.flatMap(g=>[g.request,path.join(evaluationRoot,g.name+'-token-budget.json')]),...['eval-policy.json','convert-policy.json','conversion-input.json'].map(n=>path.join(out,n)),authorization],pins=pinPaths.map(p=>({path:p,sha256:fileHash(p)}));
 put('PREPARATION.json',{createdAt:new Date().toISOString(),referenceRoot,evaluationRoot,pins,fixedBits:8,groupSize:64,nativeReference:native,referenceProtocol:validatedReference.protocol,historicalReferenceDecode:validatedReference.historicalDecode,newEvaluationDecode:0,devRows:groups[0].cases.length,testRows:groups[1].cases.length,selectionUses:'dev only; fixed 8-bit conversion, no precision sweep',memoryCapGiB:12,conversionMemoryCapGiB:64,cutoff:new Date(cutoff).toISOString(),cloudSpend:0,trainingCalls:0,releaseQualified:false});
 const state={status:'running',pid:process.pid,startedAt:new Date().toISOString(),stages:[]};put('state.json',state);let child=null,reason=null,killTimer,owned=false;const lock=path.join(runtime,'gpu.lock');
 function guard(){assert(!reason,reason);assert(Date.now()<cutoff,'GPU_CUTOFF');for(const r of[referenceRoot,evaluationRoot,out,path.dirname(authorization)])assert(!fs.existsSync(path.join(r,'CANCEL')),'CANCEL');for(const p of pins)assert.equal(fileHash(p.path),p.sha256,'PIN_CHANGED:'+p.path);}
 function stop(why){reason??=why;if(child?.pid){try{process.kill(-child.pid,'SIGTERM');}catch{}killTimer=setTimeout(()=>{try{process.kill(-child.pid,'SIGKILL');}catch{}},3000);}}
 process.on('SIGTERM',()=>stop('SIGNAL'));process.on('SIGINT',()=>stop('SIGNAL'));
 async function execute(stage,args){guard();state.stage=stage;put('state.json',state);const log=fs.openSync(path.join(out,stage+'.log'),'wx');child=spawn(python,args,{stdio:['ignore',log,log],detached:true});fs.closeSync(log);put('worker-lease.json',{pid:child.pid,parent:process.pid,stage});const timer=setInterval(()=>{try{guard();}catch(e){stop(String(e));}},2000);let code;try{code=await new Promise((res,rej)=>{child.once('exit',res);child.once('error',rej);});}finally{clearInterval(timer);clearTimeout(killTimer);child=null;put('worker-lease.json',{pid:null,stage});}assert(!reason&&code===0,reason??'WORKER_EXIT:'+stage+':'+code);guard();state.stages.push({stage,completedAt:new Date().toISOString()});put('state.json',state);console.log(JSON.stringify(state.stages.at(-1)));}
 try{
  // deploy.py owns its own exclusive lock. Subsequent eval takes a parent-owned lock.
  await execute('conversion',[path.join(dir,'deploy.py'),'--manifest',path.join(out,'conversion-input.json'),'--policy',path.join(out,'convert-policy.json'),'--dev-requests',groups[0].request,'--destination',path.join(out,'converted'),'--report',path.join(out,'conversion-report.json'),'--lease-dir',runtime,'--bits','8']);
  const conversion=read(path.join(out,'conversion-report.json'));assert.equal(conversion.status,'roundtrip-pass');assert.equal(conversion.quantizationBits,8);assert(conversion.adapterLoadedExactly&&conversion.tokenizerExact);for(const f of conversion.files){assert.equal(fs.statSync(f.path).size,f.bytes);assert.equal(fileHash(f.path),f.sha256,'CONVERTED_BYTES_CHANGED');}
  pins.push({path:path.join(out,'conversion-report.json'),sha256:fileHash(path.join(out,'conversion-report.json'))});
  fs.writeFileSync(lock,JSON.stringify({pid:process.pid,kind:'final-precision-eval',root:out}),{flag:'wx'});owned=true;
  const variants=[{name:'native',model:native.base,adapter:native.adapter},{name:'fused-bf16',model:conversion.fusedPath,adapter:null},{name:'mlx-8bit',model:conversion.quantizedPath,adapter:null}],reports={};let precisionSelection;
  for(const g of groups){const arms=[];for(const v of variants){const stage=g.name+'-'+v.name,output=path.join(out,stage+'.json'),args=[path.join(dir,'worker.py'),'eval','--model',v.model,'--policy',path.join(out,'eval-policy.json'),'--data',g.request,'--output',output];if(v.adapter)args.push('--adapter',v.adapter);await execute(stage,args);const raw=read(output);assert(raw.metadata.peakMetalBytes<=12*1024**3,'INFERENCE_CAP_EXCEEDED');arms.push({...v,raw});}
   const report=comparePrecisions(g.cases,arms);put(g.name+'-comparison.json',report);fs.writeFileSync(path.join(out,g.name+'-REPORT.md'),render(report),{flag:'wx'});reports[g.name]=Object.fromEntries(Object.entries(report.scores).map(([n,s])=>[n,{total:s.total,passed:s.passed,unsafe:s.unsafe,tasks:s.tasks}]));
   if(g.name==='dev'){precisionSelection=choosePrecision(report);put('PRECISION_SELECTION.json',{...precisionSelection,selectedAt:new Date().toISOString(),beforeAnyTestInference:true});}else assert.deepEqual(read(path.join(out,'PRECISION_SELECTION.json')).selected,precisionSelection.selected);
  }
  for(const f of conversion.files)assert.equal(fileHash(f.path),f.sha256,'CONVERTED_BYTES_CHANGED');for(const f of baseFiles)assert.equal(fileHash(path.join(native.base,f.name)),f.sha256,'BASE_CHANGED');guard();
  put('summary.json',{completedAt:new Date().toISOString(),referenceRoot,evaluationRoot,selectedAdapterSha256:native.adapterSha256,precisionSelection,variants,reports,conversion:{seconds:conversion.seconds,peakMetalBytes:conversion.peakMetalBytes,fusionMaxLogitDifference:conversion.fusionMaxLogitDifference,fusedReloadMaxLogitDifference:conversion.fusedReloadMaxLogitDifference,quantizedReloadMaxLogitDifference:conversion.quantizedReloadMaxLogitDifference},conversionMemoryIsNotInferenceMemory:true,physical16GBMachineTested:false,portableEntryGpuChecksPending:true,broaderRegressionPrecisionChecksPending:true,releaseQualified:false,activation:false});state.status='complete-research-evidence';state.completedAt=new Date().toISOString();put('state.json',state);console.log(JSON.stringify({status:state.status,precisionSelection,portableEntryGpuChecksPending:true,releaseQualified:false}));
 }catch(e){state.status='failed';state.error=String(e);put('state.json',state);console.error(e);process.exitCode=1;}
 finally{clearTimeout(killTimer);if(owned&&fs.existsSync(lock)&&read(lock).pid===process.pid)fs.unlinkSync(lock);put('worker-lease.json',{pid:null,reason,endedAt:new Date().toISOString()});}
}
if(process.argv[1]&&path.resolve(process.argv[1])===self)await main(...process.argv.slice(2));
