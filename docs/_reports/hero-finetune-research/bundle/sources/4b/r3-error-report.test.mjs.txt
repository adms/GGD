import test from 'node:test';import assert from 'node:assert/strict';import {compareArms,failureKind,renderReport} from './r3-error-report.mjs';
const cases=[{id:'a',task:'hero-source',split:'dev',lineage:'a',target:{verdict:'not-stated'},requestDigest:'a',messages:[{role:'user',content:'{"claim":"unknown"}'}]},{id:'b',task:'mechanism-template',split:'dev',lineage:'b',target:{decision:'accept',templateId:'tpl-x'},acceptedTargets:[{decision:'accept',templateId:'tpl-x'}],requestDigest:'b',messages:[{role:'user',content:'{"request":"known"}'}]}];
const raw=values=>({complete:true,metadata:{thinking:false,modelPath:'/same',versions:{mlx:'fixed'},grammar:'none'},results:values.map((value,i)=>({id:cases[i].id,requestDigest:cases[i].requestDigest,seed:1,value}))});
const arms=[{name:'base',raw:raw([{verdict:'supported'},{decision:'accept',templateId:'tpl-x'}])},{name:'candidate',raw:raw([{verdict:'not-stated'},{decision:'refuse',templateId:null}])}];
test('report preserves one hero fix and one mechanism regression without macro hiding',()=>{const r=compareArms(cases,arms);assert.equal(r.comparisons[0].byTask['hero-source'].fixed,1);assert.equal(r.comparisons[0].byTask['mechanism-template'].regressed,1);assert.equal(r.failureCounts.base['unsupported-source-claim-accepted'],1);assert.equal(r.failureCounts.candidate['supported-template-refused'],1);assert.equal(r.releaseQualified,false);assert.match(renderReport(r),/candidate/);});
test('changed runtime, seed, prompt, or incomplete output is not a paired comparison',()=>{for(const mutate of [a=>a[1].raw.metadata.modelPath='/different',a=>a[1].raw.results[0].seed=2,a=>a[1].raw.results[0].requestDigest='different',a=>a[1].raw.complete=false]){const a=structuredClone(arms);mutate(a);assert.throws(()=>compareArms(cases,a));}});
test('unknown versus contradiction is distinct from dangerous acceptance and invalid schema',()=>{assert.equal(failureKind(cases[0],{verdict:'contradicted'}),'contradicted-versus-not-stated-confusion');assert.equal(failureKind(cases[0],{verdict:'not-stated',extra:1}),'invalid-output-schema');assert.throws(()=>compareArms([...cases,cases[0]],arms));assert.throws(()=>compareArms(cases.map(c=>({...c,split:'train'})),arms));});
test('VFX semantic task cannot disappear from the rendered task table',()=>{const r=compareArms(cases,arms);r.scores.base.tasks['vfx-semantic']={passed:2,total:3,unsafe:1,schema:3};assert.match(renderReport(r),/vfx-semantic \/ base \| 2\/3 \| 1 \| 3\/3/);});
