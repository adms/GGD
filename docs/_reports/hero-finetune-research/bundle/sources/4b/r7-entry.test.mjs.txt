import test from 'node:test';import assert from 'node:assert/strict';import {canRun} from './r7-entry.mjs';
test('entry never races live core/post or a GPU lock, failed parents stop',()=>{assert.equal(canRun({status:'running'},true,{status:'waiting-for-core'},true,true),false);assert.throws(()=>canRun({status:'failed'},false,{status:'waiting-for-core'},true,false),/CORE_FAILED/);assert.throws(()=>canRun({status:'running'},false,{status:'waiting-for-core'},true,false),/CORE_DIED/);assert.equal(canRun({status:'complete-research-only'},false,{status:'complete-research-evidence'},false,false),true);assert.equal(canRun({status:'complete-research-only'},false,{status:'complete-research-evidence'},false,true),false);});
