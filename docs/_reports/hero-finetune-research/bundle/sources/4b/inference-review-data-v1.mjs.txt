/** Inventory every saved corpus; deduplicate reviewed text-classification requests for inference only. */
import fs from'node:fs';import path from'node:path';import assert from'node:assert/strict';import{spawnSync}from'node:child_process';import{fileURLToPath}from'node:url';import{digest}from'./dataset.mjs';import{fileHash}from'./precision-diagnostic.mjs';import{exposure}from'./r7-selected-baseline-v1.mjs';
const self=fileURLToPath(import.meta.url),read=p=>JSON.parse(fs.readFileSync(p));
export const cohorts=['forge-source-rehearsal-r8-v1-20260906','forge-final-three-hours-20260906/final-broader-data-v1','forge-low-lr-r7-v2-20260906','forge-joint-source-stack-r6-v2-20260906','forge-contrastive-r5-20260906','forge-source-calibration-r4-20260906','forge-mechanism-priority-r3-20260906','forge-classification-reviewed-r2-20260906'];
const tasks=['hero-source','owner-mechanism','mechanism-stack','mechanism-template','vfx-semantic'];
const withdrawn=new Set(['main-candidate-traveling-wave-supported','r6-stack-main-candidate-traveling-wave-supported']);
const answers=c=>(c.acceptedTargets??[c.target]).map(v=>digest(v)).sort();
export function merge(groups){const byInput=new Map;for(const g of groups)for(const c of g.cases){assert(tasks.includes(c.task));assert.equal(c.messages.length,2);assert(c.requestDigest);const key=digest(c.messages),member={dataset:g.name,id:c.id,split:c.split,sourceId:c.sourceId??null,quality:c.quality??null,expected:c.acceptedTargets??[c.target],withdrawn:withdrawn.has(c.id)};let r=byInput.get(key);if(!r){r={case:structuredClone(c),key,members:[],answerKeys:answers(c)};byInput.set(key,r);}assert.equal(r.case.task,c.task);r.members.push(member);r.conflictingLabels=Boolean(r.conflictingLabels||digest(r.answerKeys)!==digest(answers(c)));}return [...byInput.values()];}
export function prepare(outputs,out){
 assert([outputs,out].every(path.isAbsolute));assert(!fs.existsSync(out),'REFUSE_OVERWRITE');const disabled=path.join(outputs,cohorts[0],'TRAINING_DISABLED.json');assert.equal(read(disabled).trainingCallsAllowed,false);const discovery=spawnSync('rg',['--files',outputs,'-g','cases.private.json','-g','corpus.private.json'],{encoding:'utf8'});assert.equal(discovery.status,0);const files=discovery.stdout.trim().split('\n').sort(),groups=cohorts.map(name=>({name,file:path.join(outputs,name,'cases.private.json'),cases:read(path.join(outputs,name,'cases.private.json'))})),merged=merge(groups),r7=exposure(path.join(outputs,'forge-low-lr-r7-v2-20260906')),r3train=read(path.join(r7.parent,'cases.private.json')).filter(c=>c.split==='train');
 const trainMap=rows=>({exact:new Set(rows.map(c=>digest(c.messages))),source:new Set(rows.filter(c=>['hero-source','owner-mechanism'].includes(c.task)).map(c=>digest(JSON.parse(c.messages[1].content).source.text)))}),training={r3:trainMap(r3train),r7:trainMap(r7.train)};
 merged.sort((a,b)=>{const community=r=>r.members.some(m=>m.id.includes('community-'));return Number(community(b))-Number(community(a))||tasks.indexOf(a.case.task)-tasks.indexOf(b.case.task)||Number(b.members.some(m=>m.dataset===cohorts[0]))-Number(a.members.some(m=>m.dataset===cohorts[0]))||a.key.localeCompare(b.key);});
 const cases=[],mapping=[];for(const[rindex,r]of merged.entries()){const id='inference-review-'+String(rindex+1).padStart(5,'0'),source=['hero-source','owner-mechanism'].includes(r.case.task)?digest(JSON.parse(r.case.messages[1].content).source.text):null;cases.push({...r.case,id,split:'inference-review'});mapping.push({id,inputSha256:r.key,task:r.case.task,members:r.members,conflictingLabels:r.conflictingLabels,knownWithdrawnLabel:r.members.some(m=>m.withdrawn),scoringQualified:!r.conflictingLabels&&!r.members.some(m=>m.withdrawn),exposure:Object.fromEntries(Object.entries(training).map(([n,t])=>[n,{exactTrainingInputSeen:t.exact.has(r.key),fullSourceTextSeen:source===null?null:t.source.has(source)}]))});}
 const index=new Map(mapping.map(m=>[m.inputSha256,m]));const inventories=files.map(file=>{const j=read(file),a=Array.isArray(j)?j:j.cases??[],inFresh=cohorts.find(n=>file===path.join(outputs,n,'cases.private.json'))??null,matched=a.filter(c=>Array.isArray(c.messages)&&index.has(digest(c.messages))).length;return{file,sha256:fileHash(file),rows:a.length,tasks:Object.fromEntries([...new Set(a.map(c=>c.task??'legacy-proposal'))].map(t=>[t,a.filter(c=>(c.task??'legacy-proposal')===t).length])),splits:Object.fromEntries([...new Set(a.map(c=>c.split??'unspecified'))].map(s=>[s,a.filter(c=>(c.split??'unspecified')===s).length])),freshCohort:inFresh,exactRequestsInFresh:matched,otherRows:a.length-matched,status:inFresh?'reviewed-version-included':matched===a.length&&a.length?'duplicate-requests-covered':'historical-or-quarantined-not-silently-admitted',note:inFresh?'Original prompts and labels retained; historical capability versions are not current-engine certification.':'Superseded/quarantined corpora and older proposal protocols remain in the inventory and historical evidence; unmatched rows are NOT claimed freshly evaluated.'};});
 fs.mkdirSync(out);const put=(n,v)=>fs.writeFileSync(path.join(out,n),JSON.stringify(v,null,2)+'\n',{flag:'wx'});put('cases.private.json',cases);put('mapping.private.json',mapping);const chunks=[];let offset=0;while(offset<cases.length){const task=cases[offset].task,max=task==='mechanism-stack'?40:task==='mechanism-template'?64:96;let end=offset+1;while(end<cases.length&&end-offset<max&&cases[end].task===task)end++;const name='chunk-'+String(chunks.length+1).padStart(3,'0')+'-'+task,rows=cases.slice(offset,end),request=name+'-requests.json';put(request,rows.map(({id,messages,requestDigest})=>({id,messages,requestDigest})));chunks.push({name,task,count:rows.length,ids:rows.map(c=>c.id),request,requestsSha256:fileHash(path.join(out,request))});offset=end;}
 put('inventory.json',inventories);const manifest={createdAt:new Date().toISOString(),outputs,corpusFiles:inventories.length,totalHistoricalRows:inventories.reduce((n,r)=>n+r.rows,0),cohorts:groups.map(g=>({name:g.name,file:g.file,sha256:fileHash(g.file),rows:g.cases.length})),uniqueRequests:cases.length,chunks,taskCounts:Object.fromEntries(tasks.map(t=>[t,cases.filter(c=>c.task===t).length])),conflictingLabels:mapping.filter(m=>m.conflictingLabels).map(m=>m.id),withdrawnLabels:mapping.filter(m=>m.knownWithdrawnLabel).map(m=>m.id),priorities:tasks,trainingCallsAllowed:false,checkpointSelectionAllowed:false,precisionSelectionAllowed:false,modifiedOriginalInputs:0,modifiedOriginalLabels:0,modelArms:['base','r3','r7'],seedProtocol:'Same index-derived seed within each frozen chunk for all three models. New chunks do not reuse historical raw outputs.',r3ActualTrainUnique:434,r7ActualNewTrainPrefix:192,r8Trained:false,pins:[self,disabled,path.join(outputs,'forge-final-three-hours-20260906/INFERENCE_ONLY_DECISION.md'),...groups.map(g=>g.file),...['cases.private.json','mapping.private.json','inventory.json'].map(n=>path.join(out,n))].map(p=>({path:p,sha256:fileHash(p)})),releaseQualified:false};put('manifest.json',manifest);
 const lines=['# 全部資料版本盤點／推論隊列','',`發現${inventories.length}份corpus，歷史列數${manifest.totalHistoricalRows}（大量重複，不能當独特資料量）。8份經審查主版本與最新較廣題組合併成${cases.length}個完全相同訊息去重後請求；不重寫prompt或答案。`,`標註衝突${manifest.conflictingLabels.length}題、已撤回標註${manifest.withdrawnLabels.length}題：可保留推論作分析，但排除合格準度分母。`,`未匹配的隔離／舊proposal題保留為歷史／未新測，不宣稱全部歷史列都已重新推論。以下僅是隊列，完成需看run-state與各chunk原始輸出。`,'','| 資料集 | 歷史列 | 本次完全相同請求涵蓋 | 其他／未新測 | 狀態 |','| --- | ---: | ---: | ---: | --- |',...inventories.map(r=>`| ${path.relative(outputs,r.file)} | ${r.rows} | ${r.exactRequestsInFresh} | ${r.otherRows} | ${r.status} |`),'','所有模型使用同一chunk與seed。訓練題與未見来源分開；同一原文不同命題仍標為來源已見。新七角色沒有做神經訓練，原train/dev/test標籤只代表資料切分歷史。',''];fs.writeFileSync(path.join(out,'INVENTORY.md'),lines.join('\n'),{flag:'wx'});return{out,corpusFiles:inventories.length,totalHistoricalRows:manifest.totalHistoricalRows,uniqueRequests:cases.length,chunks:chunks.length,conflictingLabels:manifest.conflictingLabels.length,withdrawnLabels:manifest.withdrawnLabels.length};
}
if(process.argv[1]&&path.resolve(process.argv[1])===self)console.log(JSON.stringify(prepare(...process.argv.slice(2))));
