import fs from'node:fs';import path from'node:path';import os from'node:os';import test from'node:test';import assert from'node:assert/strict';import{fileURLToPath}from'node:url';import{copyFlat,inventory,bundle}from'./final-bundle-v1.mjs';import{prepare,validate}from'./research-inference-v2.mjs';import{fileHash}from'./precision-diagnostic.mjs';
import{matches}from'./portable-final-check-v1.mjs';
const output=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../../../outputs'),assets=path.join(output,'forge-final-three-hours-20260906/portable-assets-v2'),read=p=>JSON.parse(fs.readFileSync(p));
test('all ten frozen portable examples have structurally legal predeclared answers',()=>{const expected=read(path.join(assets,'EXPECTED.private.json'));assert.equal(expected.examples.length,10);let requests=0;
 for(const e of expected.examples){const spec=read(path.join(assets,'examples',e.id+'.json'));if(spec.catalog)spec.catalog.path=path.resolve(assets,'examples',spec.catalog.path);const p=prepare(spec);requests+=p.requests.length;for(const values of e.expectedValues){assert.equal(values.length,p.requests.length);const raw={complete:true,metadata:{thinking:false},results:p.requests.map((r,i)=>({id:r.id,requestDigest:r.requestDigest,error:null,value:values[i],seconds:0}))};assert.equal(validate(spec,p,raw).contractPass,true,e.id);}}
 assert.equal(requests,14);
});
test('bundle copy is byte-exact and refuses overwrite or symlink model entries',()=>{const temp=fs.mkdtempSync(path.join(os.tmpdir(),'ggd-bundle-copy-fixture-')),src=path.join(temp,'src'),dst=path.join(temp,'dst');fs.mkdirSync(src);fs.writeFileSync(path.join(src,'config.json'),'CPU ONLY FIXTURE');copyFlat(src,dst);assert.equal(fileHash(path.join(src,'config.json')),fileHash(path.join(dst,'config.json')));assert.equal(inventory(dst).length,1);assert.throws(()=>copyFlat(src,dst),/REFUSE_OVERWRITE/);fs.symlinkSync(path.join(src,'config.json'),path.join(src,'link.json'));assert.throws(()=>copyFlat(src,path.join(temp,'bad')),/REGULAR_FILES/);assert.throws(()=>bundle(path.join(temp,'missing'),src,assets,path.join(temp,'should-not-exist')));assert(!fs.existsSync(path.join(temp,'should-not-exist')));});
test('portable semantic check ignores object key order but preserves duplicate card multiplicity',()=>{const expected=[[{decision:'accept',templateIds:['a','a'],onConflict:'reject'}]],result={contractPass:true,values:[{value:{onConflict:'reject',templateIds:['a','a'],decision:'accept'}}]};assert(matches(expected,result));result.values[0].value.templateIds=['a'];assert(!matches(expected,result));});
