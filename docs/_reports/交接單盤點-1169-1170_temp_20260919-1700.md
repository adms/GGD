# 交接單重新盤點 —— #1169 模型側 · #1170 語音側（2026-09-19）

> ⭐ 這兩張是**盤點票**：它們的價值是「**今天的數字**」，⛔ 不是票文寫下的那一天的數字。
> 這份報告把**出貨的工具**重跑一次，逐項回答「今天還缺什麼 · 哪幾位 · 誰能解」。
>
> | | |
> |---|---|
> | 基準 | `main` @ `3d833f54b`（工作樹 `/private/tmp/ggd-integ-nc`） |
> | 量的時間 | 2026-09-19 17:00 |
> | ⛔ 這份報告**不改任何東西** | 它只量。票關不關由主線決定 |

---

## 0 · 我跑了哪幾支，離開碼是多少

| 指令 | 離開碼 | 它回答什麼 |
|---|---:|---|
| `node tools/hero-intake/run.mjs --batch ship153 --all --check` | **0** | 材料表是最新的（**190 位**）⇒ 下面每一個數字都是從它讀的，⛔ 不是我另外湊的 |
| `npx vitest run modelFacing.test.ts hiddenPrimitives.test.ts combatVoiceCoverage.test.ts` | **0** | 三條閘**全綠**（28 passed · 2 skipped · 8.57s） |
| `node --import tsx tools/voice-gen/src/build-combat-lines.mjs --check` | **0** | 台本 84 位 · 1,583 句 · **0 句還要算圖** |
| `node --import tsx tools/voice-gen/index-lines.mjs --check` | **0** | MANIFEST 是最新的（138 位 · 84 位 partial · 22 位變身共用） |
| `npx tsx tools/roster-guard/check.ts` | **0** | 名單漣漪 13 軸全過 |

⚠️ ⭐ **三條閘全綠，⛔ 不代表這兩張票做完了** —— 綠的意思是「**缺口被宣告了**」，
而這兩張票要的是「**缺口變小了**」。下面每一節都把「宣告數」與「缺口數」分開寫。

---

## 1 · #1169 模型側 —— 四項裡 **兩項真的關掉了、一項規模變大 26 倍、一項剩 1 列**

### 1.1 逐項對照票文

| 票文寫的（2026-09-10） | 今天量到的 | 判定 |
|---|---|---|
| ⛔ **6 名 `b2-*` 待轉檔** | **0 名** —— 7 名（aladdin · guts · kaiji · keyaru · maomao · naofumi · shinchan）在 2026-09-11 全部拿到真模型，梅普露變身態改沿用本體（GH#1217） | ⭐ **已關** |
| ⛔ **`ou99.495015` 血泥 geoset 沒宣告** | `content/models/ou99.495015.json` 有 `"hiddenPrimitives": [1]`；`hiddenPrimitives.test.ts` 15 passed / 2 skipped | ⭐ **已關**（PR #1216） |
| ⛔ **6 顆模型朝向沒有人在量** | ⚠️ **159 顆**（見 1.2） | ⛔ **沒關，而且規模 ×26.5** |
| ⚠️ **盤點表 4 列引用已改掉的上限 160** | **1 列**（`b2-boxxo`），且已改寫成「舊拒收理由失效，本項尚未重新轉換及驗收」 | ⚠️ **剩 1 列**（owner 的檔，⛔ 只讀不寫） |

⚠️ ⭐ **最後一列我量的是什麼，寫清楚**：在 `materials/hero-model-library/全角色模型盤點.md` 全文
用 `上限 ?160｜limit ?160｜160 (通道|channel)｜超過…160` 搜，**命中 1 列**；
`inventory.json` 用「歷史上限／舊拒收理由失效／237 通道／channelLimit」四個字樣搜，**命中 0**。
⛔ 我**沒有**去翻 git 歷史確認當初那 4 列是哪 4 列 —— ⭐ 我能證明的是「**今天只剩 1 列**」，
⛔ 不是「另外 3 列被修好了」（它們也可能是被整段重寫掉的）。

### 1.2 ⛔⛔ 最重要的一項：朝向未驗從 **6 顆 → 159 顆**

| | |
|---|---|
| `FACING_UNVERIFIED` 今天的列數 | **159** |
| 普查分母（model doc 有 glbPath 且 glb 真的在工作樹） | **1,063** |
| ⇒ 朝向**沒有人在量**的比例 | ⭐ **15.0%** |

⭐ **這個數字是可信的，因為那條閘是雙向棘輪**（`modelFacing.test.ts` ③④）：
③ 沒宣告的洞 ⇒ 紅 · ④ 已經不是洞卻還在表上 ⇒ 紅。
⇒ 159 **就是今天的洞數**，⛔ 不是一張放寬的豁免表。

**它是怎麼長到 159 的**（表裡自己的註解逐字記著）：

| 時間 | 列數 | 來源 |
|---|---:|---|
| 原始（GH#216） | 3 | `imported.luffe` · `imported.herolight` · `prop.guardian.beast` |
| 2026-09-15 | +63 | 09-10 起的批次匯入（ou99／community 骨架、PR #1152 的 `version.body.*` 凍結副本） |
| 2026-09-17（GH#1281） | +17 | 合併 PR #1267（模型庫整合，Codex） |
| 其餘 | +76 | 同一批社群骨架陸續進來 |

**逐前綴**：`version.body.*` 96 · `community.body.*` 41 · `ou99.*` 20 · 其他 2。
**逐理由**：`L/R 骨頭對數太少（1–2 對）` 52 · `L/R 骨頭彼此不同意` 106 · `與來源同一份幾何` 1。

⭐ **根因不是誰偷懶**：這批社群骨架的 L/R 骨頭命名少（多數只有 1–2 對），
`chiralityForward()` **結構上量不出方向** ⇒ ⛔ 這不是「還沒排到」，是**量尺對這一族是瞎的**。

⇒ ⭐ **這張票的第 3 項今天不是「補 6 顆」，是一個機制題**：
要嘛給這一族**第二個與骨頭命名無關的線索**（軸向線索：尾巴／下顎／武器插槽），
要嘛承認 15% 的模型朝向靠**人眼**驗（選角預覽逐顆看一次）。
⛔ 逐顆手工判定 159 次**不是模板化**（第零守則⑨）。

### 1.3 今天還穿引擎骨架的是**誰**

| 英雄 | 穿什麼 | 為什麼 | 誰能解 |
|---|---|---|---|
| `acquired-beatrice` 碧翠絲 | `champ.sela` | `pending-source` —— 素材庫沒有本尊模型（`query.py 碧翠絲 --pending` 查不到） | ⭐ **owner**（找來源）／素材工作流 |
| `acquired-minecraft` Steve／Alex | `champ.thorne` | `pending-source` —— 本尊模型尚未取得 | ⭐ **owner**（找來源） |

⚠️ `sela` / `thorne` 自己也在名單上，⛔ 但**它們就是引擎骨架英雄本人**，⛔ 不是缺口。
⇒ ⭐ **真的穿佔位骨架的只有 2 名**（票文那一天是 8 名）。
⭐ 兩名都是 `pending-source`（來源沒拿到），⛔ 沒有任何一名是 `pending-conversion`（拿到了還沒轉）
⇒ **模型工作流今天沒有待轉檔的活**。

### 1.4 hero-intake 材料表今天的模型／圖示段

| | |
|---|---:|
| 英雄總數 | **190** |
| 🧍 模型缺口（blocker） | ⭐ **0** |
| 🧍 模型待補（warning） | **0** |
| 🖼 圖示缺口 | ⭐ **0** |
| 🎙 語音缺口（blocker） | **56**（⚠️ 這個 56 要拆，見第 2 節） |
| 可上架（三段全綠） | **134 / 190** |

⇒ ⭐ **#1169 的四項裡，能用「補幾顆模型」解決的已經是 0 了。**
剩下的是 ①159 顆朝向（機制題）②2 名等來源（owner）③1 列盤點表註記（owner 的檔）。

---

## 2 · #1170 語音側 —— **74 名 → 37 名**，而且每一名今天都指得出「誰能解」

### 2.1 ⛔⛔ 先把 `56` 這個數字拆開 —— 它**不是**缺口數

hero-intake 材料表說「語音缺口 56」。⚠️ ⭐ **那一欄的分母不是「缺語音的英雄」** ——
它問的是「**這個 id 自己有沒有一份語音包**」，⛔ 而變身對的另一半**本來就不該有**。

| 56 名拆開 | 幾名 | 是不是缺口 |
|---|---:|---|
| `VOICE_GAP_BATCH4` 的一員（第四批「先上架、語音待補」） | **33** | ⭐ **是** |
| **變身對的另一半**，透過 form-share 借伴侶的包（`godie-e00l → godie-e002` 等 20 對） | **20** | ⛔ **不是** —— hero-intake ⛔ 不模型化 form-share |
| 引擎骨架本人（`sela` · `thorne`） | **2** | ⛔ **不是** |
| `b2-maple-alt-…`（梅普露變身態，⛔ 不在 26 對裡，玩家選不到） | **1** | ⛔ **不是** |

⭐ **form-share 那 20 名有兩份獨立證據，⛔ 不是我推的**：
① `index-lines.mjs --check` 自己印著「138 champions (84 partial), **22 form-shared**」；
② `combatVoiceCoverage.test.ts` 斷言「`select` 一格都沒有的集合 **恰好等於** `VOICE_GAP`」而它是綠的
⇒ 那 20 名**不在** `VOICE_GAP` 且在名單上 ⇒ ⭐ 他們**真的發得出聲音**。
（`MANIFEST.json` 只有 **138 筆**對 190 位英雄 —— 借用是在**讀的時候**由 `resolveVoicePackId` 解的，
⛔ manifest 裡查不到 `godie-hjai` 這一筆是**設計**，⛔ 不是缺漏。）

⚠️ ⭐ **這正是 CLAUDE.md「讀一張表之前先問這一欄的分母是什麼」的實例** ——
把 56 直接寫進交接單，下一輪就會有人去替 20 個變身態各做一份包（而那會**終止借用**、
反而讓他們在伴侶有聲音的格子上變啞巴，`combatVoiceCoverage.test.ts` 的檔頭逐字記著這個坑）。

### 2.2 ⭐ 今天真正的缺口：兩個數字，刻意不相等

| 問什麼 | 今天 | 住在哪 |
|---|---:|---|
| ⭐ **完全沒有聲音**（連 `select` 一格都沒有）——⭐ **玩家聽得出來的那一個** | **37 名** | `VOICE_GAP` ＝ `@ggd/shared/content/voiceGapBatch4`（單一宣告，棘輪只能變短） |
| **出貨門檻九格不齊**（`CATEGORIES.json.shipGate`） | **44 名** | ＝ 37 ＋ 7 名 LOL「只有原檔」 |

⭐ 交叉驗證：`COMBAT_CASTING.json.excluded` **正好 44 筆** ⇒ 兩條路算出同一個數。

⚠️ ⭐ **票文的 74（37 `b2-*` ＋ 37 `community-review-*`）今天只剩後面那一半**：

| | 今天量到的 |
|---|---|
| `b2-*` 那 37 名 | ⭐ **全部關掉了** —— 材料表裡 38 位 `b2-*` 中 **37 位門檻九格齊**，唯一沒包的是 `b2-maple-alt-…`（變身態，玩家選不到） |
| `community-review-*` 那 37 名 | ⭐ 改名成 `acquired-*`／`lol-*`，**就是今天的 37**（`VOICE_GAP_BATCH4`） |

⇒ ⭐ **74 → 37，而且是同一批人換了一半。** ⛔ 不是「74 全部縮成 37」，
⭐ 是「前 37 做完了，後 37 一個都還沒做」——⭐ 這個區別決定下一步要做什麼。

### 2.3 ⭐⭐ 37 名逐一配「誰能解」——⭐ 三桶加起來**正好 37**，⛔ 沒有一名落在表外

來源：`materials/hero-model-library/voice-gap-source-mappings-20260916.json`
（`sha256 0dd95db7…`，由 `tools/hero-model-library/sync_voice_gap_source_mappings.py` 產）

#### 桶① ⭐ **主線／Codex 今天就能做** —— LOL 第二批 **11 名**

來源**已經定到檔名層級**（`nativeId`：`Sett.ja_JP` · `FiddleSticks.ja_JP` · `Ornn.ja_JP` …），
與 LOL 第一批 7 名走**完全相同**的路（Riot 自己的 ja_JP champion WAD）。

`lol-ahri` · `lol-ashe` · `lol-blitzcrank` · `lol-chogath` · `lol-fiddlesticks` ·
`lol-garen` · `lol-malphite` · `lol-ornn` · `lol-sett` · `lol-thresh` · `lol-velkoz`

⇒ ⭐ **缺的是解碼→轉檔→上架，⛔ 不是缺素材、⛔ 不需要 owner 裁決。**
⚠️ ⭐ 唯一要注意的是 **LOL 第一批那 7 名的下場**：他們上架了，⛔ 但全部落在
`ORIGINALS_ONLY`（原檔沒錄到的格子**永遠補不起來**）⇒ 第二批這 11 名做完之後
**大機率也是 8/9 或 7/9，⛔ 不是 9/9** —— ⇒ ⭐ **開工前就要說清楚驗收標準是「可聽」⛔ 不是「九格齊」**。

#### 桶② ⭐ **等 owner 聽審**（HITL）—— **18 名**

來源組**已經綁好了**（`heroGroupBindings`，21 筆 → 18 名），
⛔ 而 **21 筆全部 `runtimeApproved: false`** ⇒ 一個都還沒過人耳。

| 英雄 | 綁到幾組 | | 英雄 | 綁到幾組 |
|---|---:|---|---|---:|
| `acquired-alice` 愛麗絲 | 1 | | `acquired-mario` Mario | 1 |
| `acquired-astralym` 枯星龍 | 1 | | `acquired-mewtwo` Mewtwo | 2 |
| `acquired-asuna` 亞絲娜 | 2 | | `acquired-morgiana` 摩爾迦娜 | 1 |
| `acquired-cattiva` 搗蛋貓 | 1 | | `acquired-naruto` 漩渦鳴人 | 2 |
| `acquired-dio` DIO | 1 | | `acquired-pokemon-trainer` | 1 |
| `acquired-emilia` 愛蜜莉雅 | 1 | | `acquired-rim` 莉姆 | 1 |
| `acquired-jetragon` 空渦龍 | 1 | | `acquired-ryu` Ryu | 1 |
| `acquired-kuroyukihime` 黑雪姬 | 1 | | `acquired-saya` 沙耶 | 1 |
| `acquired-leafa` 莉法 | 1 | | `acquired-wargreymon` 戰鬥暴龍獸 | 1 |

⚠️ ⭐ 那份對照表的 `purpose` 欄自己寫得很準（逐字）：
**「A group link proves source identity only; it does not prove every clip's speaker,
language, transcript, or battle-event meaning.」**
⇒ ⭐ **「綁到組」⛔ 不等於「這一段是他講的、是日文、意思對得上」** —— 這正是要人耳的那一段。

⭐ **owner 已經逐字點名過這一族的三種踩雷**（#1170 body 頂端的裁決塊）：
「不用中文語音 你用300英雄的語音要**很小心**」· 「銀時 ⇒ 不使用 300英雄裡的中文語音 請找 J-Stars Victory」·
「悟空 …又是中文」⇒ ⛔ **機器分不出來的正是這個**（索引 1,060 組**全部 `language: "unreviewed"`、`speakerVerified` 0 組**）。

#### 桶③ ⭐ **等 owner 裁決** —— 全庫查不到對應語音組，**8 名**

| 英雄 | 索引裡**查到卻被排除**的（⭐ 排除理由是機器寫得出來的） |
|---|---|
| `acquired-beatrice` 碧翠絲 | Re:Zero 模型檔裡**沒有音訊** |
| `acquired-ram` 拉姆 | `300heroes:310` 是**蕾姆**，⛔ 不是拉姆 |
| `acquired-inuyasha` 犬夜叉 | `300heroes:35` 是桔梗 · `300heroes:115` 是殺生丸 |
| `acquired-lord-nightmares` 金色魔王 | `300heroes:46` 與 `mba:Chara02` 都是**莉娜**（⚠️ owner 2026-09-17 已裁「金色魔王 一樣用莉娜音效」，普攻／落敗 4 段已接上，⛔ 技能唸名／受傷／擊殺那一組還答不出來） |
| `acquired-minecraft` Steve／Alex | 索引的 SSBU 日文包裡**沒有 `vc_pickel` 組** |
| `acquired-zero` Zero | Zero Lancer 是 Diarmuid · `300heroes:198` 是 Fate/Zero 的 Iskandar |
| `acquired-kita-kita` 吉他吉他老伯 | ⛔ 沒有任何候選 |
| `acquired-xiaodangjia` 小當家 | ⛔ 沒有任何候選 |

⇒ ⭐ **這 8 名 ⛔ 不是「還沒查」——是查過了、查不到**，而且排除理由逐條引用得到。
⭐ 要 owner 決定的是三選一：**找新來源** ／ **借別人的聲音**（像金色魔王借莉娜）／ **就留空**。
⛔ 判準不可以是我挑（CLAUDE.md：這一格引用不到他的原話就不要動）。

### 2.4 ⭐ 參考音：票文說 **0 個**，今天 **107 個**

| | 票文（2026-09-10） | 今天 |
|---|---:|---:|
| `voice-reference-pipeline/approved/processed` | 51（⭐ **全部 `godie-*`**） | ⭐ **107** |
| 其中 `godie-*` | 51 | 52 |
| 其中 `b2-*` | **0** | ⭐ **22** |
| 其中 `community-review-*` | **0** | ⭐ **33** |

⇒ ⭐ 「那 74 名參考音 0 個」這一句**今天已經不成立**。
⚠️ ⛔ **但它也不是「全部補齊了」**：`approved/raw` 是 **0** 個、
`summary.synthesisReadyGroups` 是 **0** 組、`speakerVerified` **0 組** ——
⇒ ⭐ **參考音有了，而「這段是不是他本人講的」全庫零驗證。**

### 2.5 還卡在 owner 手上的兩張小清單（`combat:check` 自己印的）

| 卡什麼 | 誰 |
|---|---|
| ⭐ **等 `OWNER_LINES.csv` 填台詞**（⛔ 不代填，owner 2026-09-10 逐字「請你給我名單就好 不要自己產 我會手動填寫」） | `acquired-lord-nightmares`（select 池 · victory）· `godie-e00s` 白木卡迪那（select 池 · victory）· `lol-warwick`（victory）· `lol-xerath`（victory） |
| ⭐ **2 句中文台詞跳過**（合成只講日文）—— 要日文或指定原檔 | `b2-kisaragi.victory`「地獄就是不斷重複又重複。」· `b2-kisaragi.taunt`「永無休止。」 |

---

## 3 · 兩張票今天各自「還缺什麼」——⭐ 一句話版

### #1169 模型側

| 票文 AC | 今天 | 還缺什麼 |
|---|---|---|
| 1. 6 名 `b2-*` 轉檔或寫下卡在哪 | ⭐ **滿足** | —— |
| 2. `ou99.495015` 血泥宣告或移除 | ⭐ **滿足**（PR #1216） | —— |
| 3. 6 顆朝向量得到或進表帶理由 | ⚠️ **形式上滿足，實質變大** | ⭐ **159 顆（15.0%）朝向沒有人在量**。⛔ 不是補 6 顆能解的 —— 要一個**與 L/R 骨頭命名無關的第二線索**，或改走人眼驗收 |
| 4. 8 名 `skeleton-by-design` 不要動 | ⭐ **滿足**（今天只剩 2 名穿佔位骨架，兩名都是 `pending-source`） | ⭐ `acquired-beatrice` · `acquired-minecraft` 等 **owner 給來源** |
| （Scope 第 4 列）盤點表引用上限 160 | ⚠️ **4 列 → 1 列** | `b2-boxxo` 那一列已標「舊拒收理由失效，**本項尚未重新轉換及驗收**」⇒ 剩**重新轉換＋驗收**這一步 |

⭐ **驗收指令全綠**：`hiddenPrimitives.test.ts` ✓ · `modelFacing.test.ts` ✓ · `roster-guard/check.ts` ✓

### #1170 語音側

| 票文 AC | 今天 | 還缺什麼 |
|---|---|---|
| 1. `combatVoiceCoverage.test.ts` 🔴→🟢 | ⭐ **綠** | ⚠️ ⭐ **綠的意思是「37 個缺口被誠實宣告了」，⛔ 不是「補好了」** |
| 2. `pnpm combat:check` exit 0 | ⭐ **0** | —— |
| 3. 其餘語音閘保持綠 | ⭐ `index-lines --check` 0 | （`voices:check` 未在本 lane 跑 —— 它是主線 BMPNDD 的範圍） |
| 4. ⛔ 一句台詞都不是編的 | ⭐ 保持 | 4 名等 `OWNER_LINES.csv` · 2 句中文要日文 |

⭐ **真正的剩餘工作 = 37 名**，逐一有歸屬：
**11 名主線／Codex 可做**（LOL 第二批，來源已定到檔名）·
**18 名等 owner 聽審**（來源組已綁，21 筆全部 `runtimeApproved: false`）·
**8 名等 owner 裁決**（查過了查不到，排除理由逐條可引用）。

---

## 4 · ⭐ 給主線的三個建議（⛔ 我沒有自己做）

1. ⭐ **#1170 建議**：把 37 名**按上面三桶拆成三張子票**，⛔ 不要留成一張 37 名的票 ——
   三桶的**擋路的人完全不同**（工程／owner 的耳朵／owner 的決定），混在一張票裡
   會讓「11 名今天就能做」被另外 26 名卡住。
2. ⭐ **#1169 建議**：第 3 項（159 顆朝向）**已經不是這張票的原題了** ——
   原題是 6 顆，今天是一個**量尺對一整族瞎掉**的機制題。建議**另開一張**引用 GH#216／#1281，
   把 #1169 收斂到「2 名等來源 ＋ `b2-boxxo` 重新轉換」這兩件可收的事。
3. ⚠️ **兩張票都不要只看閘的顏色**：三條閘今天全綠，⛔ 而綠只證明「缺口被宣告了」。
   ⭐ 這兩張票的驗收標準是**宣告的數字變小**（`FACING_UNVERIFIED` 159 ↓ · `VOICE_GAP_BATCH4` 37 ↓），
   ⛔ 不是測試通過。

---

## 5 · ⛔ 本 lane 沒有做、也不該做的

| | |
|---|---|
| ⛔ 沒有新增／修改 `materials/hero-model-library/` 任何檔 | 柵欄是唯讀（git 衛生棘輪：materials/ ⛔ 不准長） |

### ⚠️ 一件**柵欄外**的觀察 —— ⭐ 主線要看一眼，⛔ 我沒有動它

`git status -- materials/` 在這棵樹上列出 **163 條未追蹤項**（展開後 **239 個檔**），
其中 `materials/hero-model-library/priority-evidence/` 一個目錄就 **115 MB**
（contact-sheet PNG · `files.jsonl.gz` · VFX 預覽圖…）。

⛔ **不是本 lane 產生的**（本 lane 只讀），⭐ 但它正好撞在兩條守則上：
① 任務單自己寫的「materials/ 這一類**不准長**（git 衛生棘輪）」；
② CLAUDE.md「**一個位元組如果只能靠雜湊驗、不能靠 diff 讀，它就不屬於 git**」
⇒ 這 115 MB 的 PNG／gz **屬於 S3**，⛔ 不屬於 git。

⇒ ⭐ 建議主線在 BMPNDD 之前確認它們**不會被任何一條 lane 的 `git add` 掃進去**
（本 lane 的 commit 用逐檔 pathspec，⛔ 一個位元組都沒帶到）。
| ⛔ 沒有動 `content/` | 這兩張都是盤點票，⛔ 沒有內容改動 |
| ⛔ 沒有關票 | ⭐ 關不關由主線決定 |
| ⛔ 沒有跑 `skills:sync` / `content:build` / `ship:check` | 主線最後統一跑一次 BMPNDD |
| ⚠️ 沒有跑 `pnpm voices:check` / `assetsInContentVersion.test.ts` | 它們在主線 BMPNDD 的範圍內，本 lane 的 vitest 額度（2 次）留給兩張票的承重閘 |
