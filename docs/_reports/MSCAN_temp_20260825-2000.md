# MSCAN — 長期記憶與 CLAUDE.md 殘留誤導稽核（2026-08-25 20:00）

> 唯讀 lane。每一筆都在 `/Users/Takuro/GGD` 底下逐一驗證過（sync-io.json / package.json /
> 檔案系統 / git tag / 測試檔頭），⛔ 不含推測。已修的 34 處（`docs/_reports/product-edit-misinfo_20260825.md`）不重報。
> 掃描範圍：記憶 54 份全讀 · CLAUDE.md 全文 · docs/_session-handover.md · docs/_execution-batches.md。

## 判定依據（量到的）

| 依據 | 值 |
|---|---|
| 產物擁有權 | `tools/parallel-gates/sync-io.json` 38 步、621 份產物 |
| 最新 tag | `v0.27.3 2026-08-25`（`git for-each-ref`） |
| effect kinds | `schema/effects/index.ts:74-137` union **47 個成員**，含 `dot`（dot.ts:87） |
| 出貨 status-effects | `content/status-effects/` **43 份**（不含 _index） |
| `bundle.test.ts` | 檔頭 :90-91 逐字 "independent of whether anyone has run `pnpm content:build`" |
| `legendaryClaims.test.ts` | main 上**不存在**；`docs/requirements-status.md:2125`「刻意不合併 9542d2b」 |
| release 規則 | `scripts/release.sh:33` 跨天 → minor+1、patch 歸 0 |

---

## 🔴 高（會直接誘發「改產物」或錯誤動作）

### 1. `memory/MEMORY.md:22` — 索引行本身就是誤導源原型
> 「every content/ edit needs `pnpm content:build` before commit, **or bundle.test.ts goes red**」

雙重為假：①沒有 genguard 前置步（content/ 底下 621 份是產物）——正文已在 08-25 重寫，索引沒跟上；
②bundle.test.ts 宣稱**驗證過為假**（檔頭自述獨立於 content:build；真守衛是 `shippedBundleIsCurrent.test.ts`）。
**替換**：「改 content/ 之前先 `bash scripts/genguard.sh <path>`（621 份是產物）；手編檔才 content:build＋commit 產物（守衛＝shippedBundleIsCurrent.test.ts，⛔ 不是 bundle.test.ts）」

### 2. `memory/ggd-content-build-after-edit.md:35` — 08-25 剛重寫的檔仍留一句假的
> 「⛔ 不跑的話 `bundle.test.ts` 會用一個很不好讀的訊息紅（…＝索引過期）」

CLAUDE.md:1476 早已記錄「這句話是假的」，而 bundle.test.ts:90-91 檔頭自證。
**替換**：「⛔ 不跑的話 `shippedBundleIsCurrent.test.ts` 紅（它比對 repo 裡被 commit 的那一份；bundle.test.ts 在 temp 樹重建，**永遠綠**）」

### 3. `memory/ggd-ability-effect-repair.md:15-16 · :28` — 教人手改兩邊鏡像／逐行手編 champion JSON
> 「Any edit must **write both** or the editor silently desyncs」·「Never bulk-edit `content/champions/*.json` via a Node JSON round-trip … **Edit the specific line(s) instead**」

2026-07-26 寫的，零過期註記。現況：content/abilities **331 份**、content/champions **56 份**是產物（chmod 444），
正確動作是 genguard → 改 `tools/skill-remake/heroes/*.py` → `genrun.sh skillremake:json`。這正是「上百次事故」的形狀，
而姊妹檔 `ggd-mirror-authority-model.md` 08-25 已修，這一份沒有。
**替換**：檔頭加更正塊：「⚠️⚠️ 2026-08-25 起：動 content/{abilities,champions} 之前先 `bash scripts/genguard.sh`；產物走 .py 來源＋genrun（見 ggd-mirror-authority-model）。下述『write both』『edit lines』只適用**兩邊都不是產物**的檔。」

### 4. `CLAUDE.md:1474` — 稽核漏網的同型句（:352/:1353/:1358 已修，這行沒有）
> 「每一次 `content/` 編輯都要跑 `pnpm content:build`，**而且要把產物一起 commit**」

稽核報告把它（舊 :1451）列為「310 次同型句的出處原型」，但半徑 4 的修法表沒排它，至今原樣。
**替換**：行首補「⚠️ 先 `bash scripts/genguard.sh <path>` —— `content/` 底下 621 份是產物，這一條只適用**手編檔**；」

### 5. `CLAUDE.md:598` — 守衛訊息修了、守則沒修（稽核自己警告過「只改一處等於沒改」）
> 「需要改平衡資料（`config/stat-caps.json` 的 `unlocked`）」

`stat-caps.json` 是 `statcaps:build` 產物（unlocked＝`gen_stat_caps.ts` 的 `capAt[...]` 算出來的）。
配對的 `noOpModifierClaims.test.ts:356-363` **已修**成指向來源；CLAUDE.md 這一半沒動。
**替換**：「需要改平衡資料（`tools/stat-caps/gen_stat_caps.ts` 的 capAt 表 —— `content/config/stat-caps.json` 是 statcaps:build 的產物，手改必被 statcaps:check 判 stale）」

---

## 🟠 中（過期指示／幽靈名／記憶互相矛盾）

### 6. `memory/ggd-push-release-notes.md:20`＋`MEMORY.md:17` — 版號規則整段是舊制
> 「**第二段(minor)什麼時候動?** owner 沒授權過自動跳 —— 除非他明講,否則**永遠只動第三段**」

驗證為假：`scripts/release.sh:33` 跨天**必須** minor+1、patch 歸 0（正是這條舊散文造成 v0.9 破 10 天，CLAUDE.md 有記）。
正文「each push gets its own tag／同日 v0.4.0→0.4.1→0.4.2 是正確做法」也牴觸第零守則「一天一次發版」。
索引行「patch bump per push」同病。**替換**：加過期塊：「⚠️ 2026-08-05 起：版號一律 `bash scripts/release.sh --tag`（跨天 minor+1）；一天攢成一版；工作 session ⛔ 不 push（見 ggd-dont-push-deploy-unilaterally）。」

### 7. `memory/ggd-gcp-deploy.md:27 · :33 · :73-77 · :89`＋`MEMORY.md:19` — 整份 runbook 是被取代的舊部署法
六步協定第 3/6 步「**Playtest … for real**」已被 owner 2026-08-09 退役（`ggd-no-manual-playtest.md` 明說 CLAUDE.md 已改，這一份卻原樣保留 → 記憶互相矛盾）；
「`nohup bash deploy.sh &`」＝CLAUDE.md 地雷 1 的失敗形狀；手打 `$DC build && $DC up -d` 會掉 `GGD_BUILD_STAMP`（地雷 4）。
唯一入口是 `ssh -A can@34.81.104.163 'cd /home/can/GGD && bash scripts/host-deploy.sh'`。
**替換**：檔頭加「⚠️ 2026-08-09/08-16 起本檔 runbook 段全部過期，唯一入口 host-deploy.sh（自驗五項後置條件）；打一場×2 已退役；以下只留作考古」。索引行「playtest→push→deploy→playtest pipeline」同步改。

### 8. `memory/ggd-never-test-on-production.md:18` — 「允許清單」教的是會產 UNSTAMPED-BUILD 的指令
> 「What IS still allowed on the live host …: `git pull` + `docker compose build/up -d`」

裸 compose build 掉版本戳（CLAUDE.md 地雷 4）。**替換**：「deploy 一律 `bash scripts/host-deploy.sh`（唯一入口）；其餘允許的只有讀 log／狀態／data 備份／唯讀驗證」。

### 9. `memory/ggd-ability-effect-repair.md:34 · :40 · :43 · :49 · :70` — 五句能力／流程宣稱全部過期
- :40「Only **8** effect kinds … no DoT, no aura」→ 現在 union **47 個**成員，`dot` 是其中之一（effects/index.ts:74-137）。
- :43「statusIds only burnstun/root/slow25/30/40」→ 出貨 **43 份**（stun/fear/burn/paralysis/bankai…）。
- :34「`ability@1` has no passive/hook support」→ ability.ts 有完整 hooks／augment 詞彙。
- :49「work happens on branch `campaign/complete-tasks`」→ 現在是 main。
- :70「`_index.json` … usually **not yours to commit**」→ 與 CLAUDE.md「產物一起 commit」（shippedBundleIsCurrent 在守）**相反**。
**替換**：整檔加「以下為 2026-07-26 快照」時間柵欄＋逐句更正（能力問題一律以 `editorCapabilities.ts` 推導清單為準，正是 `ggd-capability-answers-need-full-doc` 教的）。

### 10. `memory/ggd-capability-answers-need-full-doc.md:25` — 引用一支不存在的守衛
> 「去跑既有的對帳守衛。`legendaryClaims.test.ts` 已經雙向核對 49 件…」

main 上沒有這個檔；`docs/requirements-status.md:2125` 記著它「刻意不合併（紅的）」。教人「跑既有守衛」的那一句自己指向幽靈。
**替換**：「`legendary49OwnerText.test.ts` / `legendaryOwnerRulings.test.ts`（packages/shared/src/content/）」。

### 11. `memory/ggd-conflict-precedence-ladder.md:28` — 另存檔路徑錯
> 「另存（`docs/_w3x-fidelity-superseded.md`）」→ 該路徑不存在。實際：`docs/legacy/_w3x-fidelity-superseded.md`（CLAUDE.md 同）。

### 12. `docs/_session-handover.md:14 · :119-121` — CLAUDE.md 點名的「現況」文件停在 17 天前
「線上現在是 **v0.10.1**」（實際 v0.27.3）；「owner 正在手動重製全部英雄技能，⛔ **不要動 `content/abilities/`，等他做完**」——
重製早已走 `skillremake:json` 出貨，現行規則是 genguard＋產物隔離區，理由完全不同。
CLAUDE.md 第一行就叫人「架構與現況看這一份」。**修法（主 session）**：更新現況段＋刪除/改寫 :119-121，或在頂部加大字「⚠️ 2026-08-08 快照，現況以 git tag 與 docs/_release 為準」。

### 13. `docs/_execution-batches.md:10-15` — 「待辦」文件的現況段落後 7 個 minor
現況段自稱 2026-08-18 v0.20.0；實際 v0.27.3（08-25）。violates 它自己 :6 的規則「每一次 push 都要更新現況段，否則下一個人會照過期的東西做」。
其後的批次表（:587 / :605 等「lane 獨佔 content/abilities 改寫」）也還是 07-31 的世界觀。
**修法（主 session）**：刷新現況段；建議把現況段改成產生的（board:build 家族），讓它不能再腐爛。

---

## 🟡 低

### 14. `memory/ggd-project.md:13 · :31 · :36`
- :13 「Approved plan: `~/.claude/plans/3v3v3v3-ai-quirky-donut.md`（read it before continuing）」→ 檔案不存在。
- :31 「Verification entrypoint: `make test`」→ 已被 pnpm 逐包＋ship:check 取代。
- :36 「See [[ggd-user-decisions]]」→ 該記憶檔不存在（幽靈連結）。
**修法**：時間柵欄＋修指標。

### 15. `memory/ggd-rolling-log-discipline.md:11 · :15` — 教維護兩頁死掉的手寫記錄
`docs/_requirements-audit-gaps.md` 最後 commit 2026-07-31、`docs/_hitfeel-audit.md` 07-24 —— 其後數百張票沒有一筆照它做。
現行制度是 owner 2026-08-20 的「gh issue＋ruling.sh＋當日帳本」。**修法**：加過期註記指向新制度（requirements-status.md 是 docs:status 產物那半仍正確）。

---

## 附：查過而**沒有**問題的（避免下一輪重查）
- `items:csv:import/export`、`echoloop:check`、`spec:build`、`caps:export` 等指令名全部真實存在。
- `ggd-mirror-authority-model.md`、`ggd-content-build-after-edit.md` 的 08-25 更正主體正確（僅 #2 那一句漏網）。
- `tools/w3x-import/out/` 確實 git tracked；EX_MAP.json 無產生器擁有者。
- `ggd-server-spec-and-scale`、`ggd-no-manual-playtest`、`ggd-dont-push-deploy-unilaterally`、`ggd-lanes-sweep-my-files`、
  `ggd-consumer-exists-but-cannot-consume`、`ggd-memory-temp-before-compaction` 引用的守衛與腳本逐一存在。
- 稽核已排定但未落地的 CLAUDE.md:1017（「沒有任何合法情境」）與 genguard 節盲區表：已在 product-edit-misinfo 報告內，不重報。

⛔ 本 lane 未改任何檔（記憶與 CLAUDE.md 動之前要 `bash scripts/backup-rules.sh`，主 session 執行）。
