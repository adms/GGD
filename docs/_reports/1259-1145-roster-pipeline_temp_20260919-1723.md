# #1259 / #1145 — 新英雄上架一條龍：**兩張票的前提今天都過期了**

> lane `roster-pipeline-1259` · 2026-09-19 · 工作樹 `/private/tmp/ggd-integ-nc`（main `fdbc80a03`）
> ⛔ 本 lane **沒有改任何程式** —— 產出是「量到的數字」。

---

## ⭐ 一句話

**#1259 已經整條在 main 上，而且有閘在守；#1145 的 190 位英雄模型與圖示 100% 齊全。
今天唯一還缺的是「語音」一個軸 —— 56 位。**

---

## ① #1259 的前提：「11 個 commit 只在分支 `feat/hero-intake-pipeline`」⇒ ⛔ **不成立**

| 量什麼 | 指令 | 結果 |
|---|---|---|
| 那條分支還在嗎 | `git for-each-ref \| grep -i hero.intake` | ⭐ **全 repo 零個 ref**（本地／remote／worktree 都沒有） |
| 主線上有幾個 commit | `git log --oneline main -- tools/hero-intake/ apps/admin/src/ui/HeroIntakePage.tsx docs/_review/material/hero-intake/` | ⭐ **40 個** |

⭐ 票文說的那批功能，逐字對得上主線的兩個 commit：

| commit | 訊息 |
|---|---|
| `60fc145aff` | `feat(intake): 🧍🖼🎙 新英雄上架**一條龍** —— 模型對應／圖示／語音配對全自動 → 後台一頁檢核` |
| `48492a8422` | `feat(intake): 🔑 34 名新英雄的模型交付**對帳** —— 一把 join key、兩個相反的空集合` |

⇒ ⭐ **「11 個 commit 還缺哪幾個」的答案是：一個都不缺。**

### 四個住處＋消費端逐項查過（CLAUDE.md 第一守則）

| 層 | 住處 | 在不在 |
|---|---|---|
| 工具 | `tools/hero-intake/run.mjs`（47,742 B）＋ 4 支輔助 | ✅ 已追蹤 |
| 指令 | `package.json` → `hero:intake` / `hero:intake:check` / `handoff:build:raw` / `handoff:check` | ✅ |
| ⭐ **閘** | `hero:intake:check` ＋ `handoff:check` **都在 `skills:check` 的長鏈裡** | ✅ |
| 後台頁 | `apps/admin/src/ui/HeroIntakePage.tsx` | ✅ 已追蹤 |
| ⭐ **消費端** | `App.tsx:97` import · `App.tsx:400` 導覽列「🧍 新英雄上架檢核」· `App.tsx:1293` 真的 render · `store.ts:192` page union | ✅ ⛔ **不是裝飾** |
| 守衛 | `navSections.test.ts:185`（逐字引 owner 2026-09-11「這一頁也要放到後台管理頁」） | ✅ |

### 今天實跑的兩道閘（唯讀，⛔ 沒有重生成）

```
node tools/hero-intake/run.mjs --batch ship153 --all --check
  EXIT=0  ✓ docs/_review/material/hero-intake/ship153.json 是最新的（190 位）

python3 tools/hero-intake/make-asset-handoff.py --check
  EXIT=0  ✓ docs/素材缺口交接單.md 與現況一致（資料指紋 8a553a31df54e462）
```

---

## ② #1145 的前提：「37+37 名完整材料交付 Main 驗收」⇒ ⭐ **模型／圖示已 100% 落地**

`ship153.json`（digest `e3763a6f…`，⭐ 444 唯讀產物，genguard 說⛔不可手改）逐位量到：

| 軸 | 數字 | 判讀 |
|---|---:|---|
| 英雄總數 | **190** | |
| `inContent` | **190 / 190** | ⭐ 沒有任何一位還卡在「別的 repo 交付」階段 |
| 🧍 **模型 ok** | ⭐ **190 / 190**（`modelGaps = 0`、`modelCompletelyMissing = 0`） | ⭐ 交付驗收**已完成** |
| 🖼 **圖示 ok** | ⭐ **190 / 190**（`iconGaps = 0`） | ⭐ 同上 |
| 🎙 語音 ok | **123 / 190** | ⛔ 唯一的缺口 |
| **ready（三軸全過）** | **134** | |
| **blocked** | **56** | ⭐ **100% 是語音**，⛔ 零個模型／圖示原因 |

> ⚠️ **一個要記下來的限定詞**：這次 `--check` 的 `invocation` 是
> `--batch ship153 --all --no-gen-icons`，**沒有 `--delivery`**。
> `run.mjs:500` 明寫：沒給 `--delivery` 就「答不出模型在別的 repo 交付了沒」。
> ⭐ 但那條路**只在英雄還沒進 content 時才會走**，而今天 `inContent = 190/190`
> ⇒ 這個限定詞**今天沒有咬到任何一位**。
> （沿用 CLAUDE.md「讀一張表先問這一欄的分母是什麼」。）

---

## ③ 那就逐項列出「哪一位缺什麼」—— 56 位，全部缺語音

### A. 有候選來源、**配對就能解**：35 位

⭐ 這 35 位不必新錄音，索引裡已經指得到聲音來源。

| 英雄 | 首選來源 | 檔數 |
|---|---|---:|
| `godie-e00l` 亞瑟王 Saber | 亚瑟王（300英雄） | 584 |
| `godie-h01o` 黑崎一護 | 小一（300英雄） | 649 |
| `godie-u00o` 魯夫 | 草帽小子（300英雄） | 348 |
| `godie-udre` 索隆 | 佐罗（300英雄） | 226 |
| `godie-umal` 拳四郎 | Kenshiro（公開來源） | 217 |
| `godie-nbbc` 勇者小呆 | 小呆／達伊（公開來源）※5 個候選 | 261 |
| `godie-hjai` 莉娜因巴斯 | 莉娜·因巴斯（300英雄）※3 個候選 | 232 |
| `godie-ogrh` 悟空 | 孫悟空（公開來源） | 69 |
| `acquired-alice` 愛麗絲 | 爱丽丝（300英雄） | 235 |
| `acquired-asuna` 亞絲娜 | 狂暴补师（300英雄）※2 | 522 |
| `acquired-emilia` 愛蜜莉雅 | 爱蜜莉雅（300英雄） | 190 |
| `acquired-leafa` 莉法 | 莉法（300英雄） | 167 |
| `acquired-kuroyukihime` 黑雪姬 | 黑雪姬（300英雄） | 187 |
| `acquired-morgiana` 摩尔迦娜 | 摩尔迦娜（300英雄） | 282 |
| `acquired-rim` 莉姆 | 莉姆（300英雄） | 326 |
| `acquired-saya` 沙耶 | 沙耶（300英雄）※2 | 194 |
| `acquired-wargreymon` 戰鬥暴龍獸 | 龙教练（300英雄） | 214 |
| `acquired-naruto` 鳴人 | Naruto（公開來源）※2 | 218 |
| `acquired-dio` DIO | DIO（公開來源） | 243 |
| `acquired-mario` Mario | SSBM Characters_Mario ※2 | 32 |
| `acquired-mewtwo` Mewtwo | NS 原生 vc_mewtwo ※2 | 24 |
| `acquired-ryu` Ryu | NS 原生 vc_ryu | 55 |
| `acquired-pokemon-trainer` | NS 原生 vc_ptrainer | 58 |
| `acquired-zero` Zero | Zero Lancer（⚠️ conf=**candidate**，⛔ 其餘皆 high） | 13 |
| **LoL 11 位** `lol-ahri` `lol-ashe` `lol-blitzcrank` `lol-chogath` `lol-fiddlesticks` `lol-garen` `lol-malphite` `lol-ornn` `lol-sett` `lol-thresh` `lol-velkoz` | League of Legends `ja_JP` 16.18.8159717 | 151–1378 |

⭐ **11 位 LoL 全部指到同一個官方 `ja_JP` 語音包版本** ⇒ 這是一批可以一次處理的同型工作
（第零守則⑨：N 個同型 = 1 個模板 + 一張表，⛔ 不是 11 輪）。

### B. **全庫零候選**，要新找／新錄：21 位

`acquired-beatrice` 碧翠絲 · `acquired-inuyasha` 犬夜叉 · `acquired-kita-kita` 吉他吉他老伯 ·
`acquired-minecraft` Steve／Alex · `acquired-ram` 拉姆 · `acquired-xiaodangjia` 小當家 ·
`b2-maple-alt-9769eb88b85b` 梅普露（變身）· `godie-e00n` 龍宮禮奈 · `godie-e00x` 櫻綻剎那 ·
`godie-e010` 白木卡迪那 · `godie-ewar` 天地志狼 · `godie-h02v` 草泥馬 · `godie-hgam` 妙蛙種子 ·
`godie-n01g` 依文潔琳 · `godie-nsjs` 南野秀一 · `godie-o02l` 皮卡丘 · `godie-o030` 臭作 ·
`godie-u034` 傑富力士 · `godie-uvng` 飛影 · `sela` · `thorne`

> ⚠️ **`sela` 與 `thorne` 要從分母裡拿掉** —— 它們是 CLAUDE.md 記的**骨架 fallback 英雄**
> （內容載入失敗時退回的那兩隻），⛔ 不是要上架的角色。
> ⇒ ⭐ **真正要補聲音的是 19 位**，⛔ 不是 21 位。

### C. ⚠️ `voicePending = 11` —— **有語音包但沒到出貨門檻**（`severity: warning`，⛔ 不擋上架）

門檻是 9 格必備類別。這 11 位已算在上面的 `ready 134` 裡：

| 英雄 | 有幾格 | 缺 |
|---|---:|---:|
| `acquired-astralym` 枯星龍 · `acquired-cattiva` 搗蛋貓 · `acquired-jetragon` 空渦龍 · `lol-lux` 拉克絲 · `lol-xerath` 齊勒斯 | 8/9 | ⭐ 各缺 **1 格** |
| `lol-missfortune` 好運姐 · `lol-warwick` 沃維克 · `lol-yasuo` 犽宿 | 7/9 | 2 格 |
| `lol-leesin` 李星 | 6/9 | 3 格 |
| `lol-karthus` 卡爾瑟斯 | 4/9 | 5 格 |
| `acquired-lord-nightmares` 金色魔王 | 2/9 | ⛔ **7 格** |

⭐ 前 5 位各補 1 格就滿門檻 —— 這是**最便宜的一批**。

---

## ④ ⇒ 主線要知道的三件事

1. ⭐ **#1259 建議可關**（由主線裁決）—— 工具＋後台頁＋閘三層齊全、消費端查過、兩道 `--check` 今天 EXIT=0。
   ⛔ 票文的「11 個 commit 擱在分支上」是**過期的前提**，那條分支**已經不存在**，功能在 main。
2. ⭐ **#1145 的模型／圖示軸可關**（190/190）；⛔ **語音軸別關** —— 它是 56 位的唯一阻塞。
   建議把語音獨立成一張票，⛔ 不要讓它繼續掛在「材料交付」底下（兩個不同的事實，兩個寫入端）。
3. ⚠️ **⛔ 本 lane 沒有動 `ship153.json`** —— 它是 444 唯讀產物且工作樹乾淨，
   genguard 判「檔頭自己寫著它是產生的，⛔ 不要手改」。要更新走 `hero:intake`，⛔ 不是編輯它。

---

## ⑤ 誠實的限制

- ⛔ **沒跑** `skills:sync` / `content:build` / `ship:check` / `pnpm test`（lane 硬規則）。
- ⛔ **沒跑 vitest**（本 lane 沒改程式，沒有要守的新行為 ⇒ 額度用 0 次）。
- ⭐ 上面每一個數字都出自**今天實跑出貨工具**的輸出與 `ship153.json` 本體，⛔ 不是引用票文或既有文件。
- ⚠️ 語音「候選來源」只代表**索引指得到**，⛔ 不代表聽審過 —— `speakerVerified: false`、
  `transcriptStatus: not-transcribed`、`language: unreviewed` 在多數候選上都成立。
  ⇒ 配對之後仍要走 HITL 聽審（CLAUDE.md 第二守則的 Tier 2）。
