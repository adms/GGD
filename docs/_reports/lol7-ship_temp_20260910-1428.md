# 七名 LOL 英雄上架 —— 完整報告（GH#1158 / GH#1165）

> owner 2026-09-09（逐字）：「其實還有**七個LOL英雄**也要跟著上架喔」
> owner 2026-09-10（逐字）：「⋯**已經取得審查授權可以直接上架，被認定為預設官方角色**⋯
>  **全角色模型盤點.md 會持續更新模型預設對應表，請你也配合改變上架設定**」

分支：`worktree-agent-a242b49121471b454` · 產生時間 2026-09-10 14:28

---

## ⛔⛔ 最重要的一個發現：`b7a057080` 的結論**是錯的**，而它讓這七名多卡了一輪

那個 commit（今天早上 06:46，已在 main 上）逐字寫著：

> 「⛔⛔ 而他們的**招式與數值不存在** —— 這一輪查到底了⋯
>  ⭐ **技能組不存在**，⛔ 不是「還沒找到」——⛔ 而我不替社群作者發明招式與平衡。」

它掃過 `content/` · `materials/community-hero-forge/recipes` · S3 的 13 個 manifest ·
20 個 `packageDigest` · 本機編輯器的 catalog-store · 素材庫 —— ⭐ **六條路**。

⛔ **而它沒有掃出貨的 TS。** 七名的完整六格招式住在

    packages/shared/src/content/heroForge/communityExamples.ts   `COMMUNITY_HERO_EXAMPLES`

`20702b3a5`（**2026-09-06**，commit 訊息逐字「adapt seven community acceptance heroes」）
就把它併進 main 了 —— 比那句「不存在」早了四天。而且
`docs/_reports/community-hero-forge/community-concepts/deterministic/proof.json`
的 `status` 逐字是 **`passed`**（真的 `SimWorld` 跑過六格，`kit.status: accepted`）。

⇒ ⭐ 這是 CLAUDE.md 那一條的第五個實例：
**「我掃的是哪一條路？還有別條嗎？分母是誰？」** ——
一個「查到底了」的結論，分母漏掉了 `packages/shared/src/**`。

⚠️ 而這個假前提的代價正是 CLAUDE.md 記過的那一種：**它會讓下一個實作者
再做一次已經存在的東西**（這裡是「等社群補招式」），而真缺口原封不動。

---

## ⭐ 做了什麼 —— 三張表 join，⛔ 零個新規則

| 層 | 來源 | ⛔ 不是 |
|---|---|---|
| 招式 · 數值 · 說明 · 出身 · 改編註記 | ⭐ `communityExamples.ts`（出貨 TS） | ⛔ 我編的 |
| 屬性 · 成長 · 級距 · vfx-script · 模板實例 | ⭐ `compileHeroPackageProject()`（出貨的編輯器編譯器） | ⛔ 手組 champion@1 |
| `modelKey` | ⭐ owner 的 `全角色模型盤點.md` 第「LOL 追加 7 名」節（經 `model_map.py`） | ⛔ 抄一份對應表 |
| 級距清洗 · 圖示接線 · 狀態機制補齊 | ⭐ 沿用 `gen.py` 的 `drop_baked_values` / `attach_ability_icon` / `backfill_status_mechanics` | ⛔ 重寫一份 |
| 頭圖 · 技能圖示 | ⭐ 出貨的 `tools/icon-gen/local/batch.py`（同一組 FATE 提示詞與同一條兩段式管線） | ⛔ 另一條路 |

### 新增的兩支（都住 `tools/ship-81/`，⛔ 不是第三支產生器）

| 檔 | 做什麼 |
|---|---|
| `tools/ship-81/lol7_compile.mts` | 模型解析結果 in → 出貨編譯器 → `champion@1` ＋ 6×`ability@1` ＋ `vfx-script@1` ＋ 模板實例 out |
| `tools/ship-81/lol7.py` | 編排：`model_map` 解析 → 呼叫上面那支 → `gen.py` 的三支後處理 → 寫 `content/` → 報告 |
| `tools/ship-81/model_map.py` | ＋ 一支唯讀 CLI（`--section`），⭐ 讓盤點表的解析規則**只有一個住處** |

⭐ **冪等**：連跑兩次，`content/champions/lol-lux.json` 與 `content/abilities/lol-lux.q.json`
逐位元組相同（catalog 在編譯前刻意排除 `lol-*` 自己的產物）。

---

## ⭐ 逐名結果（7/7 全滿）

| id | 名稱 | 出身/role | 頭圖 | 技能圖示 | 模型狀態 | 素材庫 asset id | GLB SHA-256（前 12） |
|---|---|---|---|---|---|---|---|
| `lol-karthus` | 卡爾瑟斯 | 法師 / mage | ✅ | 6/6 | real | `ggd.model.d76a057d4869e4913d9a7327` | `f95c32ef3c98` |
| `lol-leesin` | 李星 | 鬥士 / fighter | ✅ | 6/6 | **real-late** | `ggd.model.f9e0593bc69748052ff9f72d` | `17edce57f8c0` |
| `lol-lux` | 拉克絲 | 軟輔 / fighter | ✅ | 6/6 | real | `ggd.model.5ad7e0cc65e3eca8ead74227` | `5848bf71f2ea` |
| `lol-missfortune` | 好運姐 | 射手 / marksman | ✅ | 6/6 | real | `ggd.model.589d121ff37d383fbfaa85f6` | `8b81e5d5676b` |
| `lol-warwick` | 沃維克 | 狂戰 / fighter | ✅ | 6/6 | **real-late** | `ggd.model.b021c68877664a89b7ec8d46` | `11c877bf4348` |
| `lol-xerath` | 齊勒斯 | 法師 / mage | ✅ | 6/6 | real | `ggd.model.0817d46424cf8193b7b3229a` | `1d970e175efd` |
| `lol-yasuo` | 犽宿 | 鬥士 / fighter | ✅ | 6/6 | **real-late** | `ggd.model.1b523670f582b44d8f9595b9` | `87197c94cc04` |

⭐ 每一顆 GLB 的 SHA-256 都**當場重算並與素材庫 `catalog.json` 比對過**（7/7 相符），
⛔ 不是抄 catalog 的欄位。位元組留在素材庫／S3，⛔ 一個都沒進 git。

⚠️ **`real-late` 是什麼**：owner 的盤點表把李星／沃維克／犽宿標成「**待取得核准模型**」，
理由逐字是「單段動作通道 196 超過英雄模型上限 **160**」——
⭐ 而 `content/config/model-lod.json` 的 `championChannelLimit` 今天是 **500**（GH#1164 已改成後台可調）
⇒ **三個理由全部不再成立**，`model_map.stale_blockers()` 已經逐列喊出來（另加 `b2-boxxo` 一列）。
⇒ ⭐ **這張表是 owner 的檔，我沒有改它** —— 請他自己把那三列的「待取得核准模型」更新掉。

---

## ⭐ 上架面：144 → **151**（＋7）

```
npx tsx tools/roster-guard/check.ts   → exit 0（12 條關係全過）
上架面：英雄 151（可選本體＋變身態） · 技能 898 · 道具 89
```

| 動到的名單 | 從 | 到 |
|---|---:|---:|
| `starter.go` 的 `starterChampions` | 123 | **130** |
| `starter_content_test.go` 的 `firstOpenRoster` | 123 | **130** |
| `starterPricedChampions` | 111 | **118**（⭐ 免費那 12 位一位都沒動） |

---

## ⛔⛔ 一個「build 綠而技能是死的」缺陷，第一版就中了

第一次 `content:build` 的訊息逐字：

> `[content] 16 支技能的模板展開失敗，已個別降級（其餘內容照常註冊）⋯其中 7 份降級後完全沒有效果`

⭐ 根因與 GH#1165 的 `106e93707`（第二批 222 支）**完全同型**：
模板**實例**（`hero-template.<內容雜湊>`）住在 `compiled.dependencies`，⛔ 不是 `runtime`
⇒ 我第一版只搬 `runtime` ⇒ 5 份模板缺席 ⇒ 16 支技能 fail-open 降級，
⭐ **而 `content:build` 仍然 exit 0、選人畫面看得到那七名**。

⇒ 修法：`lol7_compile.mts` 把**被引用到的** `hero-template.*` 一起帶出來（5 份新的）。

⚠️ ⭐ **而修它的時候踩到第二個坑**：我用 `indent=2` 寫回去
⇒ **7 份既有模板整份重排**（內容一模一樣、595 行假 diff）。
出貨的 19 份 `hero-template.*` 全部是 **JCS 正規形**（鍵排序、不留空白）——
⭐ 它們的 id 就是那串位元組的雜湊。⇒ 改成 `sort_keys=True, separators=(",",":")`
⇒ 現在 **7 份既有的逐位元組未動，只新增 5 份**。

---

## ⭐ 平衡的漣漪：級距**一格都沒變**

母體從 123 → 130，四支衍生產生器重跑（`anchors:build` · `statcaps:build` ·
`archetypes:build` · `mana:audit`，以及 `tools/engine-atlas/statTiers.ts`）：

| | 從 | 到 |
|---|---:|---:|
| 傷害五級距 250/625/1250/1875/2500 | —— | ⭐ **完全不變** |
| LV30 中位 maxMana | 1756.9 | 1760.5（＋0.2%） |
| `maxMana` 上限 | 351387 | 352091 |
| `docs/hero-archetypes.json` 母體 | 123 位 | 130 位 |

⇒ ⭐ 加七名英雄**沒有動到任何一格級距**，出貨平衡實質不變。

---

## ⚠️ 三件刻意的事（都寫在程式的檔頭裡）

1. **`heroBody` 只在記憶體裡打開。** `heroBodyModelIds()` 的規則是
   「`heroBody===true` **或**已經被某張英雄卡綁著」—— 這七顆今天兩個都不是（卡還不存在）。
   ⛔ 而這**不是繞過閘**：那個閘問的是「這顆是不是核准過的英雄本體」，
   而每一顆都來自素材庫 `kind: model-body` ＋ `status: standardized` ＋ owner 表的「預設模型」欄。
   卡寫進去之後，綁定本身就讓它成為 hero body ⇒ ⭐ **出貨樹不必多一個欄位**。
2. **catalog 排除 `lol-*` 自己的產物** ⇒ 這一支可以重跑（⛔ 否則第二次會死在
   「社群作品不能佔用既有官方英雄的身分」）。
3. **`vfx-scripts` 一起寫**（35 份）。它們是編譯器對這七名輸出的一部分
   （施法 anim pulse ＋ 技能名浮動文字）；不寫的話出貨樹與編譯結果會靜靜地不一樣。

---

## ⛔ 我**沒有**做到的（誠實列出）

| 缺什麼 | 為什麼 |
|---|---|
| **Tier 2 HITL 人工驗收**（圖示像不像、演出對不對） | ⭐ 49 張圖示是機器產的（Tier 0/1 過了：非空白、非全黑、非破圖，我逐張抽看過兩張）。⛔ 而「像不像拉克絲」是 owner 的那一層，⛔ 不是我能宣稱的 |
| **遊戲畫面驗收**（第一·四守則的第 4 道界線） | ⛔ 沒有跑起遊戲 —— 這一輪的宣稱只到「**鏈路已接上**」。⛔ 不說「玩家看得到了」 |
| **`pnpm skills:sync`** | ⛔ 被明文禁止（併行工作流的鎖）。⇒ 主 session 收工時要跑一次，⭐ 而 `content:build` 之外的衍生檔（Codex 契約、engine atlas、道具 CSV、狀態頁…）現在是**過期**的 |
| **語音** | 這七名沒有語音綁定 —— ⛔ 而 74 名社群英雄也沒有，這一批不是特例 |
| **推 / 部署** | ⛔ 明文禁止 |

---

## ⛔⛔ 一個**先前就在 main 上**的缺陷（⛔ 不是我造成的，⛔ 我沒有修）

```
go test ./internal/curation/   → FAIL
  TestStarterSetMatchesContentTree:
  roster ability "b2-aladdin.q" name "烏戈代班先派一位" lacks the task #11 xx-0N prefix
```

⭐ 根因量到了：**444 份社群英雄技能（batch1 222 ＋ batch2 222）全部帶
`"provenance": "w3x-import"`** —— 而它們**沒有任何 JASS 祖先**。
那個欄位就是那條斷言的豁免依據（測試的檔頭自己逐字寫著
「The exemption is DERIVED from the doc's own provenance — never a hardcoded id list」）。

| | |
|---|---|
| 我的七名 | ⭐ `provenance: "editor-json"`（正確 —— 由出貨編譯器寫的） |
| `git diff HEAD -- content/abilities/b2-aladdin.q.json` | **空的** ⇒ ⭐ 這個值在我動手之前就是這樣 |
| 最後動它的 commit | `34f361d1e`（main，非本工作） |

⇒ ⭐ 照第零守則⑧：**開票，⛔ 不當場修**（而「開票」要 owner 說了才做）。
修法是讓 `skillremake:provenance` 對 `community-review-*` / `b2-*` 停止蓋 `w3x-import`。

---

## ⭐ 驗收逐條

| # | 條件 | 結果 |
|---|---|---|
| 1 | 七名在 `content/champions/`，`pnpm content:build` exit 0 | ✅ `cv_1d7b5c80bfcd`，champions 153 |
| 2 | `roster-guard/check.ts` 通過且上架面 +7 | ✅ exit 0 · 144 → **151** |
| 3 | 頭圖 · 說明 · 出身 · 屬性 · 技能 · 技能圖示 | ✅ 7/7 全滿（頭圖 7/7 · 技能圖示 42/42 · 六格技能 42/42） |
| 4 | 模型綁定引用得到 asset id ＋ SHA-256 | ✅ 7/7（雜湊當場重算比對過） |
| 5 | 做不到的逐名寫下來 | ✅ 見上面兩節（HITL／畫面驗收／skills:sync／語音） |
| — | `pnpm typecheck` | ✅ exit 0 |
| — | `go build ./...` | ✅ exit 0 |
| — | `go test ./internal/curation/` | ⚠️ FAIL —— ⭐ **先前就紅**（見上一節） |

---

## 重跑指令

```bash
ggd='/Users/Takuro/Dropbox/我的 Mac (Moriya.local)/Documents/ABxVFX_EDIT'
python3 tools/ship-81/lol7.py --inventory "$ggd/全角色模型盤點.md" \
    --library "$ggd/GGD-Asset-Library" --report docs/_reports/lol7-ship-report.json --write
npx tsx tools/engine-atlas/statTiers.ts
bash scripts/genrun.sh anchors:build && bash scripts/genrun.sh statcaps:build
bash scripts/genrun.sh archetypes:build && bash scripts/genrun.sh mana:audit
pnpm content:build && npx tsx tools/roster-guard/check.ts

# 圖示（⚠️ HF_HOME 要指到**主 repo**，worktree 沒有那份模型快取；
#   ⚠️ huggingface_hub 1.24 的完整性檢查會拒絕舊快取 ⇒ 直接給 snapshot 路徑）
SNAP=/Users/Takuro/GGD/tools/icon-gen/models/hf/hub/models--dreamlike-art--dreamlike-anime-1.0/snapshots/dd5f188f512fc922786abf5efcec25d5f807dd1e
HF_HOME=/Users/Takuro/GGD/tools/icon-gen/models/hf ICON_GEN_MODEL="$SNAP" \
  /Users/Takuro/GGD/tools/icon-gen/.venv/bin/python tools/icon-gen/local/batch.py \
  --category all --only "$(cat /private/tmp/lol7-only.txt)"
```
