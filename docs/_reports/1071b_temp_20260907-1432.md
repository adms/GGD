# GH#1071 收尾 —— 剩下的 2 支**接不上**，⛔ 而缺的不是模板的一格（lane 報告，2026-09-07 14:32）

> ⚠️ 暫存報告（`{用途}_temp_{timestamp}`）。工作樹 `main`（起手乾淨，上一批已在 `e5de8ac8e`）。
> ⛔ 未 commit、⛔ 未 `git add`、⛔ 沒跑任何 `*:build`／`skills:sync`／`genrun`／`--apply`。
> 前一份：`docs/_reports/993-1071_temp_20260907-0500.md`。

## 0. 一句話

`tpl-apply-status` 的 7 支裡剩下的 2 支是 **`godie-emns.q`** 與 **`godie-e010.e`**。
⭐ **兩支都不缺模板的任何一格** —— 我用探針量到 `tpl-apply-status` **今天就配得上 `emns.q`**
（`params: castTimeSec, status`，⛔ 零個新槽）。⭐ 擋住它們的是**同一道牆**：
`skillremake:json` 的**產物邊界**。⇒ 照 preamble 第 4 條**不硬接**，改成把「差在哪一格」
從一句報告裡的話，變成 `templatize.py` **自己印得出來的量測**。

⚠️ ⭐ **順手量到一個比本票嚴重的東西**（§4）：`genguard` / hook / 隔離區**三層一起**
把 `skillremake:json` 的 **126 份產物判成「正規化器專屬 ⇒ 不擋你」**，而 `sync-io.json`
逐檔列著它們。⛔ 那正是 owner 記錄過「發生上百次」的那個形狀，而三道閘今天都是開的。

## 1. 量到的（⛔ 不是引用上一份報告）

| 量 | 數 | 怎麼量的 |
|---|---:|---|
| `effects` 恰好一個 `applyStatus` 的出貨技能 | **7** | 逐檔掃 `content/abilities/*.json` |
| 其中已接 `tpl-apply-status` | **5** | `h02r.w` · `hgam.w` · `hvwd.passive` · `o030.q` · `orkn.q`（帳本 + 文件雙向對得上） |
| 剩下 | **2** | `godie-emns.q`（產物）· `godie-e010.e`（非產物，卡變身對子） |
| 全樹**其他**還沒接的 applyStatus-only 候選 | **0** | 同一支掃描 —— ⭐ 這一族的母體就是這 7 支，⛔ 沒有漏網的 |
| `templatizeEquivalence` | **113/113 綠**（3 tests） | `npx vitest run`（§3） |
| 手刻技能 / 相異形狀 | **145** / **92** | `templatize.hand_written()` ＋ `shape_of()` 本機重算，與棘輪基準線 145 一致 |

## 2. ⭐ 逐支「差在哪一格」—— 用探針量的，⛔ 不是讀上一份報告

### 2.1 探針：把「產物排除」拿掉，看 matcher 怎麼說

```python
import templatize as T
T.product_ids = lambda: set()          # ⭐ 假裝產物邊界不存在
docs, proposals, skipped, bad, notes = T.plan(ns)
```

| 支 | 探針的裁決 | ⇒ 缺的是什麼 |
|---|---|---|
| **`godie-emns.q`**<br>44-01 死神之眼 | ⭐ **提案成立** → `tpl-apply-status`（`params: castTimeSec, status`） | ⛔ **不缺模板的任何一格**。`status` 槽是 `zApplyStatus` 本人 ⇒ 逐階 `duration:[6,12,18,24]` 與 `missChance:0.5` 它本來就收得下；`castTimeSec` 0.267 在 0–5 界內。⭐ 缺的是 **`tools/skill-remake/batch1.py` 的 `A()` 沒有 `template=` 出口** |
| **`godie-e010.e`**<br>70-03 木束縛之術 | ⛔ 仍然被擋：「另一半沒有一起轉 ⇒ 會冒出 `70-03|template`（abilityCodeParity 紅）。另一半：`godie-e00s.e`（**沒有模板發 `applyStatus + damageArea`**）」 | ⛔ **不缺模板的任何一格** —— 它就是 `tpl-apply-status` 的 **exemplar 本人**（`{root,1.5,root:true}` ＋ `castTimeSec 0.833` 逐位元等於模板的 default，params 全空）。⭐ 它被**兩層**擋著（見 §2.2） |

### 2.2 ⭐ `godie-e010.e` 是**雙重**不可達，⛔ 不是「等 batch1.py」

| 層 | 是什麼 | 量到的 |
|---:|---|---|
| ① | 變身對子 `godie-e00s.e` 是 `skillremake:json` 的**產物** | `sync-io.json` 逐字列著 `content/abilities/godie-e00s.e.json` |
| ② | ⭐ **就算 ① 解開了，`e00s.e` 仍然沒有家族可去** | 它的形狀是 **`applyStatus + damageArea`**，⭐ 而那個形狀在 145 支手刻技能裡**只有它一支（N=1）** ⇒ GH#916：N=1 ⛔ 不模板化 |

⚠️ ⭐ 這**證實並精確化**了上一份報告的更正 —— ⛔ 但我是自己量的，⛔ 不是複述：
`Counter(shape_of(v))` 的結果 `applyStatus + damageArea → ['godie-e00s.e']`。

⛔ **而「幫 e00s.e 開一格」不是正解**：它的 `damageArea` 帶 `amount:{flat:1.0}`、`radius:4.5`、
`includeOrigin:true`，把它塞進「**只上狀態**」這一族 = 讓一個叫「只上狀態」的家族發傷害節點，
⭐ 那是 CLAUDE.md 記過的「**近似不是翻譯**」（用現有參數湊一個看起來像的），
⛔ 而且是為 N=1 開的槽。⇒ ⛔ 不做。

⚠️ 順帶：`70-03` 這一組**本來就漂**（baseline 已有 `70-03|effects`）——
e010.e 是 targeted／range 12／root 1.5s 無傷害，e00s.e 是 self／radius 4.5／flat 1 傷害／逐階 0.6–2.4s。
⭐ 那是**內容債**（兩半是不同的技能），⛔ 不是 #1071 的一格。

## 3. 做了什麼（⛔ 沒有硬接，改成讓「差在哪一格」變成量測）

### 3.1 `templatize.py`：⭐ 產物**照樣跑一次 matcher**（唯讀，⛔ 不提案）

在此之前產物那一格直接 `continue`，訊息只有「改 `batch1.py` 的來源列」——
⛔ **那句話說不出「改完之後它接得上嗎」** ⇒ 一張「要不要做這個產生器機制」的票
**拿不到分母**，而分母正是排序的依據（第〇·五守則：按**擋住幾支**排序）。

| 改動 | 是什麼 |
|---|---|
| 新 `match_any(tpls, doc)` | 把原本內嵌在 `plan()` 裡的 matcher 迴圈抽出來（**純重構**，⛔ 行為不變） |
| 產物分支 | 呼叫 `match_any()`，把裁決接在訊息後面：`⭐ 今天就配得上 <ref>（params: …）` / `差一格：…` / `沒有模板發 <shape>` |

⭐ **第一次跑就把上一份報告裡的那個數字變成產出**：

```
· godie-e002.w       … —— ⭐ 今天就配得上 tpl-transform（params: castTimeSec, to）
· godie-e00s.passive … —— ⭐ 今天就配得上 tpl-transform（params: castTimeSec, to）
· godie-emns.q       … —— ⭐ 今天就配得上 tpl-apply-status（params: castTimeSec, status）
· godie-ewar.q       … —— ⭐ 今天就配得上 tpl-single-strike（params: castTimeSec, damage, damageType, status）
```

⇒ **4 支產物今天就配得上**，⭐ 連帶解鎖 3 支非產物的變身對子（`e007.q` · `e00l.w` · `e010.passive`
—— 它們的擋人訊息現在**逐字帶著另一半的裁決**）⇒ **合計 7 支**。
⭐ 而 `e010.e` 的訊息現在讀得出第二層：「另一半：`godie-e00s.e`（**沒有模板發 applyStatus + damageArea**）」。

### 3.2 ⭐ 重構的驗證是**逐位元 diff**，⛔ 不是一條新測試

`diff <(新輸出 去掉「—— 裁決」那一段) 舊輸出` ⇒ ⭐ **每一個非產物桶逐字相同**、提案數仍是 **0**。
⚠️ 照第零守則⑦（工具腳本＝體驗層，一條薄守衛、⛔ 不做突變）＋「⛔ 不要過度配比」：
⛔ 我**沒有**為一段訊息新開一支 python 測試框架（這個工具今天沒有任何 py 測試，
新開一個框架去守一句文案正是那個 17.5× 的形狀）。⭐ 真正的裁判仍然是
`templatizeEquivalence.test.ts`，而它是綠的。

## 4. ⛔⛔ 順手量到的：**產物邊界的三道閘今天全是開的**（⛔ 沒動，柵欄外）

### 4.1 量到的

```
$ bash scripts/genguard.sh content/abilities/godie-emns.q.json
⚠️ … 會被**正規化器** skillremake:json 就地改欄位,⛔ 但它不是那支的產物 ⇒ 這一支不擋你。
```

⛔ 而 `sync-io.json` 的 `skillremake:json.writes` **逐字列著** `content/abilities/godie-emns.q.json`。

| 層 | 今天的裁決 | 應該是什麼 |
|---|---|---|
| `scripts/genguard.sh` | `NORMALIZER`（不擋） | `AUTHOR`（擋） |
| `scripts/preserve-before-overwrite.py::_generator_owner()` | `('skillremake:json', **True**, [])` ＝「只有正規化器認領」⇒ 警告放行 | `('skillremake:json', False, ['skillremake:json'])` |
| `scripts/product-quarantine.sh` | 那 91 份是 **644 可寫**（status 說「正規化器專屬 507 份不上鎖」） | 444 |

### 4.2 根因（⛔ 不是誰粗心）

`tools/parallel-gates/normalizers.json` 的 `skillremake:json` 那一列帶著
**`"onlyOutsideOwnWrites": true`**，而它的 `reason` 自己逐字寫著
「⭐ 它**同時**是 91 份技能的作者⋯**在它自己的 writes 裡 ⇒ 作者；在外面 ⇒ 正規化器**」。

⭐ **而那個旗標今天只有一個地方讀它**：

```
$ grep -rn "onlyOutsideOwnWrites" scripts/ packages/shared/src/ tools/
packages/shared/src/content/import/editorSource.ts:58   ← ⭐ 唯一的消費端
packages/shared/src/content/import/editorSource.ts:102
tools/parallel-gates/normalizers.json:59                ← 宣告
```

⇒ ⭐ **三個執行點（genguard.sh · hook · 隔離區）一個都沒實作它。**
`genguard.sh:55` 建 Map 的時候只留 `n.only`，`preserve-before-overwrite.py:_normalizer_steps()`
同樣只取 `only` ⇒ `normalizes('skillremake:json')` 對**每一條路徑**都回 true。

⇒ ⭐ 這是 CLAUDE.md 記過的 **「消費端存在，但它消費不到」（失敗形態⑧）**：
旗標有、宣告有、理由寫得極清楚，⛔ 而**沒有一行執行點的程式讀它**。
⚠️ 而它的症狀是**沉默的**：genguard 說「不擋你」與「這個檔可以改」長得一模一樣。

### 4.3 影響（量到的，⛔ 不是估的）

`skillremake:json.writes` = **127 個路徑**（`content/abilities` **90** ＋ `content/champions` **16**
＋ 21 個其他集合的 `_index`／契約文件）⇒ ⭐ **這 126 份真產物今天可以被 Write／Edit／
shell 重導／檔案 API 直接改，而三道閘一句話都不會說。**
⛔ 那正是 owner 2026-08-24 逐字說「這個問題發生上百次了」而建這三道閘要擋的東西。

⚠️ ⭐ **收工時複驗過（14:4x，⛔ 不是憑起手那一次）**：另一條 lane 正在改
`scripts/product-quarantine.sh` · `scripts/preserve.sh` · `tools/parallel-gates/normalizers.json`
（GH#1097，加一列 `board:build` 的 `only` 豁免）—— ⭐ **與這一件無關**：
`grep -rn onlyOutsideOwnWrites` 在他們改完之後**仍然只有 `editorSource.ts` 一個消費端**，
而 `genguard.sh content/abilities/godie-emns.q.json` 在**現在的樹上**仍然回「不擋你」。
⇒ ⭐ 主 session 可以把這一件**路由給那條 lane**（他們正好在那三個檔裡），⛔ 但它今天沒被修。

⇒ ⭐ **建議主 session 開一張票**（`scripts/**` 在本 lane 柵欄外，⛔ 我沒動）：
把 `onlyOutsideOwnWrites` 接進 `genguard.sh` / `preserve-before-overwrite.py` /
`product-quarantine.sh`，⭐ 並補一條**反方向**的閘：
「`normalizers.json` 的每一個欄位，都要有至少一個執行點讀它」——
⛔ 否則下一個旗標會用同一個方式再死一次（⑫：只驗名詞不驗關係的反方向）。

## 5. 建議開的兩張票（⛔ 我沒開 —— owner 說「開票」才開票）

| # | 標題（建議） | 為什麼 / 擋住幾支 |
|---|---|---|
| **A** | `[重要][fix] normalizers.json 的 onlyOutsideOwnWrites 只有 editorSource 讀 —— genguard／hook／隔離區三層對 126 份真產物全開` | ⭐ **126 份**。失敗形態⑧；三道閘同時失明，且症狀沉默 |
| **B** | `[優先][feature] batch1.py 的 A() 加一個 template= 出口 —— 產物也接得上模板` | ⭐ **7 支**（4 產物直接 + 3 變身對子連帶）。⭐ 分母現在由 `templatize.py` **自己印出來**，⛔ 不再住在一份會過期的報告裡 |

⚠️ **票 B 的兩件已量到的約束**（沿用上一份報告，⛔ 我沒有推翻它）：
① `A()` 的 swap 要在**所有閘跑完之後**（`build()` 末端的 A-5／tag_gate／`_fold_onhit`／`tierize`
讀 `doc["effects"]`，而 `carry_mechanisms(prev)` 下一輪讀到空 effects 會**不冪等**）；
② 等價的證據 ⛔ **不可以寫進 `templatize-ledger.json`**（那會是同一份資料的**第二個寫入端**，
CLAUDE.md 🔐 那一節逐字禁止）⇒ 產生器要寫**自己的**帳本。

## 6. 測試（⭐ vitest **1 次**、tsc **0 次**）

| 次 | 跑什麼 | 結果 |
|---:|---|---|
| 1 | `templatizeEquivalence` · `templateFamiliesAreAdopted` · `templateDefaultsHaveOrigin` · `templateStatusIsHonest` · `templateBoundDocsNoBakedTier` · `abilityCodeParity` · `handWrittenAbilitiesRatchet` | ⭐ **7 檔 20 條全綠**，EXIT=0，2.61s |

⭐ **tsc 跑 0 次是刻意的**：這一輪**一行 TypeScript 都沒動**（唯一的改動是
`tools/skill-remake/templatize.py`）⇒ 跑 tsc 就是第零守則⏱ 明文禁止的第 2 種浪費
（「開工前跑一次 tsc 確認環境是好的」的變形）。

⭐ **突變 ⛔ 沒做，而這是規則要的**：第零守則③「工具腳本 = 一條薄守衛，⛔ 不做突變」。
⭐ 本輪真正承重的那條線（`templatizeEquivalence`）的突變**上一輪已經做過**
（`iframeNode()` 的 `blocksControl` 翻面 ⇒ 逐支指名 5 支），而我這一輪**沒有動它守的任何東西**。

## 7. 動到的檔案（逐檔列名）

**工具（1）**
- `tools/skill-remake/templatize.py` —— 新 `match_any()`（純重構抽出 matcher 迴圈）＋ 產物分支加裁決註記

**報告（1）**
- `docs/_reports/1071b_temp_20260907-1432.md`（本檔）

⛔ **一份 `content/` 都沒動**（因為不硬接）：⛔ `content/abilities/**` · ⛔ `content/ability-templates/**` ·
⛔ `tools/skill-remake/templatize-ledger.json` · ⛔ `tools/skill-remake/batch1.py`／`heroes/*.py` ·
⛔ `packages/shared/src/**` · ⛔ `scripts/**` · ⛔ `tools/parallel-gates/**` ·
⛔ 任何 `*:build`／`skills:sync`／`genrun`／`--apply` · ⛔ git（沒 add、沒 commit）。

## 8. 主 session 要知道的三件事

1. ⭐ **#1071 的 AC 今天已經滿了**：AC①「5 支接上、等價閘綠」✅（113/113）；
   AC②「棘輪 231 → ≤226」✅（`HAND_WRITTEN_BASELINE` 已經是 **145**）。
   ⇒ ⭐ **剩下的 2 支不是這張票的 AC**，它們是票 B 的內容。
2. ⛔ **不需要跑 `skills:sync`**：本輪沒有動任何 `content/`、也沒有動任何產生器的**來源**
   （`templatize.py` 是正規化器且我沒 `--apply`）⇒ ⛔ 零個產物會過期。
3. ⚠️ §4 那件事**比本票嚴重**，而且它會**沉默地**讓下一條 lane 直接改到 90 份技能產物。
