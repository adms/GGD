# GH#1060 —— `tpl-pull-throw.apexHeight` 標 `wc3h` 卻填 GGD 值 1.2（lane 報告，2026-09-06 23:17）

## 做了什麼

| 檔 | 改動 |
|---|---|
| `content/ability-templates/tpl-pull-throw.json` | `apexHeight.default` **1.2 → 300**（wc3h）、`max` **8 → 2000**、`origin` 改成 `j:51828` 出處（保留 `wc3h`，走票文的路①） |
| `packages/shared/src/content/templates/pullThrowApex.test.ts`（新） | ① 展開後弧頂 = default × `GGD_APEX_PER_WC3`，且與出貨 `godie-hapm.w.json` 的 `leap.apexHeight` **同量級**（ratio ∈ (0.5, 2)，⛔ 不釘死相等）② 全部模板：`wc3u`／`wc3h` 槽的 default／min／max 必須是**整數** |

## 量到什麼

- **JASS 出處**：`war3map.j:51828` `SetUnitFlyHeightBJ( target, (-3×(Index−11)²)+300, 0 )`，原作在 j:51827 自己註解「Index=11時高度=**300**」⇒ 峰高 300 wc3 ⇒ `toApex(300) = 1.2` ＝ 出貨 `godie-hapm.w.json` 的 1.2（那一格已是 GGD 單位，`schema/effects/leap.ts` 檔頭逐字）。`docs/design/grab-family.md:301` 也早記著「A0U1 300」。
- **動手前**：模板 default 1.2 → `num()` 對 wc3h 槽再 `toApex` ⇒ **0.005** GGD（250× 之差）。
- **35 份模板同族掃描**（`unit ∈ {wc3u, wc3h}` 的 37 個槽）：**只有這一格**是 GGD 值漏進 wc3 槽；其餘 36 個 default 全是整數（最小 45＝`tpl-traveling-wave.stepSize`，0 的兩格是 `tpl-charge-push` 的兩個 apex，兩把尺上都是 0）。⇒ 一起修的：**0 個**。
- `max` 2000 ＝ 全圖最高拋物線 1000（76-04, j:36757）的 2×，仍擋 `SetUnitFlyHeightBJ` 第三參數 5000（速率）的誤貼 —— 與 `tpl-leap-strike` 同一個推理。

## 測試

- 批次 1（13 個 suite，含 paramsSchema／templateDefaultsHaveOrigin／chargePush 的 apex-ruler／schemaImportCycle）：**76/76 綠**，EXIT=0。
- `pnpm typecheck` EXIT=0。
- 突變：體驗層薄守衛，⛔ 未做（票文明說）。守衛②對 1.2 的判定可直接推得（1.2 非整數 ⇒ 紅並指名 `tpl-pull-throw.apexHeight.default`）。

## ⛔ 沒做到／需要主 session 動的柵欄外檔案

1. **產物過期**（⛔ lane 禁跑 `*:build`）：`content/bundle.json` 內嵌 `tpl-pull-throw` 的 `apexHeight.default:1.2`（`grep` 到 1 處）⇒ `shippedBundleIsCurrent.test.ts` 會紅到跑 `pnpm skills:sync`；`docs/editor-contract/*`（`caps:export`／type-catalog）同理。
2. `packages/shared/src/content/schema/effects/leap.ts:10-12` 檔頭寫「`apexHeight`/`landRadius` … converted … by **`toLen`**」—— `apexHeight` 走的是 `toApex`（1/250），⛔ 不是 `toLen`（第三守則：註解說謊）。一行註解，柵欄外，沒動。

## 順手發現但沒動

- 無（本票範圍內的同族掃描 0 命中）。
