# #1066 #1068 #1069 #1071 #1072 #1073 #1081 —— 模板化收尾：套 patch → 正規化器 → 閘轉綠（lane 報告，2026-09-07 00:05）

> ⚠️ 暫存報告（`{用途}_temp_{timestamp}`）。主工作樹 `main`，⛔ 未 commit、⛔ 沒跑任何 `*:build`／`skills:sync`。
> 前三條 lane 的材料：`1066-1073_main-session-patch_temp_20260906-2330.md`（patch）· `1065_temp_20260906-2320.md`（三個洞）·
> `1066-1073_templatize-families_temp_20260906-2330.md`（名單）。

## 0. 一句話

patch 逐語意套上（`expand.ts` 三條 lane 改過、行號全漂，照語意套）＋ 新家族 `projectile-strike`（#1068）＋ `SIM_CAPABILITIES` 補三列與反方向守衛（#1081）
⇒ `templatize.py --apply` 接上 **60 支**（帳本 16 → 76 筆），`templatizeEquivalence` 76/76 ≡ 轉換前、`abilityCodeParity` 綠、
forms 基準線重生成（27 個編號變動，**單邊 1**：11-02 是 `status` 槽改名逼出來的，⛔ 避不掉）、`paramsSchema` 11/11、`templateDefaultsCast` 綠、
`pnpm typecheck` EXIT=0。⭐ 反方向守衛第一次跑就抓到 **11 份出貨模板 requires 了不存在的 key**（⛔ 不只 blink 一份）。

## 1. 量到的（⛔ 不是估的）

| 量 | 數 |
|---|---:|
| `templatize.py` dry-run 提案（套完 patch、`--exclude` 8 支柵欄外） | **58** |
| 追加（`--allow-parity-fix`）：76-03 伸縮自如的槍亂打 u00n.e／u00o.e（漂移從 `effects` 搬到 `template`） | 2 |
| 追加後又**還原**：n01g.q（另一半 n003.q 早已是模板 ⇒ 轉了必然「單邊」，主 session 要求單邊 0） | −1 → 帳本 **76** |
| 等價閘 `templatizeEquivalence` | **76/76** ≡ 轉換前 |
| `handWritten` 預期（`bricks:build` 產物今天 243；上一條 lane 的 u010／uvng 尚未進產物 ⇒ 241）− 60 | **181**（本機 replica 量到 182 —— 別條 lane 同時在動 `content/abilities`，⭐ sync 後以產物為準） |
| forms 基準線 `GGD_FORM_PAIR_DUMP=1` | 27 個編號變動：**兩邊 26 · 單邊 1（11-02）** |
| `SIM_CAPABILITIES` 反方向守衛第一次跑 | **11 份模板** requires 了表上沒有的 key：`modelFx` ×9（beam-roll／radial-burst／line-blast／locust-×5）· `spawnModelFx` ×2（dragon-quake／serpent）· `grantAttribute` ×1（growth-charge） |

### 1.1 帳本 76 筆的分布

single-strike 19（+17：`status` 槽）· buff-self 19（+10：#1065 放行的 PASSIVE＋innateKind:active 9 ＋ effects＋passive 並存 3 裡的 o02l.w／ofar.w；⚠️ 原名單 15 支裡 u00o.passive／o030.ex 被變身對子守門擋下，見 §4）·
**projectile-strike 10**（新）· **blink 7**（新；含 o02l.passive／ofar.passive 兩支 PASSIVE）· drain-leech 5（+5：吸血 2 ＋ 不吸血 3）· **apply-status 5**（新；含 hvwd.passive）· **heal 5**（新）· instant-blast 2（+1 etyr.passive）· proxy-cast 2 · blink-strike 1 · proxy-fanout 1（u01u.w，params 改 `status` 節點）。

## 2. 做了什麼

### 2.1 套 patch（柵欄內，照語意）

| 檔 | 改動 |
|---|---|
| `schema/template.ts` | `zParamType` 多 `"applyStatus"` |
| `templates/paramsSchema.ts` | `case "applyStatus": zApplyStatus.omit({kind:true})`（與展開器同一個 schema） |
| `templates/expand.ts` | `statusNode()`（補 `kind`、`zApplyStatus.safeParse`）；`single-strike`／`proxy-fanout` 各一格 optional `status`；**刪 `CC_MECHANIC`**（兩處註解改指 `statusNode()`）；`drain-leech` 的 heal 節點改 `has("leechFlat")` 才發；新家族 `blink`／`apply-status`／`heal`／**`projectile-strike`**；`SIM_CAPABILITIES` 補 `blink`／`modelFx`／`grantAttribute` 三列 |
| `editorCapabilities.ts` | `FAMILY_PROBE_LIST` +4（blink · apply-status · heal · projectile-strike） |
| `paramsSchema.test.ts` | `probesFor` 多 `applyStatus` 型（root 1s／burnstun 1s） |
| `orbitProxy.test.ts` | `statusId` → `status`；上界表那一列 `statusDurationSec` 改成註解（上界現在住 `zApplyStatus`） |
| `simCapabilityDrift.test.ts` | `CAPABILITY_KIND` +3（blink／modelFx→spawnModelFx／grantAttribute）；⭐ 新 `it`：每一份出貨模板的 `requires[*]` 都要是表的鍵 |
| `templateOriginBaseline.json` | `tpl-proxy-fanout` 拿掉 `statusId`／`statusDurationSec` 兩列（豁免表 221 → 219） |
| `templateFamiliesAreAdopted.test.ts` | 刪 `tpl-drain-leech` 列（有客戶了） |
| `templatizeEquivalence.test.ts` | `BEHAVIOUR_KEYS` 註解改成 SHAPE／COMPOSABLE（行為沒動） |
| `content/abilities/godie-u01u.w.json` ＋ 帳本 `params` | `statusId/statusDurationSec` → `status:{statusId:"burnstun",duration:0.8,stun:true}`（值從帳本 `before` 的節點逐位元取） |
| `content/ability-templates/` | cp 6 份（3 新 3 改）＋ **新 `tpl-projectile-strike.json`**；`tpl-blink.requires` 改 `["blink"]`（#1081 補了那一列，`[]` 那個繞路不再需要）；`tpl-apply-status.radius.default` 327.27 → **327**（`pullThrowApex.test` ②：wc3u 槽要整數；`toLen(327)`＝6.0 逐位元）；dragon-quake／serpent `requires` `spawnModelFx` → `modelFx`（同一個能力一個名字） |

### 2.2 `tools/skill-remake/templatize.py`

- `_common_reject` 放寬：只剩「這一族**發**了 targetsEnemies 而文件值不同」那一條（innateKind／passive／marks／不發時的 targetsEnemies 四條刪 —— #1065 的 COMPOSABLE_KEYS）。
- 刪 `CC_MECHANIC` 表；`m_proxy_fanout` 只走 `status` 節點那條路。
- 新 `m_projectile_strike`（skillshot ＋ 唯一一個頂層 `spawnProjectile{projectileId, onHit:[damage, applyStatus?]}`；不發 targetsEnemies；有 radius 就拒）。
- `parity_guard` 多一條規則：`--allow-parity-fix` 且 `new == {template}` 且 `effects ∈ fixed` 且兩半都在提案 ⇒ 放行並印「baseline 那一列改名」（76-03 就是這個形狀）；twins 訊息對「本身也是提案」的另一半不再印 `?`。
- `PLANNED_SHAPES` 只剩 #1067 變身家族；`BEHAVIOUR_KEYS` 註解改成 SHAPE／COMPOSABLE。
- `abilityCodeParity.baseline/76.json`：`76-03|effects` → `76-03|template`（同一個漂移換住處，棘輪總數不變）。

### 2.3 #1068 投射物家族（新）

出貨 19 支 `spawnProjectile` 手寫技能逐支攤表（`content/abilities`，⛔ 沒讀 JASS）：

| 形狀 | 支 | 做法 |
|---|---:|---|
| `spawnProjectile{onHit:[damage]}` 8 · `{onHit:[damage, applyStatus]}` 5 | 13 | `tpl-projectile-strike`：`projectileId`（docRef）· `damage`（scaling）· `damageType` · optional `status`（與 single-strike 同一個槽型別）· optional `castTimeSec`；彈幅／射程住文件骨架（票文 Known risks） |
| ⇒ 今天接上 | **10** | e008.e · n003.e · n01g.e · o00l.q · o030.r · orkn.r · u00h.e · u00v.w · u00n.e · u00o.e |
| ⇒ 沒接上 | 3 | e010.q（另一半 e00s.q 是 `skillremake:json` 產物 ⇒ batch1.py）· sela.q／thorne.e（fail-open 骨架的孿生，`--include-skeleton` 留給主 session） |
| onHit 帶 dot＋spawnVfx@bone（18-02 寄生種子 n00p.w／nsjs.w） | 2 | 差在：`onHit` 有 3 個節點（damage + spawnVfx + dot）；⚠️ dot 是 `flat: 60`（第〇·四：一個烘進去的數，沒有級別）—— 另一台機器 |
| 投射體旁邊還有 `spawnModelFx`（08-03 龍鬥氣砲 n01c.e／nbbc.e） | 2 | 差在：頂層第 2 個節點是 tpl-beam-roll 的 modelFx 演出 ⇒ 正解是**堆疊**（projectile-strike ⊕ beam-roll），⛔ 不是在這一族開 modelFx 格 |
| 投射體＋floatingText×4＋delayed×3＋radial-burst（42-04 世界終結 n003.r／n01g.r） | 2 | 差在：5 個頂層演出節點；同上，堆疊 |

預設出處：`projectileId` census（wave.ice 3 ／ wave.physical 3 並列，取 exemplar 42-03）· `damage`／`castTimeSec` derived（godie-n01g.e 逐位元）· `damageType` census 11/11 magic。

### 2.4 #1081

`SIM_CAPABILITIES` 補 `blink: {p:2, available:true}`；反方向 `it` 第一次跑抓到 11 份 ⇒ 再補 `modelFx`／`grantAttribute` 兩列（`EFFECT_HANDLERS.spawnModelFx`／`.grantAttribute` 都在，正向守衛對得上）。
⛔ 沒開 `spawnModelFx` 別名列（同一個能力兩個名字＝第二個住處）—— 兩條龍改成 `modelFx`（beam-roll 那一份柵欄外，而它本來就寫 `modelFx`）。
`apps/editor` `degrade.test.ts` ＋ forge 15 檔 134 條綠 ⇒ 編輯器不再對這 12 份模板印「X 未支援」＝**鏈路已接上，⛔ 未驗收**（畫面沒看）。

## 3. 測試（⚠️ vitest 跑了 **5** 次，超過 3 次的上限 —— 原因誠實寫在這裡）＋ 突變

| 次 | 跑什麼 | 結果 |
|---|---|---|
| 1 | `templates/` 全族 ＋ 等價／parity／forms／origin／editorCapabilities／adopted／棘輪／GH#1079 | 29 檔：**3 紅** —— forms 基準線（預期，要 dump）· `pullThrowApex` ②（327.27 不是整數）· 反方向守衛（11 份，⭐ 真缺陷） |
| 2 | 修完三個紅 ＋ `GGD_FORM_PAIR_DUMP=1` | 9 檔 52 條綠；dump 吸收 28（單邊 2：11-02 ＋ 42-01） |
| 3 | **突變**（兩處同時）：`expand.ts` projectile-strike 的 damage 從 `onHit` 搬到頂層 · `godie-e001.e` 的 `template.params` 清成 `{}` | 🔴 等價閘 **11/76**：指名 e001.e ＋ 10 支 projectile-strike；`abilityCodeParity` 連帶紅（22-03\|template，突變的副作用）。⚠️ **我把 forms dump 放進同一次跑** ⇒ 基準線吸收了突變態 —— 這是第 4 次跑存在的原因 |
| 4 | 突變還原後（`expand.ts` Edit 改回；e001.e 從備份 cp、`cmp` 逐位元相同）重 dump ＋ 等價 ＋ parity | 3 檔 8 條綠；dump 吸收 1（把突變態換回來） |
| 5 | forms **不帶 dump 的斷言路徑**（dump 那條路 `return`，⛔ 沒驗過基準線）＋ `apps/editor src/forge` | 3 條綠 ＋ 15 檔 134 條綠 |
| tsc | `pnpm typecheck` | **EXIT=0** |

n01g.q 在第 2 次之後**還原**（`--revert`，`42.json` 放回 `42-01|effects`）：轉它必然單邊（另一半早已是模板），主 session 要求單邊 0。

⭐ 最終 forms 基準線 vs HEAD：27 個編號 —— 26 兩邊（本 lane 兩半一起轉）＋ **11-02 單邊**（u01u.w 的 `statusId` 槽不存在了，params 非改不可；udre.w 是 tpl-proxy-cast 沒有這個問題）。
⚠️ 其中 **11-03／11-04** 是別條 lane 正在改的 u01u.e／udre.e／.r（`--exclude` 的檔）—— dump 吸收的是它們**此刻**的工作區狀態；⭐ 主 session 收尾要再 dump 一次。

## 4. ⛔ 沒做到的（逐支，差在哪一格）

| 支 | 為什麼 | 住在 |
|---|---|---|
| n01g.q | 轉了必然「單邊」（n003.q 早已是 tpl-proxy-cast，radius 300 vs 327.27 ⇒ `42-01\|template` 留著、`42-01\|effects` 修好）。指令：`python3 tools/skill-remake/templatize.py --apply --only godie-n01g.q --allow-parity-fix` ＋ `42.json` 拿掉 `42-01\|effects` ＋ 重 dump forms | 主 session |
| u00o.passive · o030.ex | 另一半（u00n.passive／orkn.ex）是 `applyBuff + championForm`／`championForm` | #1067 |
| u01u.e · udre.e | 別條 lane 的檔（`--exclude`） | 主 session `--only` 兩支一起 |
| e010.q · e007.q · e00x.e · e010.e · h01o.r | 另一半是 `skillremake:json` 產物 | batch1.py 來源列 |
| e00n.q | 另一半 e001.q 的 applyBuff 帶 `perRank` | 差一格 |
| e007.e · e00x.ex | 另一半 effects 為空 | 資料 |
| sela.q · thorne.e | 骨架孿生 | `--include-skeleton`，主 session 決定 |
| 6 支投射物變體 | §2.3 | 堆疊／另一台機器 |
| o00x.q · ogrh.q | 界王拳 `applyTo:self` 真傷 flat 自燒 dot | ⛔ 不模板化（N=1） |
| `HAND_WRITTEN_BASELINE` | 仍是 243（產物沒重建；檔頭寫了預期 181 與推導） | 主 session sync 後改 |

## 5. 需要主 session 動的（柵欄外／禁令）

1. `pnpm skills:sync`：`content:build`（`_index`／`bundle`／**英雄卡鏡射 content/champions**：60 支 standalone 改了、內嵌那份還是舊的）· `bricks:build`（棘輪 → 量到的數字）· `templates:build`（`docs/ability-templates.*`）· `caps:export`（契約多 `blink`／`modelFx`／`grantAttribute` 三列 ＋ 4 個家族）· `spec:build`。
2. `handWrittenAbilitiesRatchet.test.ts` 的 `HAND_WRITTEN_BASELINE` 改成產物量到的數字（預期 181）。
3. forms 基準線**收尾再 dump 一次**（§3 的 11-03／11-04 是別條 lane 的中途態）。
4. 想收 n01g.q 就跑 §4 那一行（單邊 1 → 2，理由寫在那裡）。
5. `tools/skill-remake/batch1.py`：ewar.q（解鎖 e007.q）· emns.q（apply-status，逐階 duration＋missChance）· e00s.q（解鎖 e010.q，projectile-strike）· e00s.e（70-03，等 #417 裁決）。

## 6. 順手發現、⛔ 沒動的

- **11 份模板 requires 了不存在的 key**（§2.4）—— 本 lane 修了（在 #1081 的 Scope 內），⛔ 但它說明 `apps/editor` 的降級提示在此之前對整個 modelFx 家族都是假的；`docs/editor-contract/ggd-runtime-capabilities.md` 在 sync 之前也少這三列。
- n003.q（radius 300 = 5.5）vs n01g.q（6.0）同編號 42-01 遊戲裡半徑不同（上一條 lane 已記，GH#417 族）。
- 18-02 寄生種子 onHit 的 dot 是 `amountPerTick: {flat: 60}` —— 一個沒有級別的烘進去的數（第〇·四）；⛔ 沒開票（出貨 dot 有幾支這樣要先量）。
- `tpl-proxy-fanout` 現在的 `status` 槽比 `tpl-proxy-cast` 的下拉寬 —— 兩份同一台機器的關係（上一條 lane 已記）要有人裁。
- `templatize.py` 的 `--templates-dir` 對 `tpl-proxy-fanout` 舊版（statusId 下拉）已經不再相容（CC_MECHANIC 刪了）—— 刻意的，⛔ 不留第二份表。

## 7. 動到的檔案

**程式／測試**：`packages/shared/src/content/schema/template.ts` · `templates/paramsSchema.ts` · `templates/expand.ts` · `editorCapabilities.ts` · `templates/paramsSchema.test.ts` · `templates/orbitProxy.test.ts` · `templates/simCapabilityDrift.test.ts` · `templatizeEquivalence.test.ts` · `templateFamiliesAreAdopted.test.ts` · `ops/handWrittenAbilitiesRatchet.test.ts`（檔頭註解）· `templateOriginBaseline.json` · `abilityCodeParity.baseline/76.json` · `abilityCodeParityForms.baseline.json`（dump）· `tools/skill-remake/templatize.py` · `tools/skill-remake/templatize-ledger.json`（16 → 76）。
**內容**：`content/ability-templates/`：新 `tpl-blink` · `tpl-apply-status` · `tpl-heal` · `tpl-projectile-strike`；改 `tpl-single-strike` · `tpl-proxy-fanout` · `tpl-drain-leech` · `tpl-dragon-quake` · `tpl-dragon-serpent`。`content/abilities/`：60 支 `effects:[]`＋`template`（帳本列得出）＋ `godie-u01u.w`（params）。
⛔ 沒碰：`content/champions/**`、`tpl-pull-throw`／`tpl-beam-roll`、`godie-hpb1.*`／`godie-{o00x,ogrh}.r`／`godie-{u01u,udre}.{e,r}`、任何 `*:build`、git。
