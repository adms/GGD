# Lane 報告 —— #1018 自傷止血 · #1019 「打在誰身上」的閘 · #1017 一擊必殺夾限翻開

> 2026-09-06，owner 睡覺中的常設指令（逐字）：「盡量把所有票解決完我要去睡了 有問題做成開關 不要卡在我這裡 我醒了你可以回我開關列表」。
> ⛔ 這份報告裡凡是我挑的（名字、預設值、級別）都標「我挑的」；owner 的話一律 `> 引言`＋日期。

## 🔀 開關列表（owner 醒來先看這一節）

| 開關 | 住處（三個） | 出貨值 | 我挑的？ | 一鍵回頭 |
|---|---|---|---|---|
| **一擊必殺夾限總開關** `one-shot-clamp.enabled` | `content/config/one-shot-clamp.json` · `SHIPPED_ONE_SHOT_CLAMP`（`oneShotClamp.ts`）· admin「一擊必殺夾限」☠️ 頁 | **`false → true`** | ⛔ 不是 —— owner 2026-09-06 00:58 逐字「先做 A 但我想深入了解 B」（A ＝ 翻開） | 後台那一格翻回 false（要重啟 shard） |
| **self 施放傷害的閘** `damage-rules.abilitySelfDamageGuard` | `content/config/damage-rules.json` · `SHIPPED_DAMAGE_RULES`（`damageRules.ts`）· admin「傷害規則」頁 | **`true`（新格）** | ⭐ **我挑的**（名字＋預設；票文建議名 `abilitySelfDamageGuard`） | 翻成 false ⇒ 閘只印警告不擋；⛔ 不改任何一場比賽 |

⚠️ `maxFractionOfMaxHp`（1.0）與 `alsoClampMinions`（false）**沒有動**（#1017 Non-goals）。

## #1018（緊急）—— ✅ 完成 · commit `d66ab6f7a`

| 支 | 改法 | 我挑的部分 |
|---|---|---|
| `godie-ucrl.q` / `godie-u034.q`（06-01 山形修煉-放） | `castType: self → targeted`；`applyBuff` 明寫 `applyTo:"self"`（護甲留在自己身上） | `rangeTier: 中`（卡面「念彈…一直線」）|
| `godie-ucrl.w` / `godie-u034.w`（06-02 山形修煉-變） | 同上 | `rangeTier: 小`（卡面「刀刃攻擊目標」）|
| `godie-h02u.r`（92-04 馬勒戈壁） | 卡面「對周圍敵人」⇒ `damage → damageArea`（樣板 `godie-h02v.r`）；castderive 重算吟唱 1.633→1.233 | `radiusTier: 中` |

- 鏡射英雄卡 `content/champions/godie-{ucrl,u034,h02u}.json` 同步（standalone → embedded）；`abilityMirror.test.ts` 4/4 綠。
- 正規化器照票文順序：`castderive:build` → `tiers:apply`（寫回 range 6.0/4.5）→ `content:build`。
- 證據（同一支掃描）：**修前 content 樹 ⇒ 指名 5 支；修後 ⇒ 0**；天破壤碎 `godie-e008.ex` 一個位元組沒動。
- `docs/_castability-128.md` 小傑那一列仍七格 ✅ —— 現在是打在敵人身上的 ✅；表頭多了限定詞（見 #1019）。
- ⚠️ #1020 會重做小傑 Q/W 形狀（卡面是被動）；這裡只是止血，兩格 rangeTier 到時一起被取代。

## #1019（重要）—— ✅ 完成 · commit `62c2ec2a5`

- 新閘 `packages/shared/src/content/selfCastDamageTargeting.test.ts`（80 行）：載入出貨內容＋`registerAll`，
  ① `castType:self` ＋ `damage`（頂層 ＋ `delayed{shape:"single"}` 子樹）＋ 沒明寫 `applyTo:self` ⇒ 紅並逐支指名；
  ② 明寫 `applyTo:self` 的，卡面要講得出「自身／自己／代價／犧牲」。⛔ 沒有豁免名單（天破壤碎兩條自動放行）。
- ⚠️ 第一版連 `delayed{shape:"circle"}` 也算 ⇒ 誤抓 3 支週期領域（h02r.q / hgam.q / o02p.r —— 它們的 delayed 自己重解圈內敵人）⇒ 收成只走 `single`。
- **突變（一條）**：`node.applyTo !== "self"` 反過來 ⇒ ① 紅並指名 `godie-e008.ex effects[0]`、五支修好的全放行 ⇒ 它問的正是那個問題。改回 ⇒ 3/3 綠。
- `castabilitySweep.test.ts` CANNOT SAY 段補「不問打在誰身上」；產物 `docs/_castability-128.md` 表頭限定詞由 sweep 重生成（⛔ 沒手改）。
- ⭐ 開關住處**與票文不同**：票文寫 `combat-env.json`，⛔ 但那份是純倍率表（TS `zCombatEnvMultipliers` 從 `COMBAT_ENV_KEYS` 產生、Go `combatenv` 與 admin `combatEnv.ts` 都只讀寫 `multipliers`）—— 一顆布林存進去會被後台存檔丟掉 ⇒ 改住 `config.damage-rules@1`（傷害規則，三個住處齊全、admin 頁已存在）。
- 產物 `docs/editor-contract/ggd-editor-coverage.json` 新欄位入冊（已在 HEAD）。

## #1017（優先）—— ✅ 完成 · commit：本報告所在的那一顆（見 `git log -1 -- content/config/one-shot-clamp.json`）

- 三個住處 `enabled: false → true`：`one-shot-clamp.json` · `SHIPPED_ONE_SHOT_CLAMP` · admin `ONE_SHOT_CLAMP_SPEC`（intro／enabled note 的「出貨關著」改掉，⚠️ 這一段隨 #1019 的 commit 落地，同一個檔）。
- `note` 那句「owner 還沒裁決要不要夾」**已刪**，換成 owner 2026-09-06 逐字「先做 A 但我想深入了解 B」＋「有問題做成開關 不要卡在我這裡」（驗收 ③）。
- `oneShotClamp.test.ts` 第 1 條從「出貨關著 ⇒ 打滿」改成「出貨開著 ⇒ 被夾在整條血以內」（票文 Test 一節：前提消失，⛔ 不是刪掉）；3/3 綠。
- ⛔ `damage.ts:1107` 的突變沒有另外跑 —— 那條線由既有第 2 條（開著＋半條 ⇒ 被夾）守著，而它今天綠。

### 📏 量測：翻開之後實際會被夾到的比例（驗收 ④）

- ⛔ 本機語料 `data/match-stats/*.jsonl`（215 檔 · 230 round · 28,554 casts，2026-07-30 格式）**量不到**：cast 紀錄沒有 `victimMaxHp`，只有每次施放的 `damageToHeroes` 總和。
- ⭐ 改用**真的 headless 比賽**量（`MatchController` ＋ 12 bots ＋ 出貨內容，夾限關著，收每一發打在英雄身上的 `damage` 事件，算 `amount / 受害者 maxHp`）：

| 樣本 | 數字 |
|---|---|
| 比賽 / 回合 / 打在英雄身上的傷害事件 | 3 場（seed 4242 / 7 / 99）/ 30 回合 / **5,224 發** |
| 超過 1.0×maxHp（翻開後會被夾的） | **93 發 = 1.78%** |
| 被夾的那些平均削掉 | **40.9%** |
| 逐回合 | r1 3/284 · r2 7/592 · r3 11/634 · r4 11/431 · r5 11/551 · r6 8/585 · r7 12/410 · r8 7/464 · r9 10/895 · r10 13/378（後期比例上升） |
| 最大單發 | `godie-nbbc.r` **2153%** · `godie-u00h.e` 702% · `godie-ogrh.r` 655% · `godie-n003.e` 484%（11 發裡 10 發會被夾）· `godie-uvng.e` 477% |

- ⚠️ 限定詞：`damage` 事件的 amount 是**落地掉血（護盾之後）**，夾限夾的是護盾之前的 impact ⇒ 這是**下界**；bot 對局的裝備曲線與真人不同（#928 線上榜單是 12/100 列 >100%，最高 401%）。
- 探針腳本用完即刪（`apps/game-server/src/__clamp_probe_temp_20260906.ts`，未進版控）。

## ⚠️ 主 session 收工要做的（⛔ 我刻意沒做）

1. **`content/bundle.json` / `manifest.json` / `content/config/_index.json` 沒 commit** —— 工作樹裡另一條 lane 未提交的 `content/config/ugc.json`（`digestRecompute`）與 `editor-target-profile.json` 已被烘進 bundle；提交會造出「bundle 有欄位、HEAD 的 schema 不認得」（2026-08-02 事故的形狀）。⇒ 等那條 lane 落地後統一 `skills:sync` 再 commit 產物。
2. `pnpm skills:check` 我沒跑（全域鎖）；`decor:check` / `typecat:check` 之類讀 config 欄位的產物可能過期 —— `skills:sync` 一次對齊。
3. 部署 ⛔ 不可 `--content-only`（#1017 的 SHIPPED 與 admin 住在映像裡）；部署前確認線上沒有 `one-shot-clamp` / `damage-rules` 的耐久覆蓋層。

## 測試帳

vitest：shared 3 次（batch 6 支 15/16 → 修 delayed 分母 → 單跑 3/3 → 突變 1 紅）＋ admin 1 次（34/34）；typecheck 1 次（只有暫存探針紅，已刪）。

## 🔁 更正（04:55）—— 上面「主 session 收工要做的 ①」已經**不成立**

`content/bundle.json` / `manifest.json` / `content/config/_index.json` 已隨另一條 lane 的 commit `fa830092d`（#1022）進 HEAD
—— 他們的 `content:build` 把本 lane 的 content 一起烘進去了。驗過 bundle@HEAD：`godie-ucrl.q` castType `targeted`、
`godie-h02u.r` effects[0] `damageArea`、`damage-rules.abilitySelfDamageGuard=true`、`one-shot-clamp.enabled=true`。
⇒ #1017 驗收 ⑤ 已滿，三張票全部關閉。②③ 仍然成立（`skills:check` 由主 session 跑；部署 ⛔ 不可 `--content-only`）。
