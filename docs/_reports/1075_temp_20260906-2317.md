# GH#1075 —— 積木清冊列出 GH#990 的可呼叫子模組（新層 `vfx-call`）

> lane 報告（第二波，2026-09-06 23:17）。⛔ 未 commit、⛔ 未跑 `bricks:build` —— 主 session sync 時重生成。

## 做了什麼（`tools/brick-census/bricks.ts`）

| 處 | 改動 |
|---|---|
| `BrickLayer` | 新增 `"vfx-call"`（⛔ 不重用 `vfx-subtype`：那個名字已被 `zVfxPresentation` 的表示形 enum 佔走，兩處註解都寫明了這個撞名） |
| `Brick.label?` | 新增，只有子模組層有（從文件的 `label` 讀） |
| `BrickParam.min?/max?` | 新增，`slotToParam()` 一起填 —— ⚠️ **額外效果**：`template` 與 `model-preset` 層的 params 之後也會帶 min/max（同一支 `slotToParam`，單一住處；純加法） |
| `ContentScan.vfxCallRefs` | 掃 `content/vfx-scripts/*.json`：`zVfxScriptDoc.parse` → `vfxScriptCallRefs()`（出貨展開器的同一支，⛔ 不 grep `"call"`），去重到腳本層 |
| 第 ⑧ 段 | 逐份 `content/vfx-subtypes/*.json` → `zVfxSubtypeDoc.parse`（壞掉的子模組在這裡擲，⛔ 不靜靜變成空積木）→ 一顆積木：`id` / `label` / `params`（type · default · min · max · values · origin，從文件 `params[]` 推導）/ `usedBy` / `origin: vfx-subtype@1:content/vfx-subtypes/<檔>` |
| `HOME["vfx-call"] = "vfx-subtypes"` · `GATED_LAYERS` 加 `vfx-call` | adminForm／editorForm 照既有規矩問：後台開不了 `vfx-subtypes` 集合、Codex 收據沒有這一層 ⇒ 兩邊都 `missing` |
| `editorFormNeededFromCodex` 的層清單、`renderBricksMd` 的積木欄（附 label） | 跟著長 |

`packages/shared/src/ops/bricksComplete.test.ts`：`GATED` 加 `vfx-call`；`BASELINE_GAPS` **39 → 43**（分母加了 4 顆、兩邊都缺，註解寫明是分母變動不是回歸）；sentinel 多一顆 `vfx-call` 假積木；⭐ 新一條 `it`：清冊 `vfx-call` 層的 id 集合 ＝ `content/vfx-subtypes/*.json` 的 id 集合（拿掉／加一顆子模組而沒 `bricks:build` ⇒ 紅；產生器漏推那一層 ⇒ 紅）。

## 量到什麼（產生器在記憶體裡跑，⛔ 沒寫產物）

`buildBricks("/Users/Takuro/GGD")` 輸出（scratchpad `bricks-probe/after.log`）：

- counts：total 155 → **159**、`vfx-call: 4`、gated 119 → 123、gaps 39 → **43**、missingEditorForm 5 → 9
- 四顆逐顆 **usedBy = 2**（AC 的數字全對）：`sub.bladestorm-8hit`(4 params) · `sub.dive-dash-thunder`(4) · `sub.doom-mark-cast`(4) · `sub.forward-twin-blast`(4)；每格帶 type/default/min/max/values/origin（例 `finisherStrikeIndex number 7 [1,100] census:vfx-scripts/godie-e002.ex#4.strikeIndex…`）
- `pnpm bricks:check` ⇒ 🔴 `過期了：ggd-bricks.json · ggd-bricks.md`（**預期**，主 session `bricks:build` 之後轉綠）
- 沙盒驗證：把記憶體產物寫到 scratchpad 假 root ＋ 複製 `content/vfx-subtypes/`，用 ROOT 改指沙盒的測試複本跑 ⇒ **3/3 綠**
- 真的 `bricksComplete.test.ts`（對著 repo 裡過期的產物）⇒ 🔴 兩條（`缺口變少了 43→39` ＋ `vfx-call 對不上 expected []`）—— ⚠️ **這是產物落後，⛔ 不要把 BASELINE 改回 39**；跑 `bricks:build` 就綠
- 靶向 `tsc --noEmit`（bricks.ts ＋ gen.ts）⇒ EXIT 0（⚠️ `tools/brick-census` 不在任何 tsconfig 專案裡，`pnpm typecheck` 量不到它 —— 順手發現，見下）

## 突變紀錄

第 ⑧ 段的迴圈改成永遠不推（`.slice(0, 0)`）→ 重跑產生器 → 沙盒測試 🔴 兩條（gaps 43→39、`vfx-call` expected [] to equal [4 顆]）。用 Edit 改回。

## ⛔ 沒做到的

- ⛔ 沒跑 `bricks:build`（preamble 禁令）⇒ `docs/editor-contract/ggd-bricks.{json,md}` 仍是舊的；`bricks:check` 與 `bricksComplete.test.ts` 在主 session sync 之前是紅的。
- Codex 收據（`claim.editor-form-receipts.json`）沒有 `vfx-call` 層 ⇒ 4 顆 editorForm 走代理值 = false。要 Codex 回收據（票文 Non-goals 的 picker 做完之後）。

## 需要主 session 動的柵欄外檔案

- `pnpm skills:sync`（含 `bricks:build`）重生成 `docs/editor-contract/ggd-bricks.json` / `.md`，然後 `git add`。

## 順手發現但沒動的

- `tools/brick-census/*.ts` 沒有任何 tsconfig 專案涵蓋（`pnpm -r typecheck` 跑不到；只有 tsx 執行期剝型別）。這一輪用 scratchpad tsconfig 靶向驗過 EXIT 0。⛔ 沒開票（第零守則⑧：留給主 session 決定要不要開）。
