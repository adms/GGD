# #550 —— 13 首地圖新音樂「還沒接上」：逐層量測報告

> owner 2026-08-22（逐字）：「地圖新音樂還沒接上」
> lane 柵欄：`apps/client/src/audio/audioSelect.ts` · `apps/client/src/audio/AudioSystem.ts` ·
> `content/config/audio-map.json`
> commit：`99230678`（只有一個新檔：`apps/client/src/audio/mapBedWiring.test.ts`）

---

## ⭐ 一句話結論

**HEAD 上這條鏈是通的 —— 七層全部量過，包含兩段從來沒有測試碰過的執行期接線。**
⛔ 我沒有改任何實作（改了就是憑猜改綁定）。真正缺的東西是**閘**：
「送進混音器的 arena id」與「`mapBgm` 拿來當 key 的字串」是不是同一個形狀，
在此之前**沒有任何東西在守**，而它壞掉的樣子是**靜悄悄退回舊的共用曲**。

---

## 1. 逐層量測（⛔ 全部是量到的，不是讀程式推論的）

| # | 層 | 結果 | 證據 |
|---|---|---|---|
| ① | 13 個音檔存在且是真的音樂 | ✅ | `file` → `ID3 v2.4 / MPEG layer III / 128 kbps / 44.1 kHz Stereo`；1,366,352 bytes ≈ **85.4 秒**；13 個 md5 **全不同**；零位元組佔比 1.6–1.8%（⛔ 不是靜音檔） |
| ② | 音檔進了版控 | ✅ | `git ls-files` 數到 **13/13**；`git check-ignore` → not ignored；`git status` 該目錄乾淨 ⇒ ⛔ **不是 2026-08-02 那種「未追蹤來源」事故** |
| ③ | `MANIFEST.json` 收錄 | ✅ | `content/assets/audio/bgm/MANIFEST.json` 的 `mapThemes` 13 筆齊全 |
| ④ | `audio-map.json` 綁定 | ✅ | `mapBgm` 13 筆，`loop:true`，gain 0.44–0.56 |
| ⑤ | 綁定 ≡ 玩家碰得到的場地 | ✅ | 與 `config/arena-pool.json` 的 **12 輪替 + 1 決賽（`arena.royale`）逐字相等**，⛔ 沒有孤兒也沒有遺漏 |
| ⑥ | Zod schema 收得下 | ✅ | `zConfigAudioMapDoc.mapBgm`（`.strict()` 的物件裡有宣告 ⇒ ⛔ 不會被靜默 strip 掉） |
| ⑦ | 出貨 `bundle.json` 有帶 | ✅ | `mapBgm` / `map.frieren` 都在 |

### ⭐ 執行期那一段（票裡三個候選的正式排除）

| 候選 | 判定 | 證據 |
|---|---|---|
| **1. arenaId 格式對不上** | ❌ **排除** | 伺服器 `apps/game-server/src/net/snapshot.ts:98` 每 tick 寫 `state.mapId = ctl.arena.id`（`arena.*`）→ `syncHudFromState`（`RoomStore.ts:752`）→ `hudStore.mapId` → `AudioDirector.tsx:151` `useAudioArena(mapId \|\| null)` → `AudioSystem.setArena`。**三條連線路徑（dev/offline、platform、replay）全部走同一個 `onStatePatch`**，⇒ ⛔ 離線／練習房也不是例外 |
| **2. MANIFEST 沒收錄** | ❌ **排除** | 見 ③ |
| **3. 後台 override 蓋掉 `content/`** | ❌ **排除（有證據，不是「查不到」）** | `apps/admin/src/configDocCoverage.ts:328` 把 `audio-map` 列為 **KNOWN_GAP** ——「今天調一格 gain 要編 repo + content deploy」⇒ **這份 config 根本沒有後台編輯頁，⛔ 所以不可能存在 override** |

---

## 2. ⭐ 真正的缺口：兩條既有守衛都跟「線上送什麼」無關

| 既有守衛 | 它證明了什麼 | ⛔ 它證明不了什麼 |
|---|---|---|
| `apps/client/src/audio/mapBed.test.ts` | `resolveBed` 的替換規則正確（含 owner 明說的「火圈不准被換掉」） | 它餵進去的是**手寫的** `"arena.shiganshina"` |
| `packages/shared/src/content/mapBgmCoversArenaPool.test.ts` | 兩份 JSON 對得起來、檔案存在且會 loop | 它比的是**兩份靜態檔** |

⇒ **失敗形態⑤：被測的不是出貨的那個。**
⛔ 改掉伺服器塞進 `MatchState.mapId` 的東西、或改掉 arena id 的拼法，
**這兩條全綠**，而每一張地圖靜悄悄退回共用的 `combat` 床。
⚠️ 而且玩家聽到的**不是**「少了一首」，是**舊的那一首** —— 所以沒有人會回報。
（這正是 `mapBgm` 刻意設計成 fail-open 的代價：missing track 絕不可以讓比賽變安靜。）

⭐ 另外量到的：**`setArena` → `bedFile` 這一段在此之前一條測試都沒有**
（全 repo grep：只有 `LocalPrediction.setArena` 的呼叫，跟音訊無關）。

---

## 3. 交付：`apps/client/src/audio/mapBedWiring.test.ts`（139 行，含檔頭說明）

⛔ **全程不打一個 arena id**，全部從出貨的東西長出來：

```
content/config/arena-pool.json      ← 玩家真的會被丟進去的每一張場地（12 + 決賽）
  → MatchState.mapId
  → syncHudFromState()              ← 出貨的投影，⛔ 不是重寫一份
  → hudStore.mapId                  ← AudioDirector 餵給 useAudioArena 的就是這一格
  → AudioSystem.setArena()          ← 真的混音器（假的 WebAudio graph）
  → sys.bedFile                     ← 必須就是那張圖自己的曲子
```

⭐ **掃的方式本身就是回合邊界那條路**：場景固定在 `combat` 不動，只有 arena 在換 ——
那正是伺服器每回合換場地時真的會發生的事（`setArena` 在 combat 進行中換床的那條分支）。

### 突變驗證（一條，最承重）

| | |
|---|---|
| 改哪一行 | `audioSelect.ts:52` `map.mapBgm?.[arenaId]` → `map.bgm?.[arenaId]` |
| 結果 | ✅ **紅**，13 張場地**全部**被指名 |
| 訊息 | 逐張印「送到混音器的是 `"arena.frieren"`，播出來的是 `assets/audio/bgm/combat.mp3`（應為 `map.frieren.mp3`）」 |
| 為什麼這條夠 | 它同時證明**測試真的讀到了混音器的最終狀態**（看得到 fallback 的 `combat.mp3`），⛔ 不是空跑 |
| 還原 | 用 `Edit` 改回，⛔ 不是 `git checkout <檔>` |

### 測試數字

| | |
|---|---|
| `npx vitest run` | **2 次**（① 寫完 ② 突變）—— 上限 3 |
| `tsc` | **1 次**（全部寫完之後） |
| 實作行數 | **0**（⛔ 沒有改任何實作，這一版的產出就是閘本身） |
| 新增測試 | 1 檔 / 2 個 `it()` |

⚠️ **typecheck 有兩個錯，⛔ 都不是我的**：
`packages/shared/src/sim/effects/effectChildChains.ts(64,3)` 與
`effectRegistry.ts(142,14)`（缺 `spawnModelFx` / `screenFlash` / `screenShake` / `floatingText`）
—— 那是**另一條 lane 正在飛的 effect registry**。我只新增了
`apps/client/src/audio/mapBedWiring.test.ts` 一個檔。

---

## 4. ⚠️ 如果 owner 那句話現在仍然成立，剩下的可能性

程式面已經排除完了，所以只剩**部署面**。按可能性排序：

1. ⭐ **他是在這批 commit 之前說的。** `git log` 顯示 #531 的最後一批落地
   （`c8db4390` 十三種時代樂器 · `bd7b0e8`… `c00441af` 整進音樂總覽頁）就在同一天。
   ⇒ **最可能：那句話在說的狀態已經被自己的後續 commit 修掉了。**
2. **線上跑的是舊映像。** `mapBgm` 是 GH#531 才進 Zod union 的欄位。
   ⚠️ `content/` 是 live bind-mount 而映像是 build 時烘的 ——
   舊映像 + 新內容 = **整份內容載入失敗退回骨架**（2026-08-02 事故的形狀）。
   ⇒ 這一版**必須完整重建映像，⛔ 不可以 `--content-only`**。
3. **瀏覽器快取**：`config/audio-map.json` 與 mp3 都吃 HTTP cache。
   煙霧測試一律**開全新分頁**（本來就是規定）。

⛔ 我沒有、也不會去碰正式站驗證這三點。

---

## 5. 需要主 session 接線

| # | 事情 | 為什麼不是我做 |
|---|---|---|
| 1 | ⭐ **柵欄外多了一個檔**：`apps/client/src/audio/mapBedWiring.test.ts`（**新檔**，⛔ 零碰撞風險 —— 其他 lane 不會建同名檔）。票明說「這條守衛才是真正的產出」，而柵欄那三個檔都放不下它 | 請確認接受；⛔ 我沒有動柵欄內任何一個檔（`audioSelect.ts` 突變後已用 `Edit` 還原，`git diff` 為空） |
| 2 | `pnpm skills:sync` / `content:build` | 全域鎖，主 session 最後統一跑。⚠️ 我**沒有**改 `content/`，所以這一票不會讓產物過期 |
| 3 | 部署時**必須完整重建映像**（⛔ 不可 `--content-only`） | 見上面第 4 節第 2 點 |
| 4 | ⛔ **後台欄位：這一票沒有新增任何可調參數** | `audio-map` 在 `configDocCoverage.ts` 是 **KNOWN_GAP**（147 個 SFX key × 3 格不適合通用長表單，該是一頁混音表）。⇒ ⚠️ 這是**既有**的帳單，⛔ 不是我這一票造成的，也不該由這一票順手收掉（第零守則⑧：⛔ 不要替 owner 決定排序） |

### ⭐ 順手發現、⛔ 沒有當場修（第零守則⑧，建議開票）

| 發現 | 嚴重度 | 說明 |
|---|---|---|
| `apps/client/src/render/arenaSelect.ts` 的 `roundArenaId` / `roundMapId` **在 schema 裡根本不存在** | 低（今天無害） | 那是 task #145「等 sim 落地」留的前瞻欄位。今天永遠走 `mapId` fallback ⇒ 行為正確，但它是一段**看起來有在運作、實際上是死的**分支（第一·五守則的形狀：說了但不會發生）。⚠️ 真正的風險在**未來**：哪天 sim 真的加了 per-round 欄位，**渲染**會跟著它走，而**音訊讀的是 `mapId`** ⇒ 兩邊可能指向不同場地（畫面是 A、音樂是 B），⛔ 而且不會有任何東西紅 |
| `ACTIVE_BGM_VARIANTS` 一旦被重新開啟，會**繞過**地圖曲 | 低（目前是 `{}`） | `chooseBgmFile` 是 `this.rotation.next(scene, track.file)` —— rotation **以 scene 為 key**。若哪天把 `SAMANTHA_VARIANTS` 打開，`combat` 那一格的 variant 會在替換之後**再蓋掉一次**地圖曲 ⇒ 每隔一次進場就播回共用曲。⛔ 現在不會發生，但沒有守衛擋 |
