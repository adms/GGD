# 驗收批 N4「時序與持續」（11 份） —— GH#962

**日期** 2026-09-03 · **分支** main · **治具** `packages/shared/src/ops/acceptanceN4.test.ts`
**判定帳本（機器讀的唯一住處）** `docs/editor-contract/ggd-acceptance-n4.json`

---

## 0. ⛔⛔ 前提回驗：票文三個前提，**兩個不成立**

| # | 票文說 | ⭐ 量到的 | 影響 |
|---|---|---|---|
| ① | 「本批的 id 清單**只有一個住處**（#953 定案的 **#838 body**）—— ⛔ 不要抄進測試」 | ⛔ **不成立**。`gh issue view 838` 的 body 今天只有 **3 處** `godie-`（兩處是 `vfx-script@1` 示意、一處是舉證「編輯器不能寫 ability JSON」）—— ⛔ **沒有 46 份清單**。#953 的收尾留言逐字寫著它被交代「⛔ 不碰 #838」，於是機器讀的那一份落在 `ggd-acceptance-eight.json`，⭐ **而那一份只有八招／11 個 id，⛔ 不含 46 份的分批** | ⭐ 照 #953 的先例替**本批**建 `ggd-acceptance-n4.json`，⛔ 沒有把清單抄進測試 |
| ② | 共同規則 #5 必測案例：「`godie-e00r.q` 極小·**範圍** ⇒ **30s**，⛔ 陣列寫 6」 | ⛔ **不成立**。出貨文件**明填** `cooldownShape: "單體"` ⇒ `resolveCooldownTier` 解析成 **6s**（＝陣列值，⛔ **沒有分岔**）。30s 是**拿掉那一格之後**才會發生的事。⚠️ 同一件事在 `godie-nbbc.r` 上也一樣：pin 在 ⇒ **60s**（＝陣列），pin 拿掉 ⇒ 120s | ⭐ 把它翻成一個**兩個方向都動得到**的校準（見 §2） |
| ③ | 共同規則 #7「被動⛔ 不得播放假的主動施法動作」 | ⭐ 成立，⛔ **但它已經有守衛** —— `sim/abilities/passivesNeverFakeCast.test.ts`（兩個方向都驗過） | ⛔ **不重寫**（第零守則：同一件事⛔ 不做兩遍），只量形狀記進帳本 |

### ⚠️ ②為什麼值得寫下來

#953 的收尾留言逐字寫：「⭐ 而**規則 B 的兩個必測案例逐字複驗成立**：`godie-nbbc.r` 極大／陣列 **60** ·
`godie-e00r.q` 極小／陣列 **6** —— ⭐ 那正是它們值得測的理由（**陣列說 60 而級距解析成 120**）」。

⇒ ⭐ 它複驗的是**陣列與級別兩格**，⛔ 而括號裡那句「級距解析成 120」**從來沒有被跑過**。
兄弟守衛 `acceptanceEightSingleHome.test.ts` 也只斷言 `cooldownTier` 與 `cooldown[0]` 兩格 ——
⇒ ⛔ **兩層都沒有呼叫過解析器**，於是一個沒被量過的結論以「已複驗」的樣子活了下來
（CLAUDE.md：**更正也是一個宣稱** · **一個看起來已經量過的東西，量的不是你以為的那個**）。

⭐ 而真正的風險是反過來的、而且更大：**`cooldownShape` 那一格是冷卻的 5×／2× 開關** ——
刪掉 `godie-e00r.q` 的那一格 ⇒ 6s **靜靜地**變 30s，卡面／schema／註冊表沒有一處會紅。
（前科逐字記在 `content/cooldownTiers.ts` 檔頭：09-04 龜派補一格**純視覺**的範圍限定鏡頭噪動
⇒ `cooldownShapeOf` 改判「範圍」⇒ 冷卻 **60 → 120**。）
⇒ §2 兩個方向都斷言，那一格再被刪就會紅。

---

## 1. ⭐ 逐份判定（11/11，⛔ 沒有一份是空白）

| id | 技能 | 判定 | 一句話 |
|---|---|---|---|
| `godie-hart.r` | 01-04 超究武神霸斬 | 阻塞於#943 | ⭐ 時序軸**實測通過**（7 段＋7 錨，收尾在最後）；缺 `radiusTier`/`conditionTier` |
| `godie-e002.ex` | 20-002 解放.約束勝利劍MAX | 阻塞於#943 | `delayed` 7×0.12s，逐段有 vfx＋`{{i}}Hit`；⛔ 五級距**五格全缺** |
| `godie-e00l.ex` | 同上（鏡像） | 阻塞於#943 | 與 `e002.ex` 逐欄同形；⛔ 五格全缺 |
| **`godie-hjai.w`** | 04-02 炸彈陣 | ⛔ **不通過** | ⭐ **本批核心軸的違反** —— 兩段持續被混成一次（見 §3） |
| `godie-hvsh.e` | 48-03 鮮血神殿 | 阻塞於#943 | `applyBuff` 10s ＋ `onInterval`(icd 1) ⇒ 每秒一次領域結算 ✅；缺 `rangeTier`/`conditionTier` |
| `godie-edem.ex` | 45-002 天照 | 阻塞於#943 | `dot` 1s×10s ⇒ 10 次，與三個 10s 狀態同段落齊 ✅；缺 `rangeTier`/`conditionTier` |
| `godie-e00r.q` | 59-01 吞噬 | 阻塞於#943 | 兩條 `onInterval`(icd 0.9)＋1.8s「兩餐之間」狀態；⛔ 前提更正見 §0② |
| **`godie-h02u.ex`** | 92-002 最終戈壁 | ⛔ **不通過** | ⭐ 6 次精準、絕對 tick ✅；⛔ **死亡後殘留**（見 §4） |
| `godie-e00l.passive` | 20-00 銀色甲胄 | 阻塞於#943 | `onDamageTaken`(icd 1)＋1s 屏障 ✅；⛔ 五格全缺；⚠️ 卡面「魔力 >30%」條件無節點 |
| `godie-emns.passive` | 44-00 機警 | 阻塞於#943 | ⭐ 屏障吸收順序正確（護盾池**後**、免死**前**）；規則 #7 **不違反**（`innateKind:"active"`） |
| `godie-emns.ex` | 44-002 交換筆記本 | 阻塞於#943 | ⭐ 要點實測成立：換**現存生命**、`maxHp` 不動 |

**⇒ 通過 0 · 阻塞於#943 9 · 不通過 2。**
⚠️ 「0 通過」是票文 Known risks 逐字預期的（`conditionTier` **全庫 0 覆蓋** ⇒ 共同規則 #4 對 11/11 都判阻塞）
—— ⛔ **沒有為了好看而放寬**。

**級距缺口（量到的）**：`conditionTier` **11/11 缺** · `rangeTier` **8/11 缺** ·
`radiusTier` 7/11 · `manaCostTier` 7/11 · `cooldownTier` 3/11。

---

## 2. ⭐ 共同規則 #5 —— 一把**兩個方向都動得到**的尺

| 方向 | 做什麼 | 量到 |
|---|---|---|
| **A（已知有的量得到）** | 出貨那一份直接丟 `resolveCooldownTier` | `cooldownShape` = **單體** ⇒ **6s**（＝陣列） |
| **B（已知沒有的量不到）** | 把 `cooldownShape` 那一格拿掉再解析一次 | `autoShape` 深走訪看到 passive hook 裡的 `radius: 12` ⇒ **範圍** ⇒ **30s** |

⇒ ⭐ 兩邊答案**不同** ⇒ 這把尺真的在讀那一格。
⛔ 只驗 A 的話，一個根本沒有呼叫解析器的實作也會綠 —— 那正是 §0② 說的那件事。

---

## 3. ⛔⛔ `godie-hjai.w`：**兩段持續被混成一次**（不通過）

卡面（剝掉 `「…」` 對白之後）逐字宣告**兩段**：

> 每秒可以燒傷 55 點並**持續 5 秒**的時間。在火柱消散**之後**，在火焰中的部隊仍然會持續受到 10 點的傷害，**持續 5 秒**。

出貨 JSON（`tpl-periodic-field` 展開後）只有**一段**：

```
delayed{ count: 5, intervalSec: 1, delaySec: 1 }   ← 火柱那 5 秒
（第二段殘留 DoT —— 零個節點）
```

| | |
|---|---|
| **量到** | 宣告 **2** 段 vs JSON **1** 個帶持續的節點 |
| ⭐ **⛔ 不是 primitive 缺口** | `dot`（`intervalSec` + `durationSec`）**早就出貨**，同一批的 `godie-edem.ex` 就在用它 ⇒ 這是**內容側漏了第二段**，⛔ 不是 Main 缺積木 ⇒ ⛔ 不開 primitive 票（票文 Known risks 的分流） |
| ⚠️ **鏡像同洞** | `godie-h020.w` 的 `template.params` 與它**逐欄相同** ⇒ 同一個缺口（⛔ 那一份不在本批） |
| ⭐ **修法** | 在模板參數或 `effects` 補第二段 `dot`（10 點 × 1s × 5s），起點接在第一段結束之後 |

⚠️ ⛔ **本票對 `content/abilities/` 只讀**（票文 Files 節逐字），所以⛔ **沒有動它** ——
判定記成「不通過」，修好之後 §5 的棘輪會紅並要求更新帳本。

---

## 4. ⛔ `godie-h02u.ex`：三項要點**中兩項**（不通過）

票文要點逐字：「持續 6 秒、每秒 **+75 經驗共 6 次** ⇒ ⛔ 不得因幀率**多發**、**漏發**或**死亡後殘留**」。

| 項 | 量到 | |
|---|---|---|
| ⛔ 不多發／漏發 | 排程 = **絕對 tick** `[30,60,90,120,150,180]`，間隔一律 30（＝1 秒）；實跑 payouts **正好 6** | ✅ |
| ⛔ 死亡後殘留 | 第 70 tick 殺掉施法者（`alive=false` 已斷言）⇒ **payouts 仍然是 6**，剩下的 4 次照發 | ⛔ **違反** |

⭐ **根因是一格預設值**：`delayed.stopOnCasterDeath` 預設 **false**，而出貨文件沒有填它。
⇒ 修法是**一格 JSON**（`stopOnCasterDeath: true`），⛔ 不是引擎缺積木。
⚠️ 玩家看得見 —— 陣亡之後經驗條還在跳。

---

## 5. ⭐ 治具（一套 × 11 列，⛔ 不是 11 條測試）

| § | 驗什麼 | 兩個方向 |
|---|---|---|
| §1 | 判定表 11 份、三選一判定、理由 ≥20 字、id 指得到**註冊表**裡的技能 | 空白／懸空都紅 |
| §2 | 共同規則 #5 實際生效值 | pin 在 ⇒ 6s **且** pin 拿掉 ⇒ 30s |
| §3 | 多段**每一段各自落地**：段數 = `combo-strikes.json` 的 `steps+1`、逐段 `comboStrike` 錨 1…7、finisher 只在最後；且技能文件⛔ 不可自己長出 `steps` | 正：`hart.r` 7/7/1 · **反：`emns.ex` 0 段 0 wave** |
| §4 | 週期次數精準（絕對 tick 等距）＋ 落地次數 ＋ 死亡殘留棘輪 ＋ 交換**現存生命** | 正：`h02u.ex` 6 次 · **反：`emns.ex` 0 次** |
| §5 | 逐份重量輪廓與帳本比對（棘輪）＋「宣告持續段數 > JSON 節點數 ⇒ 判定必須是不通過」＋「缺標籤⛔ 不可判通過」 | 內容修好紅／內容壞掉也紅 |

⭐ **走出貨的資料**：`ContentLoader → registerAll`（模板已展開、級距已解析）＋ 真的 `SimWorld`。
⛔ 一個自造夾具都沒有（失敗形態⑤）。
⚠️ ⭐ **輪廓一定要從註冊表量**：`godie-hjai.w` 的 5 段整個住在 `tpl-periodic-field` 裡，讀原始 JSON 會量到 **0 段**。

### 🧬 突變（一批一條，實跑；`scripts/edit-or-die.py`）

**M1** `sim/effects/delayed.ts:291` 的排程從絕對 tick 改成每發都掛同一個 `world.tick + delayTicks`
（＝拿掉 `i * intervalTicks` —— 「同一 tick 全塞／遞減計數器」那一族的形狀）
⇒ 🔴 **§4 紅**，訊息逐字指名 `godie-h02u.ex` 並說「間隔不唯一 ⇒ 遞減計數器那一族的形狀」。
還原 ⇒ `git diff` 空。

⚠️ §3 的迴圈體與 §4 走**同一支** `delayedSystem` ⇒ M1 同時證明 §3 的量尺是活的，
⛔ 不另外再做一次（第零守則⑦）。

---

## 6. ⛔ 本票**沒有**做的（誠實列出）

- ⛔ 沒有改任何 `content/abilities/*.json`（票文：**只讀**）⇒ §3/§4 的兩個缺陷**留在原地**，判定記成「不通過」
- ⛔ 沒有跑 `pnpm skills:sync`（全域鎖，六批並行）
- ⛔ 沒有做「像不像」的視覺判定（⇒ #664 Tier 2）、⛔ 沒有 Promote 任何產物
- ⛔ 沒有重寫共同規則 #7 的守衛（已有 `passivesNeverFakeCast.test.ts`）
- ⚠️ 驗收標準 #2 的「**連續擷圖**」⛔ **沒有做** —— 那需要真渲染，屬於 #664 Tier 2 的批核頁；
  本票留下的是 **JSON receipt（帳本 11 列的 `measured`）＋ 失敗原因 ＋ 技能 ID**

## 7. ⭐ 建議下一步（⛔ 由 owner 排，本票不擅自插隊）

1. `godie-hjai.w`（＋鏡像 `godie-h020.w`）補第二段殘留 `dot` ⇒ §5 棘輪會紅，改帳本判定
2. `godie-h02u.ex` 補 `stopOnCasterDeath: true` ⇒ §4 棘輪會紅，改帳本判定
3. ⚠️ `godie-e00r.q` / `godie-nbbc.r` 的 `cooldownShape` 那一格 —— ⛔ **不要刪**；§2 現在守著它
4. 兄弟守衛 `acceptanceEightSingleHome.test.ts` 的規則 B 註解仍寫著「級距解析成 120」⇒ 那句話沒被量過（見 §0②）
