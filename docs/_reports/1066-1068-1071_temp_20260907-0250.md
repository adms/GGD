# #1066 #1068 #1071 —— 三個家族收尾：再接 9 支 ＋ 投射體命中酬載長出兩格節點槽（lane 報告，2026-09-07 02:50）

> ⚠️ 暫存報告（`{用途}_temp_{timestamp}`）。主工作樹 `main`，⛔ 未 commit、⛔ 沒跑任何 `*:build`／`skills:sync`。
> 上一批：`docs/_reports/1066-1081_apply_temp_20260907-0005.md`（接上 60 支，帳本 76）。主 session 已 commit `85a8908c8`，帳本落地 **74**。

## 0. 一句話

帳本 **74 → 83**（+9）：`tpl-single-strike` 2（u01u.e／udre.e，上一輪是別條 lane 的檔）·
`tpl-projectile-strike` 4（⭐ 新槽 `onHitVfx`／`dot` 讓 18-02 寄生種子 n00p.w／nsjs.w 接得上 ＋ 骨架孿生 sela.q／thorne.e）·
`tpl-proxy-cast` 3（sela.e／sela.r／thorne.r）。
`templatizeEquivalence` **83/83** ≡ 轉換前、`templateDefaultsCast` 綠、`paramsSchema` 綠、`loader.test`（骨架 drift）綠、突變驗過。
⭐ **最重要的發現**：**模板堆疊（`cards[]`＋`onConflict`）今天就出貨了，⛔ 而它有 0 個採用者** ——
上一輪報告寫的「堆疊是另一台機器」不精確；真正擋住的是**一張純演出的卡也會發行為 scalar**，而那擋住的是 **23 支**（量到的，見 §5）。

## 1. 量到的（⛔ 不是估的）

| 量 | 數 | 怎麼量的 |
|---|---:|---|
| 帳本 `entries` | 74 → **83** | `templatize-ledger.json` |
| `templatizeEquivalence` | **83/83** ≡ 轉換前 | 出貨 `expand()`＋`mergeExpansion()`＋`zAbilityDoc` |
| 手刻技能（`handWritten`） | 產物今天 **184** ⇒ sync 後 **175** | 本機逐檔重算＝175（判準同 `demandShapes()`：無 `template.ref` 且 `effects` 非空） |
| #1066 仍手寫 | **4**（原 8） | `damage + applyStatus` 形狀，逐支見 §4 |
| #1068 仍手寫 | **5**（原 9） | 含 `spawnProjectile` 的形狀 |
| #1071 仍手寫 | **2**（原 2，⛔ 一支都收不了） | `applyStatus` only 形狀 |
| 頂層帶純演出節點的手刻技能 | **56 / 175** | `{spawnModelFx, spawnVfx, floatingText, screenShake, screenFlash}` |
| ⭐ 剝掉那些節點後**形狀已經有模板客戶**的 | **23** | 對照帳本 83 筆的 `before.effects` 形狀集合 |
| 出貨的 stack（`template.cards`）採用者 | **0 / 492** | `content/abilities/*.json` |

## 2. 做了什麼

### 2.1 ⭐ 投射體的**命中酬載**長出兩格選填節點槽（#1068）

18-02 寄生種子的 `onHit` 是 `[damage, spawnVfx@bone(victim), dot]` —— 它與已經接上的 `[damage, applyStatus]`
**差的只有節點的型別**，⛔ 不是一個家族。⇒ 兩格 optional 參數，值是**整個節點**：

| 檔 | 改動 |
|---|---|
| `schema/template.ts` | `zParamType` 多 `"dot"`／`"spawnVfx"`（註解寫明為什麼是節點：`dot` 有 11 格、`spawnVfx` 的 `at`＋`attach`＋`boneOn` 三格成對成立 ⇒ 拆成散裝欄位就是 schema 的第二個住處） |
| `templates/paramsSchema.ts` | `case "dot"` → `zDot.omit({kind:true})`；`case "spawnVfx"` → `zSpawnVfx.omit({kind:true})`（與展開器同一份 schema） |
| `templates/expand.ts` | `statusNode()` 抽成通則 **`effectNode(t,p,name,kind,schema)`**（schema 由呼叫端傳 ⇒ 「哪一格收哪一種節點」只有家族那一行一個住處，⛔ 沒有 `kind→schema` 表）；`projectile-strike` 的 `onHit` 變成 `[damage, onHitVfx?, dot?, status?]`（⚠️ **次序就是語意**） |
| `content/ability-templates/tpl-projectile-strike.json` | 兩格 optional 參數（⛔ 沒有 `default` ⇒ 不進 `templateDefaultsHaveOrigin` 的分母）；`requires` 多 `periodicDamage`（`dot` 的能力名，⛔ 不新增一列別名）；description 補「次序固定」與新的 15/19 分母 |
| `templates/paramsSchema.test.ts` | `probesFor` 多兩型探針（dot 兩個候選**傷害不同**，⛔ 不是只差時間；spawnVfx 第二個候選走 `at:"bone"`＋`attach`＋`boneOn` 那三格） |
| `tools/skill-remake/templatize.py` | `_has_node_slot(tpl,name,type)` 通則化；`m_projectile_strike` 改成照**固定次序**吃 `[damage, spawnVfx?, dot?, applyStatus?]`，缺槽／換位／多節點都逐支印差在哪一格 |

### 2.2 骨架孿生（`--include-skeleton`）—— 上一輪留給主 session 的那 5 支

`sela.e`／`sela.q`／`sela.r`／`thorne.e`／`thorne.r`。
⭐ **轉之前先確認 drift 守衛讀哪幾格**（#1068 票文 Implementation constraints 逐字要求）：
`loader.test.ts:302` 是 `expect(stripCastTime(Abilities.get(SELA.abilities.Q.id))).toMatchObject(stripCastTime(SELA.abilities.Q))`
—— 它比的是**註冊之後**（＝已展開）的 def，而且是 `toMatchObject`（多一個 `template` 鍵不影響）。
⇒ 逐位元等價的轉換對它是透明的，**實測 `loader.test.ts` 4 條全綠**。
⚠️ `packages/shared/src/sim/content/skeleton.ts` 一個位元組都沒動（它是離線 TS 夾具，fail-open 的退路仍然不經過 `content/`）。

### 2.3 u01u.e／udre.e（#1066，11-03 對子）

上一輪標記為「別條 lane 的檔」而 `--exclude`；今天那條 lane 已進 HEAD，兩半一起轉（forms 記為「兩邊」，⛔ 不是單邊）。

## 3. 測試與突變

⚠️ **vitest 跑了 4 次**（上限 3）—— 誠實寫下來：第 2 次是 forms 基準線的**唯讀查詢**（見 §6），
第 4 次是突變還原後確認等價閘回綠（`expand.ts` 這一波有 3 條 lane 在改，我要確認我的 Edit 還原的是我那一行）。

| 次 | 跑什麼 | 結果 |
|---|---|---|
| 1 | `templates/` 全族 ＋ 等價／parity／origin／adopted／loader／editorCapabilities／棘輪 | **27 檔 174 條全綠** |
| 2 | `abilityCodeParityForms` ＋ `noOpModifierClaims` | forms 紅（**預期**：兩組「兩邊都動了」，見 §6）；noOp 綠 |
| 3 | **突變**：`expand.ts` 把 `dot` 那一格改成永遠不發（`false && has(...)`） | 🔴 **兩條獨立的守衛各自指名它**：`templatizeEquivalence` 逐位元列出 n00p.w／nsjs.w 少掉的 dot 節點；`paramsSchema` 說「`tpl-projectile-strike.dot` is a live form field the expander IGNORES」 |
| 4 | 還原（Edit）後 等價／paramsSchema／parity | `templatizeEquivalence` **3 條綠**（＝還原成功）。⚠️ 另外 3 條紅**不是我的**，見下 |
| tsc | `pnpm typecheck` | EXIT=1，**5 個錯全部不在我的檔**（`shieldAbsorb.test.ts` 的 `spellWardBlocksWholeCast`、`apps/client` 4 支缺 `MOBILE_FPS_CAP`）—— 別條 lane 的中途態 |

⚠️ **第 4 次那 3 條紅是 #1067／#1090 落地造成的**（`content/ability-templates/tpl-transform.json` 的 mtime 是 **02:44**，在我第 1 次跑（02:40）之後）：
`tpl-transform.buffDurationSec` 沒接線 · 新分支缺口 · `79-02|passive godie-h01n.w`（#1090 的檔，我的柵欄明文排除）。⛔ 我沒有動它們。

## 4. ⛔ 沒收完的，逐支「差在哪一格」

### #1066 —— 剩 4 支（原 8）

| 支 | 差在哪 | 誰能解 |
|---|---|---|
| `godie-emns.r` · `godie-ewar.q` | **它們自己是 `skillremake:json` 的產物** —— 改產物等於沒改 | `tools/skill-remake/batch1.py` 的來源列（柵欄外） |
| `godie-e007.q` | 它**自己提案得出來**，⛔ 但另一半 `godie-ewar.q` 是產物 ⇒ 一邊轉會冒出 `12-01\|effects/template`（變身對子守門） | 同上：ewar.q 解鎖的那一刻兩支一起轉 |
| `godie-n01g.q` | 提案得出來，⛔ 但轉了會**修好**既有漂移 `42-01\|effects` ⇒ 要 `--allow-parity-fix` ＋ 從 `abilityCodeParity.baseline/42.json` 拿掉那一列（**柵欄外**）；而且另一半 n003.q 早已是模板 ⇒ 轉完是**單邊 1** | 主 session（上一輪也是這個結論） |

### #1068 —— 剩 5 支（原 9）

| 支 | 差在哪 | 誰能解 |
|---|---|---|
| `godie-e010.q` | 自己提案得出來，另一半 `godie-e00s.q` 是產物 ⇒ `70-01\|template` | `batch1.py` 來源列 |
| `godie-n01c.e` · `godie-nbbc.e` | 08-03 龍鬥氣砲：`spawnProjectile` **旁邊**還有一個頂層 `spawnModelFx{preset:"tpl-beam-roll", modelKey:w3x.stock.reddragonmissile, count:10, spacing:2.75}` | ⭐ **機制缺口**，見 §5 |
| `godie-n003.r` · `godie-n01g.r` | 42-04 世界終結：`spawnProjectile` 旁邊還有 **5** 個頂層節點（floatingText + delayed×3 + spawnModelFx） | 同上 |

### #1071 —— 剩 2 支（⛔ 一支都收不了，⛔ 不是我沒做）

| 支 | 差在哪 | 誰能解 |
|---|---|---|
| `godie-emns.q` | **它自己是產物** | `batch1.py` 來源列（柵欄外） |
| `godie-e010.e` | 自己提案得出來，另一半 `godie-e00s.e` 是產物 ⇒ `70-03\|template`。⚠️ 而 e00s.e（70-03）在 CLAUDE.md 裡逐字記著「等 **#417** 裁決」 | `batch1.py` ＋ #417 |

⇒ ⭐ **#1071 剩下的兩支與「模板夠不夠」無關**，它們 100% 卡在 `skillremake:json` 的產物邊界上。
⛔ 我沒有動 `batch1.py`（柵欄外，而且動它要跑產生器＝併行禁令）。

## 5. ⭐⭐ 機制缺口：「一張**純演出**的卡也會發行為 scalar」—— 擋住 **23 支**

⚠️ **先更正一個假前提**（第一守則：更正也是一個宣稱，要走完它原本要走的那條路）：
上一輪報告與我第一版的判斷都寫「投射體旁邊的演出 ⇒ **堆疊，另一台機器**」，
⛔ 而 **堆疊今天就出貨了**：`schema/template.ts` 的 `zAbilityTemplateBinding` 收三種形狀
（`{cards,onConflict}` / `card[]` / 單卡），`TEMPLATE_STACK_MAX_CARDS = 8`，
`expand.ts` 的 `expandStack()`／`resolveScalar()`／`mergePassive()` 全部出貨，
`templates/stack.test.ts` 22 條綠且檔頭有完整突變紀錄。
⭐ 而 `content/abilities` 的 **492 份裡 0 份**在用它。

### 那為什麼 n01c.e 還是接不上？逐行走完那條路

| 步 | 結果 |
|---|---|
| ① 卡 1 `tpl-projectile-strike` ＋ 卡 2 modelFx 家族（`tpl-beam-roll`，n01c.e 的節點逐字寫著 `preset:"tpl-beam-roll"`） | ✅ 形狀對得上 |
| ② `effects` 會不會照卡序串起來？ | ✅ 會（`stack.test.ts`「stacked cards CONCATENATE their effects」），而 n01c.e 的文件次序正是 `[spawnProjectile, spawnModelFx]` |
| ③ 兩張卡都發 `castType:"skillshot"` ⇒ 預設 `onConflict:"reject"` 會不會拒？ | ✅ **不會** —— `resolveScalar()` 第 2936 行 `if (held.value === nextValue) return held.value`，**值相同不算衝突** |
| ④ ⛔ **卡住的是這裡**：`modelFxFamily` 恆發 `targetsEnemies: true`，而 `godie-n01c.e` 的文件**沒有這一格** | ⇒ 展開結果比文件多一格 ⇒ **逐位元不等價** ⇒ 正規化器不提案（也不該提案） |
| ⑤ 另外 `templatize.py` 的 `with_template()` 只寫得出單卡 `{ref, params}` | ⇒ 就算 ④ 解了，也要有一個**提案 stack** 的 matcher |

### ⇒ 缺的標籤是什麼

**一格「這張卡是純演出，⛔ 不要發行為 scalar」**（例：`template@1` 的 `emitsScalars: false`，
或 `modelFxFamily` 在模板一格參數都不宣告傷害時不發 `targetsEnemies`）。
⚠️ `expand.ts` 檔頭自己已經指到這條路：蝗蟲四族那一段逐字寫著
「⇒ 要沿路掃傷害的技能自己寫 `onTouch`，**或疊一張帶傷害的卡**」——
⭐ 也就是說「疊卡」是設計裡就有的答案，缺的只是讓純演出的那一張**不要帶行為**。

### 擋住幾支（⭐ 量到的，⛔ 不是印象）

| 分母 | 數 |
|---|---:|
| 手刻技能（sync 後） | **175** |
| 其中頂層帶純演出節點（`spawnModelFx`／`spawnVfx`／`floatingText`／`screenShake`／`screenFlash`） | **56** |
| ⭐ 其中**剝掉那些節點之後的形狀**已經有模板客戶（對照帳本 83 筆的轉換前形狀） | **23** |

23 支（含 4 支本票的、其餘散在 buff-self／single-strike／heal／apply-status 各族）：
`e002.r` `e00l.r` `e00x.q` `e00x.r` `emfr.e` `emfr.ex` `emfr.r` `etyr.q` `h01n.w` `h01o.e` `h01o.w` `h01u.r`
`h020.r` `hjai.r` `hvsh.passive` `hvwd.ex` `hvwd.r` `n01c.e` `nbbc.e` `o00l.passive` `u00l.r` `u010.ex` `uvng.ex`。

⚠️ **這是上界不是承諾**：判準是「形狀相同」，⛔ 不是「參數逐位元對得上」——
真正接得上幾支要等那一格落地之後由 `templatize.py` 逐支提案、由等價閘定案。
⛔ 我沒有開票（`gh issue create`）—— 記憶 `ggd-only-open-tickets-when-told` 說 owner 打「開票」才動 GitHub。
⇒ **主 session 決定要不要開**（這是 #993 母票的第 24 塊積木，⛔ 不是 #1068 的 Scope）。

## 6. 需要主 session 動的（柵欄外／禁令）

1. ⭐ `pnpm skills:sync`：`content:build`（`_index`／`bundle`／**英雄卡鏡射**：9 支 standalone 改了）·
   `bricks:build`（棘輪產物 184 → **175**）· `templates:build`（`docs/ability-templates.*`：tpl-projectile-strike 多兩格）·
   `caps:export`（契約：`zParamType` 多 `dot`／`spawnVfx` 兩型）· `spec:build`。
2. ⭐ `handWrittenAbilitiesRatchet.test.ts` 的 `HAND_WRITTEN_BASELINE` 184 → **175**。
   ⛔ 我刻意**沒有先改**（產物今天還是 184，先改會讓它對著舊產物喊「變多了」）；推導寫進了那個檔的檔頭。
3. ⭐ **forms 基準線要重 dump**（`packages/shared/src/content/abilityCodeParityForms.baseline.json`，**柵欄外**，⛔ 我沒動）：
   ```
   GGD_FORM_PAIR_DUMP=1 npx vitest run packages/shared/src/content/abilityCodeParityForms.test.ts
   ```
   量到的 diff 只有 **2 組，兩組都是「兩邊都動了」，⭐ 單邊 0**：`11-03`（udre／u01u）· `18-02`（nsjs／n00p）。
   ⚠️ 上一輪報告 §5③ 已經說「收尾再 dump 一次」—— 現在**又多了 #1067／#1090 的中途態**，
   ⇒ ⛔ 不要現在 dump，等所有 lane 收工。
4. `tools/skill-remake/batch1.py` 的來源列（解鎖 §4 那 6 支）：`ewar.q`（→ e007.q）· `emns.r` · `emns.q` ·
   `e00s.q`（→ e010.q）· `e00s.e`（→ e010.e，⚠️ 等 #417）。
5. 想收 `n01g.q` 就跑
   `python3 tools/skill-remake/templatize.py --apply --only godie-n01g.q --allow-parity-fix`
   ＋ 從 `abilityCodeParity.baseline/42.json` 拿掉 `42-01|effects`（單邊 0 → 1，理由在 §4）。

## 7. 順手發現、⛔ 沒動的

- ⭐ **模板堆疊出貨了但 0 個採用者**（§5）—— 這是「我查的那條路上沒有 ≠ 它不存在」的第 N 次；
  上一輪報告的「另一台機器」四個字讓它看起來像不存在的東西。⛔ 沒開票，主 session 決定。
- `godie-n00p.w`／`nsjs.w` 的 dot 是 `amountPerTick: {"flat": 60}` —— **一個沒有級別的烘進去的數**（第〇·四守則）。
  ⚠️ 這一輪把它原封搬進 `template.params`（正規化器的定義就是逐位元不動），
  ⇒ ⭐ 它現在**看起來像一個模板參數**，而它仍然是一個沒有出處的字面值。上一輪報告已記，⛔ 仍未開票
  （要先量「出貨 dot 有幾支這樣」才有分母）。
- `packages/shared` 與 `apps/client` 的 tsc 紅（§3）不是我的，但**它們今天擋著 `pnpm typecheck`** ——
  主 session 收尾前要確認那兩條 lane 收乾淨。

## 8. 動到的檔案（逐檔列名）

**程式／schema／測試（5）**
`packages/shared/src/content/schema/template.ts`（`zParamType` +2）·
`packages/shared/src/content/templates/paramsSchema.ts`（+2 case、+2 import）·
`packages/shared/src/content/templates/expand.ts`（`effectNode()` 通則 ＋ projectile-strike 兩格 ＋ 2 import）·
`packages/shared/src/content/templates/paramsSchema.test.ts`（`probesFor` +2 型）·
`packages/shared/src/ops/handWrittenAbilitiesRatchet.test.ts`（**只有檔頭註解**，常數仍是 184）

**工具（2）**
`tools/skill-remake/templatize.py`（`_has_node_slot()` ＋ `m_projectile_strike` 的酬載次序）·
`tools/skill-remake/templatize-ledger.json`（74 → **83**）

**內容（10）**
`content/ability-templates/tpl-projectile-strike.json`（+2 params · requires +`periodicDamage` · description）·
`content/abilities/`：`godie-u01u.e` `godie-udre.e` `godie-n00p.w` `godie-nsjs.w` `sela.e` `sela.q` `sela.r` `thorne.e` `thorne.r`（各自 `effects:[]` ＋ `template`）

⛔ **沒碰**：`abilityCodeParityForms.baseline.json` · `abilityCodeParity.baseline/*` · `tools/skill-remake/batch1.py` ·
`content/champions/**` · `godie-h020.e`／`hjai.e`（#1092）· 變身家族那 24 支／`tpl-transform.json`（#1067）·
`godie-h01n.*`／`h01o.*`（#1090）· 任何 `*:build`／`skills:sync` · git。
