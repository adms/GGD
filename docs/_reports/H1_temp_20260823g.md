# H1 — tune.sh 收技能/道具 JSON（T0.5）· 2026-08-23g

> owner 逐字：「**改暈眩只要改共享的 JSON 就好，哪裡來那麼多毛病**，又不是增加新技能效果或標籤」

## 做了什麼

`scripts/tune.sh` 加第二階 **T0.5**：
`content/{abilities,items,champions,vfx,models,ability-templates}/*.json` 放行，閘子集升一級：

| 閘 | 何時跑 | 實測 |
|---|---|---:|
| content:build（嚴格 Zod） | 一律 | 10s |
| skillnorm:check + prose:check | 一律 | 3s |
| abilityMirror + abilityCodeParityForms（單檔 vitest，一次啟動） | T0.5 一律 | 4s |
| anchors:check + echoloop:check | 只在 diff 動到 `damageTier`/`cooldownTier` 欄位（未追蹤新檔用 grep 補）；純 T0（config）維持四支全跑 | 條件式 |

⛔ 不跑 typecheck／七包 vitest。⛔ 三道 fail-closed 原封不動 —— 只裁「跑不跑」，判準一個字沒動。

## 量到的（隔離沙箱：APFS clonefile 副本 + 自己的 git，⛔ 沒碰共用樹的 content:build 全域鎖）

模擬「改一格暈眩秒數」（godie-e008 赤焰爆發 1.0→1.2 秒，JSON + 卡面文字）端到端：

- **穩態單輪 ≈ 17s**（10+3+4）；含一次真實的「來回改錯」**兩輪 ≈ 32–80s**（63s 那一輪是別的 lane 併行吃 CPU 的離群值）
- ⇒ **≤60s 目標：穩態達標**；重載下兩輪約 80s，仍遠低於 3 分鐘北極星
- ⭐ 那次「改錯」是**真的**：技能是鏡像儲存（standalone + champions 內嵌，STRICT 模型），只改一邊 ⇒ abilityMirror 15s 就紅、訊息逐字指出兩邊的值 —— 這正是 T0.5 不可省這支閘的證據
- 順手抓到既有 T0 的一個洞：`content/editor-target-profile.json` 是 buildIndexes 產物但不在白名單 ⇒ 第二輪必被誤判 OUTSIDE。已補進白名單與 commit pathspec
- case 分支順序修正：產物（`_index.json` 等）要排在集合 glob 前面，否則被誤計成 T0.5 路徑

## 守衛

`tuneScript.test.ts` 7 條全綠；突變驗證：白名單拿掉 `content/ability-templates` → 紅，訊息指名該集合 → 還原。

## 沙箱殘留

`/private/tmp/claude-503/.../scratchpad/t05sim`（已可刪）；log：`/private/tmp/t05sim-run*.log`。
