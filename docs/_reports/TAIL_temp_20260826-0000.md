# TAIL lane 完整報告 — GH#688 Phase 6 第六批（長尾）：五族一次收（2026-08-26 00:00）

> 任務：建議表 §五**剩下的家族**，按支數序再吃三到五族。⛔ 路徑柵欄禁止動
> `tools/w3x-import/**` ⇒ **本批一具新模型都沒轉**：先用 `content/models/` ＋
> `content/assets/models/imported/` 對照 census，挑出**模型已經在 repo** 而**還沒有任何
> `spawnModelFx` 綁定**的家族，按 `sites` 排序取前五名 —— 那正好就是
> **MidchilderNanohaAura(5) · BlackHole1(4) · NetherStrike(3) · AquaSpikeVersion2(3) · GrandOrcAura(2)**。
>
> ⭐ 核心發現①：**10 列裡只有 7 列有出貨落點**（QUAD 的課①第五次應驗）。剩下三類
> 無落點各有各的形狀：未出貨原主（O01Z/O02V 奈葉 · Ekee）· **JASS 柵欄**（h00X 兩個
> 生成點都在 `GetUnitTypeId=='E00Q'` 黑化Saber 分支裡）· ⭐ **「有主人但那個主人不是 GGD 的」**
> —— A0JZ「14-04 AKT戰隊」在**物件編輯器裡根本沒有主人**（只被 AI 學習表 `SelectHeroSkill`
> 叫過），而 Etyr 出貨的 14-04 是 **A0SS 聖夜降臨**（零 JASS 的另一個設計）。
> ⭐ 核心發現②：**A/B 差分尺當場抓到兩個「說了但不會發生」的節點**，其中一個是
> **全新的失敗形態**：census 的 `tint [1,0,0]` 乘上一張**純青藍**貼圖 ≈ 全黑（實測 3 px；
> 拿掉紅 tint 同一幀 621 px，**200 倍**）。⛔ 所有既有的閘對它都是綠的。
> ⭐ 核心發現③：`imported.blackhole1` / `imported.grandorcaura` 的 `doc.scale` 是**沒有人
> 驗過的佔位值**（兩份零消費端）—— 照 census scale 綁下去會得到 **91 / 87 世界單位**寬的演出，
> 而競技場只有 24×18。

---

## 一、10 列逐列驗證（交付 1 · 對照表）

對照鏈：`docs/蝗蟲群對應表.md` §五 → `tools/locust-census/census.json`（sites/params）→
`raw/war3map.j` 的 `Trig_*_Conditions`（`GetSpellAbilityId()`）→ `OBJECTS.json.abilities[<code>].name`
（編號就在名字裡）→ `OBJECTS.json.heroes/units` 的擁有者 → `content/champions/` 名冊。

### AquaSpikeVersion2 —— 3 列 / 3 落點（本族全中）

| rawcode | 生成點 | 原作技能（w3x rawcode → 名字 → 擁有者） | GGD 落點 | 判定 |
|---|---|---|---|---|
| h032 龍氣2 GodEye | FinalShotting j:34036 | **A0S6「02-002 神通眼」← Hvwd 除魔巫女** | **godie-hvwd.ex** | ✅ 新綁。`SetUnitScalePercent(800)` ⇒ scale **8**；`PolarProjectionBJ(…,160,facing)` ⇒ distance **1.6**；`UnitApplyTimedLifeBJ(1.00)` ⇒ lifeSec **1**。census tint null ⇒ 那一格不寫 |
| h032（第二個生成點） | HudGhosts j:55399 | **A0Z6「02-04 百鬼夜行」← Hvwd**（GGD 改名「死魂蟲」，編號相同） | **godie-hvwd.r** | ✅ 新綁。`SetUnitScalePercent(300)` ⇒ scale **3**；`TimedLife 6.00` ⇒ lifeSec **6**。⭐ **獨立佐證**：這一族的 **emitter 半**（`godie-aquaspikeversion2-p0`）早就綁在 hvwd.r 上 —— 本批補的是 **geoset 半**（PENTA 的 ThunderClapCaster 同款分工，⛔ 不算疊加） |
| o02Y 涅吉風花武裝解除 | WindFlowerStart j:34816 | **A0ZU「15-002 風花-武裝解除」← Emfr 魔法老師**（GGD 改名「敵彈吸收陣。太陰道」） | **godie-emfr.ex** | ✅ 新綁（改**來源** `heroes/godie-emfr.py`）。w3u `usca 2` ⇒ scale **2**；`RemoveUnitSP(u,3,1)` ⇒ lifeSec **3**。⭐ census tint **刻意不搬**（§四②）；distance **1.0** 是我挑的 |

### BlackHole1 —— 2 列 / 1 落點

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| o01N 重力之球 | newlzfsmove j:39088（由 newlzfs 開啟） | **A0MV「34-002 冥道殘月破」← Osam 犬妖** | **godie-osam.ex** | ✅ 新綁。w3u `usca 2.5` ⇒ scale **2.5**；頂點色 100,0,0 ⇒ tint **[0.3922,0,0]**；`sleep 6` → 清群 → `sleep 1` → `KillUnit` ⇒ lifeSec **7**（⛔ 不是 `RemoveUnitSP(…,12,1)` 那個保險絲） |
| o01N（第二個生成點） | GravityBall j:24644 | **A0YJ「黑色魔書-重力」← 道具 I030 黑色魔書** | ⛔ 無 | GGD 有 `content/items/godie-i030.json`，但它**只有 description ＋ 合成表，零效果節點** ⇒ 綁上去＝在一個永遠不會被觸發的地方擺演出（第一·五守則）。守衛③④釘住「它長出 effects 的那一天要重看」 |
| o01N（第三個生成點） | AKT 3 j:30944 | **A0JZ「14-04 AKT戰隊」← ⛔ 沒有主人** | ⛔ 無 | ⭐ 見下面的 AKT 裁定 |
| osp1 教室 | quiz j:53528 | **A0WK「93-00 小考」← Ekee 會叫的野獸** | ⛔ 無 | Ekee 在 `content/_legacy/champions/` |

### MidchilderNanohaAura —— 3 列 / 1 落點

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| h02D 騎英魔法陣 | Initate Crazy j:25410（`GetUnitTypeId=='Hvsh'` 分支內） | A0RQ「48-04 騎英之疆繩」／A0AP「24-03」← **Hvsh 梅杜莎 Rider** | **godie-hvsh.r 第三個節點** | ✅ 新綁。⭐ 那個 if 區塊**一次生四具**：h02D(魔法陣) · h015(AWING) · h02H(ShockwaveMissile) · h02I(Tome) —— QUAD 綁了 h02I，本批補 h02D ⇒ **同一個演出的另一半，⛔ 不是疊加**。w3u `usca 1.5` · 頂點色 255,100,100 · `PolarProjectionBJ(…,100,facing)` ⇒ distance **1.0** · `TimedLife 3.00` |
| h01Y 奈葉魔法陣 | DivineBusterEx j:36044 · StarlightBreakerPlus j:36145 | A0XN「81-03」／**A0XO「81-04 Starlight Breaker Plus」← O01Z 魔砲少女 · O02V 白色惡魔** | ⛔ 無 | 兩位都在 `content/_legacy/`（QUAD 對 h01V 已同結論） |
| h01Z 奈葉魔法陣（放大） | 同上兩點 | 同上 | ⛔ 無 | 同上 |

### NetherStrike —— 2 列 / 1 落點

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| h030 佐助萬花筒效果 | ImbaEye j:42213 | **A102「45-002 天照」← Edem 寫輪眼復仇者** | **godie-edem.ex** | ✅ 新綁（改**來源** `heroes/godie-edem.py`）。w3u `usca 4`（⛔ 這一支沒有 `SetUnitScalePercent`）· `RemoveUnitSP(u,2,1)` ⇒ lifeSec **2** · census tint 是白 ⇒ 不寫。⚠️ 同一個 Actions 的第二具是 `hfoo` ＋ **`ShowUnitHide`** ⇒ 原作刻意隱形的 soulburn 代理（oshm 同款裁定），⛔ 不綁 |
| h00X 勝利劍 黑化 | Excalibur j:32322 · Open Skill of DarkSaber j:32691 | A0D5「20-03 約束與勝利之劍」的 **黑化分支** | ⛔ 無 | ⭐ **不是「未出貨英雄」而是「JASS 柵欄」**：j:32321 的 `Trig_Excalibur_Func019C` 逐字是 `GetUnitTypeId(GetTriggerUnit()) == 'E00Q'` —— 一般 Saber（e002/e00l，已出貨）走的是 **else 的 h00S**（QUAD 已綁）。E00Q 黑化Saber 在 `_legacy` ⇒ 這一具在出貨樹裡**永遠不會生成**。守衛③直接掃那個 if |

### GrandOrcAura —— 1 列 / 1 落點

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| o015 夏娜封絕 | CloseDest j:33037 | **A0HB「21-04 討滅封絕」← E008 火霧戰士（夏娜）** | **godie-e008.r** | ✅ 新綁。頂點色 100,100,100 ⇒ tint **[0.3922,0.3922,0.3922]**；`TriggerSleepAction(lvl*4+2)` ⇒ rank1 **6 秒**（正好對上 GGD 卡面「結界持續6秒」與模板 `duration: 6`）。⭐ scale **3.1** ＝ `CloseDestEffect` j:33122 `SetUnitScalePercent(count*5+10)` 跑滿 60 tick 的**終值 310%**，⛔ 不是 w3u 的 0.1 |
| o015（第二個生成點） | AKT 1 j:30807 | A0JZ「14-04 AKT戰隊」 | ⛔ 無 | 同 AKT 裁定 |

### ⭐ AKT 裁定（o01N @ AKT 3 ＋ o015 @ AKT 1，兩列共用）

`Trig_AKT_start_Conditions` 是 `GetSpellAbilityId() == 'A0JZ'`，而 `OBJECTS.json.abilities.A0JZ`
的名字是「**14-04 AKT戰隊**」—— ⭐ 但它在 `heroes`／`units`／`items` 的能力表裡**一個主人都沒有**，
全 `war3map.j` 只有 5 次引用，其中唯一的授予是 AI 學習表 j:17225 的 `SelectHeroSkill(…, 'A0JZ')`。
Etyr（治癒系公主 木乃香）**物件編輯器裡的 14-04 是 `A0SS`「聖夜降臨」**（base `Arai`，
**零 JASS 引用**，純物件編輯器技能），而 GGD 出貨的 `godie-etyr.r` 逐字就是「14-04 聖夜降臨」。
⇒ 這是 QUAD 的 A0JE 形狀**反過來**：一支有觸發器沒主人的技能 vs 一支有主人沒觸發器的技能，
而 **GGD 採用的是後者**。⛔ 把 AKT戰隊 的演出接到 `godie-etyr.r` 上＝發明一個 owner 沒有設計的技能。
守衛③釘住兩個方向（A0JZ 有了主人 → 紅；etyr.r 改名成 AKT戰隊 → 紅）。

---

## 二、模型（交付 2）—— ⛔ 零新轉檔

| 模型 | 動作 | 量測（本 lane 直接讀 .glb 位元組） |
|---|---|---|
| `imported.aquaspikeversion2` | ⭐ **新寫 model doc**（glb 2026-07-22 就在 repo，只是**從來沒有 model@1 文件** ⇒ 不在 `content/models/_index.json` 上，任何人都引用不到） | 1/1 prim 出生可見 · 120 vert · 1 貼圖＋TEXCOORD_0 · alphaMode BLEND · 高 16.6275 ⇒ **doc scale 0.0601**（＝1/高，QUAD/PENTA 的既有慣例：節點 scale 直接讀成「幾世界單位高」）· clip 只有 `Birth`（一次性噴發 ⇒ clipMap 六格全指 Birth，同 `w3x.stock.tomeofretrainingcaster`） |
| `imported.blackhole1` | ⭐ **doc scale 5.1051 → 0.1406** | 3 prim（1 個原作就是軟刪除 `baseColorFactor[3]=0`）· 扁盤 7.111 寬 × 0.556 高 ⇒ 用**最大邊**正規化（1/7.111）。⚠️ 舊值 × census scale 2.5 ＝ **90.7 世界單位寬**（競技場 24×18）。**零消費端**（`grep -rl` 只有 `_index` 與兩份 model-budget 報告）⇒ 改它是逐位元 no-op |
| `imported.grandorcaura` | ⭐ **doc scale 1.0 → 0.0355** | 4 prim（2 個原作軟刪除）· 扁環 28.19 寬 × 3.00 高 ⇒ 1/28.189。⚠️ 舊值 × 3.1 ＝ **87.4 世界單位**。同樣**零消費端** |
| `imported.midchildernanohaaura` | ⛔ **不動**（有出貨消費端 `content/vfx/attach.ex.midchilder-aura.json` → godie-h020.ex / godie-hjai.ex） | 2 prim 全可見帶貼圖 · 高 **0**（純地面貼花）⇒ 1/高 不適用；既有 doc scale 1.0 讓它 6.94 寬，而那個尺寸**已經在線上**了 |
| `imported.netherstrike` | ⛔ **不動**（是 `tpl-beam-roll` 的 `modelKey` 預設，59-04／08-03 兩支經典在讀） | 5/5 prim 全可見帶貼圖＋UV。⭐ 順帶覆核：`modelFxStagingContract` ⑥ 的 `ZERO_PIXEL_FX_MODELS` **已經是空集合**（2026-08-24 重烘落地）—— 讀 .glb 確認 5 個 prim 都沒有 `baseColorFactor[3]=0`，那條豁免確實過期了 |

---

## 三、綁定節點與 soundKey 出處（交付 2；⛔ 零行引擎程式）

七個節點形狀一致：`{"kind":"spawnModelFx","shape":"single","preset":"tpl-locust-orb","modelKey":…,"clip":"idle","soundKey":…,…}`

| 落點 | scale | tint | lifeSec | distance | soundKey ＋ 出處 |
|---|--:|---|--:|--:|---|
| godie-hvwd.ex（02-002） | 8 | — | 1 | 1.6 | `wc3.nagabuildingcancel` ⭐**推導**：`VFX_SOUND_JOIN.modelBoundSoundsets.byModel.aquaspikeversion2` ＝ `SNDXDNBL → NagaBuildingCancel.wav`（模型自己烘在動畫軌上的音效事件） |
| godie-hvwd.r（02-04）＋champion 鏡射 | 3 | — | 6 | **0.8**⭐ | 同上 |
| godie-emfr.ex（15-002） | 2 | ⛔**刻意留白** | 3 | **1.0**⭐ | 同上 |
| godie-osam.ex（34-002） | 2.5 | [0.3922,0,0] | 7 | — | `wc3.darksummoninglaunch1` ⭐**我挑的**（模型無 SNDX 事件 · 生成點無 `PlaySound` · base `ANcl`/`AOeq` 的家族聲不在 `audio-map.json` 裡）。語意：34-002「把範圍內不分敵我全部送往冥界」＝暗黑召喚。⛔ 這是本批唯一一個引用不到出處的聲音 |
| godie-edem.ex（45-002） | 4 | — | 2 | — | `wc3.flamestrikebirth1` ⭐**推導**：`byModel.netherstrike` ＝ `SNDXAHFS → FlameStrikeBirth1.wav` |
| godie-hvsh.r（48-04）＋champion 鏡射 | 1.5 | [1,0.3922,0.3922] | 3 | 1.0 | `wc3.thunderclapcaster` —— ⭐ 與**同一個觸發器**既有的兩個節點（QUAD 綁的 h025/h02I）**同一個鍵**，⛔ 不是新的聲音 |
| godie-e008.r（21-04）＋champion 鏡射 | 3.1 | [0.3922,0.3922,0.3922] | 6 | — | `wc3.flaretarget1` ⭐**JASS 逐字**：j:33035 `PlaySoundOnUnitBJ( gg_snd_FlareTarget1, 100.00, GetTriggerUnit() )` |

⚠️ **為什麼每一格都一定要有聲音**：`tpl-locust-orb` ⛔ 不在
`modelFxStagingContract.test.ts` 的 `SOUNDLESS_TEMPLATES` 上（它的既有採用者每一支都自己帶著
聲音鍵）⇒ 少一格 ④ 就紅。⭐ 所以「不填」不是選項，⛔ 而「隨便填」違反 ⑤ 檔頭的規矩 ——
本批七格裡 **6 格引用得到出處**，第 7 格（osam.ex）明說是我挑的並登記了 rollback。

---

## 四、視覺證明（交付 3）✅ `docs/_reports/tail_visual-proof_20260826-0000/`（frames.md ＋ 14 PNG）

`calibrate()` 自證 **462,400**（全亮 quad）。近黑家族一律走 **A/B 像素差分尺**
（rig `setEnabled` on/off 兩幀逐像素比對，任一通道差 >8 記一點），每一列附 control
（同一幀關掉 `caster` 方塊的差分）。⭐ 兩具**會動**的模型取 10–26 幀序列的**峰值**。

| 落點 | 差分 | control | 幾何實測 |
|---|--:|--:|---|
| godie-hvwd.ex | **9,152** | 4,691 | 高 8.00 · 寬 1.10 · 圓心在施法者前方 1.6 |
| godie-hvwd.r | **2,122** | 4,933 | 高 3.00 · **200 tick 後場上 rig = 0**（lifeSec 6 回收乾淨） |
| godie-osam.ex | **1,171** | 6,859 | 2.50 寬的暗紅平盤（抬高 0.25/1.0 再量 ⇒ 1,024/893，**不是 z-fighting**） |
| godie-edem.ex | **33,619** | 5,683 | 5.97×5.96 的黑焰球，14 幀單調成長 |
| godie-hvsh.r | **98,545** | 78,235 | ⭐ `spawns=3`（兩具 tome ＋ 魔法陣）· 魔法陣 10.42 寬 · 圓心前方 1.0 |
| godie-e008.r | **6,175** | 4,585 | 3.10 寬的紅色結界環 |
| godie-emfr.ex | **534** | 4,792 | 水刺立在施法者旁 1.0 |

### ⛔ 差分尺當場抓到的兩個真缺陷（都已修；⛔ 沒有它會全綠出貨）

**① 02-04（hvwd.r）：整支被施法者方塊遮掉。** 原作把它擺在施法者自己的位置，而
`tpl-locust-orb` 的 `distance` 預設 0.1 ⇒ 引擎放進體內。逐格掃：0 → **120 px** ·
0.5 → 1,171 · 0.8 → 1,197 · 1.2 → 1,193（0.5 之後飽和）。施法者 bbox 半寬實測 0.42、
水刺半寬 0.21 ⇒ 幾何臨界 0.63。⇒ `distance: 0.8`（我挑的，臨界點 + 餘裕）。

**② 15-002（emfr.ex）：⭐ 兩個缺陷疊起來 = 0 像素。**
- **位置**：bbox（x −40.04…−39.76 · y 0.05…2.05）**100% 落在**施法者 bbox
  （x −40.42…−39.58 · y 0…1.85）裡 ⇒ 26 幀差分**全是 0**（PENTA 的 zombiex.ex 同款）。
- ⭐⭐ **顏色（全新的失敗形態）**：census `tint [1,0,0]` × `aquaspikeversion2.glb`
  **唯一一張純青藍貼圖** ≈ 全黑。移出體外仍只有 **3 px**；同一幀把 `albedoColor` 改回白色
  立刻 **621 px**（200 倍）。⇒ 填了那一格＝擺一個玩家看不見的節點（第一·五守則）。
  同族另外兩支的 census tint 本來就是 null，而它們量到 9,152／2,122。
⇒ 修法：`distance: 1.0`（原作本來就擺在 `GetSpellTargetLoc()`＝**目標點**）＋ **不寫 tint**。
修完 0 → **534 px**。

⚠️ **量尺自己的坑（本批新踩到的）**：`engine.runRenderLoop` 讓 Babylon 動畫**照真實時間跑**，
而 sim tick 只在 `step()` 前進 ⇒ 「載入後 sleep 2 秒再量」會量到 Birth clip 的**尾巴**
（aquaspikeversion2 第一次量到 0 就是這樣）。⇒ 這一族**必須取序列峰值**，⛔ 不是單幀。

---

## 五、守衛（`packages/shared/src/content/locustTailFamilies.test.ts`，@visual-proof · 3 條）

① 五具 .glb 靜態可見（≥1 出生可見 ＋ 帶貼圖 ＋ 有 TEXCOORD_0 的 primitive）
  ⭐ **外加 doc.scale 退化閘**：從 glb 的 POSITION accessor min/max 反推
  `bbox × doc.scale × 該族最大節點 scale`，**寬過半個競技場（12 世界單位）就紅** ——
  這一條是核心發現③的閘（⛔ 不抄字面值 5.1051/1.0，從幾何推導）。
② 七個落點擺得出節點（standalone ＋ champion 鏡射），`scale/tint/lifeSec/distance` **逐格**
  釘 JASS/w3u 證據 ＋ **每一格都要有 soundKey**（④ 的前置條件），`validateDoc` 過出貨 schema。
③ 無落點理由保持成立：四位 legacy 原主仍未出貨 · **Excalibur 的 h00X 分支仍由 `'E00Q'` 把關**
  （正則直接讀 `Trig_Excalibur_Func019C`）· **A0JZ 仍然沒有主人** 且 `godie-etyr.r` 仍叫「聖夜降臨」·
  **A0YJ 仍是道具能力** 且 `godie-i030` 仍無 effects · ⭐ 母體自證：六個 rawcode 的
  `CreateNUnitsAtLoc` 在 war3map.j 裡**找不到就紅**（安靜跳過與全過長得一樣）。

- **vitest 3 次**（上限）：①首跑（3/3 綠）②突變 ③20 檔終批（**73 tests 全綠**）
- **突變（一批一條，承重線）**：把 `godie-e008.r` 的 `modelKey` 從 `imported.grandorcaura`
  改成 `imported.crescent` → 紅，訊息逐字指名 `o015 → content/abilities/godie-e008.r.json (standalone)`
  → 用備份**逐位元組**還原（⛔ 非 `git checkout`）
- `pnpm typecheck` **1 次**：EXIT=0（全 repo）
- 終批含 `modelFxStagingContract`（④ 聲音／⑤ 身分／⑥ 零像素）· `abilityMirror` ·
  `abilityCodeParity` · `shippedBundleIsCurrent` · `modelScale` · `modelBbox` ·
  `locustQuad/Penta/Tornado/Crescent` · `vfxDocsBirthVisibility` —— **全綠**
- `bash scripts/visual-proof.sh` EXIT=0（本批**沒有碰** `apps/client/src/{vfx,render}/`）

---

## 六、#669 批核頁登記（交付 4）✅

`node tools/review/register.mjs --id tail_visual-proof_20260826-0000`，
abilities ＝七支全列。rollback＝`tpl-locust-orb` → `params.alpha.default` **1（live）⇒ 0（veto）**
（orb 家族開關，與 TORNADO/QUAD/PENTA 三批共用同一格 —— 家族級共用是前三批已接受的形狀）。
`genrun.sh review:build` 重生成 `queue.json`；`node tools/review/check.mjs` **exit 0**（**16 批全登記**）。
⚠️ 登記器只收 `.png`（`scanSequences` 逐字 `endsWith(".png")`）⇒ 證據幀用 `sips` 轉成 PNG 存檔。

---

## 七、逐檔清單（工作樹，⛔ 本 lane 零 git 寫入）

**改（來源／程式）**
- `tools/skill-remake/heroes/godie-edem.py` — 45-002 加 `model_fx=`（h030 黑焰，含證據註解）
- `tools/skill-remake/heroes/godie-emfr.py` — 15-002 加 `model_fx=`（o02Y 水刺，含**兩個實測**的註解）

**改（出貨內容，經 `product-quarantine.sh unlock/lock --step tiers:apply` 正規解鎖，改完已重鎖 402 份）**
- `content/abilities/godie-hvwd.ex.json`（standalone-only，EX）
- `content/abilities/godie-hvwd.r.json` ＋ `content/champions/godie-hvwd.json`（R 鏡射）
- `content/abilities/godie-osam.ex.json`（standalone-only）
- `content/abilities/godie-hvsh.r.json` ＋ `content/champions/godie-hvsh.json`（R 鏡射，第三個節點）
- `content/abilities/godie-e008.r.json` ＋ `content/champions/godie-e008.json`（R 鏡射）
- `content/models/imported.blackhole1.json`（scale 5.1051 → 0.1406）
- `content/models/imported.grandorcaura.json`（scale 1.0 → 0.0355）

**改（`genrun.sh skillremake:json` 的產物 ＋ 它自己連帶的 content:build）**
- `content/abilities/godie-edem.ex.json` · `content/abilities/godie-emfr.ex.json`
- `content/abilities/_index.json` · `content/champions/_index.json` · `content/models/_index.json`
  · `content/vfx/_index.json` · `content/bundle.json` · `content/manifest.json`
  · `content/editor-target-profile.json`
- `docs/_review/feature-verdicts.json`（register）· `docs/_review/queue.json`（genrun review:build）

**新**
- `content/models/imported.aquaspikeversion2.json`
- `packages/shared/src/content/locustTailFamilies.test.ts`
- `docs/_reports/tail_visual-proof_20260826-0000/`（frames.md ＋ 14 PNG）· 本報告

---

## 八、主 session 待收清單 ⚠️

1. ⛔⛔ **`git add content/models/imported.aquaspikeversion2.json`（新來源檔）** ——
   `skillremake:json` 內部串了 `pnpm content:build`，它已經把這份文件**烘進 `bundle.json`**，
   而 `shippedBundleHasTrackedSources` 正在紅：出貨走 `git pull`，未追蹤的來源 + 已 commit 的
   bundle ＝ **2026-08-02 生產故障的形狀**。⚠️ 同一條紅裡還有**另一條 lane 的四份**
   `content/vfx/fx.w3x.stock.markofchaostarget.p0[0-3].json` —— 兩邊都要 `git add`。
2. ⚠️ **`content/vfx/_index.json` 的 25 行新增不是我的** —— 我跑 `genrun.sh skillremake:json`
   時它連帶的 `content:build` 掃到了另一條 lane 那四份未追蹤的 vfx 文件。⛔ 不要回捲，
   它與那四份文件是一組（`git add` 之後就自洽）。
3. **`pnpm vfxbind:check` 現在是紅的，⛔ 不是本批造成的** —— 訊息裡 MISSING/DEAD/EXTRA
   一列都沒有本批的七支技能；`content/config/ability-vfx-bindings.json` 過期的原因是
   「未綁 `fx.w3x.*` emitter 文件」的計數變了（＝上面那四份）。⇒ 由**另一條 lane** 或
   主 session 跑 `python3 tools/vfx-bind/scan.py` 收。
4. **`pnpm speedlists:check` 也是紅的，⛔ 與內容無關** —— `ERR_MODULE_NOT_FOUND:
   packages/shared/src/content/cache/index`（那個目錄裡只有 `contentCache.ts`，沒有 `index`）。
   純 import 路徑問題，本批一個字都沒動它。
5. **`pnpm skills:sync` 收尾**：本批已經自己跑過 `skillremake:json`（含 deriveCastTimes 與
   content:build）與 `review:build`，⇒ 預期只剩 `tiers:apply` / `apconv:build` / `caps:export` /
   `spec:build` 的 no-op（本批零 tier 欄位、零新機制）。
   ⚠️ **一個要留意的連帶**：第一次 `skillremake:json` 之後 `godie-emfr.json` 的 Q/W/E/R
   掉了 `castTimeSec`（產生器的 A-6 RETIRED 與 deriveCastTimes 的競態）—— 我**當場單獨
   重跑一次 `deriveCastTimes --write` 補回來了**，現在全 72 位英雄鏡射 0 筆漂移（已驗）。
6. `scripts/visual-proof.sh` 現在 EXIT=0（沒碰畫面層）；`git add` 之後仍然是 0。

---

## 九、owner 裁決項（⛔ 不擋出貨 —— #669 頁預設 LIVE，一鍵可否決）

1. ⭐ **02-002 神通眼的水刺高 8.0 世界單位**（≈ 英雄的 4 倍，會頂出畫面上緣）。
   那是 JASS `SetUnitScalePercent(800)` 逐字，⛔ 不是我挑的 —— 但原作的英雄是 100 wc3u
   而我們的是 1.86，比例感受不同。要收斂就是節點那一格數字。
2. ⭐ **48-04 的魔法陣 10.4 世界單位寬**（競技場 24×18 的 43%）。census w3u `usca 1.5`
   × `imported.midchildernanohaaura` 既有的 `doc.scale 1.0`，而那份文件**已經有出貨消費端**
   （EX 光環 6.94 寬，線上跑很久了）⇒ 我**不重新正規化**它。要縮就改節點 scale。
3. ⭐ **45-002 的黑焰球浮在頭上 4.32 單位** —— `netherstrike` 的 `model@1.fxSpawnHeight 1.08`
   × scale 4。那一格是 `tpl-beam-roll`（59-04／08-03 兩支經典）在讀的 ⇒ ⛔ 本批不碰。
   要貼地得把 `fxSpawnHeight` 從**模型**搬到**節點**（機制票）。
4. ⭐ **osam.ex 的 `wc3.darksummoninglaunch1` 是我挑的**（本批唯一引用不到出處的一格）。
   模型無 SNDX 事件、生成點無 `PlaySound`、base 家族聲不在 audio-map ⇒ 而
   `modelFxStagingContract` ④ 又要求 orb 節點必須有聲音。要換一格聲音是一個字串。
5. ⭐ **15-002 的 census 紅 tint 被我拿掉了**（實測 200 倍差距，§四②）。如果 owner 要
   「紅色的水刺」，正解是**重烘一張有紅通道的貼圖**或讓 tint 走加法，⛔ 不是把那一格填回去。
6. ⭐ **兩份 model doc 的 scale 被我重新正規化**（blackhole1 / grandorcaura，零消費端）。
   原值會得到 91／87 世界單位。守衛①從 glb 幾何反推，改回去就會紅。
7. **15-002 的設計已經換過**（w3x「風花-武裝解除」→ GGD「敵彈吸收陣。太陰道」）。
   我按**編號是 join key** 綁了原作那具 dummy；如果 owner 認為新設計不該用舊演出，
   刪掉 `godie-emfr.py` 的 `model_fx=` 那五行即可。
8. **h00X（勝利劍黑化）刻意不綁**：PENTA 對 E00Q 走的是「綁同身分的出貨原創英雄」
   （zombiex），⭐ 但這一批**沒有這個出口** —— 出貨名冊裡的 e002/e00l 都是「亞瑟王 - Saber」
   本體，⛔ 沒有黑化/Alter 身分。E00Q 出貨那天守衛③會紅。
