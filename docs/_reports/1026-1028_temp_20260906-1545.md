# #1026 ＋ #1028 —— 帳本寫入端的兩張票（lane 報告，2026-09-06 15:45）

> 前一輪（04:30）：#1026 ①（`ruling.sh`）與 ③（commit 閘）已落地，剩 `--map`／`message-ledger.sh` 兩個寫入端；
> #1028 A–D 已落地，剩 AC ④（隔天硬檢查）。⛔ 這一輪沒有重做任何一件已在 `9396c38d1`／`dc94ac030` 的事。

## 做了什麼

| 票 | 剩下的 | 這一輪 | 落點 |
|---|---|---|---|
| **#1026** | `ledger_table.py --map` 與 `message-ledger.sh` 建置寫了帳本卻不重生成 board | ✅ 三個寫入端**共用一份** `regenerate_boards()`（步驟清單＋「什麼時候不跑」只有一個住處）；`--map`／`--dedupe`／建置收工都叫它；`ruling.sh` 的 bash 迴圈改成 `ledger_table.py --regen` | `scripts/ledger_table.py` · `scripts/message-ledger.sh` · `scripts/ruling.sh` |
| **#1028** | AC ④ 明天跑；⭐ 但 A 落地**之後**今天又長出一對重複（12:28／12:29） | ✅ 鑰匙補兩條路（見下）＋併列時 owner 原話贏；AC ④ 今天先唯讀跑一次（見下） | `scripts/ledger_table.py` · `scripts/message-ledger.sh` |

### #1026：`regenerate_boards()` 什麼時候**不跑**（每一條有理由，⛔ 不是「以防萬一」）

| 情況 | 動作 | 為什麼 |
|---|---|---|
| `GGD_LEDGER_NO_REGEN=1` | 跳過，印一行 | 回頭的開關 |
| `GGD_QUARANTINE_UNLOCKED=1`（在 `genrun`／`skills:sync` 鏈裡） | 跳過，印一行說「鏈接手」 | ⭐ 外層 `genrun.sh` 收工會**對帳**「這一支寫了誰的檔」—— 在 `msgledger:build` 裡再跑 board，board 的產物會被算成 msgledger 的**越界寫入**（RC=3）；而 `skills:sync` 鏈上本來就有 `board:build`／`board:roll` |
| 帳本不在 `docs/_daily/` 且沒給 `GGD_GENRUN` | 靜默跳過 | 測試夾具（暫存目錄）—— 守衛用 stub 驗「有沒有叫」，⛔ 不在測試裡跑真的產生器 |
| `--map` 找不到那一列／`--dedupe` 沒併掉任何列 | 不跑 | 沒寫帳本 ⇒ 沒有輸入變了 |
| 建置：帳本與全文存檔**位元組沒變** | 不跑 | 建置器每次都重寫存檔，內容常常相同 |

⚠️ 誠實一格：單獨跑 `bash scripts/genrun.sh msgledger:build` 時 board **不會**在裡面重生成（上表第二列）——
它會印出來要人補跑；漏了的話 ③（commit 閘）會在 commit 那一刻擋下並指名 `genrun.sh board:build`。

### #1028：鑰匙為什麼還要補（A 落地之後量到的）

今天 09-06 帳本在 A 落地後仍長出：

| 對 | 形狀 | 舊鑰匙為什麼對不上 |
|---|---|---|
| `12:28` owner「好吧 先開票 血量倍率4x, M=15 K=1000」／`12:29` 我記「血量倍率4x, M=15 K=1000 （⇒ #1029 …）」 | 我**掉了他開頭三個字、尾巴接了我的註** | `--find-time` 的第一段尾巴掛著「（」⇒ 整段對不上 ⇒ 退回執行時間；`_same_text` 前綴也對不上 |
| `04:03`／`04:07`（A 之前留下的）| 前 45 字相同，之後是我的改述 | 前綴相容失敗 |
| `14:18`「1開票 2開票 3(…) 4(我有給過你優先順序JASS優先不是嗎?)」／同分鐘我記「4(我有給過你…) ⇒ 小傑 Q …」 | 我只引了第 4 項 | 同上 |

補的（都在 `ledger_table.py`，兩個寫入端共用；⛔ 沒退回純文字，時間窗都還在）：

1. `_same_text`：**前 24 字相同**也算（24 ＝ 建置器 `covered()` 的 `WINDOW`，⛔ 不是另一個分母）—— 仍要過 15 分鐘窗。
2. `_contains`：**同一分鐘（容忍 ≤3 分，退回執行時間那條路）** 且一份的第一段（`=>`／`⇒` 之前，≥12 字）逐字出現在另一份裡。
   ⛔ 刻意不給 15 分：`13:00`／`13:09` 那一對（owner 把其中一段再講一次）是兩則。
3. `--find-time` 的段鍵去掉尾端「（」「：」等 —— 那是我註記的開頭，⛔ 不是他說的字。
4. ⭐ 併列時**留哪一份文字**：建置器（`prefer_incoming_text=True`，逐字來自 transcript）贏；其餘照「時間早的 → 包著對方第一段的 → 長的」。
   ⇒ 併完之後 `--check` 的 `covered()` 對得上 owner 原話（在此之前會永遠「漏了 12:28」）。

**乾跑（在暫存目錄的拷貝上，⛔ 沒動真帳本）**：09-03 併 0、09-04 併 2（`10:38`／`10:41` 兩列是舊 `ruling.sh` 的執行時間列，D 那次沒掃到 09-04）、09-05 併 0、09-06 併 3（上表三對）。
每一天併前／併後用存檔當來源跑 `--check --date` ⇒ **漏了 0 → 0**（⭐ 沒有任何一則 owner 訊息因為併列失去它的列）。

## 測試（vitest 2 次；體驗層 ⇒ 薄守衛、⛔ 沒開突變）

| 守衛 | 驗什麼 | 行數 |
|---|---|---|
| `rulingRegeneratesBoards.test.ts`（改） | 三個寫入端真的跑起來、`GGD_GENRUN` stub 記帳：`ruling.sh`／`--map`／建置都叫到兩支；反方向：`--map` 對不到列不叫、`GGD_QUARANTINE_UNLOCKED=1` 不叫 | 72 |
| `rulingScript.test.ts`（改一條） | 夾具改成 12:28 的形狀（掉開頭三字＋接註）：列鍵＝訊息時間、建置後仍一列、留 owner 原話、`--check --date` 綠 | +6 |
| `ledgerTableDedupe.test.ts`（補） | 04:03／04:07 ⇒ 一列留早的；反方向 13:00／13:09 ⇒ 兩列 | +10 |

第 1 次：8 檔 19 條，1 紅（夾具票號 `#1` 不合 `decided()` 的 2–4 位）；第 2 次：**8 檔 19 條全綠**
（含既有 `messageLedgerScript` · `ledgerTableEscapes` · `ledgerWriteWorksOutsideGenrun` · `genInputCommitGate` · `announceLedgerIsWritten`）。
實作 191 / 測試 99 = **0.5×**。

## 允許的那一次真跑

`bash scripts/genrun.sh board:build board:build:raw` ⇒ exit 0，`ggd-board.html` md5 `102ada…` → `c07d95…`
（⭐ 跑之前它**已經**過期 —— 來源在 15:2x 之後被別的 lane 動過）。留底 `docs/legacy/_overwrites/overwrite_temp_20260906-154022/`。
⛔ 沒有對任何票真的跑 `ruling.sh`、沒有真的 `--map`／`--dedupe`。

## 驗收逐條

| AC | 狀態 |
|---|---|
| #1026 `ruling.sh` 之後 board 一起 modified | ✅ ①（`9396c38d1`）；這一輪改走共用的 `--regen`，守衛仍綠 |
| #1026 `ledger_table.py --map` 之後同上 | ✅ 守衛（stub）；真產物那一段由上面那一次 genrun 證明會動 |
| #1026 只 commit 帳本 ⇒ 擋 | ✅ ③（`dc94ac030`）—— 這一輪沒動 |
| #1026 `pnpm skills:check` 綠 | ⏸ lane 禁跑；主 session 收斂時跑 |
| #1028 AC ④ `--check --date 2026-09-06` | ⏳ 今天唯讀跑了一次（15:3x）：**漏了 0**、**⏸ 未對票 58 列**（都是今天下午的討論訊息，要主 session `--map`）；明天硬檢查 |

## 需主 session 動的（⛔ 本 lane 沒做）

1. `python3 scripts/ledger_table.py --dedupe docs/_daily/2026-09-06.md`（併 3 列，含 12:28／12:29）與 `… 2026-09-04.md`（併 2 列）——
   ⭐ 它現在會自己重生成 board。
2. 09-06 那 58 列 `⏸ 未對票` 要 `--map`（每一次 `--map` 也會重生成 board；一晚 18 次 ⇒ 18 次 genrun，各幾秒 —— 嫌慢就 `GGD_LEDGER_NO_REGEN=1` 批次對完再跑一次 `--regen`）。
3. `tools/parallel-gates/sync-io.json` 的 `board:roll` reads 仍漏 `docs/_daily/????-??-??.md`（#1034，柵欄外）。

## SWITCH（都是環境變數 —— 腳本類，⛔ 不進 content/config）

- `GGD_LEDGER_NO_REGEN=1` —— 三個寫入端一律不重生成 board（預設**會**）。
- `GGD_GENRUN=<cmd>` —— 換掉 `bash scripts/genrun.sh`（守衛用）。
- `GGD_RULING_MSGTIME_OFF=1` —— `ruling.sh` 一律用執行時間當列鍵（既有，沒動）。
