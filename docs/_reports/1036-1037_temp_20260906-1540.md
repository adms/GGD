# GH#1036 ＋ GH#1037 lane 報告（2026-09-06 15:40）

> 併行 lane，主工作樹，⛔ 未 commit、⛔ 未跑任何產生器（產物由主 session 統一重生成）。
> 兩張票都動 `owner-knobs.json`，所以放同一條 lane。

---

## 一、做了什麼

### GH#1036 「拿 30 級當標準」已被 30/50/99 三錨點取代 —— 先備註、再從產生器拿掉

| 步 | 檔 | 內容 |
|---|---|---|
| ① 備註（⛔ 先於拿掉） | ⭐ **新檔** `docs/legacy/_superseded-rulings.md` §1 | 2026-08-20 19:03 的原話「÷20 發 = 一發要打多少　我的建議是拿 30 級的當標準就好，因為技能通常還有 AP 加成那塊沒算到」＋ 2026-09-06 11:22／11:26 取代它的兩則，**逐字＋日期＋帳本行號**；外加「什麼變／什麼不變」表與那句話原本住在哪裡的清單 |
| ② 拿掉 | `tools/balance-anchors/gen.ts`（4 處） | (a) `SMALLEST` 上方註解：下限從 hard limit 反推是「一定要滿足 LV30」的定義，⛔ 不代表 LV30 是唯一標準；(b) `damage-tiers.json` note 的「②出貨錨就是 hard limit LV30」→「②級距下限從 hard limit 反推」＋ 新增「⭐ 2026-09-06 錨點標準更正：三個錨點都是標準（LV30 hard limit · LV50 soft limit · LV99 極限）」；(c) 文件「⭐ 佔 LV30 血條／幾發送走 LV30 中位」兩列 → **三個錨點各一列**（既然三個都是標準，三級都要看得到）；(d) 文件末段「⚠️ 出貨錨是 LV30（owner 2026-08-20：…）」→「⭐ 三個錨點都是標準（owner 2026-09-06 逐字）…」＋「⚠️ 級距的下限仍從 hard limit LV30 反推 —— 那是『一定要滿足』的定義」 |

⭐ **程式邏輯一行都沒動**：`SHIPPED_ANCHOR_LEVEL = HARD_ANCHOR_LEVEL` 仍決定極小的下限與天花板 ——
那正是「至少要滿足 30(hard limit)」的實作。改的只有**敘述**：LV30 不再被描述成「唯一標準／出貨錨」。

### GH#1037 基礎生命倍率 `maxHealth` 9 → 4（owner 旋鈕，票裡有原話）

| 住處 | 檔 | 改動 |
|---|---|---|
| 出貨值 | `content/config/combat-env.json` | `multipliers.maxHealth` **9 → 4**（`version` 沒動 —— 08-23 兩次旋鈕改動也沒動它，platform 只回傳不比較） |
| 閘 | `content/config/owner-knobs.json` | `maxHealth` 列**換成**：`value 4`／`quote「好吧 先開票 血量倍率4x, M=15 K=1000」`／`on 2026-09-06`。舊列（9／「生命倍率調整至9」／2026-08-23）**逐字備註**在 `_superseded-rulings.md` §2（schema 是 `.strict()`，⛔ 塞不進第四個欄位） |
| Zod `DEFAULT` | `packages/shared/src/sim/combatEnv.ts` | ⛔ **不用改** —— `buildDefault()`（:715）對每一格 ×factor 一律填 **1**（中性），只有三圍係數有出貨值。9 從來不住這裡 |
| admin `SHIPPED` | `apps/admin/src/combatEnv.ts` | ⛔ **不用改** —— `neutralFor(k)`（:91）同一個設計：×factor 的「重設」目標是 1.0。`apps/admin/src/configForms/specs/combat.ts` 根本沒有 combat-env（它是 GH#626 拆出來的爽度／護盾／傷害規則那一組）|

⇒ ⭐ **票文的「三個住處」在 combat-env 這一份上是假前提**：值只有**一個**住處（json），DEFAULT／SHIPPED 是**中性重設目標**，
守它的閘是 `ownerKnobs.test.ts`（json ↔ 授權表），⛔ 不是 drift-to-DEFAULT。

---

## 二、量到的效果（母體 49 位對戰可選英雄，裸裝，`引擎最終 = 純基礎 × 倍率 + 1200`）

量法：`championStatBase()` 走出貨管線（與 `anchors:build` 同一支），LV70 是額外加量的（不是錨點）。

| 等級 | 純基礎中位 | 引擎最終 ×9（今天） | 極大 2000 佔血條 | 幾發極大 | ⭐ 引擎最終 ×4 | ⭐ 極大佔血條 | ⭐ 幾發極大 | 幾發極小 ×4 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| LV30 | 2,776.2 | 26,185.8 | 7.6% | 13.1 | **12,304.8** | **16.3%** | **6.2** | 61.5 |
| LV50 | 4,333.6 | 40,202.4 | 5.0% | 20.1 | **18,534.4** | **10.8%** | **9.3** | 92.7 |
| LV70 | 5,891.0 | 54,219.0 | 3.7% | 27.1 | **24,764.0** | **8.1%** | **12.4** | 123.8 |
| LV99 | 8,149.2 | 74,542.8 | 2.7% | 37.3 | **33,796.8** | **5.9%** | **16.9** | 169.0 |

- ⭐⭐ **五級距一個都沒變**（票 AC#2）：推導鏈 `(2776.2 + 1200) ÷ 20 = 198.8 → 200` 零倍率 ⇒ 200 / 500 / 1000 / 1500 / 2000。
  ⭐ 用 scratch 副本真的跑了一次產生器（見四），`damage-tiers.json` 五格逐位元相同、只有 note 變。
- 天花板（LV30 引擎最終中位）26,185 → **12,304**，極大 2000 仍在其下（16%）。
- `pnpm echoloop:check` 在 maxHealth=4 之下 ✔：`maxHealth` 生命端動、比值動 ⇒ 真的旋鈕，沒有回音迴圈。
- ⚠️ **票文 Known risk「怪物與殭屍波也吃這個倍率」⛔ 不成立**（走完那條路查的）：
  `mobs.ts:1699` hero-derived 血量 = `championStatBase(…) × heroHpMult + flat`（純基礎空間）；`hpMult`/`maxHp` 路徑是 config 平值；
  ×`maxHealth` 的 env 鏈只住在 `finalizeStat()`（`sim/baseBonus.ts:377`），而 `statPipeline.ts:111` 只讓英雄與召喚物走到那裡。
  ⇒ 殭屍血量不變、殭屍傷害不變，**英雄血條 ÷2.25** ⇒ ⭐ **殭屍對英雄的威脅 ×2.25** —— 這才是上線後要看的第一件事，⛔ 不是「殭屍跟著變薄」。

---

## 三、改了哪些檔（逐檔，⛔ 全部未 commit）

| 檔 | 票 | 狀態 |
|---|---|---|
| `tools/balance-anchors/gen.ts` | #1036 | M（4 處敘述，邏輯不變） |
| `docs/legacy/_superseded-rulings.md` | #1036 #1037 | ⭐ 新檔（兩則取代紀錄） |
| `content/config/combat-env.json` | #1037 | M（maxHealth 9 → 4） |
| `content/config/owner-knobs.json` | #1037 | M（只有 maxHealth 那一列） |
| `docs/_reports/1036-1037_temp_20260906-1540.md` | 兩張 | ⭐ 新檔（本報告） |

⛔ 沒動：`packages/shared/src/sim/combatEnv.ts`、`packages/shared/src/ops/ownerKnobs.test.ts`、`apps/admin/**`（理由見一）。

---

## 四、測試／驗證（vitest 1 次、tsc 0 次）

| 閘 | 結果 |
|---|---|
| `npx vitest run ownerKnobs.test.ts balanceAnchorDocHonest.test.ts knobValueNotRestated.test.ts`（一個指令） | ✅ 3 檔 8 條全綠（授權表 ↔ 出貨值對上；文件算術成立；契約無倍率） |
| `pnpm anchors:check`（唯讀） | 🔴 **預期紅**：`balanceAnchorsDerived.ts` · `damage-tiers.json` · `docs/平衡錨點量測.md` 三份過期 ⇒ ⭐ 這就是 #1036 的閘在叫（敘述改了、倍率改了，產物還沒跟上） |
| `pnpm echoloop:check`（唯讀） | ✅ 沒有回音迴圈 |
| ⭐ 產物**預覽**（scratch 副本：symlink `content/champions`＋`apps`，複製 `content/config`，改 REPO 後跑一次 gen.ts；⛔ 沒碰任何真產物） | `damage-tiers.json` 五格逐位元相同、note 換成三錨點敘述；`balanceAnchorsDerived.ts` 只有 `HP_ENV_MULT 9 → 4`；`docs/平衡錨點量測.md` diff 只在倍率快照、三張表的引擎最終欄、佔血條／幾發三級各一列、以及末段兩句；`grep "30 級的當標準\|拿 30 級"` 在預覽產物 **0 命中**；honesty 測試的兩個 regex 都解析得到 |
| #1036 突變 | ⛔ 沒有另外跑：`anchors:check` 是逐位元組比對 —— 產物一旦重生成，把 gen.ts 的三錨點敘述改回單一 LV30 **結構上必紅**（今天它已經在對現有產物紅了，同一個機制） |

---

## 五、⛔ 需要主 session 做的事

1. **重生成（順序）**：`bash scripts/genrun.sh anchors:build` → `pnpm content:build`（`docs/editor-contract/ggd-skill-tiers.md:161` 抄的是 `damage-tiers.json` 的 note，`tiers:build` 在 content:build 鏈上）→ bundle / manifest / `_index.json` 一起進 commit。
   ⭐ 重生成後再驗：`damage-tiers.json` 五格仍是 200/500/1000/1500/2000（變了就是缺陷，⛔ 不是跟著改）；`anchors:check` 綠；`balanceAnchorDocHonest.test.ts` 綠。
2. **AC#5 的柵欄外殘留**（`grep -rn "30 級的當標準\|拿 30 級" packages/ apps/` 仍會命中，全部是註解／後台說明字串，語意都可以換成「hard limit ＝ LV30（一定要滿足）；三個錨點都是標準」）：
   - `packages/shared/src/content/damageTiers.ts:27`（檔頭引言）、`:300`（`pickAnchor()` 註解）
   - `packages/shared/src/content/schema/config/damageTiers.ts:16`（檔頭）、`:59`（⭐ 後台欄位說明 `description`，owner 看得到）
   - `apps/admin/src/configForms/specs/tiers.ts:320`（後台欄位說明）
   - `packages/shared/src/content/speedGrowthTiers.ts:81`、`packages/shared/src/content/skillCostTiers.test.ts:195`
3. ⛔⛔ **#1037 owner 裁決「先不要合併」**（票留言逐字）：① main 的六維係數接上出貨（`apCoeffWired.test.ts` 綠）② 用新係數重量一次 M/K ③ 才合併。
   本 lane 只把改動落在工作樹，**合不合併是主 session 的決定**。
4. 部署時 ⛔ 不可 `--content-only`？—— 這一票**其實可以**（只動 json；DEFAULT/SHIPPED 沒改，見一），但 #1029 若同批出貨則不行。
   ⚠️ 部署前確認線上後台沒存過 `maxHealth` 的 override（override 會蓋掉 json）。
5. Rollback：後台 `maxHealth` 填回 9 **並且** `owner-knobs.json` 那一列改回（值 9／「生命倍率調整至9」／2026-08-23 —— 原話存在 `_superseded-rulings.md` §2），否則 `ownerKnobs.test.ts` 紅。
