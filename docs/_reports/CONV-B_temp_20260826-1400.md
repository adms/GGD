# CONV-B lane —— 7 張重開票 → 新票（2026-08-26）

> owner 2026-08-26 逐字：
> 「reopen 跟 開新票摘要舊票差別在哪？我的觀點是 **context 已經改變很多，所以不如開新票而不是 reopen**」

## 舊票 → 新票對照

| 舊票 | 嚴重度 | 新票 | 標題（節錄） | 舊票處置 |
|---|---|---|---|---|
| **#197** | med | **#755** | `[重要][fix]` 「打就站定」的 walkEps 一格當兩用 | 留言＋close |
| **#38** | low | ⭐ **#725（既有 OPEN）** | `[重要][improve]` 戰鬥視覺八項收尾 **AC 5** | 留言＋close（⛔ 不開新票） |
| **#74** | low | **#756** | `[優先][feature]` 語音分離度驗收整條漏斗零執行者 | 留言＋close（合併） |
| **#77** | low | **#756** | 同上 | 留言＋close（合併） |
| **#103** | low | **#757** | `[一般][refactor]` #114 語意色彩鏈有渲染沒有輸入 —— 收乾淨 | 留言＋close |
| **#175** | low | **#758** | `[優先][docs]` `_attribute-derivation-248.md` 補 superseded 標頭 | 留言＋close |
| **#162** | low | **#759** | `[一般][fix]` pc-09 6px 侵入 —— 守衛綠、升級管道謊報 in-progress | 留言＋close |

`bash scripts/ticket-lint.sh 755 756 757 758 759` → ✓ 全過。

## 合併 / 不開票的兩個決定

### ① #74 ＋ #77 → 合成 #756
⭐ **判準是「同一個修法會同時解掉兩張」，⛔ 不是主題像**：
#77 的舊 body 自己寫「**順序不可換：先重測、再建表**」—— P6 的 pair 清單**只能**由 #74 的
P1–P5 GEMM 重測產生。兩張是同一條 HITL 漏斗的機器層與人耳層，
而共同的落點都是「把 `voice` 接進 `tools/review`」。拆兩票必然造出兩份各自漂移的門檻表
（前科就在票裡：`build_voice_audition.py` 已經抄了一份 n=1 的門檻）。

### ② #38 → ⛔ 刻意不開新票，移交既有 OPEN 的 #725
#725 標題逐字含「無實體碎塊」、body 第一行寫「接手 … **#38** …」、**AC 5** 逐字是
「命中／死亡有實體碎塊層（體素 shatter），數量／壽命可調」，且 #725 的 body 本身就是
2026-08-26 新寫的。⇒ 再開一張＝同一個修法兩張票。
⭐ 已在 #725 補一則留言把追蹤責任與兩個「舊 body 已過期」的實作注意事項記在案。

## ⭐ 複驗證據裡我**驗不過 / 不採信**的（本節不可省）

⚠️ 以下每一條**都沒有寫進新票**，或寫進去時明確降級成「未重驗」。

| # | 複驗留言的說法 | 我的查證結果 | 處置 |
|---|---|---|---|
| 1 | **#197**「R1 的『遠程 bot 普攻掉 89.5%（1000→105）』可移植」 | ⛔ **驗不過**。那個數字量的是 PR #182 的 **v1**，v1 識別字（`closingOnTarget`/`WALK_EPS`）今天全 repo **零命中**。新實作 `combatFeel.ts` 的產出塌陷量**沒有人量過** | ⛔ 不寫進 #755 的事實；改成「要數字就對 combatFeel 重量」 |
| 2 | **#197**「R3 的 walkEps 語意要重量測新實作才知道」（＝關票說法，複驗未反駁） | ⚠️ **部分反駁得掉**：R3 的**症狀**是靜態可判的 —— `standstillBlocks`（`combatFeel.ts:1128-1133`）第二行 `if (!isWalking) return false`，而 `isWalking = lenSq(vel) > walkEps²` ⇒ 有效移速 ≤ 0.5 整條規則關閉。⛔ 但 R3 提的**修法**（改純角度判斷）**做不到**：`sim/**` 禁三角函數，`combatFeel.ts:1078-1079` 逐字寫明 | 症狀寫進 #755 AC 1/2；⛔ 修法整條改寫 |
| 3 | **#197**（隱含）R5「測試裡沒有任何遠程單位」仍成立 | ⛔ **已不成立**：`kiting.test.ts:98,111` 有真的遠程 bot（`RANGE = 11`、`spawnFighter`）。剩下的是**斷言太弱**（:151 `> 0`） | 寫進 #755 的「已不適用」表 |
| 4 | **#74**「triage 的資產鍵是 `triage.mjs:**42-49**`」 | ⚠️ **行號微差**：`STRING_REF_KEYS` 實際在 **:43-52**（鍵有七個，含 `modelKey`，複驗漏列） | 新票用正確行號與完整鍵表 |
| 5 | **#74**「`git ls-files \| xargs grep -ln separation-qc-gate` 只回兩個檔」 | ⚠️ **今天是四個**：規格自己 ＋ `docs/_voice-casting.md` ＋ `docs/_reports/archaeology_temp_20260822-1345.md` ＋ `docs/_reports/old-issue-triage_temp_20260823c.md`。⭐ 結論（**零程式**）不變，但數字變了 | 新票寫「零程式」，⛔ 不寫「只有兩個檔」 |
| 6 | **#103**「`fieldAdoption.test.ts:1753/1757` 兩列 debt 每跑一次印一次 banner」 | ✅ **驗得過**，⭐ 而且比複驗說的更硬：該檔 **:53-55** 逐字寫 debt「is printed as a **loud banner on every single run**」 | 寫進 #757（含出處） |
| 7 | **#103**（舊 body）「冷卻差 **5 倍**（cooldown 0.2）」 | ⛔ **驗不過**：`combat-env.json` 今天是 `multipliers.cooldown = **0.3**` ⇒ 差 **≈3.3 倍** | 新票用 3.3×，並標舊數字過期 |
| 8 | **#162**「那條守衛是把 bug **釘在原地**，是第三守則點名的反模式」 | ⚠️ **只有一半驗得過**。守衛**確實**在缺陷存在時通過，⛔ 但它是刻意的 **fail-as-stale**（`versionBadgeBand.test.ts:266-268` 逐字：「if the slot is fixed, this row fails as stale」）⇒ 它**不是**「把 bug 釘死」的反模式，而是「修好記得拆登記」的提醒 | 新票**如實寫兩面**，並改用更強的證據（見下一列） |
| 9 | **#162**（複驗**沒有**提到的，我自己找到的） | ⭐ 守衛 :262-265 自陳這件事 report 到 `docs/todo/latency-visibility.md` 的 `pc-09`；今日實查 **:115 狀態仍是 `in-progress`** —— 一個月來沒有任何東西在 progress ⇒ **它自己指定的升級管道在說謊** | 這才是 #759 的承重論證 |
| 10 | **#162**（舊 body）實測矩形 `{x:10,y:290,w:150,h:66}` / intrusion 6 | ⚠️ **今天沒有重跑 probe**（要在 `apps/client` 下起 vitest，成本高於本 lane 的授權範圍） | ⛔ 不當今天的事實；#759 明寫「要數字就跑驗證指令」 |
| 11 | **#175**「content 是 **71/71** 份 critDamage 1.75」 | ✅ **驗得過**（71 份 champion doc、無一缺漏、全部 1.75） | 寫進 #758 |
| 12 | **#38**「`find -iname '*Debris*'` 排除後零結果」與「#725 OPEN 且標題含『無實體碎塊』」 | ✅ **兩條都驗得過** | 移交 #725，⛔ 不開新票 |

## ⭐ 複驗**沒抓到**、我額外查到的過期（都已寫進新票）

| 新票 | 額外發現 |
|---|---|
| #755 | `combatFeel.ts:1060-1068` **repo 自己已更正**舊票 Y1 的「近戰前搖 0.5s vs 遠程 0.3s」——遠程 0.3 比近戰 0.25 **還長**，差五倍的是**射程**不是前搖 |
| #756 | `audio-map.json` 完全沒有 voices 路徑（`'voices' in json` = False）⇒ triage 的 sfx 那條路也載不到語音 |
| #757 | debt 條目的 `why` 自己寫「0 of **662** abilities」，今天是 **421** ⇒ 債的理由本身已過期；`descriptionRescale.test.ts` 反向鎖已從 :315-325 漂到 **:374-384** |
| #758 | §0 第 13 列「攻速上限 **2.5**」的前提已不存在（`stat-caps.json` 的 `as` = `{base:4, unlocked:10}`）；doc 的 114/111 分母 vs 今天 71 份 |
| #759 | `hudLayout.ts` 的行號從 :251/:261 漂到 **:277/:287**；守衛從 :260-262/:295-340 漂到 **:269-272/:304** |

## 授權範圍內完成、⛔ 未觸碰的

⭐ 只用了 `gh issue create` / `comment` / `close`。
⛔ 零 `gh pr`、零 git 寫入、零部署、零全域鎖指令（`content:build` / `skills:sync` 都沒跑）、零 ssh。
