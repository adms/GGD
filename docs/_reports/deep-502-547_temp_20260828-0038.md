# 深度分析證據收集：GH#502（手把全流程）＋ GH#547（投射物/衝擊波移植）

> 收集時間 2026-08-28 00:38 · 全程唯讀（gh 只 view/list；未跑任何 build/test/sync）
> 每一個數字都是**這一輪量到的**；與票上宣稱不同處逐一標明。

---

## A. GH#502 手把全流程 epic —— 現況：**epic OPEN，全部 23 張子票 CLOSED**

### A1. 子票清單（`gh issue list --search "502" / "手把"`）

**開著：只有 epic #502 本身。** 關了：23 張（K 系列 10 ＋ 點修 12 ＋ #634）。

| 票 | 狀態 | 流程段 / 主題 |
|---|---|---|
| #502 | **OPEN** | epic：登入→大廳→選人→戰鬥→結算→回大廳 |
| #503 K1 | CLOSED | 登入（文字輸入螢幕小鍵盤，解鎖 16 缺口 · 11/14 段） |
| #504 K2 | CLOSED | 橫切（modal data-pad-scope，28 缺口 · 12/14 段） |
| #505 K3 | CLOSED | 橫切（`<select>/<range>`，8 缺口 · 6/14 段） |
| #506 K7 | CLOSED | 橫切（ControlLegend 鍵位，15 缺口 · 12/14 段） |
| #507 K4 | CLOSED | 橫切（捲動容器，10 缺口 · 8/14 段） |
| #508 K5 | CLOSED | 戰鬥（相位整層停用焦點，6 缺口 · 4/14 段） |
| #509 K6 | CLOSED | 橫切（裸 div onClick，2 缺口） |
| #510 K10 | CLOSED | 戰鬥（掛機判定不認手把） |
| #511 K8 | CLOSED | 橫切（B 鍵 data-pad-back，2 缺口） |
| #512 K9 | CLOSED | 戰鬥（地面型瞄準與預覽讀滑鼠，6 缺口） |
| #513 | CLOSED | 橫切（ErrorToast 關不掉） |
| #514 | CLOSED | 大廳（宿敵榜/好友/英靈殿捲動區零可聚焦） |
| #515 | CLOSED | 大廳（排行榜段位只活在 tooltip） |
| #516 | CLOSED | 商店（disabled 列對手把隱形） |
| #517 | CLOSED | 商店（買不起原因只在 tooltip） |
| #518 | CLOSED | 選人（沙發模式 A 循環未過濾＋無鎖定/說明） |
| #519 | CLOSED | 戰鬥（指定型技能無目標指示） |
| #520 | CLOSED | 橫切（五個手感常數寫死 → 後台化） |
| #521 | CLOSED | 回合間（三選一新 scope 不落焦） |
| #522 | CLOSED | 商店（一列一列爬、無分頁/右蘑菇） |
| #523 | CLOSED | 結算（無起始焦點，非「返回大廳」） |
| #524 | CLOSED | 橫切（modal 打開不自動落焦） |
| #634 | CLOSED | 大廳（手把無法從大廳一路操作到回合 —— 焦點導航死程式） |

⭐ epic AC 逐條對照：「14 段子票全數關閉」✅ 已滿；「純手把走完一整圈」的**端到端驗收
腳本（模擬 pad 事件走六段）找不到** —— `grep 六段/fullFlow/e2e` 在 input/ 與 ui/ 無命中。
23 支 pad 相關單元測試都在（見 A2），但 epic 自己點名的那一支總圈腳本是唯一缺的收尾證據。

### A2. 基礎建設檔案與行數（`wc -l`，共 3,998 行 / 14 檔）

| 檔 | 行 |
|---|---:|
| `apps/client/src/input/GamepadInput.ts` | 924 |
| `apps/client/src/input/padFocusNav.ts` | 496 |
| `apps/client/src/input/padCursor.ts` | 264 |
| `apps/client/src/input/padKeyboard.ts` | 175 |
| `apps/client/src/input/gamepadDetect.ts` | 131 |
| `apps/client/src/input/padCamera.ts` | 98 |
| `apps/client/src/input/couchChampSelect.ts` | 100 |
| `apps/client/src/input/padMenuCapture.ts` | 30 |
| `apps/client/src/ui/PadFocusNav.tsx` | 650 |
| `apps/client/src/ui/ControlLegend.tsx` | 402 |
| `apps/client/src/ui/PadKeyboard.tsx` | 189 |
| `apps/client/src/ui/GamepadDiagnostics.tsx` | 145 |
| `apps/client/src/ui/PadCursor.ts` | 135 |
| `apps/client/src/input/MultiGamepad.test.ts` | 259 |

pad/gamepad 測試檔 23 支（input/ 19 ＋ ui/ 4：padFocusNavCapabilities · padFocusNavReach ·
padKeyboard · padModalScope）。

### A3–A5. 三個「存不存在」—— **三個都存在**

| 問 | 答 | 證據 |
|---|---|---|
| 虛擬鍵盤 | ✅（名字是 **PadKeyboard**，「VirtualKeyboard/虛擬鍵盤」字面零命中） | `ui/PadKeyboard.tsx`(189)＋`input/padKeyboard.ts`(175)，接進 `ui/platform/AuthScreen.tsx`（登入頁） |
| 虛擬游標 | ✅ | `input/padCursor.ts`(264)＋`ui/PadCursor.ts`(135)；`GamepadInput.ts`、`PadFocusNav.tsx` 都引用 |
| 戰鬥軟鎖定 | ✅（aimAssist 族，13 檔） | `input/AimResolver.ts` · `ui/displayAimAssist.ts` · `render/AimIndicator.ts` · `render/views/targetMarker.ts` · config 三住處之一 `schema/config/gamepad.ts`＋`combatFeel.ts` · `sim/combatFeel.ts`；出貨檔 `content/config/gamepad.json` 存在（#520 的後台化成果） |

### A6. 六段流程 pad focus 接線證據

⭐ 底座：`PadFocusNav` 是**全域掛載** —— `ui/GlobalChrome.tsx:71` `<PadFocusNav />`，
檔頭 :22 逐字「PadFocusNav BELONGS HERE… A pad drives the WHOLE UI flow」。
⇒ 每一段的差異在「有沒有 scope 宣告／起始焦點／段特有輸入」。

| 段 | 證據（非 test 檔） |
|---|---|
| 登入 | `AuthScreen.tsx`（PadKeyboard＋1 處 data-pad-scope）· `DeviceLoginPanel.tsx`（PadFocusNav＋2 處 scope） |
| 大廳 | `LobbyScreen.tsx` · `LobbyAnnouncement.tsx` · `LeaderboardPanel.tsx` · `RoomListPanel.tsx`（2 scope）· `StoreScreen.tsx`（2 scope）· `platform/widgets.tsx` |
| 選人 | `input/couchChampSelect.ts` 接在 `GameApp.ts`（沙發 lane）；`IntermissionStage.tsx`/`RoomView.tsx` **無**直接 pad 標記（靠全域掛載＋FOCUSABLE_SELECTOR 覆蓋）——六段中直接證據最薄的一段 |
| 戰鬥 | `HudRoot.tsx`（:320 註明相位/modal 歸 PadFocusNav 管）· `ui/hud/padHudFocus.ts` · 瞄準走 `padCursor`/`AimResolver`（#512 K9 成果） |
| 結算 | `panels/MatchEndPanel.tsx:466` `data-pad-back`＋:645 起始焦點邏輯 · `panels/RoundVictoryPanel.tsx:388` `data-pad-back`（#523 成果）· `ui/padFocusLanding.ts` |
| 回大廳 | `MatchEndPanel`（起始焦點刻意**不是**「返回大廳」，:645 有設計註記）· `LeaveConfirmDialog.tsx`（3 處 scope） |

其餘 data-pad-scope 宣告點：`PauseMenu`(2) · `SettingsScreen`(2) · `AugmentDraftPanel` · `padModalScope.ts`。

---

## B. GH#547 投射物與衝擊波 —— 現況：**OPEN；票上三個關鍵數字全部漂了**

### B1. `content/config/ability-vfx-bindings.json` 今天的統計 vs 票上的 24/342/384

⚠️ **票上的 24/342/384 量的是「技能 JSON 直接引用的 fx.*」**（#529 當時），
⛔ 不是綁定表的筆數。兩個母體今天都量了：

**(a) 綁定表本身**（`vfxbind:build` 產物，schema `config.ability-vfx-bindings@1`）：
- bindings **57 筆**（全部 `confidence: CONFIRMED`），合計引用 **125 個 fx.w3x key**（＋9 其他）
- unmatched **99 筆** —— ⭐ 以 **vfxKey** 為鍵（不是 abilityId），逐筆帶 `why`
  （例：`fx.w3x.locust.darkraor.p00` —— 原作零技能引用該模型）
- ⭐ **runtime 真的讀它**：`apps/client/src/render/vfx/{w3xAbilityArt,abilityLayers,w3xFamilyArt,generateAbilityArtContent}.ts`
  經 `ContentDb.ts`；shared 側 `content/vfxBindings.ts`＋`schema/abilityVfxBindings.ts`

**(b) 技能 JSON 直接引用**（母體 422 份 ability JSON，其中 389 份有任何 fx.* 引用）：

| | 票上（#529 當時） | 今天 |
|---|---:|---:|
| 引用 `fx.w3x.*` 的技能 | 24（6%） | **27**（w3x-only 19 ＋ 混用 8） |
| 引用 `fx.prim.*` 的技能 | 342（89%） | **356**（prim-only 348） |
| 有 fx 引用的技能總數 | 384 | **389** |

⇒ 直接引用的 prim 佔比 **348/389 ≈ 89% 幾乎沒動**。⭐ 但 #529 的機制路（綁定表→runtime
圖層）另外覆蓋 57 支 —— 兩路聯集 **64 支**技能拿得到原作美術（直接 27 ∪ 表 57，重疊 20）。

**(c) 「80/120 fx.w3x 沒人用」→ 今天**：`content/vfx` 共 649 份，`fx.w3x.*` **126** 份
（另 fx.prim.* 144 · fx.fam.* 67）。被 abilities/champions/projectiles **直接**引用的只有
**19 份**；**107/126 只出現在綁定表裡**（含 99 筆 unmatched）。
⇒ 若把「進了有 runtime 消費端的綁定表」算作有人用，孤兒是 0；若只算直接引用，107 份仍未落地。

### B2. `content/projectiles/`

- **22 個檔 ＝ 21 份文件 ＋ `_index.json`**；其中 `imported.*` **16 份**
- ⭐ **21/21 全部有 `vfxKey`，但只有 1/21 指向原作美術**：
  `imported.thrust.arcane → fx.w3x.particle.heroluffeattack.p00`
- 其餘 20 份：9 份 wave → `fx.wave.*`（`content/vfx` 只有 **8** 份 fx.wave 文件 ⇒
  `imported.wave` 與 `imported.wave.physical` **共用** `fx.wave.physical` —— #425「共用拖尾」的形狀仍在）·
  5 份 bolt → `fx.prim.*.bolt` · 2 份 → `fx.ember-bolt`（sela.q.bolt 與 imported.bolt 同一份）·
  grail ×2 → `fx.grail.bolt`（也共用）· thorn · basic-attack

### B3. 相依票現況（`gh issue view`）

| 票 | 狀態 | 標題重點 |
|---|---|---|
| #394 | CLOSED | 32 支投射物飛行美術 |
| #425 | CLOSED | wave.* 9 份裡 5 份共用拖尾 |
| #529 | CLOSED | 89% 綁通用原型（⭐ 由 **#818 OPEN** 接手：「108 份原作美術圖層沒人用 —— 先修 promoted 掉的三個欄位再 HITL」） |
| #443 | **OPEN** | 版權解禁＋資產優先序 |
| #547 | **OPEN** | 本票 |

相關的另兩張 OPEN（搜尋時撞見，與本票 Scope 相鄰）：#761（`ability@1` 仍無附著模型/
飛行物/閃電光束三圖層、發射器數住在渲染層寫死表）· #401（沿線推進 6 支未翻）。

### B4. `tools/vfx-bind/scan.py` 對 projectile 族的支援 —— ✅ **已支援**

- :69 「源 **C** 投射物美術(新)：`realArt[channel="art:missile"]` ↔ 誰射它 → `content/projectiles/*.json` 的 `vfxKey`」
- :302 `derive_projectiles()`；:92 結論寫進 `projectile@1.vfxKey`、`--check` 一起驗
- :183 `PROJECTILE_TRAIL_DOCS = 1`（一顆投射物只有一條拖尾 —— ProjectileView 單 ParticleSystem 的量測）
- :117 `art:missile` 抽不到地圖自帶模型時改試零售 stock 家族
⇒ #547 Files 欄寫的「scan.py 擴到 projectile 族」**機制已在**；缺的是把結果**落到內容**（B2 的 1/21）。

### B5. `spawnProjectile` 引用統計（技能 JSON）

- 含 `spawnProjectile` 的技能檔 **22 份**；`projectileId` 引用分佈（全部命中，無 top20 截斷）：
  `imported.wave` 4 · `imported.wave.physical` 3 · `imported.wave.ice` 3 · `imported.wave.ki` 2 ·
  `imported.wave.fire` 2 · `imported.bolt.nature` 2 · `thorne.e.thorn` 1 · `sela.q.bolt` 1 ·
  `imported.wave.void/earth/arcane` 各 1 · `imported.thrust.arcane` 1
- ⇒ **20/22 筆引用是 `imported.*` 通用彈道**；只有 `imported.thrust.arcane` 那 1 筆
  射得出 w3x 美術。`basic-attack`、`grail.*` 兩顆無技能引用（別的系統在用）。

### B6. w3x 側 missile 普查產物 —— ✅ 存在（檔名級，未 cat 大檔）

`tools/w3x-import/out/` 頂層：`vfx-census/{CENSUS.json, MODEL_USAGE.json}` ·
`vfx-bindings/VFX_BINDINGS.json` · `vfx-orient/MODEL_ORIENT.json` ·
`invocation-params/INVOCATION_PARAMS.json` · `GoDieEX22s/ABILITY_W3A.json`（w3a 欄位含 missile）·
`GoDieEX22s-src/{JASS_BEHAVIOR.json, CUSTOM_TEXT.json}` 等 —— 內文含 "missile" 的 json 15 份。

---

## 一句話結論

- **#502**：機制側 23/23 子票全關、三大件（小鍵盤/游標/軟鎖定）與六段接線都有實體；
  epic 還開著的可驗差距＝它自己 AC 點名的**六段端到端 pad 腳本**（找不到）。
- **#547**：scan.py 的 projectile 機制已上線、普查產物齊，但**內容端幾乎沒動**——
  直接引用 89% 仍是 prim、21 份彈道文件只有 1 份接原作美術、wave 共用拖尾仍在；
  票上 24/342/384 今天是 27/356/389（另有綁定表路 57 支、聯集 64 支）。
