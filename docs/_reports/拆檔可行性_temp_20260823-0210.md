# 拆檔可行性分析 —— 可執行計畫

> owner 2026-08-23：「是時候分析優化檔案、拆分檔案以利平行化處理的個案了，請你分析可行性」
> owner 2026-08-20：「如何拆檔案讓獨立平行化不會那麼容易卡住，特別是大量改技能時」

判準（交辦逐字）：**⛔ 不用行數排序，量的是「誰擋住誰」。**
五軸各自量完之後全部跑過一輪對抗複驗，本文只寫**通過複驗**的結論。
本輪唯讀：⛔ 沒有 Edit/Write 任何 content/ · apps/ · packages/ · tools/，⛔ 沒有 git 寫入、沒有 skills:sync、沒碰正式站。

---

## ⭐ 先講結論：**87% 的阻塞不是檔案佈局，是調度**

我自己量的，⛔ 不是推論：

```
$ git worktree list | wc -l        →  51
$ ls .claude/worktrees | wc -l     →  48        （wf_<workflow-id>-<lane> 命名）
$ ls -ldt .claude/worktrees/wf_*   →  全部 Jul 30      ← ⭐ 三週沒用過
$ git reflog -8                    →  今天七條 lane 的 commit **全部在主樹的 reflog 裡**
```

⇒ **per-lane worktree 這套基礎建設早就存在、07-30 用過、之後就沒再用。**
今天七條 lane 全部擠在 `/Users/Takuro/GGD` 一棵樹上，於是：

| 共用什麼 | 後果 |
|---|---|
| 共用**工作樹** | lane B 開工時看得到 lane A 的未提交改動 ⇒ 只好畫**檔案級柵欄** ⇒ 「這個檔被佔著，我交回補丁」 |
| 共用 **index** | 這正是 CLAUDE.md 那條 `git commit -- <逐檔 pathspec>` 規則的由來，而它自己記著「pathspec 擋得住我把別人的送上車，⛔ 擋不住別人把我的送上車」 |

而**真的**會撞的比例極低。把同一天每筆 commit 各自重播到共同祖先、再兩兩跑真的三方合併（`git merge-file`）：

| 檔 | 真衝突率 |
|---|---:|
| `apps/client/src/main.tsx` | **100%**（1/1） |
| `package.json` | **100%**（1/1） |
| `fieldAdoption.test.ts` | 32.1%（9/28） |
| `apps/admin/src/configForms.ts` | 16.7%（23/138） |
| **`apps/client/src/GameApp.ts`** | **7.7%**（1/13）· ⭐ **今天三條 lane 0/3** |
| `apps/admin/src/store.ts` | 5.9% |
| `apps/admin/src/ui/App.tsx` | 2.9% |
| `MatchController.ts` · `navSections.test.ts` | **0%** |

⭐ **owner 點名的 `GameApp.ts` 今天一次都沒有真的擋住誰。** 它擋住 #591/#596 是因為柵欄政策，
⛔ 不是因為 4,041 行。而 `main.tsx` 只有 226 行卻 100% 撞 —— 這一組對照就是「⛔ 不要用行數排序」的完整證明。

⇒ **第一件該做的事不是拆檔，是把 lane 放回自己的 worktree**（每棵樹有自己的 `index`，實測 `.git/worktrees/*/index` 存在）。
它花幾分鐘，解掉 0 衝突那一大類的**全部**等待，而且 ⛔ 不動任何一行出貨程式碼。

⚠️ 但它**解不掉**兩類東西，那兩類才是這份計畫的其餘部分：
① 真的撞同一行的（`main.tsx` · `package.json`）⇒ 要**換機制**
② 檔太大而每個人各改各的（`configForms.ts` · `fieldAdoption.test.ts`）⇒ 要**真的拆**

---

## ① 一頁摘要 —— 用「擋住幾條 lane」排序

| # | 標的 | 行數 | 今天擋幾條 | 真衝突率 | 判定 |
|---|---|---:|---:|---:|---|
| 1 | **lane 隔離政策**（48 個 worktree 閒置） | — | **7** | — | ⭐ **換機制**（⛔ 不是拆檔） |
| 2 | `apps/client/src/main.tsx` | 226 | 1（+18 分鐘） | **100%** | ⭐ **換機制**（⛔ 拆了沒用） |
| 3 | `package.json` 的 `skills:check` 鏈 | <4 | 0（歷史 7 條交回） | **100%** | ⭐ **換機制** |
| 4 | `pnpm skills:sync`（全域鎖 · 30 支產生器） | — | 全部 | — | ⭐ **換機制** |
| 5 | `apps/admin/src/configForms.ts` | **4,978** | 1（08-21 峰值 12） | 16.7% | ⭐ **現在拆** |
| 6 | `fieldAdoption.test.ts` 的 `EXEMPTIONS` | 3,056（表 2,568） | 0（08-18 峰值 12） | 32.1% | ⭐ **現在拆** |
| 7 | `store.ts` + `ui/App.tsx` 的 Page union | 939 / 1,141 | 1 | 5.9% / 2.9% | **換機制（只做一半）** |
| 8 | `configUnion` 閘 + `buildIndexes` 驗證順序 | — | 0 | — | ⭐ **換機制（前置條件）** |
| 9 | `apps/client/src/GameApp.ts` | **4,041** | 0 真衝突 | 7.7% | ⛔ **不要拆** |
| 10 | `MatchController.ts` | 4,767 | 0 | **0%** | ⛔ **不要拆** |
| 11 | `vfx/VfxSystem.ts` | 2,346 | 0 | — | ⛔ **不要拆**（機制已完成） |
| 12 | `schema/config.ts` · `schema/effect.ts` · `batch1.py` | 23 / 299 / 401 | 0 | — | ⛔ **不要拆**（已拆完） |
| 13 | 冷的大檔七支（`mobWaves.ts` 3,049 等） | 1,200–3,049 | **0** | — | ⛔ **不要拆** |
| 14 | `content/registries.ts` | 600 | 0 | — | ⛔ **不要拆** |

**splitNow = 2 · needsMechanism = 6 · dontSplit = 7**

---

## ② ⛔ 不要拆的（先寫，因為這一節省下的預算最多）

### 2.1 `apps/client/src/GameApp.ts`（4,041 行）—— ⭐ owner 點名的那一個，量完是**不要拆**

| 量到的 | |
|---|---|
| 今天三條 lane 的 commit（`3bb20b67` × `34a6bad8` × `ca7dec59`）三方合併 | **0/3 衝突** |
| 跨兩天 13 個配對 | 1 個衝突（7.7%） |
| 拆檔要同步改的守衛 | **20 條閘 · 7 個目錄 · 39 個字面錨點 · 153 條正向斷言** |
| 這 20 條閘會不會靜默轉綠 | ⭐ **一條都不會**（逐檔算 `neg>0 且 pos==0 且無 throw` = **0 個檔**）—— ⛔ 但也一條都躲不掉 |

⇒ 拆它要**一次佔住 7 個目錄的測試檔**，比 owner 說的那四塊補丁還寬 —— 它自己會變成當天最大的柵欄。
而它今天擋住 #591/#596 的原因，lane 報告逐字寫著「**實測開工時兩個檔都已經有 Lane 1 的未提交改動**」——
那**只可能發生在共用工作樹**。⇒ ⭐ 第 1 項（worktree）直接解掉，成本是幾分鐘 vs 拆檔的一整天。

⚠️ 而且它提議的機制**本 repo 已經試過而且沒解掉柵欄**：`render/roundFxRegistry.ts`（GH#337）就是那張註冊表，
今天 `34a6bad8` 仍然同時動了 `GameApp.ts(+4)` + `roundFxRegistry.ts(+27)` + `main.tsx(+5)`。
⇒ 再蓋一張註冊表只是把「所有 lane 都要碰的一個檔」從 4,041 行換成 148 行。

⛔ **附帶推翻一個好聽的數字**：軸②說「音訊與特效兩顆單 lane commit 不撞」是錯的 ——
`34a6bad8`（音訊 #580 在 `createRoundFx({...})` 尾巴加 `sound:`）與 `274945b0`（特效 #494 在同一個引數物件的 `coin:` 子物件加 `playSfx:`）
**落在同一個引數物件**。拆出 `install/roundFx.ts` 只會製造一個新爭用點。

### 2.2 `apps/game-server/src/match/MatchController.ts`（4,767 行）—— 拆**不開**，⛔ 不是不想拆

複驗逐條打掉了它的落地步驟：

- ⛔ `match/cheats.ts` **不存在**（存在的是 `cheatGate.ts` / 三支 test）⇒「搬進已存在的檔」的落點是虛構的
- L4260–4767 引用 **27 個 private 成員**、呼叫 **7 個 private 生命週期方法**
- ⭐ 決定性：`godModeSeats`/`zeroCdSeats`/`infManaSeats` 在 L4301-4308 **寫**、在 **L3964 / L4215 / L4239 讀** —— 正是「⛔ 不要動」的每 tick 套用路徑
- **80 個 `*.test.ts` import 它**（game-server 共 101 檔）⇒ 夾具綁死
- 「8 個 `beginCombatXxx` 收成一張表」也不成立：`beginCombatDeathWards` **刻意沒有 `enabled`**，旁邊逐字記著「一個『武裝旗標』就是一種可以被忘記的故障」⇒ `{enabled,begin,end}[]` 會把那個**被刻意移除的旗標裝回去**

而它兩個撞車日的三方合併都是 **0 衝突**。⇒ 今天沒有任何 lane 因它而停。

⚠️ ⛔ **也不要標成「之後拆」**。CLAUDE.md 逐字：「不要老是叫我排版次⋯結果後續根本沒做消失了」。
判決只有兩種：今天做，或不做。**這一項是不做。**

### 2.3 `apps/client/src/vfx/VfxSystem.ts`（2,346 行）—— 它命名的機制**已經做完了**

軸④原本說它有「374 條 id-keyed 特效常數該搬 `content/config/`（GH#384）」。實測：

- `VfxSystem.ts` 裡的 `"fx.*"` 字面值只剩 **7 個**
- 目的地**已經存在而且在用**：`content/config/ability-vfx-bindings.json`（38 KB · 36 筆 bindings）+ Zod `schema/abilityVfxBindings.ts` + 載入器 `content/vfxBindings.ts` + 閘 `vfxBindings.test.ts`

剩下的 31 個 const 是 tint / 步距 / `MAX_POOL_PER_DOC=4` 這類**調參**，⛔ 不是 id-keyed 綁定。
⇒ ⭐ 這一批**今天直接拿掉**；連帶「它會新增一份 config 所以要排在 admin 後面」這條相依性也消失。

### 2.4 已經拆完的三個（⛔ 排進今天的批次 = 重做一次）

| 檔 | 現況 | 拆檔 commit |
|---|---|---|
| `schema/config.ts` | **23 行門面** + `schema/config/` 68 檔 | `f2c8acc8`（08-22） |
| `schema/effect.ts` | **299 行** + `schema/effects/` 49 檔 | `234d96b9`（08-20） |
| `tools/skill-remake/batch1.py` | **401 行** + `common.py` + `heroes/` 16 檔 | `234d96b9` |

⭐ 而拆檔真正的效益要誠實講：招牌數字「門面拆後 0 commit」是**樣本 0** ——
`f2c8acc8..HEAD` 的 22 個 commit 裡，新增到 `schema/config/` 的 shard 是 **0 個**（唯一那份新 config 建在**上一層**）。
拿拆檔**之前**的真實流量做反事實（判準：增刪一個 union 成員 ⇒ 必須碰 `index.ts`）：48 個 commit 裡 **12 個（25%）** 要碰 ⇒ 反事實 `index.ts` maxDay = **5**。
模型驗過：`effect.ts` 反事實 2 = 拆後實測 2。

⇒ ⭐ **拆檔是 13→5，⛔ 不是 13→0。** 這份計畫下面每一個「現在拆」的效益都按這個尺度報，⛔ 不報 0。

### 2.5 冷的大檔（⛔ 拆它們換到的併行度是 **0**）

`apps/admin/src/mobWaves.ts`（**3,049 行，自 08-15 起 0 個 commit**）· `ui/combatText.ts`（1,648，0）·
`content/templates/expand.ts`（2,008，11 票**撞車日 0**）· `sim/mobs.ts`（2,779，12 票**撞車日 0**）·
`net/snapshot.ts`（659，maxDay **1**）· `rooms/MatchRoom.ts`（1,340，maxDay 1）· `content/registries.ts`（600，熱度是上游級距批次的連帶）

1,332 個手寫程式檔裡 **1,086 個撞車日 = 0**。

⛔ `packages/shared/src/protocol/schema.ts` 額外有**真風險**：它是 APPEND-ONLY 協定，`ENTITY_FLAG` 的 BIT BUDGET 住在裡面 ——
拆它可能導致 bit 重編號 ＝ 線上舊分頁 desync，**而任何一處都不會報錯**。

⇒ ⭐ **`mobWaves.ts` 3,049 行 0 觸碰 vs `main.tsx` 226 行 100% 真衝突率** —— 這一組就是判準本身。

---

## ③ 該「換機制」而不是拆檔的（⭐ 寫出是哪個機制）

### 3.1 【第 1 名】lane 隔離：**per-lane git worktree**，⛔ 不是檔案級柵欄

**機制**：workflow 派 lane 時 `git worktree add .claude/worktrees/wf_<id>-<n>`，lane 在自己的樹裡工作與 commit。
基礎建設**已經存在**（48 個目錄、命名慣例、`.git/worktrees/*/index` 各自獨立），只是 07-30 之後沒再用。

| 解掉什麼 | 為什麼 |
|---|---|
| 0 衝突檔的**全部**等待 | `GameApp.ts`（今天 0/3）· `MatchController.ts`（0%）· `navSections.test.ts`（0%）· `store.ts`/`App.tsx`（<6%） |
| CLAUDE.md 那條「別人把我的送上車」 | index 不再共用 ⇒ 那個視窗結構上不存在 |
| 「開工時看得到別人的未提交改動」 | 工作樹不再共用 |

⚠️ **它解不掉的**（⛔ 老實講）：真的改同一行時，合併階段照樣要人解 ——
那正是 3.2 與 3.3 的兩個標的，也正是為什麼它們要換機制而不是換位置。

⚠️ **前置**：`pnpm skills:sync` 寫 `bundle.json`，全域只能一條跑（CLAUDE.md）⇒ worktree 化之後**仍然**由主 session 最後統一跑一次（見 3.4）。

### 3.2 `apps/client/src/main.tsx`（226 行 · 100% 真衝突）—— ⛔ 拆檔**已被實驗推翻**

複驗做了兩個 git 探針，⛔ 不是推論：

1. 把 Lane1(`34a6bad8`) 與 Lane3(`ca7dec59`) 放上兩條真分支 merge → **CONFLICT 2 hunk**，
   而**兩個 hunk 都在「要被搬走的那 87 行」裡**（import 清單一行 + `startMatch()` 的 `resetHudStore();` 後一行）
   ⇒ 搬去 `boot/matchLifecycle.ts` 會**一字不差重現**。
2. 依「`BOUNDARY_RESETS` 一張表」的形狀建表，兩條 lane 各 append 一列 + 一行 import → **照樣 CONFLICT**。

⇒ ⭐ 「兩條 lane 在同一張清單上各加一行」這個形狀 **git 永遠合不掉**，⛔ 與檔案大小、與是不是一張表無關。

**正解：把宣告放回擁有狀態的模組（ownership inversion）。**

出貨現況我查過，三個 reset 的**擁有者本來就各自獨立**：

```
main.tsx:12  import { resetHudStore }        from "./net/RoomStore";
main.tsx:13  import { resetClientGlobals }   from "./clientGlobals";
main.tsx:14  import { resetAudioForNewMatch } from "./audio";
```

而 `startMatch()` 與 `stopMatch()` **各叫一次**（owner 2026-08-23：「不管是出口還是入口⋯寧願多次清理乾淨也不要漏清到」）
⇒ 每個新子系統今天要加的是 **2 行**，⛔ 不是 1 行。

```
apps/client/src/boot/matchBoundary.ts      ← 新增，⭐ 零相依（⛔ 不 import 任何 app 模組）
    export type Boundary = "enter" | "exit" | "round-shop";
    export function onMatchBoundary(at: Boundary[], run: () => void): void
    export function runMatchBoundary(at: Boundary): void

net/RoomStore.ts     底部加：onMatchBoundary(["enter","exit"], resetHudStore);
clientGlobals.ts     底部加：onMatchBoundary(["enter","exit"], resetClientGlobals);
audio/index.ts       底部加：onMatchBoundary(["enter","exit"], resetAudioForNewMatch);

main.tsx  startMatch() 三行 → runMatchBoundary("enter");
          stopMatch()  三行 → runMatchBoundary("exit");
```

⇒ ⭐ **下一個子系統加清理 = 在自己的檔加一行，`main.tsx` 零改動。** 兩條 lane 各改各的檔 ⇒ 真衝突率 0。

⚠️ **零相依是硬要求**：已驗證的真環是
`net/RoomStore → clientGlobals → ui/abilityHold → ui/components/abilityText → ui/components/Tooltip → ui/displayFinal:36 → ../net/RoomStore`。
bus 只要 import 任何一個 app 模組就會被捲進去。

⚠️ **這個形狀有一個真的新壞法**：module-scope 註冊**只在模組被 import 時才跑**。
⇒ 閘必須是**可達性**閘（從 `main.tsx` 靜態可達 ⇒ 註冊會發生），⛔ 不是「檔案裡有沒有那一行」。
⭐ 好消息：`ca7dec59` 帶進的 `matchLifecycleWiring.dom.test.ts` **`await import("./main")`** 已經是**行為**守衛，
「零件寫好沒人接線」今天已經會紅 —— 新閘接在它旁邊即可。

⚠️ **硬約束**：`architecture.test.ts:93` 逐字寫 `r === "main.tsx"` ⇒ 抽出去的 boot 模組**不可以帶 React**（⛔ 不可以把 `root.render` 一起搬走）。

⛔ **不要碰的三段**（爭用 = 0，而且四支守衛**正確地**要求它們留在 entry）：
5 支 CSS import（`focusGlow.test.ts:139` 釘住「唯一 importer 是 main.tsx **且在 `buttonFx.css` 之後**」，Tier-2 override 靠 source order 贏）·
兩處 `root.render(`（`globalChrome.test.ts:56` 走訪每一處）· replay 入口分支（自 07-24 沒動過）。
`§開機綁定 36–50` 也不要動：`git log -L 36,50` → **最後一次 2026-08-10**，平行 lane 時代爭用 = 0，而搬它要跨柵欄改 `cursor/cursor.test.ts:283`。

### 3.3 `package.json` 的 `skills:check` 鏈（<4 行 · 100% 真衝突 · ⭐ 歷史交回第 1 名）

7 條不同 lane 逐字交回（#450 #529 #540 #541 #548 #554 #555），最刺眼的是 #554：
「package.json 在柵欄外。⛔ 不接這一條，**這個閘等於不存在**」。

我量到的現況：`skills:check` 是**手寫的 32 個 `&&`**，而 root 有 **41 支 `*:check`**。

**機制（兩格，都不是拆檔）：**
1. **從 `scripts` 物件推導聚合鏈** —— `skills:check` 不再是字面長鏈，而是一支 runner 讀 `package.json` 的 `scripts`，
   跑每一支 `*:check`（豁免表照舊帶「能被反駁的理由」）。⇒ 加第 42 支產生器 = **0 行**要改。
   ⭐ `skillsSyncCoversGenerators.test.ts` 本來就從 `scripts` 推導，⛔ 與檔案結構無關 ⇒ 這條閘不受影響。
2. **per-tool `package.json`** —— `pnpm-workspace.yaml` **已經 glob `tools/*`**，40 個 `tools/` 目錄裡**已有 10 個**帶自己的 `package.json`。
   ⇒ 新產生器把自己的 script 宣告在自己的目錄，root 那一行消失。

### 3.4 `pnpm skills:sync` 的全域鎖 —— ⭐ 全 repo 真正的第一名，⛔ 而且拆檔對它無效

我自己量的近 24 小時撞車排行，**前六名全部是產生物**：

```
17  docs/_release/ggd-board.html
13  content/editor-target-profile.json
12  content/manifest.json
12  content/bundle.json
12  docs/reference/{roster,mechanics,items,abilities,grail-wishes}.md
```

⛔ 產生的檔拆了下一次還是全部重寫 ⇒ 這不是拆檔題。CLAUDE.md 規定同時只能一條跑它 ⇒ **它擋住的是全部 lane**。

**機制**：每支產生器宣告 `input glob` 與 `output 檔` ⇒ ① 只跑輸入變過的 ② 鎖從全域降成 per-output。

⚠️ ⛔ **不可以照「前 11 支在 `content:build` 之前、後 18 支只寫 docs/」切一刀** —— 那是假的：
`jasscombo:build` 寫 `content/config/combo-strikes.json`（`tools/jass-combo/extract.py:33`）、
`speedtiers:build` 寫 `content/champions/*.json`（`tools/speed-growth/gen.ts:212`），**兩支都排在 `content:build` 之後**
⇒ 它們一改，剛建好的 `bundle.json` 就是舊的 —— 正是 2026-08-02 事故的形狀。
⇒ 要**真的做一張相依圖**（誰寫 `content/` ⇒ 誰必須在 `content:build` 之前，或 `content:build` 跑兩次）。

⚠️ 前科：`c9cbc9a2` 的標題逐字記著 `skills:sync` 把其中一條 lane 的成果整個刪掉。

### 3.5 `store.ts` + `ui/App.tsx` 的 Page union —— ⭐ 只做**一半**推導（另一半會把閘變成恆真式）

CLAUDE.md 記載這是「已知唯一真正共用的檔」。實測那句話**在說謊**（第三守則的形狀）：
三個單一 config commit（`69306fc0` #520 · `75d39ebf` #499 · `c21674f0` #429）每次動 **4 支**檔、`store.ts` 是 **2 行**，
而漏掉的 `configForms.ts` 正是 maxDay **12** 的那一支。

| 做 | ⭐ 做什麼 |
|---|---|
| ✅ `Page` union · `NAV` · `SESSION_REQUIRED_PAGES` | 從 `CONFIG_DOC_SPECS` 推導 ⇒ 3 行變 0 行 |
| ⛔ `navSections.test.ts` 的 `BASELINE_PAGES` / `SINCE_BASELINE` | **保持手抄** |

⛔ **為什麼第二項不能推導**：那支測試的檔頭逐字寫著「這份清單是**基準線**，不是『目前的 NAV』的副本」，
斷言是 `added = NAV − BASELINE − SINCE_BASELINE` 必須為空 —— 價值**全部**來自兩個獨立維護的住處。
兩邊都從 `CONFIG_DOC_SPECS` 推導 ⇒ 對那 55 個 config 頁就是 `X − X = ∅`：**永遠綠、不再守任何東西**。
而它已被證明會抓到東西（同檔 L295 逐字記著 `apDamageScaling` 漏掉基準害它紅過）。

⭐ 兩個順帶更正：
- ⛔ **不需要 `sessionRequired` 旗標**：實測 **55/55** 個 config 頁**全部**在 `SESSION_REQUIRED_PAGES` 裡
  ⇒ 規則是「config 頁一律要 session」，**fail-closed**。加一格 optional 旗標，漏填 = 沒登入的人改得到那一頁。
- `migrationGate.test.ts` 與 `quickApprovalBundle.test.ts` 讀 `store.ts`/`App.tsx` 的**原始碼文字**
  ⇒ 推導只能吃 config 頁那一半，非 config 頁（49 個）保持手寫。

⇒ 4 個共用檔降到 **2 個**，⛔ 不是 0。

### 3.6 【前置條件】`configUnion` 閘的盲區 ＋ `buildIndexes` 的驗證順序

⛔⛔ **這一條排在所有拆檔前面**，因為它是唯一**已經通到線上**的路。

**(a) 閘的盲區 —— 我自己數的：**

```
$ grep -rlE 'z\.literal\("config[^"]*"\)' packages/shared/src/content/schema/*.ts | wc -l   → 17 個檔
$ grep -rhoE 'z\.literal\("(config[^"]*)"\)' ... | sort -u | wc -l                          → 18 個 tag
```

閘 `configUnionCoversDirectory.test.ts` 只 `readdirSync(schema/config/)` ⇒ 這 17 檔 / 18 tag **全部在閘外**。
⚠️ 交辦單說的是「`castApproachDoc.ts` 一個檔」—— 實際大 **18 倍**。

⭐ 而且還有兩個**正則也看不見**的（就算掃遍全樹）：

```
schema/screenFxDoc.ts:92   schema: z.literal(SCREEN_FX_SCHEMA_TAG)      ← 常數,⛔ 不是字面字串
content/newHeroChecks.ts:175  schema: z.literal(NEW_HERO_CHECKS_SCHEMA) ← 而且住在 schema/ **之外**
```

⇒ ⛔ 「把掃描改成整棵 `schema/**`」會做出一個**看起來修好、實際仍漏掉**的閘。

⛔⛔ **它已經響過一次而且是人手撿回來的**：`5d752488` 把 `content/config/cast-approach.json` 提交進 git 而 union 完全沒有它，
**閘全綠**，47 分鐘後 `c21674f0` 才由人在收 lane 時發現。那則 commit 訊息逐字：
「Zod union 那一行只在工作區。那正是 2026-08-02 線上壞掉四小時的形狀」
「`apps/admin` 在執行期拿到 `undefined`，走訪器直接 crash 而 tsc 之外沒人會說」。

**(b) ⭐ 新發現：`pnpm content:build` 對「新增」一份 config 是 EXIT=0**

我自己讀了原始碼，機械可查：

```
packages/shared/scripts/buildIndexes.ts:63   await new ContentLoader(new FsContentSource(CONTENT_DIR)).load({policy:"fail-closed"})
packages/shared/scripts/buildIndexes.ts:75   const manifest = rebuildAllIndexes(CONTENT_DIR)
                                             ↑ 驗證在**重建之前**

packages/shared/src/content/node/FsContentSource.ts:24
    async readIndex(collection) { return this.readJson(join(collection, "_index.json")) }
                                             ↑ 列舉走 **_index.json**
```

⇒ **一份全新的 JSON 在那一趟根本不存在** ⇒ 驗證跳過它 ⇒ 第 75 行把它烘進 `bundle.json` ⇒ **EXIT 0**。第二次跑才會紅。
`buildIndexesValidates.test.ts` 抓不到，因為它的夾具是**改壞一份既有的**文件（失敗形態 ⑤：被測的不是出貨的那個）。

⇒ ⭐ **四道綠燈可以直達骨架英雄**：configUnion 綠（盲區）→ `content:build` EXIT 0 → `shippedBundleIsCurrent` 綠（它只叫 `rebuildAllIndexes`，⛔ 不驗證）→ 上線 fail-open → 2 隻骨架英雄，**而網站看起來完全正常**。

**修法（兩條，都很小）：**
1. `buildIndexes.ts`：**先 `rebuildAllIndexes` 再驗證**（或讓驗證從磁碟列舉，⛔ 不從 `_index.json`）。
   驗證失敗時 ⛔ 不可以留下已寫入的產物 —— 寫暫存再 rename，或驗證失敗就 `git checkout` 不了的話至少 EXIT≠0 並印出「產物已過期」。
2. `configUnionCoversDirectory.test.ts` 改成**雙向相等 + 執行期推導**（照 `effectShardWiring.test.ts`，見 §4.1）：
   ⭐ 出貨端**現在就是完美雙射**（我自己數：`content/config/*.json` **86 檔 / 84 個相異 tag** ↔ union 84 個成員，兩向 orphan = 0）⇒ **現在加就是綠的**。

---

## ④ 逐個拆檔方案（⭐ 只寫「現在拆」的兩個）

### 4.0 兩個都要遵守的四條 —— 從前例的成功與**留下的洞**推導

| | 前例做對的 | 這次照抄 |
|---|---|---|
| **門面** | `schema/config.ts` 23 行 `export * from "./config/index"` 保住 **101 個 import 端**、**0 處繞過** | ✅ 兩個都留門面 |
| **閘** | ⛔ 前例的閘掃**資料夾** ⇒ 18 tag 盲區 | ⛔ **不掃目錄**，改抄 `effectShardWiring.test.ts` |
| **散文** | ⛔ 前例留下 137 條指向舊檔的註解，**0 條閘在守** | ✅ 加指路閘（§5 Slot 1） |
| **循環** | ⛔ 全 repo `no-cycle`/`madge`/`dependency-cruiser` 命中 = **0** | ✅ 循環閘要**先**就位 |

⭐ **閘的樣板 —— `effectShardWiring.test.ts`（⛔ 不是 `configUnionCoversDirectory.test.ts`）**：

```ts
expect(unionKinds).toEqual(schemaFiles);      // 檔案 == Zod union
expect(registryKinds).toEqual(schemaFiles);   // 檔案 == 執行期註冊表
expect(variantFiles).toEqual(schemaFiles);    // 檔案 == TS union
```

⭐ **`toEqual` 是雙向的 ⇒ 結構上不可能有「住在掃描範圍外」的盲區** —— 另一邊（執行期物件）看得到它，等式就不成立。
這正是 `configUnion` 那條**單向子集**缺的另一半。而且它**四邊都讀執行期的真東西**，⛔ 沒有一邊掃原始碼字串。

### 4.1 `apps/admin/src/configForms.ts` —— 4,978 行 → 55 檔 + 一扇門面

**量到的形狀（我自己數的）**：55 個 `const XXX_SPEC: ConfigDocSpec`（≈90 行一個）· **0 個 top-level `let`/`var`**（無共用可變狀態）。
與 `config.ts` 逐字同型：**N 個獨立區塊 + 一張 union 表**。8 個撞車日 45 個 commit，**區塊 0 重疊**。

**切法：按 config 文件（＝一個後台頁）切，一 spec 一檔。**

```
apps/admin/src/configSpecs/modelLod.ts          ← const SPEC: ConfigDocSpec = {...}
apps/admin/src/configSpecs/vfxCleanup.ts
apps/admin/src/configSpecs/…                    ← 共 55 檔
apps/admin/src/configSpecs/index.ts             ← CONFIG_DOC_SPECS = [modelLod, vfxCleanup, …] as const
apps/admin/src/configForms.ts                   ← 門面：export * from "./configSpecs/index"
```

**門面**：15 個外部 import 端全部是**具名匯出** ⇒ 一扇 `export *` 全保住。
⭐ 而且我掃過 `apps/admin` 全部 `*.test.ts` 的 `readFileSync` —— **⛔ 沒有任何測試把 `configForms.ts` 當原始碼文字讀**
⇒ 拆它不會製造「綠但不守任何東西」的閘。

**閘**（照 §4.0，⛔ 不掃目錄）：

```ts
// configSpecs/specWiring.test.ts
const files   = readdirSync(HERE)…                    // ① 檔案系統
const listed  = CONFIG_DOC_SPECS.map(s => s.page)     // ② 執行期陣列
const schemas = zConfigDoc.options.map(o => o.shape.schema.value)  // ③ 出貨的 Zod union
expect(listed.sort()).toEqual(files.sort());          // 雙向：多一個檔或少一行都紅
expect(new Set(listed).size).toBe(listed.length);     // 重複頁名
// ④ 每一份 config schema 都有一個後台頁（反向：schema 有而 admin 沒有 ⇒ owner 調不到）
```

⚠️ **一行拆不掉，要誠實報**：出貨現況是 `export const CONFIG_DOC_SPECS: readonly ConfigDocSpec[]`（`configForms.ts:4681`）——
⭐ 它**不是** `discriminatedUnion` 的元組,所以 `config.ts` 那條「union 不能自動組」的型別理由對這裡**不成立**。
⛔ **但若要同時做 §3.5 的 `Page` 推導就成立了**：`readonly ConfigDocSpec[]` 已經把 `page` 放寬成 `string`,
要拿回字面型別必須 `as const` + `defineConfigSpec<const P extends string>()` ⇒ 陣列必須是**靜態元組**,
而 `import.meta.glob` 給不出字面型別 ⇒ `Page` 會退化成 `string`、路由 exhaustive 檢查整個消失。
⇒ 43 個 commit 裡 **16 個（37%）** 增刪 spec 成員 ⇒ 反事實 `index.ts` maxDay = **5**。

⭐ **預期效益：12 → 5**（⛔ 不是 12 → 0）。

⭐ **必須與 §3.5 同批做**：只拆檔，55 個檔還是要回來各動 `store.ts`/`App.tsx` 一行；只做推導，metadata 反而擠回 maxDay 12 的那個檔。

**逐檔柵欄（Lane A 專屬，⛔ 別人不准碰）**：
```
apps/admin/src/configSpecs/**            （新增）
apps/admin/src/configForms.ts            （縮成門面）
apps/admin/src/store.ts                  （Page union + SESSION_REQUIRED 推導）
apps/admin/src/ui/App.tsx                （NAV 推導；⛔ 非 config 頁那 49 個保持手寫）
apps/admin/src/configDocCoverage.ts
apps/admin/src/navSections.test.ts       （⛔ 只准加「config 頁改由推導」的註記；BASELINE 保持手抄）
```

### 4.2 `fieldAdoption.test.ts` 的 `EXEMPTIONS` —— 3,056 行（表佔 2,568 = **84%**）

⭐ **沒有人提過它，而它的真衝突率 32.1% 是九個標的裡最高的。**
形狀：**一張資料表穿著測試的衣服**（第〇·四守則）。每條加 schema 欄位的 lane 都必須在這張表上加一條，而彼此的鍵從不相同。

**切法：按領域切**（`abilities` / `champions` / `items` / `augments` / …），一域一檔。

```
packages/shared/src/content/fieldAdoption/exemptions/abilities.ts
packages/shared/src/content/fieldAdoption/exemptions/champions.ts
packages/shared/src/content/fieldAdoption/exemptions/…
packages/shared/src/content/fieldAdoption/exemptions/index.ts   ← 合併 + 重複鍵閘
packages/shared/src/content/fieldAdoption.test.ts               ← 只留 ~300 行邏輯
```

⚠️ **鍵不是合法檔名**，實測長這樣：
```
enum:abilities.effects[]#applyBuff.condition|0|2.is=guardian
```
含 `| [ ] # = :` ⇒ ⛔ 不可以一鍵一檔用鍵當檔名。**一域一檔**，鍵留在檔內。

⚠️ ⛔ **落點不可以是 `content/config/*.json`** —— 任何新的 `content/config/*.json` 一定拖出
Zod union + admin spec + `Page` + `SESSION_REQUIRED` + `NAV` + `navSections` 基準，
正是 §3.5 量到的那條四檔鏈。⇒ 用 **TS 檔**，留在 `packages/shared`。

⚠️ ⛔ **重複鍵閘不是加分項，是在補回一個編譯器保證**：
今天 `Record` 字面值的重複鍵是 `tsc` / `no-dupe-keys` **免費**擋掉的；
拆成多檔再 spread 合併之後，重複鍵會**靜靜取最後一個**。⇒ `index.ts` 必須用陣列合併並 assert 唯一，⛔ 不可以 `{...a, ...b}`。

⭐ **預期效益：12 → 3**（逐日量「同一天幾個 commit 改到同一個 key」峰值 = 3；7 天 210 個 key 有 41 個被多個 commit 動過）。

⛔ **要撤掉一條理由**：軸④說「`GRACE_DAYS` 是第四個住處」是假的 —— grep 全 repo 扣掉自己 = **0 個消費端**，
它是測試本地的政策常數，⛔ 不是「三住處 + drift 在守」的出貨數值。（撤掉不影響結論，84% 體積本身就夠。）

**逐檔柵欄（Lane B 專屬）**：
```
packages/shared/src/content/fieldAdoption.test.ts
packages/shared/src/content/fieldAdoption/**       （新增）
```

---

## ⑤ ⚠️ 拆檔會踩到的閘 —— 逐條寫「怎麼不弄壞它」

| # | 閘 | 拆檔會怎樣 | ⭐ 怎麼不弄壞它 |
|---|---|---|---|
| 1 | `configUnionCoversDirectory.test.ts` | 盲區 20/84 = **23.8%**；新檔開在掃描範圍外 ⇒ **靜靜綠** | 改成 §4.0 的雙向 `toEqual` + **執行期讀 zod 物件的 `.shape.schema.value`**，⛔ 不用正則認字面值（`SCREEN_FX_SCHEMA_TAG` 是常數）。範圍要整棵 `content/**`（`newHeroChecks.ts` 住 `schema/` 之外） |
| 2 | `buildIndexes.ts` 的驗證順序 | 新增一份文件時 **EXIT 0 而且被烘進 bundle** | 先重建再驗證；驗證失敗 ⇒ EXIT≠0 **且**不可留下已寫入的產物 |
| 3 | `architecture.test.ts` 的路徑前綴豁免（5 處 `startsWith`） | ⛔ **落點決定閘還管不管它**：搬進 `render/` → 靜靜綠；搬進新目錄 → 紅（安全）。React 白名單逐字寫 `r === "main.tsx"` | 新檔一律落在**不在任何豁免清單上**的目錄；boot 模組 ⛔ 不可帶 React。⚠️ 另有 4 族帶路徑豁免的閘（`focusGlow` `surfaceParity` `shopAttachSites` `versionBadgeBand`），其中兩族就住 `ui/` |
| 4 | 豁免表本身過期 | 排除路徑指到不存在的檔 ⇒ `toBe(0)` 大聲紅、`toBeGreaterThan(0)` **靜默綠**（結構性不對稱） | ⭐ 照抄 `focusGlow.test.ts:275` 的 stale-exemption 斷言（「豁免列不再描述任何東西 → 紅」）。⛔ 不要寫成「收尾檢查表」那是判準 |
| 5 | 循環相依 | 全 repo **0 條閘**；tsc 收得下、Vite 跑得起來，只在 module-init 期給一個 `undefined` | 拆之前先加 `madge --circular` 或 dependency-cruiser。⚠️ 已驗證的真環：`RoomStore → clientGlobals → abilityHold → abilityText → Tooltip → displayFinal:36 → RoomStore` |
| 6 | 過期指路 | 前例留下 137 條指向舊檔的散文，**0 條閘** | 加指路閘（`path.ts:N` 必須存在且 `N ≤ 行數`；門面檔 ≤30 行且只有 `export *`，⛔ 不可當內容住處）。⭐ 全樹 1,724 條可解析指路裡**只有 3 條**要清 ⇒ 現在加幾乎零清理成本 |
| 7 | `navSections.test.ts` 的基準線 | 兩邊都推導 ⇒ `X − X = ∅` **永遠綠** | ⭐ 見 §3.5：`Page`/`NAV`/`SESSION_REQUIRED` 推導，**基準線保持手抄** |
| 8 | `fieldAdoption` 的重複鍵 | 從 tsc 免費擋 → spread 靜靜取最後一個 | `index.ts` 用陣列合併 + assert 唯一，⛔ 不用 `{...a,...b}` |
| 9 | 讀 `GameApp.ts` 原始碼的 **20 條**閘（7 目錄 39 錨點） | 一條都不會靜默（`neg>0 且 pos==0 且無 throw` = 0 個檔）⛔ 但一條都躲不掉 | ⭐ **不拆它**（§2.1）⇒ 這一格今天不用付 |
| 10 | `shippedBundleIsCurrent` / `shippedBundleHasTrackedSources` | 拆檔本身中性，⛔ 但**來源檔沒 commit** 會重演 2026-08-02 | 每個新檔 `git add`；`content/` 沒動就 ⛔ 不要跑 `content:build` |
| 11 | `skillsSyncCoversGenerators.test.ts` · `staleArtifacts.test.ts` · `cover()` beacon | ⭐ **拆檔完全不影響**（從 `scripts` 推導 / 遞迴全樹 / 執行期 beacon） | ⛔ 不要花預算改它們 |

⚠️ **真正會靜默的閘只有 2 條 / 3 條斷言**（158 個讀原始碼的測試檔、325 條否定斷言，篩完剩這些）。
⛔ **不要拿 325 或 448 當風險值 —— 會誇大 100 倍。** 這個 repo 的原始碼掃描閘寫得比預期好，主流寫法是 `throw` 或先斷言抽取產量。

---

## ⑥ 執行順序 —— 今天的批次

> owner 2026-08-20 逐字：「**不要老是叫我排版次**⋯頂多依照**相依性衝突分開不同批次但都要在同一天做完**」

⛔ 沒有「短期/中期/長期」。下面是**今天的四個 slot**，以及它們**為什麼不能同時跑**。

### Slot 0 —— lane 隔離（⏱ 幾分鐘，⛔ 不動任何出貨程式碼）

把 workflow 的 lane 派到 `.claude/worktrees/wf_<id>-<n>`（基礎建設已存在，48 個目錄閒置三週）。

⭐ **為什麼排第一**：它解掉 0 衝突那一大類的**全部**等待（`GameApp.ts` 今天 0/3、`MatchController.ts` 0%、`store.ts`/`App.tsx` <6%），
而且**下面三個 slot 自己就是三條 lane** —— 沒有它，Slot 2 的三條又要共用一棵樹。

### Slot 1 —— 閘與 `package.json`（**序列，一個人，⛔ 不可以派成 lane**）

| 做什麼 | 檔 |
|---|---|
| `buildIndexes` 驗證順序（§3.6b） | `packages/shared/scripts/buildIndexes.ts` |
| `configUnion` 改雙向相等 + 執行期推導（§3.6a） | `packages/shared/src/content/schema/config/configUnionCoversDirectory.test.ts` |
| 循環閘（§5-5） | `package.json` + 一支新測試 |
| 指路閘（§5-6） | 一支新測試 |
| `skills:check` 從 `scripts` 推導（§3.3） | `package.json` |

**為什麼不能與 Slot 2 同時跑**：
① 這四條閘**正是 Slot 2 出錯時該紅的東西** —— 順序反了就是無保護拆檔；
② 三項都動 `package.json`（真衝突率 100%）⇒ 只能一個人做。

### Slot 2 —— 三條 lane 並行（⭐ 按 **package** 天然互斥，逐檔柵欄見 §4）

| Lane | 柵欄 | 做什麼 | 預期 |
|---|---|---|---|
| **A** | `apps/admin/**` | `configForms.ts` → 55 檔 + 門面 **＋** `Page`/`NAV`/`SESSION_REQUIRED` 推導 | 12 → **5** |
| **B** | `packages/shared/src/content/fieldAdoption*` | `EXEMPTIONS` → 一域一檔 + 重複鍵閘 | 12 → **3** |
| **C** | `apps/client/src/{main.tsx,boot/**,clientGlobals.ts,net/RoomStore.ts,audio/index.ts}` | boundary-reset ownership inversion（§3.2） | 100% → **0** |

**為什麼這三條可以同時**：實測近 24 小時的檔案排行，三者**零交集**（admin / shared-content / client）。
**為什麼 A 內部不能再拆成兩條**：只拆檔或只做推導都會讓另一半白做（§4.1 末）。

### Slot 3 —— 收尾（**序列，主 session**）

```
pnpm skills:sync          # ⛔ 全域只能一條跑（CLAUDE.md）
pnpm skills:check
npx vitest run <改到的>    # ⛔ 一批 ≤3 次
pnpm typecheck; echo "EXIT=$?"   # ⛔ 1 次,⛔ 不要開工前先跑一次
git commit -F msg.txt -- <逐檔 pathspec>
```

⚠️ ⛔ **不要 `git add` 之後再 `git commit`**（index 共用；worktree 化之後這條仍然對，因為主 session 在主樹）。
⚠️ Slot 2 的三條 lane **全程禁止**跑 `skills:sync` / `content:build`（`--check` 唯讀，隨便跑）。

### ⛔ 今天**不做**的（已在 §2 給理由，⛔ 不排進「之後」）

`GameApp.ts` · `MatchController.ts` · `VfxSystem.ts` · `registries.ts` · 七支冷的大檔 · 三個已拆完的。

---

## ⑦ 測試預算（第零守則⑦）

| Slot | 層 | 上限 | 突變 |
|---|---|---:|---|
| 1 | 閘（體驗層 + 一條真的線上風險） | ≤80 行/條 | ⭐ **一條**：`buildIndexes` 那條（丟一份 tag 不在 union 的 config → 必須 EXIT≠0） |
| 2A/2B | 拆檔接線（體驗層） | ≤80 行 | 接線類做一次（新開一個檔不掛 union → 紅並指名） |
| 2C | 客戶端生命週期（⭐ 靈魂層邊緣） | ≤實作行數 | ⭐ 一條承重：拿掉某模組的 `onMatchBoundary` 註冊 → 可達性閘紅 |

⛔ 純位置搬移（`configForms` 的 55 個 spec、`EXEMPTIONS` 的 184 列）**0 行新測試** —— 內容一個字沒變，閘由 §4 那條雙向相等在守。

---

## ⑧ 一句話

⭐ **owner 問的是「拆檔可行嗎」，量完的答案是：兩個檔真的該拆（`configForms.ts` · `fieldAdoption` 的豁免表），
一個檔拆了沒用而要換機制（`main.tsx`），他點名的那一個（`GameApp.ts`）今天 0 真衝突、⛔ 不該拆 ——
而最大的一格根本不在檔案裡：48 個 per-lane worktree 閒置了三週，七條 lane 全擠在同一棵樹上。**
