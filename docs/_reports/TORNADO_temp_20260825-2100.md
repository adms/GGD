# TORNADO lane 完整報告 — GH#688 Phase 6：TornadoElemental ×9（2026-08-25 21:00）

> 任務：census 建議表最大宗 —— 9 隻 dummy 共用 `Abilities\Spells\Other\Tornado\TornadoElemental.mdl`，
> 模板 `tpl-locust-orb`（static-single 族），零行新引擎程式，全部 JSON 參數。

## 一、模型落地（交付 1）✅

| | |
|---|---|
| 來源 | **War3x.mpq**（repo 根目錄 stock MPQ 鏈），56,557 bytes mdx |
| 轉換 | `python3 tools/w3x-import/convert_stock_model.py tornadoelemental`（STOCK_MODELS 新列） |
| 產物 | `content/assets/models/imported/tornadoelemental.glb`（212,512 bytes）＋ 量測紀錄 `tools/w3x-import/out/stock/convert-tornadoelemental.json` |
| 文件 | `content/models/w3x.stock.tornadoelemental.json`（scale 0.0839 ＝ 1/glb高度 11.92，照 monsoon/revivehuman 慣例；clipMap idle→"Stand Walk" / death→"Death"；⛔ 刻意不寫 fxLongAxis —— 龍捲風是站著的東西，寫 y 會被放倒） |
| 轉換 gate | **全綠**：4 貼圖全部 `shape-in-rgb`（⛔ 零 LUMA-KEY 病）· 4 emitter segmentAlpha peak 68–100（⛔ 零「出生 0 判隱形」陷阱）· visiblePrims **3/3** · 動畫 Birth/Stand Walk/Death · 重跑 dry-run 對 HEAD 基線 verdict "ok" |

## 二、9 隻 dummy 逐支對照（交付 2 · 5）

對照鏈全部從 war3map.j 逐條回溯（trigger → `GetSpellAbilityId()` → OBJECTS.json 的英雄/道具擁有者 → GGD id）：

| rawcode | 原作名 | trigger → 施放技能 | GGD 落點 | census 參數（scale·tint·lifeSec） | 狀態 |
|---|---|---|---|---|---|
| `e00Y` | 雷電風暴 | LiteningWind → A052（15-03） | `godie-emfr.e`（15-03 獄炎煉我） | 3 · [0.5882,0.5882,1] · 2 | ⭐ 改**來源** `tools/skill-remake/heroes/godie-emfr.py`（產物鎖住，等 skillremake:json） |
| `e016` | 千之雷 | ThTh → A053（15-04） | `godie-emfr.r`（15-04 雷天大壯。貳式） | 2 · [1,0,1] · null | 同上 |
| `e013` | 畫龍點睛 | Toro（buff B03V，A000，FF7 Cloud） | `godie-hart.e`（01-03 畫龍點睛 —— 卡面逐字「迴旋巨劍產生**龍捲風**」） | 2 · [0.5882,0.5882,1] · null | ✅ 手編（standalone＋champion 鏡射） |
| `h01S` | 三千世界龍捲風 | ThworldStart → A0MQ（11-04） | `godie-udre.r` ＋ `godie-u01u.r`（索隆兩具身體） | 2 · [0.3922,0.5882,1] · null | ✅ 手編 ×2（各 standalone＋鏡射；為既有 musashi orb 的**第二節點**） |
| `o01H` | 螺旋劍 | spiralAttack → A0JP ＝ **item I01V** | `content/items/godie-i01v.json`（螺旋劍 onBasicAttack 螺旋擊 hook） | 4 · [0.4706,0,0] · 3 | ✅ 手編（item passive effects；authoringNote 補了稽核列） |
| `o01P` | 天翔龍閃 | HoLuKen → A0L2（78-03） | `godie-u00v.e`（78-03 廬山昇龍破） | 3 · [0.7843,0.3922,0.3922] · 5 | ✅ 手編（standalone＋鏡射） |
| `h027` | 三檔旋風 | Luf Three Effect → A0RZ（76-04 u00n/u00o） | ⛔ **不綁** | alpha 已解出＝**0.01%**（見 §三） | 無落點（刻意） |
| `u00A` | 蒸汽球 | —（census sites=0） | ⛔ 無落點 | — | 無落點 |
| `u00Z` | starbreaker | —（census sites=0） | ⛔ 無落點 | — | 無落點 |

節點形狀（六個落點同款，⛔ 零行引擎程式）：
`{"kind":"spawnModelFx","shape":"single","preset":"tpl-locust-orb","modelKey":"w3x.stock.tornadoelemental","clip":"idle","soundKey":"wc3.blademasterwhirlwind", scale/tint/lifeSec 照上表}`
- `soundKey` 是 `modelFxStagingContract` ④ 逼出來的（orb 家族不在 SOUNDLESS_TEMPLATES ⇒ 無聲節點紅）；取家族既有的旋風鍵 `wc3.blademasterwhirlwind`（出貨 orb 節點的前例），⛔ 不是我發明的聲音。
- lifeSec null 的一律不寫（吃模板預設 2.5）；`clip:"idle"` → clipMap → "Stand Walk"（龍捲風的旋轉住 stand 序列，GH#689 modelFxRig 真的會播）。
- ⚠️ e00Y/e016 原作落在 GetSpellTargetLoc()，但新版 15-03/15-04 是 self 變身技 ⇒ orb（繞施法者）是第〇·六守則第 1 層贏的錨；o01H/o01P/h01S/e013 原作本來就落在施法者腳下 ⇒ orbit 半徑 0.1 逐字等價。
- ⚠️ 15-03 獄炎煉我已被 owner 重設計成**火**技而 e00Y 的 tint 是藍的 —— 本批照 census 忠實搬（Phase 6-1 monsoon 同款前例）；要換色是 owner 一格 tint 的事，已可在後台調。

### 無落點清單（帶理由，⛔ 不是「還沒做」）

| rawcode | 理由 |
|---|---|
| `h027` | 出生即 `SetUnitVertexColorBJ(100,100,100,99.99)` ⇒ α=0.01%（war3map.j:36765，census runtimeAlpha 唯一一筆）＝**原作刻意近全隱形的死演出**。綁 α=0.0001 是零像素節點（第一·五守則），綁不帶 α 是發明原作沒有的畫面 ⇒ 不綁，列 owner 裁決項 |
| `u00A` 蒸汽球 | census sites=0（全 JASS 零生成點） |
| `u00Z` starbreaker | census sites=0 |
| `o01P` 的 SkySlash/A012 路徑 | 47-04 天翔龍閃屬 Eevi（緋村劍心）/E012（小次郎），**兩位都不在出貨名冊**（content/champions 無此二檔） |
| `o01P` 的 init Die 路徑 | 生成點是 `gg_rct_SpecialUnitCreateArea`（地圖初始化預載區），非玩法演出 |

### alpha 解不出的清單
**空** —— 9 隻裡唯一有 runtime alpha 的 h027 已解出（0.01%），如上處理。

## 三、runtimeAlphaBackfill 守衛的粒度修正

守衛的 join key 是**模型**、census 的 alpha 是**逐 dummy** —— TornadoElemental 落地後兩個粒度第一次撞上：
h027 的 0.0001 會被錯記到同模型另外六隻 dummy 的節點頭上（＝六具龍捲風全隱形）。
⇒ `packages/shared/src/content/runtimeAlphaBackfill.test.ts` 新增 `SHARED_MODEL_UNADOPTED` 表
（h027 一列，理由可被反駁），①對這一列改驗「**沒有任何採用節點帶著它的 α**」——
h027 哪天真的移植，節點帶 0.0001 ⇒ 這一列當場紅 ⇒ 刪掉它。⛔ 不是白名單。

## 四、視覺證明（交付 3）✅

- **靜態可判**（@visual-proof）：`packages/shared/src/content/locustTornadoElemental.test.ts`
  ①模型文件＋glb 存在、glb JSON chunk 逐位元解析：≥1 出生可見 primitive、有貼圖必有 TEXCOORD_0（UV 不退化）、轉換紀錄 4 emitter peak>0、4 貼圖非 LUMA-KEY
  ②七個落點 standalone＋champion 鏡射都擺得出節點、tint 逐格等於 census 建議表（顏色不可自己挑）、`validateDoc` 過出貨 schema。
- **audition**（真渲染 A/B，`docs/_reports/tornado_visual-proof_20260825-2100/`，7 張 PNG＋frames.md）：
  `beam-audition.html?ability=godie-udre.r` · **calibrate() 先自證**（全亮 quad 173,056 亮像素）·
  基線 **0 lit** → cast resolve（spawns=2）→ glb 就緒 **419 lit** → midlife 412 → 到期回收 **0** →
  重放 spawns=4 → ⭐ **A/B：關掉 musashi 節點後仍 414 lit ⇒ 亮像素幾乎全部來自 TornadoElemental**。
  shot6 目視可辨：藍 tint 龍捲風漏斗環繞施法者。誠實限制：PRE2 粒子拖尾不在 glb（同 revivehuman 前例）。

## 五、#669 批核頁登記（交付 4）✅

- `pnpm review:register` 已入帳：批次 `tornado_visual-proof_20260825-2100`，預設 LIVE。
- **rollback 開關**：`tpl-locust-orb` → `params.alpha.default`＝**1.0**（live）⇒ **0**（veto）。
  1.0 與缺席**逐位元同義**（`modelFxRig.ts:236` 對 alpha≥1 明文跳過乘法）⇒ 出貨畫面不變；
  翻 0 ⇒ 整族沒寫自己 alpha 的節點（含本批 6 具龍捲風）一起隱形，寫了的（musashi 0.5）不受影響。
  這是照 GOKU/BEAM 前例的家族級開關（count 6→1、path static→forward 同款形狀）。
- `node tools/review/check.mjs` exit 0（rollback 解析通過；queue.json 過期是既有警示行，主 session `pnpm review:build`）。

## 六、逐檔清單（工作樹，⛔ 本 lane 零 git 寫入）

**改（來源／程式）**
- `tools/w3x-import/convert_stock_model.py` — STOCK_MODELS 加 tornadoelemental 列
- `tools/skill-remake/heroes/godie-emfr.py` — 15-03/15-04 各加一列 model_fx（e00Y/e016）
- `packages/shared/src/content/runtimeAlphaBackfill.test.ts` — SHARED_MODEL_UNADOPTED（§三）
- `packages/shared/src/content/modelFxPreset.ts` — 註解更正（alpha 預設例外）
- `apps/client/src/vfx/beamAuditionWorld.ts` — PILOT_MODEL_DOCS 加 tornadoelemental（audition 索引補列，content:build 後 no-op）

**改（出貨內容，經 `product-quarantine.sh unlock/lock --step tiers:apply` 正規解鎖，改完已重鎖 402 份）**
- `content/abilities/godie-hart.e.json` ＋ `content/champions/godie-hart.json`（E 鏡射）
- `content/abilities/godie-udre.r.json` ＋ `content/champions/godie-udre.json`（R 鏡射）
- `content/abilities/godie-u01u.r.json` ＋ `content/champions/godie-u01u.json`（R 鏡射）
- `content/abilities/godie-u00v.e.json` ＋ `content/champions/godie-u00v.json`（E 鏡射）
- `content/items/godie-i01v.json`（passive effects＋authoringNote 稽核列，⛔ 原文未刪一字）
- `content/ability-templates/tpl-locust-orb.json`（alpha default 1.0 開關＋description 更正）
- `docs/_review/feature-verdicts.json`（review:register 寫入）

**新**
- `content/models/w3x.stock.tornadoelemental.json` · `content/assets/models/imported/tornadoelemental.glb`
- `tools/w3x-import/out/stock/convert-tornadoelemental.json`（量測紀錄）
- `packages/shared/src/content/locustTornadoElemental.test.ts`（守衛＋@visual-proof）
- `docs/_reports/tornado_visual-proof_20260825-2100/`（frames.md＋7 PNG）
- 本報告

## 七、測試帳（第零守則⑦）

- vitest **3 次**（上限 3）：①六檔批跑（抓出 staging④ 無聲紅→補 soundKey）②重跑同批 → **唯一紅＝emfr 兩列（設計如此，見 §八）**，其餘 18 綠（schema/tint/鏡射/staging/runtimeAlpha/monsoon/locustTemplates 全過）③突變。
- **突變（一批一條承重線）**：刪 `godie-hart.e.json` 的 spawnModelFx 節點 → `locustTornadoElemental` 紅並逐字指名 `e013 → content/abilities/godie-hart.e.json (standalone)` → 還原（Edit，⛔ 非 checkout）。
- `pnpm typecheck` **1 次**：EXIT=0（全 repo）。

## 八、主 session 待收清單 ⚠️

1. **`pnpm skills:sync`**（含 skillremake:json）—— 落地 emfr 兩個龍捲風節點。
   ⭐ 在那之前 `locustTornadoElemental.test.ts` 第二條**刻意是紅的**（4 筆 missing 全是 emfr），
   訊息自帶修法「跑 pnpm skills:sync」—— 這是防主 session 忘記的閘，⛔ 不要改測試。
2. **`pnpm content:build`** —— `content/models/_index.json` 收新模型文件、bundle 重打。
3. `pnpm review:build` —— queue.json 過期警示（review:check exit 0，不擋）。
4. **git add 之後 `scripts/visual-proof.sh` 自動轉綠** —— 現在紅只因證據目錄未追蹤
   （CHANGED 讀 git diff，看不見 untracked；路徑已驗過 match `^docs/_reports/.*visual-proof.*\.md$`）。
5. caps:export / spec:build：本批**零新機制**（純既有 spawnModelFx＋模板參數）⇒ 預期 no-op，跟著 sync 走即可。
6. apconv:build / tiers:apply：本批節點不含 tier 欄位 ⇒ 預期 no-op（正規化器只覆寫自己那幾格）。

## 九、owner 裁決項（⛔ 不擋出貨）

- **h027 三檔旋風**：原作 α=0.01%（≈全隱形）。要不要「還原成看得見」（違原作）或維持不綁（現狀）？
  現狀已由 `runtimeAlphaBackfill` 的 SHARED_MODEL_UNADOPTED 一列守著，移植即紅即改。
- **15-03 獄炎煉我 × 藍龍捲**：owner 重設計為火技，census 忠實 tint 是藍的（150,150,255）。
  換暖色只要改 `godie-emfr.py` 那一格 tint（或後台）。
