# #1014 —— ① 的另一半：`sim` shard（`singleFork`）＋ 為什麼 60 秒拉不長、為什麼只收兩支

lane · 2026-09-06 15:30–15:50 · 併行 19 條 lane（⚠️ 所有本機時間都在這個負載下量的）

## 基線（動手之前，量到的）

| 項 | 值 | 出處 |
|---|---|---|
| 上一輪（`dea1b0ade`）已落地 | `maxForks = min(16, 核數)`、ci.yml `--workspace-concurrency=1` ＋ 逐包 `--poolOptions.forks.maxForks $(nproc)`、`scripts/vitest-verdict-check.sh` ＋ 守衛 | `ticket-progress.sh read 1014` |
| 還沒做的 | 票文 ① 的後半：「把 replay 那種重播測試移進**自己的 pool/shard**」 | 同上 |
| RPC 逾時**確認拉不長** | birpc `timeout` 只從 `createBirpc` 的 options 讀（`index.68735LiX.js` `if (timeout >= 0) setTimeout(...)`）；`createRuntimeRpc`（`rpc.C3q9uwRX.js`）把 `...options` 展開在最後，但 `workers/forks.js` 的 `createForksRpcOptions(v8)` 只回 `serialize/deserialize/post/on` ⇒ **沒有任何設定路徑**。要改只能 `pnpm patch vitest`（⛔ 根 package.json，柵欄外，且脆） | 逐行讀出貨 dist |
| vitest 2.1.9 的「自己的 pool」長什麼樣 | `createForksPool`（`resolveConfig.rBxzbVsl.js`）：**一顆** Tinypool，大小＝**根設定**的 `maxForks`（逐 project 的不讀）；`poolOptions.forks.singleFork` 的 project **等所有 multi-fork 檔跑完**才跑、`recycleWorkers()` 後在一個子行程逐檔序跑（逐檔仍 `resetModules`） | 同上 |
| 誰會中 | 只有「**真 I/O `await` → 緊接 > 60s 同步區段**」：`@vitest/runner` 的 10ms debounce 要一次 macrotask 轉圈才會送 `onTaskUpdate`；整檔零 `await` 的同步模擬（settlement 409s · royale 322s · roundHistory 327s · arenaRotation 291s · match 119s，CI attempt 2）在 attempt 1 的 11.6× 超額訂閱下**全部 ✓** | attempt 1/2 log ＋ runner 原始碼 |
| CI attempt 2 逐檔（11.6× 超額訂閱） | replay 513,034 · teamHealth 436,054 · settlement 408,972 · roundHistory 326,696 · royale 321,999 · arenaRules 317,311 · arenaRotation 290,976 · draftRedesign 140,734 · match 119,382 · analytics 108,772 ms | `gh run view 33976263791 --attempt 2 --log` |
| 三個 `beforeAll(async)` 的形狀 | teamHealth / arenaRules / draftRedesign：`await ContentLoader.load()` → `registerAll` **就結束**，模擬在後面的同步 `it` 裡 ⇒ ⛔ 不是會中的形狀 | 逐行讀 |
| replay 的形狀 | `await MatchRecorder.open` → 同步錄影 → `await finish` → `await ReplayPlayer.open` → 同步重播：**兩段**「await 後緊接同步」，單段 CI 40s 級，再擠一倍就跨 60s ⇒ ⭐ 潛在受害者（票文的直覺對了一半：它不是 attempt 1 的元兇，但它是**最暴露**的） | 逐行讀 |

## 做了什麼

| 檔 | 改動 |
|---|---|
| `apps/game-server/vitest.workspace.ts`（新，82 行） | 兩個 inline project：`unit`（＝原設定 ＋ `exclude` 名單，`configDefaults.exclude` 帶著）與 `sim`（`include` ＝ 名單，`singleFork: true`）。⚠️ 用 `import base` 展開，⛔ 不是 `extends`：vite `mergeConfig` 把陣列**串接**（`[].concat(existing, value)`），`extends` 縮不了 `include`。`root` 明寫（ship.mjs 的 `--root apps/game-server` 從 repo 根跑也一樣）。名單：`src/replay/replay.test.ts`、`src/analytics/analytics.test.ts`，每一支帶量到的出處。🔀 `GGD_VITEST_SIM_SHARD=0` ⇒ `["./vitest.config.ts"]`（＝舊行為） |
| `apps/game-server/vitest.config.ts` | 檔頭 +7 行：workspace 在隔壁、`maxForks` 是整顆 Tinypool 的大小、為什麼不用 `extends` |
| `apps/game-server/src/vitestSimShard.test.ts`（新，65 行，體驗層薄守衛） | ① 名單每支存在且是 `src/**/*.test.ts` ② 分割完整不相交（`unit.exclude ⊇ 名單`、`sim.include == 名單`、`unit.exclude ⊇ configDefaults.exclude`、母體 `readdir(src)` 反方向再走一次）③ `sim.singleFork === true`、`unit` 不是 ④ 票文兩支在名單裡；`GGD_VITEST_SIM_SHARD=0` ⇒ 單一 project。⛔ 不做突變（體驗層） |
| `scripts/vitest-verdict-check.sh` | 處方那一行多一句：會中的形狀進 `vitest.workspace.ts` 的 sim shard；開關名。⛔ 被守衛斷言的字串一個都沒動 |

⛔ 沒碰 `replay.test.ts`、`analytics.test.ts`（tick 數一個都沒少）、⛔ 沒碰 ci.yml、⛔ 沒靜音任何 unhandled rejection。

## 驗證（2 次 vitest，第 3 次沒用到）

| # | 跑了什麼 | 結果 |
|---|---|---|
| 1 | `npx vitest run src/vitestSimShard.test.ts` | 4/4 綠，980ms；輸出帶 `|unit|` 前綴 ⇒ workspace 真的被 `readdir` 找到了 |
| 2 | **整包** `npx vitest run --reporter=default --reporter=json`（19 條 lane 同時在跑） | **156 檔 925 tests 全 ✓ · 0 行 RPC 逾時 · `VITEST_EXIT=0` · wall `real 100.0s`（Duration 97.79s；collect 260s + tests 454s）**。順序證據：最後兩行是 `✓ |sim| replay.test.ts 33,238ms` → `✓ |sim| analytics.test.ts 3,556ms`，前面 154 行全是 `|unit|` ⇒ shard 真的等 unit 跑完才獨跑 |
| — | `bash scripts/vitest-verdict-check.sh run2-full.log` | `Test Files 156 passed (156)` 帳平，exit 0；`|sim|` 前綴不影響它 |

⚠️ **analytics 獨跑 3.6s**（上一輪同負載下 11.7s、CI 攤在 16 forks 下 108.8s）—— 這就是 shard 治的那件事：它的同步區段現在是 native 速度，離 60s 有 **16×** 的餘裕（本機）。

## 代價（誠實，估的那格標了）

| | |
|---|---|
| 序跑尾巴 | replay 33.2s ＋ analytics 3.6s ＝ **36.8s**（量到，19 lane 負載下） |
| 本機整包 wall | 100s。⚠️ 沒有同負載的舊基線；**估**舊布局 ≈ max(unit 段 ≈61s, replay 33s) ≈ 61s ⇒ 本機約 **+35s**（估的）。ship.mjs 的地板是 apps/client ~220s，game-server 100s 仍在地板下 |
| CI | ⭐ 反而該變短：replay 從 16 forks 搶 4 vCPU 的 513s 變成獨跑（估 60–90s，估的）。⛔ 要看 3 次 `unit` 才算數 |
| 為什麼名單只有 2 支 | 收「很慢但同步」的檔只會拉長尾巴，⛔ 治不了任何東西（它們結構上中不了）。加一支的判準寫在 workspace 檔頭 |
| 殘餘風險 | 即使同步檔，在「timer 剛送出 RPC、主行程回覆還沒到、下一個 `it` 已經進同步區段」的窄窗仍可能中 —— 概率的，只有分核能壓它（上一輪已做）。⛔ 這是 vitest 2.1.9 結構的洞，不是設定能關的 |

## 開關

| 開關 | 住處 | 預設 | 回頭 |
|---|---|---|---|
| `GGD_VITEST_SIM_SHARD` | `apps/game-server/vitest.workspace.ts` | 開（**1**） | `=0` ⇒ 單一 project，逐位元＝2026-09-06 之前 |
| `GGD_VITEST_MAX_FORKS` / `GGD_WORKSPACE_TEST_CONCURRENCY` / `GGD_VITEST_FORKS_PER_PACKAGE` | 上一輪 | 不變 | 見 `dea1b0ade` |

## 柵欄外（主 session 決定）

- `.github/workflows/ci.yml`：**不需要改**。`--poolOptions.forks.maxForks $(nproc)` 是根設定 ⇒ 仍是整顆 Tinypool 的大小；`singleFork` 住在 project 裡不受 CLI 影響。可選：unit job `env` 加一行 `GGD_VITEST_SIM_SHARD: "1"` 純為了可發現性。
- `pnpm typecheck`（開工不跑）：新守衛從 `src/` import `../vitest.workspace` ⇒ tsc 會跟著查它。用了 `as unknown as Project[]` 避 TS2352；`{...base.test}` 展開進 `ProjectConfig` 靠「spread 不做 excess property check」。CI 的 Typecheck step 會給答案。
- 其它三個包（`packages/shared` / `apps/admin` / `apps/client`）仍寫死 `maxForks: 16`；⛔ 沒有同病（沒有 > 60s 的 await 後同步段），沒動。

## 剩下的

- ⏸ AC「同一 commit 連續 3 次 `unit` 綠」—— 要 push 後看 CI。若 CI 的 sim 尾巴意外長（> 3 分鐘）⇒ `GGD_VITEST_SIM_SHARD=0` 一行回頭，⛔ 不要縮短那兩支。
