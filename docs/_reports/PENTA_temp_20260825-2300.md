# PENTA lane 完整報告 — GH#688 Phase 6 第五批：三族一次收（2026-08-25 23:00）

> 任務：ForgottenOneTent（聖杯黑泥）· ThunderClapCaster · ChaosOrcRange 共 11 列。
> ⭐ 核心發現①：ForgottenOneTent 全族的 w3x 原主是 **E00Q 黑化Saber（`_legacy`，未出貨）**，
> 但出貨名冊有 GGD 原創英雄 **godie-zombiex（聖杯黑泥醬 喪標麥可）** —— 它的身分就是這一族
> （EX 名「此世全部之咖哩」＝この世全ての悪的黑泥哏）⇒ 綁 zombiex，veto 走 #669 頁（我挑的，帶開關）。
> ⭐ 核心發現②：**ChaosOrcRange 全族零「可見」落點 ⇒ 刻意不轉** —— oshm 五個生成點
> **逐點 `ShowUnitHide`**（隱形 bloodlust/stomp 代理；綁可見模型＝發明原作沒有的畫面，
> TORNADO h027 同款裁定）。
> ⭐ 核心發現③：audition 差分尺抓到一個**全綠出貨級**的真缺陷（EX 泥 100% 藏在英雄體內），當場修掉。

## 一、11 列逐列驗證（交付 2 · 對照表）

對照鏈：census templateSuggestions → war3map.j trigger（`GetSpellAbilityId()`）→
OBJECTS.json 擁有者（heroes/units 的 abilities＋hero_abilities）→ HERO_NUMBERS →
content/champions 名冊；sites=0 的列照 QUAD 課①**再查 w3a 二進位**。

### ForgottenOneTent（本批轉，`w3x.stock.forgottenonetent`；⚠️ 聖杯＝owner 在意的家族）

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| u02S 黑洞聖杯泥 | InSpace → A0FK（j:32745） | **69-002 固有結界-黑洞**（E00Q 黑化Saber） | **godie-zombiex.r**（100-04 百式・哈基米「聖杯的黑泥從體內爆發」） | ⭐ 原主在 `_legacy` ⇒ 綁**同一身分的出貨原創英雄**（我挑的；rollback＝orb 家族 alpha 開關 ＋ #669 頁一鍵否決）。原作 `loop 1..9` 散在 600×600、`sleep 8` 清場 ⇒ **orbit count 9 · distance 3 · lifeSec 8**（±300 wc3u ÷100；引擎的 count 等分環＝散布的既有編碼，dx=dz=0 不動）。⚠️ 建議表寫 tpl-locust-line —— 分群器把「loop 內生成」都判線，但這個 site 是**繞施法者的隨機散布**，線會往單一方向拉 16 單位 ⇒ 改 orb 環，理由量在 j:32770 的 `RectFromCenterSizeBJ(600,600)` |
| u02V 聖杯黑泥 | sites=0；**w3a `AOsw.Hwe1` lvl1** | 黑化Saber 的 AOsw 召喚 L1（scale 1.5） | （併入 u02W 列） | ⭐ QUAD 課①應驗：census 零生成點，真落點在 w3a 召喚欄。**三列＝同一召喚的 L1/L2/L3 外觀** ⇒ 一個落點一具，⛔ 不是三個 |
| u02X 聖杯黑泥 | w3a `AOsw.Hwe1` lvl2 | 同上 L2（scale 1.75） | （併入 u02W 列） | 同上 |
| u02W 聖杯黑泥 | w3a `AOsw.Hwe1` lvl3 | 同上 L3（scale 2） | **godie-zombiex.ex**（100-002 此世全部之咖哩＝黑泥受肉哏） | ⭐ 取最高階外觀 scale 2 · tint [0.1176,0,0] · **distance 1.5**（見 §四的缺陷）· lifeSec 缺 ⇒ 模板 2.5 |

### ThunderClapCaster（本批轉，`w3x.stock.thunderclapcaster` —— geoset 那半；PRE2 emitter 半已出貨 `fx.w3x.stock.thunderclapcaster.p00`）

| rawcode | 生成點 | 原作技能 | GGD 落點 | 判定 |
|---|---|---|---|---|
| o006 雷切 | LightCutRun → A0IJ（j:41999 命中分支，TimedLife 1.00，ZZ_LC_P1＝命中瞬間施法者腳下） | **45-03 千鳥**（佐助 Edem） | **godie-edem.e** | ⭐ 新綁（改**來源** `heroes/godie-edem.py` 的 model_fx 第二列：scale 3.5 · tint [1,0,0] · lifeSec 1 · soundKey `wc3.thunderclapcaster`＝模型家族自己的聲）。⚠️ 45-03 既有的 monsoon 節點是 Kirin 分支的雨，⛔ 不同一個原作特效 ⇒ 不算疊加（hvsh.r 雙節點前例） |
| o006 的另一半 | HolySword → A0OD（j:31174） | 23-04 雷焰聖劍（Ntin 菲特） | ⛔ 無 | Ntin 在 `content/_legacy/` |
| e00G Chain Ball Effect | sites=0 · w3a 零引用 · JASS 零引用 | —— | ⛔ 無 | 定義了沒接線的死內容（int32 形式也查過） |
| o01F 雷光劍(落雷) | Light Fight → A0JE（j:49495） | 「77-04 真-雷光劍」**舊版** | ⛔ 無 | ⭐ **A0JE 在 w3x 裡就沒有主人**：E00W/E00X 的 hero_abilities 是 A0UB（Light_sword 路徑，走 o02U），A0JE 只剩 4 個 trigger 條件、零 SelectHeroSkill/UnitAddAbility ⇒ 被 A0UB 取代的死版本。守衛③掃 OBJECTS.json 釘著這個理由 |
| h00Z 九頭龍閃（任務點名覆核） | NineSlash → A01B；NineSlashEffect 的 ThunderClap chest 特效同屬 A01B | 47-03 九頭龍閃（Eevi 劍心） | ⛔ 無 | ⭐ 覆核完成：census 把 h00Z 歸 **WarStompCaster**（unit.model），ThunderClap 只是同技能的 chest 命中閃 —— 兩半都掛在 A01B，劍心未出貨 ⇒ **QUAD 的除名成立**，不動、不重複守（QUAD 守衛已釘「九頭龍閃」） |

### ChaosOrcRange —— ⭐ **全族零可見落點 ⇒ 模型刻意不轉**

| rawcode | 證據 | 理由 |
|---|---|---|
| oshm | 五個生成點（j:33217 KillPowerEX · 33271 KillPower · 50256 TwoPowerEX · 52652 pandaDie · 52806 pandaJizz）**每一點下一行都是 `ShowUnitHide`** | 原作刻意隱形的 bloodlust/stomp 代理（22-00/22-003/89-03/89-04 的施法殼）。技能本身**已出貨**（godie-e001/e00n.passive、godie-h02k.e/.r），可見的演出是英雄身上的 bloodlust/stomp target art（別的 census 家族）⇒ 給 dummy 綁可見模型＝發明原作沒有的畫面（TORNADO h027 α=0.01% 同款裁定） |
| h019 金鋼狼 | WolfStrike → A0I4＝31-01 迴旋爪擊（Othr X戰警） | Othr 在 `content/_legacy/` |
| o031 安云加速 | sites=0 · w3a 零引用 · JASS 只有 AzumiHit 的清場 ForGroup（j:27717）| 零生成點的死內容 |

## 二、模型（交付 1）

| 模型 | 動作 | 量測（`tools/w3x-import/out/stock/convert-*.json`） |
|---|---|---|
| `w3x.stock.forgottenonetent` | **新轉**（War3x.mpq，`convert_stock_model.py` 新列） | glb 185,572B · **1/1 prim 出生可見** · 高 11.723 ⇒ doc scale 0.0853 · anims Birth/Stand/Attack/Death · 貼圖 2/3 shape-in-rgb ＋ **Dust5A LUMA-KEY-NEEDED（已知的 emitter 塵霧貼圖，轉換器 fm≥3 分支處理；守衛①只豁免這一張，新的 alpha 病仍紅）** · 3 emitter 全 visible-at-peak |
| `w3x.stock.thunderclapcaster` | **新轉**（war3.mpq 40,937B mdx → 49,308B glb） | **2/2 prim 出生可見**（雷環＋頭頂電光面片幾何都在）· 高 3.851 ⇒ doc scale 0.2597 · anims nothing/stand · 3 貼圖全 shape-in-rgb · 1 emitter visible-at-peak。⚠️ 誠實限制：電光的**材質動畫**（flipbook 閃爍）不在 glb —— 面片在但不閃（mdl-params.md 既有結論） |
| ChaosOrcRange | ⛔ **不轉**（零可見落點，§一） | —— |

## 三、綁定節點（交付 2；⛔ 零行引擎程式）

| 落點 | 節點 | 參數出處 |
|---|---|---|
| godie-zombiex.r（手編 standalone ＋ champion R 鏡射；經 quarantine unlock/lock 正規進出） | orb · **path orbit · count 9 · distance 3** · scale 2 · tint [0.1176,0,0] · lifeSec 8 · soundKey `wc3.parasite` | j:32770-32776（9 具 · 600×600 · sleep 8）；tint＝census（w3u 30,0,0）；**soundKey＝InSpace 自己播的 `gg_snd_Parasite`（j:32766）**，⛔ 不是我發明的聲音 |
| godie-zombiex.ex（standalone-only，champion 只存 id） | orb · scale 2 · tint [0.1176,0,0] · **distance 1.5** | w3a AOsw.Hwe1 L3（u02W scale 2）；distance 1.5 是我挑的（原作召喚物立在施法者旁，⛔ 不在體內 —— §四實測 0.1 全遮擋）；同族聲 `wc3.parasite` |
| godie-edem.e（**改來源** `heroes/godie-edem.py`，⚠️ 等主 session skills:sync 落地） | orb · scale 3.5 · tint [1,0,0] · lifeSec 1 · soundKey `wc3.thunderclapcaster` | census o006 逐格＋TimedLife 1.00 逐字（j:42000） |

## 四、視覺證明（交付 3）✅ `docs/_reports/penta_visual-proof_20260825-2300/`（frames.md＋5 PNG）

calibrate() 自證 **462,400**。⭐ 這一族追加**A/B 像素差分尺**（rig on/off 兩幀逐像素比對，
控制組＝已知可見的 caster 方塊先自證）——聖杯黑泥 census tint [0.1176,0,0] 是**刻意近黑的泥**，
亮像素尺（>96）數學上永遠量不到它，但「黑」的可見性模式是對比，差分尺量得到：
- **zombiex.r**：0 → **9 具 rig 全誕生**（環 r=3），差分 **6,761 px**（control 4,691）→ 8s 到期回收 0
- **zombiex.ex**：單具泥觸手立在施法者旁，差分 **735 px**（control 4,190）→ 2.5s 到期回收 0
- shot1/shot4 目視可辨：暗紅泥觸手（ForgottenOne 造型清楚）
- **⭐ 差分尺當場抓到真缺陷**：EX 節點吃模板預設 distance 0.1 時差分＝**0**——bounding box
  實測整具泥 100% 包在施法者模型內（第一·五守則「說了但不會發生」的形狀，⛔ 所有既有閘全綠）。
  修＝節點寫 `distance: 1.5`。
- ThunderClap 動態證明**待 sync**（edem.e 是產物）：sync 後 `beam-audition.html?ability=godie-edem.e`
  即可補量；靜態半（2/2 prim＋貼圖＋UV＋轉換柵欄）已由守衛①釘住。

## 五、守衛（`packages/shared/src/content/locustPentaFamilies.test.ts`，@visual-proof）

① forgottenonetent/thunderclapcaster glb 靜態可見（≥1 出生可見 prim＋貼圖＋TEXCOORD_0；
  轉換紀錄柵欄：LUMA-KEY 只豁免已知的 Dust5A、emitter 全 visible-at-peak）
② 三個落點擺得出節點（standalone＋champion 鏡射），scale/tint/lifeSec/count/distance
  逐格釘 JASS/w3a 證據，`validateDoc` 過出貨 schema
③ 無落點理由保持成立：census sites 仍 0（e00G/o031）· E00Q/Othr/Ntin 仍未出貨
  （**E00Q 出貨那天這裡紅 ⇒ 聖杯黑泥的綁定要搬回原主**）· A0JE 仍無主人（掃 OBJECTS.json）·
  oshm 生成點仍逐點 ShowUnitHide（**掃真的 war3map.j，找不到生成點也紅**）·
  出貨技能名查無「迴旋爪擊／雷焰聖劍」

- vitest **4 次**（上限 4）：①首跑（唯一紅＝edem 兩列，設計如此）②突變 ③突變訊息確認（浪費的一次，認）
  ④六檔終批（**26 過 1 紅**＝同一組 edem 設計紅；staging/runtimeAlpha/fieldAdoption/QUAD/TORNADO 全綠）
- **突變（承重線）**：拔 godie-zombiex.r 的 spawnModelFx → 紅並逐字指名
  `u02S → content/abilities/godie-zombiex.r.json (standalone)` → 還原（重寫節點＋驗鏡射逐位元一致，⛔ 非 checkout）
- `pnpm typecheck` **1 次**：EXIT=0（全 repo）

## 六、#669 批核頁登記（交付 4）✅

`--id penta_visual-proof_20260825-2300`，abilities＝zombiex.r/zombiex.ex/edem.e。
rollback＝`tpl-locust-orb` → `params.alpha.default` **1（live）⇒ 0（veto）**（orb 家族開關，
與 TORNADO/QUAD 批共用同一格 —— 家族級共用是前兩批已接受的形狀）。
`genrun.sh review:build` 重生成 queue.json；`node tools/review/check.mjs` **exit 0**（15 批全登記）。

## 七、逐檔清單（工作樹，⛔ 本 lane 零 git 寫入）

**改（來源／程式）**
- `tools/w3x-import/convert_stock_model.py` — STOCK_MODELS 加 forgottenonetent/thunderclapcaster 兩列
- `tools/skill-remake/heroes/godie-edem.py` — 45-03 model_fx 第二列（o006 雷環，含證據註解）
- `apps/client/src/vfx/beamAuditionWorld.ts` — PILOT_MODEL_DOCS 加兩份新模型（audition 索引）

**改（出貨內容，經 `product-quarantine.sh unlock/lock --step tiers:apply` 正規解鎖，改完已重鎖 402 份）**
- `content/abilities/godie-zombiex.r.json` ＋ `content/champions/godie-zombiex.json`（R 鏡射，逐位元一致已驗）
- `content/abilities/godie-zombiex.ex.json`（standalone-only）
- `docs/_review/feature-verdicts.json`（register）· `docs/_review/queue.json`（genrun 重生成）

**新**
- `content/models/w3x.stock.forgottenonetent.json` · `content/models/w3x.stock.thunderclapcaster.json`
- `content/assets/models/imported/forgottenonetent.glb` · `thunderclapcaster.glb`
- `tools/w3x-import/out/stock/convert-forgottenonetent.json` · `convert-thunderclapcaster.json`
- `packages/shared/src/content/locustPentaFamilies.test.ts`
- `docs/_reports/penta_visual-proof_20260825-2300/`（frames.md＋5 PNG）· 本報告

## 八、主 session 待收清單 ⚠️

1. **`pnpm skills:sync`**（含 skillremake:json）—— 落地 edem.e 的雷環節點。
   在那之前 `locustPentaFamilies` ② 的 edem 兩列**刻意是紅的**，訊息自帶修法，⛔ 不要改測試。
2. **`pnpm content:build`** —— models `_index` 收兩份新 doc、bundle 重打。
3. **git add 之後 `scripts/visual-proof.sh` 轉綠**（CHANGED 讀 git diff 看不見 untracked
   的證據目錄；frames.md 路徑 match `^docs/_reports/.*visual-proof.*\.md$`—— TORNADO/QUAD 同款狀態）。
4. sync 後可補量 ThunderClap 動態證明：`beam-audition.html?ability=godie-edem.e`（§四寫了步驟）。
5. tiers:apply / apconv:build / caps:export / spec:build：本批零 tier 欄位、零新機制 ⇒ 預期 no-op。

## 九、owner 裁決項（⛔ 不擋出貨 —— #669 頁預設 LIVE，一鍵可否決）

1. **聖杯黑泥綁 zombiex 是我挑的**：census 原主 E00Q（黑化Saber）未出貨；zombiex（聖杯黑泥醬）
   是同一身分的 GGD 原創。E00Q 哪天出貨，守衛③會紅，屆時要裁「搬回去還是兩邊都要」。
2. **EX 泥的 distance 1.5 是我挑的**（0.1 會 100% 藏進英雄體內——實測）。要貼身一點/遠一點是一格數字。
3. **R 環 count 9 用 orb 環取代建議表的 line**：理由量在 j:32770（600×600 自心散布）。
   要忠實隨機散布需要引擎新 path（`scatter`），那是機制票，⛔ 本批不自己發明。
4. **thunderclapcaster 電光不閃**（材質動畫不在 glb）：要閃爍得做 flipbook 支援 —— 既有下一批的活。
5. **oshm 家族**：技能已出貨、dummy 刻意隱形 ⇒ 不綁。哪天要「還原成看得見」是設計新增。
