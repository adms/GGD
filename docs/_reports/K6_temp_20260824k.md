# K6 #641 幻之匕首 20% 傷害對帳 — 2026-08-24

> owner 2026-08-23：「幻之匕首似乎沒有20%傷害」

## 結論：**機制沒有缺口 —— 20% 傷害真的會發生**，兩條攻擊路徑都量到了

真 SimWorld + 出貨內容（非合成 payload），持有者裝 `godie-i039` 自動接敵對打：

| 路徑 | 種子 | 普攻數 | 觸發數 | 每發佔受害者**現有生命** |
|---|---|---:|---:|---|
| 近戰揮擊（BasicAttackSystem:568） | 7 / 4242 / 99 / 31337 | 158 | 8 / 4 / 5 / 3 | **0.1947**（= 0.2 × 護甲減傷 0.9737，逐發一致） |
| 遠程投射物（ProjectileSystem:118） | 7 / 99 | 118 | 6 / 5 | **0.1947**（同上） |

事件 origin = `hook:item:godie-i039#0`，金額 = `hpPct(0.2) × 現有生命 × combatEnv.damageDealt × mitigationMult(護甲)` —— 逐位元對得上，`basis:"current"` 生效（金額隨血量遞減）。

## owner 看不到它的可能原因（機制之外）

1. **3% 機率**：期望 ~33 次普攻才 1 次。短測（例如打 10 幾下）約 6 成機率一次都不出。
2. **沒有專屬視覺提示**：這一發只是一個普通的傷害數字（物理色），與普攻數字混在一起，即使觸發也認不出來。→ 若要「看得見」，是 client 端加專屬浮字/特效的一張新票（本 lane 柵欄外，未動）。

機率 `chance: 0.03` 與 `perRank: [0.2]` 都是 owner 卡面原話（items_GGD CSV：「[On-Hit] 3%機率造成敵方 20%生命傷害」），⛔ 沒有動任何數值。

## 對 JASS 原作

w3x `I039`（parsed/items.json）：主動闇影擊（A103，350 傷）+ 敏捷+40（A0WX+A0ME）+ 攻速+50%（A06R）——**原作沒有這個 proc 也沒有閃避**。GGD 卡是 owner 新版設計（階梯第 1 層），取代考古，屬刻意重製非缺陷。

## 落地

- **新守衛** `packages/shared/src/sim/daggerGodieI039.test.ts`（2 條）：
  ① registry 載入後 hook 形狀仍在（防 Zod 靜默剝欄位）；
  ② 真對打 4000 tick：觸發 ≥2 次、觸發數 << 普攻數（機率是真閘）、每發金額 = doc 的 hpPct × 現有生命 × 出貨減傷（全部因子執行期推導，零字面出貨值）。
- **突變驗證**：`effects/damage.ts` 的 hpPct 加項改成 `+= 0` → 守衛紅（金額比對失敗）→ 已還原（Edit，非 checkout）。
- `content/items/godie-i039.json` **一個位元組都沒改**。
