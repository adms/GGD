# lane DOCS — GH#675 / GH#443 逐條 HEAD 複驗 + 落地（2026-08-27）

> ⚠️ 這是暫存報告（`{用途}_temp_{timestamp}`）。結論已回報，過期就進 legacy。
> 柵欄：`docs/editor-contract/**` · `docs/todo/**` · `docs/_reports/laneDOCS_*` ·
> `packages/shared/src/content/editorCapabilities.ts`＋測試 · `tools/capability-export/**` ·
> `packages/shared/src/ops/laneDOCS*.test.ts`

---

## GH#675 📘 Codex 編輯器契約 —— **AC 兩條已成立，第三條（回饋管道）本 lane 補上**

### AC① 四個新機制都在契約文件裡 —— ✅ **名詞層全部到位**（逐一 grep 過）

| 機制 | 票 | 在哪一份契約 | 證據 |
|---|---|---|---|
| `statusImmunity` | #656 | `ggd-runtime-capabilities.md:200`（參數名清單）＋`.json:350`＋`技能標記機制與效果規則.md:1492/3279/3591` | 有**完整說明**（`_shared.ts:904` 用 `.describe()`，內省讀得到） |
| `at:"bone"` | #565 | `技能標記機制與效果規則.md:2956`（`at` 的 enum 含 `bone`）＋`:2957`（`attach` 骨頭掛點） | enum 值與配套欄位都在 |
| `path:"static"` | #649 | `技能標記機制與效果規則.md:2866`（`path` enum）＋`:2867` `anchor`＋`:2871` `spacing` | 三格一起在 |
| `polarity` | #662 | `ggd-runtime-capabilities.md:185/200`＋`.json:309/639`＋`技能標記機制與效果規則.md:1479/2110/4463` | ⚠️ **名字在，語意不在** —— 見下 |

### 🔴 AC① 的唯一破口：`polarity` 的**推論語意**在契約裡是一片空白

`技能標記機制與效果規則.md:1479` 逐字是：

```
| `polarity` | `buff` / `debuff` | 選填 | — | — |
```

⭐ **最後一格（說明）是空的。** 而 `schema/effects/applyBuff.ts:306–325` 有 **20 行 TSDoc**
寫著三件外部編輯器非知道不可的事：

1. **省略 = 沒有極性，而有方向的淨化拔不到沒有極性的來源**（`clearPools.polarityPasses`
   「不知道不當成是」）—— ⇒ 要被淨化拔得掉，`dispellable: true` 與 `polarity` **兩格都要填**。
2. GH#662 的**例外**：`modifiers` 每一條都明確往下拉時，引擎在掛上去那一刻自動記成
   `debuff`（旋鈕 `config.dispel@1.inferDebuffFromNegativeModifiers`，**出貨 true**）。
3. 推論是**安全網不是單一住處** —— 減益仍然要明寫。

⇒ 契約上是空白 ⇒ 外部編輯器**合理地**不填 ⇒ 產出的減益在任何淨化面前是無敵的，
⛔ **而每一道閘都是綠的**（Zod 收得下、引擎跑得動、不會有任何錯誤）。
這正是 #662 在**我們自己的** 14 份出貨文件上發生過的事，只是換到契約那一側。

### ⭐ 而它不是單一個案 —— 量到的（2026-08-27）

| | |
|---|---|
| 契約參數表的說明格總數 | **803**（有說明 348 · **空白 455**） |
| 空白格中**有明確定義檔**的 | **351** |
| ⭐ 其中**定義檔（或它繼承的 `_shared`/`_hook`）真的有 TSDoc** | **213（60%）** |
| 純粹沒人寫過說明的 | 138 |

⇒ ⭐ **213 格是「我們寫過，但從來沒送出去」。**

**根因（讀碼確認）**：`spec:build` 的說明欄吃 `.describe()`（Zod 內省讀得到），
而 `schema/effects/*.ts` 那一族用 `/** … */` TSDoc。`tools/skill-spec/gen_spec.ts::tsdocFields()`
**有**一條 TSDoc 旁路，但它只對兩個**手動錨定**的檔開（`vfx.ts` / `abilityVfx.ts`）——
其餘整欄空白。`statusImmunity` 之所以有說明，只是因為它剛好用了 `.describe()`。

⚠️ ⛔ **修法在 `tools/skill-spec/gen_spec.ts`（本 lane 柵欄外）** ——
把 `tsdocFields()` 的錨點擴到 `schema/effects/*.ts` 全族。
⛔ **不可以**把說明手抄進 `docs/技能標記機制與效果規則.md`（那是 `spec:build` 的產物，
而且手抄＝第二個住處）。

### AC② 「」對白剝除規則仍在第〇章 —— ✅ **在**

`docs/技能編輯器引擎須知 20260811.md`：
- `## 〇、⭐ 衝突時的優先序階梯`（L103）→ `### 〇.1 第 1 層自己內部的兩條細則`（L135）
- → `#### ② ⛔ 「」裡面是**角色對白**，不是效果`（**L148–166**）
- 含 owner 逐字引言（L150）、三支誤讀實例表（L159）、以及「剝的是**整段**含跨行含行中」的但書（L165）
- 加碼：`〇之二`（L206–207）重申台詞與 `（GGD 註記 …）` 一個字都不會被動到

### AC③ 外部編輯器回饋管道 —— 🔴 **在此之前完全不存在** → ⭐ **本 lane 補上**

量到的：`FieldIssue { path, message, code }`（`packages/shared/src/content/errors.ts:4`）
與 `validateDoc()`（`loader.ts:240`）**早就是機器可讀的**，
⛔ **但整套契約文件裡一個字都沒提過它們**（grep `FieldIssue` / `validateDoc` → 0 命中）。
⇒ Codex 拿得到拒絕，卻沒有任何文件說那個拒絕長什麼樣。

**落地**：`tools/capability-export/export.ts` 新增 `rejectionFormatSection()` → 契約 **§12**。

⭐ **它的每一個位元組都是跑出來的，⛔ 不是打出來的**：拿一份**出貨的**技能文件、
只弄壞一個地方、餵進**出貨的** `validateDoc()`，把它**真的回傳的東西**貼上去。
⇒ 驗證器的訊息一改，`caps:check` 的逐位元組比對當場紅。

六個探針（`REJECTION_PROBES`，決策點寫成一張可加減的表）：

| § | 破壞點 | 契約因此說出的事 |
|---|---|---|
| 12.1 | 拿掉一格必填 | `path` 就是那一格的名字 |
| 12.2 | 引擎沒有的 effect kind | ⭐ 拒絕訊息**自己列出 45 個合法 kind** |
| 12.3 | enum 打錯字 | 合法值也在訊息裡 |
| 12.4 | 多一個不認得的鍵 | ⚠️ `path` 是**空字串**＝這一層物件；未知欄位**不會被忽略**，整份被拒 |
| 12.5 | 數字超界 | 訊息會說出那個界（`maxRank` → `less than or equal to 6`） |
| 12.6 | 🔴 軟參照指到不存在的 id | ⭐ **回 `ok: true`** —— 逐份驗證**不查參照**（參照在整批載入時才解，解不開的隔離理由 `dangling-ref`） |
| 12.7 | — | 通過驗證 ≠ 上得了線（① 參照 ② capability ③ 空 modifier） |

⚠️ 12.6 是這一節最貴的一格：在此之前，一次乾淨的 dry-run 會讓對方**合理地**以為
自己的 `vfxKey` 指得到東西。

⭐ 刻意**不印**基底文件的檔名／內容：對方沒有這個 repo（檔頭「必須自足」），
而且印了會讓一個不相干的內容改動把契約弄 stale。六個破壞點都是**與基底無關**的。

### 🧱 新守衛 —— `packages/shared/src/ops/laneDOCSContractSilence.test.ts`（97 行）

驗**兩個名詞的關係**（出貨 schema ↔ 產生的契約），⛔ 不是「文件裡有沒有那個字」：

| 條 | 驗什麼 |
|---|---|
| ① **校準（兩個方向）** | 餵一份合成的表：有 TSDoc 而說明空白的 ⇒ **抓到**；已經有說明的 ⇒ **不抓** |
| ② **出貨態棘輪** | 沉默格數 ≤ **213**，只能變少。新增一個帶 TSDoc 的欄位而沒送進契約 ⇒ 紅，訊息指向 `gen_spec.ts` |

⭐ **突變紀錄（實跑）**：把偵測空白格的 `desc !== "—"` 改成 `desc !== "@@"`（把量尺弄瞎）
→ **校準那一條紅**，訊息指名 `demoKind.silentField`。改回來。

⚠️ ⭐ **而棘輪那一條在同一次突變下是綠的** —— 瞎掉的量尺數出 0 格，0 ≤ 213。
這正是 CLAUDE.md「一把只驗過單邊的尺不算自證過」：⛔ 只有棘輪的話，
「偵測器壞了」與「契約修好了」量起來一模一樣。校準那一條就是為此存在的。

---

## GH#443 ⭐ 版權規則 —— 🔴 **四層閘 0/4 動過**，而且**全部在柵欄外**

### ① 規則本身（owner 的裁決，逐字，⛔ 不重新詮釋）

> owner 2026-08-19（兩則）：
> 「w3xArtFamilies.ts 的檔頭就是在寫 WarStompCaster，記錄你當初裁決
>  『這一族用參數化原型，⛔ 不出貨模型』…**幫我改掉規則 現在都可以出貨**」
>
> 「我說過**有就用，沒有才用外部 cc0/ccby，再沒有就自己生成**，請記得規則，
>  **我已經全部都可以使用 BZ 了**」

⇒ 順序：**① 原作（w3x/MPQ）→ ② 外部 CC0/CC-BY → ③ 自己生成**。
⇒ 條件（owner 兩次都說）：**註記來源**。

### ② 規則今天落在哪幾個檔（逐檔，HEAD 讀碼）

| # | 檔 | HEAD 現況（逐字） | 判定 |
|---|---|---|---|
| 1 | `apps/client/src/render/vfx/w3xArtFamilies.ts:25-26` | 「WHAT THIS IS NOT. It is not a claim that the shipped art IS `WarStompCaster`. **Those files are Blizzard's and are not in this repo (#81/#116).**」 | 🔴 **未改** |
| 2 | `apps/client/src/render/vfx/w3xAbilityArt.ts:30-31` | 「the art must be a MAP-IMPORTED model (`IN_REPO_*`). **1305 of the 1529 resolved art entries are retail Blizzard `.mdl` paths we cannot ship**」 | 🔴 **未改**（1305 仍被擋，理由已不存在） |
| 3 | `content/assets/blizzard-local/README.md:30-32` | `sounds/` 511 ⛔ 維持原狀 · **`models/` 每單位 glb 40 ⛔ 維持原狀** · imported mp3 ⛔ 維持原狀 | 🔴 **未改**（#402 的放寬只給音訊，L3 逐字寫著「取消了**一部分**」） |
| 4 | `apps/client/src/render/views/blizzardOverlayGate.test.ts`（385 行） | 已翻面成「每一個 Blizzard 位元組都要有出處」，⛔ 但母體只有 `content/assets/audio/wc3/`（音訊） | 🔴 **未擴到模型** |
| 5 | 出處帳本 | `content/assets/audio/wc3/PROVENANCE.json` + `.md`（音訊）· `content/assets/models/scenery-cc0/PROVENANCE.md`（CC0 場景） | 🔴 **模型帳本不存在** |
| 6 | `CLAUDE.md` | grep `CC0` / `CC-BY` / `自己生成` / `資產優先序` → **0 命中** | 🔴 **優先序規則沒進守則** |

⚠️ 票文的 `Files / modules likely affected` 有一處已自我更正且**正確**：
守衛實檔在 `apps/client/src/render/views/`，⛔ 不是票正文寫的 `packages/shared/...`。

### ③ 其他仍在執行舊規則的地方（`grep -rl 'blizzard-overlay|blizzard-local'`，25 個檔）

`.dockerignore` · `Makefile` · `README.md` · `docker/compose.family.yaml` ·
`deploy/helm/ggd/files/nginx.conf` · `tools/deploy/ggd-assets.sh` ·
`tools/model-budget/emit_report.ts`＋`report.test.ts` · `tools/w3x-import/*.py`（8 支）·
`content/config/unit-tints.json` · `content/models/_overlay-hidden-geometry.json` ·
`content/models/_standin-overrides.json` …

⇒ ⭐ **「解禁」不是改一個布林** —— `models/` 一旦進 `content/`，
`_standin-overrides.json`（KayKit 替身）與 `_overlay-hidden-geometry.json` 的存在理由
也一起改變。⚠️ 而票文的 `Known risks` 已經點名了另一個：
「解禁後 .mdx→glb 湧入會壓 #382 的 VRAM 閘」。

### ④ 順序（票文自己寫的，⭐ 讀碼後確認它是對的）

> **Implementation constraints / Known risks**：先開閘後建帳本＝「沒有出處的位元組」自由進場
> ⇒ **順序必須先帳本後開閘**。

⇒ 建議落地順序：**⑤ 模型 PROVENANCE 帳本 → ④ 閘擴到模型（兩方向＋sha256）→ ③ README →
② 可渲染性閘改看位元組出處 → ① 檔頭 → ⑥ CLAUDE.md（先 `backup-rules.sh`）**。

---

## 🚧 柵欄外餘量（⛔ 本 lane 沒動，交給主 session 排）

| # | 要動什麼 | 為什麼不能在本 lane 做 |
|---|---|---|
| A | `tools/skill-spec/gen_spec.ts::tsdocFields()` 錨點擴到 `schema/effects/*.ts` 全族 ⇒ 213 格沉默一次補完 | `tools/skill-spec/` 在柵欄外；且要跑 `spec:build`（全域鎖） |
| B | #443 的四層閘 + 模型 PROVENANCE 帳本 + CLAUDE.md 優先序規則 | 全部在 `apps/` `content/` `CLAUDE.md`（柵欄外） |
| C | `pnpm caps:export`（或 `pnpm skills:sync`）**必須跑一次** | 全域鎖，本 lane 明令禁止 |

## ▶️ 要跑哪些產生器（主 session）

```bash
bash scripts/genrun.sh caps:export     # 或併進下一次 pnpm skills:sync
```

⚠️ 跑之前 `caps:check` **會紅**（`交付物已過期：docs/editor-contract/ggd-runtime-capabilities.md`）
—— 那是**預期的**：§12 是新的一節，來源已改，產物還沒重生成。
⭐ 跑完要 `git add docs/editor-contract/ggd-runtime-capabilities.{md,json}`。
（本 lane 已用 `--out-dir /private/tmp/laneDOCS_out` 驗過產出正確、指紋 `89bae3c0`、
`assertNoInternalLeaks` 零命中。）
