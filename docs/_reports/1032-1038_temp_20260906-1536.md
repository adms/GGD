# GH#1032 ＋ GH#1038 —— 原始碼裡的 NUL ＆ `.gitignore` 吃掉原始碼（lane 報告，2026-09-06 15:36）

> 一條併行 lane，主工作樹，⛔ 未 commit（由主 session 以 pathspec 逐檔收）。

## TL;DR

| 票 | 做了什麼 | 狀態 |
|---|---|---|
| **#1032** | 8 份原始碼的 **15 顆原始 NUL** 全部改成跳脫 `\0`（位元組級 replace ＋ count 斷言 ＋「下一個位元組不是數字」斷言）；閘 `noNulInSources.test.ts`（從檔案系統走，sentinel 自證兩個方向，棘輪今天空的） | ⭐ vitest 綠（閘 2/2 ＋ 4 份直接消費者測試 34/34 ＋ client 29/29 ＋ game-server 5/5） |
| **#1038** | `.gitignore:20 build/` 收窄成 **4 條錨定規則**（`/build/` · `/apps/*/build/` · `/packages/*/build/` · `/tools/*/build/`）＋ `!**/src/build/` belt-and-braces；閘 `gitignoreDoesNotEatSources.test.ts`（正方向 7 個探針 ＋ 反方向從實體走、8 列帶理由豁免表） | `git check-ignore` 七個探針全對 · 反方向 stray = **0** · 閘檔 esbuild 解析 OK ＋ node harness 同邏輯全過；⚠️ **閘的 post-fix vitest 沒有再跑**（3 次額度用完，見「誠實欄」） |

## 基線（動手前量到的，⛔ 不是票文）

- 8 份檔 `grep -c import <檔>` **全部印空**（連 0 都不印 —— macOS grep 把整檔當 binary 靜默跳過）；NUL 顆數逐檔 1/3/1/1/4/1/1/2 ＝ **15 顆**，與票文一致。8 份在 git 裡都是乾淨的（有救得回來的副本 ⇒ 直寫安全）。
- 每一顆 NUL 的**下一個位元組**：`c` `f` `f` `f` `$` `"` `$`×4 `b` `$` `$` `$` —— **沒有一顆接數字** ⇒ `\0` 不會變成八進位跳脫，執行期字串逐位元相同。
- `git check-ignore -v apps/client/src/build/x.ts` ⇒ `.gitignore:20:build/`（被吃）；`apps/client/src/build/` 目錄今天**已不存在**（`buildStamp.test.ts` 已搬到 `apps/client/src/buildstamp/`，且已在 git）。
- 全 repo 真的被 ignore 的 `build/` 目錄**只有一個**：`tools/bgm-gen/build/`（json/wav 產物）⇒ 錨定到 `tools/*/build/` 就夠。
- `.dockerignore` **沒有** `build` 規則（只有 `**/dist`）⇒ 這一次沒有 #1013 那種第二個載體。

## ⭐ 最重要的發現：反方向掃描 —— 被 ignore 吃掉的「原始碼」19,903 個，**全部是合法的**

`git ls-files --others --ignored --exclude-standard | grep -E '\.(ts|tsx|go|py|mjs)$'`：

| 群 | 檔數 | 吃它的規則（`check-ignore -v`） | 判定 |
|---|---:|---|---|
| `tools/icon-gen/.venv/**` | 11,085 | `.gitignore:7 tools/**/.venv/` | ✅ python venv，相依不是原始碼 |
| `voice-reference-pipeline/.venv/**` | 8,093 | `voice-reference-pipeline/.gitignore:1 .venv/` | ✅ 同上 |
| `docs/legacy/_overwrites/**/.claude/worktrees/**` | 700 | `.git/info/exclude:13 **/.claude/worktrees/`（⚠️ 本機 exclude，CI 沒有 —— 但那些檔 CI 也沒有） | ✅ 覆蓋前留底裡夾帶的 worktree 副本 |
| `scratchpad/**` | 14 | `.gitignore:67 /scratchpad/` | ✅ agent 誤寫到 repo 根的暫存 |
| `.backups/**` · `tools/bgm-gen/.backup-task88-*/**` | 14 | `.gitignore:64 .backup*/` | ✅ 整樹快照 |
| **其餘** | **0** | —— | ⭐ **沒有第二個 `buildStamp.test.ts`** |

加上 `.js`（我的閘比票文多掃 `.js`／`.cjs`／`.mts`／`.cts`）之後多出 **286 個 `apps/client/dist/assets/*.js`** —— Vite 產物，⛔ 不是原始碼（repo 裡**零個**手寫 `.js` 被追蹤：`git ls-files '*.js' | grep -v node_modules | grep -v /dist/` ⇒ 0）⇒ 豁免表加 `dist/` 與「產物深度的 build/」兩列。

⇒ ⭐ **收窄之後沒有任何原本被吃的產物跑出來**：`git ls-files --others --exclude-standard | grep '(^|/)build/'` ⇒ 空。

## 改了哪些檔（逐檔）

### #1032 —— 8 份，每份只動 NUL 那幾個位元組（`diff --stat` 8 files, +7/−7；其中 3 份 git 顯示 `Bin` —— ⭐ 那正是票文說的「整檔被當 binary」）

| 檔 | 顆 | 改後的那一行 |
|---|---:|---|
| `apps/game-server/src/net/sanitizeText.test.ts` L17 | 1 | `sanitizeDisplayName("ab\0cde")` |
| `apps/client/src/render/views/blizzardOverlayBoot.test.ts` L63–65 | 3 | `"glTF\0fake-model-bytes-E00R"` · `…Hpal` · `"RIFF\0fake-wav-what-clip"` |
| `apps/client/src/render/views/voxelLook.ts` L240 | 1 | `` fnv1a(`${championId}\0${archetype}`) `` |
| `apps/client/src/audio/spatialStarvation.test.ts` L199 | 1 | `key.includes("\0")` |
| `packages/shared/src/content/refs.ts` L370 · L372 | 4 | `` `${e.field}\0${e.targetCollection}\0${e.targetId}` `` ×2 |
| `packages/shared/src/content/import/manifestPathSegments.test.ts` L62 | 1 | `"a\0b", // NUL —— C 層截斷` |
| `packages/shared/src/voxel/look.ts` L184 | 1 | `` hash32(`${seed}\0${salt}`) `` |
| `packages/shared/src/sim/clearPools.ts` L197 · L241 | 2 | `` `${e.statusId}\0${e.sourceId}` `` · `` `${d.origin}\0${String(d.sourceId)}` `` |

驗證：8 份 NUL 顆數 ⇒ **0**；`grep -c import` ⇒ 2/10/1/10/17/8/4/5（**grep 看得見了**）。

### #1038 —— `.gitignore` L18–31

```
dist/
/build/
/apps/*/build/
/packages/*/build/
/tools/*/build/
!**/src/build/
*.tsbuildinfo
```
（＋ 6 行註解說明為什麼、指向閘。）

| 探針 | 期望 | 實際 |
|---|---|---|
| `apps/client/src/build/buildStamp.test.ts` | 不被吃 | ✅ exit 1 |
| `packages/shared/src/build/x.ts` | 不被吃 | ✅ exit 1 |
| `tools/x/src/deep/build/y.go`（⭐ 這一個只有**收窄**救得了，`!` 救不了） | 不被吃 | ✅ exit 1 |
| `tools/testrunner/internal/coverage/x.go`（#1013 的） | 不被吃 | ✅ exit 1 |
| `build/x.js` | 被吃 | ✅ `.gitignore:24 /build/` |
| `apps/client/build/x.js` | 被吃 | ✅ `.gitignore:25 /apps/*/build/` |
| `packages/shared/build/x.js` | 被吃 | ✅ `.gitignore:26 /packages/*/build/` |
| `tools/bgm-gen/build/x.wav` | 被吃 | ✅ `.gitignore:27 /tools/*/build/` |
| `apps/client/dist/x.js` | 被吃 | ✅ `.gitignore:19 dist/` |

### 新閘（兩條，體驗層，⛔ 不做突變）

| 檔 | 行數 | 驗什麼 |
|---|---:|---|
| `packages/shared/src/ops/noNulInSources.test.ts` | 70 | ① sentinel：tmp 目錄塞一份原始 NUL 的 `.ts` ＋ 一份寫跳脫的 ⇒ 掃到前者、掃不到後者（量尺兩個方向自證）② 從檔案系統走 `apps/ packages/ tools/`（跳 node_modules/.venv/venv/__pycache__/.git/dist/coverage/out/.backup*/backup-*；⚠️ **`build` 刻意不跳**）⇒ 含 NUL 的原始碼紅並指名顆數；非空母體 ≥1000（實測 4,423 份 · 0.47s）；`KNOWN` 棘輪（空）只能變短 |
| `packages/shared/src/ops/gitignoreDoesNotEatSources.test.ts` | 61 | ① 正方向：上表 7 個探針（`git check-ignore -q`，離開碼 0/1，其他擲出）② 反方向：`git ls-files --others --ignored --exclude-standard -z` 的原始碼檔 − 8 列帶理由豁免表 ⇒ 必須是空；非空時用 `check-ignore -v -z --stdin` 指名**哪一條規則**吃了它（實測 39,294 份 · 1.05s） |

## 測試結果

| 跑 | 結果 |
|---|---|
| ① `apps/client`：blizzardOverlayBoot · voxelLook · spatialStarvation | ✅ 3 files / **29 passed** |
| ② `apps/game-server`：sanitizeText | ✅ **5 passed** |
| ③ `packages/shared`：noNulInSources · gitignoreDoesNotEatSources · refs · look · clearPools · manifestPathSegments | 5 files **34 passed** ＋ ❌ `gitignoreDoesNotEatSources` **transform 失敗** |

### 誠實欄：③ 的那個紅是什麼、修了什麼、為什麼沒有第四次

- 紅：`Expected ";" but found "src"` —— 檔頭註解逐字寫了 #1013 的 `` `**/coverage` `` ⇒ 裡面的 `*/` **把 block comment 提早關掉**。⛔ 不是邏輯錯。
- 修：那一句改寫成 `` `coverage/` 規則 `` 並留一行說為什麼不逐字寫。
- 驗（⛔ 沒有第四次 vitest —— lane 額度 ≤3 已用完）：
  1. 用 repo 自己的 `esbuild@0.21.5`（vitest 的 transformer）`transformSync` 兩個閘檔 ⇒ **PARSE OK**。
  2. node harness（`scratchpad/gitignore-harness.mjs`）**同一組** `execFileSync`／regex／`-z --stdin` 四段解析 ⇒ 7 探針全對 · stray 0 · blame 4-tuple 正確。
- ⇒ ⭐ **這條閘在 vitest 底下的第一次綠，會是主 session 收工整包跑的那一次。** 它應該綠；如果紅，責任在這條 lane。

## AC 對照

**#1032** ① 8 份 NUL=0 且 grep -c import >0 ✅ ② 守衛紅並指名、棘輪空 ✅（跑過綠） ③ 既有測試綠 ✅
**#1038** ① `check-ignore` 兩個方向 ✅ ② 反方向掃描 0（286 份 dist .js 逐一歸為 Vite 產物）✅ ③ 閘突變「把 `!` 拿掉」—— ⛔ 沒做（parent：體驗層不做突變）；⚠️ 而且**收窄之後 `!` 對 `src/build` 不再是承重的**（沒有正規則會吃它）—— 它承重的情境是「哪天有人又加回一條不帶錨的 `build/`」。承重的是 `tools/x/src/deep/build/y.go` 那個探針。

## 順手看到、⛔ 沒動的

- `docs/legacy/_overwrites/**/.claude/worktrees/**` 靠 `.git/info/exclude`（本機）被 ignore —— 在別台機器上這 700 份會變成 `??`。不是缺陷，記一筆。
- git 對 3 份改前含 NUL 的檔仍顯示 `Bin 8521 -> 8524 bytes` —— commit 之後這個 diff 會是最後一次「看不到內容」。

## 主 session 收工要做的

```bash
git commit -F msg.txt -- .gitignore \
  apps/game-server/src/net/sanitizeText.test.ts apps/client/src/render/views/blizzardOverlayBoot.test.ts \
  apps/client/src/render/views/voxelLook.ts apps/client/src/audio/spatialStarvation.test.ts \
  packages/shared/src/content/refs.ts packages/shared/src/content/import/manifestPathSegments.test.ts \
  packages/shared/src/voxel/look.ts packages/shared/src/sim/clearPools.ts \
  packages/shared/src/ops/noNulInSources.test.ts packages/shared/src/ops/gitignoreDoesNotEatSources.test.ts \
  docs/_reports/1032-1038_temp_20260906-1536.md
```
