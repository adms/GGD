# GH#992 Main lane（後台）—— 2026-09-06 第二波報告

> ⚠️ 這是 lane 報告，⛔ 不是規格。票的現況以 `bash scripts/ticket-progress.sh read 992` 為準。

## 做了什麼（六件，逐條對票文）

### ① 手寫欄位清單逐族退場 —— 棘輪 **963 → 664**

- **量到今天**（開工先量，⛔ 不引票文）：71 份 spec · 963 格手寫標籤，全部欠 `@zh`；其中 799 格欠 `note`、248 格 `optionLabels`、27 格 `pattern`、33 格補上下界。
- **一支 TS-AST codemod**（scratchpad，一次性）：對每一格手寫標籤，在 Zod 來源檔找到那顆葉子，`.describe("@zh …\n@note …\n@opt …")` 貼上去；既有的無指令 `.describe()` 與手寫 `note` **取聯集**（兩邊各帶對方沒有的資訊，⛔ 不挑一邊），複述出貨值的句子換成 `{{出貨值}}`（`configFormsShippedProse` 才不會紅）；spec 元素上的行註解跟著搬到 Zod 那一格上方（`// [spec] …`），⛔ 知識沒有無聲消失。
- **313 格**搬進 44 份 `schema/config/*.ts`；44 份 spec 的 `fields: [...]` 改成 `derivedFields(zod, [覆寫])`（新函式，`apps/admin/src/configForms/schemaToForm.ts`）。覆寫只剩 Zod 給不出的那一半：`pattern`/`patternError`（27）與補上下界（33）—— zh/note 仍住 Zod。
- **顯示順序 ＝ Zod 宣告順序**（或 `@order`）。⚠️ 這是一個決定：14/71 份的舊手寫順序與 schema 不同，現在跟 schema 走 —— 順序只住一個地方。
- 棘輪 `adminFormsHandWrittenRatchet.test.ts` `BASELINE 963 → 664`，雙向不變；檔內逐類寫了剩下 664 格為什麼還在（見下面「沒做到的」）。

### ② 新增一份 config 只需要 Zod ＋ 一行

- **`specFromZod(zod, page)`**（`schemaToForm.ts`）：整份 `ConfigDocSpec` 從 Zod 推導 —— 根節點 `@title` / `@intro`（可重複）/ `@consumer` / `@effect` / `@nav <emoji> <分組> <標籤>`，分支 `@preserve <為什麼>`；`docId`/`schemaTag` 從 `schema: z.literal("config.x@1")` 讀，⛔ 不再打第二次。`page` 由呼叫端那一行給（TS 拿不到執行期字串的字面型別，而它同時是註冊表的順序＝決定）。
- **四份整份走 `specFromZod`**：wounds · shield · augment-filter · controller-scheme。
- **左欄那一列自動長出來**：`ConfigDocSpec.nav?`（engine.ts）＋ `withDerivedConfigRows(HAND_NAV, CONFIG_DOC_SPECS)`（`ui/navGroups.ts`）⇒ `App.tsx` 的 `NAV` ＝ 手寫 ＋ 推導；手寫的列**贏**（位置是決定）。第一個走這條路的是 **手把操作版本**（`App.tsx` 那一列刪了，它曾是「唯一還要手加的接線」）⇒ 它現在排在「系統」的最後（⚠️ 位置變了，一格之差）。
- **`navSections.test.ts` 改成從推導驗**：`BASELINE_PAGES` / `SINCE_BASELINE` / `APPROVED_MOVES` 裡的 **127 個設定頁條目刪掉**（含它們的逐列註解 —— 那些理由的家在 `App.tsx` 那一列），新一條 `⭐ 設定頁從註冊表推導：每一份 spec 都在 NAV 上，而基準線裡一個設定頁都沒有`（兩個方向）。⇒ `berserk@1` 那種「兩個住處」不再存在。
- **`configDocCoverage.test.ts`** 的「導覽列一列」從掃 `App.tsx` 字串改成問**真的那份 `NAV`**（推導出來的列在舊版會被誤報成「沒有那一列」）。

### ③ 後台的節點編輯 —— 🧩 技能積木（`abilityNodes`，鑄技工坊那一組）

- `apps/admin/src/abilityNodes.ts`（邏輯，無 React）＋ `ui/AbilityNodesPage.tsx`（視圖）＋ `abilityNodes.test.ts`。
- **效果清單**：選一支技能（`/content/abilities/_index.json` ＋ bundle 名稱索引）→ 底是線上生效的那一份（overlay ?? 出貨）→ 加／刪／↑↓ 排順序。
- **每顆積木一張表單**：`zEffectDefUnion.optionsMap.get(kind)` 走**同一支** `walkZod`（depth 3），純量畫輸入框（`@zh`/`@note`/`@opt` 有就用，沒有印路徑，⛔ 不編一個像中文的名字），走訪器歸成分支的東西誠實地畫 JSON 框。
- **積木來源只有 `ggd-bricks.json`**（`docs/editor-contract/`，#989）：`EFFECT_BRICKS`（47 顆）；閘 `abilityNodes.test.ts` 逐顆對出貨 union **兩個方向**。
- **儲存**走 `putOverlayDoc("abilities", …)`（Zod 閘在那一支裡）；存前 `validateAbilityDoc` 同一道閘先擋。
- **試放**借 `apps/editor/src/preview/PreviewController.ts` 的 `createSimPreviewController()`（`editor:accept` 的 sim-preview harness，⛔ 沒有另寫渲染）：`ensureRegistries()` 把 bundle ⊕ overlay 走 `registerAll`（與 shard 同一條）→ `castAbility` / 被動走 `triggerPassiveAbility` → 畫面印事件型別×次數、魔力、冷卻、accepted/reason。**⚠️ 試放的是存檔之後線上生效的那一份，⛔ 不是草稿**（畫面上的按鈕文字就這樣寫）。
- **像素那一半：鏈路已接上，⛔ 未驗收** —— 只給 `/editor/vfx-forge?qa=accept-46&ids=<id>` 的連結（`capture-browser-proof.mjs` 開的同一條路），沒有 `@visual-proof`。
- 接線：`store.ts` `Page` union ＋ `SESSION_REQUIRED_PAGES`、`App.tsx` import／NAV 一列／route、`navTags.ts` 一列。

### ④ packet 給 Codex

`docs/editor-contract/coordination/question.effect-graph-bridge.json` —— `coord:check` 綠（9 份 0 缺）。
⚠️ **票文要的檔名 `feature-request.…` 不合法**：`tools/coord/schema.mjs` 的 `KINDS` 沒有 `feature-request`，而 `dedupeKey` 必須以 `<kind>.` 開頭 ⇒ 用 `question`（packet 的 `asks[]` 最後一條寫明了這件事）。`unblocks[]` ＝ 18 份 `skill-forge-effect-graph`（從驗收包 JSON 讀）；`contractFingerprint afc7c6a573a673f5`；`baseCommit ddcb3c1b8`。三條 claims 各帶可重跑的 repro；asks 含「Codex 表單要解析同一套 `.describe()` 行首指令」。

### ⑤ 守衛 ＋ 突變（體驗層各一條，接線類各一次）

| 守衛 | 問什麼 | 突變（2026-09-06 驗過） |
|---|---|---|
| `schemaToForm.test.ts` ④ | 拿掉一個 Zod 欄位 ⇒ 表單少一格；覆寫合併進同一格 | `derivedFields` 的 `{ ...f, ...o }` → `f` ⇒ **紅** |
| `abilityNodes.test.ts` ② | damage 有 damageType 下拉；`.omit()` ⇒ 少一格 | `formFromSchema` 物件不遞迴 ⇒ **紅** |
| `schemaToForm.test.ts` ② | 量尺兩個方向：對照組改成**合成**的無描述葉子（feel-fx 搬走後出貨裡沒有對照組了） | —— |
| `navSections.test.ts` 新條 | spec ⊆ NAV；手寫清單 ∩ 設定頁 ＝ ∅ | —— |

### ⑥ 收尾

- `cd apps/admin && npx vitest run`：**130/132 檔綠、1320 過**；2 紅見下。
- `pnpm typecheck`：**apps/admin 0、packages/shared 0**；`apps/editor` 224 個 error 全在 `vfx-forge/*`（`VfxScriptDoc` / `vfx-script@2`）—— 別條 lane 的 `schema/vfxScript.ts` WIP，⛔ 不是本 lane。

## 量到的

| | 動手前 | 現在 |
|---|---:|---:|
| 手寫欄位（棘輪） | 963 | **664** |
| 引用 `.describe()` 指令的 config schema | 6 份 | **50 份** |
| 整份由 Zod 推導的 spec | 0 | **4** |
| `App.tsx` 要手加的設定頁接線 | 1（controllerScheme） | **0** |
| `navSections.test.ts` 裡的設定頁條目 | 127 | **0** |
| 後台開得了的積木 | 0（`ggd-bricks.json` gaps 119 全是 `adminForm`） | 47 顆 effect 可加可填（⚠️ 清冊還沒重量，見下） |

## ⛔ 沒做到的（誠實）

1. **664 格還在手寫**，三類：
   - Zod **不在柵欄裡**（`schema/` 上一層，⛔ 不是 `schema/config/`）：map-spec 34 · displacement-tiers 30 · victory-podium 13 · ranking 11 · practice 10 · new-hero-checks 9 · icon-style 8 · toggle-ability 5 · audio-mix 3 · cast-approach 3 · mitigation 1 ＝ **127**。
   - 標籤由**迴圈／模板**產生而 Zod 那側是 `Object.fromEntries` 或共用子物件（`zPointCue`/`zLineCue`/`zTelegraphChannelStyle`…）：stat-normalization 245 · 五級距家族 76 · ap-coefficient 30 · authoring-rules 29 · world-cues 39 · range-guide 17 · ambient-vfx 11 · rank-growth 7 · arena-rules 21 ＝ **475**。正解＝把 spec 裡的模板函式（`rungWhy`、`DAMAGE_TIER_WHY`、`generatedNormalizationFields`…）搬進 schema 檔當 describe 模板（#1001 在 `apCoefficient.ts` 的 `zByTier(…, describe)` 就是這個形狀）。⚠️ stat-normalization 刻意**沒動**：另一條 lane（GH#1024 A4）同一時間在改 `statNormalization.ts`（`roleFromOrigin`）。
   - Zod 給不出的：`pattern` 27（走訪器不帶 regex，`apps/editor/src/form/walk.ts` 是別條 lane 的）· 補上下界 33（arena-rules；正解是寫回 Zod，⛔ 柵欄不准改上下界）。
2. 「後台能用表單拼出一支含 ≥3 顆積木的技能並預覽，存檔後 `content:build` 綠」—— **鏈路已接上，⛔ 未驗收**：⛔ 沒有開瀏覽器點過（無 dev server；`AbilityNodesPage` 只有 tsc ＋ 邏輯層測試），試放只回事件不回像素，`content:build` 本 lane 禁跑。
3. `newEffect()` 對含 `zRef`／union 欄位的積木（如 `applyStatus.statusId`）給不出合法起始值 —— 畫面上靠紅字擋在存檔之前（同一道 `validateAbilityDoc` 閘），測試只驗 damage/heal。

## 需要主 session 動的（柵欄外）

| 檔 | 為什麼 |
|---|---|
| `pnpm bricks:build`（`docs/editor-contract/ggd-bricks.json` 三份） | `bricks:check` 已經紅（誰動的分不出來：另一條 lane 的 `roleFromOrigin` 多一葉，或本 lane 的 Zod）。⚠️ **`adminFormSource` 仍然只看 `CONFIG_DOC_SPECS[].collection`** ⇒ 119 個 `adminForm` gap 不會因為技能積木頁而翻綠 —— 要在 `tools/brick-census/bricks.ts` 的 `adminSurface()` 把 `apps/admin/src/abilityNodes.ts` 的 `EFFECT_BRICKS`（`abilities` collection）算進 `collections`（一行 `collections.add("abilities")` 帶出處）。 |
| `tools/coord/schema.mjs` `KINDS` | 若真要 `feature-request` 這種 kind，加進去再把 packet 改 dedupeKey；⛔ 本 lane 沒改。 |
| `apps/admin/src/configForms/specs/stats.ts` ↔ `statNormalization.ts` | 另一條 lane 的 `roleFromOrigin` 讓 `configForms.test.ts`「恰好一筆標籤」紅（244 vs 245）—— ⛔ 不是本 lane 造成，等那條 lane 補標籤（正解：Zod 已有 describe，補 `@zh` 後那一格由 `derivedFields` 推導）。 |
| `packages/shared/src/content/schema/{mapSpecDoc,displacementDoc,victoryPodium,rankingDoc,practiceDoc,iconStyleDoc,toggleAbilityDoc,audioMixDoc,mitigationDoc}.ts` ＋ `content/newHeroChecks.ts` | 127 格的 Zod 住這裡；同一支 codemod（scratchpad `codemod.mjs`）改 `SCHEMA_DIR` 就能跑，⛔ 本 lane 柵欄不含。 |
| `apps/editor` | 224 個 tsc error（`vfx-script@2`）是別條 lane 的；Codex 側收 `question.effect-graph-bridge` packet。 |

## 順手發現但沒動的

- `configFormsShippedProse` 只掃 `spec.fields[].note` —— `specFromZod` 的 `intro[]` 段落不在它的分母裡（今天 intro 裡沒有「出貨 N」的句子，但沒有閘）。
- `PreviewController.castAbility` 的 `definition` 參數可以吃草稿，但把 authoring JSON 變成 runtime `AbilityDef` 的那一段（`apps/editor/src/forge/resolveRuntimeDraft.ts`）是 Codex 的；後台若要「試放草稿」要借它，⛔ 不要在 admin 再寫一條展開路。

## 檔案

**新增**：`apps/admin/src/abilityNodes.ts` · `apps/admin/src/abilityNodes.test.ts` · `apps/admin/src/ui/AbilityNodesPage.tsx` · `docs/editor-contract/coordination/question.effect-graph-bridge.json`
**改**：`apps/admin/src/configForms/{schemaToForm.ts,schemaToForm.test.ts,engine.ts}` · `apps/admin/src/configForms/specs/*.ts`（14 份：arena audio authoring combat hudLayout itemCard lobby ops render stats tiers ugc ui visuals）· `apps/admin/src/{store.ts,navTags.ts,navSections.test.ts,configDocCoverage.test.ts}` · `apps/admin/src/ui/{App.tsx,navGroups.ts}` · `packages/shared/src/content/schema/config/*.ts`（44 份，只加 `.describe()`）· `packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts`
