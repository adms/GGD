# K1 #636 傷害排行榜(Redis 十萬筆)—— lane 報告 2026-08-24

owner:
> 「顯示被哪個人哪招傷害最高 比例分布 並在後台顯示」
> 「可以用 Redis 做,排名可以容納十萬筆,並且記錄第幾回合、英雄、裝備、技能、傷害、時間戳記、版本」

## 做了什麼(全部在柵欄內)

| 檔 | 是什麼 |
|---|---|
| `packages/shared/src/sim/stats/matchLedger.ts` | 新增純推導 `topDamageCasts(snap, limit)`:一場的 top 單發(單位=一次施放,封包沒有 abilityId 可歸因)。round/seatId/championId(從 picks)/abilityId/slot/damage/tick/castId/items(施放當下持有,從 itemTxns 折算)。決定性:tie-break castId,items 升冪,無時鐘。 |
| `apps/game-server/src/stats/redisCommands.ts` | 依賴零的 RESP request/response client —— **重用** `config/redisSubscriber` 匯出的 `encodeCommand`/`decodeResp`(一個 codec,不分岔)。錯誤 reject 不掛住。 |
| `apps/game-server/src/stats/damageBoard.ts` | 收斂點。`publishMatchDamageBoard(snap)`:host 補 ts(`Date.now`)與 version(`buildStamp()`),`ZADD ggd:damage-board:v1`(score=damage,member=JSON,`matchId+castId` 唯一鍵)+ `ZREMRANGEBYRANK 0 -(cap+1)` 修剪尾端。**永遠 fail-open**(自帶 5s deadline,永不 throw)。讀側 `readDamageBoard`(ZREVRANGE,壞 member 跳過)+ `serveDamageBoard(res)` 給 `/_internal/damage-board` 一行掛載。 |
| `apps/admin/src/damageBoard.ts` | 頁面純邏輯:normalize / 過濾(英雄/技能/版本)/ 佔比(**分母=目前過濾結果的傷害總和**,頁面印出計算基礎)/ 分頁。 |
| `apps/admin/src/damageBoardPage.tsx` | 唯讀頁:前 1000 筆、50/頁分頁、三格下拉過濾、每英雄佔比橫條。 |

## 開關(owner 常設:留一鍵 rollback)

`GGD_DAMAGE_BOARD=0` 整個關;`GGD_DAMAGE_BOARD_CAP`(預設 100_000)、
`GGD_DAMAGE_BOARD_TOP`(每場寫入上限,預設 100,我挑的)。
⚠️ server-ops 三住處的後台欄位在柵欄外,主 session 要的話再補(語意不變)。

## 守衛

- `apps/game-server/src/stats/damageBoard.test.ts`(123 行):夾具走**出貨的** `MatchLedger`;fake client 模擬 zset rank 語意。驗:top 排序+七格 payload+施放當下道具、寫入後修剪到 cap、fail-open(壞 Redis 回 false 不 throw、開關關掉連 factory 不碰)、讀回跳過垃圾 member。
- `apps/admin/src/damageBoard.test.ts`(57 行,體驗層薄守衛):佔比分母=過濾結果、壞列不清空整頁。
- **突變**:拿掉 `writeDamageBoard` 的 ZREMRANGEBYRANK 一行 → 「writes then TRIMS」紅 ✅(已還原)。

## ⚠️ 柵欄外的接線(主 session,共 4 處,各 1–2 行)

1. `apps/game-server/src/rooms/MatchRoom.ts` ~L1174/1193(`statsRecorder.finish` 旁):`void publishMatchDamageBoard(this.ctl.ledger.snapshot());`(⛔ 不要 await 在關鍵路徑)。
2. `apps/game-server/src/index.ts` httpServer:`if (req.url?.startsWith("/_internal/damage-board")) { void serveDamageBoard(res, {…parse offset/count…}); return; }`(與 `/_internal/replays` 同佈局:非公開,platform proxy 進來)。
3. platform(Go):`/admin/damage-board` → proxy 到 game-server 或直接用 redisx 讀同一個 zset key `ggd:damage-board:v1`。
4. `apps/admin/src/ui/App.tsx`:import `DamageBoardPage` + nav 一格(已知共用檔,兩行)。

沒接線之前這是失敗形態②(算出來但沒送達)—— 已在此列明,⛔ 不是留給記憶。

## 測試帳

vitest 3 次(初綠/突變/修 strict 後確認)、tsc 1 次(workspace 全跑;我的檔 0 錯。
⚠️ 既有紅:`sim/mobs.ts normalMobShadow`、`client mobBoss*`、`GameApp.ts` —— 別的 lane 在飛的檔,非本 lane 造成)。
