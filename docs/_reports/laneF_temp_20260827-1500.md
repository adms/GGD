# lane F — 內容補完小批次（#745 · #740 · #751 · #739）

2026-08-27。HEAD 起點 `146ddb99`。⚠️ 四張票**逐條在 HEAD 上讀碼複驗過** —— 其中 **#740 有兩項已經是舊聞、第三項是假前提**。

---

## 🎫 #745 近戰武器 tag 補完 —— **部分落地**（29 隻 → 8 隻補完 · 3 隻被誤分類擋住 · 18 隻缺類別）

### 盤點（2026-08-27 重掃 `content/champions/*.json`，`attackType==="melee"` 且無 `WEAPON_TAGS` 成員）

**29 隻**，與票文一致。逐隻對照角色原作武器（保真層）之後分成三堆：

#### ⭐ 已補（8 隻，本次 commit）

| champion | 角色 | 依據（原作武器） | tag |
|---|---|---|---|
| `godie-e001` | 龍宮禮奈 | W「22-02 **染血的柴刀**」＝鉈（單手刃器） | `sword` |
| `godie-e00n` | 龍宮禮奈（變身對子另一半） | 同上 —— ⭐ **兩邊一起動** | `sword` |
| `godie-hpb1` | 蒼月潮 | 名號「**獸矛**傳承使」＝獸の槍（雙手長柄） | `greatsword` |
| `godie-hvsh` | 梅杜莎 Rider | Q「48-01 **魔法鎖鏈**」＋原作釘（鎖鏈短刀，刃器） | `sword` |
| `godie-n00b` | 哆拉A夢 | Q「57-01 空氣砲」/ R「57-04 竹蜻蜓」＝口袋**道具**（投擲物） | `thrown` |
| `godie-o02l` | 皮卡丘（神騎寶貝） | Q「58-01 十萬伏特」＝電擊（conjured bolt） | `magic` |
| `godie-o02p` | 初音 | Q「99-01 **甩蔥歌**」＝手持揮擊 | `sword` |
| `godie-ubal` | 巴恩大魔王 | Q 凱薩之鷹 / E 黑核晶 ＝全魔法 | `magic` |

判準（我自己定的一條規則，⛔ 不是逐支感覺）：
**雙手長柄／巨型 → `greatsword` · 日本刀 → `katana` · 其餘單手刃器 → `sword` ·
投擲道具 → `thrown` · 能量/電擊/法術 → `magic`。**

#### 🚫 被 **sync-io 誤分類**擋住（3 隻）—— ⭐ **這是本 lane 最重要的發現**

`godie-e00r`（初號機 → `sword`，W「59-02 高週波短刀」）·
`godie-edem`（佐助 → `katana`，草薙劍）·
`godie-h01u`（呂布 → `greatsword`，E「80-03 鬼神烈戟」）

genguard 判它們是 `skillremake:json` 的**產物**並擋下（且隔離區 `chmod 444`）。
⭐ **但逐行讀 `tools/skill-remake/batch1.py:358–368` 之後，那個分類是錯的：**

```python
ch = json.load(open(p, encoding="utf-8"))     # ← 讀既有的英雄卡
for s in ("Q", "W", "E", "R"):
    if s in slots:
        m = dict(slots[s]); m.pop("schema", None)
        ch["abilities"][s] = m                # ← ⭐ 只換這一格
json.dump(ch, f, ...)                          # ← 其餘每一個欄位原樣寫回
```

⇒ 它對 champion 檔是 **read-modify-write 的正規化器**（只換 `abilities[QWER]`），
⛔ **不是作者** —— `tags` / `baseStats` / `passiveAbility` … 全部原樣保留。
（對 `content/abilities/*.json` 那一半它**確實是作者**，⛔ 兩半不同性質。）

⚠️ ⭐ **這是 CLAUDE.md 已經記過兩次的同一個病的第三個實例**：
`skillremake:provenance`（glob 灌大 492 份）· `speedtiers:build`（glob 認領 71 份）·
**現在是 `skillremake:json` 的 champion 那 16 份**。
⇒ 症狀一模一樣：**改產物被擋／改來源沒有來源** 的死路 ——
批次改任何一個非 `abilities` 欄位（tag、模型、tint、playstyle…）今天在這 16 隻身上都做不到。

**要動的檔**：`tools/parallel-gates/sync-io.json`（⛔ 柵欄外）——
把 `skillremake:json` 的 `content/champions/*.json` 那 16 條標成正規化器
（＝ genguard 會改回「不擋你」）。改完之後這三隻各是**一行**。

#### ⛔ 出貨詞彙表描述不了（18 隻）—— ⭐ 缺的是**機制**不是內容

`WEAPON_TAGS = greatsword | katana | gun | bow | magic | thrown | sword` ——
**沒有「拳」也沒有「爪」**。

| 缺的類別 | 擋住幾支 | 是誰 |
|---|---:|---|
| **`fist`**（徒手武術） | **12** | ewar 天地志狼 · h02k 熊貓 · huth 普烏 · o00x/ogrh 悟空 · u00l/umal 拳四郎 · u00n/u00o 魯夫 · u00v 基廉列克 · u034/ucrl 傑富力士 |
| **`claw`**（獸類爪牙/藤鞭/啃咬） | **5** | h02r/hgam 妙蛙花+種子 · h02u/h02v 草泥馬 · zombiex 喪標麥可 |
| （另）`thorne` | 1 | 見下 |

⛔ **我刻意沒有拿現有的 7 類去湊。** 把拳四郎標成 `sword`，
會把一個錯的聲音從「**意外**」升級成「**決定**」—— 而 `combatSfx.ts` 的檔頭
逐字寫著這正是它存在的理由（「a class with no row falls back by ACCIDENT
instead of by decision」）。CLAUDE.md 第一守則：⛔ **禁止「用現有參數湊一個看起來像的」**。

**要動的檔（⛔ 柵欄外，各一行）**：
1. `packages/shared/src/sim/systems/BasicAttackSystem.ts` → `WEAPON_TAGS` 加 `fist`（＋`claw`）
2. `apps/client/src/audio/combatSfx.ts` → `WEAPON_SFX` 各補一列。
   ⭐ **沒有拳擊音源時，照 `thrown` 的先例明寫 `GENERIC_SWING`** ——
   「明說的中性揮空聲」比「意外落到刀刃斬擊」誠實，而且那一列就是日後音源進來的落點。
3. 兩類落地之後，這 18 隻的 tag 是一張 18 列的表（⛔ 不是 18 輪）。

#### 🚫 `thorne`（第 29 隻）

`packages/shared/src/sim/content/skeleton.ts:224` 的 `THORNE.tags` 字面值
**必須與 `content/champions/thorne.json` 逐位元一致**（`loader.test.ts` round-trip，
檔內註解逐字寫著）。`skeleton.ts` 在柵欄外 ⇒ 兩邊要同一次動。
⚠️ thorne **不在上架白名單**，⛔ 不影響票的驗收標準。

### 守衛（一條，突變驗過）

`packages/shared/src/content/laneFMeleeWeaponTags.test.ts`（2 條斷言）

- ① 每一隻近戰要嘛有 tag、要嘛在豁免表裡**帶著理由**
- ② ⭐ **棘輪**：`NEEDS_CLASS` 每一列指名一個**還不存在**的類別 ——
  哪天 `WEAPON_TAGS` 長出 `fist`，那 12 列**當場變紅**逼下一輪補完；
  補了 tag 卻忘了刪列也紅 ⇒ 豁免表**只能變短**。
- `WEAPON_TAGS` 從出貨的那一支 `import`，⛔ 不抄字面值（⛔ 第四個住處）。
- ⛔ 出貨數值（29 / 8 / 12 …）**一個都沒有寫進斷言**。

**突變紀錄**（一次跑，兩個方向）：
① 拿掉 `godie-o02p` 的 `sword` → ①紅，訊息指名 `godie-o02p (夢幻之星 - 初音)`
② 把 `NEEDS_CLASS["godie-ewar"]` 從 `fist` 改成 `sword`（＝假裝類別已存在）
→ ②紅，訊息逐字說「WEAPON_TAGS 已經有 'sword' 了 ⇒ 補 tag 並刪這一列」
兩處都用 `Edit` 改回（⛔ 不是 `git checkout`）。

### 🔄 rollback（owner 一鍵回頭）

每一個 tag 就是英雄卡 `tags` 陣列裡的**一個字串**。
拿掉 ⇒ 那一隻立刻回到 `weaponClassOf` 的預設，⛔ **不必重建映像**
（`content/` 是 live bind-mount）。改完把該 id 補進守衛的 `NEEDS_CLASS`/`BLOCKED` 即可。

---

## 🎫 #740 內容補完小批次 —— **三項裡兩項已是舊聞、一項是假前提**

### ① 4 支閃避 modifier → ⭐ **3 支早就做完了，只有 1 支是真的**

| doc | 票文說 | HEAD 實況 |
|---|---|---|
| `godie-hvsh.w` 心眼 | 缺 evasion | ⭐ **已有** `evasion flat 0.12/0.18/0.24/0.30`（4 階） |
| `godie-u00l.w` 轉龍呼吸法 | 缺 evasion | ⭐ **已有** `0.07/0.11/0.15/0.19` |
| `godie-umal.w`（拳四郎對子） | 缺 evasion | ⭐ **已有**，與 u00l 逐值一致 |
| `godie-u00j.passive` 74-00 JENOVA | 缺 evasion | 🔧 **真的缺** —— 只有 `critChance 0.15` + `critDamage 1.25` |

⇒ **本次只補 `godie-u00j.passive` 一支**：加 `{"stat":"evasion","op":"flat","value":0.15}`，
對上描述「有 **15%** 的機會閃避攻擊」（數值照描述字面，⛔ 我沒有自己挑）。

⭐ **順帶查證（差點誤報成缺陷）**：同一段描述說「15% 機會造成攻擊 **3 倍**的傷害」，
而 JSON 寫的是 `critDamage flat **1.25**` —— 看起來像謊話。
實際：`Stat.CritDamage` 的**基底是 1.75**（`BasicAttackSystem.ts:449`、`effects/damage.ts:165`
等五處 `|| 1.75`）⇒ `1.75 + 1.25 = **3.0**` ＝ 正好 3 倍。⭐ **那一格是對的，⛔ 不要動它。**

⚠️ **同一句話裡有一個真的空宣稱**：「**無視對方迴避能力**」——
出貨 modifier 沒有任何東西表達它（引擎也沒有「無視迴避」這個機制）。
⛔ 我沒有順手修（第零守則⑧）⇒ **建議開票**（第一·五守則：卡面不可以有說了不會發生的字）。

### ② `godie-ogld` 天生技 → ⛔ **假前提。⭐ 這支英雄在原作裡就沒有天生技。**

⭐ **三條互相獨立的證據**（⛔ 不是複述 README 的那一句散文）：

1. **整張地圖沒有 `72-00`**。`ABILITY_W3A.json` 裡 72-* 共 **8 支**：
   `72-01洗刷刷` · `72-02 黑人牙菌斑`(×2) · `72-03 超亮白` · `72-04 黑化` ·
   `72-05 億萬星殞落` · `72-002 億萬星殞落`(EX) · `72-02-x 抗魔抗穿刺`。**沒有 00。**
2. **他的英雄物件第 5 格是 Blizzard 的內建技**。`OBJECTS.json` 的
   `Ogld.hero_abilities = ["ANmo","ACpa","Afae","A0CO","**Aamk**"]`，
   而 `Aamk` ＝「00-屬性提升」（Attribute Modifier Skill，增加力/敏/智各 2 點）——
   ⭐ **全圖 111 位英雄都帶著它**，⛔ 它不是任何人的天生技。
3. **`72-02-x` 不是他的**。`A0H1「72-02-x 抗魔抗穿刺」` 掛在
   `o02R 成熟的牙菌斑` 身上 —— 那是他 **W 召出來的小兵**，⛔ 不是英雄本體。
   （另一個 `A0YO「00-00 抗魔抗穿刺」` 沒有任何單位帶它。）
   JASS 側：他四支主技在 `jass-spells/INDEX.details.json` 全是 `no-trigger-handler`，
   單位層也沒有天生技觸發器。

⇒ ⭐ **`content/abilities/godie-ogld.passive.json` 不該被「補」出來。**
⛔ **我也不會替他發明一支** —— 出貨的 `provenance` 詞彙只有兩個值
（`w3x-import` 330 份 / `owner-spec` 91 份）。發明一支就只能標成 `owner-spec`，
⭐ 而那是**替 owner 背書一個他從沒做過的決定**（CLAUDE.md「我的推測會變成他的需求」）。

**⇒ 兩條合法出路，都在柵欄外，要 owner 或主 session 挑：**
- 🅐 **owner 給一份 72-00 新版規格**（第〇·六守則第 1 層贏過考古）⇒ 照抄落地
- 🅑 **`audit_hero.py` 第 2/5 列給 `godie-ogld` 一個具名豁免**
  （理由＝上面三條證據）⇒ 稽核回到 16/16 而**不製造假內容**
  ⚠️ `tools/hero-onboarding/` 不在本 lane 柵欄內（柵欄寫的是 `tools/hero-audit/**`，
  那個目錄不存在）

⭐ 附帶：**「q 名稱缺空白」這一項也不必做了。**
`audit_hero.py:129` 的正則早就從 `\s` 放寬成 `([^0-9]|$)`，
⇒ `72-01洗刷刷` **今天解析得出來**（實跑確認第 5 列只剩 `無法解析 passive`）。
⚠️ 而 w3x 原始名稱 `ANmo` 本身就叫「72-01洗刷刷」，**沒有空白是保真的**。

### ③ `audit_hero.py` `championPrices` KeyError → ⭐ **已修，commit `496d2a37`（那個 commit 自己就掛著 `#740`）**

實跑 `python3 tools/hero-onboarding/audit_hero.py godie-ogld` → **12/16**，
4 個 FAIL 全部源自「沒有 passive 文件」這一件事（第 2 · 4 · 5 · 15 列），
⛔ 沒有一列跟 store schema 有關。第 16 列現在讀
`freeChampionIds` / `championUnlockCost` 並印「300 水晶」。

---

## 🎫 #751 Asph 球體附掛剩餘 11 筆 —— ⛔ **柵欄外，零改動**

複驗：`content/config/ambient-vfx.json` 的 `bindings` 今天**只有 3 筆** ——
`attach.godie-e00x.awing` · `attach.godie-u01u.poweraura` · `attach.godie-o00x.hands`
⇒ ⭐ **票文準確，剩下 11 筆確實還沒補。**

**⛔ 但它要動的每一個檔都在柵欄外**：`content/config/**`（明文禁止）·
`content/vfx/`（不在允許清單）· `tools/w3x-import/**`（明文禁止）。
⇒ **我一個位元組都沒有動。**

📌 交接用的事實：`bash scripts/genguard.sh content/config/ambient-vfx.json` →
**沒有產生器擁有者、沒有被隔離區鎖過** ⇒ 它是**手編檔**，
接手的 lane 可以直接 `Edit`（⚠️ 仍要 `grep -rl ambient-vfx tools/` 確認沒有上游）。

---

## 🎫 #739 體素條碼收尾 —— ⛔ **柵欄外，零改動**（但票文**逐條複驗為真**）

| # | 票文 | HEAD 複驗 |
|---|---|---|
| 1 | `ChampionView.ts:561` 只傳兩個參數 | ✅ 真 —— `acquireVoxelSkinTexture(scene, skin)`，⛔ 沒有第三個 |
| 1 | `ContentDb.ts` 不載 `_voxel-barcodes.json` | ✅ 真 —— 全檔 `grep barcode` **零命中** |
| 2 | `paintFaceDecals(s, recipe, p)` 不收 barcode | ✅ 真 —— `paint.ts:221` 簽章仍是三參數 |
| 3 | `tools/voxel-gen/` 只有 pngRead | ✅ 目錄存在（`extract.ts` / `barcodeExtract.ts`） |

**⛔ 四個落點全部在柵欄外**：`apps/client/**`（明文禁止）·
`packages/shared/src/content/voxelSkin/paint.ts`（柵欄只開 `content/laneF*.test.ts`）·
`tools/voxel-gen/`（柵欄寫的是 `tools/voxel/**`，⛔ 不是同一個目錄）。

⚠️ 這張是 **失敗形態⑧** 的教科書實例（optional 參數 ⇒ `tsc` 結構上抓不到），
⭐ 建議照票文的 `Test / verification criteria` 做：補一條**跑真的呼叫路**的承重守衛
＋一次突變（把第三參數拿掉 → 紅），⛔ 不要只驗 cache key 分流。

---

## 🧾 交接清單

### 要跑的產生器（⛔ 我一支都沒跑 —— 全域鎖）

```bash
pnpm content:build     # ← 必跑：改了 8 份 champion + 1 份 ability
                       #   要重建 bundle.json / manifest.json / 各 _index.json 並一起 commit
pnpm tiers:apply       # ← genguard 對這 9 份都說「真的手改了就跑一次」
```
⛔ 沒有動任何 `skillremake:json` / `speedtiers:build` / `vfxbind:build` 的產物。

### ⛔ 柵欄外的餘量（照「解一件事擋住幾支」排序）

| # | 要動的檔 | 一句話 | 解鎖 |
|---:|---|---|---:|
| 1 | `packages/shared/src/sim/systems/BasicAttackSystem.ts` ＋ `apps/client/src/audio/combatSfx.ts` | `WEAPON_TAGS` 加 `fist`（＋`claw`），`WEAPON_SFX` 各補一列（先指向 `GENERIC_SWING`，照 `thrown` 的先例） | **18 隻** |
| 2 | `tools/parallel-gates/sync-io.json` | `skillremake:json` 的 16 條 champion writes 標成正規化器（證據見上） | **3 隻**＋未來**每一次**要改那 16 隻非 abilities 欄位的 lane |
| 3 | `tools/hero-onboarding/audit_hero.py` | `godie-ogld` 天生技具名豁免（或等 owner 的 72-00 規格） | #740 收尾 |
| 4 | `packages/shared/src/sim/content/skeleton.ts` | `THORNE.tags` 與 `content/champions/thorne.json` 同一次加 `sword` | 1 隻 |
| 5 | `content/config/ambient-vfx.json` 等 | #751 全部 | 11 筆 |
| 6 | `apps/client/**` ＋ `voxelSkin/paint.ts` ＋ `tools/voxel-gen/` | #739 全部 | 4 個斷點 |

### ⚠️ 順手發現的（⛔ 我沒有修 —— 第零守則⑧，請 owner 排序）

1. **`apps/game-server` typecheck 是紅的**：`src/index.ts(403,44) TS2345:
   'unknown' is not assignable to 'CacheReport'`。
   ⛔ **不是我造成的**（本 lane 一個 `apps/` 檔都沒動），`packages/shared` 是綠的。
2. **`godie-u00j.passive` 的「無視對方迴避能力」是空宣稱**（見上）。

### 📋 守則犯錯（已記帳本）

`0-產物 / 憑印象` —— 只 genguard **抽驗兩份** champion 就對 11 份下手，
e00r/edem/h01u 三份被 hook 擋下。⭐ 正解是**逐檔** genguard，
⛔ 不是抽驗兩份然後推廣（`content/` 是混的，肉眼分不出來）。

### 測試預算

`npx vitest run` **3 次**（新守衛 → 突變 → 最終三支一起）·
`pnpm typecheck` **1 次** · 突變 **一批一條**（兩個方向合成一次跑）。
新增測試 **123 行**（含檔頭與 18 列豁免資料表）。
