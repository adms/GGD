# CONV-A lane —— 7 張已重開的舊票 → 新票（2026-08-26）

> owner 2026-08-26 逐字：「**reopen 跟 開新票摘要舊票差別在哪？我的觀點是 context 已經改變很多，所以不如開新票而不是 reopen**」

⇒ 每一張都**開新票**（body 用今天驗到的現況重寫）＋ **舊票留言互相引用後關閉**。
`bash scripts/ticket-lint.sh 760 761 762 763 764 765 766` ⇒ ✓ 全過。

---

## 1. 舊票 → 新票 對照

| 舊 | 嚴重度 | 新 | 新標題（收斂後的範圍） |
|---|---|---|---|
| #242 | high | **#760** | `[重要][perf]` 快照零剔除 —— `snapshot.ts:407` 的迴圈今天原封不動 |
| #61 | med | **#761** | `[優先][feature]` 「一次施法有幾個發射器」住在渲染層硬寫死表裡 |
| #64 | med | **#762** | `[優先][improve]` VFX 普查鏈沒跟著 `EnableTrigger` 走 —— ⭐ 而閉包早就寫好了 |
| #41 | med | **#763** | `[優先][improve]` 打擊音效仍不讀 `ImpactTier` |
| #132 | med | **#764** | `[重要][fix]` 草泥馬變身態 W/E 兩格同名，消化液整支不存在 |
| #146 | med | **#765** | `[優先][test]` TouchControls 沒有矩形模型（`028aa3bf` 量到 88×38 的真碰撞） |
| #177 | med | **#766** | `[優先][improve]` 兩隻開放名單英雄回血 7–10× 中位數，而 cap 1366 是柵欄不是上限 |

## 2. 合併

⛔ **一張都沒合併。** 判準是「**同一個修法會不會同時解掉兩張**」，⛔ 不是「主題像」。

唯一認真評估過的候選是 **#132（移速換算率離群）× #177（回血離群）** ——
兩張都是「一個從 w3u 派生的 per-champion 數值離群，而沒有任何東西紅」，
而且**一條從資料推導的離群閘**確實能同時覆蓋兩邊。

⇒ 仍然分開的理由：
- #132 的**最貴那一項不是離群** —— 是 `godie-h02u` 兩格技能同名 ＋ 描述／語音／技能列三份出貨資料互相矛盾（玩家看得到的卡面缺陷，而且 CLAUDE.md 明文把 join key 排除在優先序階梯之外）。
- #177 的另一半是**權限邊界**（數字該砍到多少是 owner 的旋鈕），AC 刻意寫成「⛔ 不提案數字」。
- 合起來會變成一張 AC 混雜、驗收標準互相稀釋的大票。

⭐ 兩張的 body 都寫了「從資料推導的離群閘」這同一個**解決模板**，落地時可以共用實作，⛔ 但票分開。

## 3. ⭐ 複驗證據裡**我驗不過**的（本節不可省）

### ⛔⛔ (A) #64 —— 「repo 裡沒有 `EnableTrigger` 遞移閉包」**是錯的**

複驗逐字寫：「唯一有的是 `tools/jass-combo/extract.py:50-74` 的**手寫 5 列** resolvedVia 表，⛔ 不是遞移閉包」。

實測 `grep -rniE "enabletrigger" tools --include='*.py'`（排除 `/out/`）今天的命中：

| 檔 | 是什麼 |
|---|---|
| ⭐ `tools/skill-audit/jassfacts.py:90` | `_ENABLE = re.compile(r"\bEnableTrigger\s*\(\s*gg_trg_(\w+)\s*\)")` |
| ⭐ `tools/skill-audit/jassfacts.py:320` | `def closure(groups, seeds)` —— **真的遞移閉包**（stack + `seen` + 排序輸出），docstring 逐字「沿 `EnableTrigger` 邊走到整條演出鏈」 |
| `tools/skill-audit/audit.py:50` | `from jassfacts import closure, …` |
| `package.json:116` / `:117` | `audit:build` / `audit:check`，**兩支都在 `skills:sync` / `skills:check` 裡** |
| `packages/shared/src/ops/skillAuditFresh.test.ts:18` | 突變紀錄逐字：「`jassfacts.closure()` 拿掉沿 `EnableTrigger` 展開的那一段」 |
| `tools/w3x-import/join/derive_join.py:114` | 另一份含 `EnableTrigger` 的 activation 正則 |

⇒ **缺口本身仍成立**（`build_vfx_census.py` / `build_vfx_bindings.py` / `extract_invocation_params.py` 三支**零命中**），
⛔ 但**修法完全變了**：從「寫一個閉包」變成「**重用既有的閉包，接到普查鏈上**」。
⚠️ 舊 body 的「建議做法：在 `extract_invocation_params.py` 上加一個遞移閉包」會造出**第二個住處**，已在 #762 明說作廢。

### ⚠️ (B) #61 —— vfxLayers 採用率的**分母錯了**

複驗寫「`content/abilities` 只有 **19/674** 份用 vfxLayers」。
實測 `find content/abilities -name '*.json' ! -name _index.json | wc -l` = **421**（含 `_index.json` 是 422）。
⇒ 正確是 **19 / 421（4.5%）**。方向不變（採用率很低），⛔ 但 674 這個數字沒有來源，已在 #761 更正。

### ⚠️ (C) #242 —— 「殘餘無票可追」**言過其實**

複驗說「關票把 LOD 殘餘導向 #35，而 #35 同日被關 ⇒ 兩條 lane 互指對方的接手票，殘餘無票可追」。
`#35` 確實 CLOSED ✅，⛔ 但實測 **#242 body 裡的 LOD 缺口本身已經被修掉了**：
- `apps/client/src/render/modelLod.ts`（`-mid` / `-small` 的挑選）
- `apps/client/src/render/AssetManager.ts:159` 的 `resolveLodPath` 真的抓它們
- 出貨預設住 `config.model-lod@1`（`packages/shared/src/content/schema/config/modelLod.ts`）

⇒ 「無票可追」的**只剩 #35 那一欄內部預算頁的 `lod` 欄**，而那不是玩家看得到的缺口。
#760 因此**刻意不接手** LOD，並把這件事寫進「舊票已不適用」表。

### ⚠️ (D) #41 —— 複驗沒說錯，⛔ 但舊 body 的兩個實作前提已死

複驗的核心論點（`soundImpact` 是**家族軸**不是**重量軸**）✅ 驗過成立。
⛔ 但舊 body 的兩條實作約束今天**都不存在**了：
- 「三個 clip 是 orphan / No caller」⇒ GH#390/#440 之後三列都是 `kind: "combat"`，有真呼叫端。
- 「regression pin `sfxReachability.test.ts:177` 斷言 `isPlayableSfxKey("hit-light") === false`」⇒ **該 pin 已不存在**（該檔今天 :217-221 說這三個是「故意離開那張名單」）。
⇒ #763 少一個實作步驟。

### ⚠️ (E) #132 —— 複驗**低估**了缺陷，另有一項舊 body 整項過期

- 複驗說「h02u.w = 92-02 狂草泥馬、h02u.e = 92-03 狂草泥馬」✅ 逐字驗過（standalone `content/abilities/godie-h02u.{w,e}.json` ＋ champion 內嵌 `:95`/`:151`）。
- ⭐ **複驗沒抓到的兩條**（已補進 #764）：
  - h02u **自己的卡面描述** `content/champions/godie-h02u.json:6` 逐字列著「…**消化液**、固有結界-馬勒戈壁」；
  - **語音還在講它** —— `content/assets/audio/voices/lines/godie-h02u/skill-name.e.mp3.method` 的 `text` = 「消化液！」、`status.json:1280` 同 ⇒ 玩家按 E 聽到「消化液！」而卡面寫「92-03 狂草泥馬」。
- ⛔ **舊 body 第 2 項整項過期**：`content/champions/godie-u011.json` **檔案已不存在**；今天對應的是 `godie-e010`（白木老樹精 - 白木卡迪那），值是 `maxHealth 150` / `armor 10` / `healthRegen 0.25` —— ⛔ **沒有任何負值**，「匯入器被地板夾住」沒有今天的證據。
- 移速離群我自己重算了一次（`TRANSFORM_FORMS.json` 26 對）：**34 位落在 51.4–52.2**，脫鉤的是 `godie-h02u`（0/5.5）、`godie-e010`（0/5.5）、`godie-n01g`（160/4.0 ⇒ 40.0）。⭐ 複驗只點名兩位，**漏了 `godie-e010`**。

### ⚠️ (F) #146 —— 複驗說「四個元件都沒有幾何守衛」**過度嚴格**

`028aa3bf` 的 88×38 逐字證據 ✅、三個 rect 名稱 0 命中 ✅、`hudBottomCluster.test.ts:124-126` 的 `rowsFor(touch)` ✅ 全部驗過。
⛔ 但 `prepClockDraftCollision.test.ts` **確實存在**且是真矩形交集，`hudBottomCluster.test.ts` 也已在做真矩形的**垂直**檢查。
⇒ #765 把範圍精確化成「**TouchControls 的矩形模型 ＋ 水平方向的兩兩交叉**」，⛔ 不是「四個元件都沒守衛」。
另：`feat/275b-gutter-chrome` 今天**仍未合併** ⇒ #765 只走 (a)，⛔ 不等 gutter。

### ✅ (G) #177 —— 複驗**逐條驗過全對**

`stat-caps.json:29-36` healthRegen 1366 / manaRegen 1711 ✅；`godie-huth` healthRegen **12**、`godie-u00k` **8** ✅；
`apps/platform/internal/curation/starter.go:324` / `:341` 兩隻都仍在預設開放名單 ✅；`godie-o02o.json` 確已不存在 ✅。

---

## 4. 順手發現、⛔ 未開票（第零守則⑧ —— 排序是 owner 的權力）

| 發現 | 證據 | 嚴重度 |
|---|---|---|
| `MatchController.faultCount` getter **零呼叫端** | `apps/game-server/src/match/MatchController.ts:4411-4414`；`grep -rn tickFault` 在 MatchController 之外 0 命中 | 低 —— 它是「被攔下來的例外」計數（⛔ 不是 shed tick，那半已由 `tickHealth.ts` → `/healthz` 解決）。同型於「fail-open 沒錯，靜默才是缺陷」 |
| `godie-e010` 的移速也離群（`umvs 0` / `ms 5.5`） | `TRANSFORM_FORMS.json` × `content/champions/godie-e010.json` | 已併進 #764 的離群閘 AC，⛔ 不另開票 |
| `godie-huth` 的 EX 描述在 content 裡是空的 | `apps/platform/internal/curation/starter.go:146` 逐字記著 | 低，已在 #766 的 Known risks 標明「不在本票範圍」 |
