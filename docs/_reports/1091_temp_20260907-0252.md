# GH#1091 —— 法術護盾擋**整發**（07-01 臨、兵、鬥 vs 原作 ANss Spell Shield）

lane（併行第二波）· 2026-09-07 02:52 · `HEAD=a47e80a7` 工作樹 · 狀態：**完成**
（玩家看得到的那一拍有終端證據 —— `docs/_reports/1085_visual-proof_20260907-0252.md`）

## 動手之前它今天的行為（基線）

GH#1085 的護盾只擋得下**狀態**那一半（掛載路徑 `sim/effects/applyStatus.ts`）。
⇒ 一支「魔法傷害＋減益」的敵方指定目標法術打上來：**減益被吃掉、傷害照樣落地**；
純傷害的指定目標法術則什麼都不擋。卡面「可抵擋對方負性魔法」在傷害那一半是假的。

## ⭐ 最重要的一個量測：**只接 `abilitySystem.ts` 會覆蓋 0 支技能**

票文與檔案柵欄都寫 `abilities/abilitySystem.ts`。逐份掃出貨內容（`content/abilities/*.json`）：

| | |
|---|---:|
| `castType: "targeted"` 的出貨技能 | **105** |
| 其中 `castTimeSec === 0`（走 `abilitySystem.ts` 的瞬發分支） | ⭐ **0** |
| 其中有吟唱（走 `systems/CastResolveSystem.ts`） | **105（100%）** |

⇒ ⭐ **整發攔截只接瞬發那一邊 ＝ 一支技能都擋不到，而且每一條守衛都會是綠的**
（失敗形態②的教科書形狀）。所以這條 lane **動了柵欄外的一個檔**
（`packages/shared/src/sim/systems/CastResolveSystem.ts`，只加攔截那一段）——
理由寫在下面「需要主 session 知道的」①。
⚠️ 瞬發那一半仍然要接：`castTimeSec` 是**後台可調**的（`config.cast-time@1` 的倍率／下限），
一格調成 0 就會整批換條路走。CLAUDE.md 自己在 G6-1 增幅那一對上記過同一個判例。

## 做了什麼

| 層 | 檔 | 內容 |
|---|---|---|
| 機制（引擎） | `packages/shared/src/sim/spellWardCast.ts`（新，130 行） | `spellWardRefusesCast(world, caster, def, targets, origin)` —— 開關 ⇒ `castType==="targeted"` ⇒ 單一目標 ⇒ 不是自己 ⇒ 不同隊 ⇒ 找到還有次數的護盾 ⇒ **扣一次 · 發 `immune` · 扣到 0 拔掉來源 · 回 true**。回 true 時呼叫端整發不跑 |
| 機制（引擎） | `sim/statusTagImmunity.ts` | 加 `chargedWardSource(sources, tick)`：「這具身體帶著哪一份還有次數的護盾」。⭐ **刻意不問 tags** —— 一發還沒開始跑的法術沒有 tag；問 tags 只會讓純傷害的法術（原作最經典的 Storm Bolt）整個溜過去 |
| 消費端① | `sim/abilities/abilitySystem.ts:914`（瞬發） | `const wardRefused = spellWardRefusesCast(…)`；`if (!wardRefused)` 包住 `runEffects` 與命中迴圈 |
| 消費端② | `sim/systems/CastResolveSystem.ts:102`（有吟唱＝今天的 105/105） | 同一支、同一個形狀（雙胞胎） |
| 開關（住處1） | `content/config/shield.json` | `"spellWardBlocksWholeCast": "whole"` |
| 開關（住處2） | `sim/shieldRules.ts` | `SpellWardScope` · `SPELL_WARD_SCOPES` · `ShieldRules.spellWardBlocksWholeCast` · `DEFAULT_SHIELD_RULES` 出貨 `"whole"` · `normalizeShieldRules` 認不得就退回出貨值 |
| 開關（住處3） | `content/schema/config/shield.ts` | `z.enum(["whole","status-only"]).optional()` ＋ `@zh`/`@note`/`@opt`；後台頁由 `specFromZod(zConfigShieldDoc,"shieldRules")` **整份推導** ⇒ ⛔ 一行手寫標籤都不用加（`apps/admin/src/configForms/specs/combat.ts:81` 一個字都沒動） |
| 開關（住處4＝消費端） | ⭐ `packages/shared/src/sim/spellWardCast.ts:105` `if (world.shieldRules.spellWardBlocksWholeCast !== "whole") return false;` | 這一行就是「誰讀它」的答案（GH#1035 那一族的閘） |
| 卡面 | `content/status-effects/spell-shield.json` | 說明從「下一份負性魔法掛不上來」改成「下一發**指定目標**的法術整發被吃掉 —— 傷害與減益一起消失」＋ 明說範圍技不吃這一層（第一·五守則：卡面不可以說不會發生的事，也不可以漏講會發生的事） |
| 守衛（靈魂層） | `packages/shared/src/sim/spellShieldWholeCast.test.ts`（新，170 行） | 出貨內容、真的 world、真的 `castAbility` ＋ 真的 `castResolveSystem`；⛔ 零個寫死的技能 id |
| 相容 | `sim/spellShieldOneShot.test.ts`（−1/+6 行） | `beat` 的判準拿掉 `statusId` 相等（`immune` 現在有**兩個**發射站，整發那一站沒有 statusId）。⚠️ 這是「一條綠燈的第④種假來源」：釘住 statusId 會讓它在整發攔截打開那天**紅得像回歸**，而其實是前提消失了 |
| 相容 | `sim/effects/shieldAbsorb.test.ts`（−1/+3 行） | `r.world.shieldRules = { absorbOrder }` → 展開既有規則（`ShieldRules` 多一格 ⇒ tsc 紅） |

## 我挑了什麼（決策點與出處）

> owner 2026-08-23（常設）：「沒做完以前別問我了自己判斷 但是留後台開關可以簡易 rollback」

| 決策 | 挑了 | 出處／理由 |
|---|---|---|
| 純傷害的敵方指定目標法術算不算「負性魔法」 | ⭐ **算**（整發擋掉） | 第〇·六守則的階梯：原作 `ANss` 擋整發（Storm Bolt 的傷害與暈眩一起消失），而 owner 沒有裁決過 ⇒ 高層級預設啟動。⛔ 留開關 `status-only` 一鍵回上一版 |
| 擋在哪一層 | **施放解算層**，⛔ 不是 `combat/damage.ts` 的封包閘 | 票文自己點名的風險：擋在傷害入口 ⇒ 狀態那一半仍走 `applyStatus` ⇒ **同一發扣兩次次數**。擋在解算層 ⇒ `runEffects` 沒跑 ⇒ 結構上不可能扣兩次 |
| 擋不擋範圍技／投射物 | ⛔ **不擋**（只擋 `castType:"targeted"`） | 原作 Spell Shield 擋的是**指定目標**的法術 —— 「用範圍技逼掉神符盾」這個操作存在就是它的出處。⚠️ 擋 AoE 會讓一層護盾吃掉一整波殭屍潮的傷害，而玩家分不出它擋掉的是哪一發 |
| 範圍技帶來的減益怎麼辦 | 仍由 GH#1085 的 `applyStatus` 那道閘擋（也消耗一次） | ⭐ **兩條路合起來才是完整的護盾**，⛔ 這一版不是取代 |
| 「整發取消」取消到哪 | ✅ `runEffects`（傷害／狀態／位移／這一發掛在自己身上的連段窗）＋ `onAbilityHit` / `abilityHit`；⛔ 不取消魔力、冷卻、`onAbilityCast`、收招餘韻 | 原作也不退魔力；他**確實放了一發**，被吃掉的是「命中」那一半 |
| 誰算「對方」 | 不同隊；自己 ⇒ 不算 | 與 #1085 的 `appliedByHostile` 同一個判準（卡面的「對方」）。⚠️ 同隊其實走不到 —— `castAbility` 對同隊的敵方技能回 `bad-target`，所以那一段是**不變量的守衛**，守衛用直接呼叫驗它 |
| 開關住哪 | `config.shield@1`（後台「護盾規則」頁） | ⭐ 語意上它就是護盾規則；而且那一頁 `specFromZod` 全推導 ⇒ ⛔ **不必新增 config 文件**，也就不必動 `apps/admin/src/store.ts` 與 `ui/App.tsx`（CLAUDE.md 逐字稱它們是「已知唯一真正共用的檔」，8 條 lane 併行時動它們＝撞車） |
| Zod 欄位為什麼 `optional` | 缺席 ⇒ 出貨預設 | `normalizeShieldRules` 已經是唯一解讀者；必填會讓既有的覆蓋層與夾具在升級那一刻被整份拒絕，而「文件被拒 ⇒ 退回預設」看起來跟正常一模一樣 |

## 量到的

- 出貨 targeted 技能 **105 支，100% 有吟唱**（見上）。
- 出貨「targeted ＋ damage ＋ 一份 debuff 狀態」的技能 **10 支**（`godie-e007.q` 混亂 · `godie-h020.r`／`hart.q`／`hjai.r`／`huth.r`／`hvwd.ex` 減速 · `u01u.e`／`udre.e` burnstun …）——⚠️ 票文寫的是 `godie-h020.q`，實際是 `.r`。
- 出貨「ground ＋ damage ＋ debuff」**6 支**（守衛的範圍技那一條從中挑）。
- 動手後（出貨內容、真的 world）：Q → 敵方那一支打上來 ⇒ **零個 `damage` 事件帶那個 origin**、狀態沒掛上、`sc.sources` 的 `spell-shield` 消失、`immune` 出現一次；第二位敵人再放 ⇒ 傷害與減益**都進來**。
- 開關 `status-only`：同一場 ⇒ 傷害**進來**、減益仍被擋 ⇒ 逐條回到 #1085。

## 突變紀錄

`systems/CastResolveSystem.ts:102` 的 `const wardRefused = spellWardRefusesCast(…)`
改成 `const wardRefused = false && spellWardRefusesCast(…)`
⇒ `spellShieldWholeCast.test.ts` **1 紅**：
「`godie-e007.q` 的**傷害**應該跟著整發一起被吃掉（#1085 的缺口）: expected true to be false」
（log：`scratchpad/run2-mutation.log`）。
⭐ 同一次 `spellShieldOneShot.test.ts` **照樣全綠** —— 那正是「#1085 的守衛量不到解算層」的證據。
用 Edit 改回來（⛔ 沒有 `git checkout`）。

（第二次突變在 visual-proof 那份報告裡：拿掉 `spellWardCast.ts` 的 `world.emit("immune")` ⇒ 客戶端證據紅。）

## 跑了什麼

| | 結果 |
|---|---|
| `npx vitest run`（shared，10 檔） | **79/79 綠**（`run5-shared.log`）—— 含 purity、`sourceGrants`、`shieldAbsorb`(35)、`configFacadeSurface`、`configUnionCoversDirectory` |
| `npx vitest run`（client，4 檔） | **13/13 綠**（`run7-client.log`） |
| `npx vitest run`（admin，5 檔） | `shieldRulesSave` **8/8 綠** · `configDocCoverage` **11/11 綠**；⚠️ **4 紅全部在 `arena-rules`**，而 `apps/admin/src/configForms/specs/arenaRules.ts` 是**另一條 lane 正在改的檔**（`git status` 顯示 M）⇒ ⛔ 不是我的 |
| `pnpm typecheck` | 第一次 EXIT=1（`shieldAbsorb.test.ts` 的夾具少一格）⇒ 修好 ⇒ `packages/shared` `tsc` **EXIT=0**，其餘 17 個 workspace 全 Done |
| `bash scripts/visual-proof.sh` | EXIT=1，指名 **`apps/client/src/render/platformPolicy.test.ts`**（`git status` 顯示 `??` ＝**另一條 lane 的新檔**）⇒ ⛔ 不是我的。⚠️ 而我的 `@visual-proof` 在 `apps/client/src/ui/` ⇒ 它的 liar-scan **掃不到**（已知洞 GH#1083） |

## ⚠️ 現在會紅、要主 session 的產生器才會綠的（⛔ 不是缺陷）

| 閘 | 為什麼 | 修法 |
|---|---|---|
| `content/shippedBundleIsCurrent.test.ts` | `content/config/shield.json` 多一格、`content/status-effects/spell-shield.json` 說明改了 ⇒ `_index.json` 雜湊與 `bundle.json` 過期 | `pnpm content:build`（`pnpm skills:sync` 裡就有） |
| `docs/技能標記機制與效果規則.md` / `editor-contract/*` | `config.shield@1` 多一格 ⇒ 契約文件的欄位表過期 | `pnpm spec:build` / `pnpm caps:export`（同一支 sync） |

## 需要主 session 知道的（柵欄外／跨 lane）

1. ⭐ **我動了 `packages/shared/src/sim/systems/CastResolveSystem.ts`（柵欄外）**，只加了整發攔截那一段（+34/−17，全部是那一段與註解）。理由是上面那個 105/105 的量測：不接它 ＝ 這張票對**每一支出貨技能**都無效。⛔ 如果主 session 認為不可以，唯一的替代是把攔截提前到 cast-begin（護盾會在吟唱**開始**時就碎，且吟唱被打斷也白扣一次）——那不符合原作。
2. `apps/admin/src/configForms/specs/combat.ts` **一個字都沒動**（`specFromZod` 全推導）。但 `specs/stats.ts` / `arenaRules.ts` 等 11 個檔正被別條 lane 改 ⇒ commit 時請逐檔 pathspec。
3. `apps/game-server/src/net/eventFanout.ts:447` 附近的契約註解：`immune` 現在有**第三個** emit 站（`sim/spellWardCast.ts`，`amount` 恆 0、`dmgType:"magic"`、⛔ **沒有 `statusId`**）。#1085 的報告已經請求補第二站，這是同一行的更新。
4. `packages/shared/src/sim/effects/invulnerable.ts` 檔頭②仍寫著「07-01 …它是純免控」——今天 07-01 走 `statusImmunity.charges` ＋ 整發攔截，那段是過期的存在證明（第三守則）。#1085 已列，仍未動。

## ⛔ 沒做到的

1. **DoT／hook 的後續跳**：一發被整發擋掉之後不會有後續（`runEffects` 沒跑），但**一發沒被擋的**法術點燃的延燒每一跳仍帶 `ability:<id>` 的 origin —— 票文 Known risks 提的那一題今天**不會**觸發整發攔截（攔截只在施放解算那一刻問一次），所以它不是缺口，只是沒有守衛釘住。
2. **投射物**（`skillshot`/`dash`）與 `ground` 一樣不吃整發攔截 —— 有出處（原作），但⛔ 沒有為投射物寫獨立守衛（範圍技那一條已經涵蓋同一個判準分支）。
3. 測試行數：sim 守衛 176 行 ≤ 實作 304 行 ✅；⚠️ 加上 visual-proof 的 181 行是 **1.18×** —— 超標的那一格是**終端證據**，比照 GH#1052 的判例（`docs/_reports/1052_visual-proof_20260906-2325.md` 最後一節）。

## 順手發現（⛔ 沒動、⛔ 沒開票）

- `scripts/visual-proof.sh` 的 liar-scan 只掃 `apps/client/src/{vfx,render}` ⇒ 住在 `ui/` 的 `@visual-proof` 貼了標記而零可見性詞彙**不會紅**。⭐ 已經是 **GH#1083**（#1052 那條 lane 開的），⛔ 不重複開票。
- `apps/admin` 的 4 條紅與 `visual-proof.sh` 的 1 條紅都指向**別條 lane 正在飛的檔**（`arenaRules.ts` M · `platformPolicy.test.ts` ??）—— 併行中的暫態，⛔ 不是缺陷。
