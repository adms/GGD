# GH#1058 —— 三支卡面的第二個 AP% 翻成條件式 ratio（09-04 龜派／11-03 鬼氣九刀流／07-02 者皆陣）

lane 報告 · 2026-09-06 · 狀態：**鏈路已接上，⛔ 未驗收**（JSON 與 KNOWN_UNBINDABLE 已落地；`{{ap2}}`／`{{ap3}}` 的綁定與英雄卡鏡射等主 session 跑 `skills:sync`）

## 做了什麼

| 技能 | JASS（第〇·六守則第 3 層） | 翻成 `ratios[].when` | 檔 |
|---|---|---|---|
| 09-04 龜派氣功 | `Trig_Turtle_Power_Actions` j:31896：`GetUnitTypeId(caster)=='O00X'`（`Func019C` j:31827）⇒ `str×5`（EX_Mode false）／`str×10`（true）；否則 `str×2` | 保留 `{ap 0.5}`（str×2，⭐ 必須留在 index 0 —— 09-002 augment `damageCoeffAp add 1.3` 用 `findIndex(stat==ap)` 改**第一條**）＋ 新增 `{ap 0.8, when: recentCast self slot E withinSec 8}`（str×5−str×2 = str×3 = 80%；E = 09-03 超級賽亞人，`ogrh.e.championForm.durationSec = 8`） | `godie-o00x.r` · `godie-ogrh.r`（對子兩邊逐位元一致） |
| 11-03 鬼氣九刀流 | `Trig_Roaction_Actions` j:28990：基礎 `lvl×150+150`；`UnitHasBuffBJ(B02Y)`（三刀流，`Func004C` j:28976）⇒ `+str×2`；`GetUnitTypeId=='U01U'`（武裝色，`Func005C` j:28983）⇒ `+str×3` | 原本那條**無條件** 0.5 改成 `{ap 0.5, when: recentCast self PASSIVE withinSec 15}`（三刀流 buff 15 秒）＋ 新增 `{ap 0.8, when: recentCast self EX withinSec 15}`（11-002 武裝色 `durationSec 15`）—— 與 #1049 對 11-04 的做法逐字同形 | `godie-u01u.e` · `godie-udre.e`（對子兩邊逐位元一致） |
| 07-02 者、皆、陣 | `Trig_MoonKnock_Actions` j:34390：基礎 `str×lvl+225`；`udg_MoonCombo==1`（Q 臨兵鬥 j:34157 撥 1、1 秒後歸 0）⇒ `EX_Mode false ? +agi×3 : +agi×6`（if/else **互斥**） | 保留 `{ap 0.3}`；新增 `{ap 0.8, when: all[recentCast self Q withinSec 1, not learned self EX]}` ＋ `{ap 1.5, when: all[recentCast Q withinSec 1, learned self EX]}`（`learned EX` = `udg_EX_Mode` 的翻譯，#1020） | `godie-hpb1.w` |

AP 換算照 `tools/ap-conversion/knobs.json`（×0.25、四捨五入到 10%）：str×2→50 · str×3→80 · agi×3→80 · agi×6→150 ✓。

- `packages/shared/src/content/abilityProse.test.ts`：`KNOWN_UNBINDABLE` 的 5 列（hpb1.w／o00x.r／ogrh.r／u01u.e／udre.e）刪光，留一行註解指到本報告。**ap 列剩 1 列**（`godie-h02v.r|ap`：抽取器對 passive 只讀第 1 階，⛔ 不在本票範圍；#1058 票文寫的「三支」全部處理完）。
- ⚠️ **柵欄外動了一個檔**：`tools/ap-conversion/claims.json` 的 `godie-u01u.e`／`godie-udre.e` 兩筆 —— `str×2` 的 `stacking: base → conditional`、`amounts[0].ratios: [{ad 0.5}] → null`。理由：`apply.py::apply_doc`（`mode=replace`）**每一次 sync 都會把 base 宣稱寫成一條無條件 ap ratio**（`amount["ratios"] = [ratio] + conditional`）⇒ 不改它，下一次 `skills:sync` 會把 11-03 變成 `[{0.5 無條件},{0.5 when PASSIVE},{0.8 when EX}]`（0.5 **算兩次**，而且 `apconv:check` 之後是綠的 —— 靜默）。⭐ #1049 對 11-04 做過完全同形的 claims.json 改動（commit 056eb0a52 @895）。⛔ 主 session 若不同意，把這兩筆還原 **且** 把 u01u.e／udre.e 的 0.5 改回無條件（兩邊要一起）。

## 量到什麼

| 閘 | 結果 | 說明 |
|---|---|---|
| `apconv:check` | 5 份技能 JSON **零過期**；唯一過期的是 `docs/_data/ap-conversion-applied.json`（報告產物，因 claims.json 改了） | ⭐ 我的 `when` ratio 在正規化器下是冪等的（apply.py 明文保留帶 `when` 的條目） |
| `prose:check` | `待轉 5 份` = 正好這 5 支 | 唯讀預覽（`scratchpad/1058_prose_preview.ts`，逐字複製 apply_placeholders 的磁碟字面值規則）：hpb1.w `80→{{ap2}}`、`150→{{ap3}}`；其餘四支 `80→{{ap2}}`；算繪後玩家看到 hpb1.w `131.33／210.12／210.12`（⚠️ ap 曲線封頂讓 80 與 150 印出同一個數 —— 07-03 的 `{{ap2}}/{{ap3}}` 今天已經是這樣，⛔ 不是本票造成）、o00x/ogrh `99.21／158.74`、u01u/udre `242.16／332.97` |
| `abilityProse.test.ts` ③（sync 前） | 🔴 6 條 orphan（hpb1.w 80／150、o00x.r 80、ogrh.r 80、u01u.e 80、udre.e 80） | ⭐ **票文的假設在這裡不成立**：③ 拿**註冊表**的曲線值（99.21）對卡面字面值（80），⛔ 在 ap 曲線開著的今天永遠對不上 ⇒ **sync 前刪掉 KNOWN_UNBINDABLE 必紅，⛔ 不是我的 ratio 沒接上**。`prose:build` 把字面值換成 `{{ap2}}` 之後 ③ 無字面值可問 ⇒ 綠；那時承重的守衛變成 **①**（裸佔位符） |
| `abilityCodeParityForms` | 🔴 09-04、11-03 各一筆「⚠️ 兩邊」（⭐ 對子一致、⛔ 不是單邊）；另有 24 筆單邊是**別條 lane** 的（04-002／08-02／09-03／11-01／11-02／12-03／18-*／22-*／25-*／38-04／42-002） | 基準線 `abilityCodeParityForms.baseline.json` 要主 session `GGD_FORM_PAIR_DUMP=1` 重生成後 `git add` |
| `abilityMirror` / `abilityMirrorSemantic` | 🔴 我的 5 支 effects standalone≠embedded（其餘 15 筆是別條 lane） | `tiers:apply` 的 `MIRRORED` 表含 `effects` 與 `description`，standalone→embedded，在 sync 鏈裡排在 `prose:build` 之前 ⇒ sync 後自動齊 |
| `descriptionClaims.test.ts` | ✅ | |
| `pnpm typecheck` | 🔴 但全在別條 lane 的檔：`compat.test.ts`／`mobs.*.test.ts`（`ChampionDef.role` 變必填）、`apps/editor vfx-forge/acceptanceFixtures.test.ts` | 我唯一的 .ts 改動（刪 KNOWN_UNBINDABLE 列）零錯誤 |

**消費端（第四個住處）**：`sim/effects/effect.ts:564` `resolveScaling` 逐條問 `r.when`（fail-closed）；`damage.ts:145`／`damageLine.ts:156` 都傳 `scalingOracle(ctx.world, ctx.caster, …)`；`recentCast` 讀 `castLedger.lastCastTickInSlot`（施法**提交點** `abilitySystem.ts:812` 寫入，per-slot、跟著實體走 ⇒ 變身前放的 E 在變身態仍讀得到）；`learned EX` 讀 `slotRankOf(EX) ≥ 1`。

## 突變紀錄（一條承重的線）

前置：把 `godie-hpb1.w` 的卡面**暫時**手寫成 prose:build 會產生的樣子（`{{ap2}}`／`{{ap3}}`）⇒ run 2：① 綠、③ 只剩其餘 4 支（hpb1.w 消失）。
突變：拿掉 hpb1.w 的 `{ap 1.5, when: all[recentCast Q, learned EX]}` ⇒ run 3：**① 紅** `godie-hpb1.w（07-02 者、皆、陣）：{{ap3}}`（卡面出現裸佔位符 = 守衛承重）。
還原：ratio 用 Edit 放回、卡面改回字面值（綁定留給 `prose:build`，照票文）。vitest 共 3 次（run 1 五支測試檔一次撈、run 2、run 3）。

## ⛔ 沒做到的 / 誠實的限制

1. **`recentCast E／EX withinSec = 變身秒數` 是「出貨值下等價」的過渡**，⛔ 不是 JASS `GetUnitTypeId == 'O00X'/'U01U'` 的逐字翻譯 —— 缺的葉子是「主體現在是變身態」（`form`）。秒數因此有第二個住處（`championForm.durationSec` ↔ `withinSec`），而且變回本體後 withinSec 尾巴內 `ogrh.r` 會多吃 +80%（E 在變身態仍可按）。⇒ 開 **GH#1070**。
2. 三支的 `{{ap2}}` 綁定與英雄卡鏡射**沒有落地**（⛔ 不跑 build）—— 等 `skills:sync`。
3. 07-01 臨兵鬥 `effects: []`：零效果技能能否被提交／進 castLedger **沒有量** ⇒ 07-02 的 1 秒窗可能按不出來；07-03 用 `level<30` 當 EX 分岔（與 07-02 的 `learned EX` 不一致）⇒ 開 **GH#1074**。
4. 沒有為這批寫新守衛：承重的是既有的 `abilityProse.test.ts` ①③（突變驗過）；ratio `when` 的求值由 #936 的守衛守。

## 需要主 session 動的（柵欄外）

1. `pnpm skills:sync` ⇒ `tiers:apply` 鏡射 5 支 effects/description 進 `content/champions/godie-{hpb1,ogrh,o00x,udre,u01u}.json`；`prose:build` 綁 `{{ap2}}`／`{{ap3}}`（5 份，等於上面的預覽）；`apconv:build` 重寫 `docs/_data/ap-conversion-applied.json`。
2. `GGD_FORM_PAIR_DUMP=1 npx vitest run packages/shared/src/content/abilityCodeParityForms.test.ts` → `git add packages/shared/src/content/abilityCodeParityForms.baseline.json`（09-04／11-03 兩筆「兩邊」是本票，請確認過再吸收）。
3. ⚠️ `tools/ap-conversion/claims.json`（u01u.e／udre.e 兩筆）是我在柵欄外改的 —— 見上面的理由；commit 時請一起帶。
4. sync 之後 `abilityProse.test.ts` ①②③ 應全綠；紅了先看 `prose:check`。

## 改了哪些檔（逐檔）

- `content/abilities/godie-o00x.r.json`、`content/abilities/godie-ogrh.r.json`（＋1 條 when ratio）
- `content/abilities/godie-u01u.e.json`、`content/abilities/godie-udre.e.json`（0.5 改條件式 ＋1 條 when ratio）
- `content/abilities/godie-hpb1.w.json`（＋2 條互斥 when ratio）
- `packages/shared/src/content/abilityProse.test.ts`（KNOWN_UNBINDABLE −5 列）
- `tools/ap-conversion/claims.json`（⚠️ 柵欄外，2 筆）
- 新增：`docs/_reports/1058_temp_20260906-1805.md`（本檔）

## 順手發現但沒動的

- GH#1070：條件葉 `form`（見上）。
- GH#1074：07-01 臨兵鬥空效果 ＋ 07-03 的 EX 分岔用 level 代理。
- `abilityProse` ③ 在 ap 曲線開著時對任何**字面值** AP% 都綁不上（它拿註冊表曲線值比對）—— 票文「可在 sync 前用 KNOWN_UNBINDABLE 已刪的狀態驗」這句對 ap 軸不成立；`prose:check` 才是 sync 前的量尺（沒開票：這是測試的分工說明，不是缺陷）。
