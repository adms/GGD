# lane W3 —— #1095（屬性正規化頁的兩句過期散文）· #1096（genguard 對部分擁有的檔過度封鎖）

> 2026-09-07 · 主工作樹（⛔ 未 commit，主 session 統一）

---

## 一、動手之前它今天的行為（baseline，量到的）

| | |
|---|---|
| `bash scripts/genguard.sh README.md` | 🚫「是產生器 **docs:readme** 的產物」＋ **exit 1** —— ⛔ 對**整份** 2,075 行 |
| hook（Edit README 的人寫散文） | **exit 2**，訊息叫人「改來源再 genrun」—— ⛔ 那條路對散文**不存在** |
| 後台「英雄屬性正規化」頁 `skipTransformedBodies` | 說明逐字「出貨**開著**」，而 `content/config/stat-normalization.json` 是 **`false`** |
| `configFormsShippedProse`（既有的閘） | 綠 —— ⛔ 它第一行是 `if (typeof v !== "number") continue`，**布林看不見** |

---

## 二、#1096 做了什麼

### ⭐ 判準從「這個檔是不是產物」換成「這一次改的**位元組**是不是產物」

三個新東西，⛔ 沒有任何一張手寫的「哪些檔是部分產物」名單：

| 檔 | 是什麼 |
|---|---|
| **`tools/parallel-gates/marker_regions.py`**（新） | marker 語法的**唯一住處** ＋ 兩個推導：①區段從**檔案自己的 marker** 讀 ②「這一支是不是 marker 拼接器」從 `package.json` 追到它跑的那支程式再 grep `BEGIN GENERATED:` |
| `scripts/preserve-before-overwrite.py` | `_generator_owner()` 多回傳**全部**作者；新 `_marker_mod()` / `_edit_spans()`；genguard 迴圈加部分擁有分支 |
| `scripts/genguard.sh` | node 段多輸出第四欄（全部作者）；AUTHOR 分支先問 marker_regions，是部分產物就**逐段列出行號**並 exit 0 |

### 裁決表（實測，⛔ 不是設計意圖）

| 動作 | 結果 |
|---|---|
| Edit 落在 marker 區段**內** | 🚫 exit 2，指名 `docs:readme` ＋ 那一段的行號 |
| Edit 落在區段**外** | ⚠️ **exit 0 放行**，並印出九段的行號（「這幾行別動」） |
| Write／Bash `>`（整份覆蓋） | 🚫 exit 2 —— 它會把產生區段一起蓋掉 |
| `old_string` 在檔案裡對不上 | 🚫 exit 2（fail-closed，⛔ 不可以當成「沒碰到區段」） |
| 有作者**不是** marker 拼接器 | 🚫 整份照舊擋 |

### ⭐ 「⛔ 不可以只看第一個作者」是量出來的，不是謹慎

| 檔 | 認領者 | 判定 |
|---|---|---|
| `README.md` | `docs:readme`（marker） | ⭐ 部分產物 · 9 段（combat-env / arenas / stat-bands / tiers / roster / abilities / items / grail / mechanics） |
| `docs/效果標籤詞彙表v2.md` | `contract:numbers`（marker） | ⭐ 部分產物 · 1 段 |
| `docs/技能編輯器引擎須知 20260811.md` | `contract:numbers` ＋ **`skillremake:docs`** | ⛔ **整份照舊擋** —— 後者用 `### 13.10 …` 標題拼接、⛔ 沒有 marker ⇒ marker 外面**不是**自由的 |

⇒ 判準是「**每一個**作者都是 marker 拼接器」。突變 M2（`all` → `any`）當場讓第三列被誤判成部分產物。

### 守衛：`packages/shared/src/ops/genguardMarkerRegions.test.ts`（4 條，⭐ 兩個方向都驗）

真的把 hook 跑起來（PreToolUse JSON 事件）＋ 真的出貨檔案 ＋ 真的戶籍表，
⛔ 不掃原始碼字串（形態⑥）、⛔ 不自造 payload（形態⑤）。行號**現場算**，⛔ 不抄。

**突變紀錄（2026-09-07，逐條驗過）**

| 突變 | 結果 |
|---|---|
| `hits_region()` → `return None`（永遠「在區段外」） | ① 紅 —— 區段內的編輯被放行 |
| `partial_regions()` 的 `all(...)` → `any(...)` | ④ 紅 —— `技能編輯器引擎須知` 被誤判成部分產物並放行 |

---

## 三、#1095 做了什麼

⚠️⚠️ **票文與 lane 指示的前提在動手當下已經變了**（⛔ 這一段要看）：
派工時說「標籤由 Zod 的 `@zh`／`@note` 推導（#992）」——
我第一次讀 `packages/shared/src/content/schema/config/statNormalization.ts` 時它**只有 150 行、零個 `.describe()`**，
而 `STAT_NORMALIZATION_SPEC.fields` 是 `[...NORM_HAND_WRITTEN, ...generatedNormalizationFields(...)]`
⇒ 當下那句話**不成立**（手寫的贏）。
⭐ 幾分鐘後同一個檔變成 **417 行 ＋ `NORM_PROSE` 表**、`fields: derivedFields(zConfigStatNormalizationDoc, [])`
—— **另一條 lane 正在同一棵樹上把 #992 的 stat-normalization 那 247 格搬過來**（兩個檔都是 ` M` 未 commit）。
⇒ 於是那句話**現在成立了**，而那 31 格 owner 裁決版本的散文（含這一句過期的）是被**逐字搬過來**的。

### 改的那一格

`NORM_PROSE.skipTransformedBodies` —— 「出貨**開著**」→ 「出貨 **{{出貨值}}**」，
並補上載重的那半句：**開著時 `transformBandShift` 與 `transformInheritsOrigin` 兩格都讀不到**
（＝票文說的「操作者會以為自己在調的兩格是死的」）。owner 2026-08-13 的原話逐字保留並標了引號。

⛔ 一個出貨值都沒改。

### 守衛：`packages/shared/src/ops/configProseBooleanDefaults.test.ts`

⭐ 既有的 `apps/admin/src/configFormsShippedProse.test.ts` 只掃**數字**出貨值（第一行 `typeof v !== "number"` 就 continue）
⇒ 布林那一半在結構上沒有守衛。這一條把同一個判準補到布林：
「說明宣稱的開/關 ＝ `content/config/*.json` 的實際值」，母體 checked、specs 兩個都有下限（⛔ 母體不可以塌掉）。

**突變**：把 `{{出貨值}}` 換回「**開著**」⇒ 紅，訊息指名 `stat-normalization.skipTransformedBodies`。

**⚠️ 第一版的誤報（記下來，因為它是判準的一部分）**：
`vfx-cleanup.lifecycleLedgerEnabled` 出貨 `true` 而被指「說明寫預設是關的」——
逐字讀那一段：「⛔ 不受「顯示效能面板」那格影響，因為**那格預設是關的**」
⇒ ⭐ 它講的是**另一格**，而且是對的（同一段後面也誠實寫著「出貨開著」）。
⇒ 宣稱前 10 個字含 `那格/那一格/上面/下面/另一格/別的/其他/旁邊` ⇒ 跳過。⛔ 不是加豁免名單。

---

## 四、⛔ 沒做到的（柵欄外，需要主 session 動）

| # | 檔 | 要做什麼 | 為什麼我沒做 |
|---|---|---|---|
| 1 | `apps/admin/src/configForms/specs/stats.ts:36` | `STAT_NORMALIZATION_SPEC.intro[2]`「⭐ 這一版只套用**移動速度**與**魔抗**」**已過期** —— 出貨 `appliesTo` 是 **11 項**。⭐ 正解不是改成「11 項」（那是第四個住處），是從 `DEFAULT_STAT_NORMALIZATION.appliesTo` 現算，或把 intro 搬進 Zod 根節點的 `@intro`（`specFromZod`） | 柵欄 ⛔ 不動 `apps/admin/src/**`，而且**另一條 lane 此刻正在改這個檔** |
| 2 | `scripts/product-quarantine.sh` | ⭐ **README.md 現在是 444** ⇒ hook 放行了，而寫入仍然吃 **EACCES**。隔離區要跟 genguard 一樣讀 `tools/parallel-gates/marker_regions.py`，對部分產物**不上鎖**（＝ GH#707「三個消費端再度分家」的第四次）。⚠️ 我已經在 genguard.sh 與 hook 兩邊都加了「但這個檔現在是 444」的第二把量尺，⛔ 但那只是出聲 | 不在柵欄裡（柵欄只列 genguard 的兩支）。⇒ 已開票 **#1097** |
| 3 | `README.md` L475 / L509 / L520–564 | #1096 AC④：GH#1089 那幾行平台政策散文要更新成新政策 | README.md 不在柵欄裡，且被 ② 的 444 擋著 |
| 4 | `packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts` | ⭐ 棘輪 **362 → 55** 紅（逐份欠帳：range-guide=17 · item-card=11 · damage-colors=9 · new-hero-checks=9 · victory-podium=3 · displacement-tiers=2 · speed-growth-tiers=2 · victory-fx=2）。⛔ **這不是我造成的** —— 我改的那一格在我動手之前就已經有 `@zh`＋`@note`，residue 不變。它是 #992 那條 lane 的帳，⭐ 由他們調 BASELINE 並寫下出處 | ⛔ 不動別人的帳本，會蓋掉他們的 commit 訊息理由 |

---

## 五、順手發現但沒動的

- **#1097**（新開）：`product-quarantine.sh` 不認得部分產物 ⇒ README 的人寫散文仍然 EACCES。
- ⚠️ `pnpm typecheck` **exit 1**，兩族錯誤，⛔ **都不在我改的檔案裡**：
  `packages/shared/src/content/templates/expand.ts:1714`（`applyBuff` 不 assignable）與
  `packages/shared/src/content/schema/victoryPodium.ts:244/250/256`（`PODIUM_CLIP_OPTS` 未定義）——
  兩個都是**其他 lane 飛在半空中的**未 commit 改動。⭐ 收工前主 session 要確認它們落地。

---

## 六、改了哪些檔

| 檔 | 動作 |
|---|---|
| `tools/parallel-gates/marker_regions.py` | ⭐ **新增** |
| `scripts/preserve-before-overwrite.py` | 改（`_generator_owner` 回傳值 · `_marker_mod` · `_edit_spans` · genguard 分支） |
| `scripts/genguard.sh` | 改（第四欄 · 部分產物分支 · 444 的第二把量尺） |
| `packages/shared/src/content/schema/config/statNormalization.ts` | 改（`NORM_PROSE.skipTransformedBodies` 一格） |
| `packages/shared/src/ops/genguardMarkerRegions.test.ts` | ⭐ **新增**（守衛，4 條） |
| `packages/shared/src/ops/configProseBooleanDefaults.test.ts` | ⭐ **新增**（守衛，1 條） |
| `docs/_reports/1095-1096_temp_20260907-0453.md` | ⭐ **新增**（本檔） |

## 七、跑過什麼

```
npx vitest run <11 支 ops 守衛>          → 11 passed / 38 tests / EXIT=0
（含 laneAGenguardHeredoc · backupRules · genInputCommitGate · laneYCommitRefHookMounted
  · hookHeredocOpenerSingleHome · normalizerListIsReal · boardBackupBeforeOverwrite
  —— ⭐ 因為我改了 hook 的 `_generator_owner` 簽名，這幾支要一起驗）
pnpm typecheck                            → EXIT=1，⛔ 兩族錯誤都在別人未 commit 的檔裡（見第五節）
```
