# GH#1084 —— 量測沙盒不再複製 `.claude/worktrees`

## 做了什麼
`tools/parallel-gates/trace.mjs` 的重建從「整棵 `cp -Rc`」改成**逐個頂層項目**複製並跳過 `.claude/`。
⛔ 不是只跳 `worktrees`：`.claude/` 底下其餘的（settings／agents）對量測同樣無關，而它們的數量隨併行 lane 變。

## 量到的（2026-09-07 02:10）
| | |
|---|---:|
| `.claude/worktrees` | **37G**（55 個 worktree） |
| 沙盒（改前，整棵） | ≈53G |
| 沙盒（改後，預期） | ≈16G |

⚠️ 改後的實測大小**沒有量**：重建一次要 8 分鐘而這一批有 7 條 lane 在跑，⛔ 觸發重建會讓它們的量測全部作廢。
下一次依賴指紋變動時自然會重建 ⇒ 那一次的 log 就是證據（`🧹 重建量測沙盒` 那一行之後 `du -sh`）。

## ⛔ 沒做的：刪掉那 55 個 worktree
⭐ **票沒有要求刪**，而且逐個查過：**55 個全部**有未合併的 commit 或髒檔（`git log main..<branch>` 非空，或 `git status --porcelain` 非空）。
`noStrandedLaneCommits.test.ts` 綠（它只管**新鮮**的 lane 分支），⛔ 但那不等於「這 55 個可以丟」——
它們是幾週前併行批次留下的工作樹，⭐ 而「決定丟掉」是 owner 的事（CLAUDE.md：`git branch -D` 那一條逐字說「⭐ 真的不要了 —— 那才叫『決定丟掉』，⛔ 不是忘記」）。
⇒ 清單在 `scratchpad/wt-unmerged.txt`，要清時逐個看過再刪。

## 守衛
`packages/shared/src/ops/traceSingleStep.test.ts` 4/4 綠（沙盒重用／重建兩個方向都在裡面）。
