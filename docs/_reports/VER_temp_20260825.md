# Lane VER —— 三批連續圖片驗收（GH#694 重產 · #696 重產 · #695 新產）

**結論**：六個證據目錄全部重新產生於 **1280×760**（前一版 645×407），
量尺 `calibrate()` = **515,524** 亮像素（六批同一個數字），登記 **6 批全部 rollbackOk**、
`buildFeatureQueue` **invalid 0**。commit `e0cdb801`（49 檔）。

| 批 | 證據目錄 | 張數 | rollback 開關 | 判定 |
|---|---|---:|---|---|
| ① 天氣 | `weather_rain_visual-proof_20260825` | 7 | `weather.rainEnabled` true→false | ⭐ **#700 已驗收**（雨真的下得出來）／⛔ 霧仍零像素（#610） |
| ② 幻之匕首 | `dagger_visual-proof_20260825` | 7 | `gore.style` blood→off | ⭐ **#702 已驗收**（翻開關 ⇒ 血花 0 顆） |
| ③ 01-04 超究武神霸斬 | `classic_omnislash_visual-proof_20260825` | 7 | `vfx-families.families.shockwaveRing.enabled` | ✅ 七連斬＋收尾／⛔ 模型層冷快取 0 頂點 |
| ③ 20-002 理想鄉MAX | `classic_avalon_visual-proof_20260825` | 6 | `screen-fx.enabled` | ✅ 反彈→七斬→勝利之劍全鏈 |
| ③ 04-03 龍破斬 | `classic_dragonslave_visual-proof_20260825` | 6 | `vfx-families.families.dissipate.enabled` | ✅ 落點爆炸／⚠️ 飛行段 0 亮像素 |
| ③ 42-04 世界終結 | `classic_endworld_visual-proof_20260825` | 6 | `tpl-radial-burst.params.count.default` 12→2 | ✅ 十二向＋到點 12 記冰爆 |

---

## ① #694 天氣 —— ⭐ GH#700 **已驗收**（這是本 lane 最重要的一句話）

S7 那一版量到出貨路徑**一定擲** `The WebGL2ParticleSystem class is not available!`，
每一張雨的圖都是「台子自己補了一行 import」之後的樣子。

⭐ **這一版台子一行都沒補**（`harnessImportedWebgl2Platform: false` 逐字寫在 facts 裡），
而出貨 `buildRain`：`threw: null` · `gpu: true` · `psReady: true` · `texReady: true` ·
**活粒子 1,200**。

| | clear 級（競技場） | rain 級（希干希納） | 室內（納薩力克） |
|---|---:|---:|---:|
| 與基線相異像素（Δ>8） | **422** | **3,003** | **0** |

⚠️ **量法要換**：出貨 `rainAlpha 0.22` ⇒ 一道雨絲峰值只有 ~22–32/255 ⇒
`亮像素(>200)` 與 `lit(>96)` 兩道門檻**全是 0**（結構性失明）。這一份因此加了
「與基線相異像素」欄（PIL 逐像素）—— ⭐ **它才是承重讀數**。

⛔ **霧仍是零像素**：`w6_fog_on` 與 `w5_fog_off` 的 PNG **sha1 完全相同**（＝沒被畫，
⛔ 不是太淡）。⛔ 根因未查（不在柵欄內）⇒ **GH#610 仍然開著**，照實寫進 frames.md。

## ② #696 幻之匕首 —— ⭐ GH#702 **已驗收**：登記的那一格開關現在真的關得掉血

新增 **d6**：翻 `config.gore@1.style` `blood`→`off`（走出貨 `applyGoreDoc()`）之後，
**同一顆種子**的 3% 在第 **2,942** tick 又擲中一次 —— sim 照樣發 `vfxSpawn`、
出貨 `VfxSystem` 照樣收到，而 `play()` 直接回 null ⇒
**血花活粒子 0**、**全場 `liveSystems` 空陣列**。

⚠️ d6 的 316 個亮像素**不是血**（liveSystems 空）—— 是兩具替身走了 2,942 tick 後的站位差異。
⭐ 血的證據是 `goreOffLiveParticles: 0`，⛔ 不是「畫面全黑」。

## ③ #695 四支經典（新產）

⭐ 世界那一半**直接用出貨的 `buildBeamAuditionWorld`**（真 `SimWorld` ＋ 真 `castAbility()`
＋ 目標型別由文件自己的 `castType` 推導），⛔ 不造第二個世界建構器；
四支的差別只是**一張參數表**（第零守則⑨）。

| 支 | 承重讀數 |
|---|---|
| **01-04** | 0（詠唱中）→ **19,213** → **51,526** → **66,151**（收尾閃光）→ 0。事件：`damage 7` · `vfxSpawn` 逐段 · `immunityGranted` · `stunApplied` |
| **20-002** | ⭐ 走**真的反彈**：`exSlot rank1` ＋ 出貨 `syncAbilityPassives` ⇒ `stats.sources` 出現 `abilityPassive:godie-e002.ex`；敵人一發魔法傷害 ⇒ **反彈真的打回 1,137.4 傷害**（對照組）⇒ `onReflectSuccess` 才跑。像素 0→**1,281**（七彩爆炸）→447（七斬中）→**16,465**（勝利之劍＋第二發爆炸）→0；`damageLine 1` · `floatingText 7` |
| **04-03** | 0→0→0→**1,749**（落點爆炸）→0。⚠️ 飛行段模型在（164 頂點、enabled）而**亮像素 0** |
| **42-04** | 0→**164,206**→**126,155**→**31,473**（12 池 `ice.nova` 各 40 顆）→0 |

⭐ **台子這一版最重要的改動：取樣點用事件對齊（`ClassicBeat.until`），⛔ 不猜 tick。**
⚠️ 這是踩出來的 —— 照 `config.combo-strikes@1` 的秒數算 tick 拍到**兩次空畫面**；
20-002 的爆炸那一張在兩輪之間從 14,500 掉到 58 亮像素（同一份程式、同一顆種子）。
⇒ 一張「剛好沒拍到」的圖與「這個特效不存在」在證據上長得一模一樣，
而那正是 CLAUDE.md 👁 節要擋的東西。

---

## ⛔ 三個要開票的發現（本 lane ⛔ 未修 —— 不在柵欄內、⛔ 不 gh 寫入）

| # | 嚴重度 | 一句話 |
|---|---|---|
| 1 | 中 | **01-04 的 `spawnModelFx` 在冷快取的第一次施放 0 頂點**：`imported.herocloudstrife.glb` 389 KB，而演出是 `path:"toTarget"` `speed:30`（≈0.1 秒）⇒ 載入比演出長一個數量級。熱快取重放量到 1,949 頂點。⚠️ 實戰嚴重度較低（克勞德在場時 glb 早載過），⛔ 但「技能引用沒被預載的大模型」這個形狀是真的 ⇒ **預載/暖機**票 |
| 2 | 中 | **01-04 的連段節奏與共用表對不上**：實測 57→87 tick（每 3 tick 一記、整段 **1 秒**），而 `config.combo-strikes@1` 的 `superff7` 逐字是 `steps [0,.5,.6,.7,.8,.9]`＋`finisherDelaySec .1`（**3.6 秒**，JASS 原文）。⚠️ 只影響節奏，段數對得上 |
| 3 | 低 | **`godie-e00l.r`（20-04 的鏡像那一份）沒有 `incomingPct`**，而 `godie-e002.r` 有 ⇒ 兩份 mirror 的反彈機制不一致（順手 grep 到，⛔ 未驗證是否刻意） |

⚠️ 另外**照實記**：`04-03 的飛行段 0 亮像素`（模型在、材質不發光）與
`20-002 的紅柱只有 49 亮像素`（`tint [0.39,0,0]`）都寫進了各自的 frames.md ——
⛔ 本 lane 不判定它們是不是缺陷，⭐ 那正是「一頁批次後台驗收」要 owner 看的那種題。

## ⚠️ 三個誠實的界線

1. **浮字（`floatingText`）不在任何截圖裡** —— 出貨由 `ui/WorldAnchorLayer.tsx`（**DOM**）畫，
   這一頁量的是 canvas。事件層看得到 7 記，PNG 上看不到 ⇒ ⛔ 這不是缺陷的證據。
2. **rollback 開關的粒度**：01-04／04-03 用的是**家族級**（`vfx-families.families.*.enabled`，
   同族其他技能會一起關）、20-002 用的是**全域**（`screen-fx.enabled`）、
   42-04 用的是**模板級**（`tpl-radial-burst.params.count.default`）。
   ⭐ 理由逐批寫進帳本的 `rollback-note`：這四支**沒有一支有自己的專屬 config entry**，
   而登記閘只認 `content/config` 與 `content/ability-templates`。
3. **`apps/client/public/feature-proof-audition.html` 的改動被別的 lane 掃進了 `f6176d1b`**
   （已驗 HEAD 的內容是對的，含四個 classic 目錄映射）⇒ 照 CLAUDE.md ⛔ 不回捲、⛔ 不改寫歷史。

## 驗證

- `apps/client` `tsc --noEmit`：**0 error**
- `buildFeatureQueue`：11 批（我的 6 批全部 `pending` ＋ `rollbackOk`），**invalid 0**；
  逐張表格被 `parseFrameTable` 讀得出來（⚠️ 表格第一欄⛔ 不可以有 emoji 前綴 —— 踩過，會 parse 成 null）
- 每一個目錄 `shasum *.png` 掃過重複：天氣的 w0/w1/w4 三張刻意相同（＝「這一格什麼都沒做」的證據，
  frames.md 逐字說明），其餘無重複
- ⛔ 未跑全域鎖（`content:build` / `skills:sync` / `ship:check`）、⛔ 未 push、⛔ 未 gh 寫入
