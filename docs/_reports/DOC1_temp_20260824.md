# DOC1 — docs/開票守則.md（通用化開票規格文件）· 2026-08-24

lane：DOC1（文件 lane）。任務：寫一份給其他軟體專案參考的 `docs/開票守則.md`。

## 產出

`docs/開票守則.md`（約 380 行），結構照指示五段：

1. **為什麼要有開票規格** —— 「做完」沒有證據標準會變謊話（終端證據 vs 工作鏈完成）、
   票是唯一長期記憶（換手/context 斷）、規格必須配自動化（判準 vs 閘）。
2. **五件必填** —— 優先級 tag（標題、[一般] 不可省略）· 類型 tag（標題、
   [breaking change] 是部署節奏訊號）· 驗收標準（無關者可照做得到過/不過）·
   [思考策略]（通用化 8 種表）· [解決模板]（通用化 7 種表）。逐件附好壞範例。
3. **內文六節＋三節建議** —— Objective/Scope/Files（實查、雙重理由）/Constraints
   （票攜帶守則子集）/AC/Test（契約 vs 證據計畫分開講）＋ Dependencies/Non-goals/Risks
   與「為什麼建議不硬擋」。
4. **三層自動化** —— scaffold（可硬擋）/建立當下警告（⛔ 不擋，缺欄票仍比不開好）/
   事後 lint（離線 fail-open 但明說沒驗到；lint 自身三層：欄位在不在→佔位符→前提回驗）
   ＋「導入既有專案」小節（截止線、append-only、重抓最新、冪等）。
5. **六條進階心得** —— ①catalog 化（8+10 蓋 95%）②前例路徑最值錢 ③策略帶已知坑
   （量尺校準例）④Files=併行化燃料（檔案級柵欄）⑤前提腐爛→lint 第三層（#348 案例）
   ⑥tag 路由驗證預算（表格）。
6. **一頁可抄模板全文＋採用 checklist**。

## 通用化處理

GGD 名詞（技能/英雄/vfx/玩家）全部改寫為通用例（結帳 API、CSV 匯出、UUID 遷移、
連線池 p99）；GGD 實戰只以匿名化形式留在 4 個「📦 案例」框（零像素特效、47 張回補
3 張前提錯、目錄級柵欄慢三倍、#348 前提翻轉）。策略/模板名稱通用化
（「玩家看得到」→「使用者看得到」、「三個住處」→「落齊所有住處」）。

## 素材來源

CLAUDE.md 🎫 節（第 35 條 v1–v3）· `scripts/ticket-lint.sh` · `scripts/ticket-new.sh` ·
`scripts/preserve-before-overwrite.py` 🎫 段 · `docs/_reports/{RT_ad,RT_ae,V3_c1,V3_c2}_temp_20260824.md` ·
指示裡的六條 owner 已核可心得（全數寫入 §五）。

## 備註

- 寫作期間另一 lane 更新了 `ticket-new.sh`（選項清單改從 `tools/ticket-templates/catalog.json`
  動態長出）—— 正好落實本文件 §五-① 的建議，文件內容（通用形式）與現況一致，無衝突。
- 授權遵守：只新增本兩檔；未動其他檔、未跑全域鎖、無 gh 寫入、無 push。
