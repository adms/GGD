# GH#1003 —— `/private/tmp`／`mktemp -t` 是 macOS 專屬：13 支改成 `${TMPDIR:-/tmp}` ＋ 一條會紅的閘

lane：#1003（2026-09-06，owner 睡覺中 —— 沒問人，卡住的地方自己選並寫在「決定」那一節）

## 基線（今天量的，⛔ 不是票文的「6 支」）

`grep -rnE '/private/tmp|mktemp -t' scripts/ tools/ --include='*.sh' --include='*.py' --include='*.mjs'`
⇒ **53 行 · 21 檔**。分三類（⭐ 判準是「這一行會不會被執行」，⛔ 不是檔名）：

| 類 | 行 | 檔 | 動作 |
|---|---:|---|---|
| **可執行**（重導 / mktemp / 預設值 / 印給人貼的指令 / docstring 裡的重跑指令） | 34 | tune.sh(13) · host-deploy.sh:88 · asked-before.sh · branch-protection.sh(2) · ship-it.sh · reset-sandbox.sh(2) · trace.mjs(3) · force-writes.mjs(2) · sync.mjs(4) · worktree.mjs · admin-live/cache.mjs · gen_legendary_xlsx.py(2) · arena-bg/generate.py | ⭐ **全部改掉** |
| **docstring 裡的範例，別的 lane 正在改的檔** | 2 | preserve-before-overwrite.py:569-570（動手時它已是 `M`，命中從 523 漂到 569 ⇒ 確認別人在編） | ⛔ 柵欄：不碰 ⇒ **進閘的豁免表**（帶理由 ＋ 棘輪） |
| **解釋這條規則的註解** | 17 | mini-deploy(3) · genrun · ticket-lint · ruling · lane-plan · host-deploy:609 · ship.mjs · batch1.py · 以及上面那 13 檔裡的 6 行 | 留著（票文逐字：「有些是註解裡的範例，那些要留著，但閘要看得懂」）；我改到的檔順手把註解也改掉 |

⚠️ 票文說「6 支」—— 實際可執行的是 **13 支**（多出的 7 支：asked-before · reset-sandbox · sync · worktree · cache · 兩支 python）。

## 改了什麼（⛔ 不改腳本在做的事）

| 檔 | 形狀 |
|---|---|
| `scripts/tune.sh`（13 處） · `branch-protection.sh` · `ship-it.sh` · `asked-before.sh` | 檔頭 `TMPD="${TMPDIR:-/tmp}"; TMPD="${TMPD%/}"`（沿用 `ruling.sh:31-39`），路徑改 `"$TMPD/…"`（未加引號的一律補引號） |
| `scripts/host-deploy.sh:88` | `mktemp -t ggd-host-deploy` → `mktemp "${TMPDIR:-/tmp}/ggd-host-deploy.XXXXXX"`。⚠️ 量到：在 Linux 主機上它**一直**是 `mktemp: too few X's` ⇒ 這幾個月全靠 `\|\| __pin=/tmp/…$$` 的退路撐著（退路是對的，所以沒炸） |
| `tools/parallel-gates/reset-sandbox.sh` | 柵欄改成兩邊 `pwd -P` 解析**實體**路徑再比（`$TMPDIR` 或 `/tmp`）。⛔ 解析失敗退到 `/__no-tmpdir__` —— 空字串接 `/*` 會匹配任何絕對路徑，柵欄會整個消失而看起來像通過 |
| `trace.mjs` · `force-writes.mjs` · `sync.mjs`（提示文字） · `worktree.mjs`（提示文字） · `admin-live/cache.mjs` | `os.tmpdir()`（＝ node 版的 `${TMPDIR:-/tmp}`）。cache.mjs 那個「先 `existsSync("/private/tmp")` 再分岔」只是把同一個寫死藏進 if，拿掉 |
| `gen_legendary_xlsx.py` docstring · `arena-bg/generate.py` help | 文案改成 `"${TMPDIR:-/tmp}/…"` / `$TMPDIR 或 /tmp` |

## 閘：`packages/shared/src/ops/noMacOnlyTmpPaths.test.ts`（66 行，形狀照抄 `shellWideCharVars.test.ts`）

- 掃 `scripts/` `tools/` 的 `.sh/.py/.mjs`（跳 node_modules/.venv/out）；`/private/tmp` 或 `mktemp (-x)* -t` ⇒ 紅並**逐行指名**。
- ⚠️ **這是掃字串（失敗形態⑥）—— 而這一次是對的**：缺陷本身就是那個字串。一條會炸的路徑不是「行為」，是原始碼裡逐字寫著的位元組；把它改掉就是修好。檔頭寫明。
- 註解行（`#` · `//` · `* ` · `/*`）跳過 —— ⛔ `*` 後面要有空白：shell 的 `*) …` case 分支不是註解（reset-sandbox.sh 那一行就是）。
- **豁免要還命中**：`EXEMPT` 每一列若已經不命中 ⇒ 紅，逼你拿掉（棘輪只能變短）。目前 1 列：`preserve-before-overwrite.py`。
- 量尺自證：掃到的檔數 > 50，⛔ 不然在量空氣。

**誠實的界線**：它看不到 (a) 跨行字串裡的路徑（docstring 內縮的那種它看得到，因為不是註解開頭）(b) `scripts/ tools/` 以外（CLAUDE.md 自己、lane 指令都寫 `/private/tmp/msg.txt`，見下）(c) 用變數拼出來的路徑。

## 驗證

| | 結果 |
|---|---|
| vitest（run 1） | 2/2 綠 |
| **突變（票 AC-2）**（run 2） | tune.sh 檔尾塞 `: > /private/tmp/x` ⇒ **紅，指名 `scripts/tune.sh:204`** ⇒ 用 python 精準還原（⛔ 不是 git checkout），還原後 `grep -c` = 0 |
| **AC-3 Linux 容器**（uid 1001，repo 唯讀掛載） | `debian:bookworm-slim`：舊形 `> /private/tmp/x` 失敗、`mktemp -t ggd-host-deploy` → `too few X's`；新形 tune/host-deploy/branch-protection/ship-it/asked-before 五段全部建得出檔；reset-sandbox 柵欄 `/repo` ⇒ rc 2、`/tmp/sbx` ⇒ 通過。`node:22-alpine`：5 支 `--check` 綠、sandbox 預設 `/tmp/ggd-syncgraph-sandbox` mkdir OK、`cache.mjs` 真的 import 並 `defaultCacheDir({})` → `/tmp/ggd-live-cache` mkdir OK。`python:3.12-slim`：兩支 ast 解析 OK |
| `bash -n` ×6 · `node --check` ×5 · `py_compile` ×2（本機） | 全綠 |
| `branch-protection.sh check`（票的 Known risks） | 見 commit 前那一次執行的輸出（唯讀路徑，`check` 不經過暫存檔） |
| `$VAR` 緊接全形字（shellWideCharVars 那條的規則） | 新增行 0 命中 |

## 決定（沒問 owner；每一條都有回頭的開關）

| 決定 | 開關 |
|---|---|
| `admin-live/cache.mjs` 預設快取目錄從 `/private/tmp/ggd-live-cache` 改為 `os.tmpdir()/ggd-live-cache`（macOS 有 `TMPDIR` 時會落在 `/var/folders/…/T/`；launchd 沒有 `TMPDIR` 時仍是 `/tmp` ＝ 同一個實體路徑）。純快取，可丟 | 既有 env `GGD_LIVE_CACHE_DIR` |
| parallel-gates 沙盒預設從 `/private/tmp/ggd-syncgraph-sandbox` 改為 `os.tmpdir()/ggd-syncgraph-sandbox`（同上，macOS 互動 shell 下會換到 `/var/folders`；`cp -Rc` 仍在同一顆 APFS 卷） | 既有旗標 `--sandbox <dir>`；reset-sandbox 柵欄同時接受 `$TMPDIR` 與 `/tmp` |
| `asked-before.sh` 讀 transcript 撈出的 owner 原話改從 `$TMPD/` 找（那個檔今天本來就不存在，`docs/_daily/ledger-source_temp_*.md` 才是耐久的那份） | `TMPDIR` |
| 註解裡解釋規則的 `/private/tmp` **留著**（票文明說要留） | 閘的 COMMENT 規則 |

## 給主 session

1. ⛔ `scripts/preserve-before-overwrite.py:569-570`（docstring 裡的 `printf … > /private/tmp/msg.txt`）—— 別的 lane 在改；改成 `${TMPDIR:-/tmp}` 後**把閘的 `EXEMPT` 那一列拿掉**（不拿掉閘會紅：棘輪）。
2. `scripts/ruling.sh:33` 只是註解（規則本身），閘會跳過，不必動。
3. ⚠️ **CLAUDE.md 與 lane 指令本身**仍逐字寫 `printf '%s\n' "$MSG" > /private/tmp/msg.txt` / `git commit -F /private/tmp/lane2-msg.txt`（這條 lane 也照做了）。在 macOS 上沒事；如果 lane 有一天跑在 Linux 容器裡，這是下一個會靜默失敗的住處 —— 閘管不到 `docs/`/CLAUDE.md，要改要另開票。
4. 兩棵 `.claude/worktrees/agent-*` 裡還有舊版的這幾支檔（含較舊的 `asked-before.sh`）—— 合併時以 main 為準。
