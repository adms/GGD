# GH#992（棘輪 362 → 37）＋ GH#1025（逐段量測）—— 2026-09-07 第四波 lane 報告

> ⚠️ 這是 lane 報告，⛔ 不是規格。兩張票的現況以 `bash scripts/ticket-progress.sh read <票號>` 為準。

## 一句話

**GH#992**：棘輪 **362 → 37**（-325，雙向），`apps/admin` 整包 vitest **132/132 檔綠**、`pnpm typecheck` 全 repo **EXIT=0**。
**GH#1025**：三段逐段量完，⭐ **票文有兩處已過期**（段 A 的「一律 503」與段 B 的「量不到」），
本 lane 只落地了柵欄內的那一項（一句過期散文 ＋ 一條會紅的閘），其餘逐段寫「斷在哪一行、缺什麼」。

---

# 任務 A —— GH#992 棘輪 362 → 37

## 量到的（開工先量，⛔ 不引票文）

| | 動手前 | 現在 |
|---|---:|---:|
| 手寫欄位（棘輪 `BASELINE`） | **362** | **37** |
| `apps/admin` vitest | 132/132 檔綠 | **132/132 檔綠**（1322 過 · 6 skip） |
| `pnpm typecheck`（全 repo） | 0 | **0** |

逐份欠帳（動手前 → 現在）：
`stat-normalization 247 → 0` · `arena-rules 33 → 0` · `authoring-rules 27 → 0` ·
`range-guide 17 → 8` · `item-card 11 → 11` · `damage-colors 9 → 9` · `new-hero-checks 9 → 9` ·
`victory-podium 3 → 0` · `displacement-tiers 2 → 0` · `speed-growth-tiers 2 → 0` · `victory-fx 2 → 0`

## 做了什麼（四類，逐條對票的指示）

### ① stat-normalization 247 格 —— 六族模板 ＋ 31 格 owner 裁決，**搬家不是複製**

`apps/admin/src/configForms/specs/stats.ts` 的 `generatedNormalizationFields()`（一支四族迴圈的產生器）
與 `NORM_HAND_WRITTEN`（31 列）**整段刪掉**，`fields:` 改成 `derivedFields(zConfigStatNormalizationDoc, [])`。
人話全部進 `packages/shared/src/content/schema/config/statNormalization.ts`：

| 住處 | 內容 | 格數 |
|---|---|---:|
| `zBandsFor(stat)` | `bands.<屬性>.<級距>` | 55 |
| `zNormArchetypeBands(stat)` | `byArchetype.<屬性>.<定位>` ＋ 五格 `@opt` | 44 |
| `zNormOriginBands(stat)` | `byOrigin.<屬性>.<出身>`（`ORIGINS` 逐項展開） | 110 |
| `channel` 那一段 | `channel.<屬性>` ＋ 兩格 `@opt` | 11 |
| `zBandsByScaleFor(stat, type)` | `bandsByScale.<屬性>.<尺>.<級距>` | 10 |
| `zScaleByOriginRow(stat)` | `scaleByOrigin.<屬性>.<出身>` ＋ 兩格 `@opt` | 10 |
| `NORM_PROSE`（key ＝ 完整 path） | ⭐ **owner 裁決那 31 格逐字** | 31 |

⭐ **模板與 `NORM_PROSE` 不重疊**：`say(path, template)` 只在那一格有 owner 的判斷時覆蓋。
「坦克吃裝甲不吃魔抗」「移速為什麼只能走初始值」這一族**一個字都沒有被壓掉**。
⭐ 詞彙表（`STAT_ZH` 11 列 · `ARCHETYPE_ZH` · `SCALE_ZH`）也是**搬過去**的 ——
`NORM_ORIGINS` 那份手打的十列出身**刪掉了**，改用 `ORIGINS`（同一個住處）。

⚠️ **順序變了**：在此之前是「手寫的在前、產生的在後」，現在是 **schema 的宣告順序**
（`@order` 可以覆寫）。⭐ 那是刻意的 —— 一個決定只住一個地方。

### ② arena-rules 補上下界 33 格 —— 界寫回 Zod

⭐ 正解就是票裡寫的那一個：`.max(…)` 進 `schema/config/arenaRules.ts`（33 格逐格），
`specs/arenaRules.ts` 的 `{ path, max }` 覆寫**同時刪掉**。
⚠️ ⛔ **不是選擇題**：`boundsFor()` 本來就在「schema 與標籤表同時給」時丟例外
（`configForms.test.ts`「上下界只有一個來源」），所以搬過去**必須**刪掉那一份。
⚠️ **出貨值一格都沒動** —— `configForms.test.ts` 的「content/ 裡那一份過得了它自己的 schema」
逐份驗過（33 格全部落在新界內）。

### ③ authoring-rules 27 格 —— 描述由呼叫端給，選項現算

| 位置 | 形狀 |
|---|---|
| `zCooldownBand(minDesc, maxDesc)` | 那個子物件**被用兩次**（單體／範圍）⇒ 描述不可以寫死在它身上 |
| `zMinDamageTierRow(shape)` | 十五格帶形狀；五個 `@opt` 從 `DEFAULT_DAMAGE_TIERS.damage` **現算** |
| `proportionality.model` | 四個 `@opt` 由 `tableForModel()` 現算（每個模型的範圍五格） |
| `aimRiskMult` / `expectedHits` | 逐形狀的 `@zh` ＋ `@note`（owner 2026-08-20 的逐字引言保留） |
| `maxTiersAboveMin` | `@zh` ＋ `@note`（`{{出貨值}}`）＋ `describeProportionalityCeiling()` |

⭐ **那一族推導函式本來就已經 import 在那個 schema 檔裡** ⇒ 搬過去**沒有多一份會漂的知識**；
`apps/admin/src/configForms/specs/authoring.ts` 反而少了五個 import（`SKILL_TIER_NAMES` ·
`COOLDOWN_SHAPES` · `DEFAULT_COOLDOWN_TIERS` · `DEFAULT_DAMAGE_TIERS` · 整包 `proportionality`）。

### ④ 共用子物件那一族 19 格 —— 一律改成「描述由呼叫端給」

| schema | 形狀 | 格數 |
|---|---|---:|
| `config/rangeGuide.ts` | `zTelegraphChannelStyle(prose)` × 3 條通道 × 5 格 | 15（其中 9 格離開欠帳） |
| `config/victoryFx.ts` | `zVictoryFireworkTier(desc)` × 2 層 | 2 |
| `victoryPodium.ts` | `PODIUM_CLIP_OPTS`（三格共用一份 `@opt`） | 3 |
| `displacementDoc.ts` | `WALL_BLOCK_POLICY_OPTS`（⭐ 從 `WALL_BLOCK_POLICIES` **逐項展開**） | 2 |
| `config/speedGrowthTiers.ts` | `ladder` 的兩個 `@opt`（五格數字**現算**） ＋ `enabled` 的 `@zh` | 2 |

⭐ `WALL_BLOCK_POLICY_OPTS` 比它取代的東西**更強**：admin 那一份是
`satisfies Record<WallBlockPolicy, string>`（多一種處置**編不過**），新的那一份是逐項 `map`
（多一種處置**自己會多一行**，而漏標籤時 `configForms.test.ts` 的「每一個選項都有中文標籤」指名它）。

## ⛔ 修掉的兩條**靠缺陷才綠**的守衛（CLAUDE.md 假綠燈形態⑩）

⚠️ ⭐ 兩條都是**上一批**留下的，⛔ 而它們紅的意思是「帳付掉了」，⛔ 不是「回歸」：

| 守衛 | 它原本靠什麼 | 修法 |
|---|---|---|
| `packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts` 第二條（量尺兩個方向） | 拿**出貨的 `ladder`** 當「已知**有**欠帳」那一邊 —— 而這一批把它還清了 | ⭐ 改成**兩邊都由本檔造**：同一顆節點 `.describe("")` ＝ 已知有欠帳（`toEqual(["zh","note","optionLabels"])`）、出貨那一份 ＝ 已知還完了。⇒ 它量的是「這把尺分不分得出有／沒有」，⛔ 不是「今天剛好還有誰欠著」 |
| `apps/admin/src/configForms/schemaToForm.test.ts` 第二條 | `toEqual(["optionLabels","zh"])` —— 同一個原因 | 改成 `toEqual([])`（⭐ 一份整份推導得出來的 spec 欠帳就是空的），另一邊的 `probe` 校準原封不動 |

## 守衛與突變

| | |
|---|---|
| **棘輪** | `BASELINE 362 → 37`，**雙向不變**（變多紅、變少也紅）。檔內逐類寫了剩下 37 格**為什麼還在**，⛔ 不是「還沒做」 |
| **量尺兩個方向** | 見上表：這一批把它從「靠出貨欠帳」改成「自己造兩邊」——⭐ 那才是一把驗過兩個方向的尺 |
| **突變** | 本批**零新機制**（`schemaToForm` / `derivedFields` / `specFromZod` 一行沒改），全部是補既有推導路的**輸入**。⭐ 真正在守它的是棘輪本身：任何一格 `.describe()` 指令打錯 ⇒ 那一格回到欠帳 ⇒ 37 變大 ⇒ 紅並**指名那一份文件**。（GH#1025 那一條新守衛有做突變，見下） |

## ⛔ 沒做到的（誠實）

1. **`pattern` 27 格**（damage-colors 9 · range-guide 8 · item-card 10）——
   ⭐ 票的指示是「讓 `walkZod` 的 `UIText` 帶 `.regex()`」，⛔ **而那兩個檔（`apps/editor/src/form/{walk,uiSchema}.ts`）
   在本 lane 的柵欄裡被明文禁止**（「⛔ 不動 `apps/editor`」）。⇒ 照 preamble 第 3 條，寫進「需要主 session 動的」。
   ⚠️ 我**刻意沒有**在 admin 側另寫一支走訪器 —— 那就是第二份「Zod 長什麼樣」的知識。
2. **new-hero-checks 9 格** —— Zod 住 `packages/shared/src/content/newHeroChecks.ts`（⛔ 連 `schema/` 都不在），柵欄外。
3. **item-card `unknownCategory` 1 格** —— 它的四個選項中文與**下面那張 32 列對照表**共用同一份
   `ITEM_CARD_CATEGORY_OPTIONS`（`tables[].options` 也吃它）。搬去 Zod 只換得 1 格，
   代價是那張表要嘛跟著搬、要嘛變成第二份 ⇒ ⛔ 不划算，留給「表格那一半也從 Zod 推導」的那一輪。
4. **item-card 的 8 格 `categories.*.{label,color}`** 的 `zh`/`note` 仍在 spec ——
   它們的欠帳理由是 `pattern`（第 1 點），搬 `zh`/`note` **一格棘輪都不會動**；
   ⚠️ 但那仍是「同一句人話兩個住處」⇒ 與第 1 點一起做才對。

---

# 任務 B —— GH#1025 逐段量測（⭐ 票文兩處已過期）

⚠️ ⭐ **本 lane 沒有做 A/B/C 的實作**，兩個理由，兩個都寫在票上：
① 票 body 最上面的 owner 逐字裁決（2026-09-06）「**你把相關的票開好就好了，現在開始我想嘗試
你做開票跟驗證、codex 來做開發實作**」；
② 這張票的落點（`apps/game-server/**` · `packages/shared/src/roomSettings.ts` · `tools/review/**` ·
`content/config/ugc.json`）**幾乎整片在本 lane 的檔案柵欄外**。
⇒ 交付的是**量測 ＋ 逐段「斷在哪一行、缺什麼」**，加上柵欄內的那一項（一句過期散文 ＋ 一條會紅的閘）。

## 段 A —— 一步發布（冪等鍵）

| 問題 | 量到的 |
|---|---|
| 「通過」與「套用」還是兩個按鈕嗎？ | ⭐ **是**。`apps/admin/src/ui/SubmissionsReviewPage.tsx:162-168`（頁面上逐字說「兩個決定」）· `:233-241` ☑️ 通過 · `:279` 🚀 套用 · `:451-453` Promote |
| 冪等鍵 `x-ggd-operation-id` 在哪？ | ⭐ **在 `apply`（ZIP 匯入）那一條，⛔ 不在 promote**：`apps/content-api/src/importRoutes.ts:1087`（header）· `:1090-1102`（必填）· `:1103-1113`（重放回同一個結果）。⇒ **promote 的 body 是 `{confirm, expectedDigest, reason}`**（`apps/admin/src/api.ts` 的 `promoteSubmission`），⛔ 一個 operationId 都沒有 |
| promote 今天防重複靠什麼？ | ⭐ **指紋**，⛔ 不是冪等鍵：`apps/platform/internal/submissions/promote.go:203-206`（重驗之後**再讀一次**材料，位元組變了 ⇒ 409 `candidate_changed`）＋ `PromotableWithOwnership` 的 `ApprovedDigest != Digest ⇒ 失效` |

### ⛔⛔ 票文過期處①：「promote 一律回 503」**不成立**

`apps/admin/src/api.ts` 的 `promoteSubmission()` 檔頭逐字寫著
「今天這條路線**一律回 503 `revalidator_missing`**」。
⭐ **那句話從 2026-09-02（GH#1022）起就是假的**：
`apps/platform/internal/server/playercontent.go:186,204` 的 `submissionPromoteDeps()` 從
`GGD_CONTENT_API_URL` 造 `ContentAPIRevalidator`，promote 會對 content-api 的
`POST /content-import/validate` 發**一次真的驗證**；`docker/compose.yaml:95` 也已經有 pass-through。
守衛 `apps/platform/internal/server/playercontent_digest_test.go:57-70` **雙向釘死**
（沒設 ⇒ 鉤子必須 nil；設了 ⇒ 鉤子不可以 nil）。

⇒ ⭐ **503 是「那個環境變數沒設」的樣子，⛔ 不是「這條路線還沒做」** —— 兩者的下一步完全不同。
⚠️ 這正是第三守則記過的形狀：**一句在它到期之後還活著的散文，⛔ 而沒有任何東西會紅**。

**⭐ 本 lane 落地的那一項（柵欄內）**：
· `apps/admin/src/api.ts` 的那一段改寫（舊句子**另存在引號裡**，⛔ 不是刪掉 —— 第一·五守則）
· 新守衛 `apps/admin/src/promoteRevalidatorProse.test.ts` —— ⭐ 它驗**兩個名詞的關係**：
  Go 側**有沒有**把鉤子接到環境變數（讀出貨原始碼，⛔ 不是讀註解）× TS 側**有沒有**還在宣稱「一律 503」。
  ⚠️ 判斷前先剝掉 `「…」`（引言不是主張，與 CLAUDE.md 第〇·六守則同構），母體有下界（剝完 <400 字 ⇒ 紅）。
· **突變驗過**：在那個檔頭插回一行「今天這條路線一律回 503 revalidator_missing。」⇒ **紅**，
  訊息指名兩個檔與 GH#1022；用 `Edit` 改回來（⛔ 不是 `git checkout`）。

### ⇒ 段 A 今天真正缺的（給 Codex）

1. `POST /submissions/:id/promote` **收不到 operationId** —— 冪等要嘛沿用 `x-ggd-operation-id`
   （`importRoutes.ts:1087` 那一套）、要嘛承認「指紋就是冪等鍵」並寫進 AC。⛔ 今天票的 AC
   「同一個 operation-id 送兩次 ⇒ 只發布一次」在 promote 這條路上**沒有欄位可放**。
2. 「一個動作」＝ 前端把 `decideSubmission("approved")` ＋ `promoteSubmission()` 併成一顆按鈕
   （`SubmissionsReviewPage.tsx`），⭐ 而 Go 那一側**兩個半邊本來就是分開寫的**
   （`promote.go:186` 逐字「Promote 寫**第三個**半邊。⛔ 它一個內容欄位、一個裁決欄位都不碰」）
   ⇒ 併按鈕**不需要**動 Go 的資料模型。

## 段 B —— 覆蓋層熱生效

| 問題 | 量到的 |
|---|---|
| `contentBus.ts` 那條 `ok:false` 還在嗎？ | ⭐ **在**：`apps/game-server/src/config/contentBus.ts:203-210`，`"content-overlay": { run: async () => ({ ok: false }) }` |
| 它是**靜默**的嗎？ | ⛔ **不是**。它帶 `consequence` 字串、上 `/healthz`（`healthz.ts:19` 的 `platformStatusWithContent`），而且檔頭逐字說明「⭐ 那正是 fail-loud」。⇒ ⭐ **票文「按下發布之後什麼時候到玩家眼前**今天量不到**」在 ops 那一層不成立** —— shard 說得很清楚：「到重啟為止都不會用」 |
| ⭐ 那**哪一層**真的沒有答案？ | **後台審核頁**。`grep content-overlay apps/admin/src/**` ⇒ 只有 `contentOverlay.ts`（寫入端）與測試，⛔ **`SubmissionsReviewPage.tsx` 一個字都沒說「什麼時候生效」** |

### ⛔⛔ 票文過期處②（⭐ 這是最重要的一個發現）：熱生效**不是全斷**，而**分母**決定 AC 成不成立

`defaultRefreshers` 有**四個 kind**，⭐ **三個是活的**：

| kind | `run` | 活的？ |
|---|---|---|
| `curation`（⭐ **白名單**） | `sharedWhitelistCache().refresh()` | ⭐ **活的** |
| `combat-env` | `sharedCombatEnvCache().refresh()` | ⭐ 活的 |
| `server-ops` | `sharedServerOpsCache().refresh()` | ⭐ 活的 |
| `content-overlay`（技能／英雄／道具**文件**） | `{ ok: false }` | ⛔ **斷的** |

⇒ ⭐ **AC「按下發布 → 開一個新房 ⇒ 那隻英雄選得到」有兩個分母，而票文沒有分開：**

| 情況 | 今天會發生什麼 |
|---|---|
| 那隻英雄的**文件本來就在出貨樹裡**，發布只是把它**加進白名單** | ⭐ **今天就會生效**：`curation` refresher 是活的，而白名單在**開房那一刻**取快照（`MatchRoom.ts:465-471`）⇒ 下一場就選得到 |
| 那隻英雄是**只住覆蓋層的新文件**（UGC 的正常情況） | ⛔ **白名單放行也沒用** —— `loadContent()` 只在開機跑一次，shard **根本不認得那個 id** |

⚠️ ⭐ 而「對局不中途換版」那一半**今天是真的**（票文說對了）：
`curation/liveRefresh.test.ts:57-80` 逐條釘死「白名單縮小不會從進行中的比賽抽走英雄」、
「選角中途縮小仍然收下當時提供的那一隻」。⇒ ⭐ **熱生效不會破壞進行中的對局，⛔ 這一點不必重做。**

### ⇒ 段 B 今天真正缺的（給 Codex）

1. `contentBus.ts:203-210` 的 `ok:false` —— 熱換整棵內容樹（`index.ts` 的 `loadContent()` ＋
   `registerAll()`）。⚠️ ⭐ 難點檔頭已經寫明：**進行中的比賽握著那些定義的參照**。
2. ⭐ **或者**（票說兩條都可接受）：`SubmissionsReviewPage.tsx` 顯示「這一台 shard 到重啟為止不會用」
   —— ⭐ 資料**已經存在**（`/healthz` 的 `content` 區塊），缺的只是後台把它讀出來畫在按鈕旁邊。
   ⇒ ⭐ **這是兩條路裡便宜得多的那一條**，而且它把「不知道」變成「知道」。
3. ⚠️ ⭐ **AC 要先分母**：「那隻英雄選得到」是①白名單題還是②新文件題？①今天就成立，②要等第 1 點。
   ⛔ 不分開的話，一條只驗①的測試會**在②壞著的時候是綠的**（一把單邊的尺）。

## 段 C —— 社群房 `contentPool`

| 問題 | 量到的 |
|---|---|
| `contentPool` / `roomType` 存在嗎？ | ⛔ **零命中**：`grep -rn "contentPool\|roomType" --include=*.ts --include=*.go apps packages` ⇒ **0 行**。票文說對了 |
| `roomSettings.ts` 今天有幾格？ | **四格**（`champSelectSec` · `intermissionSec` · `combatMaxSec` · `maxRounds`），`ROOM_SETTING_KEYS` ＋ `ROOM_SETTING_LIMITS` ＋ `sanitizeRoomSettings` 三處都要跟著加 |
| ⚠️ 加一格要注意什麼？ | ⭐ `roomSettings.ts` 的檔頭逐字寫著「這個檔是**契約**」：四層讀它（client 表單 min/max · game-server 權威夾取 · `config@1` 的 Zod 上下界 · 後台顯示）。⛔ 而 `contentPool` **不是數字**（是 enum）⇒ `sanitizeRoomSettings` 今天的迴圈**只處理數字**（`Number(received)` / `below-min` / `above-max`）⇒ ⭐ **加一個 enum 格要先讓那支洗淨函式長出「enum 那一種拒絕理由」**，⛔ 不是硬塞進數字那條路 |

### ⇒ 段 C 今天真正缺的（給 Codex）

1. `packages/shared/src/roomSettings.ts`：`contentPool` 加進 `RoomMatchSettings` ＋ `ROOM_SETTING_KEYS`，
   ⭐ 並讓 `sanitizeRoomSettings` 長出**非數字**欄位的拒絕理由（今天四個理由全部是數字語意）。
2. `apps/game-server/src/rooms/MatchRoom.ts:465-471`：開房快照那一行旁邊多一個「這一房吃哪個池」。
   ⛔ **不要動它的語意**（快照在開房那一刻取，那是對的）。
3. 三個住處的開關 `ugc.communityRoomOnly` / `ugc.publishMode`（`content/config/ugc.json` ＋ Zod ＋ admin）
   ＋ ⭐ **第四個住處：消費端** —— 回報時要寫得出「`<檔>:<行>` 讀它」（GH#1035／#1043 的教訓）。

## 段 D（G12「誰批的」）—— 票文說對了

`tools/review/stores.mjs:56` 的 `VERDICT_FIELDS` **收 `"by"`**，
⛔ 而 `tools/review/features.mjs:483-496` 的 `saveFeatureVerdict()` **連參數都沒有 `by`**，
`saveVerdictEntry(...)` 只寫 `{verdict, verdictHash, reason, verdictAt}`。⇒ ⭐ 那一格永遠是空的。

⚠️ ⭐ **但這與 `submissions` 那一條路是兩回事，⛔ 不要混**：
`apps/platform/internal/submissions/promote.go:194-195` **要求** `by` 非空
（「promotion requires an authenticated admin actor」）並寫進 `Promotion.PromotedBy`。
⇒ ⭐ 「批核紀錄有誰」在**投稿**那一側**已經有了**；缺的是**功能批次驗收帳本**那一側。

## 需要主 session 動的（柵欄外）

| 檔／指令 | 為什麼 |
|---|---|
| ⭐ `apps/editor/src/form/{walk.ts,uiSchema.ts}` | GH#992 剩下 27 格 `pattern` 的正解：把 `ZodString` 的 regex check 帶進 `UIText`。⛔ 本 lane 的柵欄明文禁止動 `apps/editor` |
| `packages/shared/src/content/newHeroChecks.ts` | GH#992 那 9 格的 Zod 住這裡（⛔ 連 `schema/` 都不在）。同一套 `@zh`/`@note` 貼上去就收 |
| ⭐ `pnpm content:build`（＋ 把來源檔一起 commit） | ⚠️ `npx vitest run --dir packages/shared/src/content` 今天 **9 檔 17 條紅**，⭐ **全部**是別條 lane 的在飛內容改動造成的 bundle 過期（`git status content/` ⇒ 10 份 abilities ＋ 3 份 ability-templates 是 `M`）。⛔ **與本 lane 無關**（本 lane 一個 `content/` 檔都沒碰），⛔ 但它會擋 commit 前的全跑 |
| `bash scripts/rule-slip.sh 3-註解說謊 沒有閘 "…"` ＋ `2-守衛 沒有閘 "…"` | ⭐ 這一批修掉的兩條「靠缺陷才綠」的守衛與一句過期散文，照第零守則📋 要進 `docs/守則犯錯.md`。⛔ 那個檔在柵欄外而且今天是 `M`（別人在寫），所以本 lane **沒有寫**，請主 session 補兩列 |
| `tools/review/features.mjs` · `apps/game-server/**` · `packages/shared/src/roomSettings.ts` · `content/config/ugc.json` | GH#1025 段 B/C/D 的落點，全部在柵欄外（且 owner 裁決那部分歸 Codex） |

## 順手發現但沒動的

- ⚠️ `handWrittenResidue()` 量的是「**Zod 給不給得出來**」，⛔ 不是「spec 有沒有第二份」——
  一格只要 Zod 有 `@zh`/`@note`，即使 spec 的覆寫**還寫著同一句話**也不算欠帳。
  ⇒ ⭐ 這條棘輪**擋得住退步，⛔ 擋不住「兩個住處」本身**。這一批我逐份把搬走的覆寫**真的刪掉**了
  （⛔ 沒有靠這個洞把數字做小），⛔ 但下一個人可以。真正的閘應該再加一條：
  **覆寫裡的 `zh`/`note` 如果與推導出來的相等 ⇒ 紅**。⛔ 本 lane 沒加（新機制要突變驗證，超出這一輪）。
- ⚠️ `configFormsShippedProse.test.ts` 只掃**數字**出貨值 ⇒ **布林**的宣稱（「出貨**開著**」）它結構上看不見。
  今天已經有一例被別條 lane 抓到並修掉（`skipTransformedBodies`，GH#1095）——
  ⭐ 那不是巧合，是**同一個結構性盲點**：`if (typeof v !== "number") continue;`。
- ⚠️ `apps/admin/src/configForms/specs/tiers.ts` 的 `SG()` / `SG_SHIPPED_TIER` 仍在（`intro[]` 用得到），
  ⛔ 但 `ladder` 的選項標籤現在是 schema 側**現算**的第二份同型計算。⭐ 正解是 `content/speedGrowthTiers.ts`
  匯出一支 `describeSpeedGrowthLadder(id)`，⛔ 那個檔在柵欄外。

## 檔案（本 lane 改的，逐檔列名）

**`packages/shared/src/content/schema/`**
`displacementDoc.ts`（`WALL_BLOCK_POLICY_OPTS` ＋ blink/leap 兩格）·
`victoryPodium.ts`（`PODIUM_CLIP_OPTS` ＋ 三格 clip）

**`packages/shared/src/content/schema/config/`**
`arenaRules.ts`（⭐ 33 格 `.max()`）· `authoringRules.ts`（`zCooldownBand(…)` 參數化 ＋ `zMinDamageTierRow(shape)`
＋ model/aimRisk/expectedHits/maxTiersAboveMin）· `rangeGuide.ts`（`zTelegraphChannelStyle(prose)`）·
`speedGrowthTiers.ts`（enabled ＋ ladder）· `statNormalization.ts`（⭐ 六族模板 ＋ `NORM_PROSE` 31 格）·
`victoryFx.ts`（`zVictoryFireworkTier(desc)`）

**`apps/admin/src/configForms/specs/`（刪掉搬走的那一份）**
`arenaRules.ts` · `authoring.ts` · `stats.ts`（⭐ 產生器 ＋ 手寫表整段刪）· `tiers.ts` · `ui.ts` · `visuals.ts`

**`apps/admin/src/`**
`api.ts`（GH#1025：`promoteSubmission` 檔頭的過期散文）·
`configForms/schemaToForm.test.ts`（靠缺陷才綠的那一條）

**新增**
`apps/admin/src/promoteRevalidatorProse.test.ts`（GH#1025 段 A 的閘，突變驗過）

**閘**
`packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts`（`BASELINE 362 → 37` ＋ 逐類理由 ＋ 量尺改成兩邊自造）

⛔ **不是本 lane 的**（同一棵樹上別條 lane 的在飛改動）：
`content/abilities/*.json`（10 份）· `content/ability-templates/*.json`（3 份）·
`schema/config/statNormalization.ts` 的 `skipTransformedBodies` 那一段（GH#1095，在我搬完之後貼上來的）。
