# Lane RA — GH#690 runtime alpha 逐技能接線（#688 Phase 4b）· 2026-08-25

> ⛔ **鏈路已接上，未驗收**（alpha 只是四欄之一 —— 回填了不保證玩家看得見）。

## 一、量到的：57 個呼叫點 → 只有 **3 支** rawcode 在出貨內容上有落點

census（`tools/locust-census/census.json` §runtimeAlpha，`pnpm locust:build` 的產物）
把 57 個 `SetUnitVertexColorBJ` 分成三類：**resolved 26**（回溯得到 dummy 的 rawcode）·
**event-unit(-var) 25** · **unresolved 6**。

resolved 的 26 個呼叫點落在 **18 個 rawcode** 上，其中 **15 個**有可計算的 α%
（另 3 個 —— `o015`×2 / `o01G`×2 / `o01D`×1 —— 第 4 參數是 JASS 算式
`I2R(udg_WindEffectCount)*1.5` 這一族，census 標 `—`，⛔ 不求值）。

⭐ **join key 是模型，⛔ 不是名字**：census 的 `units[].model`（`Foo.mdl`）→
出貨模型文件 id（`imported.foo` / `w3x.stock.foo`）→ 出貨 `spawnModelFx.modelKey`。
15 具裡只有 **3 具**的模型今天真的被出貨節點引用：

| rawcode | dummy | 模型 | census α% | 出貨落點 | 動作 |
|---|---|---|--:|---|---|
| `h002` | 幻影（Trig_SuperFF7 L33818） | `HeroCloudStrife.mdl` | **60** | `godie-hart.r` **01-04 超究武神霸斬** | ✅ 填 `alpha: 0.6` |
| `o018` | 索龍分身（Trig_Roaction L29012–29034，6 個呼叫點同值） | `HeroMusashiMiyamoto.mdl` | **50** | `godie-u01u.r` ＋ `godie-udre.r` **11-04 三千世界**（變身對子兩邊） | ✅ 各填 `alpha: 0.5` |
| `o01L` | 召喚顯示特效（Trig_AKT_CloseEffect L31048） | `OblivionAura.mdl` | **100** | `godie-etyr.q` 14-01 東風繪扇、南風末廣 | ⛔ **刻意不填** |

⭐ **`o01L` 為什麼不填**：α=100% ＝ 全不透明 ＝ **一格乘 1 的空宣稱**（第一·五守則，
同 `fxTint` 中性色不填的前例）。原作那一行是 `(100,100,100,0)` —— 那是**染回原色的還原呼叫**，
⛔ 不是一個效果。守衛①反過來斷言那個節點**沒有** `alpha`。

⚠️ **只回填 α，⛔ 沒回填 tint**：tint 的來源是 w3u 的 `uclr/uclg/uclb`（`UNIT_TINTS.json`），
⛔ 不是這 57 個呼叫點 —— 混進同一批就是把兩個來源的帳算成一本。
（`o018` census tint `[50,0,0]`、`o01C` `[255,0,0]`… 逐隻在 `蝗蟲群對應表.md` §五。）

## 二、改了什麼

| 檔 | 改動 |
|---|---|
| `content/abilities/godie-hart.r.json` ＋ `content/champions/godie-hart.json` | `spawnModelFx.alpha: 0.6` |
| `content/abilities/godie-u01u.r.json` ＋ `content/champions/godie-u01u.json` | `spawnModelFx.alpha: 0.5` |
| `content/abilities/godie-udre.r.json` ＋ `content/champions/godie-udre.json` | 同上（變身對子的另一邊） |
| `packages/shared/src/content/runtimeAlphaBackfill.test.ts` | **新**：census ↔ 內容兩方向對帳 ＋ 豁免表 |
| `packages/shared/src/content/fieldAdoption.test.ts` | 刪掉 `spawnModelFx.alpha` 的 landing 豁免（它自己寫著「第一支回填出現那天就 stale」，STALE 斷言逐字點名它「now adopted by 6 doc(s)」） |
| `packages/shared/src/content/templates/locustTemplates.test.ts` | ②的逐位元比對**排除 `alpha`** 一格 |

⭐ **為什麼 `locustTemplates` ② 是排除而不是把 alpha 補進 golden**：那張 golden 的契約是
「`git show HEAD:` 的**逐字原文**」（那是「逐位元等價」這句話的出處）。把回填值補進去
就會讓它變成一份**我照著回推的副本** —— 而 alpha 不是演出幾何，它有自己的兩方向守衛。

⚠️ **產物隔離區**：6 份內容檔是 `apconv:build`／`tiers:apply` 的**就地正規化**目標
（genguard 逐檔問過：「不是那支的產物 ⇒ 不擋你」，且 `grep -rl` 到 `tools/` 只撈到
census／report 這類**唯讀輸入**，⛔ 沒有上游產生器來源）。
⇒ 走 `scripts/product-quarantine.sh unlock --step apconv:build|tiers:apply` → 編輯 →
`scripts/product-quarantine.sh lock`（收工已鎖回 403 份）。
⛔ 沒有手動 `chmod`，⛔ 沒有跑 `tiers:apply`／`apconv:build`（全域鎖，而且 `alpha`
不在那兩支重算的欄位裡）。

## 三、守衛（`runtimeAlphaBackfill.test.ts`，三條）

| # | 方向 | 紅的條件 |
|---|---|---|
| ① | **census → 內容** | 某具 dummy 有 α 而出貨有節點用它的模型 ⇒ 那個節點沒填／填錯 ⇒ 紅；沒有採用者又不在 `EXEMPT` ⇒ 紅；已經有採用者卻還留在 `EXEMPT` ⇒ 紅（要求刪那一列）；α=100 卻填了 ⇒ 紅 |
| ② | **內容 → census** | 節點上的 `alpha` 回不到 census 的某一具 dummy ⇒ 紅（⛔ 沒有人可以自己挑一個透明度）；一格都沒回填 ⇒ 紅（接線掉了） |
| ③ | **57 個呼叫點逐筆有歸宿** | 沒有 rawcode 又不屬於兩類分類豁免 ⇒ 紅；數量與 `census.meta.counts` 對不上 ⇒ 紅 |

**突變驗證（一批一條，承重線）**：`godie-u01u.r` 的 `alpha` 0.5 → 0.4 ⇒ ①②同時紅，
訊息逐字指名：`abilities/godie-u01u.r.json（o018）alpha 與 census 不符：內容 0.4 · census 50% ⇒ 0.5`。
改回 0.5（用字串取代，⛔ 不是 `git checkout`）。

### 豁免表（誠實列出來的，⛔ 不是猜一支技能塞進去）

**A. census 有 α、出貨零採用者（12 具）** —— 逐列理由寫在測試的 `EXEMPT`，都能被反駁
（那支技能一移植／改用這具模型，①就會要求刪掉那一列）：

`h016` 飛鼠先生殘影(HeroDeathKnight) · `h023` 熊熊幻影(PolarBear) · `h027` 三檔旋風(TornadoElemental，
⚠️ α=0.01% ≈ 全隱形，移植前要先確認那不是原作的死演出) · `h02G` Saber 殘影(HeroSaber，模型文件有、零引用) ·
`o01C` 殺生丸幻影(Sesshomaru，模型文件有、零引用) · `o01E` 雷光劍(BlinkTarget) · `o01V` 黑龍波龍形2(DuneWorm) ·
`o02J`／`o02K` 涅吉那兩支(Banditmissile) · `o02T` B 叔殘影(FelgaurdBlue) · `o02X` 96-04x 獨孤九劍殘影(hzyn) ·
`u018` 安云衝刺(DarkPortalTarget)

⚠️ **`o01E` 是最想猜的那一個**：它的 dummy 名字就叫「雷光劍」，而出貨有一支 **77-04 真-雷光劍**。
⛔ 但那支用的是 `w3x.stock.monsoonbolttarget`，⛔ 不是 `BlinkTarget` ——
**名字不是 join key，模型才是**。⇒ 留在豁免表，⛔ 不塞。

**B. 25 個 event-unit(-var) 呼叫點** —— 目標是**事件單位**（`GetTriggerUnit`／`GetDyingUnit`／
`udg_XxxUnit` ＝ 施法者本人），⛔ 不是生成的 dummy。那是「把英雄自己染色」的**狀態演出**，
引擎對應物不是 `spawnModelFx` ⇒ ⛔ 解不出技能歸屬就不猜。
（而且其中很多是 `(100,100,100,0)` 的**還原**呼叫，本來就不該有內容欄。）

**C. 6 個 unresolved** —— census 逐字：`udg_BlackDragonUnit` 在 war3map.j **全檔零指派**（5 筆，
＝**原作死碼**）＋ `udg_PandaUnit` 一筆解不開的指派（1 筆）。

## 四、測試帳

vitest ×4（①三支合跑 ②修完紅再一次 ③突變 ④mirror/noOp/tsc 修正後確認）·
`tsc` ×2（第一次 `packages/shared` 兩行 TS2532，修完 EXIT=0）·
⛔ 未跑任何全域鎖（`content:build` / `skills:sync` / `tiers:apply` / `apconv:build` / `caps:export`）。

⚠️ **誠實記一筆（測試預算）**：實作只有 **6 行 JSON**，而守衛 191 行 ⇒ 帳面上超標。
理由是票上的 `Test / verification criteria` 逐字要求「census ↔ 內容對帳守衛（兩方向）」
與「unresolved 進豁免表**帶理由**」—— 那 12＋3 列**理由本身就是交付物**，
⛔ 不是為了驗一格 JSON 而多寫的斷言。

## 五、⚠️ 留給主 session

1. ⛔ **`content/bundle.json` / `_index.json` 現在對這 6 份檔是過期的** —— 收斂時要跑
   `pnpm content:build` 並 `git add content/`（`shippedBundleIsCurrent` 會紅）。
2. `field:abilities.effects[]#spawnModelFx.alpha` 的豁免已刪；**`field:models.fxAlpha`
   （模型級那一半）仍是 `landing`** —— 這一批刻意沒動它（模型級 = 「這具 dummy 天生半透明」，
   而 census 的 57 個呼叫點是 per-cast 那一半；把 `imported.herocloudstrife` 整份調透明
   會連英雄本體一起變半透明）。
3. `docs/蝗蟲群移植計畫.md` 的 Phase 4-② 那一格可以把「57 個 per-cast 接線是後續批」
   更新成「resolved 側完成（3 支落地 · 12 具豁免 · 25+6 誠實留表）」—— 我沒有改它，
   避免與別的 lane 在同一份計畫表上撞車。
4. ⛔ **未驗收**：alpha 只是「大小顏色透明度身分時序」五欄之一，回填不等於玩家看得見。
   要驗收得走 Phase 6 的一頁式連續圖片批核（#669）。
