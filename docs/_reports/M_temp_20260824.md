# Lane M — GH#679 滑鼠二段施放 · GH#680 選人技能說明捲動（2026-08-24）

## 結論先講

兩張票的**核心在今天凌晨就已經落地**（兩張票是 21:05 批次開的，引的正是同一句 owner 原話）：

| 票 | 已落地於 | commit | 時間 |
|---|---|---|---|
| #679 二段施放 | GH#639（同一句 owner 逐字） | `13407ceb` | 01:14 |
| #680 說明捲不動 | GH#640（同一句 owner 逐字） | `ea8dc8b7` | 01:21 |

⇒ owner 若在部署站上再現，多半是**部署落後**，不是程式還壞著。

## #679 —— 本 lane 補上的兩個真缺口

既有鏈路（點格→武裝→`ui/abilityHold` 圈→場景點→`buildCastCommand`→`onCommand`）完整，
targeted/dir/ground/self 四型都走同一支 `AimResolver.buildCastCommand`（與鍵盤 quick-cast 同路）。
缺的是：

1. **Esc 取消不存在**（票的驗收列了「再點按鈕/右鍵/Esc ⇒ 取消」，前兩個有、Esc 沒有）
   → `InputCapture.onKeyDown` 加 `case "Escape": cancelTwoStageCast()`。
   ⚠️ 只清瞄準，A 武裝/面板各自的 Esc 一格不動。
2. **指定型（targeted）沒有二段守衛**（票明文「指定型與方向型都要能瞄」）
   → `mouseTwoStageCast.test.ts` 補兩條：Esc 取消；targeted 點空地維持瞄準/點敵人送
   `{type:"entity"}`。方向型與 ground 共用同一條解析路（`inputMapping.test.ts` 47 條在守），不重驗。

**後台開關**：⛔ 沒有新開 `mouseButtonTwoStageCast` —— 同一個決策已有一格
`uiCues.mouseTwoStageCast`（GH#639），三個住處齊全且出貨 **on**：
`content/config/ui-cues.json:15` · `schema/config/uiCues.ts`（DEFAULT true）·
`apps/admin/src/configForms.ts:5322`。再開一格就是第〇·四守則的第二個住處。

**驗證**：4 檔一次跑（mouseTwoStageCast 4 · profileScroll 3 · TouchInput 20 · inputMapping 26）
= 53/53 綠 ⇒ 觸控長按流程零迴歸。
**突變**（本批一條）：拿掉 `Escape → cancelTwoStageCast()` → 🔴「Esc＝取消瞄準 (GH#679)」，已還原。
（#639 的承重突變 `castTwoStage return` 已記錄在 `13407ceb`。）

## #680 —— 驗證完畢，零改動

- 桌機：`profileScrollContract`（`championProfile.ts`）三層鏈 —— tab body `overflowY:auto` +
  160px 地板 → stage 讓位到 180px → root column 最後兜底捲動。守衛 `profileScroll.test.ts`（50 行，3 條）綠。
- 手機：外層單欄 `overflowY:auto` + `WebkitOverflowScrolling:touch`，內層不開第二條捲軸。
- 手把右搖桿：`padFocusNav` #506 用 **computed style 泛掃** 找 overflow box
  （`scrollTargetFor` → nearest scrollable ancestor，掃不到就取最大 overflow box）——
  ⭐ `data-pad-scroll` 這個屬性**全 repo 不存在**，#507 那族不是用屬性標的，不必補。
- 觸控拖曳：真 overflow box + 預設 touch-action ⇒ 原生可捲。

## 順手發現（⛔ 未修，不在柵欄內）

- `apps/client/src/render/vfx/w3xRigHonoursDissipateCap.test.ts:68` **在 HEAD（`43bda859`，#660 lane）
  就 typecheck 紅**：TS2554 Expected 0 arguments, but got 1。⛔ render/vfx 是別的 lane 的柵欄，只回報。

## 改動檔案

- `apps/client/src/input/InputCapture.ts`（Esc case，+5 行）
- `apps/client/src/input/mouseTwoStageCast.ts`（header 補 Esc 取消路）
- `apps/client/src/input/mouseTwoStageCast.test.ts`（+2 條：Esc · targeted；stub 改可換）

測試/實作比：本批新測 ~30 行 > 新實作 ~7 行 —— 但 targeted 那條是 #639 已落地 100+ 行
實作的**票面驗收**（「castAbility 收到正確 target」），合併計算在上限內。
