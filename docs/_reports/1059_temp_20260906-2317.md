# GH#1059 —— `tools/icon-gen` / `tools/testkit` 檔頭的「沒有 package.json／不是 workspace」過期散文

> lane 報告（第二波，2026-09-06 23:17）。⛔ 未 commit —— 主 session 統一 commit。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `tools/testkit/findPython.ts:2-15` | 檔頭理由改成現況：**這個目錄**不是 workspace 套件（刻意沒有 package.json），而 `tools/icon-gen` 與 `tools/w3x-import` **是**套件（`@ggd/icon-gen` / `@ggd/w3x-import`）仍相對 import 它。末尾多一行**結構化宣稱** `ggd:workspace-package tools/testkit=no tools/icon-gen=yes tools/w3x-import=yes` |
| `tools/icon-gen/test/icon-gen.test.ts:23-30` | `cover` 為什麼仍是相對 import 的理由改成真的：`@ggd/icon-gen` **是**套件，但它的 package.json 只宣告 `vitest`（`tools/icon-gen/node_modules/` 量到只有 `vitest`）⇒ `@ggd/shared` 沒有 node_modules 入口可解析；加相依會動 lockfile（票文 ⛔）。`findPython` 相對是另一個理由（testkit 沒有 package.json）。末尾同一行 `ggd:workspace-package tools/icon-gen=yes tools/testkit=no` |
| ⭐ 新增 `tools/icon-gen/test/workspaceClaims.test.ts`（33 行） | 薄守衛：讀兩個檔的 `ggd:workspace-package` 行，逐個 `<dir>=yes\|no` 與 `existsSync(<dir>/package.json)` 比對 —— **從 package.json 存在與否推導**，⛔ 不是掃「有沒有那句過期散文」。兩個方向：testkit 哪天長出 package.json ⇒ 紅並點名檔案；icon-gen 的 package.json 被拿掉 ⇒ 紅。宣稱行不見 ⇒ 也紅 |

⚠️ 票文 Scope 問「要不要把 `cover` 改回 `@ggd/shared/testkit/cover`」—— **不改**，理由量到了：bare specifier 在 `tools/icon-gen` 解析不到（沒有 `@ggd/shared` 相依），而票文 ⛔ 不動 package.json／lockfile。

## 量到什麼

- `pnpm --filter @ggd/icon-gen test`：`icon-gen.test.ts` **8/8 綠**（AC 保住）＋ `workspaceClaims.test.ts` 2/2 ⇒ 10/10。
- 靶向 `tsc --noEmit`（scratchpad tsconfig extends `tsconfig.base.json`，files 含 `workspaceClaims.test.ts`）⇒ EXIT 0。

## 突變紀錄

`findPython.ts` 的宣稱行 `tools/testkit=no` → `=yes` ⇒ 🔴
`tools/testkit/findPython.ts claims tools/testkit IS a workspace package, but tools/testkit/package.json does not exist — fix the header…`。用 Edit 改回。

## ⛔ 沒做到的

無。票文的 `GGD_COVERAGE_FILE` beacon 那一半沒另外驗（既有 8 條測試本身沒動，`cover` import 也沒動）。

## 需要主 session 動的柵欄外檔案

無。

## 順手發現但沒動的

- `tools/w3x-import/test/*.test.ts` 六支同樣相對 import `../../testkit/findPython`，檔頭沒有過期散文（grep 過），所以沒加宣稱行；`findPython.ts` 那一行已涵蓋 `tools/w3x-import=yes`。
