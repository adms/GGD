# V3_c1 — 開票規格 v3 追加（24 張）｜2026-08-24

lane：V3_c1。任務：在 24 張票尾**追加** v3 六節（Objective/Scope/Non-goals/Files/Dependencies/Constraints/Risks/Test），既有 v2 footer 一字未動。冪等（已有「開票規格 v3 追加」者跳過）。Files 節逐張以 grep/ls 對 repo 實查，⛔ 不憑票名猜。

收尾閘：`bash scripts/ticket-lint.sh <24 張>` → **EXIT 0 全綠**（十件必填齊、三節建議節也齊，零 advisory 警告）。

## 逐張一行

| # | v3 重點（Files 實查發現） |
|---|---|
| 325 | zZoneDef superRefine＋fail-loud；TEAM_SIZE 實址 `packages/shared/src/constants.ts:29`（import ⛔ 不抄） |
| 327 | strict-by-default＋tokenizer；實查 import/ 目錄 **tokenizer 不存在**（新模組）；計畫檔在 repo 根 `main_load_editor_plan.md` |
| 330 | AbilityBar 實址 `apps/client/src/ui/components/AbilityBar.tsx`；自動加點復用 `Tier0Brain.ts:220`；開關三住處 |
| 382 | 地標 role 分母：`tools/model-budget/{limits,guard,baseline}`；LOD 是 `tools/lod-gen/gen_lod.py` |
| 401 | tpl-line-sweep 展開在 `templates/expand.ts`；DELAYED_MAX_COUNT 在 `sim/effects/kindLimits.ts` |
| 409 | navHeadroom 住 `content/config/map-spec.json`＋configForms；chokepointClusters 在 `map/{graph,validate}.ts` |
| 423 | summon schema/sim 都在（零引用，第一個真消費者）；champion schema **無 selectable 欄位** ⇒ 新欄位或走 curation 白名單 |
| 425 | ⚠️ imported.wave.\* 9 份實住 `content/projectiles/`（⛔ 不是 vfx/）；一個 primitive＋9 列 |
| 441 | spatialPolicy 實址 `apps/client/src/audio/`；生成管線 `tools/tts-gen`＋`tools/voice-gen`（地端） |
| 443 | ⚠️ blizzardOverlayGate.test.ts 實住 `apps/client/src/render/views/`（票文寫 packages/shared 是錯的）；帳本先例 `content/assets/audio/wc3/PROVENANCE.json`；blizzard-local 現況只有 README |
| 444 | 三住處實查齊：`content/config/range-tiers.json`＋`schema/config/rangeTiers.ts`＋configForms；舊值表在 docs/legacy |
| 448 | blink.ts／applyStatus.ts 都在 schema/effects/ ⇒ 缺的是 to:"markedUnit" 一格；o030/orkn 兩份卡都在 |
| 452 | ⚠️ 失敗形態⑧現在進行式：`tools/arena-bg/generate.py` 已在、`content/assets/arena-bg/` 已有 3 張 webp、client **零引用** —— 接線是主體 |
| 453 | style-spec 由 prompt.py 推導（sha256）；共同 Fate token 建議落 content/config 一處 |
| 457 | ⚠️ pipeline 本體在 `tools/icon-gen/local/pipeline.py`（⛔ 不在 src/）；schema 側 `iconStyleDoc.ts` |
| 473 | Approvals 頁實址 `apps/admin/src/ui/ApprovalsPage.tsx`；稽核要抽成函式供 runtime＋測試共用 |
| 502 | ⚠️ MultiGamepadSystem 住 `GamepadInput.ts`（無獨立檔）；虛擬鍵盤掛 AuthScreen（blocker） |
| 509 | MerchantShop:1392／LobbyScreen:116；FOCUSABLE_SELECTOR 唯讀契約 PadFocusNav.tsx:41-49 |
| 511 | 四處補 data-pad-back；BACK_ALLOW/VETO_RE 在 `input/padFocusNav.ts:118-127` |
| 529 | ⭐ sfx 白名單那半**已完成**：WC3_ABILITY_SFX 已搬進 `content/audio-manifests/ability-sfx-cues.json`（combatSfx.ts:175 檔頭載明）—— v3 反映現況 |
| 538 | 預載器掛 `ui/platform/store.ts:513` 既有 landing fan-out；消費端四面板列名 |
| 547 | JASS 源實址 `tools/w3x-import/out/GoDieEX22s/jass-spells/*.j`；衝擊波既有家族 fx.fam.shockwave-ring.\* |
| 558 | ⚠️ 實檔是小寫 `render/modelFxRig.ts`（票文大寫）；③的三份上一層 schema（castApproach/displacement/mitigation）都實查在 |
| 565 | 15 份 ability JSON 逐一 ls 驗在；VfxSystem 對 anchorBone 零引用（AmbientVfx:162/222 有讀法可參考）；bloodbreathstream.json 確缺 |

## 授權遵守
只用 `gh issue view`／`gh issue edit`，只動名單 24 張；零 comment/close/create/push；repo 檔只加本報告。
