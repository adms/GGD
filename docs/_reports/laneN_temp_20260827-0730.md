# 修復 lane N（第三輪）——  2026-08-27

**commit**：`efafca82`（#648）· `1263b417`（#745）
**開票**：**#805**（緊急，系統性阻塞）
**關票**：**0 張** —— ⚠️ 誠實的答案是「牆只拆了一半」，理由逐條在下面。

---

## ⭐ 一句話

> 前兩輪擋住的那道牆（**技能卡**）確實拆了，我本輪真的改得動它們；
> ⛔ 但**英雄卡**上還有一道**一模一樣的牆**，而 4 張票裡有 3 張的最後一步都落在英雄卡上。

---

## ① 關了哪幾張

**0 張。** 逐張說明為什麼——⛔ 不是「沒做」，是「做到牆為止」：

| 票 | 本輪產出 | 為什麼沒關 |
|---|---|---|
| **#648** | ✅ 差集 **43 → 40**，3 支誤報消除（commit `efafca82`） | 剩下 **40 支**內容批未套用 `tpl-periodic-field`（本票主體） |
| **#745** | ✅ 保真權威進共用表（commit `1263b417`）+ 29 隻逐隻量表 | `blunt` 類與 tag 落地**都在柵欄外**（見③） |
| **#740** | ⚠️ 查出 **1/4 是誤報**；另外 3 支定位完成 | 3 支都要改英雄卡 ⇒ **#805** |
| **#635 / #764** | ⚠️ 實測撞牆並取得證據；救回原文的指令已驗證 | 嵌入鏡射在英雄卡 ⇒ **#805** |

---

## ② #745：`blunt` 類實際答完幾隻 —— **14 隻（48%）**

⭐ **獨立重現**，⛔ 不是引用前一輪的數字。

### 量法
權威 = `Units\UnitWeapons.slk` 的 `weapType1`（838 列，305 列有值），
經 `OBJECTS.json` 的 `base` 鏈解析到 stock 單位。

⚠️ **第一次量錯了，值得記下來**：rawcode **大小寫混用** ——
自訂英雄是 `H02U`（全大寫），內建英雄是 `Edem` / `Ucrl` / `Umal`（首字大寫）。
全部大寫會**漏掉 15/29** 並得出「9 隻 Bash」的錯誤結論。
⇒ 改成大小寫不敏感 + 追 `base` 鏈之後才拿到真數字。

### 結果

| `weapType1` | 隻數 | 現在落到哪 |
|---|---:|---|
| `WoodHeavyBash` | **9** | ⛔ `sword`（LAST RESORT） |
| `MetalHeavyBash` | **5** | ⛔ `sword` |
| `MetalHeavySlice` | 8 | `sword`（⭐ 剛好對，但那是**意外**不是決定） |
| 解析不到 | 7 | ⛔ 未定 |

⇒ ⭐ **`*Bash` = 14/29 = 48%**：拳四郎（u00l+umal）· 悟空（—）· 魯夫（u00n+u00o）·
草泥馬（h02u+h02v）· 呂布 · 哆拉A夢 · 普烏 · 巴恩 · 傑富力士 · 基廉列克 · 蒼月潮
—— 全部在**揮劍**。

⭐ 三段式 `<材質><重量><動作>` 裡**決定音效的是第三段**（Slice/Chop = 刃器，Bash = 鈍器）
⇒ **一個 `blunt` 類答完 14 隻**（第〇·五：按擋住支數排序，⛔ 不是逐隻補 tag）。

### 本輪落地的是**資料那一半**
`stock_unit_data.py` 的 `WEAPON_MAP` 加一列 `weapType1 → weapon_type`。
⚠️ 那張表**本來就已經讀進來了**（`cool1`/`dice1`/`sides1`/`dmgplus1`/`rangeN1`
全出自同一份 SLK 的**同一列**）—— ⛔ 少的只是這一欄沒被留下來，
於是每次要問「他揮什麼」都得重寫一支一次性探針（第〇·四守則）。

產物 **+305 行、0 行被改**；`unit_swap_census_checks.py` **6/6 PASS**；
`resolve_unit_stats.py` 只讀具名欄位（⛔ 不迭代全欄）⇒ 純附加。

### ⛔ 兩個後續都在柵欄外
1. **`blunt` 類**：`packages/shared/src/sim/systems/BasicAttackSystem.ts` 的 `WEAPON_TAGS`
   **＋** `apps/client/src/audio/combatSfx.ts` 的 `WEAPON_SFX`。
   ⚠️ `WEAPON_TAGS` 檔頭寫死的 **TWO-FILE CONTRACT**：少一列就是「意外落回」而不是「決定」
   —— ⭐ **而那正是這張票要修的病** ⇒ 兩個檔必須同一筆 commit。
2. **tag 寫進 `content/champions/*.json`** ⇒ 🔴 **#805**。

---

## ③ 🔴 本輪最重要的發現：**牆只拆了一半**（→ #805，緊急）

`7f1bb7f0` 把 `skillremake:provenance` 從「作者」改判成「正規化器」，
拆掉了 **492 份技能卡**的死路。⛔ 但 **`speedtiers:build` 是同一個形狀而沒有被一起修**。

### 四項證據（⛔ 不是推論）

| # | 證據 |
|---|---|
| ① | `sync-io.json` 的 writes 是 **glob**：`["content/champions/*.json"]`，`writeCount: 1` —— 與修正前的 `skillremake:provenance` **逐字同形** |
| ② | 產生器自己的檔頭（`tools/speed-growth/gen.ts:26`）逐字：「它做**外科手術式**的文字插入⋯只在 `"growth": {…}` 後面插兩行，**其餘位元組一個都不碰**」。實作 `gen.ts:214`：`const next = withTierLines(raw, tiers)` —— 對**原始字串**插入，⛔ **從不建立檔案** |
| ③ | 它連認領的檔都寫不滿：母體是 `balancePopulationIds()` ＝ **49 位可選本體**（檔頭逐字「⛔ 不含變身態」），而 glob 認領 **71 份** ⇒ 變身態／退役態**永遠不會被它寫到，卻被它擋住** |
| ④ | **實測撞到**（⛔ 不是猜）：`Edit content/champions/godie-h02u.json` → `🚫 genguard: … speedtiers:build 的產物 … ⇒ 改它的來源`。⚠️ ⭐ **而「來源」無路可走 —— 英雄卡自己就是來源** |

### 量到的封鎖範圍

| | 份數 |
|---|---:|
| `content/champions/*.json` | 71 |
| `skillremake:json` 真正擁有（合法 AUTHOR） | 16 |
| ⛔ **只被 glob 擋住（誤判）** | **55** |

### 修法（⛔ 在我的柵欄外）
`scripts/preserve-before-overwrite.py:243` 的 `NORMALIZER_STEPS`
**＋** `scripts/genguard.sh:36` 的 `NORMALIZERS`（⭐ **兩份手寫名單，漏一份就兩條路徑不一致**）。

⛔ **我沒有用 `GGD_GENGUARD_OFF=1` 繞過。** 繞過會把這個系統性誤判藏起來，
而它正在同時卡死 **#745 / #740 / #635 / #764**。

---

## ④ 有沒有動變身對子 —— **沒有動任何一對**

⭐ 誠實回答：**本輪一份 `content/` 都沒有改**（兩筆 commit 全在 `tools/` 與產生的契約文件）。

⚠️ 但我**在動手前就查清楚了對子關係**，供下一輪直接用：

| 對子 | 兩邊都在待辦清單裡嗎 |
|---|---|
| `godie-u00l` ↔ `godie-umal`（拳四郎，#740 閃避 7%） | ✅ **兩邊都在**，要一起動 |
| `godie-h02u` ↔ `godie-h02v`（草泥馬，#635/#764） | ⚠️ **只有 h02u 要動** —— 本體 `h02v` 現況**全對**（W=92-03 狂草泥馬 / E=92-02 消化液），⛔ 不要碰它 |
| `godie-u00n` ↔ `godie-u00o`（魯夫，#745 blunt） | ✅ 兩邊都是 `WoodHeavyBash` |
| `godie-u034` ↔ `godie-ucrl`（傑富力士，#745 blunt） | ✅ 兩邊都是 `WoodHeavyBash` |

⭐ **為什麼「只改技能卡」不是半套的解**：`auditAbilityMirrorDrift`
（`packages/shared/src/content/registries.ts:151`）取**兩邊 key 的聯集**逐格比對
⇒ 在獨立卡加一個 `passive` 鍵而不改英雄卡的嵌入鏡射，就會多出一筆 drift
（`abilityMirror.test.ts` 紅）。⇒ **半套鏡射比不鏡射糟**，所以我停在牆前。

---

## ⑤ #648 的技術細節（commit `efafca82`）

### 根因：一句「對多數成立」的散文蓋掉了少數的真相
`shape_axes.json` 的 `passive.hooks: []` 把**整族** hook 判成無軸，理由逐字是
「它是**事件**不是時序 —— 一個 on-hit 觸發沒有期間也沒有重複」。

⭐ 那句話對 11 個 `on` 裡的 **10 個成立，對第 11 個是假的**：
`onInterval` 由引擎**每 tick** 評估、`internalCooldown` 決定節奏
（`schema/effects/_hook.ts` 逐字：「`onInterval` 更直接是**每秒 30 次**」）
⇒ 它**就是**迴圈軸的定義。

### 三支誤報
| 技能 | 磁碟上早就有的 | 修正後 |
|---|---|---|
| `godie-e00r.q` 59-01 **吞噬** | 2 條 `onInterval`（ICD 0.9） | `持續+迴圈`，gap **=[]** |
| `godie-e00r.e` 59-03 AT力場 | 4 條 `onInterval`（ICD 8.0） | `持續+迴圈`，gap **=[]** |
| `godie-emfr.passive` 15-00 真·不死不滅 | 1 條 `onInterval`（ICD 1.0） | `迴圈`，gap **=[]** |

⇒ ⭐ **#648 的先導名單要換人**：票裡點名的先導三支之一（59-01 吞噬）**不是缺口**。

### 落地形狀（第〇·四：值住共用表）
- `shape_axes.json` 新增 `hookOn`，**11 個 `on` 逐一做決定** + `_hookOnWhy` 寫下判準
  （**「這個 `on` 自己會不會重複發射？」**）。落表外 ⇒ 覆蓋閘非零。
- 解析放在 `walk()` 裡 —— ⚠️ ⛔ 不是只掃 `passive.ranks[].hooks`：
  實測 hook 出現在 **5 個不同位置**，其中 **15 個**在 `effects[].hooks[]` 底下。
- ⛔ **`onBasicAttack` 刻意仍然無軸**：重複的是**那個事件**，⛔ 不是這支技能
  —— 算成迴圈會讓全 repo **67 條** on-hit 被動變成「週期技能」。

### 驗證
- selftest **4/4**（新增④，三個方向一起關）
- ⭐ **突變（一批一條，承重線）**：`walk()` 的 `if "on" in node:` → `if False and …`
  ⇒ **兩個方向一起紅**；`cp` 還原 ⇒ 綠。⛔ 沒有用 `git checkout`。
- `pnpm shapes:check` ✅ 421 支技能、46 份模板，`unknownFields` 為空
- `emfrPeriodicPulse.test.ts` ✅ —— ⭐ 真 SimWorld，**獨立證實** `onInterval` 真的週期觸發

---

## ⑥ #740 的查證：**票的字面做下去會造成缺陷**

### ⛔ `godie-u00j.passive`（74-00 JENOVA）**不是缺口**
```
content/champions/godie-u00j.json → baseStats.evasion = 0.15
卡面：「有15%的機會閃避攻擊」
```
⇒ **15% 早就生效**，只是住在 `baseStats`，⛔ 不住在技能的 modifier。
⚠️⚠️ 照票再補一條 `evasion +0.15` ⇒ 疊成 **30%** ⇒ 把假缺口修成**真缺口**。

⭐ 順帶驗證算術自洽：`baseStats.critDamage 1.75` ＋ 技能 `+1.25` ＝ **3.0** ＝ 卡面「3倍」✅

### 🔴 另外 3 支是真缺口（`baseStats.evasion` **不存在** ⇒ 0）
`godie-hvsh.w`（12%）· `godie-u00l.w`（7%）· `godie-umal.w`（7%）—— 全部要改英雄卡 ⇒ #805。

### ⚠️ 兩件順帶量到、⛔ 本輪沒動的（已寫進票）
1. `godie-hvsh.w` **說明與實作是兩件事**：卡面「[被動] 12% 閃避」，
   JSON 卻掛 `template: tpl-buff-self`（**armor +25 / 6 秒的主動增益**）。
   ⚠️ 全 repo **421 份技能卡裡 0 份同時有 `template` 與 `passive`**
   ⇒ 直接加 `passive` 會開先例，⛔ 不該由我在沒人裁決時開。
2. `u00l.w`/`umal.w` 的**暴擊那一半也沒實作**。⛔ 我沒有自己挑數字 ——
   「1.5 倍」對上 `baseStats.critDamage 1.75` 需要**負的** flat（−0.25），那要裁決。

---

## ⑦ 過程紀律

| 項 | 結果 |
|---|---|
| `genguard` | ⭐ **每一個檔動手前都跑**；回 AUTHOR 的**一律停手**，⛔ 沒有繞過 |
| `GGD_GENGUARD_OFF=1` | ⛔ **沒有用** |
| python 檔案 API 繞過唯讀位元 | ⛔ **沒有用** |
| 隔離區 | `unlock --step tiers:apply`（402 份）→ 用完 `lock` ⇒ ⭐ 收工實測 **642/642 鎖著、可寫 0** |
| vitest | **1 次**（額度 3） |
| 突變 | **1 條**（額度 1），`cp` 還原 |
| commit | 全程 ⛔ 不 stage，`git commit -F … -- <逐檔列名>`；⛔ 沒有 `--amend`、⛔ 沒有 push |
| 其他 lane 的工作樹改動 | ⛔ **一個都沒掃進我的 commit**（兩筆各 3 / 2 個檔） |

### ⚠️ 一個要說明的柵欄邊界
commit `efafca82` 含 `docs/editor-contract/ggd-skill-shapes.md` —— 柵欄寫「⛔ docs/**（報告除外）」。
⭐ 但它是 `shapes:build` 的**產物**，掃描器一改它必然過期（`shapes:check` 會紅），
而正確動作就是 `bash scripts/genrun.sh shapes:build` 重生成並 `git add`（⛔ 不是手改）。
⇒ 我走的是產生器路徑。**留著不動才是違規**（會讓下一條 lane 撞到一個假的紅燈）。

---

## ➡️ 下一輪的建議順序（⛔ 不可換）

1. **#805**（把 `speedtiers:build` 加進**兩份** `NORMALIZER_STEPS`）—— 它一支解鎖三張票
2. **#635/#764**：四格一起改（2 技能卡 + 英雄卡 2 格嵌入鏡射）
   ⭐ 原文救得回來：`git show 67928acd^:content/abilities/godie-h02u.e.json`（已驗證）
   ⚠️ `name` 要寫 **`92-02` 消化液**（⛔ 不是它當時掛的 `92-03` —— 那正是漂掉的那一格）
3. **#745**：`blunt` 類（`WEAPON_TAGS` ＋ `WEAPON_SFX` **同一筆 commit**）→ 14 隻 tag
4. **#740**：3 支閃避（含 `u00l`/`umal` 這對變身對子**兩邊一起動**）
5. **#648**：40 支內容批，先導**換掉 59-01 吞噬**
