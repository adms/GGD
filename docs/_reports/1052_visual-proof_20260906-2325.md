# 1052 — V 鍵通訊輪盤的終端證據（GH#1052 · DOM 級 @visual-proof）

> 📅 **證據的時間身分（GH#795）**：`HEAD=1de2bd31` 工作樹
>
> ⭐ 這一行是拍攝當下寫的（`git rev-parse --short=8 HEAD`），⛔ 不是事後補的。
> 「工作樹」：`resolveUiCues()` 補回 `commsWheel` 的修復（`packages/shared/src/content/schema/config/uiCues.ts`）
> 這一刻**還在工作樹、未進 HEAD**（主 session 統一 commit）—— 所以這份證據驗的是**修復之後**的狀態，
> ⛔ 但 `evidenceOrder()` 靠祖孫關係判不出來，寫在這裡才叫誠實。

## 台子

| | |
|---|---|
| 測試 | `apps/client/src/game/commsWheelVisualProof.test.ts`（`@visual-proof` · jsdom） |
| 跑法 | `npx vitest run apps/client/src/game/commsWheelVisualProof.test.ts` |
| 為什麼是 DOM 不是像素 | 輪盤是 **React HUD**（`ui/hud/CommsWheelOverlay.tsx`），⛔ 不是 WebGL —— 玩家看到的就是這棵 DOM。repo 既有的同型前例：`apps/client/src/ui/floatingTextRenders.test.ts`（GH#701，同樣 jsdom＋出貨元件＋可見性斷言） |

## 鏈路（逐段，證明沒有一段是台子造的）

| 段 | 誰 | 出貨的？ |
|---|---|---|
| ① 出貨 config | `content/config/ui-cues.json` 讀進 `zConfigUiCuesDoc.parse` → `Configs.register` | ⭐ 出貨檔本人 |
| ② 解析 | `uiCues()`（`ui/uiCuesConfig.ts:33`）→ `resolveUiCues()`（#1052 修的那一格） | ⭐ |
| ③ 決策 | `commsWheelRunner(() => null)` → `CommsWheelState`（`game/commsWheel.ts:71/:89` 讀 `cfg.enabled`） | ⭐ |
| ④ 輸入 | **出貨的 `InputCapture`**（`input/InputCapture.ts`）`attach()` 後收**真的** `pointermove`／`keydown`／`keyup` DOM 事件；deps ＝ `{…與輪盤無關的 stub, ...runner.inputDeps}` | ⭐ 輪盤的四個 hook 全是出貨的；stub 的只有 `screenToGround` 那一族（輪盤不讀） |
| ⑤ 狀態 | `recordCommsWheel` → `hudStore`（`net/RoomStore.ts:1275`） | ⭐ |
| ⑥ 畫面 | **出貨的 `<CommsWheelOverlay/>`**（HudRoot.tsx:222 掛的那一個，⛔ 不是 `…View` 純元件）→ DOM | ⭐ |

⛔ **台子沒接的一段**（誠實）：`GameApp.ts:1102` 的 `...this.comms.inputDeps` 攤進 `InputCapture` 那一行，
與 `HudRoot.tsx:222` 把 overlay 掛進 HUD 樹那一行 —— 測試各自用同一個物件／元件直接接，⛔ 沒有跑 `GameApp`／`HudRoot` 本人。
兩行都是字面上一眼看得到的接線；要驗它們得起一整個 `GameApp`（Babylon scene），這台台子做不到。

## ⭐ 量尺先自證：兩個方向都跑過

| 方向 | 做什麼 | 量到 |
|---|---|---|
| **A 功能開**（出貨 `enabled:true`） | 指標移到 (300,300)，`keydown KeyV` | `[data-hud-surface="comms-wheel"]` **出現**（見下表） |
| **B 功能關**（`Configs.register({...shipped, commsWheel:{...,enabled:false}})`） | 同一下 V | `querySelector` ⇒ **null**（畫面上什麼都沒有） |
| **B → A**（再 `register(shipped)`） | 同一下 V | **又出現** ⇒ 開關是活的，⛔ 不是卡在某一邊 |
| **突變**（`commsWheelRunner.ts:38` `uiCues().commsWheel ?? FALLBACK` → 只回 `FALLBACK`） | 同一下 V | ① 紅：「⛔ 按下 V 之後畫面上沒有輪盤 —— 鏈路某一段是斷的: expected null not to be null」；② 也紅（翻回來沒回來）。`edit-or-die.py` 改壞→跑→改回，`git diff` 空 |

## 看到什麼（A 功能開）

| 量的東西 | 斷言 | 為什麼它是「看得見」 |
|---|---|---|
| 輪盤根節點 | 在 `document.body` 上；祖先鏈沒有 `display:none`／`visibility:hidden`／alpha 0 | 節點在但被藏起來 ＝ 零像素（天譴那次的形狀） |
| 釘在哪 | `position:fixed; left:300px; top:300px`（＝指標那一點）；`zIndex>0` | 畫在畫面外／畫在別的東西底下 ＝ 看不到 |
| 五格文字 | DOM 順序逐字 ＝ 出貨 `entries[].zh`：**撤退！ · 小心！ · 幹得好 · ？？？ · 哼歌** | 格數與字都從 config 來（⛔ 元件裡零文案） |
| 每格 | `fontSize` 15px>0 · `color` 是 `rgb(…)`（TEXT_MAIN）· `background` 是 `rgba(…)`（PANEL_BG）· 距圓心 <200px | 字級 0／透明字／透明底 ＝ 讀不出格子 |
| 五格座標 | **5 個不同的 (left,top)**（照 `CommsWheelOverlay.tsx` 的式子：R=108、每 72°：(0,−108)·(103,−33)·(63,87)·(−63,87)·(−103,−33) —— ⚠️ 座標是式子算的，測試斷言的是「5 個相異且 <200px」） | 全疊在圓心 ＝ 退化成一點（UV 全 (0,0) 的同型） |
| 指向 | `pointermove` 到 (300,200)（正上方）⇒ `hoveredIndex=0`；第 0 格 `outline` 含 `solid`、`fontWeight=700` | 玩家要看得出自己指著誰 |
| 放開 | `keyup KeyV` ⇒ 根節點 **消失** | ⛔ 不會留一個永遠掛著的輪盤 |

## 關掉時看到什麼（B）

`commsWheel.enabled=false` ⇒ 同一段 `pointermove`＋`keydown KeyV` 之後 `querySelector('[data-hud-surface="comms-wheel"]')` 是 **null**。
⇒ 後台那一格**真的關得掉**（#1043 那個「三個住處齊全但沒人讀」的洞，現在從畫面這一端也量得到）。

## 跑過的

| | 結果 |
|---|---|
| run ①（新守衛） | 2/2 綠，436ms（`scratchpad/lane1052-run1.log`） |
| run ②（突變） | 2/2 紅，訊息如上（`scratchpad/lane1052-run2-mutation.log`）；改回後 `git diff --stat commsWheelRunner.ts` 空 |
| `bash scripts/visual-proof.sh` | 綠（「這次改動沒有碰到畫面層」—— 它只看 `vfx/render`，見下面順手發現） |
| `pnpm typecheck` | 見 lane 報告 `1052_temp_20260906-2325.md` |

## 誠實的界線

- jsdom **不 raster**：字體渲染、真瀏覽器的疊層／遮擋、品質階梯、與 Babylon canvas 的 z 序，這裡驗不到。要那一層得開真瀏覽器截圖 —— 這台台子刻意不做（owner 2026-08-09：不用手動玩遊戲測試；而 DOM 級證據就是這個功能的終端）。
- 語音那一半（`playContextualVoice`）刻意不驗：`champId` 給 `null` ⇒ 放開時靜靜不播。那是 GH#731 的事，⛔ 不是 #1052 的（#1052 只問「後台那格到不到得了畫面」）。
- 測試 141 行（含 20 行檔頭）—— 超過體驗層 80 行上限；理由：它是**終端證據**（visual-proof 那一族），兩個方向＋指向＋收起各一段，⛔ 沒有第二條重複斷言。

## 順手發現（⛔ 沒動）

`scripts/visual-proof.sh` v2 的「空殼標記」掃描 `find apps/client/src/vfx apps/client/src/render -name '*.test.ts'`
⇒ 一支住在 `ui/` 或 `game/` 的 `@visual-proof` 測試（本檔、`ui/floatingTextRenders.test.ts`）**不在它的 liar-scan 裡**：
標記貼上去而零可見性詞彙，它不會紅。形態⑫（只從一頭走）。→ 已開票 **GH#1083**（⛔ 沒動）。
