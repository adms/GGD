# R3 Tier-0 機器面：出貨態掃描＋標記誠實＋量尺校準（GH#664）

2026-08-24 · lane R3 · 柵欄：`vfxDocsBirthVisibility.test.ts`（新）· `visual-proof.sh` ·
`chainLightningAudition.ts`（只加 calibrate）

## ① 出貨態掃描 `packages/shared/src/content/vfxDocsBirthVisibility.test.ts`

載入全部 645 份出貨 `content/vfx`（585 vfx@1 · 56 ribbon@1 · 4 attachment@1），
逐份判「有沒有可能畫出一個像素」：peak alpha > 0.05 · 覆寫後 peak size > 0 ·
additive/modulate 疊全黑（max(R,G,B)<0.02）⇒ 紅 · ribbon 寬度>0 ＋ alpha>0。
有效梯度照 schema 字面（colorStops/sizeStops **overrides** legacy）。
⭐ 量尺自驗（取代突變）：5 份 sentinel（alpha0 / additive全黑 / size0 / 可見對照 /
零寬 ribbon）內建在測試裡，檢查器抓錯任何一份就紅。

### ⛔ 掃到 8 份真缺陷（⛔ 本 lane 依約不改 content/ —— 請主 session 裁決）

| 文件 | 病型 |
|---|---|
| fx.w3x.particle.holyawakening.p00 / .p01 | 每個 colorStop 的 alpha 烘成 0（modulate） |
| godie-holyawakening-p0 / -p1 | 同上 |
| godie-deathwave-r0 / r1 / r2 | ribbon color alpha=0（additive）—— 死亡波的帶子從未亮過 |
| godie-sd2-r0 | widthAbove=0 + widthBelow=0 —— 零寬帶 |

全部長得像 w3x 匯入器把 alpha/寬度烘錯 ⇒ 修法多半在 `tools/w3x-import` 的來源側，
⛔ 不是逐份手改 8 個 JSON（第零守則⑨）。已列入測試的 `KNOWN_INVISIBLE`
（帶過期偵測：修好一份就紅著要求刪那一列 —— 名單只能縮小）。

## ② visual-proof.sh v2 標記誠實

帶 `@visual-proof` 的測試檔必須含可見性斷言詞彙
（readPixels|opacityTexture|getVerticesData|emissive|alpha|isEnabled|bright），
否則 exit 1 指名該檔＋「標記不是斷言（第三守則）」。掃**全部**畫面層測試檔
（含未追蹤），且跑在「沒碰畫面層」early-exit **之前**。
⭐ 突變驗過：造只有標記的暫存測試檔 → exit 1 指名它 → 刪掉 → exit 0。

## ③ audition `calibrate()`

`chainLightningAudition.ts`：全亮 quad（emissive 白＋disableLighting）→ render ×2 →
`engine.readPixels` → 亮像素>0 → 移除 quad → 回傳數字；頁面載入時自動跑，
失敗拋錯（頁面錯誤框接住）。
⭐ **第一次跑就抓到真坑**：`scene.render()` 對沒 ready 的材質**靜默跳過**
（KHR_parallel_shader_compile）⇒ 1280×720 量到 0 亮像素。修法
`await forceCompilationAsync(quad)`；連同「canvas 300×150」「readPixels 上一幀」
共**三個坑**寫進檔頭。瀏覽器實測：頁面正常開（校準通過）、場景無殘留、
灰 canvas 下 128×128 量到 14400/16384 亮像素。

## 驗證帳

vitest 1 次（3/3 綠）· tsc client×2（修 calibrate 紅前後）＋ shared×1，全 exit 0 ·
突變 1 條（閘②）· sentinel 自驗 5 份（閘①，常駐）。
