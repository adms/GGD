# 隱形幾何當基準（GH#767）—— 09-04 龜派氣功的 ReviveHuman 核心，改前／改後

> 📅 **證據的時間身分（回填，GH#795）**：`HEAD=e44bf446` —— ⚠️ **工作樹**（拍攝時 #767 的修復尚未 commit）。
>
> ⭐ 這一行是**回填**的，⛔ 不是憑空補：
> · **出處①（作者自己寫的）**：本目錄 `frames_reverify.md`「**修後**（工作樹＝`e44bf446`）」。
> · **出處②（git 佐證）**：`e44bf446`（08-26 13:53）是這一批擷圖（PNG mtime **14:10–14:37**）
>   當下的 HEAD —— 它與這批 PNG 進版控的 `470cb1fd`（14:49）之間**沒有第三個 commit**。
>
> ⚠️ ⛔ **「工作樹」三個字是關鍵，⛔ 不可以省**：#767 的 TeamGlow 轉檔修復當時只在工作樹上，
> 它到 `470cb1fd` 才進歷史。⇒ `evidenceOrder()` 只比 commit 的祖孫關係，
> **結構上分不出「拍攝時修復已在工作樹」與「拍攝時根本還沒修」** —— 這一批屬於前者。

台子：`public/beam-audition.html?ability=godie-ogrh.r`（出貨 `SimWorld` → 出貨
09-04 → 真 `modelFxSpawn` → 出貨 `VfxSystem`／`ModelFxRig` → 真 `revivehuman.glb`）。
量尺先自證：`calibrate()` 全亮 quad = **462,400** 亮像素（1280×720）；precast 基線 = **0**。
亮像素 = max(R,G,B) > 200；lit = > 96（**離線**從 PNG 重數 ——
台子的 `measure()` 在這個場景回 0 而畫面明明是亮的，⛔ 那一支不可信，見報告「量尺」一節）。

## ⛔ 這一批**不是**「看不見 → 看得見」——⭐ 是**兩個**具體的錯

| # | 量到的 | 改前 | 改後 |
|---|---|---|---|
| ① | `revivehuman.glb` 其實是 **Awaken 的幾何**（來源路徑 2026-08-26 才修好） | 4 primitive，⛔ 缺 90 頂點的 `Yellow_Glow3` 幾何組（＝核心的金橘/紅） | **5 primitive**，那一組回來了 |
| ② | `TeamGlow0`（132 頂點、佔 y 軸 **53%**）被轉檔器軟刪除 | 材質 alpha **0** ⇒ `fxLongAxis`／`scaleAxis` 的基準是**看不見的幾何** | alpha **1**（luma-key 加法發光） |

⇒ 可見包圍盒 **7.817 × 5.127 × 7.817（長軸 x）** → **10.751 × 16.757 × 10.751（長軸 y）**，
`longAxisVisibleFrac` **0.306 → 1.000**。兩個數字都由 `ModelFxRig` 現場讀出來對過
（同一個節點 `modelfx-15`：改前 4 片／`TeamGlow0` alpha 0，改後 5 片／alpha 1）。

## 逐張

| 圖 | 是什麼 | 亮像素 | lit | 說明 |
|---|---|---:|---:|---|
| 0904_before_f00_precast | 改前 · 施放前 | 0 | 0 | 基線（量尺自證：這裡必須是 0） |
| 0904_after_f00_precast | 改後 · 施放前 | 0 | 0 | 同上，逐位元相同 |
| 0904_before_f01_cast_done_t37 | 改前 · 詠唱完 t37 | 0 | 255 | 光束還沒生成 |
| 0904_after_f01_cast_done_t37 | 改後 · 詠唱完 t37 | 0 | 255 | 同上 |
| 0904_before_f02_beam_t42 | 改前 · 光束 t42 | 269 | 1,447 | 3 個 modelFxSpawn、7 個 beam 節點 |
| 0904_after_f02_beam_t42 | 改後 · 光束 t42 | 259 | 1,464 | 同上 |
| 0904_before_f03_beam_t60 | 改前 · t60 | 2,436 | 5,943 | 火柱（FlameStrike1）為主 |
| 0904_after_f03_beam_t60 | 改後 · t60 | 2,381 | 6,254 | 同上 |
| 0904_before_f04_beam_t90 | 改前 · t90 | 4,325 | 12,715 | 全景最亮的一張 |
| **0904_after_f04_beam_t90** | 改後 · t90 | 2,134 | **15,122** | lit **+19%**；亮>200 下降＝新加的是**中亮度有顏色**的加法層，⛔ 不是爆白 |
| 0904_before_f05_expired_t130 | 改前 · t130 | 1,416 | 4,351 | 模板 lifeSec=2 之後 |
| 0904_after_f05_expired_t130 | 改後 · t130 | 2,206 | 5,285 | 同上 |
| **0904_iso_before_core** | ⭐ 改前 · **只留 ReviveHuman 那一具**（t45） | 17,724 | 34,802 | 亮區平均 RGB **(187,187,188)** ＝ 一根**灰白管子** |
| **0904_iso_after_core** | ⭐ 改後 · 同上（t45） | 3,595 | **42,694** | 亮區平均 RGB **(147,98,98)** ＝ ReviveHuman **自己的紅橘色**回來了；lit **+23%** |

⚠️ 兩張 `iso_*` 是**同一個 tick（45）、同一個攝影機、同一具活著的實例**，只有 `.glb` 不同 ——
其餘 mesh 全部 `setEnabled(false)`（粒子系統不是 mesh，仍在畫，兩邊一樣）。
⚠️ 第一次拍 `iso_before` 時我用的是**已到期**的池化實例，量到 0 個像素 ——
那是**台子的 0 冒充模型的 0**，已作廢重拍。這一份是重拍的。

## 要人看的那一題（⛔ 機器判不了）

**改後那道紅橘色的光束，比改前那根灰白管子更像「約束與勝利之劍／龜派氣功」嗎？**
否決 ⇒ 翻 `tpl-beam-roll.params.modelKey.default`（見帳本登記）。
