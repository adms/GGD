# GH#1001 ＋ GH#1033 —— lane 報告（2026-09-06）

> 兩張票同一條 lane，因為它們都動 `arena-rules` 的三個住處。
> ⛔ 本 lane 沒有 commit／content:build／skills:sync；下面「主 session 要做的」那一節是收尾清單。

---

## 一句話

| 票 | 做了什麼 | 狀態 |
|---|---|---|
| **#1001** | 36 格手寫標籤搬進 Zod 的 `@zh`／`@note`，admin spec 改成 `schemaToForm()` 推導的 spread；棘輪 `BASELINE` 999 → **963** | ⭐ 36/36 逐位元相同；⚠️ 工作樹量到 **969**（+6 是別的 lane 的帳，見下） |
| **#1033** | 開關 `arena-rules.humanSeatsFromRound`（三個住處，出貨 **1**，3 ＝ 舊行為）；`MatchController.enterCombat` 在殭屍波沒武裝時仍把真人名單交進 sim | ⭐ 承重守衛綠、突變紅；headless 量到 round 1–2 三套機制的差異（表在下面） |

---

## #1001 —— 36 格搬回 Zod 的 `@zh`

### 機制（量過，⛔ 不是推論）

`handWrittenResidue(spec)` 走 `spec.fields[]`，逐格問 Zod 推導的 `zh`／`note` 在不在 ⇒
只補 `@zh` 而**留著**手寫標籤，棘輪也會降 —— 但那是同一句人話的**兩個住處**（第〇·四守則）。
⇒ 正確形狀：人話只住 Zod，spec 的 `fields[]` 用 `schemaToForm(zod).fields.filter(...)` **spread** 進來
（`configForms.test.ts` 的雙向斷言仍然每一個葉子都有一筆標籤）。

### 落地

| 住處 | 改了什麼 |
|---|---|
| `packages/shared/src/content/schema/config/arenaRules.round11.ts` | 21 個葉子各加 `.describe("@zh …\n@note …")`，逐字抄自手寫標籤（含 `{{出貨值}}`） |
| `packages/shared/src/content/schema/config/apCoefficient.ts` | `zByTier(min, max, describe?)` 多一個模板參數；`frequency.{basicAttack,abilityCast,specialCondition}` 各給一個 `(tier) => …` ⇒ **3 個模板 × 一張五級距表**（第零守則⑨） |
| `apps/admin/src/configForms/specs/arenaRules.ts` | 刪 21 格手寫；`...fromZod((p) => p.startsWith("round11."))`；⚠️ `fromZod` 是 4 行本地 helper（`_shared.ts`／`schemaToForm.ts` 在柵欄外，該搬過去） |
| `apps/admin/src/configForms/specs/tiers.ts` | 刪 15 格 `SKILL_TIER_NAMES.map(...)`；`...schemaToForm(zConfigApCoefficientDoc).fields.filter(frequency.)`；⛔ 今天新加的 `multiHit.*`／`proseFromFormula` 三格一個字沒動 |
| `packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts` | `BASELINE = 963`；紅的訊息多一行**逐份欠帳**（`docId=n`），下一個人對得出是誰動了 |

### 驗證

- **AC3 逐格不變**：改動前用 tsx 把 36 格 dump 成 JSON（`before1001.json`），改動後比對 ⇒ **36/36 逐位元相同**；
  `arena-rules` 頁順序不變（只多插一格 `humanSeatsFromRound` 在 `botOnlyRingAccelSec` 後）、`ap-coefficient` 頁順序不變。
- admin：`configForms.test.ts`（20）· `configFormsShippedProse`（1）· `schemaToForm.test.ts`（3）· `configFormsSave`（12）· `configDocCoverage`（11）· `configPageDerivation`（2）全綠（一次 invocation）。
- **突變**：把 `round11.enabled` 的 `@zh` 改成 `@zzz` ⇒ 棘輪 **+1** 紅，逐份欠帳 `arena-rules=54 → 55`（指名文件）。改回，`grep -c @zzz` = 0。

### ⚠️ 棘輪量到 969，⛔ 不是 963 —— 那 6 格是別的 lane 的

`git diff c40493591..HEAD -- specs` ⇒ HEAD 在 963/999 那一筆之後**又加了 7 格手寫標籤、0 格刪除**，而基準線沒動：

| 格 | 欠 | 來源 |
|---|---|---|
| `ap-coefficient.multiHit.enabled` · `.decayPerHit` · `proseFromFormula` | zh+note | 今天的 AP 第七維／卡面 `{{ap}}`（#1035 / 051123371） |
| `ap-damage-scaling.apCurveK` · `apCurveP` · `apCurveMaxMult` | zh | #1029 三段式法強乘數 |
| `ugc.digestRecompute` | （已有 `@zh`，不計） | #1022 |

⇒ **HEAD 本身量到 1005 vs BASELINE 999 —— 這條在 HEAD 上就已經紅**（⛔ 不是本 lane 造成）。
本 lane 還清 36 ⇒ 工作樹 **969**。刻意把 `BASELINE` 留在 **963**（#1001 的帳逐格對得上），並在測試檔頭記下那 6 格；
⭐ 主 session 決定：把那 6 格搬去 `@zh`（正解；`ap-coefficient` 那 3 格今天在本 lane 的柵欄外）或帶理由調到 969。

### 順手量到（⛔ 沒修，在柵欄外）

`schemaToForm.ts` 的 `DIRECTIVE` 正則帶 `m` 旗標，`(?=…|$)` 的 `$` 在每一行尾都成立 ⇒
**`@note` 只吃到第一個換行**（實測 `"@note 第一行\n第二行"` ⇒ `第一行`），而檔頭寫著「可跨行，直到下一個 `@`」。
本 lane 寫的每一句 `@note` 都是單行，所以沒踩到；但那句檔頭是第三守則的形狀。

---

## #1033 —— `humanSeats` 從 round 1 就在 sim 裡

### 根因（讀碼確認）

`enterCombat` 只在 `if (mobWaves && (practice || (rogueliteMobs !== false && round >= mobWaves.fromRound)))`
這一段把 `humanSeats` 交給 `mobRulesFromConfig(...)`；else 走 `endCombatMobs` ⇒ `world.mobRules = null`。
sim 的六個消費端全讀 `world.mobRules?.humanSeats`（`OrderSystem` ×3 · `MobSystem` ×2 · `combatFeel` 的後搖取消）
⇒ round 1–2 真人＝bot。

### 量測（真的 `MatchController` ＋ 出貨內容，seed 7919，seat 0 = Saber 真人，回合上限 90 s）

`autoWaves` 欄：`False` ＝ 只送名單那條路；`(未設)` ＝ 殭屍波真的武裝。「打帶跑窗口」＝點一次地板後 `moveOrderNoAggroUntil > tick`。

| 開關 | feed | R | humanSeats | mobRules | autoWaves | 殭屍上限 | 打帶跑窗口 | 握自動目標 tick | 首次自動目標 | 普攻命中 | 被普攻命中 | 回合 tick |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | idle | 1 | ✓ | armed | False | 0 | - | 0 | — | 0 | 0 | 791 |
| 1 | idle | 2 | ✓ | armed | False | 0 | - | 0 | — | 0 | 6 | 384 |
| 1 | idle | 3 | ✓ | armed | (未設) | 30 | - | 871 | 29 | 14 | 0 | 994 |
| 1 | click | 1 | ✓ | armed | False | 0 | ✓ | 0 | — | 0 | 0 | 772 |
| 1 | click | 2 | ✓ | armed | False | 0 | ✓ | 0 | — | 0 | 6 | 384 |
| 1 | click | 3 | ✓ | armed | (未設) | 30 | ✓ | 635 | 31 | 4 | 9 | 769 |
| 3 | idle | 1 | ✗ | null | — | 0 | - | 208 | 485 | 0 | 0 | 791 |
| 3 | idle | 2 | ✗ | null | — | 0 | - | 287 | 199 | 5 | 9 | 486 |
| 3 | idle | 3 | ✓ | armed | (未設) | 30 | - | 510 | 29 | 7 | 5 | 632 |
| 3 | click | 1 | ✗ | null | — | 0 | ✗ | 476 | 228 | 1 | 0 | 733 |
| 3 | click | 2 | ✗ | null | — | 0 | ✗ | 133 | 175 | 3 | 8 | 308 |
| 3 | click | 3 | ✓ | armed | (未設) | 30 | ✓ | 758 | 31 | 12 | 11 | 892 |

讀法（⭐ 每一列的分母是「同一個座位、同一份輸入」）：

1. **打帶跑窗口（GH#637）**：舊行為 round 1–2 點地板 **✗**（窗口一個位元都沒寫）、round 3 才 ✓；開關 1 ⇒ 三回合全 ✓。
2. **LoL 索敵模型 ＋ idle 自動索敵（GH#652 / #863）**：舊行為 round 1–2 站著不動的真人會在第 485／199 tick 自己握住一個**敵方英雄**（輔助模型「站著自動索敵」），round 3 起改成第 29 tick（＝ `idleAutoEngageSec` 1 秒）握住**殭屍**（v4 `pveOnly`）。開關 1 ⇒ round 1–2 握自動目標 **0 tick**（場上沒有 PvE 目標）—— ⭐ 與 round 3+ 同一個模型。
   ⚠️ 玩家看得到的後果：round 2 idle 的真人被打 6 下**沒有反擊**（舊行為：被打 9 下、還手 5 下）。這正是 LoL 語意（「沒有指令就不打人」），只是以前 round 1–2 沒套上；開關填 3 一鍵回頭。
3. **後搖取消／殭屍王優先打玩家**：閘同一扇門（round 1–2 名單已在），沒有另做行為量測（前者要揮空＋新指令的劇本；後者 round 1–2 沒有王）。
4. **索敵半徑**：舊行為 round 1–2 的真人走 bot 那條路（`nearRadius`＝近戰地板 6 u，`idleSeeks:false` ⇒ 不拿 `seekRadius`）；開關 1 ⇒ idle 1 秒後拿 `seekRadius` 48 且 `pveOnly`。表裡「首次自動目標 485 → —」就是這兩條路的差。
5. **一隻殭屍都沒漏**：開關 1 的 round 1–2 `殭屍上限 = 0`（round 3 才 30）。

### 落地（三個住處 ＋ 消費端）

| 住處 | 改了什麼 |
|---|---|
| `content/config/arena-rules.json` | `"humanSeatsFromRound": 1`（`botOnlyRingAccelEnabled` 之後；genguard：沒有產生器擁有者） |
| `packages/shared/src/content/schema/config/arenaRules.ts` | `humanSeatsFromRound: z.number().int().min(1).max(20).optional().describe("@zh …\n@note …")` ＋ `DEFAULT_HUMAN_SEATS_FROM_ROUND = 1`（⚠️ `.optional()`：線上耐久覆蓋層相容） |
| `apps/admin/src/configForms/specs/arenaRules.ts` | `...fromZod((p) => p === "humanSeatsFromRound")` —— 人話只住 Zod（棘輪不加一） |
| `apps/game-server/src/match/arenaRules.ts` ⚠️ 柵欄外 | `ArenaRules.humanSeatsFromRound: number` · `DEFAULT_ARENA_RULES` · `rulesFromDoc` 的 `doc.x ?? DEFAULT`（3 處，同 `botOnlyRingAccelSec` 的形狀） |
| `apps/game-server/src/match/MatchController.ts`（只動 humanSeats 那一段） | 名單算一次共用；`humanSeatsDue = typeof knob === "number" && round >= knob && size > 0`；新增 `else if (mobWaves && humanSeatsDue)`：`beginCombatMobs(world, { ...mobRulesFromConfig(cfg, dt, round, undefined, undefined, humanSeats), autoWaves: false }, [])` |

設計判準：
- ⭐ **走 GH#577 開的同一扇門**（`MobRules.humanSeats`），⛔ 不在協定／`SimWorld` 上開第二個欄位（六個消費端在 `packages/shared/src/sim`，柵欄外，也不該為此改）。
- `autoWaves: false` ＝ 練習房那一格（GH#343）—— 排程停、規則表在；再加**零個 zone** 雙保險。
- ⛔ 不傳 `mobChampionPicker()` ⇒ 零次 RNG 抽取 ⇒ 決定性不變（`mobLevelForRound` 對 round < fromRound 夾在 `Math.max(0, …)`，不會算出負等級）。
- **空集合 ⇒ 不武裝**：全 bot 沙盒／`mobGate` 的「OFF 與 mobless 位元組相等」對照／每一份 all-bot 夾具逐位元不變。
- 舊錄影表頭沒有這一格 ⇒ `undefined` ⇒ 當機制關著（同 `botOnlyRingAccelEnabled` 的判讀）。

已知副作用（都量過、都無害）：
- `mobTicks` 在 round 1–2 從 −1 變成計數 ⇒ 進 `digest()`；同一份設定兩次 run 仍相等（`arenaRules.test.ts` 的決定性斷言綠）。
- snapshot 的 `mobVisualJson` 在 round 1–2 帶出貨表而不是 `MOB_VISUAL_DEFAULT`（沒有殭屍可畫）。
- `rogueliteMobs=false` 的房間從此在 round ≥ 開關也拿得到名單（以前整場都拿不到）—— 這是修正，⛔ 不是開關的事。
- `mobWaves` 缺席的場地仍送不進去（沒 config 組不出規則表）；出貨場地沒有這種。
- 客戶端預測影子／重播的純函式重新武裝**本來就**不傳名單（GH#577 逐字）—— 不變。
- dev cheat「生怪」在 round 1–2 會因規則表存在而不再被 `mob-rules-off` 擋（devCheats only）。

### 守衛與突變

`apps/game-server/src/match/humanSeatsFromRound.test.ts`（2 條，SKELETON_ARENA、波次 0.2 s 一波）：
1. 出貨 1：round 1 名單在、`autoWaves false`、90 tick 內殭屍 0、點地板窗口武裝；round 3 同一份輸入答案相同（且排程開著）。
2. 填 3：round 1 沒名單、窗口不武裝、殭屍 0；round 3 才有 —— rollback 真的回得去。

**突變**：`else if (this.rules.mobWaves && humanSeatsDue)` → `else if (false && …)` ⇒ 第 1 條紅（「round 1 的規則表裡沒有真人座位」），第 2 條照樣綠。改回（`grep -c` = 1）。

game-server 一次 invocation：`humanSeatsFromRound` · 量測暫存檔 · `mobGate` · `mobRoundArming` · `arenaRules.test` · `autoAcquireWhileMoving`（#999）· `idleEnemyEngage`（#999）· `configWiringCompleteness` · `practiceMode` · `practiceDummies` ⇒ **10 檔 68 條全綠**。

### tsc（收工一次）

`apps/admin` 0 錯 · `apps/game-server` 0 錯 · `packages/shared` **2 錯**，全在 `src/ops/enabledSwitchesHaveConsumers.ts`（本 lane 沒碰、工作樹乾淨 ⇒ HEAD 既有）。

---

## 主 session 要做的（柵欄外／統一動作）

1. **`pnpm content:build`**：`content/config/arena-rules.json` 動了 ⇒ `bundle.json` 過期 ⇒ `shippedBundleIsCurrent.test.ts` 會紅到重建為止；產物一起 add。
2. **`pnpm skills:check`**：Zod 多了 `.describe()` 與一格新欄位；讀 config schema 描述的產生器（`tools/declared-shape`、`type-catalog`…）若有輸出變化就 `skills:sync`。
3. **棘輪 969 vs 963**：那 6 格（上表）搬去 `@zh` 或帶理由調線 —— ⛔ 不是本 lane 的帳，但它會在整包測試裡紅。
4. **柵欄外我動了的檔**（都是必要的接線，逐檔可審）：`apps/game-server/src/match/arenaRules.ts`（3 處）· `packages/shared/src/content/schema/config/arenaRules.round11.ts`（柵欄寫的是 `arenaRules.ts`，round11 的 21 個葉子住在這個兄弟檔；只加 `.describe()`）。
5. **兩句過期的散文**（柵欄外，沒改）：`packages/shared/src/sim/mobs.ts:1932`「唯一會傳它的正式呼叫端是 `MatchController.enterCombat`」現在是兩條路；`MobSystem.ts::beginCombatMobs` 檔頭「The host only calls this from ROUND `fromRound` onward」已不成立。
6. `schemaToForm.ts` 的 `@note` 跨行截斷（上面）—— 要不要開票由 owner 說「開票」。

## 改到的檔（逐檔）

- `packages/shared/src/content/schema/config/arenaRules.round11.ts`（21 × `.describe`）
- `packages/shared/src/content/schema/config/apCoefficient.ts`（`zByTier` 第三參數 ＋ 3 個模板）
- `packages/shared/src/content/schema/config/arenaRules.ts`（新欄位 ＋ `DEFAULT_HUMAN_SEATS_FROM_ROUND`）
- `apps/admin/src/configForms/specs/arenaRules.ts`（−21 手寫 ＋ `fromZod` ×2）
- `apps/admin/src/configForms/specs/tiers.ts`（−15 手寫 ＋ spread；`multiHit`/`proseFromFormula` 未動）
- `packages/shared/src/ops/adminFormsHandWrittenRatchet.test.ts`（963 ＋ 逐份欠帳訊息 ＋ 帳）
- `content/config/arena-rules.json`（+1 格）
- `apps/game-server/src/match/arenaRules.ts`（3 處接線）
- `apps/game-server/src/match/MatchController.ts`（humanSeats 段）
- `apps/game-server/src/match/humanSeatsFromRound.test.ts`（新，承重守衛）
- 量測暫存 `humanSeatsRound_temp_measure.test.ts` 已刪（hook 留底在 `docs/legacy/_overwrites/`）；原始數據 `scratchpad/measure1033.keep.json`
