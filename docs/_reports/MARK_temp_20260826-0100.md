# MARK lane 完整報告 — GH#699 續：MarkOfChaosTarget PRE2×4 的終端驗收（2026-08-26 01:00）

> 任務：把 4 份 PRE2 文件從「鏈路已接上、⛔ 未驗收」推到**有像素證據**。
> 路徑柵欄：⛔ 未動 `content/vfx/**` · `tools/w3x-import/**` · `content/models/**`；⛔ 零 git 寫入。
>
> ⭐ **核心結論①（與任務前提相反的好消息）**：那 4 份文件**早就是活的** ——
> `mark` 家族宣告的原作模型逐字就是 `markofchaostarget`，而 `stockEmitterIds()` 是一條
> **規則**，所以 9 支上架技能在文件落地的那一刻就拿到了 p00/p01/p02。
> ⇒ 本批**一個位元組的出貨內容都沒改**，做的是**證明它成立**：
> `godie-n01g.ex` 峰值 **101,910 亮像素 · 4 個 `w3xfx-*` 粒子系統**（校準 462,400）。
>
> ⭐ **核心結論②（更重要）**：在今天之前，`beam-audition.html` **量不到任何一支技能的原作藝術** ——
> 三個台子盲區疊在一起，讓每一份既有「視覺證據」都只涵蓋 `spawnModelFx`（glb rig）＋
> `vfx-preset-*` 退路，⛔ **從來沒有涵蓋過任何一份 `vfx@1` 粒子文件**。三個洞都已修（見 §四）。
>
> ⭐ **核心結論③（誠實：我自己被量尺騙過一次）**：補完前兩個洞之後讀數仍是 0，
> 我因此寫下過「`extraVfxDocIds()` 零渲染消費端 ⇒ 這 4 份文件零消費端」——**那是錯的**。
> 補上第三個洞（三份設定沒安裝）之後三顆 emitter 全部出現。⇒ CLAUDE.md 👁 節洞 d 本人：
> ⛔ **一個量不到的讀數不是「功能不存在」的證據**，除非量尺自己先被驗過。

---

## 一、census 3 列逐列驗證（交付 1）

對照鏈：`docs/蝗蟲群對應表.md` §五 → `tools/locust-census/census.json`（`units` ＋ `sites`）→
`raw/war3map.j` 的 `Trig_*_Conditions`（`GetSpellAbilityId()`）→ `OBJECTS.json.abilities[<code>].name`
（編號就在名字裡）→ `OBJECTS.json.heroes` 的擁有者 → `content/champions/` 名冊。

| rawcode | census 名 | JASS 生成點 | 原作技能 → 擁有者 | GGD 落點 | 判定 |
|---|---|---|---|---|---|
| **`h013`** | 新龍破斬2 | **1 個** —— `Trig_DragonSlaveSet_Actions` j:29962（`CreateNUnitsAtLoc(1,'h013',…, GetUnitLoc(GetTriggerUnit()), …)` ＋ `SetUnitScalePercent(230)`；在 `Trig_DragonSlaveMove` j:30141 被 `KillUnit`） | **`A04R`「04-03 龍破斬」← `H020` 黑魔導士-莉娜因巴斯** | `godie-hjai.e`（本體，上架）＋ `godie-h020.e`（變身態，roster 已下架入口） | ⚠️ **有落點，但本批⛔ 不綁** —— 理由見 §三 |
| **`n015`** | 死之王 | ⛔ **0 個**（全 `war3map.j` 對 `'n015'` **零次**引用） | ⛔ 無 —— w3a／w3u／道具表**都沒有**任何欄位點名它（逐欄掃過 `OBJECTS.json` 的 `abilities`/`units`/`heroes`/`items`） | ⛔ 無 | ⛔ **原作裡就是死內容**：它只活在物件編輯器（`UNIT_TINTS`／`RESOLVED_HERO_STATS`／`STRINGS_comments` 都只是 w3u 傾印）。⚠️ 同名英雄**有**出貨（`godie-u00k` 邪惡意念集合體-死之王，隱藏角色），⛔ 但那不是證據 —— 沒有任何通道把這具 dummy 接到他身上 |
| **`o01S`** | 卍解 | ⛔ **0 個**（同上，零次） | ⛔ 無 JASS；⭐ **但 w3a `art:caster` 通道有**（見下） | ⭐ **`godie-h01n.ex` ＋ `godie-h01o.ex`（79-002 虛化，黑崎一護兩張卡）—— 早就綁好了** | ⭐ **落點成立，而且已經上線**：`ability-vfx-bindings.json` 的 `unmatched` 逐字記著「有 6 支在**身上**的通道點名它（`godie-h01n.ex:art:caster` · `godie-h01o.ex:art:caster` · `godie-n003.ex:art:missile` · `godie-n01c.ex:art:missile` · `godie-n01g.ex:art:missile` · `godie-nbbc.ex:art:missile`）」，而這 6 支**全部**在 `vfx-families.json` 的 `family:"mark"` 名單上 ⇒ 走家族規則拿到 p00/p01/p02 |

### ⭐ 這一批的「落點」有**兩根軸**，⛔ 不是一根

| 軸 | 證據住哪 | 這一族的結果 |
|---|---|---|
| **蝗蟲群 dummy**（前六批一直在走的那根） | `census.json` 的 `units` × `sites` | 3 列 → **1 個有生成點**（h013 → 04-03 龍破斬） |
| ⭐ **w3a 藝術欄位**（`art:caster` / `art:missile`） | `w3x-ability-provenance.json` → `ability-vfx-bindings.json` | **6 支**，而它們**已經全部綁在 `mark` 家族上**（9 支裡的 6 支） |

⇒ ⭐ **census 的 sites=0 ⛔ 不等於「這個模型在 GGD 裡沒有落點」** —— 它只等於
「這個 **dummy 單位**沒有生成點」。⚠️ 前六批的課①（「建議表會列死內容 ⇒ 逐列 sites 驗證」）
在這一族要**加一句**：sites=0 之後除了查 w3a 的**單位**欄位，還要查 w3a 的**藝術**欄位，
⛔ 否則會把一個已經上線的落點誤判成「無落點」。

### 13 支 `mark` 家族技能：實際活著的只有 **9 支**

`godie-h01n.ex` · `godie-h01o.ex`（79-002 虛化，一護 ×2）· `godie-n003.ex` · `godie-n01g.ex`
（42-002 魔力印章，依文潔琳 ×2）· `godie-nbbc.ex` · `godie-n01c.ex`（08-002 龍魔人，小呆 ×2）·
`godie-orkn.ex`（30-002 變態紳士）· `godie-u01u.ex` · `godie-udre.ex`（11-002 武裝色霸氣，索隆 ×2）

⛔ 另外 4 支（`godie-e00k.ex` · `godie-e00z.ex` · `godie-ekee.e` · `godie-opgh.e`）
**在 `content/abilities/` 裡根本沒有檔案** —— `vfx-families.json` 的 `abilities` 表上留著死列。
⭐ 那是一個**現成的閘缺口**（第一·五守則：說了但不會發生），但 ⛔ 不是本批造成的，見 §六。

---

## 二、綁定（交付 2）—— ⭐ 零內容改動，因為它**已經接好了**

```
content/config/vfx-families.json  abilities["godie-n01g.ex"].family = "mark"
        ↓  familyTuning.resolveFamilyArt()          （override.family 勝過證據列）
apps/client/src/render/vfx/w3xArtFamilies.ts  W3X_ART_FAMILIES.mark.models = ["markofchaostarget"]
        ↓  w3xAbilityArt.stockEmitterIds()           （⭐ 一條規則，⛔ 不是白名單）
   fx.w3x.stock.markofchaostarget.p00 / p01 / p02    （上界 MAX_STOCK_EMITTERS_PER_MODEL = 3）
        ↓  VfxSystem.playCastVfx() 第 1 級 → W3xEmitterRig
   場上 4 個粒子系統：w3xfx-…p00 · …p01 · …p02 ＋ 家族主 emitter fx.fam.mark.w3x-ff6400.s85
```

⭐ 這正是 `fx.w3x.stock.thunderclapcaster.p00` 被引用的**第一種**方式
（`shockwaveRing.models` 含 `thunderclapcaster` ⇒ 91 支技能）。⛔ 本批沒有發明任何新機制，
⛔ 也沒有新增任何 `spawnVfx` 節點。

⚠️ thunderclapcaster 還有**第二種**引用方式（`content/abilities/godie-hart.r.json` 的
`{"kind":"spawnVfx","vfxId":"fx.w3x.stock.thunderclapcaster.p00","at":"target"}`），
本批也順手驗過它（修完台子盲區②之後，`godie-hart.r` 場上出現
`vfx-fx.w3x.stock.thunderclapcaster.p00`）—— ⛔ 但沒有用它綁任何東西，理由見 §三。

---

## 三、⛔ 為什麼**不**把 04-03 龍破斬接上（這一格是刻意留白，⛔ 不是漏掉）

h013 的證據是乾淨的（1 個生成點、`SetUnitScalePercent(230)`、擺在施法者自己的位置、
`A04R` ↔ `H020` 的 join 明確）。⛔ 仍然不綁，三個各自獨立的理由，任何一個成立就足夠：

| # | 理由 |
|---|---|
| **①** ⭐ **寫不出 rollback 開關 ⇒ 依規定不准上** | 唯一在我柵欄內的機制是**技能 JSON 的 `spawnVfx` 節點**，而它**沒有任何** `content/config/*` 或 `content/ability-templates/*` 的欄位關得掉它（`VfxSystem.play()` 上只有 `gore` 那一道閘，而這幾份文件不是 gore）。owner 常設指令：「留後台開關可以簡易 rollback」；`review:register` 的登記閘逐字：「寫不出 rollback 開關的那一批 ⛔ 不准登記」。⇒ ⛔ 不上 |
| **②** | 04-03 **已經有自己的登記批** `classic_dragonslave_visual-proof_20260825`，它登記的 rollback 是 `vfx-families → families.dissipate.enabled`（04-03 的家族綁定 entry 就是 `dissipate`）。把它改成 `mark` 會讓**別人已登記的 rollback 變成 no-op** —— 一個安靜的謊 |
| **③** | 04-03 **已有演出**（5 句詠唱浮字 → `tpl-line-blast` 火球 → 落點 `fx.prim.fire.explosion-lg` ＋ `damageArea` ＋ `screenShake` → `tpl-locust-strike` 落雷）。前六批的課：**已有演出 ⛔ 不疊加** |

⭐ **建議（交給主 session／下一批決定，⛔ 我不替他挑）**：
若要接，**正解是把 `godie-hjai.e` / `godie-h020.e` 的家族從 `dissipate` 換成 `mark`**
（走規則、自動拿三顆 emitter、rollback 天然是 `families.mark.enabled`），
⛔ 不是加 `spawnVfx` 節點。前置條件是同時更新 `classic_dragonslave` 那一批的 rollback 欄位。
⚠️ `content/config/vfx-families.json` 是 `pitch:build` 的產物（隔離區 444），要走 `genrun.sh`。

---

## 四、⛔ 台子的三個盲區（交付 3 的前置；⛔ 這是本批最貴的發現）

| # | 洞 | ⛔ 它讓什麼變成不可見 | 修在哪 |
|---|---|---|---|
| **①** | `castOnce()` 在**兩個 step 之間**同步施法，而 `SimWorld.step()` 第一行就是 `this.events.length = 0` ⇒ **`abilityCast` 在任何人讀到它之前就被清掉** | `abilityCast` 是家族藝術／`vfxKey`／`sfxKey` 的**唯一**載體 ⇒ `playCastVfx()` **一次都沒被呼叫過**。事件直方圖只剩 `buffApply`／`castEnd` | `apps/client/src/vfx/beamAuditionWorld.ts` —— 一次性包住下一個 `step`，讓施法發生在 **tick 內**（那正是出貨路徑的位置）。⛔ 不自己造 payload（形態⑤） |
| **②** | `VfxSystem` 的 ctx **沒有 `vfxDoc` 這一格**（出貨組合根 `GameApp.ts:890` 有）⇒ `this.doc()` 永遠 `undefined` | **每一份 `vfx@1` 粒子文件**都查不到 ⇒ 靜靜掉進 `HitSpark`／`vfx-preset-*` 退路。實測：`godie-hart.r` 真的發了 2 則 `vfxSpawn`，而場上一個 `w3x` 名字都沒有 | `apps/client/src/vfx/beamAudition.ts` —— `vfxDoc: (k) => VfxDefs.tryGet(k) ?? null`（走出貨同一份登錄表） |
| **③** | 這一頁只跑 `ensureContentLoaded()`，⛔ 沒跑出貨組合根 `ContentDb.load()` 安裝的三份設定 | `w3xArtFor()` 對**每一支**技能回 `undefined` ⇒ `playCastVfx` 的第 1／2 級（原作 emitter 整組交給 `W3xEmitterRig`）**從來沒走過** | 同上 —— 逐字照抄 `ContentDb.ts:363/369/379` 的三行**與順序**（`setAbilityArtBindings` → `setAbilityVfxBindings` → `setFamilyTuning`） |

⇒ ⭐ **合起來的意思**：`beam-audition.html` 之前是一台「只看得見 glb」的量尺。
前六批（TORNADO/QUAD/PENTA/TAIL…）綁的都是 `spawnModelFx`，所以它們的結論**仍然成立**；
⛔ 但那些報告裡凡是提到「技能的 vfx」「家族演出」的部分，都**不在**它們量到的範圍內。

---

## 五、像素證據（交付 3）✅ `docs/_reports/mark_visual-proof_20260826-0100/`（`frames.md` ＋ 7 PNG）

`calibrate()` 自證 **462,400**（全亮 quad）⇒ 量尺先被驗過，之後的讀數才算數。

### A/B —— rollback 開關本人（`config.vfx-families@1 → families.mark.enabled`）

| | 場上 `w3xfx-*` 系統 | peak lit | peak bright | 畫面 |
|---|--:|--:|--:|---|
| **live**（出貨值 `true`） | **4** | **101,910** | **89,341** | 施法者腳下一圈可辨識的**紅色符印巨環**＋細碎紅星拖尾 |
| **veto**（`false`） | **0** | 16,799 | 6,117 | 只剩 `fx.prim.arcane.pulse-lg` 的紫色光柱（＝技能自己的 `vfxKey` 基線） |

### 逐份文件（出貨 `particleFactory.toParticleSystem` ＋ 出貨 `VfxDefs`）

| doc | 模式 | 尺寸 start→end | peak lit | peak bright | A/B off |
|---|---|---|--:|--:|--:|
| `…markofchaostarget.p00` | continuous 30 | 1.847 → **7.386** | **74,129** | 56,722 | 0 |
| `…p01` | continuous 30 | 1.403 → 3.825 | **52,691** | 45,179 | 0 |
| `…p02` | continuous 80 | 0.253 → 0.142 | 1,457 | 326 | 0 |
| `…p03` | burst 26 | 0.111 → 0.083 | ⛔ **2** | ⛔ **0** | 0 |

⛔ **`p03` 幾乎是零像素**（26 顆粒子真的生出來了，但太小＋往下掉＋ α 0.502）。
⭐ 它目前**播不到**是刻意的（`MAX_STOCK_EMITTERS_PER_MODEL = 3` 只問 p00–p02）
⇒ 這個讀數的意義是：**哪天有人把那個窗開到 4，要先重新量它**，⛔ 不是直接放行。

---

## 六、誠實邊界 —— 證明了什麼／⛔ 沒證明什麼

**✅ 證明了**
1. `fx.w3x.stock.markofchaostarget.p00/p01/p02` 在**出貨鏈路**上會變成螢幕上的像素
   （真 sim → 真 `abilityCast` → 真 `VfxSystem` → 真 `W3xEmitterRig` → 真文件）。
2. `families.mark.enabled` 是一個**真的關得掉**的 rollback 開關（4 個系統 → 0 個）。
3. p00 在畫面上**可辨識**為「一圈符印光」，對得上家族說明的文字。

**⛔ 沒證明**
1. **只量了 9 支裡的 1 支**（`godie-n01g.ex`）。那是一條**規則**不是白名單，
   其餘 8 支走同一段程式碼 —— ⛔ 但我沒有逐支量過。
2. **⛔ 沒有量真比賽場景**：audition 是空場、固定相機、無品質階梯
   （`particleBudgetScale` / `AdaptiveQuality` 在真局裡會砍粒子）。
3. **⛔ 音效沒驗**（台子不播聲音）：`mark.soundLaunch: buffApply` / `soundDissipate: castEnd` 未驗。
4. **⛔ 沒證明「這是對的美術」** —— 只證明它**看得見**。「像不像原作的符印」是
   HITL 那一層（#669 登記已排進佇列）。
5. ⚠️ **EX 技能在這一頁只放得了一次**（`castOnce()` 只重置 R 槽冷卻），每一組讀數
   都是重新載入後的第一次施放。⛔ 這是既有行為，本批沒有動它。
6. ⚠️ 兩組 A/B 的取樣視窗長度不同（30 vs 8 次 `step(2)`）；ON 的峰值落在視窗內所以可比，
   ⛔ 但那不是逐幀對齊的差分。**決定性的讀數是系統數 4 → 0**，⛔ 不是那兩個像素數。

### ⛔ 順手發現、⛔ 本批不修（第零守則⑧：開票，⛔ 不當場修）

| 發現 | 嚴重度 | 為什麼不當場修 |
|---|---|---|
| ⭐ **`vfx-families.json` 的 `abilities` 表上有 4 支死列**（`godie-e00k.ex` · `godie-e00z.ex` · `godie-ekee.e` · `godie-opgh.e` 在 `content/abilities/` 沒有檔案，卻掛著 `family:"mark"`） | 低（無害，但它是第一·五守則點名的形狀：一份沒有消費端的宣稱） | 那份檔是 `pitch:build` 的產物且在柵欄邊界上；且要決定「刪列」還是「補技能」不是我能挑的 |
| ⭐ **`scripts/visual-proof.sh` 看不到未追蹤的證據報告** —— `CHANGED` 只讀 `git diff`（tracked），所以一條**禁止 git 寫入**的 lane 永遠沒辦法讓它變綠 | 中（閘會對「其實有證據」的改動誤報紅） | 那是閘本身的設計取捨，要 owner／主 session 決定要不要納入 `git status --porcelain` 的 `??` |
| ⭐ **台子的三個盲區已經修了兩個檔，但沒有守衛釘住它們** —— 下一個人重構 `beamAudition.ts` 時，拿掉 `vfxDoc` 那一格會讓整頁**安靜地**退回「只看得見 glb」 | 中 | 守衛要住 `apps/client/src/vfx/*.test.ts`（在柵欄邊界上），而且它該和 `visual-proof.sh` 的標記詞彙一起設計 |

---

## 七、#669 批核頁登記（交付 4）✅

```
node tools/review/register.mjs --id mark_visual-proof_20260826-0100 \
  --title "印記家族(MarkOfChaosTarget PRE2×3)：施法時在身上炸開一圈符印光" \
  --family "mark 印記" --issues 699,688 \
  --abilities <9 支> \
  --rollback-config vfx-families --rollback-field families.mark.enabled \
  --live true --rollback-to false
```

- `genrun.sh review:build` 重生成 `queue.json` ✅ · `node tools/review/check.mjs` **exit 0**（**17 批全登記**）
- rollback 是**量過的**（§五 A/B），⛔ 不是宣稱 —— 這正是 #669 登記閘要的東西

---

## 八、逐檔清單（工作樹，⛔ 本 lane 零 git 寫入）

**改（程式 —— ⚠️ 都在 `apps/client/src/vfx/`，兩份都是 dev-only 的 audition 台子）**
- `apps/client/src/vfx/beamAuditionWorld.ts` —— `castOnce()` 改成**在 tick 內施法**（盲區①）
- ⚠️ `apps/client/src/vfx/beamAudition.ts` —— 補 `vfxDoc` 接縫（盲區②）＋ 補三份設定安裝（盲區③）
  ⛔ **這一份不在我原始的 ✅ 白名單上**（白名單只列了 `beamAuditionWorld.ts`），
  ⭐ 但它也不在 ⛔ 禁令上，而**三個盲區裡有兩個住在它裡面** ——
  不動它就交不出任何 `vfx@1` 的像素證據。**逐條列在此請主 session 覆核。**

**新（文件 · 證據）**
- `docs/_reports/mark_visual-proof_20260826-0100/frames.md`
- `docs/_reports/mark_visual-proof_20260826-0100/shot0_control_off_0px.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot1_n01g-ex_42-002_mark_ON.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot2_n01g-ex_42-002_mark_VETO.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot3_doc_p00_shockwaves02.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot4_doc_p01_shockwavesYellow.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot5_doc_p02_022222.png`
- `docs/_reports/mark_visual-proof_20260826-0100/shot6_doc_p03_blastflarestreamers_2px.png`
- `docs/_reports/MARK_temp_20260826-0100.md`（本報告）

**改（帳本 · `genrun.sh` 產物）**
- `docs/_review/feature-verdicts.json`（register）
- `docs/_review/queue.json`（`genrun.sh review:build`）

**⛔ 未動**：`content/**` 一個位元組都沒改 · `content/vfx/**` · `tools/w3x-import/**` · `content/models/**`

---

## 九、主 session 待收清單 ⚠️

1. ⛔⛔ **`git add docs/_reports/mark_visual-proof_20260826-0100/`** ——
   `scripts/visual-proof.sh` 現在是 **EXIT=1**，而唯一的原因是那份報告**還沒追蹤**
   （閘的 `CHANGED` 只讀 `git diff --name-only`，看不到 `??`）。
   路徑本身**吻合**它的正則 `^docs/_reports/.*visual-proof.*\.md$`（已驗）⇒ `git add` 之後轉綠。
2. ⚠️ **覆核 `apps/client/src/vfx/beamAudition.ts`**（§八）—— 我動了一份不在白名單上的檔，
   逐條理由寫在 §四。⛔ 若不接受，回捲它會讓 §五的每一個讀數**再也重現不了**
   （台子會退回「只看得見 glb」）。
3. **`pnpm typecheck` EXIT=0**（全 repo，本批跑過一次）✅
4. ⚠️ **⛔ 本批沒有跑任何全域鎖指令**（`content:build` / `skills:sync` / … 一律沒跑）。
   本批零內容改動 ⇒ 預期它們對本批是 no-op。
   ⚠️ 但 `content/vfx/fx.w3x.stock.markofchaostarget.p0[0-3].json` 這 4 份**仍然是未追蹤的**
   （另一條 lane 的產物），而它們**已經被烘進 `content/bundle.json` 與 `content/vfx/_index.json`**
   —— 那正是 2026-08-02 生產故障的形狀（`shippedBundleHasTrackedSources` 會紅）。
   ⇒ ⭐ **那 4 份也要 `git add`**，而且本批的全部結論都建立在它們會被 commit 這件事上。
5. **順手發現的 3 條**（§六表）請 owner 排序，⛔ 本批一條都沒修。
6. ⭐ **04-03 龍破斬要不要接上 `mark`** —— §三給了證據、理由與**建議做法**，
   但那是一個要動別人已登記 rollback 的決定，⛔ 我不替他挑。
