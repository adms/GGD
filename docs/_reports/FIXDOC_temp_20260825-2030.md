# FIXDOC · 文件層誤導修正（依 DSCAN_temp_20260825-2000.md）

> 2026-08-25 · FIXDOC lane。只改註解/字串/文字，零程式邏輯、零 git 寫入、零全域鎖指令。
> 每檔先跑 `bash scripts/genguard.sh`，分類與 lane 指示一致（例外見 §3）。

## 1. 改了什麼

| # | 檔 | 改動 | genguard |
|---|---|---|---|
| F1 | `docs/新英雄上架SOP.md`（:46 表列 4） | `bundle.test.ts 變紅` → `shippedBundleIsCurrent.test.ts 變紅（bundle.test.ts 刻意在 temp 樹重建，抓不到出貨那一份過期）` | 手編 ✅ |
| F2 | 同檔 :29-30 | `content/_index.json` → `content/champions/_index.json` / `content/abilities/_index.json` 兩份實名；114/668 → 「以 `_index.json` 實數為準」 | 手編 ✅ |
| F3 | `docs/_map-report.md:3` | banner 指令 → `pnpm --dir tools/anime-arena-map map:gen` | 無擁有者（不在 sync-io）⇒ 依 lane 指示直接改 |
| F3 | `docs/_新場地計畫.md`（:216 :364） | 同上（:364 是 `map:check`）。:106/:178/:611 是描述非指令，未動 | 手編 ✅ |
| F3 | `docs/_更新機制清單-v0.16.2-v0.18.1.md:60` | 同上 | 手編 ✅ |
| F3 | `tools/anime-arena-map/gen.ts`（:11-13 註解 · :152 · :174 · :194 · :205 字串） | 五處 `pnpm map:gen`/`map:check` → `--dir` 形式＋註解補「root 打會 ERR_PNPM_NO_SCRIPT」。⭐ 純字串，零邏輯。:194 與 `_map-report.md:3` 逐位元一致（map:check 不會 drift） | 來源檔 |
| F4 | `docs/_codex-handoff.md`（:6 :15 :187） | 指紋字面值 `9a5fed3e` → 「以 `docs/editor-contract/ggd-runtime-capabilities.md` 檔頭為準（有守衛）」；:15 保留歷史鏈但標「當時的」 | 手編 ✅ |
| F4 | 同檔 :247 | 78/461 → 「以兩份 `_index.json` 實數為準，⛔ 不抄快照數字」 | 手編 ✅ |
| F8 | `docs/_execution-batches.md:9` | 現況段 → 「看本檔**最後一節**（v0.27.3 已上線 2026-08-25）」＋舊表降級為 2026-08-18 歷史快照 | 手編 ✅ |
| F9 | `docs/_session-handover.md`（檔頂＋:119-121） | 檔頂加「⚠️ 2026-08-08 快照已過期：現況以 _execution-batches.md 尾節＋git tag 為準」；「owner 手動重製中勿動 abilities」節改寫成 `skillremake:json`＋genguard＋產物隔離區的現行規則（照 MSCAN #12） | 手編 ✅ |
| F5 | `docs/技能編輯器引擎須知 20260811.md`（:119 :422，⭐ 實際路徑在 docs/ 不在 legacy/） | :119 → `docs/legacy/_w3x-fidelity-superseded.md`；:422 → `packages/shared/src/content/balanceAnchors.ts`。chmod u+w → python 檔案 API 改 → chmod 444。兩行都在手維護區（refresh_docs.py 只重寫 §13.10） | 🚫 skillremake:docs 產物，依 lane 指示 chmod 舞步 |
| F7 | `README.md`（:1925 :1935-1940，標記外＝手維護區） | 「兩份」→「三份」＋補 `pnpm tiers:build` 一行＋「三者都有 :check 版（含 `tiers:check`）＋ `skillTiersDocFresh.test.ts`」。chmod 舞步同上 | 🚫 docs:readme 產物 |

## 2. 驗證（唯讀）

- `git diff` 確認兩份產物檔的 diff 只落在上列行（README 標記區 387-421/433-457/623-672 未動；§13.10 未動）。
- `pnpm skillremake:docs:check` EXIT=1：紅在「**§13.10 已過期**」（檔頭指紋行報最新）——
  §13.10 我沒碰，是上游 skill-remake 來源已被其他 lane 動過的既有 stale。
- `pnpm docs:readme:check` EXIT=1：**全部九個標記段**報 replaced（cv_46addb46c5b6 過期）——
  同樣是既有 stale，非本 lane 造成。⇒ 兩個紅都由主 session 的 `pnpm skills:sync` 統一收。

## 3. 留給主 session

1. **重跑 `pnpm --dir tools/anime-arena-map map:gen`**：gen.ts 的 note 字串改了，
   `content/arenas/*.json` 與 `config.map-report@1` 的 note 還是舊句（lane 禁跑產生器）。
   `docs/_map-report.md:3` 已與 gen.ts:194 逐位元一致，重跑不會打回。
2. `apps/admin/src/configForms.ts:3888` 同一句 `pnpm map:gen`（DSCAN F3 尾註，掃描範圍外）未改。
3. DSCAN F6（vfx-families 寫入者）修法在 `editorCapabilities.ts:1298`＋`caps:export`——
   caps:export 是全域鎖指令，本 lane 未動。
4. DSCAN F4 建議的 `codexHandoffFresh.test.ts` 加一行指紋比對：未做（lane 限純文字）。
5. `docs/_codex-handoff.md:146` 還有一個 `9a5fed3e` 歷史敘述（「引擎已經走了三步到」）——
   在病歷/歷史脈絡裡，DSCAN/lane 未點名，未動；主 session 可裁量。
