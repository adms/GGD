# GH#1078 —— 模板預設展開要**真的施放**：`templateDefaultsCast.test.ts`（lane 報告 2026-09-06）

> 狀態：**完成**（閘落地、突變紅、tsc 綠）。⛔ 未 commit（主 session 統一）。

## 做了什麼

新守衛 `packages/shared/src/content/templates/templateDefaultsCast.test.ts`（216 行，一條 `it` 收全部 enabled 模板）：

| 步 | 走的路（⛔ 沒有一行手寫 EffectDef） |
|---|---|
| ① | 真的 `ContentLoader(FsContentSource(content/))` 載入出貨內容 |
| ② | 每一份 `status:"enabled"` 模板 → `defaultParamsFor`（編輯器開卡的預填）→ 一份只帶 `template:{ref,params}` 的骨架 doc（同 `paramsSchema.test.ts::throughRegistryPath`）→ `store.add("abilities", …)` |
| ③ | `registerAll(store)` —— 出貨的 `expandIfTemplated` → `zAbilityDoc` → `withTiers`（級距解析）→ `withProse` |
| ④ | 記憶體夾具英雄：`godie-hart` 的複本，Q（或天生技格）換成探針，⚠️ 自己的被動拿掉（`castabilityVfxOnly.test.ts` 踩過：一個 onAbilityHit 補刀會讓純演出也量到 damage）；`registerChampion`（出貨的註冊器） |
| ⑤ | 真的 `SimWorld` ＋ 貼身假人（敵／友各一，同普查的 1.35u）→ 半血半魔、施法者魔力剛好一次 → 真的 `castAbility` |
| ⑥ | 吟唱 ＋ 4 s 的觀察窗，逐 tick 交給**出貨的判定** `castabilityVerdict.ts::classifyCastOutcome`（普查讀同一份）；有動就提早收工 |

三種「有動」全部從資料推導（⛔ 不是一張手寫的模板名單）：

| 形狀 | 判準 | 今天幾份 |
|---|---|---:|
| 主動 | `classifyCastOutcome` PASS（damage／heal／status／buff／dash／summon） | 24 |
| 被動（`isPassiveOnly`／`isPassiveInnate`） | ModifierSource／標記真的掛上（同普查 PASSIVE） | 4 |
| 純演出 modelFx（展開的 `spawnModelFx` 既沒 `onTouch` 也沒 `onArrive`；expand.ts 逐字「⛔ 純演出」） | `modelFxSpawn` 事件至少一發、實例數 > 0（客戶端畫的就是這個事件 —— 它們**唯一**的終端，⛔ 不是跳過） | 7 |

`paramsSchema.test.ts` 的舊探針（effects 非空）留著，加了 4 行交叉引用：它是中間節點，終端在新檔。

## 量到什麼（`GGD_1078_ROWS=1 npx vitest run …` 印出的 35 列）

| 模板 | 判定 | 頻道 |
|---|---|---|
| tpl-beam-roll · blink-strike · combo-finisher · drain-leech · ground-nova · instant-blast · line-blast · line-sweep · orbit-array · periodic-field · proxy-cast · proxy-fanout · pull-throw · radial-burst · random-barrage · single-strike · traveling-wave | PASS | damage |
| tpl-buff-self | PASS | buff |
| tpl-charge-push · leap-strike · teleport | PASS | dash |
| tpl-life-manipulate | PASS | heal |
| tpl-lock-combo | PASS | status |
| **tpl-summon-agent** | PASS | **summon**（⚠️ 補丁指針，見下） |
| tpl-growth-charge · on-attack · on-hit-react | PASSIVE | passive:hooks |
| tpl-mark-stacks | PASSIVE | passive:marks |
| tpl-dragon-quake ×12 · dragon-serpent ×3 · locust-line ×6 · locust-swarm ×3 · locust-orb ×1 · locust-strike ×1 · locust-travel ×1 | MODEL_FX | modelFxSpawn×N |

⭐ **最重要的一個發現**：`sim/castabilityVerdict.ts::snapshotChannels()` **看不見召喚** —— `world.summon` 不在六個頻道裡，`spawnSummon` 不掛狀態／buff／不發 `EFFECT_EVENTS` 的事件。`tpl-summon-agent` 召喚成功（0→1 具）而出貨判定回 FAIL。照那個檔自己的檔頭，那是「缺一個 kind 是**假的 ❌**」。⇒ 新檔暫時多量 `world.summon.size` 這一根指針（`EXTRA_CHANNELS`，檔頭寫明是補丁、它補上的那天刪掉）；正解開票 **GH#1087**（`castabilityVerdict.ts` 在本 lane 柵欄外）。

## 突變紀錄

`sim/effects/summon.ts:140` 的 `capRaw <= 0 ? Number.POSITIVE_INFINITY : capRaw` 改回 `Math.max(0, capRaw)`（GH#1076 修法回退，用 `scripts/edit-or-die.py` 改／還原）⇒ 🔴：

```
+ "tpl-summon-agent: FAIL（cast accepted but produced no measurable effect (no-op)）"
```

還原後 grep 驗過那一行逐字回來（⚠️ 該檔另一條 lane 也在改，改／還原在同一個 Bash 裡完成）。

## 跑過的閘

- `npx vitest run` 新檔 ＋ `paramsSchema` · `modelFxAnchorBone` · `expand` · `stack` · `spawnModelFxBone`：59/59 綠
- `pnpm typecheck`（packages/shared）：EXIT 0

## ⛔ 沒做到的 / 誠實的邊界

- 觀察窗是「吟唱 ＋ 4 s」的固定上界，⛔ 不是普查那三支逐樹推導的 window 函式（它們住在測試檔內、沒有匯出）。35 份預設裡作者填的最長延後是 periodic-field 首發 1 s、lock-combo 五段 2 s，4 s 是上界，而且有動就提早收工。
- 帶隨機節點的格子只跑一顆 seed（`random-barrage` 的假人有照普查釘到引擎排定的落點；`on-attack` 的 chance 是被動，不施放）。
- 召喚指針是補丁（見上，GH#1087）。

## 需要主 session 動的柵欄外檔案

- ⛔ 這張票要過**不需要**任何柵欄外改動。
- （選）GH#1087：`sim/castabilityVerdict.ts` 補 `summons` 頻道 → 刪新檔的 `EXTRA_CHANNELS`。

## 順手發現但沒動

- GH#1087（上）。
- 守則犯錯一筆（帳本 `docs/守則犯錯.md` 在柵欄外，請主 session 記）：`bash scripts/rule-slip.sh 0-成本 順手 "1078：一批 vitest 跑了 5 次（首跑、列表、突變、批次、tsc 修完重跑）——列表那一次該併進首跑"`

## 改了哪些檔

- 新增 `packages/shared/src/content/templates/templateDefaultsCast.test.ts`
- `packages/shared/src/content/templates/paramsSchema.test.ts`（+4 行註解，探針本體不動）
