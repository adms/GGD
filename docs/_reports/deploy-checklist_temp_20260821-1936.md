# 🗳 部署協定第 2 步：owner 勾選表（v0.22.x · 2026-08-21 19:36 GMT+8）

> CLAUDE.md 部署協定第 2 步：
> 「把**這一輪順手開的 issue** 整理成一張表拿給 owner 勾選 —— 一行一個：
>  **是什麼 · 多嚴重 · 修起來多大 · 不修玩家會遇到什麼**」
> ⚠️ 少了這一步，第零守則⑧ 就從「把排序權還給 owner」變成「把缺陷埋進 issue 堆」。

**排序依據 = 玩家看不看得出來**，⛔ 不是票號、⛔ 不是嚴重度標籤。
第 0 區排在最前面，因為它**不是票** —— 它是「不做，deploy 完當天就壞」的操作。

| 資料 | 值 |
|---|---|
| 開著的 issue 總數 | **329**（`gh issue list --state open --limit 400`，19:30 抓） |
| 這一輪（2026-08-20 起）新開**仍開著** | **10** 張（#484 · #491–#499） |
| 這一輪新開**已關** | 6 張（#485–#490） |
| 工作樹狀態 | `main`，領先 `origin/main` **27 個 commit**，⚠️ **有 4 個檔的 lane 工作未 commit**（見 §0-③） |

⚠️ **本表的可信度分兩層，⛔ 不要混著讀**：
- 🔬 **量到的** —— 我這一輪真的讀了程式碼／檔案確認的（§0 全部 · §A 全部 · §F 全部）
- 📄 **從票面判讀** —— 只讀了標題與部分 body，⛔ **沒有逐張回頭復驗**（§B §C §D §E）
  這些票最早開在 2026-07-28，其中一部分很可能**已經被後續版本修掉而沒關票**（§F 已證實有這種）。

---

## 🔴 §0 — 部署當下必做的三件事（⛔ 不是票，勾了才能按 deploy）

### ☐ 0-① 線上白名單仍是 **59**，本機是 **49** —— deploy **碰不到它**

🔬 **量到的**（⛔ 不是推測）：

| 事實 | 證據 |
|---|---|
| 本機 `data/curation/whitelist.json` 的 `champions` = **49** | 直接讀檔 |
| 這個檔**在 .gitignore 裡** | `git check-ignore -v` → `.gitignore:34:/data/**` |
| ⇒ `git push` + `git pull` 型的 deploy **一個位元組都不會動到線上那份** | 同上 |
| 線上仍是 **59**（含 10 個變身態） | GH#472 的 owner 裁決紀錄；⛔ 我這一輪**沒有**連線去查（守則：不 curl 正式站） |

**不修玩家會遇到什麼**：選人畫面上**多出 10 個變身態**（超級賽亞人、雷天大狀那一類）可以直接選。
owner 2026-08-21 逐字：「**上架不能包含變身態 我們討論過了 之前就是這樣才沒改到正確的英雄技能**」。

**⭐ 但這一版做完之後，它有三層防線 —— 每一層都要求「完整重建映像」：**

| 層 | 在哪 | 這一版有沒有 | 需要什麼 |
|---|---|---|---|
| ① game-server 拒絕選人 | `apps/game-server/src/curation/whitelist.ts:132` `if (isTransformedBody(id)) return false` | ✅ 已在 | **game 映像重建** |
| ② platform 讀寫兩邊都剔除 | `apps/platform/internal/curation/transformevict.go` `WhitelistGate.Evict`，在 `Load`(:207) 與 `Save`(:257) 兩處，**預設 on** | ✅ 已在（#486，commit `f0779b70`） | **platform 映像重建**（Go 改動） |
| ③ 後台一鍵清理存量 | 後台內容白名單頁「🧹 清理變身態」（兩段式 + 還原點） | ✅ 已在 | 只要 admin 有部署 |

⇒ **✅ 要勾的動作**（照順序）：

1. ⛔ **這一版不可以用 `--content-only` 部署** —— ①②都是**程式**改動，`--content-only` 只重啟 game shard、
   ⛔ 不重建 platform 映像 ⇒ 三層防線一層都不生效，線上照樣 59。
2. deploy 完（**完整重建**）後讀 `GET /api/v1/curation/whitelist` → `champions` 應該是 **49**。
   ⭐ 因為 ② 是在 `Load` 上剔除的，**正常情況下它會自己變 49，⛔ 不必按按鈕**。
3. **如果它還是 59** ⇒ 表示閘是啞的（platform 讀不到 `/srv/content`，或 `GGD_CURATION_TRANSFORM_GATE=0`）。
   這時候才需要 owner 去後台按那顆「🧹 清理變身態」。
   ⚠️ 平台 boot log 會直接說是哪一種：`whitelist TRANSFORM gate is INERT` / `SWITCHED OFF` / `armed`。
4. ⭐ 按鈕**仍然建議按一次** —— ② 只是「服務不出去」，磁碟上那 10 個 id 還躺著，
   每一次 `opstate export` 都會被複製到下一台機器。按鈕才是把**存量**清乾淨的那一下。

---

### ☐ 0-② `--content-only` 這一版**明確不可用**

🔬 這一版改到的東西**橫跨三個映像**：

| 改到什麼 | 例 | ⇒ |
|---|---|---|
| Go（platform） | `apps/platform/internal/curation/transformevict.go`、`internal/room/rally.go` | platform 映像必須重建 |
| TS（game-server / client） | `curation/whitelist.ts`、大廳集合令、掉金幣 VFX | game + client 映像必須重建 |
| `content/` | 420 支技能說明推導、五級距、`lobby-rally.json` | bind-mount，跟著 pull 走 |

⛔ 用 `--content-only` 的後果就是 2026-08-02 那次事故的形狀：**新內容 + 舊映像**。

---

### ☐ 0-③ ⚠️ 有 4 個檔的 lane 工作**還沒 commit** —— 現在 deploy 會出貨「被 owner 推翻的那個版本」

🔬 `git status` 量到（⛔ 我沒有動它們，那是別的 lane 的）：

```
 M apps/client/src/ui/platform/RallyConfirmDialog.tsx   (+97)
 M apps/client/src/ui/platform/lobbyRally.ts            (+101)
 M apps/platform/internal/room/rally.go                 (+72)
 M content/config/lobby-rally.json                      (opt-in → opt-out)
?? apps/client/src/ui/platform/userIdle.ts              (未追蹤)
```

**為什麼這件事會咬人**：已 commit 的 `15877dff` 做的是 **opt-in**（跳窗 → 按「同意」→ 進房），
而 owner 2026-08-21 當天就推翻了它：

> 「你說的是對的，**預設是加入，五秒是讓人按否定的**」

⇒ **不修玩家會遇到什麼**：建房廣播跳出來，**5 秒**內要主動按「同意」才進房。
5 秒對「主動點同意」太短 ⇒ 幾乎沒有人會加入 ⇒ owner 最在意的「**創建房間最重要的就是拉人進來**」
出貨當天就等於沒做，而畫面上看起來完全正常（視窗真的有跳）。

⭐ 而且未追蹤的 `userIdle.ts` 如果沒 `git add`，就是 **2026-08-02 事故的形狀**
（來源檔沒進版控，產物卻被 commit）。

**✅ 要勾的動作**：**先把這條 lane 收完並 commit（含 `userIdle.ts`），再 code-cut**。
⛔ 不要在這個狀態按 deploy。

---

## 🆕 §A — 這一輪（2026-08-20 起）新開、仍然開著的 10 張

⭐ 這一區就是部署協定第 2 步真正要 owner 勾的東西。
「已落地？」欄是 🔬 量到的（`git log --grep`），⛔ 不是憑印象。

| 勾 | # | 是什麼 | 已落地？ | 多嚴重 | 修起來多大 | ⛔ 不修玩家會遇到什麼 |
|---|---:|---|---|---|---|---|
| ☐ | **492** | 大廳集合令的**語意反轉**：opt-in → **opt-out**（倒數結束＝加入） | ⚠️ **工作樹裡，未 commit**（§0-③） | 🔴 **高** | 已寫完，只差 commit | **5 秒內沒人來得及按「同意」⇒ 建了房還是一個人打**。功能看起來有，實際等於沒有 |
| ☐ | **499** | 所有帳號預設與管理員成為好友（新帳號自動 + 既有 198 個回填） | ⛔ **零 commit** | 🟡 中 | 中（接線 + 一次性回填 + 後台一格 `adminAccountId`） | 玩家的好友列表**永遠是空的**，第一次進大廳沒有任何人可以互動；owner 也沒有一條能主動找到玩家的路 |
| ☐ | **494** | 殭屍掉小金幣 → 停 1 秒 → 貝茲加速吸回擊殺者 + 音階 | ✅ `274945b0` | 🟢 低（爽度） | 已做 | （已落地）⇒ 只要確認在 release note 上 |
| ☐ | **495** | Quick Approval 收攏常用批核（清理變身態 · 邀請碼 · 上下架） | ✅ `375660ac` | 🟡 中（營運） | 已做 | （已落地）⛔ 但注意：它只是把 §0-① 那顆按鈕搬過來，**不改變 §0-① 的結論** |
| ☐ | **496** | 後台「戰鬥回放」連結指向 `localhost` | ✅ `cd348023` | 🟡 中（只有 owner 看得到） | 已做 | （已落地） |
| ☐ | **497** | 上下架／免費解鎖名單只印 id，看不出是誰、變身態沒標 | ✅ `98902435` | 🟡 中（只有 owner 看得到） | 已做 | （已落地）⭐ 這一張正是 §0-① 那 10 個變身態**當初沒被發現**的原因 |
| ☐ | **498** | 錄影保留天數：出貨在刪（30 天），owner 要**預設不刪** | ✅ `cd348023` | 🟡 中 | 已做 | （已落地）⚠️ 不修的話**30 天前的對戰錄影會被自動刪掉**，而那是唯一的覆盤資料 |
| ☐ | **493** | 後台左選單切換後右側沒回頂端、超過一螢幕的內容被吃掉 | ✅ `4f70d015` | 🟡 中（只有 owner 看得到） | 已做 | （已落地） |
| ☐ | **491** | 創建房間頁底下那個「一堆 id」的下拉 | ✅ `dfa09b66` | 🟢 低 | 已做 | （已落地）它從來沒把選擇送到任何地方 ＝ 純困惑源 |
| ☐ | **484** | 逐則對票自動化（owner 每一句話都要對到票） | ✅ `1956db25` + `4b3e079a` + `1438c1d7` | 🟢 低（流程） | 已做 | （已落地）玩家看不到；但它是「owner 講的話不會消失」的閘 |

**⭐ §A 的結論（一句話）**：這一輪 10 張裡 **8 張已經落地只是沒關票**，
**真正需要 owner 決定的只有兩張** ——
**#492（已寫完，等 commit）** 與 **#499（零行程式碼，要不要併進這一版）**。

---

## 👀 §B — 舊票裡「玩家第一場就會遇到」（📄 從票面判讀，⛔ 未逐張復驗）

⚠️ 這一區**不是**這一輪開的，所以照第零守則⑧ 它們本來就在 owner 的排序權裡。
放在這裡只為了回答部署協定第 2 步的另外半句：「**確認沒有未修的 T0 / major bug**」。

| 勾 | # | 是什麼 | ⛔ 不修玩家會遇到什麼 |
|---|---:|---|---|
| ☐ | **329** | 22/26 對變身英雄壞掉：變身態技能是 w3x 殘骸、或本體根本沒有變身技 | 選了會變身的英雄 → 大招按下去要嘛沒反應、要嘛變成一個技能欄全是亂碼的身體 |
| ☐ | **243** | 46 支技能完全沒有任何 effect | 按下去**模擬器什麼都不會發生**：法力扣了、冷卻轉了、傷害是 0 |
| ☐ | **280** | 指定友軍的技能放不出去 —— **10 支已上架**的補血/增益技是死的 | 治療型英雄按 W 治隊友，指令直接被拒，什麼都沒發生 |
| ☐ | **373** | 5 支【天生技·主動】整支只有 spawnVfx | 卡片寫「嘲諷／隱形／偵查／隨機道具」，遊戲裡**一個數字都不會動**（只有特效） |
| ☐ | **404** | 70-04 千年練成卡片寫「招喚樹精 4/6/8 棵」 | **一棵都沒召喚**。大招放完場上什麼都沒多 |
| ☐ | **459** | 48-03 鮮血神殿只剩一發單體傷害 · 13-02 的 hpPct 沒填 | 大招的一半機制不存在，而卡片照樣印著 |
| ☐ | **250** | 克勞德：凶斬沒有真的飛過去 + **超究武神霸斬完全沒作用** | 招牌大招按下去＝零效果 |
| ☐ | **223** | 拳四郎變身後**兩個模型同時渲染** | 變身完場上出現一個穿模的雙身怪物 |
| ☐ | **34** | 黑崎一護 glb 同時畫著常態與卍解兩套身體 | 同上，一直都是雙身 |
| ☐ | **422** | 中場兩支隊伍疊在同一個出生點 | 中場休息時 12 具身體擠在 6 格互相穿模 |
| ☐ | **183** | 死亡觀戰視角整個畫面**全黑** | 一死掉就看不到任何東西，直到重生 |
| ☐ | **185** | 敵方魔力顯示**負數**（-344 / 825） | 血魔條上直接印一個負數 |
| ☐ | **184** | PRISMATIC 三選一**偶發空盤**（標題在、卡片零張） | 三選一畫面跳出來但一張卡都沒有，只能等它過去 |
| ☐ | **115** | 武器三選一「先抽後濾」 | 白名單一縮，玩家就會拿到**少於三張、甚至零張**的「三選一」 |
| ☐ | **219** | 三個 UI 遮擋：結算評價被「已分出勝負」蓋住等 | 每一場結束時評價被壓在另一塊 UI 底下看不到 |
| ☐ | **421** | 視野遮蔽在 zone 1 等於不存在（`occludeArgs` 永遠拿 zone 0 的牆） | 在第 2 分區打時**牆是透明的**（或該擋的沒擋），兩隊視野規則不一樣 |
| ☐ | **397** | frieren / shiganshina 的 routeSwap 門機關**上架起就是死的** | 那兩張圖的門從來不會開關，路線變化是假的 |
| ☐ | **216 / 21** | 走路/攻擊面向錯誤；揮劍/施法時角色沒有轉向目標 | 角色背對敵人揮空氣，畫面上看起來像沒打中 |
| ☐ | **120** | 鍵盤按 Q/W/E/R **看不到範圍預覽圈**（觸控/滑鼠/手把都有） | 用鍵盤的人放技能全靠猜距離 |
| ☐ | **159 / 160** | 友軍傷害根因未修 + 殺隊友照領 150+100 金與連殺 | **可以打死隊友並領錢**。一局裡有一個人想搗亂就毀了整場 |
| ☐ | **318** | 🔴 35 個位移/擊退效果裡 29 個會穿牆 | ⚠️ **疑似已被今天的 `5ea04900` + #487 修掉** → 見 §F |

---

## 🕑 §C — 「玩家玩幾場才會遇到」（📄 從票面判讀）

| 勾 | # | 是什麼 | ⛔ 不修玩家會遇到什麼 |
|---|---:|---|---|
| ☐ | **225** | 🔴 #82 的 7,500 金頂點路線閘門是 `round≥6`，實打每場只有 5–6 回合 | 商店最貴那條路線**永遠買不到**，玩家存了錢卻沒有東西可買 |
| ☐ | **248** | 每回合硬上限 5 分鐘就出火圈收場 | 打到一半被火圈強制結束，不論場上發生什麼 |
| ☐ | **274** | 回合報告卡硬給 `shelfOpen=true` | 提示「你買得起 4 件」，商店只有 1 個按鈕能按 |
| ☐ | **259 / 228 / 217** | 分紅面板把特殊殭屍標成「殭屍王」· 特殊殭屍不顯示分紅提示 · 特殊殭屍/殭屍王沒有血條 | 打大怪時看不出還剩多少血、也看不出獎金給了誰 |
| ☐ | **224** | 53 位可選只有 42 種模型（26% 共用） | 選了不同英雄卻長一樣 |
| ☐ | **345 / 362** | 七張動漫場地的牆還是水泥灰盒；場景特色太薄 | 換了地圖但看起來都是同一個灰盒子 |
| ☐ | **368** | 商店與英靈殿的模型尺寸/站姿（多拉A夢巨大支、下半身傾斜） | 商店裡的英雄比例明顯壞掉 |
| ☐ | **168** | 英靈殿輪播仍會抽到**變身態** | ⭐ 與 §0-① **同源** —— 白名單清乾淨後仍要確認這一條 |
| ☐ | **229** | 環狀柱子地圖開場有一堆奇怪天空火焰 | 開場畫面被干擾 |
| ☐ | **199 / 109** | 手機不顯示操作說明；手機/沙發 HUD 技能被拒時不說原因 | 手機玩家不知道為什麼技能放不出去 |
| ☐ | **119** | 按下技能後冷卻圈要等一趟 RTT 才開始轉 | 手感黏，連按會誤判成沒放出去 |
| ☐ | **330** | 技能點要手動加，而「沒加點」按下去幾乎沒有回饋 | 新玩家整場技能都是 0 級而不自知 |

---

## 🔐 §D — 玩家看不出來，但風險落在 owner 身上（📄 從票面判讀）

| 勾 | # | 是什麼 | ⛔ 不修會怎樣 |
|---|---:|---|---|
| ☐ | **181** | 🔴 **兩個外洩憑證**（管理員帳密已被 commit 進 `docs/legacy`）未輪替，且要裁決要不要清 git 歷史 | 任何拿到 repo 的人可以用管理員身分登入正式站 |
| ☐ | **85 / 84** | `/auth/register` 是未認證的 argon2id CPU+記憶體放大器（無限流）；`X-Real-Ip` 被無條件信任 ⇒ 登入限流可繞過 | 一台機器就能把 platform 打到不能服務 |
| ☐ | **89 / 82** | access token 可用 URL query 帶（會進 nginx access log）；refresh token 存 localStorage | 憑證外洩面 |
| ☐ | **83** | audition/debug HTML 被打包進正式 client 並**公開服務**（已長到 14 頁） | 任何人可以打開內部工具頁 |
| ☐ | **241** | 🔴 Go 消費的 config 接不上 content-overlay | 後台**存了但收費路徑一輩子讀不到** ⇒ owner 改了以為生效，實際沒有 |
| ☐ | **54** | 後台戰鬥系統存過一次之後，改 `combat-env.json` 對線上零影響 | 同上形狀：後台是啞的 |
| ☐ | **91 / 92** | game-server 的 contentBus 沒訂 content-overlay；client 的 bootContent 沒包 OverlayContentSource | 後台改內容仍須**重啟容器**才生效 |

---

## 🧱 §E — 玩家永遠看不出來（內部債／文件／測試／工具）

⛔ **刻意不逐張列**（第零守則：預設不查）。數字如下，代表例各一：

| 桶 | 張數 | 代表 |
|---|---:|---|
| `docs-triage` 標籤（2026-07-28 一次性倒進來的稽核債） | **142** | #105 需求狀態頁落後任務帳本 83 筆 |
| 引擎缺原語（DoT / summon / 擊退 / 無敵…） | ~12 | #55 · #57 · #58 · #59 · #140 · #141 |
| 特效／模型/貼圖資產債 | ~25 | #62 · #63 · #64 · #65 · #439 |
| 語音債 | ~12 | #74 · #75 · #77 · #79 · #441 |
| 產生器/文件/守衛自身的漂移 | ~15 | #378 · #381 · #389 · #427 |

⚠️ **這一桶的正確處理不是「今天修」，是「不要再長」** —— 它們全部早於這一輪。

---

## 🧹 §F — 🔬 疑似**已經修好但票還開著** ⇒ 建議 deploy 前確認並關掉

⭐ **為什麼這一區重要**：release note 的「逐句對票」表如果把已修的東西列成待辦，
owner 讀到的就是一份說謊的進度表 —— 而那正是 2026-08-19 他抓到的那個問題。

| 勾 | # | 票說什麼 | 🔬 我量到的 |
|---|---:|---|---|
| ☐ | **491–498** | 8 張這一輪的票 | **每一張都有今天的 commit**（見 §A 的「已落地？」欄）⇒ 建議**關票**，⛔ 不要留在待辦欄 |
| ☐ | **238** | `formAwareModelResolve.test.ts` 的 import 路徑不存在，讓全 repo typecheck EXIT=2 | **那個檔案現在存在**（`apps/client/src/render/views/formAwareModelResolve.test.ts`）⇒ 票面前提已不成立 |
| ☐ | **318** | 🔴 29/35 位移效果會穿牆 | 今天有 `5ea04900`「位移的終點必須落在牆的**這一邊**」+ #487（已關）+ #490 飛行例外 ⇒ **很可能已被取代**，⛔ 但我沒有重跑那 35 個效果去證實 |
| ☐ | **469** | 隱藏英雄名單是空的 | 今天有 `fcd51d7c`「隱藏英雄名單四位落地」⇒ 疑似可關 |
| ☐ | **472** | 稽核範圍收斂到「上架中」 | ⭐ **⛔ 不要關** —— 它的 owner 裁決區塊是 §0-① 的唯一書面來源 |

---

## 🧪 方法與限制（⚠️ 第三守則：我自己的宣稱也要能被反駁）

**我做了什麼**
- `gh issue list --state open --limit 400`（唯讀）→ 329 張，全部讀過標題
- 這一輪 10 張 + #472 / #486 / #485：讀了完整 body 與 owner 裁決區塊
- 🔬 真的讀了程式碼／檔案的四處：
  `data/curation/whitelist.json`（49）· `.gitignore:34`（`/data/**`）·
  `apps/platform/internal/curation/transformevict.go`（`WhitelistGate.Evict`，預設 on，在 `Load`/`Save` 兩處）·
  `apps/game-server/src/curation/whitelist.ts:132`（`isTransformedBody(id) → false`）·
  `docker/compose.yaml`（platform 掛 `../content:/srv/content:ro`，`CONTENT_DIR=/srv/content`）
- `git status` / `git log --grep="#N"` 對每一張新票

**我沒做什麼（⛔ 刻意）**
- ⛔ 沒有 ssh／curl 到 `ggd.adms.ai`（守則：不在正式站上測）⇒ 「線上白名單 59」這一項是**引用 #472 的紀錄**，⛔ 不是我今天量的
- ⛔ 沒跑 `pnpm test` / `pnpm typecheck` —— 工作樹裡有**別的 lane 未 commit 的改動**，這時候跑出來的紅綠不指向任何人
- ⛔ 沒有逐張復驗 §B–§E 的 319 張舊票（第零守則：預設不查）

**這份表最可能錯在哪**
1. §B–§E 是**從標題判讀**的。像 #238 / #318 那樣「票還開著但問題已經沒了」的情況**已經被證實會發生** ⇒ 勾任何一張之前先確認前提還在
2. §0-① 的「線上 59」是**引用**的數字。deploy 完第一件事就是去讀 `/api/v1/curation/whitelist`，那才是量到的
