# GH#579 —— 回合結算後變身沒有還原（Lane 2 完整報告）

日期 2026-08-23 · 分支 `main` · 逐檔柵欄：`MatchController.ts` / 新測試 / （`ChampionFormSystem.ts` 未動）

---

## ① 複驗票的三件事（⭐ 全部成立，⛔ 但測試落點是錯的）

| 票說 | 我量到的 | 結論 |
|---|---|---|
| `endCombatChampionForms()` 全 repo 零呼叫者 | `grep -rn endCombatChampionForms packages apps tools` → 只有**定義**（`ChampionFormSystem.ts:405`）、**它自己的單元測試**（`championForm.test.ts:40,342,352,362`）、**一句註解**（`auraCarrier.ts:64`）。`apps/game-server` **一次都沒有** | ✅ 成立 |
| `concludeCombat` 叫了 7 支 `endCombat*` | `MatchController.ts:3005-3011` flowers / revives / fireRing / guardians / coins / deathWards / mobs —— 就是沒有這一支 | ✅ 成立（行號也對） |
| 它冪等 | `for (const id of [...world.championForm.keys()]) revertToBaseForm(world, id);` —— 空表就是 no-op | ✅ 成立 |

⭐ 出貨碼 `ChampionFormSystem.ts:394-403` 自己逐字寫著「a form is a WITHIN-ROUND state … cannot
leak across the intermission and into a fresh duel」與「the MATCH HOST calls it on combat exit」——
**兩句在這次改動之前都是假的**（第三守則）。

## ② 改了什麼

`apps/game-server/src/match/MatchController.ts`

- `:186` 新增 import（`@ggd/shared/sim/systems/ChampionFormSystem`）
- `:3012-3020` 在 `concludeCombat` 的 `endCombat*` 那一叢**最後**加 `endCombatChampionForms(this.world);`
  - ⭐ 在 `concludeCombat` 而**不是** `enterIntermission`：後者會被 `skipPhase` 作弊與 fault failsafe 跳過
  - ⭐ 順序仍在 `restoreForNextRound` **之前**（本來就是）—— 變回本體會改 `maxHp`，回滿要拿還原後那個數字

⛔ `ChampionFormSystem.ts` **一個字都沒動**：那段檔頭在這一行落地之後就變成真的了，改它反而是把
「它曾經說謊」這個事實抹掉。守衛的檔頭把這件事記下來了。

## ③ 守衛 + 突變

新增 **`apps/game-server/src/match/championFormRoundBoundary.test.ts`**（1 個 `it`）：
出貨 `MatchController`（12 bots，`combatMaxTicks: 30` 短到沒有人會死）→ 取一個活著的席位 →
接一對 `transform` 上去（骨架內容一對都沒有，與 `championForm.test.ts` 的夾具同法）→
出貨 `applyChampionForm(..., "toggle", undefined, ...)` ⇒ `expiresTick === FORM_NEVER_EXPIRES` →
推到 `resolution` → 斷言 **`hp.alive === true`**（⭐ 夾具的意義：清除**不可能**是死亡那條路做的）
→ `world.championForm.size === 0` 且 `world.champion.get(id).championId === baseId` →
再推到 `intermission` 重驗一次。⛔ 全部讀最終物件，⛔ 不掃原始碼字串。

**突變**（`MatchController.ts` 的那一行換成 `// MUTATION`）：

```
FAIL  championFormRoundBoundary.test.ts
AssertionError: expected 1 to be +0     ← world.championForm.size
  70|     expect(ctl.world.championForm.size).toBe(0);
```

⇒ 🔴 紅。改回來（用 `Edit`／`python` 就地換字串，⛔ 不是 `git checkout`）。

### ⛔ 票裡的測試落點是**錯的**，我改了 —— 這是與票不符的地方

票寫 `packages/shared/src/sim/championFormRoundBoundary.test.ts`。我照著放，`vitest` **綠**，
但 `tsc` 紅 **12 個 TS6059**：

```
src/sim/championFormRoundBoundary.test.ts(18,48): error TS6059:
  File '…/apps/game-server/src/match/MatchController.ts' is not under 'rootDir' '…/packages/shared'
（連帶把 Seat / Tier0Brain / whitelist / ownership / PhaseMachine / PairedDuels … 全部拖進來一起紅）
```

`packages/shared/tsconfig.json` 的 `rootDir: "."`＋`include: ["src"]` ⇒ 那裡放不下一個 import
`apps/` 的檔；而且方向本來就反了（`@ggd/shared` 是被 game-server 依賴的那一半）。
⇒ 落在票自己提供的第二個選項那一側：**`apps/game-server/src/match/`**（`intermissionRestore.test.ts`
的鄰居，同一個形狀）。⚠️ 這正是 CLAUDE.md 那條「`pnpm test` 綠而 `pnpm typecheck` 紅」的陷阱 ——
只跑 vitest 會把它放過。

## ④ 離開碼

| 指令 | |
|---|---|
| `npx vitest run` × 5 支（新守衛 + intermissionRestore + roundResetPools + roundEnd + roundTeardown） | **11 passed / 0 failed** |
| `cd packages/shared && npx tsc --noEmit` | **EXIT 0** |
| `cd apps/game-server && npx tsc --noEmit` | **EXIT 0** |

⚠️ 第一次跑 5 支時 esbuild 噴 `The service was stopped`（4 個 suite 收集失敗）——
那是**併行 lane 把 esbuild service 打斷**的基礎設施雜訊，重跑即全綠，⛔ 不是這次改動。

---

## ⑤ ⭐ owner 的問題：「其他變身態有沒有機會落日、替換 3D model 特效部分」

> owner 2026-08-22：「變身帶來許多問題，因此我想要開啟變身態盡可能下架」
> owner：「還有其他變身態也請你分析建議做法是否有機會落日替換 3d model 特效部分」

⛔ **我沒有下架任何一個，也沒有動任何內容檔。** 下面是盤點與建議，勾選權在 owner。

### 量到的規模

| | |
|---|---|
| 帶 `transform` 連結的英雄文件 | **40 份**（＝ 20 對） |
| 真的有技能觸發得到的對 | **19 對**（`godie-e007 ↔ godie-ewar` 天地志狼**沒有任何 championForm 效果**＝已經是死的連結） |
| 帶 `kind: "championForm"` 的文件 | 34 份（去掉英雄內嵌鏡像後 = **23 支技能**） |
| 其中 `{"to":"toggle"}` 且**沒有** `durationSec`（＝永不到期，正是這次的受害者） | **8 處**，住 `godie-e00s.passive` · `godie-e00l.w` · `godie-e002.w`(×2) · `godie-e010.passive` ＋ 英雄內嵌鏡像。⭐ **兩支是 passive**，玩家連「再按一次切回來」都沒有 |

### ⭐ 結論先講：**19 對裡有 15 對的 3D model 完全一樣**

| 英雄 | 本體 modelKey | 變身態 modelKey | 同？ | 除了屬性/技能組以外的視覺差 | 建議 |
|---|---|---|---|---|---|
| 蟬在叫人壞掉 - 龍宮禮奈 `e001→e00n` | `imported.renaryugu2` | `imported.renaryugu2` | ✅ 同 | 無 | **可落日**：純屬性＋技能組 |
| 亞瑟王 - Saber `e002↔e00l` | `imported.herosaber` | `imported.herosaber` | ✅ 同 | 無（屬性**完全一樣**，只有技能組換） | **可落日**（⚠️ 這一對是 toggle 永不到期，正是本票的受害者） |
| 白木老樹精 - 白木卡迪那 `e00s↔e010` | `champ.sela`（骨架佔位） | `champ.sela` | ✅ 同 | `bodyScale 1.1→(無)`、`immobile → true`（紮根） | **可落日**：immobile 是狀態，bodyScale 是一格修飾 |
| 神鳴流劍士 - 櫻綻剎那 `e00w→e00x` | `imported.mfls` | `imported.mfls` | ✅ 同 | 無 | **可落日** |
| 開外掛的死神 - 黑崎一護 `h01n→h01o` | `imported.heroichigo` | `imported.heroichigo` | ✅ 同 | 無 | **可落日** |
| 看似憂鬱的神獸 - 草泥馬 `h02v→h02u` | `imported.horse` | `imported.horse` | ✅ 同 | 無（bodyScale 兩邊都 0.85） | **可落日** |
| 種子神奇寶貝 - 妙蛙種子 `hgam→h02r` | `imported.bulbasaur` | `imported.bulbasaur` | ✅ 同 | `bodyScale 0.62→(無)` ⇒ 變身瞬間**跳大 1.6 倍**（疑似漏填） | **可落日**（順便修那格 scale） |
| 黑魔導士 - 莉娜因巴斯 `hjai→h020` | `imported.linainvers` | `imported.linainvers` | ✅ 同 | 無（差別是 `maxMana 100→5000`、`manaRegen 0.1→1000`） | **可落日**：這一對根本就是一個 buff |
| 黑暗福音 - 依文潔琳 `n003→n01g` | `imported.long` | `imported.long` | ✅ 同 | 無 | **可落日** |
| 傳說的龍騎士 - 勇者小呆 `n01c↔nbbc` | `imported.sd2` | `imported.sd2` | ✅ 同 | 無（只有 `mr 50→28`） | **可落日** |
| 賽亞人 - 悟空 `ogrh→o00x` | `imported.goku` | `imported.goku` | ✅ 同 | 無 | **可落日**（⚠️ 超級賽亞人「看起來要不一樣」是**特效**的事，不是身體的事） |
| 電車癡漢 - 臭作 `orkn→o030` | `champ.sela`（骨架佔位） | `champ.sela` | ✅ 同 | `bodyScale (無)→3` ⇒ **變成 3 倍大**，這是真的看得見 | **可落日**：3 倍大做成一格 scale 修飾就好 |
| 草帽小子 - 蒙其.D.魯夫 `u00n→u00o` | `imported.luffe` | `imported.luffe` | ✅ 同 | 無 | **可落日** |
| 三刀流劍士 - 索隆 `u01u↔udre` | `imported.heromusashimiyamoto` | 同 | ✅ 同 | 無（`armor 15→0`、`mr 50→28`） | **可落日** |
| 邪眼師 - 飛影 `uvng→u010` | `imported.herohehi` | `imported.herohehi` | ✅ 同 | 無 | **可落日** |
| **妖狐藏馬 - 南野秀一** `nsjs→n00p` | `imported.fox2` | **`imported.fox`** | ❌ 換 | 連 `attackType melee→ranged` 都換 | ⚠️ **真的換身體**：留著，或需要「只換 model 不換 champion doc」的機制 |
| **神奇寶貝兒 - 皮卡丘** `ofar→o02l` | `imported.heropikachu` | **`imported.picacugy`** | ❌ 換 | `attackType ranged→melee`、`bodyScale 0.6→(無)` | ⚠️ **真的換身體**（雷丘） |
| **職業獵人 - 傑 富力士** `ucrl→u034` | **`champ.thorne`（骨架佔位＝本體根本沒有模型）** | `imported.herobiggon` | ❌ 換 | tint 綠→灰 | ⚠️ 這一對的「換模型」其實是**本體缺模型**造成的，先補本體模型再談落日 |
| **北斗神拳掌門人 - 拳四郎** `umal→u00l` | `champ.skin.barbarian`（體素皮膚） | **`imported.heropikachu`** | ❌ 換 | `bodyScale 1.65→(無)` | 🔴 **疑似資料錯誤**：拳四郎的變身態指到**皮卡丘的模型**。⛔ 不要照這個結論落日，先確認 |

### ⭐ 建議做法（⛔ 我沒有挑，這是三條路）

1. **15 對「model 相同」的 ——「效果狀態＋增益狀態」替換是對的方向（#552 那條路）**，
   但 ⚠️ **它不是純 buff**：這 15 對每一對的 **Q/W/E/R 四格＋passive 全部換成另一組 ability id**。
   ⇒ 落日之前引擎要先有「**技能組覆寫**」這個機制（一個狀態掛著、把某幾格換成另一支技能），
   ⛔ 不是靠既有的屬性 modifier 就能表達。⭐ 這正是第〇·五守則的形狀：**先做那一個機制，
   15 對一起解鎖**，⛔ 不要一對一對改。
   ⭐ 好消息：`whileForm` 形態閘（`ability.ts:667`）已經存在，只要把它的判斷從 `championForm`
   改讀狀態，被動那一半就跟著搬過去。
2. **4 對「真的換 model」的** —— 要嘛留著變身機制，要嘛做一個「**只換 modelKey / bodyScale /
   tint，不換 champion doc**」的輕量機制（那正是 owner 說的「替換 3d model 特效部分」）。
   ⭐ 這比整個 champion 換身體便宜非常多，而且它**不會**再產生本票這種
   「屬性/技能組跨回合洩漏」的缺陷（洩漏的只剩一個外觀欄位）。
3. **`godie-e007 ↔ godie-ewar`（天地志狼）已經是死連結** —— 沒有任何技能觸發得到它。
   ⭐ 這一對可以**直接落日**，成本是零。

⚠️ 順帶：`godie-hgam`（0.62→無）、`godie-ofar`（0.6→無）、`godie-umal`（1.65→無）、
`godie-e00s`（1.1→無）四處的 `bodyScale` 在變身態**沒有重填**，所以變身瞬間體型會跳回 1.0。
⛔ 我沒有改（那是內容，不在這條 lane 的柵欄裡）—— 值得單獨開一張票。
