# EX 空殼稽核與落地（09-002 · 20-002）— lane EX，2026-08-24

## 結論一行

**20-002 不是空殼**（交辦時的量測過期）；**09-002 才是真缺**，缺的是「龜派氣功額外附加 250% [AP]」那一半 —— 已用既有 `ability@1.augment` 機制落地，⛔ 零新程式、零新機制。

## 20-002 解放.約束勝利劍MAX（godie-e002.ex / godie-e00l.ex）

交辦單寫「effects 都是 0，只有 vfxKey」—— `effects: []` 是真的，但它是**被動技**：
完整實作住在 `passive.ranks[0].hooks`（`onReflectSuccess` → screenFlash/shake →
`delayed count:7` 每刀 `incomingPct 7×, maxChainDepth:1` → `finalEffects` damageLine
（[現存魔力]+[AP]）×7）。兩份 mirror 內容一致（parity 指紋 `df99b1906c16f286` 兩邊相同）。

規格（`godie-e002.ex.json` description，provenance **owner-spec**，逐字）：

> 「永恆的理想鄉」[反彈]成功時發動，給予敵人連續七次斬擊，每次造成7倍[反彈]傷害；
> 最後施展「約束與勝利之劍」，對[前方][直線]敵人造成（[現存魔力]+[AP]）×7倍傷害。

缺的只有**出貨行為守衛**（既有守衛全是夾具層 —— 失敗形態⑤），已補（見下）。

## 09-002 十倍龜派氣功（godie-ogrh.ex / godie-o00x.ex）

### 規格（第〇·六階梯：owner 新版說明**不存在**，最高可得層 = w3x 卡面文案，逐字）

> 在超級賽亞人三的狀態下，可獲得額外**25%攻擊力加成**(無法與其他靈氣加疊)，
> 使出龜派氣功將**額外附加250% [AP]點傷害**(無法與超級賽亞人三附加傷害加疊)。

（250% 是 `docs/技能AP換算計畫.md` 已裁定的換算：w3x 原式 `力量×10` → `250% [AP]`，
兩份 mirror 該表都有列。）

### 交辦時的現況

- `godie-ogrh.ex`：`effects: []`＋`template tpl-buff-self`（ad +35%/6s 佔位樣板）
- `godie-o00x.ex`：inline applyBuff ad +35%/6s
- **「250% [AP] 附加在龜派氣功」整條不存在** —— 卡面說了但不會發生（第一·五守則）
- 佐證 0.35/6s 是樣板不是設計：`godie-hvsh.ex`、`godie-u00h.ex` 等未完工 EX 全是
  **同一組參數**的 tpl-buff-self。

### 落地（純內容，零程式）

兩份 mirror 改成**逐位元一致**（modulo id；已 diff 驗證）：

1. `effects`: applyBuff `ad pctAdd 0.25`、6s（inline，ogrh 側移除 template ref）
2. **新增 `augment`**（本 repo 第一份帶 augment 的出貨文件；機制 `ability-augment@1`
   四個面 2026-08-10 已全接上）：
   `targets:[{abilityId:"godie-<form>.r", ops:[{op:"damageCoeffAp", mode:"add", value:2.5, nodeKind:"damage"}]}]`
   —— 各指向**自己形態**的 09-04 龜派氣功；EX 解鎖（rank>0）即生效，
   `applyAugmentToEffects` 深走訪 `spawnProjectile.onHit` 把 ap coeff 0.5→3.0。
3. 跑過 `bash scripts/genrun.sh apconv:build`（genguard 指示的正規化器）—— 無額外改寫，check 綠。

### ⭐ 我推的（引用不到原話，逐項標明；rollback 見下）

| 決定 | 依據 | 引用得到嗎 |
|---|---|---|
| 附加 +2.5 AP coeff（add，不設條件、無時窗） | 卡面「使出龜派氣功將額外附加250% [AP]」無時窗描述 | ✅ 卡面 |
| buff 0.35→**0.25** | 卡面「額外25%攻擊力加成」；0.35 是無出處樣板 | ✅ 卡面 |
| buff 持續 6s、掛在 EX 主動施放上 | 卡面把 25% 綁在「超級賽亞人三狀態」—— GGD 沒有 SSJ3 形態，此件**不可表達**；保守沿用既有 6s 主動 buff 形狀 | ⛔ **我推的** |
| 「無法加疊」兩個括號子句未實作 | GGD 無 SSJ3 亦無第二個同名靈氣來源 —— 出貨設定下**空集合**（同 第一·五守則出路②） | ⛔ **我推的** |
| nodeKind:"damage"（只打傷害節點） | R 只有一個 damage 節點；收窄防誤傷 | ⛔ 我推的（可觀測等價） |

### Rollback（不必改程式）

- **EX 上架開關**：`learnEx` 不解鎖 = 整支 EX（含 augment，`collectAugmentOps` 檢查 rank>0）不存在
- augment/buff 全是 content JSON 欄位，後台/編輯器可調；刪 `augment` 區塊即回舊行為

## 守衛（靈魂層，一批一條突變）

`packages/shared/src/sim/exAbilitiesShipped.test.ts` —— 全部出貨內容＋真 `SimWorld.step()`，斷言讀血條：

- **①** 兩個形態：學 EX 的悟空放 R（skillshot＋真投射物），受害者掉血 **>** 沒學 EX 的同場（含控制組 lostPlain>0）
- **②** Saber 學 EX＋出貨 Avalon buff，敵人打魔傷 → 反彈成功後攻擊者在**≥3 個不同 tick** 繼續中刀（七連斬 delayed 序列真的落地）

**突變**（真的跑過）：移除 `godie-ogrh.ex.json` 的 `augment` 區塊 → 紅：
`expected 689.9729745231207 to be greater than 689.9729745231207`（兩場逐位元相同）→ 還原 → 綠。

## 額度帳

vitest 3 次（初跑綠 / 突變紅 / parity 重吸收）· tsc 1 次（EXIT=0）· 突變 1 條。

## 事件與未盡事項（主 session 注意）

1. **parity 基準線被別的 lane 蓋回**：commit `475a92cf` 重生成基準時把我的 09-002
   指紋回捲（他們讀到的樹較舊）；我重跑 `abilityCodeParityForms.test.ts` 讓 H5
   自動吸收（diff 僅 09-002 一列），基準線隨本批 commit。
2. **`pnpm skills:sync` 未跑**（lane 禁令）—— prose/契約/tiers 衍生文件等主 session 統一跑。
3. 既有 `prose-number-unbound`：卡面「0秒冷卻時間」vs `cooldown:[60]`（兩份 mirror 皆然，
   normalize 計畫已列管）—— 不在本批範圍，未動。
4. o00x/ogrh 的 EX 附掛 `cooldownTier:"極大"` 對應 60s —— 原樣保留。
