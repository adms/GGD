# MSG3 lane — 「訊息指名產生器」閘擴到兩塊宣告過掃不到的域

2026-08-25 20:00 ｜ 完整報告（摘要在回傳值）

## 做了什麼

### ① 既有閘擴域：`content/{items,augments,status-effects}` 收進 PATH_RE

檔：`packages/shared/src/ops/guardMessagesNameTheGenerator.test.ts`

- `PATH_RE` 從三目錄擴成六目錄。判定仍**逐檔問 sync-io**（`steps[].writes`），
  ⛔ 沒抄任何清單 —— grail:wishes 哪天多寫一份，閘自動跟上；
  手編檔（augments 32 份、items/status-effects 除 `_index.json` 外全部）解析成 null ⇒ 天然跳過。
- 檔頭判準②改寫（收錄理由：content/augments 60/92 是 grail:wishes 的產物）、
  「已知的漏」拿掉「這一版不掃」那一行，改指向新閘。
- **擴域後新抓到的違規：0。** 事前用 python 過近似掃描驗證過：全 repo *.test.ts 裡
  提到這三個目錄之產物路徑的只有兩處 ——
  `grailWishesFresh.test.ts`（自帶 genguard 字樣 ⇒ named）、
  `apps/admin/src/content.test.ts:90`（fixture 物件的 key ＝ 程式碼位置，⛔ 不是訊息 ⇒ 本來就不在域裡）。

### ② 新閘：tools/scripts 的 prose 也要指名產生器

新檔：`packages/shared/src/ops/guardProseNamesTheGenerator.test.ts`
新棘輪：`tools/parallel-gates/guard-prose-pending.json`（8 列，只准變短）

- 域：`tools/**/*.{py,mjs,ts}`（⛔ 不含 `*.test.ts` —— 姊妹閘的域）＋ `scripts/*.{sh,py}`。
- 只讀 **prose**：註解（#／//／block comment）、python docstring、print/echo/console/throw 行。
  ⛔ 純程式碼字串（`readJson("content/…")`）不算 —— 校準時它們是誤報大宗
  （engine-atlas 讀檔、mesh_audit 的 glob、sfx-bind 的報表 label）。行級 regex＋狀態機，無 AST。
- 產物判定與姊妹閘同一套 `productOwners()`（sync-io 推導；glob/`<id>`/`${x}`/`{aid}` 佔位
  只在涵蓋的每一個實檔都是產物時命中）。⭐ 補了一個姊妹閘沒有的：glob 擁有者**排序後**
  才寫進列 —— `readdirSync` 順序跟檔案系統走，不排序的話 PENDING 會跨機器假紅。
- 另兩條：genrun 步驟名幽靈檢查（prose 裡 `genrun.sh <step>` 的 step 必須在 sync-io ——
  prose:apply 的前科；現有兩處 `content:build`/`shapes:build` 都是真的）＋
  「請改這個 JSON」祈使句偵測（GUIDE_RE，母體目前 0，留給未來）。

### ③ 首輪抓到 13 支，high 的 5 支當場修（只改字串）

| 檔 | 原句的病 | 修法 |
|---|---|---|
| `tools/w3x-import/build_slash_pitch.py`（2 句 print） | 「vfx-families.json 與量測不一致」不說誰寫；「跑 python3 build_pitch.py」**在隔離區下會吃 PermissionError** | 指名 pitch:build ＋ 改推 `bash scripts/genrun.sh pitch:build` |
| `tools/w3x-import/build_vfx_orient.py`（print） | 同上：指令是對的產生器但過期了（直跑會撞 chmod 444） | 同上 |
| `tools/w3x-import/resolve_unit_tints.py`（docstring） | ⭐ 最貴一句：逐字宣稱 champion docs 是 **hand-maintained**（它們是 skillremake:json/tiers:apply 的產物）| 改寫並補 genguard 指引 |
| `tools/w3x-import/extract_unit_stats.py`（docstring） | 「--apply 直接 PATCH content/champions/*.json」無任何警告 | 補「已是產物、sync 會打回來」警告 |
| `tools/w3x-import/apply_attributes_to_content.py`（docstring） | 「push … into content/champions/*.json」同型 | 同上 |

其餘 8 支進棘輪（多為 w3x-import 一次性 importer 的歷史 docstring 與 reference 註解，
read-only 描述居多，誤導性低）：features.ts · build-champ-names.mjs · build_vfx_bindings.py ·
extract_ex.py · extract_jass_spells.py · gen_ex_content.py · model_audit_61.mjs · strip_teamglow.py。

### ④ NORM lane 檔案：一個都沒碰

`scripts/genguard.sh` 與 `scripts/preserve-before-overwrite.py` 實測都自帶 genguard 字樣
⇒ named ⇒ 本來就不違規，不需要進棘輪。

## 驗證

- vitest 共 3 次（≤3 額度用滿）：①首輪（新閘紅、列 10 句 8 檔；擴域後的姊妹閘綠）
  ②修完＋填棘輪後兩支全綠（8 tests）③突變。
- **突變紀錄**：往 `tools/w3x-import/mesh_audit_report.py` 塞
  `# 改 content/config/stat-caps.json 讓上限一致` → 「新增一律紅」**指名該檔第 4 行
  ＋擁有者 statcaps:build** → 撤掉（diff 已確認歸零）。
- GUARD-THE-GUARD：掃 409 檔、OWNERS>500、產物提及檔 ≥8 —— 掃到 0 個不會靜默變綠。

## 已知的漏（誠實）

- 動態拼路徑（`"content/abilities/" + aid`）與報表資料字串刻意不掃（寧可漏報）。
- named 是逐檔判定：檔裡任何一處 marker 就整檔放行（與姊妹閘同一個取捨）。
- `.test.mjs`（如 tools/deploy-timing/tier.test.mjs）走 prose 閘不走姊妹閘 —— 它的
  expect 訊息只有 print-like/註解那部分被掃到。

## 改動檔案清單（給主 session commit 用）

```
packages/shared/src/ops/guardMessagesNameTheGenerator.test.ts   (M) 擴域＋檔頭
packages/shared/src/ops/guardProseNamesTheGenerator.test.ts     (新) prose 閘
tools/parallel-gates/guard-prose-pending.json                   (新) 棘輪 8 列
tools/w3x-import/build_slash_pitch.py                           (M) 訊息字串
tools/w3x-import/build_vfx_orient.py                            (M) 訊息字串
tools/w3x-import/resolve_unit_tints.py                          (M) docstring
tools/w3x-import/extract_unit_stats.py                          (M) docstring
tools/w3x-import/apply_attributes_to_content.py                 (M) docstring
docs/_reports/MSG3_temp_20260825-2000.md                        (新) 本報告
```

⚠️ 未跑 tsc／全套（lane 約束）；新測試檔 import 面與姊妹閘一致（vitest/node:fs/node:path/node:url），
無 typescript 依賴，型別風險低。
