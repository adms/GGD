# L6 —— #648 迴圈內容批先導（90-00 · 04-02 · 15-00）

**日期** 2026-08-24 · **柵欄** `tools/skill-remake/heroes/*.py`（e00r 除外）·
`content/ability-templates/tpl-periodic-field.json`

## 1. ⭐ 最重要的一個發現：**先導 3 支裡只有 1 支走得到「改產生器來源」這條路**

| 先導 | 檔 | 量到的真相 |
|---|---|---|
| **15-00 真·不死不滅** | `godie-emfr.passive`（產生器 `heroes/godie-emfr.py`） | ⭐ **迴圈早就存在**：`passive.ranks[].hooks` 的 `onInterval` + `internalCooldown: 1.0`（GH#369，`IntervalHookSystem` 發射）。名單把它算進 43 是**掃描器看不見 hook 層**（`shape_axes.json` 的 `passive.hooks: []`、`internalCooldown` 在 ignored）——⛔ 不是機制不存在。⇒ 內容**零改動**，補了缺的那一半守衛（見 §2） |
| **90-00 寄生種子** | `godie-hgam.passive` · `godie-h02r.passive` | ⛔ **無產生器來源**（champion 不在 `batch1.HERO` 的 15 位裡），JSON 是 `apply_tiers.py` 檔頭說的「直接編的 330 份」。⛔ 而 genguard（今晨 `1168d95c`）把它們判給 **tiers:apply** ⇒ Write/Edit 一律 exit 2。⛔ 兩個柵欄（genguard + 本 lane 路徑柵欄不含 `content/abilities/`）都擋 ⇒ **沒有動**，翻譯稿在 §3 |
| **04-02 炸彈陣** | `godie-hjai.w` · `godie-h020.w` | 同上。D10 已歸類：`delayed` + `targetMode:"reresolve"` + `shape:"circle"`（anchor 省略＝釘在落點，正是火柱） |

⚠️ **結構性阻塞（要 main session 裁決）**：43 支裡 **39 支**是直接編的 JSON，而
`sync-io.json` 量寫入時 `force-writes.mjs` 的擾動讓 `tiers:apply`（就地正規化器，
reads≈writes 401 檔）**認領了全部** ⇒ genguard 把「直接編」這條**唯一合法路**也封了。
出路三選一：① 豁免就地正規化器的 ownership（tiers:apply 只動級距欄，手改 effects 不會被打回）
② 給這一批開自己的產生器（§4 推薦）③ `GGD_GENGUARD_OFF`（⛔ lane 規則禁止，我沒用）。

## 2. 15-00 的守衛（真 SimWorld，⛔ 不是參數存在）

`packages/shared/src/sim/abilities/emfrPeriodicPulse.test.ts`（新檔，1 條 it）：
出貨內容載入 → 真 SimWorld 血量壓到門檻下跑 5 個週期 → **逐 tick 數魔力往下掉的跳**：
① N 個週期 N 跳（允許相位 +1）② ⭐ 承重：**跳距等距且逐字等於出貨 `internalCooldown`**。
期望值全部從出貨 JSON 推導（第二守則：0 個字面值）。
既有 `emfrThresholdPassive.test.ts` 只驗門檻 —— ICD 整段拿掉它照樣綠；這一條補的正是那半。

**突變（一批一條，驗過）**：`hooks.ts` ICD 閘 `world.tick - last < icdTicks` → `< 0`
⇒ `expected 15 to be less than or equal to 6` 紅。已用 Edit 還原。
**測試預算**：vitest 2 次（綠 1 + 突變 1）· tsc 0（純新增測試檔，vitest 已編譯它）。

## 3. 90-00 / 04-02 的翻譯稿（機制已驗有家，等 §1 阻塞解開就能落）

```jsonc
// 04-02 炸彈陣（hjai.w · h020.w）：火柱每秒重算圈內，5 秒 5 發
{ "kind": "delayed", "shape": "circle", "radiusTier": "中", "targetMode": "reresolve",
  "delaySec": 1, "count": 5, "intervalSec": 1,        // count=durationSec/intervalSec
  "effects": [ { "kind": "damage", "damageType": "magic", "amount": { "damageTier": "小", "ratios": [{ "stat": "ap", "coeff": 0.6 }] } } ] }
// 「火柱消散之後仍持續受到傷害」那一半 → finalEffects 裡掛 dot
// 90-00 寄生種子（hgam/h02r.passive；18-02 n00p/nsjs.w 同型）：
{ "kind": "dot", "damageType": "magic", "amountPerTick": { "damageTier": "極小", "ratios": [{ "stat": "ap", "coeff": 0.6 }] },
  "intervalSec": 1, "durationSec": 5 }
// ⚠️「吸收生命回饋給施法者」dot 沒有 leech 格 —— 缺口：要嘛 dot 加 healCasterPct 一格，
//    要嘛描述改為只講真的會發生的事（第一·五守則的三條出路，留給裁決）
```

⚠️ 說明裡的 55/10 是 w3x 字面值 —— 批次落地時要照第〇·四換 `{{dmg}}`+級距，⛔ 不是抄數字。

## 4. 其餘 40 支的機械化評估（任務要的答案：**能 script 化，但不是 40 支全部**）

| 群 | 支數 | 怎麼做 | script 化程度 |
|---|---:|---|---|
| dot 型（每秒燒/獻祭） | ~19 | 一顆 `dot` 節點 | ⭐ **高**：「每秒N點…持續M秒」正則可抽參數，級距走 `tierize.py` 既有映射 |
| delayed 型（落點場/回復/跟人走） | ~10 | `delayed`(+`anchor:"caster"`) | ⭐ **高**：D10 §1 已逐支歸類，參數五格內 |
| 掃描器假陽性 | 4 | ⛔ 內容零改動 | `prose_markers.json` 加 negative（「不斷地修煉」「小周天循環」×各2）|
| hook/aura/devour 已實作而掃描器盲 | ≥3 | ⛔ 內容零改動 | `shape_axes.json`：`onInterval`+ICD 記「迴圈」、`auras` 補軸、devour ICD 同理 —— **不改，`shapes:check` 差集歸零永遠達不成**（驗收卡在這） |
| 召喚物/投射物錨 | 3 | ⛔ 引擎缺（D10 §5） | 不可 script，先開票 |
| 蓄力 | 1 | `tpl-growth-charge` | 另一個模板的事 |

⭐ **推薦形狀**（第零守則⑨）：開 `tools/skill-templates/apply_loops.py` ——
一張 **~30 列翻譯表**（技能 id → {kind, 參數}，逐列人工讀說明填一次）+ 一支套用器，
註冊進 `skills:sync` ⇒ ownership 變成真的（genguard 一致）、可重跑冪等、
`--check` 有閘。⛔ 逐支手編 39 份 JSON 是 N 輪，而且今天被 genguard 封死。
⚠️ template 引用（`tpl-periodic-field`）**還不能用**：`expand.ts` 家族接線仍缺
（D10 §4 留給 main session），引用會在註冊時擲 ExpandError ⇒ 先用裸 delayed/dot 參數。

## 5. 沒做的（與為什麼）

- ⛔ 沒動 90-00/04-02 的 JSON（§1 雙柵欄），⛔ 沒用 GGD_GENGUARD_OFF。
- ⛔ 沒動 `godie-emfr.py` / `tpl-periodic-field.json`（15-00 內容本來就對，模板不需微調）。
- ⛔ 沒跑 skillremake:json / content:build / skills:sync（無來源改動＋全域鎖）。
- ⛔ 沒動 `shape_axes.json` / `prose_markers.json`（柵欄外）—— §4 列了要改哪幾格。
