# GH#1062 —— 編輯器存檔閘 `templateWriteBlockers` 補跑 `zAbilityDoc` lane 報告（2026-09-06）

**狀態：完成（⛔ 未 commit，主 session 統一 commit）。** 閘現在走出貨那條路的兩步；守衛紅→綠；`apps/editor` typecheck 在我的兩個檔 0 錯（紅的兩行是別條 lane 在飛的 `condition.ts`，見 §5）。

## 1. 動手之前量到的基線

夾具＝GH#1063 關掉之前 `tpl-beam-roll` 的形狀：出貨模板複本只把 `anchor.values` 放寬加 `bone`（模板本來就沒有 `attach` 格），params `{...defaults, path:"static", anchor:"bone"}`。三段都是出貨程式：`paramsSchemaFor` 收、`modelFxFamily` 發（有 bone 沒 attach）、`zSpawnModelFx` 的 GH#1063 refine 擋。

對 HEAD（#1062 之前）的 `templateWriteBlockers` 跑同一條測試（`run1.log`）：

| 段 | 結果 |
|---|---|
| `resolveTemplateExpansion(doc).ok` | `true`（展得開） |
| `zAbilityDoc.safeParse(doc)`（磁碟骨架自己） | `success: true` ⇒ 伺服器 `/validate` **也不會擋** |
| `templateWriteBlockers(doc)` | ⛔ **`[]`**（`expected '' to contain 'effects.0.attach'`） |

⇒ 這就是票文說的接縫：兩條各自對的守衛（表單驗 params、註冊表驗 doc），存檔閘只跑了前半。載入時 `registries.ts::expandIfTemplated` 第二步 `zAbilityDoc.safeParse(merged)` 紅 ⇒ 降級成「模板展開失敗，此技能目前沒有效果」。

## 2. 做了什麼

### `apps/editor/src/forge/ForgeWriteback.ts`（+26 / −1，只動 `templateWriteBlockers` 這一處）
- 新增 `expandedSchemaBlockers(merged)`：`merged.schema !== undefined ? zAbilityDoc : zAbilityDef`（逐字同 registries 的 standalone/embedded 分支），`safeParse` 失敗 ⇒ **每一條 Zod issue 一條 blocker**，文案 `展開後的文件過不了載入 schema（<path>）：<message> —— 存下去之後…降級成「沒有效果」`。
- `templateWriteBlockers` 的 `res.ok ? [] : …` 改成 `res.ok ? expandedSchemaBlockers(res.merged) : …`。
- import `zAbilityDef, zAbilityDoc` from `@ggd/shared/content/schema/ability`（shared 已 export，編輯器裡已有 3 個檔這樣 import）。⛔ 零行 refine 抄本。
- 消費端：`planForgeCreate` / `planForgeWrite`（確認對話框 `plan.blockers`，`ForgeStudio.tsx:1719` 渲染）與 `runForgeWrite` / `runForgeCreate`（執行前重算）都經過同一支函式 ⇒ 存檔兩條路都擋。

### `apps/editor/src/forge/forgeWritebackTemplateGate.test.ts`（+40）
- 一條 `it`：「展得開但載入 schema 的 refine 擋 ⇒ 存檔前就指名那一格」。先斷言夾具真的是接縫（resolve ok ＋ 骨架自己過 zAbilityDoc），再斷言 blockers 含 `effects.0.attach`、`runForgeWrite` 擲含 `effects.0.attach` 且 `calls` 為空。
- 檔頭 MUTATION LOG 補一筆。
- 既有 12 條不變、全綠（「好的綁定放行」證明第二步不是「一律拒絕」）。

### `docs/editor-contract/coordination/claim.editor-write-gate-zod.json`（新開，from main → to codex）
- `contractFingerprint ede2a824444ea458`（`ggd-type-catalog.json` 現值，`node tools/coord/check.mjs` 10/10 過）。
- ⚠️ `commit` 欄位＝`baseCommit`（1de2bd31f）：packet 與程式在主 session 同一個收編 commit 落地，packet 結構上不可能含自己的 sha；文內明說，驗證以 repro＋evidence 為準。
- 留給 Codex 的相鄰工作寫在 text 裡（表單 dropdown 縮成 `modelFxPathsFor`/`modelFxAnchorsFor`；`ConditionEditor.tsx` 跟上 form 條件葉）。

## 3. 測試與突變（vitest 2 次、tsc 1 次）
- Run 1（新測試 vs HEAD 程式）＝ **突變**：舊碼就是「拿掉 `safeParse`」的那個版本 ⇒ 1 紅 12 綠，紅在 `blockers.join(" ")` 為空。
- Run 2（實作後，用 packet 的 repro 指令從 repo root 跑）：13/13 綠，EXIT 0。
- `cd apps/editor && pnpm typecheck`：EXIT 2，**2 個錯全在 `ConditionEditor.tsx:792/867`**（`"form"` 不在 `ClauseKind`、`FormLeaf` 不是 `StatLeaf`），我的兩個檔 0 錯。`ConditionEditor.tsx` 對 HEAD 乾淨；`packages/shared/src/content/schema/condition.ts` 與 `sim/content/condition.ts` 在工作樹是 dirty（別條 lane 加 form 條件葉，另有未追蹤 `sim/content/conditionForm.test.ts`）⇒ 那條 lane 的 shared 型別先動、Codex 的編輯器檔還沒跟上。
- 行數：實作 26 / 測試 40（體驗層 ≤80 行上限內；整檔含既有守衛，不是新開檔）。

## 4. 需要主 session 動的（柵欄外）
1. commit 逐檔列名：`apps/editor/src/forge/ForgeWriteback.ts` · `apps/editor/src/forge/forgeWritebackTemplateGate.test.ts` · `docs/editor-contract/coordination/claim.editor-write-gate-zod.json` · 本報告。
2. ⚠️ **收編前 `apps/editor` typecheck 會因別條 lane 的 `condition.ts`（form 條件葉）而紅** —— `ConditionEditor.tsx:792/867` 要跟上（那是 Codex 的檔；若那條 lane 沒補，主 session 決定是誰補）。與本票無關，但收編後 `pnpm typecheck` 會紅在那裡。
3. `ticket-progress.sh` 的 `--commit` 由主 session 補。

## 5. 順手發現、沒動的（⛔ 沒開票的理由寫在旁邊）
- `ConditionEditor.tsx` 的 2 個 tsc 錯：另一條 wave-2 lane **在飛**（shared `condition.ts` dirty ＋ 新測試未追蹤），開票會變成對一個進行中改動的重複票 ⇒ 只寫進本報告與 packet text，由主 session 收編時判。
- `registries.ts::zodMessage` 只帶 Zod 的**第一條** issue 進失敗帳本；編輯器這一側逐條列。兩邊分母不同是刻意（帳本一行、作者要全部），⛔ 不是缺陷。

## 6. ⛔ 沒做到的
- 沒有在瀏覽器裡看確認對話框真的印出那行文案 —— 鏈路（`plan.blockers` → `ForgeStudio.tsx:1719`）是既有通道，測試證明的是 blockers 內容與零 API 呼叫；UI 文案的樣子**未驗收**。
- 沒有動表單層（dropdown 仍靠 JSON `values` ＋ #1057 的 ⊆ 守衛），那是 Codex 柵欄。
