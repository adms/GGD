# Lane G — GH#688 Phase 1–3 產生器化 ＋ #687 chain 修 ＋ alpha JASS 掃描（2026-08-25）

## 交付

| 件 | 落點 |
|---|---|
| 產生器（Phase 1+3+alpha 單一寫入端） | `tools/locust-census/gen.mjs`（`pnpm locust:build` / `locust:check`，--check 逐位元組、零時鐘欄位） |
| 正式版對應表 | `docs/蝗蟲群對應表.md`（§二 普查 236 隻含 runtime α% 欄 · §三 JASS 交叉 263 rawcode/644 生成點 · §四 runtimeAlpha 57 呼叫點） |
| 機讀版 | `tools/locust-census/census.json`（units / jass / sites / runtimeAlpha ＋ meta.counts） |
| 薄守衛（≤80 行） | `tools/locust-census/gen.test.mjs` —— ①--check 綠 ②表非空+h007+tint 逐位對 UNIT_TINTS ③runtimeAlpha 覆蓋全部呼叫點、解析數 ≥ 動態相鄰對下界（⛔ 不硬編 57） |
| chain（#687 + locust 掛載,一次改完） | `package.json`：`skills:sync` 頭 `quarantine:unlock`、尾 `quarantine:lock`；`locust:build` 插 `roster:check` 前；`locust:check` 進 `skills:check` 尾；新 script `quarantine:unlock/lock` 包 `product-quarantine.sh` |
| 偵察紀錄退位 | `docs/_reports/locust_scan/` 四份 md 檔頭標「正式版在 蝗蟲群對應表.md」（⛔ 未刪）；`蝗蟲群移植計畫.md` Phase 0(alpha)/1/3 狀態更新 |

## 數字（量到的）

- 普查 236 隻（Aloc 231 · fx-name 24 · hp1-noatk 27 · 灰 combat-stats 34 · no-Aloc 5）——與偵察 units.json **逐列 0 差**（tint/criteria/gray/scale/model 全比過）
- 生成點 644（Trig 411 · 動態 id 6）· 263 rawcode。⭐ 比偵察多抓 1 個 ogru 生成點（`set bj_lastCreatedUnit = CreateUnit(…)` 指派形式,偵察只掃 call 行）
- runtimeAlpha 57/57 呼叫點全覆蓋：resolved 26（last-created 9 · var-local 10 · var-global 7）· event-unit(-var) 25 · **unresolved 6 誠實標**（udg_BlackDragonUnit×5 —— war3map.j 全檔零指派,原作死碼；udg_PandaUnit×1 有解不開的指派）
- 回溯規則：GetLastCreatedUnit→同函式上溯 Create*；udg 變數→同函式最近指派,其次全檔（**全部**指派同一 rawcode 才 resolved,多候選列 candidates ⛔ 不挑）

## 突變驗證（一條承重線）

gen.mjs 把 tint 欄從單位列拿掉 → rebuild → `gen.test.mjs` ②紅（tinted.length=0）→ 還原 → rebuild → `locust:check` 綠。

## 測試帳

vitest ×2（①gen.test.mjs＋skillsSyncCoversGenerators 合跑全綠 ②突變紅）· tsc 0（未動 TS）· ⛔ 未跑 skills:sync（全域鎖）。

## ⚠️ 留給主 session

1. **sync-io 要重量**（chain 字串已變,`skills:sync`/`sync:plan` 會拒跑/報 full —— #687 已知,收斂時 trace 統一重量）。重量後 `census.json`＋`蝗蟲群對應表.md` 會進 quarantine writes（我已先 chmod 444）。
2. 突變後 `pnpm skills:sync` 首次跑會經 `quarantine:unlock/lock` —— 驗收 #687 的「裸 sync 全綠+跑完全鎖」要在 sync-io 重量之後才測得了。
3. Phase 2 的 MDL 參數（mdl-params.md）**不在** locust:check 覆蓋內（仍一次性掃描）,計畫 md 已標明。
