# NORM lane — 幽靈步驟名 `prose:apply` 修除 ＋ normalizer 清單閘（2026-08-25 20:00）

## 結論一行

兩份 genguard 腳本的正規化器清單都掛著 `"prose:apply"` —— 一個**不在 package.json
也不在 sync-io.json 任何步驟**的名字 ⇒ 永遠比不中、永遠不紅（「閘自己在說謊」形狀）。
已從兩份腳本移除，並立 `normalizerListIsReal.test.ts` 把「清單裡只准放真名」變成會紅的閘。

## 第 1 步：prose:build 實際做什麼（證據）

`package.json:124-125`：
- `prose:build` = `tsx tools/card-prose/apply_placeholders.ts`
- `prose:check` = 同一支 `--check`

讀 `tools/card-prose/apply_placeholders.ts`（檔頭 + 寫入點 172/190/215/223/253）：
它是**就地改欄位的正規化器**（只換 `"description"` 字面值，逐字元外科手術），寫**四類**檔：

| 寫什麼 | 位置 |
|---|---|
| ① 出貨技能檔 | `content/abilities/<id>.json`（sync-io 量到 331 份是產物） |
| ② 英雄鏡像 | `content/champions/<cid>.json`（56 份是產物） |
| ③ 90 支的來源 | `tools/skill-remake/heroes/*.py` |
| ④ 備份 | `docs/legacy/_ability-prose-before-placeholders_temp_*/` |

## ⚠️ 更深的洞（⛔ 本 lane 不修 —— 修法是全域鎖工程）

`prose:build` **不在 sync-io.json 的 38 個步驟裡**（逐一比對過步驟名清單）。後果三連：

1. **隔離區不會替它解鎖**：`content/abilities`、`content/champions` 平時 chmod 444，
   `prose:build` 單獨跑會吃 EACCES（除非有人手動解鎖 —— 那正是守則禁止的動作）。
2. **`genrun.sh` 跑不了它**（genrun 按 sync-io 步驟解鎖，查無此步）。
3. **genguard / hook 不認它的寫入**：它寫 content 產物的行為在擁有權表上是隱形的。

⇒ 正解是把 `prose:build` 補進 sync-io 的 trace（重量 writes），那需要跑全域鎖的
trace 工程，⛔ 本 lane 禁跑。**已列入 leftovers，主 session 決定。**

## 第 2 步：兩份腳本的修法

- `scripts/preserve-before-overwrite.py:161`（現 164）：`NORMALIZER_STEPS` 拿掉
  `"prose:apply"`，留三行註解說明為什麼＋指名守它的測試。
- `scripts/genguard.sh:21`（現 23）：`NORMALIZERS` 同步拿掉，同樣留註解。
- 兩份清單現在一致：`{tiers:apply, apconv:build, apdmg:build}` —— 三個都
  ∈ sync-io steps ∈ package.json scripts（已驗）。
- 為什麼**不是**改成真名 `prose:build`：真名也不在 sync-io 步驟裡（見上），
  塞進去一樣永遠比不中，而且會違反新閘的斷言②。等 trace 補上它之後再加。

## 第 3 步：閘

`packages/shared/src/ops/normalizerListIsReal.test.ts`（新檔，之前**沒有**半條既有閘
碰這兩份清單 —— grep 過 ops/*.test.ts 無 NORMALIZER 相關斷言）：

- ① 兩份腳本用 **regex 從原文抽**清單（⛔ 不抄清單進測試 —— 第四個住處），
  集合相等；regex 比不到（變數改名）⇒ 大聲紅，⛔ 不靜默變空。
- ② 每個名字 ∈ `sync-io.json` 的 `steps[].name`（幽靈名在這裡死）。
- ③ 每個名字 ∈ `package.json` 的 scripts（「請跑 pnpm <名>」要跑得動）。
- ②③ 掃**兩份清單的聯集**：幽靈塞單邊被①抓、塞兩邊①綠但聯集必含它⇒②③抓。

## 突變驗證（實跑）

往 `genguard.sh` 的 NORMALIZERS 塞回 `'prose:apply'` → 斷言① 紅
（AssertionError「兩份腳本的裁決必須逐字一致…」，列出兩邊差異）→ 改回來 → 綠。
vitest 共 3 次（額度上限）：初綠 / 突變紅 / 聯集修正後終綠，3 tests passed。

## Leftovers（主 session）

1. **prose:build 的寫入不在 sync-io trace 裡**（上面「更深的洞」）—— 需要全域鎖
   的 trace 重量；補上後兩份清單可考慮把 `prose:build` 收進正規化器族（它對
   content/abilities/champions 是就地改 description，⛔ 不是作者）。
2. `docs/legacy/_overwrites/overwrite_temp_20260824-04*/scripts/preserve-before-overwrite.py`
   等**備份副本**仍含 `prose:apply` —— 退休區，刻意不動。
3. 另一條 lane 的 worktree（`.claude/worktrees/goofy-dhawan-1d1607/`）也有舊清單 ——
   不是我的樹，不碰；它收斂時會撞新閘，改法同上（兩行）。

## 改動檔案（工作樹，未 commit —— 按約束留給主 session）

- `scripts/preserve-before-overwrite.py`
- `scripts/genguard.sh`
- `packages/shared/src/ops/normalizerListIsReal.test.ts`（新）
- `docs/_reports/NORM_temp_20260825-2000.md`（本報告）
