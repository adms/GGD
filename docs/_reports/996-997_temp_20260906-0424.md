# #996 ＋ #997 —— lane 報告（2026-09-06 04:24 開工）

> commit `0f3912790`（四檔逐檔 pathspec）· 測試 3 次 vitest ＋ 1 次 tsc（packages/shared）· ⛔ 體驗層不做突變
> `SWITCH: none` —— 兩張都是閘／文件，沒有玩家看得到的行為改動，⛔ 沒有需要留的後台開關。

---

## #996 `cover("ping-band-gutter")` 零命中 —— ⭐ **前提不成立：信標一直在，是量尺瞎了**

### 量到的

| 問 | 答 | 出處 |
|---|---|---|
| 宣告住哪 | `docs/todo/latency-visibility.md:115`（pc-09，`done`） | 真的 `parseTodoMarkdown` 跑出來 |
| 發射點住哪 | ⭐ `apps/client/src/ui/hud/versionBadgeBand.test.ts:307` `cover("ping-band-gutter")` | `grep -a` ／ `readFileSync` |
| 為什麼 `grep -rn` 量成零 | 那一檔有 **2 顆原始 NUL 位元組**（`${d.file}<NUL>${d.value}` 當 Map key 分隔符）⇒ macOS grep 把整檔當 **binary 靜默跳過**，連 `Binary file matches` 都不印；`file` 判它 `data` | python 位元組掃描：offset 43057 / 43510 |
| CI 的 ground truth | run `33985736831` 的 `TODO runtime gate` 紅了 **31 列**，**沒有** pc-09 ⇒ 那條測試在 CI 真的跑了、真的發射了 | `gh run view --log-failed` |
| 修完之後 | `grep -rn ping-band-gutter apps packages tools` **命中 :307**；`GGD_COVERAGE_FILE` 下跑那一檔 13/13 綠，NDJSON 真的落地 `{"cover":"ping-band-gutter"}` | run 2 |

⇒ **選項：補呼叫點／刪宣告 —— 兩個都不對**。正解是修**量尺看不見的原因**：NUL → `\0` 跳脫（執行期逐位元相同，2 行 diff）。
⚠️ 這同時解釋了 09-05 兩處「查證過」的複述（`latency-visibility.md:115` 的更正段、`incidental-issues-for-owner_temp_20260905-1600.md:33`）—— 三處用的是同一把瞎的尺。

### AC-2 反方向閘（新）：`packages/shared/src/ops/todoDoneBeaconsHaveEmitters.test.ts`

- 從**宣告**那一頭走：docs/todo 每一列 `done` 的 Test ID，在 `apps/ packages/ tools/` 零個 `cover()` ⇒ 紅並逐列指名。
- 逐檔 `readFileSync`（NUL 不失明）；認得 `cover("x")` · Go `Cover(t,"x")` · `cover(\`pre-${…}\`)` 前綴 · `cover(ident)` 退回整檔 kebab 字面值（刻意**多算** ⇒ 紅的時候是真的沒人提過）。
- ⭐ 對照 CI：第一次跑量到 **26 列，全部 ⊆ runtime 的 31 列 ⇒ 零誤報**。多出的 5 列（`build-stamp-env` · `icon-gen-*`×4）有發射點但 CI 沒跑到 —— runtime 閘的管區。
- 26 列進棘輪帳本 `KNOWN_UNEMITTED`（只能變短），⭐ **#1031** 逐列列著修法（5 列是「一格多個 id」結構上永遠對不到；21 列真的零發射點）。
- ⚠️ 第一次跑就中了一個：尺量到自己（帳本就是一串 kebab 字面值，而檔裡提到 `cover(`）⇒ 26 列全綠 ⇒ 排除本檔。
- 哨兵驗兩個方向：缺發射器的被指名；字面值／前綴／退回三種不被誤判。

### 全部信標掃一次（票的 Scope）

| 分母 | 數 |
|---|---:|
| docs/todo 宣告列 | 1,507（done 1,409） |
| 原始碼裡的發射 id（含退回） | 3,063 exact ＋ 1 前綴（`arena-play-`） |
| **done 而零發射點** | **26**（→ #1031） |
| 同型的 grep 失明檔（原始 NUL） | 另 **8 份**出貨原始碼（→ #1032，⛔ 不順手修） |

## #997 `AGENTS.md` 檔頭 sha 沒有守衛

### 量了什麼、選了什麼

| 選項 | 量到的 | 裁決 |
|---|---|---|
| ② 刪 sha、改成「基準永遠是 origin/main 的 HEAD」 | 永不過期，零維護 | ⭐ **語意採用**（那一行現在逐字這樣寫），但保留一顆校對 sha —— Codex 讀得出「這段文字多老」 |
| ① sha ＋ 距離守衛 | main 每天 **55–172** commit（中位 ≈100） | ⭐ **採用**，`BASELINE_MAX_BEHIND = 1000` ≈ 一個發版週；理由寫在常數旁 |
| 「必須是某個 tag」 | tag 一天切 **1–6** 個 | ⛔ 不採用：packet 的 `baseCommit` 與 `check.mjs` 都用 sha（同一種貨幣）；tag 不更新鮮，且會暗示一個不存在的「契約版本」語意（契約的版本是 `contractFingerprint`） |

### 落地

- `AGENTS.md:7`：`57e955735` → `78ecc838e`（今天的 origin/main），並寫明「基準永遠是 origin/main 的 HEAD；這顆只記上次校對位置；由哪條守衛守」。⛔ 只動這一行。
- `agentsMdIsHonest.test.ts` 第 ④ 條：檔裡**每一顆** sha（反引號/雙引號、含數字的 7–40 hex：檔頭 ＋ JSON 範例的 `baseCommit`/`commit` ＋ §5 表的兩顆）必須存在且在 HEAD 歷史上（永不過期的性質）；檔頭那顆另加落後 ≤ 1000，紅了指名距離並給修法（`git rev-parse --short=9 origin/main` 貼回）。shallow clone ⇒ 紅，⛔ 不是跳過。
- 哨兵：假 sha／缺檔頭被指名，HEAD 自己 0 距離不誤判。

## 測試

| 次 | 指令 | 結果 |
|---|---|---|
| 1 | `packages/shared` vitest ×2 檔 | 帳本閘紅 —— 尺量到自己（見上）|
| 2 | `apps/client` `versionBadgeBand.test.ts`（`GGD_COVERAGE_FILE` 開） | 13/13 綠，信標真的落地 |
| 3 | `packages/shared` vitest ×2 檔（修後） | **7/7 綠** |
| tsc | `packages/shared` | EXIT=2，**3 條全在 `releaseNotePlayersRange.test.ts`（既有，不是本次）** |

## 開出去的票

- **#1031** 26 列 done 零發射點（一張表，含拆列／補 cover 的分類）
- **#1032** 8 份含 NUL 的出貨原始碼（grep 失明同型）
