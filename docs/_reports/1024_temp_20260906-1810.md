# GH#1024 PR-1（Main —— schema／validator）lane 報告 · 2026-09-06 18:10

> 範圍：票文 A1–A5 ＋ 承重守衛 ＋ 給 Codex 的 PR-2 packet。⛔ 沒 commit、沒跑任何 `*:build`／`skills:sync`（preamble）。

## 做了什麼（逐項對 A1–A5）

| # | 決定 | 落點 |
|---|---|---|
| **A1** | `champion@1.origin` 的值域＝`ORIGINS`（`z.enum(ORIGINS)`，⛔ 不是手寫 union），`ORIGINS` 與 `content/config/origin-routes.json` 的鍵**連順序**鎖住 | 閘 `championOriginCoverage.test.ts` A1。⚠️ **誠實**：值域仍是 TS 常數**鏡射**那份表，⛔ 不是 build 時從 JSON 推導 —— `packages/shared/tsconfig.json` 的 `rootDir: "."` 擋掉跨 package 的 JSON import（TS6059），而「產生器＋`--check`」路線要動 `package.json`／`sync-io`（柵欄外）。既有的 `zConfigOriginRoutesDoc`（strict、鍵從 ORIGINS 建）本來就雙向鎖住兩邊；這一條閘只多鎖了順序並把訊息說清楚 |
| **A2** | 新欄位 `champion@1.statOverrides`：十一屬性逐格**選填級別名**；合併順序 **覆寫 > 出身 `byOrigin` > 定位 `byArchetype`** 只住 `statNormalization.ts` 的 `bandFor()` 一處；`resolveChampionStats`／`bandsOf`／`championPitchOf`（選角畫面距離標示）全部改走它 | `schema/champion.ts` · `statNormalization.ts`（`StatOverrides` / `statOverridesOf` / `bandFor`）· `championPitch.ts` |
| **A3** | 同一格填數字 ⇒ schema 紅並**指名那一格**（`statOverrides.ms 是算好的值（12）—— 這一格只收級別名…值在載入時從 stat-normalization 解析`，`z.enum` 的 `errorMap`）；另一條閘：覆寫的屬性必須在出貨 `appliesTo` 裡（否則卡面說了不會發生的事，第一·五） | `schema/champion.ts` 的 `zBandOverride` · `championOriginCoverage.test.ts` A3 |
| **A4** | ⭐ **`role` 從 `origin` 推導**（`ORIGIN_TO_ARCHETYPE[originOf(doc)]`，四格 tank/fighter/marksman/mage），開關 **`config.stat-normalization@1.roleFromOrigin`（預設 true，我挑的）**；英雄卡的 `role` 格降為**退路原始值**（開關關掉那一秒照抄它），⛔ 沒刪 71 份的那一格（與 `growth.ms` 同一個處境）。仍**必填**：`compat.test.ts` 釘住文件→`ChampionDef` 可指派，第一版改成選填讓 4 個 tsc 紅，改回 | 介面／DEFAULT／parser：`statNormalization.ts`；Zod：`schema/config/statNormalization.ts`（`.optional()`，出貨檔可晚一步補格）；⭐ **消費端 `registries.ts:495`** `registerChampion({ ...resolved, role: resolveChampionRole(resolved, statNorm) })` |
| **A5** | 22 位沒有 `origin` 的（⛔ 不是票文的 23 —— 那個數把 `_index.json` 數進去了；真實母體 **71 位**）全部進豁免表，逐隻一行能被反駁的理由：**20 具變身態**（不可獨立選取，出身載入時由 `originOf()` 推導；該不該繼承本體出身是 owner 決定 → **GH#1064**）＋ **2 位骨架**（sela/thorne，`attributes.source=authored`，fail-open 用） | `championOrigin.exemptions.json` · 閘 `championOriginCoverage.test.ts` A5（兩個方向：沒 origin 也不在表 ⇒ 紅；表裡的文件不存在／前提不成立／本體出身變了／推導出身變了／已經有 origin ⇒ 紅） |

## 量到的（⛔ 不是估的）

- 英雄卡 **71** 份（票文 72 含 `_index.json`）：`origin` 49 有／22 沒有；沒有的 = 20 變身態 ＋ 2 骨架。
- **20 具變身態裡 16 具**由三圍推導出的出身與本體 owner 指派的不同（例：草泥馬本體坦克 → 變身態法刺；索隆本體鬥士 → 變身態坦克）。⇒ 兩防從極大掉到極小而卡面沒說。⛔ 我沒挑：owner 2026-08-13「屬性不用多一份考量，都是一樣」有兩種讀法 ⇒ **GH#1064** 二選一。
- `role` vs `ORIGIN_TO_ARCHETYPE[originOf(doc)]`：**36/71 不一致**；`role` 有 **66/71** 逐字是 `attackType` 的別名（melee→fighter / ranged→marksman）⇒ 它是匯入粗分類，不是設計。
- `role` 的消費端：圖鑑篩選／標籤（`apps/client/src/ui/codex/codexLabels.ts` ROLE_LABEL）、選人畫面那一行、**戰後評分權重**（`sim/stats/rating.ts` ROLE_WEIGHTS 有 tank/fighter/marksman/mage 四格都在）、admin 列表。⇒ 開關開著時那 36 位的圖鑑標籤與評分權重會變。
- `bandsOf()`（後台稽核用）在此之前只查 `byArchetype`，與場上跑的 `byOrigin` 不同表 —— 已改走 `bandFor`（同一個接縫，不算順手修）。
- 6 顆拖不出表單的積木（Codex 收據）：`tpl-dragon-shockwave`（Main 的債：展開路徑）＋ `locust-line/orb/strike/swarm/travel`（wiring=node，preset 的表單入口是 Codex 的）。
- 契約指紋：`ggd-type-catalog.json` 只從 `content/ability-templates/` 推導，⛔ 不吃 champion schema ⇒ packet 的 `contractFingerprint` 仍是 `afc7c6a573a673f5`，`coord:check` 7/7 ✅。

## 守衛與突變

- 承重 `championTemplateOverrides.test.ts`：改模板（`byOrigin.ms.坦克` 換一格）⇒ 沒覆寫的英雄跟著變且真的離開出貨值；有覆寫的英雄停在覆寫那一格 —— **兩個方向**。另兩條：填數字 ⇒ 紅並指名 `statOverrides.ms`；註冊表 role ＝ `ORIGIN_TO_ARCHETYPE[坦克]` 而非卡上的 marksman。
- **突變①（跑過）**：`bandFor` 拿掉 `statOverridesOf(def)[key] ??` ⇒ 承重那條紅（`expected 5 to be 8`：被覆寫的英雄塌回模板格）。改回。
- 只測預設開啟那一邊（`roleFromOrigin=true`），關掉那一邊不測（第〇·六）。
- 跑了 3 次 vitest（批次 · 修完 · 突變）＋ 1 次 `pnpm typecheck`。測試 156 行 < 實作 219 行。

## ⛔ 沒做到的

- A1 的「build 時從 JSON 推導」—— 見上表（rootDir）。要真的推導得走「產生器 + `--check`」，柵欄外。
- A5 沒有「補上」任何一位 —— 全部豁免（理由在表裡，每一列可反駁）。
- `apps/admin` 的欄位表：**沒動**（柵欄外）⇒ 見下。

## 需要主 session 動的柵欄外檔案

1. ⛔ **`apps/admin/src/configForms.test.ts` 現在紅**：「stat-normalization 的標籤表與 schema 對不上 … `roleFromOrigin`」。在 `apps/admin/src/configForms/specs/stats.ts` 的 `skipTransformedBodies` 那一列後面加：
   ```ts
   { path: "roleFromOrigin", zh: "英雄定位（role）由出身推導", note: "出貨**開著**：圖鑑篩選／戰後評分用的四格定位（坦克/近戰/遠程/法師）從出身查表（ORIGIN_TO_ARCHETYPE），英雄卡上的 role 格只是退路。關掉 = 照抄英雄卡（匯入時的粗分類，71 份裡 36 份與出身不一致，66 份只是 attackType 的別名）。GH#1024 A4。" },
   ```
   （順帶：同一頁 `intro` 第三句「這一版只套用**移動速度**與**魔抗**」是過期散文 —— 出貨 `appliesTo` 是 11 項。）
2. `content/config/stat-normalization.json` 補第三個住處：`"roleFromOrigin": true`（Zod 是 `.optional()`，不補也照預設跑；補了後台才看得到）。⚠️ 那份檔是手編的（genguard：非產物），⛔ 但我的柵欄說唯讀。
3. commit 後 `pnpm skills:sync`（`typecat` 不受影響；`archetypes:build`／文件產生器若讀註冊表 role 會跟著變）。
4. 若 commit 之後 packet 的 `commit` 欄想改成落地 sha，改 `docs/editor-contract/coordination/claim.export-champion-package.json` 五處（現在填 baseCommit `ddcb3c1b8`）。

## 不是我的紅（同一棵樹上別條 lane 的，⛔ 我沒動）

- `fieldAdoption.test.ts`：缺 `enum:vfx-scripts.schema=vfx-script@2` ＋ 53 條 stale（全是 `vfx-scripts.segments[]…` 與 `abilities.effects[]#applyBuff.condition|…slot`）。我在同一份豁免表加了 `field:champions.statOverrides`（landing，2026-09-06），round-trip 驗過沒動別人的列（另一條 lane 的 GH#1054 `chanceFrom` 列保留）。
- `pnpm typecheck`：apps/client／apps/editor 的 `vfx-script@2` 型別錯 62 處（`vfxScriptStudio.ts`、`acceptanceFixtures*`…）。packages/shared 我造成的 4 處（role 選填）已改回 0。

## 順手發現但沒動

- 20 具變身態的出身與本體不同 → **GH#1064**（開了票）。
- admin stat-normalization 頁 intro 過期散文（上面第 1 點順帶）。
- `championPitch.ts` 檔頭與 `originRoutes.ts` 檔頭仍寫「真正驅動數值的是 bands × byArchetype（4 格）」—— 2026-08-16 之後 `byOrigin` 才是主表；散文過期，⛔ 沒動（不在這張票的機制上）。

## 改了哪些檔

- `packages/shared/src/content/schema/champion.ts`（`statOverrides` 欄位 · `zBandOverride` · `role` 註解）
- `packages/shared/src/content/statNormalization.ts`（`roleFromOrigin` 介面/DEFAULT/parser · `StatOverrides`/`statOverridesOf`/`bandFor`/`resolveChampionRole` · `resolveChampionStats`/`bandsOf` 改走 `bandFor`）
- `packages/shared/src/content/schema/config/statNormalization.ts`（Zod `roleFromOrigin` 選填 ＋ DEFAULT_DOC）
- `packages/shared/src/content/registries.ts`（消費端：註冊時 `role` 推導）
- `packages/shared/src/content/championPitch.ts`（距離標示走 `bandFor`）
- `packages/shared/src/content/loader.test.ts`（第五個被授權的分歧：頂層 `role`）
- `packages/shared/src/content/fieldAdoption.exemptions.json`（＋`field:champions.statOverrides` landing）
- 新：`packages/shared/src/content/championOrigin.exemptions.json` · `championTemplateOverrides.test.ts` · `championOriginCoverage.test.ts` · `docs/editor-contract/coordination/claim.export-champion-package.json`
