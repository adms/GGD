# GH#986 Main lane E/F ＋ Codex A–D packet —— 完整報告

- 時間：2026-09-05 04:33
- HEAD：`476a0f200bf55f0200f892193a5898a86f36df51`（＝ `origin/main`）
- 動到的檔（2 個）：
  - `packages/shared/src/ops/skillsSyncCoversGenerators.test.ts`（只改 `skillforge:visual-advisory:check` 那一列的理由字串，188–193 行 → 188–203 行）
  - `docs/editor-contract/coordination/claim.codex-lane-visual-advisory-and-brick-ids.json`（新）
- ⛔ 沒有 commit、沒有 `git add`、沒有跑 `skills:sync`。

---

## 1. 量到的（⛔ 不是照抄票文）

| 指令 | 離開碼 | 輸出（節錄） |
|---|---:|---|
| `git log --format='%h %an' -- tools/skill-forge/codex-visual-advisory.source.json` | 0 | `d71d027be CanLin`（**唯一**一筆；2026-09-04 `feat(editor): complete 42x46 visual acceptance evidence`） |
| `grep -n "humanPending" tools/skill-forge/build-codex-visual-advisory.mjs` | 0 | `27:if (manifest.summary?.captured !== 46 \|\| manifest.summary?.humanPending !== 46) {` |
| `node tools/skill-forge/build-codex-visual-advisory.mjs --check` | **1** | `stale manual review: source 1e89586fba3677fa… != current packet 692c8240ff11635d…` |
| `node tools/skill-forge/build-visual-review-packet.mjs --check` | **0** | `PASS human review packet · 12 pages / 46 documents · 692c8240ff11635d` |
| `node tools/skill-forge/check.mjs`（`skillforge:check`） | 1 | 其餘每一列 PASS，唯一 FAIL＝`46-document Codex visual advisory` |
| `shasum -a 256 docs/editor-contract/ggd-type-catalog.json \| cut -c1-16` | 0 | `4a8cd96d6e2bf97f` |
| `node tools/coord/check.mjs` | **0** | `2 份 packet，0 份有缺` |
| `npx vitest run …skillsSyncCoversGenerators.test.ts …coordCheck.test.ts` | **0** | 2 files / 11 tests passed |

### ⭐ 三個票文沒有的發現

1. **「等 owner 人工批核」不只沒出處，它方向相反。**
   `build-codex-visual-advisory.mjs:27` **要求** `manifest.summary.humanPending === 46`
   ⇒ ⭐ owner 真的去批核，這支反而會紅。
   而 `codex-visual-advisory.source.json` 的**唯一作者**是 Codex 的 commit `d71d027be`（CanLin），
   ⛔ 不是 owner 的 verdict。⇒ 它是 Codex 的**產生器輸出**，重跑就好。

2. ⚠️ **重跑不是只 bump digest。**
   source 有 **7** 列 `disposition:"main-blocked"`，而今天的 manifest（`692c8240…`）只有 **6** 列
   帶 `owner:"main"` 的 `MISSING_VISUAL_BRICK` blocker —— 多出來的是 **`godie-hart.r`**。
   ⇒ 對 `692c8240…` 重跑會撞 `build-codex-visual-advisory.mjs:66` 的
   `disposition disagrees with deterministic Main blocker routing`。

   | | ids |
   |---|---|
   | manifest（6） | `godie-nbbc.e` · `godie-ogrh.r` · `godie-o00x.r` · `godie-e002.ex` · `godie-e00l.ex` · `godie-hvsh.r` |
   | source（7） | 上面 6 個 ＋ **`godie-hart.r`** |

   ⇒ ⭐ **票文與 `AGENTS.md` §3 範例寫的 7 個 `unblocks` 是從已經過期的 advisory 數來的。**
   （`godie-hart.r` 的 advisory 備註自己寫「與橫向氣功砲**同屬** Main 實心光束積木缺口」——
   若它真的該算，正解是讓 manifest／驗收包在那一列**發出** `MISSING_VISUAL_BRICK`，
   ⛔ 不是在 packet 裡手寫第 7 個 id。）

3. ⭐⭐ **C 的缺口比票文寫的大一格 —— 而 `coord:check` 今天對它是靜默的。**
   `tools/coord/check.mjs::acceptanceRows()` 的母體是
   `docs/_reports/editor-skill-acceptance-42x46.json`，⛔ **不是** manifest。
   而那 46 列的 `machineIssues` **全部是空的** ⇒ `anyMachineIssues === false`
   ⇒ `checkUnblocks()` 的 `brickId` 交集檢查**靜默 `continue`**。
   ⇒ ⭐ `coord:check` 現在是綠的，⛔ 而那一格什麼都沒驗（失敗形態⑫的形狀）。
   ⇒ C 要把 `MISSING_VISUAL_BRICK` ＋ `brickId` 同時寫進**驗收包那 46 列**。

---

## 2. E —— 豁免表那一列的新理由（逐字）

`packages/shared/src/ops/skillsSyncCoversGenerators.test.ts` 的 `EXEMPT["skillforge:visual-advisory:check"]`：

```
⛔⛔ **它會寫檔（`build-codex-visual-advisory.mjs` 的 `mkdirSync`＋`writeFileSync`）⇒ 照規矩它該接進
skills:check** —— 這一列是**暫時**的（**GH#986** 的 A／F）。⭐ 它今天紅在 **Codex 的 advisory 對不上
驗收包**，⛔ 不是在等人：`stale manual review: source 1e89586f… != current packet 692c8240…`
（2026-09-05 實跑 EXIT 1）。⛔⛔ 舊理由引用的「等 owner 人工批核」不只沒有出處，它**方向相反**：
① 那份 source 的作者是 **Codex 的 commit `d71d027be`**（`git log --format='%h %an' --
tools/skill-forge/codex-visual-advisory.source.json`）；② 工具第 27 行**要求**
`humanPending === 46`（`grep -n humanPending`）—— ⭐ owner 真的去批反而會讓它紅。
⇒ `source` 帶的是**跟著包走的 digest**，重跑就好，⛔ 不需要任何人重新看圖。
⭐⭐ 到期條件（**一行，可以當場檢查**）：`node tools/skill-forge/build-codex-visual-advisory.mjs --check`
回 **0** 的那一刻，這一列就該刪掉，並把 `skillforge:visual-advisory:check` 接進 `skills:check`（GH#986 F）。
⚠️ 而重跑**不是只 bump digest**：source 有 **7** 列 `main-blocked`，而今天的 manifest 只有 **6** 列帶
`MISSING_VISUAL_BRICK`（多出來的是 `godie-hart.r`，2026-09-05 實測）⇒ 對 `692c8240…` 重跑時會撞
`disposition disagrees with deterministic Main blocker routing`。
```

替換方式：`python3 scripts/edit-or-die.py … --old-file … --new-file …` ⇒ `✓ 替換了 1 處`（EXIT 0）。
⛔ 沒有 sed 就地替換。

## 3. F —— 接線條件

- ⛔ **今天不拿掉那一列**（`--check` 今天是 **1**）。
- ⭐ 到期條件已經逐字寫進理由，而且它是**一行指令**：
  `node tools/skill-forge/build-codex-visual-advisory.mjs --check` 回 **0**
  ⇒ 刪那一列 ＋ 把 `pnpm skillforge:visual-advisory:check` 接到 `package.json` 的 `skills:check` 尾端。
- **理由閘（#987）對這一列是綠的**：`exemptRowProblem()` 走「暫時列」分支
  （文字含「暫時」）⇒ 只要求 `GH#\d+`，而新理由帶 `GH#986`。
  `skillsSyncCoversGenerators.test.ts` 6 tests 全綠。

---

## 4. Codex packet（A–D）

`docs/editor-contract/coordination/claim.codex-lane-visual-advisory-and-brick-ids.json`

| 欄位 | 值 |
|---|---|
| `schema` | `ggd-coord-packet@1` |
| `dedupeKey` | `claim.codex-lane-visual-advisory-and-brick-ids`（＝檔名，`claim.` 開頭） |
| `kind` / `from` / `to` | `claim` / `main` / `codex` |
| `baseCommit` | `476a0f200bf55f0200f892193a5898a86f36df51`（origin/main 的祖先，驗過） |
| `contractFingerprint` | `4a8cd96d6e2bf97f` |
| `unblocks[]` | 那 **6** 個 id（`coord:check` 驗過都在驗收包裡） |
| `status` | ⛔ **沒有這個欄位** |

五條 claim 與各自的 repro（⛔ 全部無 shell 元字元、前綴在白名單內）：

| # | 對到 | kind | `repro.command` | `expectedExit` |
|---|---|---|---|---:|
| 0 | A | confirmed | `node tools/skill-forge/build-codex-visual-advisory.mjs --check` | 1 |
| 1 | A | confirmed | `pnpm skillforge:check` | 1 |
| 2 | B | confirmed | `node tools/skill-forge/build-visual-review-packet.mjs --check` | 0 |
| 3 | C | confirmed | `node tools/coord/check.mjs` | 0 |
| 4 | D | refuted | `npx vitest run packages/shared/src/ops/coordCheck.test.ts` | 0 |

⭐ 五條的離開碼**全部是今天實跑量到的**（見第 1 節的表），⛔ 不是推測。
⚠️ claims[0]／[1] 期望 **1**：它們宣稱的就是「今天是紅的」。A 落地那一刻它們會翻成 0
—— ⭐ 那正是這份 packet 到期的訊號（也是 F 的觸發條件），屆時連同 `unblocks` 一起由後續 PR 收掉。
（今天 CI 只跑 `pnpm coord:check`，⛔ 沒有 `--run-repro`，所以不會因此變紅。）

`ownerQuotes[]` 兩則（逐字，來源＝GH#986 票文的引言區）：
2026-09-04「整個矩陣是用來微調的…」· 2026-09-05「我同時也要通知 codex 編輯器遵守規則對吧?」。

`asks[]` 五條：A（重跑＋處理 `godie-hart.r`）· B（逐份 digest ＋ 一起改 contact-sheets）·
C（`brickId` 要寫進**驗收包**，⛔ 不是只寫 manifest）· D（`unblocks` 用 6 個 id）·
⛔ 不要動 Main 的豁免表。

---

## 5. 離開碼總表

```
git log … codex-visual-advisory.source.json            EXIT=0
grep -n humanPending …                                  EXIT=0
node tools/skill-forge/build-codex-visual-advisory.mjs --check   EXIT=1   ← 這就是今天的紅
node tools/skill-forge/build-visual-review-packet.mjs --check    EXIT=0
node tools/skill-forge/check.mjs                        EXIT=1
python3 scripts/edit-or-die.py …                        EXIT=0（✓ 替換了 1 處）
node tools/coord/check.mjs                              EXIT=0（2 份 packet，0 份有缺）
npx vitest run …skillsSyncCovers… …coordCheck…          EXIT=0（11 tests passed）
```
