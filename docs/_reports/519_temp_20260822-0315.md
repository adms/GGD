# GH#519 指定型技能沒有目標指示 —— lane 報告（2026-08-22 03:15）

## 先確認「是不是已經有了」

`git log --oneline -12` + 讀檔：#504/#505/#506/#508/#512 全部落地，但**沒有一條做目標指示**。
#512 做的是**方向**（`setCursorlessAim` → AoE 圓心走搖桿方向），而它為了不畫錯圈，
在 `withCursorlessAim` 裡把 `hoveredEntityId` **寫死成 `null`** ——
於是 `targeted` 技能在手把長按時 `resolveCastTarget` 永遠回 null ⇒ 沒有落點 ⇒ 沒有圈。
⭐ #519 就是 #512 刻意留下的那個洞。

## 做了什麼（只動名下三個檔）

| 檔 | 改動 |
|---|---|
| `apps/client/src/input/AimResolver.ts` | 新增**軟鎖定暫存器** `setCursorlessTarget` / `getCursorlessTarget`；`withCursorlessAim` 改成帶著它（⛔ 不再寫死 null）；`setCursorlessAim(null)` **一起清掉**目標（同一個生命週期 —— `PadDescribeHold` 放開技能鍵時只叫得到那一支） |
| `apps/client/src/render/views/targetMarker.ts`（新） | 目標環的**決定**那一半：畫不畫 · 畫在哪 · 多大 · 什麼顏色。⛔ 一顆 mesh 都不建（GH#519 明說不要另做一套渲染） |
| `apps/client/src/render/views/targetMarker.test.ts`（新） | 守衛，5 條，78 行 |

### 顏色/粗細一格都不是這個 lane 發明的（第一守則 · 第〇·四守則）

- 顏色 → `vfx/telegraphChannel.paletteFor(relation)`：敵/友/自己三條通道，**已經是後台欄位**
  （`config.range-guide@1` 的 `telegraph.{self,ally,incoming}`）。operator 把敵方預告改成紫色，
  這個標記自己就跟著變。
- 框粗細 → `ui/abilityRangeGuide.rimThickness`，同一份文件同一格。
- 幾何只新增**兩個比例**（`TARGET_RING_RATIO=1.3` · `TARGET_RING_Y_LIFT=0.005`），
  底數是 `ChampionView.SELF_RING_DIAMETER` / `SELF_RING_Y` **本人**，⛔ 不抄字面值。
  ⇒ ⛔ 沒有新增 `content/config/*.json`，所以**沒有動到 `store.ts` / `App.tsx` 那兩行共用檔**。

## 守衛與突變（靈魂層 —— 技能回饋）

`targetMarker.test.ts`，全部走出貨那條路（`setCursorlessAim`/`setCursorlessTarget` →
`resolveAoeCenter` / `resolvePadTargetMarker`），⛔ 沒有一條手捏 `hoveredEntityId`（失敗形態⑤）。

1. ⭐ **承重**：軟鎖定換人 → 環**與 AoE 圓心一起**搬到新的那個人腳下
2. 放開技能鍵 → 暫存器清空 → 不畫（一個活過放開的目標會指著沒有人在瞄的敵人）
3. 查不到位置 → 不畫，⛔ 不退回施法者腳下（GH#415 判死的那個謊）
4. 顏色 `toBe(paletteFor(relation).ring)`（**同一顆物件**，⛔ 不比字面值）＋ 敵/友不同
5. 環比自己人光環寬且更高（失敗形態①：鎖到自己時兩環不會疊成一個）

**突變**：`AimResolver.ts:133` `hoveredEntityId: cursorlessTarget` → `null`
⇒ 第 1 條紅（`expected null to deeply equal { x: 3, z: 0 }`）；
同批 5 個既有 pad/aim suite（`aoeIndicatorCentre` · `GamepadInput` · `MultiGamepad` ·
`inputMapping` · `TouchInput`，97 條）全綠 ⇒ ⛔ 沒有其他守衛蓋著這一行。已用 `Edit` 還原（⛔ 不是 checkout）。

測試預算：vitest **3 次**、`pnpm typecheck` **1 次**（EXIT=1，錯誤**只有**我這一份的
`noUncheckedIndexedAccess`，已修；其餘 0 錯 —— 工作區其他 lane 的檔案是乾淨的）。

## ⚠️ 沒做完的那一半：**mesh 還沒有人畫**（失敗形態②的風險，必須有人接）

lane 的檔案柵欄不含 `GameApp.ts` / `AimIndicator.ts` / `TouchInput.ts` / `GamepadInput.ts`，
而 `git status` 顯示 `GameApp.ts` 與 `GamepadInput.ts` **此刻正被別的 lane 改著**，
所以我一個字都沒有動。⇒ 目前這個 lane 交出的是**決定**，⛔ 還不是玩家看得到的環。

### 接線清單（由主 session 或下一條 lane 做，照 CP 值排序）

**① 最便宜、單獨就有效** —— `apps/client/src/input/GamepadInput.ts`
`mapGamepadFrame` 的**長按段**（`describe` 解出來之後）每幀解一次軟鎖定並 publish，
`PadDescribeHold.set()` 順手轉發：

```ts
// out.describe 之後
if (describe) {
  const ab = ctx.ability(describe);
  out.describeTarget =
    ab && ab.castType === "targeted" && self
      ? ctx.nearestEnemy(self, padCastReach(ab, rangeMult), aimDir)
      : null;
}
// PadDescribeHold.set(slot, aim, target)
setCursorlessTarget(slot ? target : null);   // ⛔ 要在 setCursorlessAim 之後叫
```

⭐ 只做這一條，**既有的 AoE 圈**（`AimIndicator` 已經在畫）就會落在被鎖定的那個人腳下 ——
對**有 `radius` 的**指定型技能而言 #519 當場就修好了，⛔ 零新渲染。

**② 單體技能（radius = 0）需要的環** —— 三個檔各一小段：
- `input/TouchInput.ts`：`AimIndicatorState` 的 `"range"` 變體加
  `targetX / targetZ / targetDiameter / targetRgb / targetAlpha`（全部 nullable）
- `GameApp.resolveHoldPreview`：**一行**
  `const mark = resolvePadTargetMarker((id) => this.entityPos(id), (id) => this.vfx.relationOf(id));`
  然後把 `mark?.x ?? null` 等塞進回傳的物件
- `render/AimIndicator.ts`：`update()` 尾巴多叫一次既有的 `paintCircle(...)`（第三個 `GuideCircle`），
  ⛔ 不要另寫一支 make/paint

**③（⛔ 我刻意沒做）** `ui/abilityCue.ts` 在名下但沒有動：
「換目標時給一聲/一下震動」需要同樣的呼叫端才活得起來，先加等於多一份死碼（第零守則）。
接線 ① 落地後再評估。

## 沒有做的事
⛔ 沒 push · ⛔ 沒 `content:build` / `skills:sync` · ⛔ 沒 gh 寫入 · ⛔ 沒動任何名下以外的檔。
