# GH#1099 —— `onlyOutsideOwnWrites` 只有 1/4 個執行點讀得到

**lane 報告 · 2026-09-07 15:35 · ⛔ 未 commit（依指示）**

---

## ① 前後對照（⭐ 量到的，⛔ 不是估的）

```
修之前  🔒 隔離區:鎖著 336 · 可寫 510 · 不存在 0 / 追蹤 846,正規化器專屬 508 份不上鎖,部分產物 2 份不上鎖
修之後  🔒 隔離區:鎖著 441 · 可寫 405 · 不存在 0 / 追蹤 846,正規化器專屬 403 份不上鎖,部分產物 2 份不上鎖
        ⇒ 鎖著 +105 · 可寫 −105 · 正規化器專屬 −105
```

`skillremake:json` 在 `sync-io.json` 裡**逐檔列名** **127** 份（⭐ 零 glob）：
`content/abilities` 91 · `content/champions` 16 · 其餘 20（各集合 `_index.json` ＋
`bundle.json` / `manifest.json` ＋ `docs/editor-contract` 2 ＋ 2 份 md）。

| | 修之前 | 修之後 |
|---|---:|---:|
| 那 127 份裡**可寫**（644） | **105** | **0** |
| 已經鎖著（別的作者也認領，例 `content:build` 寫 `bundle.json`） | 22 | 22 |

⚠️ 105 而不是 127 —— ⭐ 那 22 份**本來就有第二個作者**，所以舊行為在它們身上看不出來。
⛔ 「大部分是鎖著的」曾經是這個缺陷最好的偽裝。

---

## ② 四個執行點各改了什麼

⭐ **判準搬進一支新檔**：`tools/parallel-gates/normalizer_rules.py`
（`author_steps(path, claimants, entries)`）—— 形狀照抄 GH#1097 的 `marker_regions.py`：
⭐ 讀同一支判準、⛔ 不抄 regex、⛔ 不寫名單、讀不到 ⇒ **fail-closed 而且大聲**。

| # | 執行點 | 改了什麼 |
|---|---|---|
| 1 | `scripts/genguard.sh` | inline node **只算「誰認領」**（印 `HIT\t<csv>\t<fieldNote>`），分類改呼叫 `normalizer_rules.py`。python 非零 ⇒ 印「genguard 自己壞了」＋ `RC=1`，⛔ 不靜默放行 |
| 2 | `scripts/preserve-before-overwrite.py` | `_normalizer_steps()` → `_normalizer_rules()`（`importlib` 載模組，比照既有的 `_marker_mod()`）。載不到 ⇒ **每個認領者都當作者＝擋** ＋ stderr 出聲 |
| 3 | `scripts/product-quarantine.sh` | 移除 inline `NORMALIZERS` / `_normalizes()`，`has_author()` 改呼叫同一支。載不到 ⇒ **全部鎖** ＋ stderr 出聲 |
| 4 | `packages/shared/src/content/import/editorSource.ts` | ⚠️ **見下面「AC3 的例外」** |

### ⛔ 刻意**不**改的兩處（都給得出理由）

- **`tools/parallel-gates/reconcile.mjs`** —— 它的 `classify()` 第一行就是
  `if (isMine(p)) continue` ⇒ 它問的**本來就是**「自己 writes **之外**」那一半
  ⇒ `onlyOutsideOwnWrites` 在那裡的正確行為是**照樣當正規化器**。
  ⭐ 接上去反而會把 `skillremake:json` 合法的 `castTimeSec` 就地改寫誤報成越界寫入
  （＝ 一條會誤報的閘，而它會被人放寬）。
- **`apps/platform/internal/submissions/ownership.go`** —— 它**已經**讀對了
  （`if x.OnlyOutsideOwnWrites { continue }`），而且 Go 叫不到 python。

### ⭐ 順帶找到的第五份手抄實作

`packages/shared/src/ops/laneYQuarantineAgreesWithGenguard.test.ts` 的 ④ 自己有一份
分類邏輯（同樣漏 `onlyOutsideOwnWrites`）⇒ 改成呼叫 `ownershipOf()`。
⛔ 不改它的話這條閘會在修好之後**紅 127 份**（它會把真產物讀成「正規化器專屬卻唯讀」）。

### ⚠️ 出貨影響：`pnpm skillremake:json:no-build` 從今天起會吃 EACCES

`batch1.py` 的三個入口裡只有 `skillremake:json`（走 `genrun.sh` ⇒ 解鎖）與 `:check`（唯讀）
是安全的；`:no-build` 直接跑 python ⇒ ⭐ 那正是隔離區要擋的通道。
⭐ 這**不是新的**：2026-09-02 之前那 127 份也是鎖著的。
⭐ 而 `--doctor` 的**提示層**已經逐支指名它（「`X:check` 的兄弟中推導收不到的 20 支」）
⇒ ⛔ 不必也不該把它加進豁免表；正解是包成 `genrun.sh`（⛔ 不在本票範圍）。
⚠️ 我 grep 過 CI／`sync.mjs`／`ship`／`skills:*`：**沒有任何自動化流程呼叫它**。

---

## ③ AC 逐條

| # | AC | 結果 |
|---|---|---|
| 1 | `genguard content/abilities/godie-e002.w.json` ⇒ 擋下並指名擁有者與重生成指令 | ✅ **exit 1**，「🚫 … 是產生器 **skillremake:json** 的產物 … `bash scripts/genrun.sh skillremake:json`」 |
| 2 | `product-quarantine.sh status` ⇒ 那 126 份在**鎖著**那一欄 | ✅ 127/127 現在 0 可寫；鎖著 336→**441** |
| 3 | `editorSource.ts` 的行為**不變** | ⚠️ **有一個例外，見下** |
| 4 | 四個執行點對同一份檔給同一個判定，一條測試逐檔比對 | ✅ 846 份檔全掃，0 不一致 |

### ⚠️ AC3 的例外（⭐ 我改了它，理由與範圍）

`onlyOutsideOwnWrites` 的處理**一個位元組都沒動**（票說它今天是對的 —— 對）。
⭐ 但 AC4 的全掃第一次跑就找到 **2 份**不一致，而**兩份都是 TS 錯**：

```
docs/_data/ap-conversion-applied.json   TS=[]  py=[apconv:build]
docs/_release/ggd-board.html            TS=[]  py=[board:build]
```

⇒ 根因是**同一個病的第二格設定**：`ownershipOf` **完全沒有讀 `only`**
⇒ 對這兩份**真產物**回 `normalizer-only`＝「編輯器可以直接寫」。
（`normalizers.json` 自己的 reason 逐字說了它們是產物：apconv 那份是「它自己整份 emit 的清單」、
board 那份是「整份重算的產物，照樣鎖」。）

⭐ 我補了 `only`（＋ `:raw` 後綴對齊，與 `ownership.go` 一致），⛔ 沒有動 `onlyOutsideOwnWrites`。
- **範圍**：影響**恰好那 2 份**，⛔ 兩份都不是 `content/` 集合文件 ⇒ 編輯器實際路徑上零影響。
- **驗證**：`editorSource.test.ts` 7/7 ＋ `apps/content-api/src` 全綠。
- ⚠️ 選擇的理由：把它留著＝我的「四者一致」閘要**自我豁免兩份**，
  ⭐ 而一條為了讓自己綠而放寬判準的閘，正是這張票在修的東西。

---

## ④ 守衛與突變

**新守衛**：`packages/shared/src/ops/normalizerRuleAgreesAcrossEntrypoints.test.ts`（3 條）

| 段 | 驗什麼 |
|---|---|
| ① **全掃** | 846 份被認領的檔，TS `ownershipOf` ↔ python 判準**逐檔**比對作者集合 |
| ②③ **校準＋接線** | ⭐ 先**推導**出「拿掉 `onlyOutsideOwnWrites` 就會改判」的那些檔（⛔ 不寫死路徑），斷言 **>0**（⛔ 否則③是空的），再拿第一份把 `genguard.sh` 與 PreToolUse hook **真的跑起來** |
| ④ **隔離區** | 沙盒 io/normalizers：帶旗標的步驟在**自己 writes 裡**⇒ 鎖；純正規化器認領 ⇒ ⛔ 不鎖 |

⭐ ② 是量尺自證（`calibrate()` 的形狀）：⛔ 沒有它，③ 可能在一個沒有鑑別力的檔上永遠綠。
⭐ ③ **跑真的執行檔**，⛔ 不是 grep 原始碼字串（失敗形態⑥）。

**突變（一條，承重的）**：`scripts/genguard.sh` 裡新接上的那一行
（`AUTHORS_RAW=$(python3 tools/parallel-gates/normalizer_rules.py …)`）→ `AUTHORS_RAW=""`：

```
× ②③ 校準＋接線
  → genguard 沒接上判準：content/abilities/godie-e002.e.json 是 skillremake:json 的產物
    而它說「不擋你」（GH#1099）
```

✅ 紅並**指名 `skillremake:json`**。用 `python3 scripts/edit-or-die.py` 施加與還原
（⛔ 全程沒有 `git checkout <檔>`）。

⚠️ 突變當下順帶量到一件事：那一版 genguard 的**離開碼仍然是 1** ——
因為 NORMALIZER 分支的「第二把量尺」（`! -w "$p"`）會叫。
⭐ 所以斷言比對的是**訊息內容**，⛔ 不是只看離開碼：一個只看 exit code 的斷言在這次突變下**會是綠的**。

---

## ⑤ 離開碼

| 指令 | 結果 |
|---|---|
| `npx vitest run packages/shared/src/ops packages/shared/src/content/import apps/content-api/src` | **1 failed / 240 passed**（241 檔）—— 唯一紅是 `skillSpecFresh.test.ts`，⭐ **與本票無關**（見下） |
| `pnpm typecheck` | **1 fails / 17 passes** —— 唯一紅是 `apps/editor/src/preview/forgeRealCast.test.ts`（別條 lane 的柵欄），⭐ 與本票無關 |
| `pnpm skills:check` | **exit 1**，在第 2 支 `spec:check` 就短路 —— ⭐ 同一個 `skillSpecFresh` 的根因 |
| `bash scripts/genguard.sh content/abilities/godie-e002.w.json` | **exit 1** ✅（AC1） |
| `bash scripts/product-quarantine.sh --doctor` | **exit 0**（入口 0 · 孤兒 0 · 未分類 0 · 欄位級孤兒 0 · **總計 0**） |

### ⭐ 那條紅**不是我造成的** —— 兩個獨立證據，⛔ 不是「我覺得應該不是」

`spec:check` 說 `docs/editor-contract/ggd-ability-prose.json` 過期。

1. **把鎖拿掉再跑一次，它照樣紅**（`unlock --step skillremake:json` → `pnpm spec:check` → 仍 exit 1）
   ⇒ ⛔ 與 chmod 444 無關。（跑完已 `lock` 復原：鎖著 441 · 可寫 405。）
2. **`gen_spec.ts` 的輸入是 Zod schema／註冊表／`content/**`** —— ⛔ 我一個都沒動。
   而工作區裡有**別條 lane 未提交的內容改動**：
   `content/config/ap-coefficient.json` · `content/config/ugc.json` ·
   `packages/shared/src/content/schema/config/{apCoefficient,ugc}.ts`（＝ ap-coefficient lane）。

⇒ ⭐ **修法是主 session 統一跑 `pnpm skills:sync`**（⛔ 我依指示沒有跑）。

⚠️ 順帶一個量尺陷阱：我第一次跑 `skills:check` 包了 `timeout`，
⛔ 而 macOS 沒有那支 ⇒ **exit 127，log 只有一行 `command not found`**，
而背景任務通知回報 **exit 0**（最後一個指令是 `tail`）——
⭐ 抓到它的是我自己寫進 log 的 `SKILLS_CHECK_EXIT=`，⛔ 不是通知。已記進守則犯錯帳本。

---

## ⑥ 動過的檔案（逐檔列名 · ⛔ 未 commit、未 `git add`）

```
新增  tools/parallel-gates/normalizer_rules.py
新增  packages/shared/src/ops/normalizerRuleAgreesAcrossEntrypoints.test.ts
改    scripts/genguard.sh
改    scripts/preserve-before-overwrite.py
改    scripts/product-quarantine.sh
改    packages/shared/src/content/import/editorSource.ts
改    packages/shared/src/ops/laneYQuarantineAgreesWithGenguard.test.ts
```

⭐ 另有**檔案權限**改動 105 份（644 → 444），全部是 `skillremake:json` 的產物，
由 `bash scripts/product-quarantine.sh lock` 施加 —— ⛔ 內容一個位元組都沒動
（那 127 份在動手前 `git status` 全部乾淨，⛔ 沒有別條 lane 正在編它們）。

⛔ **沒有**碰：`content/**`（除了 mode 位元）· `apCoefficient*` · `apps/editor/**` ·
`tools/parallel-gates/sync-io.json`（量出來的，⛔ 不手改）· `normalizers.json`（⛔ 這一票不需要改它）。

---

## ⚠️ 留給主 session 的兩件事（⛔ 我沒有動，也沒有開票）

1. **`scripts/lane-plan.sh:93`** —— **第五個**讀 `normalizers.json` 的地方，
   而它 `{n["step"] for n in …}` ⇒ ⛔ `only` 與 `onlyOutsideOwnWrites` **兩格都忽略**
   ⇒ 它算出來的「🔒 全域鎖」少了 `skillremake:json` 的 127 份與那 2 份 `only` 產物
   ⇒ ⭐ 兩張都動技能產物的票會被判成「可以並行」。
   ⛔ 不在本 lane 的柵欄裡（我的柵欄只列了 genguard / hook / quarantine 三支）。
   ⭐ 修法是現成的：`sys.path` 掛上 `tools/parallel-gates` 再呼叫 `normalizer_rules.author_steps()`。
2. **`spec:check` 的紅** —— 需要一次 `pnpm skills:sync`（⛔ 併行時只能主 session 跑）。
