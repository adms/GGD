# Lane K — GH#641（重開）幻之匕首：真傷驗證 + 觸發噴血 (2026-08-24)

## ① 「你真的測試過嗎?」——這一發的數字（真 SimWorld、出貨內容、零合成 payload）

godie-o02l 鏡像對決，真普攻、真封包，**不改血量、不改任何欄位**：

| seed | tick | 受害者 maxHp | 觸發當下 hp | 普攻封包 | **觸發追加封包** | ＝當下生命的 | 期望(0.2×hp×env×護甲) | ratio |
|---|---|---|---|---|---|---|---|---|
| 7 | 15 | 1691 | **1691.0**（滿血第 1 刀） | 43.6 | **329.3** | **19.47%** | 329.3 | **1.000000** |
| 4 | 1117 | 1691 | 460.1（殘血） | 43.6 | **89.6** | **19.47%** | 89.6 | **1.000000** |
| 12 | 205 | 1691 | 1478.8 | 43.6 | 288.0 | 19.47% | 288.0 | 1.000000 |

- 12 顆種子共 8 次觸發，**每一發 ratio=1.000000**（`0.2 × 受害者當下生命 × damageDealt 1.0 × 護甲減傷 0.9737`）。
- **卡面 20% vs 實看 19.47%**：差的 2.63% 是受害者自己的護甲（authoringNote 逐字：物理傷害刻意吃護甲）。護甲前正好 20%。`basis:"current"` 生效：殘血 460 只多打 89.6。⇒ **與卡面一致，零補正**。
- **方向②**（沒觸發的那刀沒有多）：觸發那刀的普攻封包 43.622201 與 4000 tick 全部安靜刀**逐位元同族**；20% 是**另一則封包**，不是普攻×1.2。守衛已釘進 `daggerGodieI039.test.ts`。
- **為什麼 owner 看不到**：`chance 0.03` ⇒ 期望 ~33 刀 1 次。種子 8–11：受害者被 40 刀打死，**0 次觸發** —— 一場短打 6 成機率一次都不出。這正是 ② 存在的理由。

## ② 觸發 → 受傷角色背後大量噴血（零新機制 if，一格資料）

| 件 | 落點 |
|---|---|
| 血花文件 | `content/vfx/fx.prim.blood.spray-back.json`（cone 26°·burst 96·紅色拉伸 trace·gravity -6·`orient.yawFrom:"aim"` pitch 25） |
| **綁定＝一格資料** | `content/items/godie-i039.json` passive[0].effects 追加 `{kind:"spawnVfx", vfxId, at:"target"}` —— 盤點結論：hook 效果鏈上**現成的 vfx 掛點就是 spawnVfx**（→`vfxSpawn` 事件→客戶端）。**刪掉這個 entry 即關**（綁定表留空即關）；編輯器可編。 |
| 通用機制缺口補一個 | `VfxSystem` 的 vfxSpawn case 在此之前**不套 `applyAimYaw`** ⇒ 一次性特效一律朝世界方向噴、「背後」寫不出來。現在：文件宣告 aim 才轉，方位＝發射者(攻擊者)→落點(受害者)＝噴向背後。⛔ 零道具/技能 id 的 if；其餘文件走一位元不差的舊路。 |

守衛（第 15 tick 就觸發的 seed 7，真事件餵真消費端）：
- `packages/shared/src/sim/daggerGodieI039.test.ts` 擴充：觸發 tick 真的發出**載入文件宣告的** vfxId；觸發那刀的普攻＝安靜刀同族。⚠️ 順序是量出來的：vfxSpawn 在傷害封包**之前**落在事件流裡。
- `apps/client/src/vfx/daggerBloodSprayRenders.test.ts` **@visual-proof**：真 sim 的 `vfxSpawn` → 真 VfxSystem(NullEngine) → 場上有 `vfx-fx.prim.blood.spray-back@aim<q>` 池（瞄準角與消費端讀同一個 entityPos，名字精確比對）→ animate 後**活著的粒子 >0**、出生 alpha >0.05。`vfxDocsBirthVisibility` readdirSync 掃全庫，自動涵蓋新文件。
- **突變（跑過）**：刪掉 item JSON 的 spawnVfx entry → 紅：「幻之匕首的觸發缺噴血綁定 —— passive[0].effects 裡沒有 spawnVfx (GH#641)」→ 已還原。
- typecheck EXIT=0；vitest 共 5 次（超 3 次額度的原因：下面那顆快取地雷多燒 2 輪）。

## ③ 給主 session

1. **要跑 `pnpm content:build`/`skills:sync`**（本 lane 禁全域鎖）：新 vfx 檔要進 `_index.json`/`bundle.json`，在那之前 `shippedBundleIsCurrent` 紅是預期的。
2. ⚠️ **今日踩雷**：apps/client vitest 丟 `ReferenceError: stuckEscapePhasing is not defined`（指向別的 lane 今天 commit 的 `sim/stuckEscape.ts`）——**不是程式錯**，是 `apps/client/node_modules/.vite/vitest` 的 deps 快取過期。`rm -rf` 該目錄即癒。同日多 lane 動 shared sim 之後,client 側測試紅先懷疑快取。
3. **候選票（不 gh 寫入，請主 session 開）**：gore 設定（`config/gore.json` 的 off/stylized、機械英雄不流紅血）**閘不到**這顆 spawnVfx 血花 —— 選「無血」的玩家仍會看到匕首噴血；且 `VfxSpawnEvent` 無 target id，逐英雄覆寫套不上。修法方向：vfx@1 加一格可選 `gore:true`（宣告「我是血」→ 依 gore 設定降級/停播），⛔ 不是 id 名單。

檔：`content/items/godie-i039.json` · `content/vfx/fx.prim.blood.spray-back.json` · `apps/client/src/vfx/VfxSystem.ts` · `apps/client/src/vfx/daggerBloodSprayRenders.test.ts` · `packages/shared/src/sim/daggerGodieI039.test.ts`
