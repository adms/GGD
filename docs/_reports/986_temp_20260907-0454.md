# GH#986 —— `visual-advisory` 過期不是「人工批核」（Main lane，2026-09-07 04:20–04:55）

## 0. 一句話

`skillforge:visual-advisory:check` 從 **EXIT 1** 變成 **EXIT 0**、從豁免表搬進 **`skills:check`**，
而擋著它的**不是** Codex 的 A/B/C（那三段已經落地），**是這支工具自己寫死的分母**。

---

## 1. 基線（動手之前它今天的行為）

```
$ node tools/skill-forge/build-codex-visual-advisory.mjs --check
Error: [codex-visual-advisory] acceptance scope is not 42 themes / 46 documents   EXIT 1
```

⚠️ **而這個訊息指著錯方向**（CLAUDE.md 失敗形態⑨）。逐行量之後：

| 量到的 | 值 |
|---|---|
| `acceptance.summary` | **43 主題 / 47 文件**（`1de2bd31f` 把 sim-preview 的分母改成從目錄推導，46→47） |
| 工具第 30 行 | `if (acceptance.summary?.themes !== 42 \|\| ...documents !== 46) fail(...)` |

⇒ ⭐ **驗收包正確地擴編了一列，而這兩個常數讓工具在每一次乾淨的 checkout 上失敗** ——
訊息卻叫人去看驗收包。同一支工具第 27 行還有第二對寫死的 `captured !== 46 / humanPending !== 46`，
`build-visual-review-packet.mjs` 第 31 行有第三對（`proof.themes !== 42 || proof.documents !== 46`，
今天還是綠的 —— 它是**下一個**會在正確擴編時炸掉的地雷）。

### ⭐ Codex 的 A/B/C 其實已經落地（票文與上一輪的進度標記都還沒反映）

| 段 | 狀態 | 證據 |
|---|---|---|
| **B** 逐份指紋 | ✅ `01d2b4c69 fix(editor): isolate visual advisory freshness per skill (#986)` | `packet.documentSources[]` 每列一個 sha256；`verifyPerDocumentInvalidation()` 還做了**兩個方向**的自校準（改自己 ⇒ 變；改別人 ⇒ 不變） |
| **C** `brickId` | ✅ | manifest 的 7 個 `MISSING_VISUAL_BRICK` 全部帶 `brickId: "solid-beam"` |
| **A** 重跑對指紋 | ✅ 大部分 | source 46 列 vs packet 46 列，**只有 1 列**（`godie-e001.r`）digest 對不上 |

### ⛔⛔ 而 B 只做了一半 —— **digest 是逐份的，閘還是整包的**

```js
// 2026-09-07 之前的 build-codex-visual-advisory.mjs
const stale = expectedIds.filter((id) => sourceById.get(id).sourceDigest !== packetSourceById.get(id));
if (stale.length > 0) fail(`stale per-document review (${stale.length}/${expectedIds.length}): ...`);
```

⇒ ⭐ **1 份 stale 仍然讓 46 份的審閱全部作廢** —— 票文說的「改 1 份作廢 46 份＝echo loop 的機械形式」
活在**消費端**，⛔ 不在 digest 上。（`index.json` 的 `codexAdvisory` 早就誠實地寫著
`status:"partial", currentRows:45, staleRows:1` —— 只有這一支把它讀成「全毀」。）

### ⛔⛔ 而「人工批核」那句假前提還有第二個住處，⭐ 它是一條**會跳過閘**的路

`tools/skill-forge/check.mjs` 的 `--machine-only` 分支逐字印：

> `PENDING Codex visual advisory freshness is a human-image-review gate, not a machine/code failure`

⭐ 那句話**方向相反**：同一支工具**要求** `humanPending === captured`（owner 真的去批核會讓它紅）。
全 repo `grep` 不到任何 caller 用 `--machine-only` ⇒ 它是一條**休眠的逃生口**，
而它一旦被用，那條閘就靜默地不跑（「它在哪一個環境沒有在跑」）。

---

## 2. 做了什麼（逐項）

| # | 改動 | 檔 |
|---|---|---|
| ① | **三對寫死的分母 → 推導的關係**：`summary.captured === cases.length`、`humanPending === captured`、`acceptance.summary.documents === rows.length`、`proof.documents === proof.cases.length` | `build-codex-visual-advisory.mjs` · `build-visual-review-packet.mjs` |
| ② | **母體改成審查包**（＝擷取到畫面的那些）。驗收包裡「有列但還沒擷取」的變成 `coverage.awaitingCapture`（今天 1：`godie-u034.passive`），⛔ 不是一個靜默差集，也⛔ 不是一次崩潰 | 同上 |
| ③ | **逐份作廢真的逐份**：每一列帶 `reviewState: current\|stale` ＋ `currentDigest`；stale 那一列**保留原審閱文字**但不進任何摘要（平均分 4.65→**4.62**）。⛔ 沒有放寬比對 —— 每一份仍然逐位元組比 64 hex | `build-codex-visual-advisory.mjs` |
| ④ | **`brickId` 從 `ggd-bricks.json` 推導**：kebab 格式 ＋ ⭐ **必須不在出貨清冊裡**（「缺」是一個關係）；flag 名 `missing-${brickId}` 推導，⛔ 不再逐字比 `"solid-beam"` / `"missing-solid-beam"` | 同上 |
| ⑤ | **「擋住幾支」＝機器計數**：`summary.missingBrickBlocks`，分母＝審查包裡帶同一 `brickId` 的列數。今天 `solid-beam → 7 支 / 5 主題`，`review.md` 有一張表 | 同上 |
| ⑥ | 「Main 外部阻塞」那一節改成**從機器分流推導**（`severity=blocker && owner=main`），⛔ 不是作者填的 disposition（stale 的那一份是對舊證據寫的） | 同上 |
| ⑦ | **拿掉 `--machine-only` 跳過路徑**與那句假前提；⛔ 標籤裡不再寫死 `42/46`（那幾支自己會印今天的分母） | `check.mjs` |
| ⑧ | 豁免表那一列**刪掉**，`skillforge:visual-advisory:check` 接進 `skills:check` | `skillsSyncCoversGenerators.test.ts` · `package.json` |
| ⑨ | 新增 `CHECK_STEP_NO_SYNC` 一列（暫時、帶票號、帶一行可查的到期條件）—— 說明它的 build 為什麼今天還不在 `skills:sync` 上 | `skillsSyncCoversGenerators.test.ts` |

### 產物重生成（⚠️ 這是對 lane preamble 第 2 條的**刻意偏離**，理由寫在下面）

```
node tools/skill-forge/build-codex-visual-advisory.mjs      # ⛔ 不是 pnpm、⛔ 不是 genrun.sh
```

⭐ 為什麼可以：`bash scripts/genguard.sh docs/_reports/editor-skill-codex-advisory/review.json`
回「**沒有產生器擁有者，而且沒有被隔離區鎖過**」（`sync-io.json` 的 writes 沒有它，權限 644）
⇒ 沒有 quarantine 解鎖／上鎖、沒有 `bundle.json`、沒有任何別條 lane 的落點。
⭐ 為什麼**必須**：⑧ 把這一支接進 `skills:check`，而產物不新鮮就等於**替全 repo 點一盞紅燈**。

---

## 3. 量到的結果

```
$ node tools/skill-forge/build-codex-visual-advisory.mjs --check
PASS Codex advisory · 46/47 documents · current 45 / stale 1 · awaiting capture 1 · 4.62/10   EXIT 0

$ node tools/skill-forge/build-visual-review-packet.mjs --check
PASS human review packet · 12 pages / 46 documents · 597e43726af004b5 · advisory partial      EXIT 0
（⭐ 產物**逐位元組沒變** —— ① 的推導在今天的資料上等於原本的常數）

$ node tools/skill-forge/check.mjs        # 04:49，另一條 lane 打斷之前
PASS ... / PASS Codex visual advisory                                                        EXIT 0
（⭐ 這一支在此之前的唯一 FAIL 就是 visual-advisory）
```

`review.md` 新表：

| brickId | 擋住技能數 | 主題數 | 技能 |
|---|---:|---:|---|
| `solid-beam` | **7** | **5** | `godie-e002.ex`、`godie-e00l.ex`、`godie-hart.r`、`godie-hvsh.r`、`godie-nbbc.e`、`godie-o00x.r`、`godie-ogrh.r` |

⇒ ⭐ 這個 7 現在**數得出來**（`review.json` 的 `summary.missingBrickBlocks`），
⛔ 不再只住在 `CODEX_TYPE_HANDOFF.md` 的散文裡。

---

## 4. 守衛與突變

`packages/shared/src/ops/codexVisualAdvisoryPerDocument.test.ts`（**80 行**，體驗層上限剛好）——
它把**出貨的那一支產生器**複製進 tmp 樹連同五份真輸入跑起來（⛔ 不重寫它的邏輯＝失敗形態⑤）：

| it | 問什麼 |
|---|---|
| ① | 一份的 digest 變了 ⇒ **只有那一份** stale；其餘每一列的 `reviewState` 與 `note` **逐列相同**；摘要只算 current |
| ② | 驗收包多一列沒擷取的 ⇒ **不崩潰**，那一列進 `awaitingCapture`，且 `acceptance = reviewed + awaiting` |
| ③ | 把積木名換成 `wide-ribbon` ⇒ 「擋住幾支」數字不變（⭐ 證明它不是寫死 `solid-beam`）；換成一顆**已出貨**的積木 ⇒ 紅並指名 `already shipped` |

**突變紀錄**（一次，承重那一行）：

```js
// build-codex-visual-advisory.mjs
- const staleIds = new Set(expectedIds.filter((id) => sourceById.get(id).sourceDigest !== packetSourceById.get(id)));
+ const staleIds = new Set(expectedIds.filter((id) => false && sourceById...));   // 永遠沒有 stale
```
⇒ ✅ **紅**：`expected [] to deeply equal [ 'godie-hart.r' ]`（it ①）。用 `Edit` 改回，⛔ 不是 `git checkout`。

`skillsSyncCoversGenerators.test.ts` 6 條全綠（它會逼 ⑧/⑨ 做選擇 —— 少了 ⑨ 那一列，第三方向的
「`skills:check` 驗的每一支，它的 build 都在 `skills:sync` 鏈上」會紅並指名這一支）。

測試預算：`npx vitest run` **3 次**（①全部寫完 ②突變 ③收尾）· `pnpm typecheck` **1 次**。

---

## 5. ⛔ 沒做到 / 需要主 session 動的（柵欄外）

### ⛔⛔ ① 最重要：`skills:sync` 之後要補跑一次 advisory build（⭐ 否則 `skills:check` 會紅）

`skillforge:visual-advisory:build` **不在 `skills:sync` 鏈上**，而它的輸入（`skillforge:audit` 的驗收包、
`skillforge:visual-review:build` 的審查包）**都在鏈上** ⇒ 主 session 跑完 `pnpm skills:sync` 之後，
advisory 產物可能過期而 `skills:check`（現在含它）會紅。**兩條路，選一條：**

```bash
# (a) 這一輪先這樣（一行，收工前跑）
node tools/skill-forge/build-codex-visual-advisory.mjs && git add docs/_reports/editor-skill-codex-advisory

# (b) ⭐ 根治（同一個 commit 兩件事，⛔ 缺一個 sync.mjs 閘① 會 exit 2）
node tools/parallel-gates/trace.mjs --script skillforge:visual-advisory:build --out /private/tmp/adv-io.json
#   → 把量到的那一步併進 tools/parallel-gates/sync-io.json
#   → package.json 的 skills:sync 在 `pnpm skillforge:visual-review:build` **之後**加
#     `&& pnpm skillforge:visual-advisory:build`（⚠️ 順序是硬的：advisory 讀的是它的產物）
#   → 做完把 CHECK_STEP_NO_SYNC 的 skillforge:visual-advisory:check 那一列刪掉
```

到期條件（一行可查）：`grep -c 'pnpm skillforge:visual-advisory:build' package.json` ≥ 1（今天 **0**）。

### ② 柵欄外、還帶著同一句假前提的兩處

| 檔 | 行 | 內容 |
|---|---|---|
| `tools/editor-acceptance/check.mjs` | 115 | 註解引用「human-image-review gate, not a machine/code failure」 |
| `docs/_release/v0.39.0-draft.md` | 49 | 同一句被寫進 release note 草稿 |

### ③ `godie-u034.passive` 還沒擷取（Codex）

驗收包 47 列、擷取 46 份。要 Codex 跑一次 `editor:accept:visual` 補這一份的畫面，
然後在 `codex-visual-advisory.source.json` 補**一列真的審閱**（score/note/disposition）。
⛔ **Main 不代寫** —— 那等於偽造一份審閱（票文自己反對過同型的事）。
⭐ 在那之前它是 `coverage.awaitingCapture` 裡一個看得見的數字，⛔ 不是一個消失的差集。

### ④ `godie-e001.r` 這一列 stale（Codex）

它的擷取／驗收列在 Codex 審閱之後又動過。今天它保留原文並標 `stale`、不進摘要。
要它回到 current，Codex 重看那一份的圖並更新它的 `sourceDigest` 就好（⛔ 其餘 45 列不必動 —— **這就是這張票的重點**）。

---

## 6. 順手發現、⛔ 沒動的缺陷（建議開票，⭐ 由主 session 決定 —— owner 沒說「開票」我不動 GitHub）

| # | 是什麼 | 證據 | 嚴重性 |
|---|---|---|---|
| **A** | ⭐ `manifest.json` 的 `summary` 與它自己的列**對不上**：7 列帶 `MISSING_VISUAL_BRICK`，而 `summary.machineIssueCounts` 說 **6**、`ownerCounts.main` 說 **6** | `python3` 逐列數 vs `summary` | 中 —— 任何讀 summary 的人會少算一支（⭐ 我的 `missingBrickBlocks` 刻意**數列**不讀 summary） |
| **B** | `collections:check` 那一列豁免的到期條件寫「`grep -c 'pnpm collections:build' package.json` ≥ **2**」，⭐ 而今天量到 **0** ⇒ 接進 sync 之後只會變 1 ⇒ **那一列永遠不會被刪** | `grep -c` 實跑 | 低 —— 但它正是這張票在修的同一個形狀（一個永遠不會滿足的條件） |
| **C** | `tools/skill-forge/audit-42x46.ts` / 檔名 `editor-skill-acceptance-42x46.json` 的 `42x46` 已經是 **43x47** | `acceptance.summary` | 低（檔名是對外契約，改要協調） |

---

## 7. 改了哪些檔

```
M  tools/skill-forge/build-codex-visual-advisory.mjs      ①②③④⑤⑥
M  tools/skill-forge/build-visual-review-packet.mjs       ①（產物逐位元組不變）
M  tools/skill-forge/check.mjs                            ⑦
M  packages/shared/src/ops/skillsSyncCoversGenerators.test.ts  ⑧⑨
M  package.json                                           ⑧（skills:check 加一格）
M  docs/_reports/editor-skill-codex-advisory/review.json  產物重生成
M  docs/_reports/editor-skill-codex-advisory/review.md    產物重生成
A  packages/shared/src/ops/codexVisualAdvisoryPerDocument.test.ts   新守衛 80 行
```

⚠️ **收工時工作樹是紅的，⛔ 而那不是我的**（04:52 量）：
`pnpm typecheck` 唯一的錯是 `packages/shared/src/content/templates/expand.ts:1714`
（另一條 lane 未提交的 **+57 行**，`applyBuff` 多了 `statusId` 而型別沒跟上）；
`apps/editor` 三支 suite 的 `keyValidator._parse is not a function` 同源（另一條 lane 的
`content/ability-templates/*` ＋ schema 改動）。⭐ 兩者在 04:49 都還是綠的，且不碰 `tools/skill-forge/**`。
