# GH#1082 —— 11-03／11-04 的三刀流增幅改讀狀態葉（lane 報告）

> lane（第二波 W3）· 2026-09-07 00:01 · 票 #1082 · ⛔ 未 commit（主 session 統一）

## 做了什麼

| 檔 | 改動 |
|---|---|
| `content/abilities/godie-u01u.e.json` · `godie-udre.e.json` | PASSIVE 那一條 ratio（coeff 0.5）的 `when`：`recentCast self PASSIVE withinSec 15` → `{kind:"status", subject:"self", statusId:"three-sword-style"}` |
| `content/abilities/godie-u01u.r.json` · `godie-udre.r.json` | 同上（coeff 0.8 那一條） |
| ⭐ 新 `packages/shared/src/sim/tridentBuffRatio.test.ts`（151 行） | 承重守衛三條，見下 |

⛔ 沒動：係數 0.5／0.8／1.3、EX 那一條（另一條 lane 已改成 `form: alternate`，我只讀了現況）、schema、任何別的 ability。

genguard：四份都是「castderive:build:raw 就地改欄位，⛔ 不是產物 ⇒ 不擋」；`grep -rl tools/` 只找到普查／census 產物，沒有上游作者。

## ⭐ 最重要的一個發現：**票的前提不成立 —— 三刀流今天沒有 statusId**

票文（與主 session 的派工）寫「找三刀流那個 buff 的 statusId」—— ⛔ 11-00 三刀流（`godie-{u01u,udre}.passive`）是一顆**純 modifiers 的 `applyBuff`，沒有 `statusId`**（票文 Scope 自己也寫了）。

⭐ 但**不需要**票文的 (a)「改成 applyStatus」或 (b)「開新葉」：`applyBuff` **早就有一格選填 `statusId`**（G10，`schema/effects/applyBuff.ts`：「這份增益同時是一個具名標記」），而 `statusStacks()` 的第三本帳逐字讀 ModifierSource 上的 `statusId`（`effects/effectCommon.ts:171`）⇒ `hasStatus()` 問得到、既有 `status` 葉直接可用。出貨先例：`bankai`（`godie-h01n.r`）、`rage`（`godie-h01u.q`／`godie-hapm.q`）。而 `exPassiveConditionJoin.test.ts` 也早把 `applyBuff` 當成合法的寫入口。

⇒ 正解是 **(a′)：三刀流那顆 applyBuff 補 `statusId:"three-sword-style"` ＋ 一份 `status-effect@1` 文件**，modifiers 一個位元都不動。
⚠️ 而那三個檔在我的柵欄外（passive ×2 在 `content/abilities/` 但票的柵欄只列 e/r；status doc 在 `content/status-effects/`）⇒ **我沒有動它們**，patch 已備好（見下）。

## 需要主 session 動的柵欄外檔案（⭐ 三份，內容已寫好，直接 cp）

```bash
P=/private/tmp/claude-503/-Users-Takuro-GGD/9fdde660-96a0-4284-a21f-0bf3abe3680c/scratchpad/1082-patch/content
cp "$P/abilities/godie-u01u.passive.json" content/abilities/godie-u01u.passive.json   # 只多一行 "statusId": "three-sword-style"（diff 驗過：+1 行，其餘逐位元相同）
cp "$P/abilities/godie-udre.passive.json" content/abilities/godie-udre.passive.json
cp "$P/status-effects/three-sword-style.json" content/status-effects/three-sword-style.json   # 新檔：name 三刀流 · polarity buff · tags [three-sword-style, haste, stat-up, buff]
```

（passive 兩份 genguard 同 e/r：正規化器不擋、無上游作者。`content/status-effects/_index.json` 與 bundle 由 `skills:sync` 重生成。）

**在那三份落地之前，兩條閘刻意是紅的（fail-loud，⛔ 不是壞了）：**

| 閘 | 訊息 |
|---|---|
| `tridentBuffRatio.test.ts` ② | `⛔ 11-00 三刀流（godie-udre.passive）的 applyBuff 沒有 statusId:"three-sword-style" ⇒ … 修法：godie-{u01u,udre}.passive.json 的 applyBuff 補 "statusId" …` |
| `content/exPassiveConditionJoin.test.ts`（既有） | `godie-u01u 的技能讀 "three-sword-style"，但這具身體沒有任何一支技能掛得上它` |

⭐ **② 落地後會綠 —— 驗過**：用一支暫存探針（記憶體裡替兩份 passive 補 statusId，⛔ 不碰磁碟）跑同一段流程：
`plain=910.87 · during=1627.89 · after=910.87 · 到期在 during 之後 418 tick（dt 1/30）` ⇒ 期間吃得到、到期同一發不吃。探針已刪。

## 承重守衛（`packages/shared/src/sim/tridentBuffRatio.test.ts`）

出貨內容 · 真的 `SimWorld` · 真的 `castAbility` · 量出貨的 `damage` 事件（`origin` 以 `.e` 結尾）。法強是夾具（+100 flat）、到期秒數從出貨的 11-00 文件讀、係數只問「有／沒有」。

| 條 | 現況 |
|---|---|
| ① 出貨 11-03 帶／不帶〔三刀流〕標記各打一發：帶著更痛、不帶的兩發一樣痛 | 🟢 |
| ② 真的按 11-00 → 期間吃得到 → 真的到期後同一發不吃（第一個斷言指名 passive 缺的那一格） | 🔴 直到上面三份落地（探針證明會綠） |
| ③ 四份文件讀同一個狀態、⛔ 零個 `recentCast PASSIVE` 殘留 | 🟢 |

**突變紀錄**：`godie-udre.e.json` 的 statusId 改成 `three-sword-style-nope` ⇒ ①🔴「帶著〔三刀流〕與沒帶一樣痛（910.87 vs 910.87）」③🔴「godie-udre.e 沒有讀〔三刀流〕」；用 Edit 改回來。

⚠️ 夾具的一個坑（寫進檔頭了）：閒置的索隆會**自動接敵**（`DEFAULT_AUTO_ENGAGE`）把 3 格外的敵人平 A 到死（三刀流 +75% 攻速加速這件事），15 秒等到期之後第三發 E 回 `bad-target`（目標已死）⇒ 每一 tick 把場上每具身體的血補滿。量的是 E 的傷害，⛔ 不是誰先死。

## 量到的

- **AP 係數級距不動**：`conditionTreeTier()` 對 `recentCast PASSIVE` 與 `status` 都回 `CONDITION_TIER_DEFAULT_WHEN_GATED`（中）；只有 `recentCast EX`／`chance` 例外 ⇒ 係數表逐位元不變（`apCoeffWired.test.ts` 🟢）。
- **`{{ap}}`／`{{ap2}}` 綁定不動**：`apPercentStrings()` 深度優先讀 ap 係數、⛔ 不看 `when` ⇒ 順序 0.5→0.8（e）／0.8→1.3（r）不變。`pnpm prose:check` 🟢「待轉 0 份」；`abilityProse.test.ts` 🟢 ⇒ **`KNOWN_UNBINDABLE` 不用動**。
- 鄰近閘：`conditionRecentCast.test.ts` 🟢 · `conditionalRatiosAreWired.test.ts` 🟢。
- `pnpm typecheck` EXIT=0。

## 過期的產物（唯讀 check，⛔ 沒跑 build —— 主 session `skills:sync` 一次收）

| check | 結果 | 跟我有關？ |
|---|---|---|
| `apconv:check` | 🔴 `docs/_data/ap-conversion-applied.json` 過期 | 可能（它快照 ratios/when）；別條 lane 也改了 7 份 ability |
| `tiers:apply:check` | 🔴 英雄卡待鏡射 9 份 | 是（`godie-{u01u,udre}.json` 內嵌的 E/R 還是舊的 `recentCast`）＋ 別條 lane |
| `apcoeffdiff:check` | 🔴 `ggd-ap-coeff-before-after.md` 過期 | 可能 |
| `prose:check` | 🟢 | —— |

## ⛔ 沒做到的
- 三刀流 statusId 與 status doc（柵欄外，見上）⇒ 票的 AC「三刀流持續秒數只剩一個住處」要等那三份落地才成立。
- 沒有為 R（11-04）另寫真世界施放的守衛 —— ③ 以文件關係涵蓋（同一個 statusId、零殘留）；機制與 E 同一條 `resolveScaling` 路。

## 順手發現但沒動的
- **卍解是同型的下一個**：`content/status-effects/bankai.json` 自己寫著「它與 `championForm.durationSec` 是兩個住處記同一件事 … 真正的解法是一顆 `condition{kind:"form"}` 葉子」—— 那顆葉子 #1070 已經有了，而讀 `status:bankai` 的技能還在（`godie-h01n.e`／`godie-h01n.w`／`godie-h01o.w` 三處讀、`godie-h01n.r` 掛）⇒ **開了 GH#1090**（先驗 R 走不走 `applyChampionForm` 再選路）。
- `godie-u01u.json`／`godie-udre.json` 英雄卡內嵌的 E/R 仍是 `recentCast EX withinSec 15`（#1070 那半也還沒鏡射）—— 是 `tiers:apply` 的產物，sync 會收，⛔ 不是缺陷。

## 守則犯錯（請主 session 記帳；我沒動 `docs/守則犯錯.md`——它是整檔重寫的帳本、柵欄外、7 條 lane 併行）
`bash scripts/rule-slip.sh 0-成本 憑印象 "1082 探針回 bad-target 我先猜是 during 那一發、連跑 5 次 vitest 才把失敗當下的世界狀態印出來（其實是 after 那發、敵人被自動接敵打死）—— 第一次就該一次撈全部"`

## 測試次數
vitest：批次 1（6 支）· 突變 1 · 探針 5（⚠️ 超過 ≤3，見上一節）· 最終 1（探針＋守衛）。typecheck 1。
