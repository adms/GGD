# D9 · 世界終結 12 具冰塊全靜音 + 理想鄉 R 零回饋 —— 完整報告（2026-08-23）

兩條 lane 各留了一個「R 槽鏡射＝柵欄外」的缺口。這條 lane 有柵欄，兩個都收掉了。

---

## ① 42-04 世界終結：12 具冰塊出聲了

### 缺的是**兩格欄位**，⛔ 不是一個機制

`spawnModelFx` 的聲音通道（GH#605）在 2026-08-23 已經整族做完了：schema 有
`soundKey` / `arriveSoundKey`、sim 的 `payload` 帶得出去、客戶端 `vfxSound.ts` 收得到、
`audio-map` 的政策開關也在。⇒ 42-04 靜音**不是引擎缺一塊**，是那兩支技能文件沒填。

⭐ 這正是第〇·五守則要的形狀：**引擎做機制、JSON 做技能**。這一票的落地是
**四個檔各兩行**，⛔ 沒有動任何一行程式。

### 填了什麼（逐格，含理由）

| 格 | 值 | 為什麼是這一個 |
|---|---|---|
| `soundKey` | `magicIce` | 12 具大冰塊**噴出來**那一刻。它是冰系魔法音；`w3xAbilityArt` 認定這一支的家族是 **frostnova**（`jass-literal` 從 `AddSpecialEffectLocBJ(..., "frostnova.mdx")` 撈出來的），冰是它的身分 |
| `arriveSoundKey` | `wc3.gluescreenmeteorhit1` | 12 具**砸到地上**那一刻。重量級撞擊；`cooldownMs 1200 / maxConcurrent 1` ＝ 出貨音效表本來就是替「一次施放一發的大招落點」設定的 |

⭐ **兩個 key 都是 `content/config/audio-map.json` 上既有的列**，資產也都在
（`content/assets/audio/sfx/lab/magic-ice.mp3` · `content/assets/audio/wc3/gluescreenmeteorhit1.wav`）。
⇒ ⛔ 沒有新增音效表的列（那是柵欄外），⛔ 沒有新增資產。

⚠️ **`arriveSoundKey` 一次施放只響一發**，⛔ 不是 12 發 ——
`sim/effects/spawnModelFx.ts` 取整組實例裡走最久的那一具當 `arriveDelaySec`，
而 radial×12 十二具同時落地 ⇒ 一聲重擊對得上畫面。（12 發會是音爆，⛔ 不是音效。）

### ⛔ 原作**沒有**替它宣告聲音 —— 這兩格是我挑的

`tools/w3x-import/out/VFX_SOUND_JOIN.json` 的 `abilityDeclaredSounds` 裡
**一筆 frostnova / n003 都沒有** ⇒ 🪜 階梯第 3–5 層在這一題上是空的。
⇒ 依 owner 常設指令「**沒做完以前別問我了自己判斷 但是留後台開關可以簡易 rollback**」，
我自己挑了，並且**兩層 rollback 都不需要部署**：

| # | 怎麼換／怎麼關 | 要不要部署 |
|---|---|---|
| ① **換一個音** | 技能 JSON 改那一格的字串（`content/` 是 live bind-mount） | ⛔ 不必 |
| ② **整族靜音** | `content/config/audio-map.json` 的 `spawnModelFx` 政策格（`schema/config/audioMap.ts:90`：false ＝ `soundKey`/`arriveSoundKey` 兩格都不播） | ⛔ 不必 |

⇒ ⛔ **沒有新增第三個住處**：這兩格本來就是後台可編的內容欄位，
第一守則的「三個住處」在 GH#605 就已經齊了。

### 🚨 主 session 要收（第二件）：這兩個字串**應該搬進模板**，⛔ 不是留在四個節點上

⚠️ 我開工到一半，**另一條 lane 就地改了 `packages/shared/src/content/modelFxPreset.ts`**：
新增 `SOUND_FIELDS = ["soundKey", "arriveSoundKey"]` 並無條件併進 `fillOne()`，
同時在 `content/ability-templates/tpl-beam-roll.json` 加了兩格 `params` 預設值，
說明逐字寫著「⭐【聲音也住這裡】…補在這裡**一格解四支**，⛔ 不是逐支填」。

⇒ ⭐ **那條 lane 是對的，而它只做了 `tpl-beam-roll`。**
`content/ability-templates/tpl-radial-burst.json` 到現在**還沒有**這兩格 ⇒
42-04 的聲音現在住在**四個節點**上（本體 ＋ 變身態 ＋ 兩份 champion 鏡射），
而第〇·四守則說的正解是**一個住處**：模板的 `params`。

```jsonc
// content/ability-templates/tpl-radial-burst.json 的 params 尾端補兩格
"soundKey":       { "type": "docRef", "default": "magicIce",                 "optional": true },
"arriveSoundKey": { "type": "docRef", "default": "wc3.gluescreenmeteorhit1", "optional": true }
```
⇒ 然後把我加在**四個檔**上的那兩行**刪掉**（節點自己寫下的值永遠贏 ⇒
留著它們，模板那兩格就變成「填了卻沒有人讀」——正是那條 lane 自己在文件裡警告的形狀）。

⛔ **我沒有自己搬**：`content/ability-templates/` 在這條 lane 的柵欄外，
而且那條 lane **此刻正在寫它**（`tpl-beam-roll.json` 與 `_index.json` 都是髒的）——
兩條 lane 同時寫同一個目錄正是今天已經發生過三次的那個事故。
⭐ 而**先落在節點上是安全的**：搬進模板是「刪四行、加兩格」，
⛔ 反過來（今天不出聲、等模板那一票）就是 12 具冰塊**再靜音一天**。

### 🚨 主 session 要收：**豁免現在是紅的，請把那兩列刪掉**

```
packages/shared/src/content/schema/effects/modelFxStagingContract.test.ts:102
const FENCED_OUT = new Set(["godie-n003.r", "godie-n01g.r"]);   ← ⛔ 整行刪掉
```

實測（`npx vitest run`，2026-08-23 18:44）：

```
× ② 每一支帶多實例演出的技能都出得了聲
  → 這幾支已經有聲音了 —— 把 FENCED_OUT 裡的那一列刪掉:
    expected [ 'godie-n003.r', 'godie-n01g.r' ] to deeply equal []
```

⭐ **這就是 B2 lane 當初寫「補齊即紅」要的那一聲**，⛔ 不是一個新缺陷 ——
那個豁免自己帶著到期日，而到期日就是今天。
⛔ 我**沒有自己動那個檔**（它在這條 lane 的柵欄外）。
刪掉那一行之後，第二條斷言（`stale`）就空了，同一支測試全綠。

⚠️ 順帶：豁免拿掉之後，那兩支會被**第一條**斷言（`silent`）接手 ——
而它們現在兩格都有，所以⛔ 不會換一個地方紅。

---

## ② 理想鄉 R 反彈成功零回饋 —— 缺的只剩**鏡射的另一半**

### 進場先量：B3 lane 在我開工的**三分鐘前**把它做掉了

`daf72473`（2026-08-23 18:41:30）「反彈成功的回饋掛錯了技能 —— 它住在追打上，
而反彈是 R 做的」把四個回饋節點從 `godie-e002.ex` **搬**到 `godie-e002.r`。
⇒ ⛔ 我**沒有重做**，也⛔ 沒有回捲任何一行。

### ⛔ 但那一手只做了鏡射的一半

`content/champions/godie-e002.json` 的 `abilities.R` **沒有跟著搬** ——
它的 hooks 還是只有 `['onDamageTaken']`。

| | standalone `godie-e002.r.json` | embedded `champions/godie-e002.json` |
|---|---|---|
| `onDamageTaken` | ✅ | ✅ |
| `onReflectSuccess` | ✅（`daf72473` 加的） | ⛔ **沒有** |

⚠️ **這一半不是裝飾**：`abilityMirror.test.ts` 的檔頭逐字寫著它為什麼致命 ——
`fillGaps` 只補 standalone 缺的鍵，所以**比賽裡看不出來**，
而 `apps/editor/src/preview/PreviewController.ts` 帶 `overrideAbilities: true`
**整份**渲染內嵌那一份 ⇒ 編輯器預覽、後台內容管理、codex browser 讀到的
是一支**沒有反彈回饋**的 20-04。這正是 #79 那一次 192 格漂移的形狀。

⇒ 我把四個節點逐位元組補進 `champions/godie-e002.json` 的 R。
驗過三對現在只差 `schema`（唯一被 `STANDALONE_ONLY_OK` 認可的一格）：

```
godie-e002 R diff fields: ['schema']
godie-n003 R diff fields: ['schema']
godie-n01g R diff fields: ['schema']
```

### ⛔ 沒有複製第二份演出（任務書點名的那一點）

任務書逐字：「⭐ 正解是讓那套演出**掛在「反彈成功」這個事件**上（B3 已經把表的鍵做成
**演出文件 id**），⇒ R 只要把 `spawnVfx.vfxId` 指到同一列就好，⛔ 不是複製一份演出」。

⭐ **落地就是這個形狀**：R 的節點是 `spawnVfx { vfxId: "fx.avalon.reflect-burst" }`，
而 `apps/client/src/vfx/reflectArcBurst.ts` 的 `REFLECT_ARC_CUES` 表**鍵就是那個 id**
⇒ 七彩閃電是**查表查到的**，⛔ 不是第二份演出文件、⛔ 不是 `VfxSystem` 裡多一個 `case`。
第三支反彈技能要這套演出的成本仍然是「在自己的 JSON 指到那一列」。

---

## ③ ⛔ 順手發現，**沒有當場修**（第零守則⑧）—— 請 owner 排序

### `godie-e00l`（舊版 Saber）：EX 掛著一條**永遠不會觸發**的 hook

| 檔 | 現況 |
|---|---|
| `content/abilities/godie-e00l.r.json`（同樣叫「20-04 Avalon-永恆的理想鄉」） | `effects` 只有**一個 `damage` 節點**，⛔ 零反彈機制 |
| `content/abilities/godie-e00l.ex.json` | `passive.ranks[0].hooks` 有 `onReflectSuccess` ＋ 同樣那四個回饋節點 |

⇒ 這位英雄身上 `reflectDepth > 0` 的封包**生不出來** ⇒ 那條 hook 是死的，
而它的卡面說明逐字寫著「『永恆的理想鄉』[反彈]成功時發動」——
**第一·五守則的形狀：卡片上有一句不會發生的話。**

⛔ **沒有當場修**，因為兩條出路都是**設計裁決**，⛔ 不是接線：
(a) 替 `e00l.r` 補上反彈（＝新增一個機制到舊版英雄身上）
(b) 這隻整個退休（🪜 階梯上 `godie-e002` 是 `owner-spec`、`e00l` 是 `w3x-import`，
    第 1 層贏第 5 層 ⇒ 多半是它該下架）
⭐ 而「知識不可以無聲消失」：不論選哪一個，被取代的那一份要另存
（`docs/legacy/_w3x-fidelity-superseded.md`）。
（B3 lane 的報告 §5③ 已經記過同一件事，這裡只是加上**卡面說謊**這一層。）

---

## ④ 動到的檔（逐檔）

```
content/abilities/godie-n003.r.json   +2 · soundKey / arriveSoundKey
content/abilities/godie-n01g.r.json   +2 · 同上（變身態）
content/champions/godie-n003.json     +2 · R 槽鏡射
content/champions/godie-n01g.json     +2 · R 槽鏡射
content/champions/godie-e002.json     +42 · R 槽鏡射（補 B3 lane 漏掉的另一半）
docs/_reports/D9_temp_20260823c.md    本檔
```

⛔ **沒有動**：任何 `.ts` / `.py`、`packages/shared/`、`apps/`、
`modelFxStagingContract.test.ts`（柵欄外，見上面那一節）、
產生器擁有的任何文件、`content/config/`、`content/assets/`。

⛔ **沒有跑**：`content:build` / `skills:sync` / `spec:build`（全域鎖，主 session 做）。
⚠️ ⇒ `content/bundle.json` 與三份 `_index.json` **現在是過期的**，
`shippedBundleIsCurrent.test.ts` 會紅到主 session 跑完 build 為止。
（⛔ 我也刻意沒 commit `content/*/_index.json` —— 我進場時它們就帶著別條 lane 的雜湊。）

---

## ⑤ 測試預算

| | 用量 | 上限 |
|---|---|---|
| `npx vitest run` | **1 次**（三支一起） | ≤ 3 |
| `tsc` | **0 次** | 1（⛔ 這一批**零行 TS 改動**，跑它是純浪費） |
| 突變 | **⛔ 不必自己做** | 一批一條 |

⭐ **突變已經內建在這一票裡**：`modelFxStagingContract.test.ts` 的
`stale` 斷言就是承重那一條的反面 —— 我補上音效，它**當場紅並指名這兩支**。
⇒ 「這兩格真的被出貨的載入鏈讀到了」是被**出貨的守衛**證明的，
⛔ 不是我自己造一份夾具（失敗形態⑤）。

| 測試 | 結果 |
|---|---|
| `packages/shared/src/content/abilityMirror.test.ts` | ✅ 4/4（三對鏡射只差 `schema`） |
| `packages/shared/src/sim/effects/avalonReflectFeedback.test.ts` | ✅ 1/1（B3 lane 的守衛，我的鏡射沒有弄壞它） |
| `packages/shared/src/content/schema/effects/modelFxStagingContract.test.ts` | 🚨 **1 紅＝預期**：「把 FENCED_OUT 裡的那一列刪掉」 |

⛔ 純數值調整 0 行測試（第零守則⑦）：這一批**沒有新增任何一行測試** ——
兩格音效鍵是內容欄位，而守衛（`modelFxStagingContract` ②、`abilityMirror`）
**都已經存在而且都真的在問這件事**。再寫一條就是第四個住處。
