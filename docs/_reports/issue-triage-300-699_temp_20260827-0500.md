# #300–#699 open issue 稽核（2026-08-27，HEAD `e77ef0c4`）

唯讀 lane。唯一的寫入是 `gh issue comment`（4 張）。⛔ 零關閉、零 commit、零程式碼改動。

---

## 結論：**44 張全部留開，0 張可關**

⭐ 而這個結論的**理由與昨天不同**，值得先講清楚：

**這 44 張在 2026-08-26 已經被一條稽核 lane 逐張讀碼過**（每一張都有一則
「🔎 2026-08-26 全票稽核（#300–#799，100 張逐張讀碼）」留言），結論全部是
🟡 留開。⇒ 今天的問題不是「它們做完了嗎」，是「**08-26 之後有沒有東西讓它們做完了**」。

### 量到的答案：沒有

`git log --since="2026-08-26" --grep="#<n>"` 對 44 個票號逐一掃過，
命中的只有 **#457 / #673 / #688 / #699** 四張，而那四筆 commit
（`a35694aa` `43a0ed69` `3c20937a` `18bc23cf`）**全部在 08-26 稽核之前**就已落地
⇒ 已經計入昨天的結論。

74 個新 commit 全部落在 **#700–#790** 這一段（v0.28.4 → v0.29.0），
與 #300–#699 的開票不重疊。

---

## 我自己重驗過的（⛔ 不是引用留言 —— 第三守則）

一次撈完（批次錯），24 張的機器可判斷言逐條實查，**24/24 與 08-26 的結論一致**：

| 票 | 判準 | 今天實測 |
|---|---|---|
| #325 | `arena.ts` 有沒有 `TEAM_SIZE` | **0** 命中；`:59` 仍 `z.array(zVec2).min(1)` |
| #327 | strict vs passthrough | `.passthrough()` **19** vs `.strict()` **3**；`extensions` **0** |
| #382 | baseline 有沒有重轉 | `updatedAt` 仍 **2026-08-18T17:25:53.805Z**（開票前 8 分鐘） |
| #401 | `tpl-line-sweep.json` 的 `inert` | 仍 **3** 格；`advance`/`hitOncePerTarget` 不存在 |
| #423 | `summon` 內容引用 | `content/abilities/` **0 支** |
| #452 | `content/assets/arena-bg/` | **3** 張（目標 13） |
| #473 | `CurationPage.tsx` 稽核觸發 | `audit│稽核│approval` **0** 命中 |
| #529 #547 | fx.prim / fx.w3x | **346 / 25**（見下，⚠️ 退步） |
| #538 | `store.ts` 預載器 | `prefetch│preload` **0** 命中 |
| #558 | 三筆 | ③✅ ①⛔ ②⛔（見下） |
| #565 #674 | `"at": "bone"` 內容 | `content/` **0 個檔**；`imported.bloodbreathstream.json` 不存在 |
| #588 | 房間 30 分鐘硬上限 | `MatchRoom.ts` **0** 命中 |
| #623 | 20 對退場進度 | 40 份帶 transform；`retiredChampions` 11 筆，其中帶 transform 的 **5** ⇒ **仍是 5/20** |
| #626 | 行數 | `configForms.ts` **5,783**（⚠️ 見下）；`fieldAdoption.test.ts` 3,333 |
| #627 | 跨英雄守衛 | 唯一命中 `abilityPassives.ts`（無關） |
| #629 | `MobSystem.ts` 全表掃描 | `for (const [cid, cteam] of world.team)` 仍在 |
| #635 | 92-03 名字 | `h02u.w`=「92-02 狂草泥馬」· `h02u.e`=「92-03 狂草泥馬」⇒ **消化液在這一殼整個不見** |
| #648 | `tpl-periodic-field` 內容引用 | **0 支** |
| #650 | `zBlockGrant` 特效軸 | `_shared.ts:380` 六根軸無任何 vfx 欄 |
| #663 | commit-ref lint | `scripts/` 無此腳本 |
| #667 | 手擁有欄位白名單 | `HAND_OWNED│preserve_keys` **0** 命中 |
| #670 #672 | 序列/亂舞模板 | `tpl-sequence│tpl-chain` **0**；`tpl-dragon` 內容引用 **0** |
| #671 | docker build 量尺 | `deploy-timings.json` 無 docker 欄 |
| #675 | 四機制進契約 | ✅ 四個都 grep 得到；「」剝除規則在第〇章（**AC 已滿足**，見下） |
| #684 | `statusImmunity` 內容 | 只有 `godie-zombieking.passive.json` 一份；`godie-e002` **0** |
| #686 | `lane-plan.sh` | 不存在；`ticket-lint.sh` 對 catalog **0** 引用 |
| #699 | ReviveHuman PRE2 | `content/vfx/` 無 revive 相關文件 |
| #509 | 商店裸 div | `panels/MerchantShop.tsx` 的 `data-pad-focusable` = **0** |

---

## 掛了留言的 4 張（⛔ 其餘 40 張刻意不留言，理由見下）

| 票 | 為什麼值得一則新留言 |
|---|---|
| **#558** | ⭐ 發現一個**新的誤導源**（見下） |
| **#626** | 指標**還在惡化**：一天 +113 行 |
| **#529** | 落差**又擴大**：+4 通用原型 / +1 原作 |
| **#614** | 票上的處置表**今天兩格都是假的** |

### ⛔ 為什麼另外 40 張不留言

它們的 08-26 留言今天逐字仍然正確，我重驗過的每一條都對得上。
再貼一則「今天也沒做」＝ 40 則噪音，而且會把真正有訊號的那 4 則稀釋掉。
（第零守則：預設不查、預設不測；⛔「每張都寫一段話」不是稽核，是計價。）

---

## ⭐ 最重要的一個發現：**修好一半的那一手，把另一半寫成了「設計如此」**

`#558-①`「`ModelFxRig.dispose()` 會 dispose 它不擁有的 AssetContainer」今天仍然成立，
⚠️ **而它比昨天更難被發現了。**

### 缺陷本體（逐段追過，⛔ 不是憑印象）

```
GameApp.ts:905          loadModelContainer: (glbPath) => this.assets.load(glbPath)
  → VfxSystem.ts:749    loadContainer: (p) => ctx.loadModelContainer!(p)
  → assetManager.ts:169 load()  →  :147 private readonly cache = new Map<…>
  → modelFxRig.ts:668   for (const c of this.containers.values()) c?.dispose();
```

`AssetManager` 一個 Scene 一份（`GameApp.ts:739`），而 `assetManager.ts:161-163` 的檔頭
自己列了共用戶：ChampionView · GuardianView · FlowerView · StorePreview ·
IntermissionScene · audition harnesses。⇒ **rig 收的正是別人手上那一份。**

### ⚠️ 而 `#782`（commit `312f0134`，08-26 之後落地）替它背書了

那一手修的是**另外半邊**（`disposeInstanceTree()` 只收 `-fxtint` clone，⛔ 不碰來源材質／貼圖），
是對的。⛔ 但它在 `modelFxRig.ts:919-920` 寫下的判準跨出了自己的範圍：

> ⇒ 判準：**rig 造的（節點、`-fxtint` clone）rig 收；容器的（來源材質/貼圖/幾何）
>    容器收**（`dispose()` 走 `containers.dispose()` 那條路）。

⭐ 那句話把 `:668`（bulk-dispose 共用快取）**指名為正確的歸還路徑**。
下一輪打開這個檔的人會讀到一段自信、具體、有票號的判準，然後認為這一格已經有結論。

### 為什麼沒有任何守衛會叫

`#782` 自己的守衛 `modelFxRigRoundLeak.test.ts` 鎖的是
「clone 要離場、來源材質要活著」—— ⛔ 它**沒有問「這個容器是誰的」**。
⇒ 缺陷、註解、守衛**三層自洽地一起錯**，而且都是綠的。

### ⇒ 這是第三守則 ×「修一半」的複合形狀

CLAUDE.md 已經記過「註解會說謊」，也記過模板預設的推測會變成「原作就是這樣」。
這一次是第三個載體：**一段在窄範圍內正確的判準，被寫在寬範圍的檔頭上。**
⚠️ 修 `#558-①` 的時候必須**連 `:919-920` 那段判準一起改** —— 只改程式，
那段散文會在下一輪把同一個結論再賣一次。

---

## 兩張值得 owner 知道的「狀態變化」

1. **#626 是唯一一張指標還在惡化的票。**
   `configForms.ts` 4,978（08-23 開票）→ 5,670（08-26）→ **5,783（08-27）**。
   一天 +113 ⇒ 約 **11 天**後撞到第〇·七守則的 7,000 強制拆線。

2. **#529/#547 的落差在擴大，⛔ 不是停滯。**
   五天內 `fx.prim.*` +4、`fx.w3x.*` +1，而 126 份原作特效文件裡仍有 **101 份**沒人引用。
   owner 對 #547 的原話是「請儘快補上」。

---

## 一個判準上的灰色地帶（⛔ 我沒有自己裁決）

**#675** 的 `[acceptance criteria]` 逐字是
「四個新機制（statusImmunity/at:bone/path:static/polarity 推論）都出現在契約文件；
「」剝除規則仍在第〇章」—— ⭐ **這兩條我今天實查，全部成立。**

⛔ 但票文的 `Scope ③`（外部編輯器回饋管道：Codex 產 JSON → strict Zod 驗 → 可機讀錯誤格式）
仍然零進度，而它不在 AC 裡。

⇒ 我**沒有關它**：關掉等於把 ③ 埋進一張 closed 票（第零守則⑧說的正是這個 ——
「把缺陷埋進 issue 堆」比順手修還糟）。
⭐ 正解是把 ③ 拆成一張自己的票再關 #675，⛔ 但開票不在本 lane 的權限內 ⇒ 留給 owner／主 session。

**#664** 表面上也像（`## 驗收` 四格全 `[x]`），⛔ 但它的 v3 `[acceptance criteria]` 明確
把 Phase 2 四項寫進驗收，`Objective` 也已改寫成「Phase 2：把『人審一次』變成『機器守永遠』」
⇒ 那張票**現在就是一張 Phase 2 票**，留開正確。

---

## 分堆（供排序用）

| 堆 | 票 |
|---|---|
| ⭐ **指標在惡化，愈晚愈貴** | #626（+113/日）· #529 #547（落差擴大） |
| 🔴 **已咬過／會再咬的線上風險** | #558①（共用快取被收，⛔ 且註解替它背書）· #325（合法文件 runtime undefined）· #588（房間無壽命上限） |
| 🧊 **只差一步就能收** | #675（AC 已滿足，卡 Scope ③ 要不要拆票）· #668（AC1 綠，卡進 ship:check）· #509（2 缺口修 1） |
| ⏸ **卡在 owner 身上** | #614（要一句「不卡了」）· #423 #443 #448 #453（裁決已給，等執行） |
| 🚧 **進行中的大計畫（owner 明說不關）** | #688（「持續追蹤直到做完為止」）· #699 · #673 #674 |
| 📋 **零進度，等排序** | 其餘 26 張 |

---

## 本 lane 做過的動作（完整清單）

- `gh issue comment` × 4：#558 · #626 · #529 · #614
- 寫檔 × 1：本檔
- ⛔ 零 `gh issue close`、零 commit、零 push、零程式碼改動、零全域鎖指令、零正式站連線
