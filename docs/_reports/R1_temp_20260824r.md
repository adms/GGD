# R1 —— #664 HITL 驗收 triage 引擎＋核准帳本＋review:check（2026-08-24）

## 做了什麼

| 件 | 落點 |
|---|---|
| triage 引擎 | `tools/review/triage.mjs` —— 盤點（vfx 411 · model 239 · sfx 232 ⇒ 共 1,116 資產）＋引用圖＋risk＋佇列 |
| 抱怨表 | `tools/review/complaints.json` —— 閃電/雷電 · 光束砲/蝗蟲群 beam-roll · dissipate 殘留，逐列附 owner 原話與 spec |
| 核准帳本 | `docs/_review/approvals.json`（review-approvals@1，空殼先 commit）|
| 佇列 | `docs/_review/queue.json`（review-queue@1，零時鐘欄位 ⇒ 可逐位元組比對）|
| CLI | `pnpm review:build` / `pnpm review:check`（`tools/review/{build,check}.mjs`）|
| middleware | `tools/review/middleware.mjs` —— `createReviewMiddleware(repoRoot)`，GET `/__review/queue` · POST `/__review/verdict`（寫入前驗 hash，不一致回 409）|
| 薄守衛 | `packages/shared/src/ops/reviewTriage.test.ts`（sentinel：pending→pass→hash 漂→又 pending；帳本 round-trip；Tier0 排除）|

## 量到的（2026-08-24 第一次跑）

- **1,116 資產 · Tier0 794 · pending 322**。
- 前 5 名：`fx.prim.void.explosion-lg`(28) · `fx.prim.void.pulse-lg`(21) · `fx.prim.lightning.beam-lg`(20，吃到閃電＋光束兩族 bonus) · `fx.prim.fire.explosion-lg`(18) · `sfx:explosion`(16)。
- risk＝引用它的上架技能數（R/EX 槽 ×2）＋抱怨族 bonus（4/4/3）。

## 設計要點

- **hash 是核准的判準**：vfx＝canonical JSON、model＝GLB 位元組、sfx＝audio-map 綁定的 canonical JSON。內容變 ⇒ hash 與帳本不符 ⇒ 核准過期回 pending。
- **Tier0（risk 0）不進人審**：結構正確性已有 refs.ts/models:check/sfxbind:check/budget:check，零上架引用＝玩家看不到。
- **`review:check` 預設不擋**（--help 逐字寫著「部署不可以被『人不在』卡死」）；`GGD_REVIEW_PENDING_MAX=<n>` 才擋 pending。queue.json 過期（逐位元組）則 exit 1——那是機器修得好的（`pnpm review:build`）。
- tpl preset 解析：`params.*.default`（例：`tpl-beam-roll` → `imported.netherstrike`）也算引用。

## ⚠️ 超出「兩行」的 package.json 改動（必要，原因如下）

`skillsSyncCoversGenerators.test.ts` 三條閘要求：每支 `*:check` 要嘛在 `skills:check` 裡要嘛豁免（豁免表在測試檔裡＝柵欄外）；寫 docs/ 產物的 tools 目錄要在聚合視野裡。⇒ 除了兩行 script，把 `review:build` 接進 `skills:sync` 尾端（`shapes:build` 之後）、`review:check` 接進 `skills:check` 尾端。已驗：該測試 3 條全綠。

## 驗證

- vitest：reviewTriage ＋ skillsSyncCoversGenerators 一次跑，全綠。
- 突變（一批一條）：`buildQueue` 的 `e.hash === a.hash` 改成 `!!e` → 紅（hash 漂不回 pending）→ Edit 還原。
- middleware 煙霧（scratchpad 假 repo 起 http）：GET queue → POST pass 200 → 帳本落 `vfx:fx.zzz` → 舊 hash POST 409 → 佇列消掉。
- `review:check` exit 0；`GGD_REVIEW_PENDING_MAX=5` 時 exit 1；--help 含不擋宣告。

## 給 R2（頁面 lane）

- queue：GET `/__review/queue` → `{items:[{id,kind,hash,risk,reasons,refs,spec?}]}`（risk 降冪）。
- 裁決：POST `/__review/verdict` body `{id,hash,verdict:"pass"|"fail"|"unsure",note?}`（id 跨 kind 撞名才需帶 `kind`；409＝內容已變請重抓佇列）。
- 掛法：vite `configureServer` 裡 `server.middlewares.use(createReviewMiddleware(repoRoot))`。
