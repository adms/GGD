# #1066 #1069 #1071 #1072 #1073 —— 主 session 要落地的柵欄外改動（逐檔逐行）

⚠️ 這條 lane 的柵欄裡沒有 `expand.ts`／`template.ts`／`paramsSchema.ts`，而**五個家族每一個都要展開器讀那一格**
（`paramsSchema.test.ts:177`「每一格參數要嘛 MOVES 展開結果」· `:128`「enabled 模板一定要 isExpandable」）。
⇒ 模板檔與展開器要**同一個 commit**落地；先落模板檔會讓 `paramsSchema.test` 紅（enabled 無展開路徑）或 `brickCensusRatchet` 紅（draft ⇒ engineMissing +3）。

模板檔（6 份，可直接 cp）：`<scratchpad>/lane-tpl5/templates/{tpl-blink,tpl-apply-status,tpl-heal,tpl-single-strike,tpl-proxy-fanout,tpl-drain-leech}.json`

## ① `packages/shared/src/content/schema/template.ts` —— `zParamType` 多一種

```ts
export const zParamType = z.enum([
  "number", "enum", "scaling", "statModifiers", "condition", "docRef", "rgb",
  /**
   * ⭐ GH#1066 —— 一整個 `applyStatus` 節點（去掉 `kind`）。機制欄位（root／stun／moveSpeedMult／
   * berserk／missChance／逐階 duration…）住在值上，由 `zApplyStatus` 本人驗 —— ⛔ 不是一個 id 的下拉：
   * `CC_MECHANIC` 那張手寫表是第〇·四守則的第二個住處（同一個 id 要對到哪幾格機制，抄了一份）。
   */
  "applyStatus",
]);
```

## ② `packages/shared/src/content/templates/paramsSchema.ts` —— `slotSchema` 多一個 case

```ts
import { zApplyStatus } from "../schema/effects/applyStatus";
// …
    case "applyStatus":
      // ⭐ GH#1066 —— 與展開器 `statusNode()` 用**同一個** zApplyStatus（同 `condition` 槽的做法）；
      //    `kind` 由展開器補，表單不用填。⚠️ walkZod 認得 ZodObject ⇒ 渲染成巢狀欄位組。
      return zApplyStatus.omit({ kind: true });
```

## ③ `packages/shared/src/content/templates/expand.ts`

### ③-a 讀取器（放在 `condition()` 旁邊）

```ts
import { zApplyStatus } from "../schema/effects/applyStatus";

/**
 * ⭐ GH#1066 —— 一個 `applyStatus` 槽：值是節點去掉 `kind`，這裡補回 `kind` 並用 `zApplyStatus` 本人驗
 * （與表單那側 `paramsSchema.ts` 同一個 schema ⇒「表單收得下的」與「展開收得下的」不可能分岔）。
 */
function statusNode(t: TemplateDoc, params: Record<string, unknown>, name: string): EffectDef {
  const v = raw(t, params, name);
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    throw new ExpandError(`template ${t.id}: param "${name}" must be an applyStatus object`);
  }
  const parsed = zApplyStatus.safeParse({ kind: "applyStatus", ...(v as Record<string, unknown>) });
  if (!parsed.success) {
    throw new ExpandError(
      `template ${t.id}: param "${name}" is not a valid applyStatus — ${parsed.error.issues[0]?.message ?? "invalid"}`,
    );
  }
  return parsed.data as unknown as EffectDef;
}
```

### ③-b `single-strike`（#1066 族 A，17 支立刻、22 支最終）

```ts
  "single-strike": (t, p) => ({
    castType: "targeted",
    targetsEnemies: true,
    ...(has(t, p, "castTimeSec") ? { castTimeSec: num(t, p, "castTimeSec") } : {}),
    effects: [
      damageEffect(damageType(t, p, "damageType"), scaling(t, p, "damage")),
      // ⭐ GH#1066 —— 「打一下＋上狀態」是一格 optional 參數，⛔ 不是第二個家族（22 支同型）。
      ...(has(t, p, "status") ? [statusNode(t, p, "status")] : []),
    ],
  }),
```

### ③-c `proxy-fanout`（#1066 族 B 的殘餘 ＋ CC_MECHANIC 退場）

```ts
  "proxy-fanout": (t, p) => {
    const effects: EffectDef[] = [
      damageEffect(damageType(t, p, "damageType"), scaling(t, p, "damage")),
    ];
    if (has(t, p, "status")) effects.push(statusNode(t, p, "status"));
    return { castType: "ground", targetsEnemies: true, radius: num(t, p, "radius"),
      ...(has(t, p, "castTimeSec") ? { castTimeSec: num(t, p, "castTimeSec") } : {}), effects };
  },
```

並**刪掉** `const CC_MECHANIC = …`（第〇·四：狀態 id ↔ 機制欄位的第二個住處）。
⚠️ 連帶：
- `content/abilities/godie-u01u.w.json` 的 params：`"statusId":"burnstun","statusDurationSec":0.8` → `"status":{"statusId":"burnstun","duration":0.8,"stun":true}`；
  `tools/skill-remake/templatize-ledger.json` 的 `godie-u01u.w.params` 同步改（`before` 不動）。
- `packages/shared/src/content/templates/orbitProxy.test.ts:30`／`:52-59`：`statusId: undefined` → `status: undefined`、`delete params["statusId"]` → `delete params["status"]`。
- `packages/shared/src/content/templateOriginBaseline.json`：`tpl-proxy-fanout.params` 拿掉 `statusId`、`statusDurationSec` 兩列（那兩格不存在了；新的 `status` 格自帶 origin）。
- `tools/skill-remake/templatize.py` 的 `CC_MECHANIC` 表可以一起刪（`m_proxy_fanout` 已經走 `_has_status_slot()` 那條路）。

### ③-d `drain-leech`（#1073，3 支）

```ts
      // ⭐ GH#1073 —— `leechFlat` 是 optional 槽：清空 ⇒ 沒有 heal 節點（純「打一下＋dot」，出貨 3 支）。
      ...(has(t, p, "leechFlat")
        ? [{ kind: "heal", amount: { flat: num(t, p, "leechFlat") }, applyTo: "self" } as unknown as EffectDef]
        : []),
```
（取代原本無條件的 heal 節點；`drainLeechSubject.test.ts` 用 `defaultParamsFor(t)` ⇒ 仍帶 leechFlat 50 ⇒ 不受影響。）

### ③-e 三個新家族（加進 `FAMILIES`）

```ts
  /** ⭐ GH#1069 純位移 —— 7 支 effects 逐位元相同；零參數是刻意的（多一格就是編一格）。 */
  "blink": (t, p) => ({
    castType: "ground",
    ...(has(t, p, "castTimeSec") ? { castTimeSec: num(t, p, "castTimeSec") } : {}),
    effects: [{ kind: "blink", shape: "single", to: "point", applyTo: "self" } as EffectDef],
  }),

  /** ⭐ GH#1071 只上狀態 —— radius 有填 ⇒ ground 範圍；沒填 ⇒ 指定單體（同 instant-blast 的推導）。 */
  "apply-status": (t, p) => {
    const withRadius = has(t, p, "radius");
    return {
      castType: withRadius ? "ground" : "targeted",
      targetsEnemies: true,
      ...(withRadius ? { radius: num(t, p, "radius") } : {}),
      ...(has(t, p, "castTimeSec") ? { castTimeSec: num(t, p, "castTimeSec") } : {}),
      effects: [statusNode(t, p, "status")],
    };
  },

  /**
   * ⭐ GH#1072 回血（heal ≠ restore）—— `target` 決定「要不要瞄」：self ⇒ castType self；
   * ally ⇒ targeted＋targetsEnemies:false。`applyTo` optional：省略＝這次施法的對象（GH#1046 的省略語意，
   * 對回血族正是要的人）；填 self ＝ 不管點誰都回自己。
   */
  "heal": (t, p) => {
    const target = str(t, p, "target");
    if (target !== "self" && target !== "ally") {
      throw new ExpandError(`template ${t.id}: param "target"="${target}" 不是 self/ally`);
    }
    return {
      castType: target === "self" ? "self" : "targeted",
      ...(target === "self" ? {} : { targetsEnemies: false }),
      ...(has(t, p, "castTimeSec") ? { castTimeSec: num(t, p, "castTimeSec") } : {}),
      effects: [
        {
          kind: "heal",
          amount: scaling(t, p, "amount"),
          ...(has(t, p, "applyTo") ? { applyTo: str(t, p, "applyTo") as "self" | "target" } : {}),
        } as unknown as EffectDef,
      ],
    };
  },
```

## ④ `packages/shared/src/content/editorCapabilities.ts` —— `FAMILY_PROBE_LIST` 加三個名字

`"blink", "apply-status", "heal"`（`editorCapabilities.test.ts:138`：enabled＋isExpandable 的家族一律要在契約裡）。

## ⑤ `packages/shared/src/content/templates/paramsSchema.test.ts` —— `probesFor` 多一型

```ts
    case "applyStatus":
      return [
        { statusId: "root", duration: 1, root: true },
        { statusId: "burnstun", duration: 1, stun: true },
      ].filter(differs);
```

## ⑥ 落地之後（順序）

```bash
cp <scratchpad>/lane-tpl5/templates/*.json content/ability-templates/      # 6 份（3 新 3 改）
python3 tools/skill-remake/templatize.py --exclude godie-o00x.r,godie-ogrh.r,godie-u01u.e,godie-udre.e   # 應提案 34 支
python3 tools/skill-remake/templatize.py --apply --exclude …                # 寫檔＋帳本
npx vitest run packages/shared/src/content/templatizeEquivalence.test.ts packages/shared/src/content/abilityCodeParity.test.ts \
  packages/shared/src/content/templates/paramsSchema.test.ts packages/shared/src/content/templates/orbitProxy.test.ts \
  packages/shared/src/content/templateDefaultsHaveOrigin.test.ts packages/shared/src/content/editorCapabilities.test.ts
pnpm skills:sync   # bricks:build ⇒ handWritten 243 → 207；content:build ⇒ _index／bundle／英雄卡鏡射；templates:build ⇒ docs/ability-templates.*
# 然後 handWrittenAbilitiesRatchet.test.ts 的 HAND_WRITTEN_BASELINE 改 207；templateFamiliesAreAdopted 的 tpl-drain-leech 列刪掉（有客戶了）
```

再加 3 支（各要一個柵欄外動作）：
- `godie-n01g.q` → `--apply --only godie-n01g.q --allow-parity-fix` ＋ 從 `abilityCodeParity.baseline/42.json` 拿掉 `42-01|effects`。
- `godie-u01u.e`／`godie-udre.e`（另一條 lane 改完 ratio 之後）→ `--apply --only godie-u01u.e,godie-udre.e`。
- 產物側（`tools/skill-remake/batch1.py` 的來源列）：`ewar.q`（解鎖 `e007.q`）· `emns.q`（apply-status，逐階 duration＋missChance）· `e00s.e` 的形狀是 damageArea，⛔ 不是 #1071 ⇒ `e010.e` 要等 70-03 那一組的 parity 裁決。


---

# 附錄：6 份模板檔全文（⛔ 與 expand.ts 同一個 commit 落地；直接存成 content/ability-templates/<id>.json）

## `content/ability-templates/tpl-blink.json`

```json
{
  "id": "tpl-blink",
  "schema": "template@1",
  "name": "純位移瞬移",
  "description": "把自己的身體搬到指定的地面點 —— 真的瞬移（`kind:\"blink\"`；owner 2026-08-09「是真的瞬移，不是平移」，GH#301-2），中間那兩格身體不存在：不會被範圍技掃到、飛行途中不吃碰撞；落點仍照 `content/config/displacement-tiers.json` 結算牆前截斷／飛行豁免／場地邊界（GH#1044）。距離上限就是技能自己的射程（skeleton 的 `range`，⛔ 不是這裡的欄位）。⛔ 零參數是刻意的：出貨 7 支（57-02 任意門 · 86-02 電光一閃 · 09-02 瞬間移動 ×2 · 65-01 神出鬼沒 · 58-00 電光一閃 ×2）的 `effects` 一個位元組都不差，多一格就是編一格。要「瞬移到目標身上再打一下」走 tpl-blink-strike；要「抵達點才結算 onLand」走 tpl-teleport。",
  "family": "blink",
  "status": "enabled",
  "params": {
    "castTimeSec": {
      "type": "number",
      "optional": true,
      "default": 0.467,
      "min": 0,
      "max": 5,
      "unit": "s",
      "origin": "census: 出貨 7/7 支純位移的 `castTimeSec` 逐位元 0.467（content/abilities/godie-n00b.e.json · o00k.w · o00x.w · ogrh.w · udea.q · o02l.passive · ofar.passive）；⚠️ 它是 castderive:build:raw 推導的欄位，文件與 params 同一個值（同 71/79 位既有採用者的慣例）。"
    }
  },
  "requires": [],
  "gapScore": 9,
  "exemplar": {
    "skill": "57-02 任意門（content/abilities/godie-n00b.e.json）",
    "jass": "A0PM 82-02 虛空瞬動 j:35335-35336（GetOrderPointLoc → SetUnitPositionLoc；同一台「指向點」機器，expand.ts teleport 註解逐字）"
  }
}
```

## `content/ability-templates/tpl-apply-status.json`

```json
{
  "id": "tpl-apply-status",
  "schema": "template@1",
  "name": "只上狀態",
  "description": "只掛一個狀態、不打傷害：指定一個敵人（不填 radius）或指定地面點的範圍內每一個敵人（填 radius）都吃到 `status`。⭐ 狀態的機制欄位（root／stun／moveSpeedMult／berserk／missChance／逐階 duration…）住在節點上，由 `zApplyStatus` 本人驗 —— ⛔ 不是一個 id 的下拉：id 只是 HUD 的標籤，sim 讀的是那幾格（`godie-e00t.w` 掛了 slow30 卻沒帶 moveSpeedMult，今天誰都沒減速）。出貨 7 支：70-03 木束縛之術（root）· 90-02 麻痺粉 ×2（範圍 slow60）· 30-01 綁架 ×2（root）· 02-00 淨化（root）· 44-01 死神之眼（逐階時長＋失手率）。",
  "family": "apply-status",
  "status": "enabled",
  "params": {
    "status": {
      "type": "applyStatus",
      "default": {
        "statusId": "root",
        "duration": 1.5,
        "root": true
      },
      "origin": "census: 出貨 7 支 applyStatus-only 裡 root 4 支最多（70-03 · 30-01 ×2 · 02-00）；時長取 exemplar 70-03（content/abilities/godie-e010.e.json effects[0] 逐位元）。"
    },
    "radius": {
      "type": "number",
      "optional": true,
      "default": 327.27,
      "min": 50,
      "max": 1500,
      "unit": "wc3u",
      "origin": "derived: 出貨兩支範圍版 90-02 麻痺粉（godie-h02r.w／godie-hgam.w）radius 6.0 ÷ (11/600) = 327.27（`toLen` 逐位元回 6.0）。清空 ⇒ 指定單體（targeted）。"
    },
    "castTimeSec": {
      "type": "number",
      "optional": true,
      "default": 0.833,
      "min": 0,
      "max": 5,
      "unit": "s",
      "origin": "derived: exemplar 70-03（godie-e010.e）`castTimeSec` 0.833；castderive:build:raw 推導欄位，文件與 params 同值。"
    }
  },
  "requires": [
    "applyStatus"
  ],
  "gapScore": 9,
  "exemplar": {
    "skill": "70-03 木束縛之術（content/abilities/godie-e010.e.json）",
    "jass": "entanglingroots 系（root:true ↔ 22 份出貨文件的配對，expand.ts 原 CC_MECHANIC 註解）；GGD 出貨值以 content 為準（第〇·六守則第 2 層）"
  }
}
```

## `content/ability-templates/tpl-heal.json`

```json
{
  "id": "tpl-heal",
  "schema": "template@1",
  "name": "回血",
  "description": "加一個量的回血（`kind:\"heal\"`，走 Scaling：perRank ＋ 係數），⛔ 不是把生命回到某個比例 —— 那是 tpl-life-manipulate 的 `restore`，兩個 effect kind，⛔ 不塞進同一個下拉。`target`：self ＝ 對自己施放（castType self）；ally ＝ 指定一位隊友（targeted、點不到敵人）。`applyTo` 留空 ＝ 回這次施法的對象（self 施法就是自己、指定隊友就是那位隊友）；填 self ＝ 不管點誰都回自己（GH#1046：省略的語意是 ctx.targets，對回血族那正是要的人）。出貨 5 支：42-02 吸血祭品 ×2（self）· 08-01 雙龍紋 ×2 · 99-02 最初的聲音（ally）。⚠️ 回血沒有級距表（damage-tiers 是傷害的），所以 `amount` 走 perRank —— 五支出貨文件本來就是字面 perRank，這裡沒有第二個住處。",
  "family": "heal",
  "status": "enabled",
  "params": {
    "target": {
      "type": "enum",
      "values": [
        "self",
        "ally"
      ],
      "default": "ally",
      "origin": "taxonomy: 出貨 5 支 heal-only 裡 3 支指定隊友（08-01 ×2 · 99-02）、2 支回自己（42-02 ×2）⇒ 預設跟多數與 exemplar 走。"
    },
    "amount": {
      "type": "scaling",
      "default": {
        "perRank": [
          50,
          85,
          120,
          155
        ],
        "ratios": [
          {
            "stat": "ap",
            "coeff": 0.465
          }
        ]
      },
      "origin": "derived: exemplar 08-01 雙龍紋 content/abilities/godie-n01c.q.json effects[0].amount 逐位元（孿生 godie-nbbc.q 同值）。"
    },
    "applyTo": {
      "type": "enum",
      "values": [
        "self",
        "target"
      ],
      "optional": true
    },
    "castTimeSec": {
      "type": "number",
      "optional": true,
      "default": 0.067,
      "min": 0,
      "max": 5,
      "unit": "s",
      "origin": "census: 出貨 5/5 支 heal-only 的 `castTimeSec` 逐位元 0.067；castderive:build:raw 推導欄位，文件與 params 同值。"
    }
  },
  "requires": [],
  "gapScore": 9,
  "exemplar": {
    "skill": "08-01 雙龍紋（content/abilities/godie-n01c.q.json）",
    "jass": "GGD 出貨值以 content 為準（第〇·六守則第 2 層：編輯器 JSON ＞ JASS）；原作對照見 tools/skill-remake 的 08-01 列"
  }
}
```

## `content/ability-templates/tpl-single-strike.json`

```json
{
  "id": "tpl-single-strike",
  "schema": "template@1",
  "name": "單體斬擊",
  "description": "單一指向目標的一次魔法斬擊（僅數值核心，執行斬條件為紅色降級槽）。 可以再掛一格 `status`（打一下＋上狀態：暈眩／定身／減速／混亂…機制欄位住在節點上，由 zApplyStatus 本人驗，⛔ 不是 id 的下拉）；清空 ⇒ 純傷害。",
  "family": "single-strike",
  "status": "enabled",
  "params": {
    "damage": {
      "type": "scaling",
      "default": {
        "perRank": [
          350
        ],
        "ratios": []
      }
    },
    "damageType": {
      "type": "enum",
      "values": [
        "magic",
        "physical",
        "true"
      ],
      "default": "magic"
    },
    "status": {
      "type": "applyStatus",
      "optional": true
    },
    "castTimeSec": {
      "type": "number",
      "optional": true,
      "default": 0,
      "min": 0,
      "max": 5,
      "unit": "s"
    }
  },
  "requires": [
    "applyStatus"
  ],
  "gapScore": 9,
  "exemplar": {
    "skill": "23-04 雷焰聖劍",
    "jass": "A0OD"
  }
}
```

## `content/ability-templates/tpl-proxy-fanout.json`

```json
{
  "id": "tpl-proxy-fanout",
  "schema": "template@1",
  "name": "範圍逐一施法",
  "description": "對範圍內的每一個目標各結算一次單體法術。原作是用一隻拋棄式 dummy 逐一 IssueTargetOrder —— 那是 WC3 沒有多目標指令的實作繞道,不是設計,所以 GGD 這邊不召喚任何東西。傷害之外可以再掛一格 `status`（纏繞/暈眩/緩速/混亂…機制欄位住在節點上，由 zApplyStatus 本人驗，⛔ 不再是三個 id 的下拉），那才是這台機器跟純震波不一樣的地方。⚠️ 十五支成員裡有兩支是對友方放的(53-03 破法對咒、99-03 初音戰意),本版做不到 —— 見說明文件。",
  "family": "proxy-fanout",
  "status": "enabled",
  "params": {
    "radius": {
      "type": "number",
      "default": 600,
      "min": 100,
      "max": 3600,
      "unit": "wc3u"
    },
    "damage": {
      "type": "scaling",
      "default": {
        "perRank": [
          280
        ],
        "ratios": []
      }
    },
    "damageType": {
      "type": "enum",
      "values": [
        "magic",
        "physical",
        "true"
      ],
      "default": "magic"
    },
    "status": {
      "type": "applyStatus",
      "optional": true,
      "default": {
        "statusId": "root",
        "duration": 4,
        "root": true
      },
      "origin": "derived: 2026-09-06 之前這份模板的兩格 `statusId`（default root）＋ `statusDurationSec`（default 4）合成一格；root→root:true 是 22 份出貨文件的配對（原 expand.ts CC_MECHANIC 註解）。⭐ #1066：機制欄位住在節點上 ⇒ slow40／slow60／confusion／alcohol-enema 那些下拉表達不了的狀態都收得下。"
    },
    "castTimeSec": {
      "type": "number",
      "optional": true,
      "default": 0,
      "min": 0,
      "max": 5,
      "unit": "s"
    }
  },
  "requires": [
    "applyStatus"
  ],
  "gapScore": 8,
  "exemplar": {
    "skill": "45-02 千鳥流",
    "jass": "A0JX (war3map.j:41741-41762)"
  }
}
```

## `content/ability-templates/tpl-drain-leech.json`

```json
{
  "id": "tpl-drain-leech",
  "schema": "template@1",
  "name": "汲取吸附",
  "description": "汲取吸附：打一下 → 持續傷害（同傷害型別、同級距、同係數的 dot）→ 回施法者一個定量（`leechFlat`，⭐ 可清空 ⇒ 純「打一下＋dot」，#1073）。",
  "family": "drain-leech",
  "status": "enabled",
  "params": {
    "damageTier": {
      "type": "enum",
      "values": [
        "極小",
        "小",
        "中",
        "大",
        "極大"
      ],
      "default": "小",
      "origin": "derived: 出貨兩份逐位元 `小`（`content/abilities/godie-hgam.passive.json` · `godie-h02r.passive.json` 的 `effects[0].amount.damageTier` 與 `effects[2].amountPerTick.damageTier`）。⛔ 走級距，⛔ 不烘數字（第〇·四守則）。"
    },
    "apRatio": {
      "type": "number",
      "default": 0.6,
      "min": 0,
      "max": 5,
      "unit": "ratio",
      "origin": "derived: 出貨兩份逐位元 `ratios:[{stat:\"ap\",coeff:**0.6**}]`。"
    },
    "damageType": {
      "type": "enum",
      "values": [
        "magic",
        "physical",
        "true"
      ],
      "default": "magic",
      "origin": "derived: 出貨兩份 `damageType:\"magic\"`；原作 `A0KV` 是 WC3 Parasite 系（`OBJECTS.json`）。"
    },
    "leechFlat": {
      "type": "number",
      "default": 50,
      "min": 0,
      "max": 5000,
      "unit": "count",
      "optional": true,
      "origin": "j:26608 `call SetUnitLifeBJ( udg_Frog_Hero, ( GetUnitStateSwap(UNIT_STATE_LIFE, udg_Frog_Hero) + **50.00** ) )` —— 回的是**施法者**（`udg_Frog_Hero = GetTriggerUnit()`）。⚠️ 原作是**每跳** +50 × 5 跳，⭐ 而出貨把它收斂成**一次** 50 —— 我照抄出貨（diff=0 優先），⛔ 不是原作。⭐ GH#1046：展開器的 heal 節點要帶 `applyTo:\"self\"` —— 在此之前漏寫，而 `heal.ts` 的省略語意是 target ⇒ 回的是**被點的敵人**（守衛 `drainLeechSubject.test.ts`，真 sim 兩具缺血身體）。⚠️ 出貨那兩份（`godie-hgam.passive`／`godie-h02r.passive`）的 heal 節點**同樣漏寫** —— 「diff=0」抄到的正是那個錯，⛔ 不在這條 lane 的柵欄裡，另記報告。 ⭐ #1073：這一格改成 **optional** —— 清空 ⇒ 展開沒有 heal 節點（「打一下＋持續傷害」而不吸血：30-03 痴漢火焰 ×2 · 72-02 黑人牙菌斑，出貨 3 支逐位元對得上）；default 50 只是編輯器的建議值（`has()` 對 optional 槽：沒填＝缺席）。"
    },
    "intervalSec": {
      "type": "number",
      "default": 1,
      "min": 0.2,
      "max": 10,
      "unit": "s",
      "origin": "derived: 出貨兩份 `intervalSec: **1.0**`。⚠️ ⭐ 原作 `j:26604` 是 `TriggerSleepAction( **0.95** )` —— 兩個來源打架，而第〇·六守則的階梯是「**編輯器產生的 JSON ＞ JASS 實際效果**」⇒ 取 1.0。⛔ 0.95 這個事實留在這裡，⛔ 不無聲丟掉。"
    },
    "durationSec": {
      "type": "number",
      "default": 5,
      "min": 0.2,
      "max": 60,
      "unit": "s",
      "origin": "derived: 出貨兩份 `durationSec: **5.0**`；原作是 `j:26603` `exitwhen udg_Frog_Seed_Index > **5**` 五跳 × `j:26604` 的 0.95s = 4.75s，w3a `abilities.A0KV.duration.1 = 5.0999999` ⇒ 三個來源都在 5 附近。"
    },
    "stacking": {
      "type": "enum",
      "values": [
        "refresh",
        "independent",
        "stack"
      ],
      "default": "refresh",
      "origin": "derived: 出貨兩份 `stacking: \"refresh\"`。"
    },
    "castTimeSec": {
      "type": "number",
      "min": 0,
      "max": 10,
      "unit": "s",
      "optional": true,
      "origin": "taxonomy: ⛔ **刻意不給 default** —— 兩支候選差太遠（`godie-h02r.passive` 1.7 vs `godie-osam.passive` 0.267），⭐ 給一個 default 就是編一個（同 `tpl-locust-orb.tint` 留空的理由）。"
    }
  },
  "requires": [],
  "gapScore": 4,
  "exemplar": {
    "skill": "90-00 寄生種子（妙蛙種子／妙蛙花 `godie-hgam.passive` ＋ `godie-h02r.passive`）",
    "jass": "war3map.j:26592 / 26603 / 26604 / 26608（Trig_Parasitism_Seed_Actions：buff 閘 → 5 跳 × 0.95s → 回施法者 +50）"
  }
}
```
