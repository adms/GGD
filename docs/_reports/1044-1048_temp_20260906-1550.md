# GH#1044–#1048 模板票五張 —— lane 報告（2026-09-06）

> 併行 lane，⛔ 無 commit。柵欄：`content/ability-templates/*.json` · `packages/shared/src/content/templates/{expand.ts,*.test.ts}` · `docs/_reports/`。
> ⛔ 沒動 `content/abilities/*.json`、沒動 `templateDefaultsHaveOrigin` 豁免表、沒動 sim 的 heal/hooks/grantAttribute（⭐ 查證後**引擎受詞沒有錯**，錯在模板展開器）。

## 基線（動手前量到的，⛔ 不是複述票文）

| 量尺 | 結果 |
|---|---|
| `defaultParamsFor → resolveTemplateExpansion → zAbilityDoc`（出貨 registries 的那條路） | 35 張 enabled，**33/35** 過（紅：`tpl-beam-roll` effects.0.spacing · `tpl-dragon-serpent` effects.0.clipTimeScale）—— 與 #1047 票文一致 |
| 同一條路，enum 每個值＋optional 無 default 填探針（281 個探針） | **59 紅**：其中 beam-roll／dragon-serpent 的大半是上面兩個預設缺陷的連帶；剩下一族是**結構性**的（見「發現」） |
| `tpl-drain-leech` 展開的 heal 節點（無 `applyTo`）跑真 sim | 施法者 +0、被點的敵人 +N（`heal.ts:21` 省略語意＝target）—— 與 #1046 一致 |
| `tpl-growth-charge` 展開的 onKill hook（無 `target`）跑真 `fireHooks(world, killer, "onKill", victim)` ×8 | 擊殺者 +0、受害者各進度 1 —— 與 #1048 一致 |
| 三張說明卡 | teleport 說「不吃地形也不吃碰撞」／mark-stacks 說「sim 沒有延後排程」／combo-finisher 說「是 draft、沒有展開路徑」—— 三句都與主線相反 |

## 五張各做了什麼

| 票 | 修在哪 | 守衛 |
|---|---|---|
| **#1044** | `tpl-teleport.json` description 重寫：展開是 `leap` 不是 blink、空中不碰撞 vs 落點走 `resolveLandingPoint`→`world.wallBlock`（出貨 leap:"clamp"／flightExempt:true／pillarsBlock:false）、一般角色**牆前截斷**、飛行豁免要 `flightExempt`∧被搬者 `flightIgnoresObstacles`、**邊界**照截、rally 只拉到腳邊、落地傷害可選。`tpl-mark-stacks.json`：「sim 沒有延後排程」→「**這張模板**尚未接 `delayed`（引擎有：delaySec/count/intervalSec/effects，例 godie-e00r berserk_smoke），接了要另驗時序」。順帶 `tpl-leap-strike.json` 同一機制的一句補上牆／飛行豁免 | `templateProseCurrent.test.ts` ②（`EFFECT_HANDLERS.delayed` 在 ⇒ 不准說引擎沒有）③（展開帶 leap 且宣稱穿牆／teleport 家族 ⇒ 要有 牆／飛行／邊界） |
| **#1045** | `tpl-combo-finisher.json`：開頭加「現況：enabled、`FAMILIES` 有展開路徑、預設展開成 comboStrikes…；家族可用 ≠ 每支英雄接好」；原「這張卡是 draft」段落標成【歷史（2026-09-02 接線前）】，原文保留 | `templateProseCurrent.test.ts` ①（enabled 且 isExpandable 的卡，「沒有展開路徑／是 draft」只准活在含「歷史」的段落） |
| **#1046** ⭐靈魂層 | `expand.ts` drain-leech 的 heal 節點加 `applyTo: "self"`（⛔ 不改全域 heal 預設）；`leechFlat.origin` 記下這一格 | `drainLeechSubject.test.ts`：出貨模板→expand→那個 heal 節點→出貨 Zod union→真 `runEffects`→兩具缺血身體：施法者回血、敵人不動。**突變**（拿掉 applyTo）→ 🔴「施法者要被回血: expected 1384 to be greater than 1384」 |
| **#1047** | `expand.ts` modelFxFamily：`spacing` 只在 `count ≥ 2` 發（schema refine 逐字）；`tpl-dragon-serpent.json` 新增 `clip` enum（default `idle`，origin＝`imported.darkraor.json` clipMap 全對到 `stand - 1` ＋ j:44063/44070/44073 無 SetUnitAnimation、j:44066/44072/44075 慢的就是那條 stand）；`clipTimeScale.origin`／`tpl-beam-roll.spacing.origin` 記成對關係 | `paramsSchema.test.ts`：`PROBE_COMPANION["tpl-beam-roll.spacing"]={count:2}`（anti-silence 仍問對問題）＋ 新 describe「★ 預設 35/35 過 zAbilityDoc」＋「enum／optional 分支 vs `KNOWN_BRANCH_GAPS` 棘輪（31 格，只能變短）」。**突變**（拿掉 count≥2 閘）→ 🔴 兩條 |
| **#1048** ⭐靈魂層 | `expand.ts` growth-charge 的 hook 加 `target: "self"`（⛔ 不改全域 hook 預設） | `growthChargeSubject.test.ts`：①出貨模板 hook `attachSource` 掛擊殺者，真 `fireHooks` 跨 8 個 THORNE 受害者：第七殺 0、第八殺擊殺者 +agi、每個受害者 agi 0 且無 `attrGrantProgress`、擊殺者計數器歸零；②真死亡入口（damageQueue→DeathSystem→fireHooks，everyNth=1）擊殺者 +agi、死者 0。**突變**（拿掉 target）→ 🔴「第八殺要加在擊殺者身上: expected 0 to be greater than 0」＋「擊殺者要拿到增益」 |

## 測試（3 次 vitest，1 次 tsc）

- 第 1 次（28 支相關檔）：185/186 —— 唯一紅是我自己的守衛③對 `tpl-charge-push`（它沒說穿牆卻被要求寫牆）⇒ 規則改成「觸發的是**宣稱**」＋ teleport 家族一律。
- 第 2 次：**28 檔 186/186 綠**。
- 第 3 次（突變，三個一起拿掉）：3 檔 5 紅 8 綠，紅的訊息逐字如上；restore 後 `grep` 驗過三行都在（⛔ 沒有再跑第四次確認綠）。
- `pnpm typecheck`：EXIT=1，**兩個錯都在 `packages/shared/src/ops/enabledSwitchesHaveConsumers.ts`（別的 lane 的工作區檔）**，零個指到我的檔。
- 行數：實作（含 JSON）48 / 測試 380（paramsSchema +110 · drain 87 · growth 110 · prose 73）。⚠️ 比值高 —— 三個修法各是一行，而五張票各自點名要行為守衛＋突變控制組＋全 schema 閘＋文案防回歸；註解占了一半以上。主 session 可砍註解。

## ⭐ 最重要的發現

1. **「diff=0 抄出貨」抄到的正是那個錯**：`tpl-drain-leech` 逐位元抄自 `godie-hgam.passive`／`godie-h02r.passive`，而那兩份出貨文件的 heal 節點**同樣沒有 `applyTo`** ⇒ 出貨的寄生種子今天就是在**替敵人回血**。⛔ 不在我的柵欄（`content/abilities/`）；genguard 說它們只被 `castderive` 正規化、`tools/skill-remake/heroes/` 無來源 ⇒ 直接改那兩份 JSON 的 heal 節點加 `applyTo:"self"` 即可（玩家看得到的機制錯）。
2. **modelFx 家族的 enum `path` 有 30 個分支「表單收、載入拒」**：參數集合是按預設 path 宣告的（static 族沒有 speed/distance；forward 族沒有 count/spacing/lifeSec），換 path ⇒ expand 擲例外或被 refine 擋。⛔ 不在 #1047 範圍（它修預設），已用 `KNOWN_BRANCH_GAPS` 逐格點名成棘輪；正解是展開器按 path 過濾輸出或表單按 path 隱藏格 —— 值得一張票（我不開票，owner 說「開票」才開）。
3. `tpl-summon-agent.body=champion` 要先填 championId（同 PROBE_COMPANION 那種前提）—— 也在棘輪上。

## 改了哪些檔（逐檔）

- `content/ability-templates/tpl-teleport.json`（description）
- `content/ability-templates/tpl-mark-stacks.json`（description 一句）
- `content/ability-templates/tpl-combo-finisher.json`（description 開頭＋歷史段標記）
- `content/ability-templates/tpl-dragon-serpent.json`（新增 `clip` param；`clipTimeScale.origin`）
- `content/ability-templates/tpl-drain-leech.json`（`leechFlat.origin`）
- `content/ability-templates/tpl-beam-roll.json`（`spacing.origin`）
- `content/ability-templates/tpl-leap-strike.json`（description 一句）
- `packages/shared/src/content/templates/expand.ts`（三處：spacing 閘 · onKill `target:"self"` · heal `applyTo:"self"`）
- `packages/shared/src/content/templates/paramsSchema.test.ts`（PROBE_COMPANION ＋ 新 describe）
- 新：`packages/shared/src/content/templates/drainLeechSubject.test.ts` · `growthChargeSubject.test.ts` · `templateProseCurrent.test.ts`

## 需主 session 重生成的產物（⛔ 我沒跑 sync）

`content/ability-templates/*.json` 的 description／params 變了 ⇒ 至少：`typecat:build`（`docs/editor-contract/ggd-type-catalog.{json,md}` 逐字嵌 description）· `content:build`（`bundle.json`／`_index.json`）· `templates:build`（`docs/ability-templates.{csv,md}`）· 可能 `bricks:build`／`editorcov:build`／`caps:export`（讀 ability-templates）。一次 `pnpm skills:sync` 全解。

## 沒做／界線

- 沒改 `content/abilities/godie-hgam.passive.json`／`godie-h02r.passive.json`（柵欄外）—— 上面發現 1。
- 沒把 mark-stacks 接到 `delayed`（票文自己說要另驗時序；只修說明）。
- 沒宣稱遊戲端 E2E／視覺驗收：#1046／#1048 是真 SimWorld 的 handler 級重現，⛔ 不是整招施放。
