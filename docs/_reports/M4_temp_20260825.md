# M4 lane 報告 — GH#688 Phase 4+5：蝗蟲群機制三件＋悟空 09-04 pilot（2026-08-25）

規格：`docs/_reports/locust_scan/synthesis.md`＋`docs/蝗蟲群移植計畫.md`。
狀態一句話：**機制①②落地（守衛+突變）、機制③評估完成⛔未實作、pilot 已過連續圖片驗收；
`content:build`/`skills:sync` 由主 session 統一跑（併行鎖），跑完前 bundle 類閘照例紅。**

## 機制①：static 沿線 N 具（#673-④）

- `sim/effects/spawnModelFx.ts` static 分支：`count`（≤ MODEL_FX_MAX_INSTANCES=12）×
  `spacing`，第 k 具（k=0..count−1）在錨點沿開火方向 `spacing×k` 處。
  ⚠️ 與原作差**一個步長的平移**（JASS `i=1..6 × 200` 從 spacing 起；我們第 0 具在錨點
  —— 光束/火柱從施法者腳下長出來，count:1 逐位元＝今天單具）。純度：單位向量乘加，零三角。
- schema：`spacing` 新欄（僅 static+count≥2）；`count` 開放給 static；refine 四道
  （spacing 沒人讀／N 具沒間距 都在載入時擋）。TS variant 同步補 `spacing`。
- `modelFxPreset.ts` PRESET_FIELDS 補 `spacing`；`tpl-beam-roll` 新增 params
  `count` 預設 **6**、`spacing` 預設 **2**（synthesis §0 的原作值：200 wc3u ÷100；
  59-04 原作其實是 10×1.5 —— Phase 6 逐支覆寫）。⇒ 四支經典從「原地一團」變
  「沿開火方向 12 世界單位的光束帶」。
- **rollback（第一守則）**：`tpl-beam-roll.params.count.default` 改回 1＝整族單具，
  ⛔ 不動任何技能 JSON。count:6 是本 lane 照原作值挑的預設（owner 不在，留開關）。
- 突變（一批一條）：沿線迴圈 `stN` 改常數 1 → `modelFxStatic.test.ts` 紅
  「count 沒有被讀 —— 整條線退化成 1 具」→ Edit 還原。

## 機制②：`model@1.fxAlpha`（alpha 欄兩側空白的引擎側那半）

- schema `model@1.fxAlpha`（0..1，缺席=1=今天 131 份文件逐位元不變）；client
  `applyFxTint(root, tint, alpha?)` 在同一入口做**材質 alpha 乘法**（⛔ 非 visibility
  開關；<1 時一併解鎖 PBR `transparencyMode=ALPHABLEND`，否則 glTF 的 OPAQUE 鎖讓
  這格「寫了但不會發生」）。守衛：`modelFxRig.test.ts` ③（讀**最終** clone 素材）。
- 內容 0 筆是刻意邊界：57 個 `SetUnitVertexColorBJ` 呼叫點的逐技能接線是後續批
  （fieldAdoption `field:models.fxAlpha` landing 豁免，since 2026-08-25）。

## 機制③（評估，⛔ 未實作）：glb 動畫剪輯播放＋timeScale

- 現況：`modelFxRig.ts`/`modelFxPath.ts` 全檔 0 個 Animation —— modelFx 通道唯一的
  動作是 `spinDegPerSec`。轉出的 glb **有剪輯**（flamestrike1: birth/stand/death；
  revivehuman: Stand）但從未播放。
- 擋多大（jass-sites 排序表逐列加總）：anim 呼叫點 14（可見 dummy 12：u018×2、
  o018×6、o001×2、o01T×1、o01A×1；hfoo×2 是隱形施法者不算視覺）＋ timeScale 8–9
  （h008×3、u018×1、o01A×1、o01V×1、hfoo×2）。⇒ 至少 **13 個 Trig 函式／7 個可見
  rawcode**，含招牌用法 h008「`SetUnitTimeScalePercent 15` ＋ 立即 KillUnit ＝
  死亡動畫 15% 凍播」（Excalibur／ExcaliburMAX／Turtle Power 三支的外層爆殼）。
  另外**每一支** stock 靜態 dummy 的視覺一半住 stand/birth 剪輯（火柱不播 stand
  就沒有火焰翻騰）—— 機制③同時抬升所有已轉模型的品質。
- 建議接縫：`ModelFxRig.fillGeometry` 已走 `instantiateModelsToScene`，其回傳的
  `animationGroups` 就是掛載點；欄位住 `spawnModelFx`（`anim`: birth|stand|death
  ／`animTimeScale` 0.01–10，模板可補預設），剪輯名經 `model@1.clipMap` 解析。
  回收規矩：release() 停組、池重用重播。估 rig ~50 行＋schema 2 格＋守衛 1 條。
  ⚠️ 本 lane ⛔ 不 gh 寫入 —— **請主 session 開後續票**（機制③＋fxAlpha per-cast
  接線＋59-04 spacing 1.5×10 覆寫，三張）。

## pilot：悟空 09-04（h006×沿線＋h007 主體）

- **stock MPQ→glb 第一條路打通**：`war3.mpq`→`w3xlib.models.convert_all`（luma-key
  路徑，零 alpha 軟刪除）→ `content/assets/models/imported/revivehuman.glb`（3/4
  可見 prim；1 是 TeamGlow 刻意丟）與 `flamestrike1.glb`（5/5）。放 `imported/`
  是刻意的 —— staging contract ⑦ 的零像素柵欄掃那個資料夾。
- model doc：`w3x.stock.revivehuman`（scale 0.101＝netherstrike 出貨慣例「單位縮放
  ≈1.7 世界單位」對齊：16.76×0.101≈1.69；`fxLongAxis:"y"` 躺向開火方向＝owner
  「橫放光束」裁決；`fxSpawnHeight 0.55`＝躺下後半厚 10.75×0.101÷2）；
  `w3x.stock.flamestrike1`（scale 0.102，直立火柱 ⇒ 不宣告長軸，`beam-orient`
  EXEMPT 帶可反駁理由）。tint：h006/h007/h008 原作全白（UNIT_TINTS.json）⇒ 兩份
  doc 都不填 fxTint（「tint 有來源」閘本來就只驗有填的）。
- 綁定：`godie-ogrh.r`＋`godie-o00x.r`（超賽形態）各：beam 節點補
  `modelKey:"w3x.stock.revivehuman"`（⑤ 身分格；SHARED_MODEL_FENCED_OUT 刪
  09-04 那列——被 stale 斷言點名）＋新節點 h006 火柱
  `{static, count:6, spacing:2, lifeSec:2, modelKey:"w3x.stock.flamestrike1"}`；
  champion 鏡射（godie-ogrh/godie-o00x）同步（abilityMirror 閘綠）。
- **連續圖片驗收（@visual-proof）**：`docs/_reports/goku_visual-proof_20260825-0200/`
  —— calibrate() 自證後，基線 0 亮 → tick37 出生（節點座標即到位）→ tick38 glb
  回填 **1384 亮/3060 lit**（兩層 12 具、340v/425v）→ 2 秒壽命內持續 →
  tick118 到期回收（enabled 0）。逐張數字見該目錄 `notes.md`（含誠實限制：
  PRE2 emitter／骨架螺旋不在 glb ⇒ 星柱呈星芒面片，屬機制③＋emitter 批）。
- h008 外層爆殼：正解＝機制③凍播 ⇒ **本批不做**（synthesis 3c-③ 的 fallback vfx
  doc 也留給該批一起，避免做兩次）。

## 併行批次的接縫（主 session 待辦）

1. `pnpm skills:sync`（bundle/_index/manifest＋spec/caps 等）—— 新 model doc、
   spacing/fxAlpha schema、模板改動都要進產物；跑完 `shippedBundleIsCurrent` 等
   bundle 閘才會綠（現在紅是併行常態，⛔ 不是本 lane 新增的紅）。
2. `pnpm apconv:build`（genguard 提示的正規化器，abilities 改過）。
3. 開三張後續票（見機制③節）。
4. `templates:build` 已由本 lane 以 `genrun.sh` 重生成（docs/ability-templates.*）。
5. ⭐ 測試夾具升級：`shippedContent.ts` 的檔案樹退路改 `WorkingTreeFsSource` ——
   `_index.json`（產物）先與真目錄對帳（缺補、刪除去），否則併行批次裡新增文件會
   讓硬參照它的整族被隔離連坐（量到：悟空整隻被隔離、~20 支測試會紅）。
   `content:build` 落地後該段 no-op。瀏覽器側 `beamAuditionWorld.workingTreeSource()`
   同理（自我過期）。

## 驗證帳

- vitest：批①（client 4 檔＋shared 6 檔，紅 2）→ 批②修後全綠（7 檔 40 條＋
  client 14 條）→ 批③突變紅（見機制①）；另 pre-commit 掃 model-doc 閘
  （modelGeosetAlpha/IdleGrounding/Bbox/Texture 81 條＋modelFxAxis/modelFacing/
  hiddenPrimitives 20 條）全綠。tsc 1 次（紅 1 根因：variant 缺 spacing）→ 修後
  shared/client 0。`beam-orient --check` 0 條。
- 測試行數：新增測試 ≈110 行 vs 實作（sim+schema+preset+rig+fixture）≈180 行，≤1×。
