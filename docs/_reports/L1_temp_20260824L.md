# L1 —— #649 類④ 定點 3D 模型機制（`path:"static"`）2026-08-24

## 做了什麼

`spawnModelFx` 加第五種路徑 **`static`**：在指定錨點固定放一具模型、活 `lifeSec`、
⛔ 不位移。原作 266 具 dummy 有 **238 具（89%）站著不動**（`DUMMY_ORB_MAP.json`：
151 world-point ＋ 87 hero-attached），擋住 **60 支**技能 —— ⭐ 一個機制解 60 支
（第〇·五守則），⛔ 沒有新開 effect kind。

| 檔 | 改了什麼 |
|---|---|
| `packages/shared/src/content/schema/effects/spawnModelFx.ts` | `path` enum ＋ `"static"`；新欄位 `anchor:"self"\|"point"\|"target"`（省略＝self）；refine：static 必填 `lifeSec`、禁填 `speed`/`distance`、`anchor` 只有 static 讀得到、身分三格對 static 只剩兩格 |
| `packages/shared/src/sim/effects/variants/spawnModelFx.ts` | variant 鏡射（`variantMirrorsSchema` 的兩住處對齊） |
| `packages/shared/src/sim/effects/spawnModelFx.ts` | `modelFxInstances` static 分支：錨點解析 target→point→self 逐層退化（⛔ 不整支消失），一具、travel 0；`apply` 的 fail-loud 身分檢查與 speed 對 static 改成 0 |
| `apps/client/src/render/modelFxStatic.test.ts` | ⭐ 新守衛（見下）。**rig 零行改動** —— static 的線路形狀（`dx=dz=0`）與 orbit 環上一點同族，`modelFxPoseFromWire` 的「不動」分支照畫 |

## 守衛（走出貨鏈，⛔ 不是自造夾具）

真 Zod 閘（`zEffectDef` 含 refine）→ 真 `SimWorld`＋`runEffects` → 真 `modelFxSpawn`
→ 出貨 `modelFxDocFor` 接縫＋出貨 `Models`（`imported.midchildernanohaaura`）→ 真
`VfxSystem` → **場景樹世界矩陣**：模型在施放點、`vfx.update()` 推進後**一格不動**。
另一條驗 refine 三個反例（缺 lifeSec／填 speed／非 static 填 anchor）。

**突變**（一批一條，承重線）：sim static 分支 `const p = at ?? origin;` →
`const p = origin;` ⇒ 紅：「⛔ static 的錨點沒有被讀 —— 模型長在施法者腳下,不在施放點上」。已還原。

測試：vitest 2 次（10＋16 條相鄰 modelFx 守衛全綠 · 突變紅）· tsc 1 次
（`packages/shared` Done；apps/client 僅有的 2 個 error 在 `vfx/VfxSystem.ts`
`resolveAttachment`/`VfxSpawnEvent` —— **L2 lane 未完成的工作樹改動**，⛔ 非本 lane）。

## 2 支先導（證據最全）—— ⛔ 內容檔是 tiers:apply／skillremake 產物（genguard 擋手改），節點已備好，主 session 走產生器落地

DUMMY_ORB_MAP 打分（lifetime＋ability join＋模型已在出貨樹）最高分＝
**godie-hvsh.r 騎英之疆繩**（48-04，現況 `effects: []` 佔位）的兩具（line 25410/25413）：

```json
{ "kind": "spawnModelFx", "shape": "single", "path": "static", "anchor": "point",
  "modelKey": "imported.midchildernanohaaura", "lifeSec": 3 }
{ "kind": "spawnModelFx", "shape": "single", "path": "static", "anchor": "point",
  "modelKey": "imported.awing", "lifeSec": 3, "scale": 1.0 }
```

次選 **godie-h01o.e 月牙天衝**（虛化 aura，hero-attached ⇒ `anchor:"self"`，
`modelKey:"imported.deathwave"`，原作無 timed life ⇒ lifeSec 由 owner 級距／模板定）。

## 留給主 session／後續

1. **`pnpm skills:sync`**（全域鎖，本 lane 未跑）—— schema 變了，`spec:build`
   產物（技能標記機制與效果規則.md 等）會過期。
2. **後台開關**（報告 C-3 提的 `modelFx.staticEnabled` 預設 on／`staticMaxLifeSec`）
   —— 需動 `content/config` 新檔＋admin `store.ts`/`App.tsx` 共用行，⛔ 本輪零出貨
   內容用到 static（機制無行為變化，無可 rollback 之物）⇒ 與先導內容同批落地才有意義。
3. `templates/expand.ts:537` 的 castType 推導不認得 `static`（只影響 ability-templates
   家族展開；先導不走模板）。若做 static 模板家族時補 `static → "ground"`。
