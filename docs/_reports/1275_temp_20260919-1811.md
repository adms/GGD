# GH#1275 木乃香（`godie-etyr`）—— JASS 逐格翻譯參數表

> lane `konoka-1275` · 2026-09-19 · 柵欄 `tools/skill-remake/**` ＋ `docs/_reports/`
> ⚠️ 本份**只做來源查證與翻譯表**，⛔ **一個 `content/` 檔都沒動**（原因見 §0）。
> 所有數字都是**這一天實際讀出來的**（w3a 物件表、`war3map.j`、出貨 schema 與 sim），
> ⛔ 不是引用票文、⛔ 不是憑印象。

---

## 0. ⛔⛔ 最重要的一件事：**這張票的前提過期了 —— 她沒有產生器來源**

派工說「在**產生器來源**裡把翻譯補上（⛔ 不手改 `content/abilities` 產物）」。
⭐ **量到的結果是：`godie-etyr` 的六份 ability JSON 不是任何產生器的產物。**

| 問法 | 答案 |
|---|---|
| `bash scripts/genguard.sh content/abilities/godie-etyr.<槽>.json`（六格全跑） | 六格全回「⚠️ 只被**正規化器** `castderive:build:raw` 就地改欄位，**不是那支的產物** ⇒ 不擋你」 |
| `grep -rl "etyr" tools/` | 只有 `tools/skill-remake/templatize-ledger.json`（正規化器自己的**退路帳本**，記 `passive`／`q` 轉模板前的樣子）＋ `templatize.py` 的一行註解 |
| 她在 `tools/skill-remake/batch1.py` 的 `HERO` 表裡嗎？ | ⛔ **不在**。那張表是 15 位英雄 × 6 格 ＝ 90 支，`provenance` 是重製戳；她六格全是 `"provenance": "w3x-import"` |

### ⛔ 而「把她加進 `batch1.py`」是**錯的**，四個理由都是讀出來的

1. `batch1.py` 有 `assert len(docs) == 90`（多一位英雄 ⇒ 96 ⇒ 當場炸）。
2. `batch1.py:85-93` 的註解逐字寫著那張 `HERO` 字面值有**三個外部讀者用正則抓它**：
   `stamp_provenance.py`、`export_xlsx.py`、出貨守衛 `packages/shared/src/content/abilityProvenance.test.ts`。
3. `refresh_docs.py` 會把 `HERO` 的內容寫進 `docs/英雄技能第一批重製-90支.md`
   與 `docs/技能編輯器引擎須知 20260811.md` §13.10 —— 把她塞進一份**標題就叫「第一批重製-90支」**
   的產物裡，那份文件當場變成謊話。
4. ⭐ 更根本的：第〇·六守則的**第 1 層（owner 的新版技能說明）對她是空的** ——
   票文自己也說她不在重製規格裡。把她蓋上重製戳＝宣稱有一份不存在的設計。

⇒ ⭐ **`tools/skill-remake/` 是這張票的錯門牌。** 正確的落點是
`content/abilities/godie-etyr.{passive,q,w,e,r,ex}.json`（手編檔，改完跑 `pnpm castderive:build:raw`）
＋ `content/champions/godie-etyr.json`（內嵌鏡像，方向 standalone → embedded）。
⇒ **請主線用那個柵欄重派一條 lane**；本份的 §2 參數表可以直接照抄落地。

---

## 1. 先驗鑰匙（第〇·六守則：⛔ 不要拿一把沒驗過的 join key 去同步）

⚠️ ⭐ **「14-xx」不是唯一的編號** —— w3a 裡有**兩位**英雄用 14-xx：

| 不是她的（另一位英雄） | 是她的 |
|---|---|
| `A00W` 14-01 匕首狂風 · `AOhw` 14-02 痊癒之風 · `A0JZ` 14-04 AKT戰隊 · `A0K2/A0K3/A0K4` 14-04-0x 涼宮系列 | 見右表 |

⭐ 所以我**不用編號 join**，改讀單位自己的技能欄（`OBJECTS.json` → `heroes.Etyr`，⛔ 注意大寫 E）：

```
Etyr 治癒系公主 / proper_name 木乃香 / model HeroOichi.mdl / INT 主屬
abilities       = ["AInv", "A0JJ", "A0ST"]
hero_abilities  = ["A0JL", "AOae", "A0JM", "A0SS", "Aamk"]
```

| GGD 槽 | 名稱 | rawcode | w3a base | JASS？ |
|---|---|---|---|---|
| `passive` | 14-00 召喚式神 | **A0JJ** | `ACtn` | ✅ `MoveSlaveStart/Order` |
| `q` | 14-01 東風繪扇、南風末廣 | **A0JL** | `AEtq` | ✅ `MagicFan` ＋ `Wind_Effect` |
| `w` | 14-03 魔力應援 | **AOae** | `AOae` 耐久光環 | ⛔ 純物件表 |
| `e` | 14-02 式神炸裂 | **A0JM** | `AHtc` | ✅ `SlaveExp` |
| `r` | 14-04 聖夜降臨 | **A0SS** | `Arai` 復活死者 | ⛔ 純物件表 |
| `ex` | 14-002 魔力激發 | **A0ST** | `AHab` 輝煌光環 | ⛔ 純物件表 |

⭐ 附帶三個**配角**（原作把機制拆在它們身上，⛔ 不在主技能上）：

| id | 是什麼 | 關鍵欄位 |
|---|---|---|
| `u00P` **式神** | passive／R 召出來的本體 | base `uaco` · hp 1000 · 跑速 522 · scale 3.0 · 技能 `Aloc`（**蝗蟲：點不到、打不到**）＋ `A0JK` |
| `A0JK` **14-00-01 式神之火** | 式神身上的**永久獻祭光環** | base `Apxf` · `area 350` · `pxf1 = 95` 傷害 |
| `n01D` **聖夜** ＋ `A0T3` **14-041聖夜（自爆）** | R 一起召出來、**立刻自爆**的炸彈 | hp 1 · duration 0.05 · base `Asd3` 自爆 · **半徑 275／傷害 200** |

⭐ **一個把鑰匙釘死的算術**：R 的卡面寫「瞬間造成周圍 200／400／600 傷害」，
而 `A0SS.Rai2`（第二類召喚數）＝ **1／2／3**，`A0T3` 每顆 **200**
⇒ `1×200 / 2×200 / 3×200` ＝ **200／400／600**，逐位元對上。
⇒ 「R 會爆傷害」不是一個寫在文案裡的數字，是**一個機制**：它召了 N 顆炸彈。

---

## 2. 逐格參數表（JASS 行號／w3a 欄位 → JSON 標籤）

換算常數（讀出來的，⛔ 不是我挑的）：
- **距離**：`GGD_PER_WC3 = 11/600 = 0.018333`（`templates/expand.ts`）
- **AP**：`intToAbilityPower = 1`（`sim/stats/attributes.ts`）⇒ JASS 的 `INT × k` → `ratios:[{stat:"ap",coeff:k}]`
  ⚠️ 不是逐位元相等：GGD 的 `ap = baseStats.ap + INT×1 + growth.ap×(L−1)`，多一條成長項。
- ⚠️ `RectFromCenterSizeBJ(loc, S, S)` 是**正方形**，半邊長 `S/2`。GGD 沒有方形 AoE ⇒ 下表用半邊長換成圓半徑，**角落那一圈（×√2）翻譯不過去**，照實記。

### 2-1 `passive` 14-00 召喚式神（A0JJ）

| 來源 | 值 | → JSON |
|---|---|---|
| w3a `A0JJ.cooldown` | **5.0 秒** | `cooldownTier`（⚠️ 今天出貨是 **30**） |
| w3a `A0JJ.mana` | 150 | `manaCostTier` |
| w3a `A0JJ.cast_range` | 450 → **8.25** | `rangeTier` |
| w3a `A0JJ.duration` / `hero_duration` | **25 秒** | `summon.durationSec` |
| w3a `A0JJ.castTime` | 0.5 | `castTimeSec` |
| ubertip（原文） | 「召喚出式神跟隨木乃香，此式神周圍將附帶 **95** 點傷害，持續 **25** 秒。」 | —— |
| `OBJECTS.units.u00P` | hp 1000 · 跑速 522 · `Aloc`（點不到打不到） | `summon{ championId, hpMult, autoTargetable:false, manualTargetable:false, mobTargetable:false, targetPriority:"mob" }` |
| `A0JK` `area 350` → **6.417** · `pxf1 95` | 式神身上的傷害光環 | ⛔ **今天做不到**，見 §3-① |
| **JASS `war3map.j:30188-30201`**（`MoveSlaveOrder`） | 每 **3.00 秒** 對每一隻 `u00P` 下 `"move"` 到木乃香身上 | 「跟隨」＝ `summon` 的既有跟隨行為（或一格 follow 參數） |
| **JASS `war3map.j:30214`**（`MoveSlaveStart`） | 施放 A0JJ ⇒ 打開上面那個週期觸發 | —— |

⛔ **JASS 與 w3a 裡沒有任何「在落點打一發範圍傷害」** —— 今天出貨的
`template: tpl-instant-blast { radius 327.27, damageTier "小", ap 0.6 }` 是**匯入時憑空長出來的**。

### 2-2 `q` 14-01 東風繪扇、南風末廣（A0JL）

| 來源 | 值 | → JSON |
|---|---|---|
| **JASS `j:30356`** | `udg_MoNiReHP = 150.00 + ((abilityLevel + 1) × INT)` | 治療量。⚠️ `GetUnitAbilityLevelSwapped` **從 1 起算** ⇒ 階 1／2／3 ＝ `150 + 2·INT` ／ `150 + 3·INT` ／ `150 + 4·INT`。**AP 係數逐階是 2／3／4**，⛔ 不是固定值 |
| **JASS `j:30364`** | `RectFromCenterSizeBJ(caster, 650, 650)` ⇒ 半邊 325 → **5.958 ≈ 6** | 技能層 `radiusTier`（「中」＝6） |
| **JASS `j:30362`** | **EX 開啟時** `1200×1200` ⇒ 半邊 600 → **11.0** | ⭐ EX 卡面「使用東風繪扇有效範圍也能增加」是**真的** |
| **JASS `j:30271-30275` / `30305-30309`** | 過濾：`IsUnitAlly` ＆ 非建築 | `targetsEnemies: false` |
| **JASS `j:30300` / `30334`** | `IssueTargetOrderBJ(dummy, "purge", 該友軍)` | ⭐ **淨化是真的** → `dispel{ shape:"circle", side:"allies", radiusTier }` |
| **JASS `j:30357-30360`** | dummy `ogru` ＋ 技能 `A02H`（purge）＋ 隱藏 ＋ 1 秒 timed life | ⛔ **投遞手段，不是效果** —— ⛔ 不要翻成一個召喚物 |
| **JASS `j:30238-30243`**（`Wind_Effect`） | 2 隻 `o01G`（模型 `OblivionAura.mdl`）＋ `StarfallCaster.mdl` | ⭐ 今天**已經翻對了**：`spawnModelFx{ count:2, modelKey:"imported.oblivionaura" }` |
| **JASS `j:30365-30368`** | `TriggerSleepAction(3.00)` 後清掉 `ogru` ＋ `o01G` | 那兩顆扇子存在 **3 秒** |
| w3a `A0JL` | cd **25**（今天 15）· mana **120/170/220**（今天 **0**）· castTime 0.3 | tier 三格 |

⚠️ 卡面寫「範圍 300」，**JASS 是 650 方形（半邊 325）** ⇒ 照階梯以 JASS 為準，卡面改 `{{radius}}`。

### 2-3 `w` 14-03 魔力應援（AOae）—— 純物件表，⛔ 無 JASS

| w3a 欄位 | 值 | → JSON |
|---|---|---|
| `AOae.area` | **400** → **7.333** | 光環半徑（「大」＝8） |
| `Oae1 增加移動速度 (%)` | 0.05 / 0.10 / 0.15 / **0.20** | `ms pctAdd` |
| `Oae2 增加攻擊速度 (%)` | 0.35 / 0.50 / 0.65 / **0.80** | `as pctAdd` |
| base `AOae` ＝ 耐久光環 · buff `B04A` | —— | ⭐ **對範圍內友軍**，⛔ 不是只有自己 |

⭐ 數值今天**全部是對的**，⛔ **投遞對象整個錯了**：出貨寫成 `passive.ranks[].modifiers`（只加自己）。

### 2-4 `e` 14-02 式神炸裂（A0JM）

| 來源 | 值 | → JSON |
|---|---|---|
| **JASS `j:30536-30544`** | `ForGroupBJ(GetUnitsOfPlayerAndTypeId(owner, 'u00P'), …)` | ⭐ **逐一遍歷「我方的每一隻式神」** |
| **JASS `j:30518-30520`** | 每一隻：`KillUnit` ＋ `NagaDeath.mdl` | 式神被消耗掉 |
| **JASS `j:30521`** | `RectFromCenterSizeBJ(那隻式神的位置, 500, 500)` ⇒ 半邊 250 → **4.583** | 爆炸半徑（「小」＝4.5）。⭐ 圓心是**式神**，⛔ 不是木乃香 |
| **JASS `j:30451` / `30501`** | `(abilityLevel('A0JM') × 150) + 100 + (INT × 3)` | 階 1–4 ＝ **250 / 400 / 550 / 700**，`ratios:[{stat:"ap",coeff:3.0}]` |
| **JASS `j:30430-30438`** | 過濾：敵方 ＆ 活著 ＆ 非建築 | —— |
| **JASS `j:30473` / `30523`** | `SetUnitManaBJ(木乃香, mana + 150.00)` | ⭐ **每一隻回 150 魔**（固定值，⛔ 不逐階） |
| **JASS `j:30537`** | `TriggerSleepAction(0.01)` | 幾乎瞬發 |
| w3a `A0JM` | cd **35**（今天 30）· mana **150/180/210/240**（今天四階都 150）· `Htc1 AOE傷害 = 0` | ⭐ **底層技能的傷害是 0** ⇒ 全部在 JASS |

⛔⛔ ⭐ **沒有式神 ⇒ 這一招什麼都不會發生**（ForGroup 的迴圈體一次都不跑：零傷害、零回魔）。
卡面「注意先要招換式神」是 JASS 真話。⇒ 這正是**承重守衛要驗的兩個方向**。

⛔ 今天出貨的 `tpl-orbit-array { rayCount:12, reach:327, aim:"inward" }` 與 JASS **零對應** ——
它以木乃香為中心打一圈，而 JASS 是以**每一隻式神**為中心各打一發。

### 2-5 `r` 14-04 聖夜降臨（A0SS）—— 純物件表，⛔ 無 JASS

| w3a 欄位 | 值（階 1／2／3） | → JSON |
|---|---|---|
| `cooldown` | **0.01 / 0.01 / 0.01** | ⭐ 卡面「0 秒冷卻時間」**是真的**（今天出貨 30） |
| `mana` | 150 / 225 / 300 | `learn_ubertip`：「每等級增加耗魔 75」✅ |
| `cast_range` ＝ `area` | 450 / 550 / 650 → **8.25 / 10.083 / 11.917** | `rangeTier` ＋ 召喚散佈 |
| `duration` / `hero_duration` | **8 / 8 / 8 秒** | `summon.durationSec` |
| `Rai1 召喚的部隊(第一類)` | **1 / 2 / 3** | 式神 `u00P` 的隻數（今天寫死 `count: 1`） |
| `Rai2 召喚的部隊(第二類)` | **1 / 2 / 3** | 炸彈 `n01D`，⇒ `A0T3` 半徑 **275 → 5.042**、傷害 **200**／顆 |
| base `Arai`（復活死者）· `targets_allowed` 含 `dead` | —— | ⚠️ 原作**要屍體**，見 §3-④ |

`learn_ubertip` 逐字：「基礎屍體轉換式神個數 **1**／每等級增加轉換數量 **1**／
每等級增加招喚瞬間傷害 **200**／每等級增加耗魔 **75**」。

⛔ 今天出貨的是 `tpl-summon-agent { count:1, body:"self" }` ＝ 召喚**木乃香自己的分身**
（`damageMult 0.5`／`hpMult 0.5`），⛔ 不是式神；而那個分身**不會**被 E 引爆
（E 今天根本不看召喚物）。

⚠️ ⭐ 順帶證實票裡 Scope 5 的第一格：R 的 `range: 8` 與 `rangeTier: "大"` 還留在文件上，
而 `tpl-summon-agent` 把它改成 `body:"self"`（以自身為中心）⇒ **施法距離欄位與實際施放方式不一致**。
⛔ 我沒有改它（不在柵欄），但它與上面「count 寫死 1」是同一個來源：
⭐ **模板參數被挑來「像」原作，⛔ 不是從 w3a 翻過來的**（owner 2026-08-26 的「翻譯 ≠ 近似」）。

### 2-6 `ex` 14-002 魔力激發（A0ST）—— 純物件表，⛔ 無 JASS

| w3a 欄位 | 值 |
|---|---|
| base | `AHab`（輝煌光環） |
| `area` | 50 → **0.917**（⇒ 實際上只有自己） |
| `Hab1 增加法力回復` | **0.07** |
| ⭐ `Hab2 加權百分比` | **true** |

⭐⭐ **`Hab2 = true` 就是這一招的全部**：0.07 是「**每秒最大魔力的 7%**」，⛔ 不是「每秒 0.07 點」。

**今天出貨**：`passive.ranks[0].modifiers = [{ stat:"manaRegen", op:"flat", value:0.07 }]`
⇒ **每秒 +0.07 點**。

⭐ 量一下差多少（用她自己的 `content/champions/godie-etyr.json` ＋ `attributes.ts` 寫明的法則
`maxMana = baseStats.maxMana + growth.maxMana×(L−1) + intToMaxMana(15)×INT`；
她的 `attributes.int = 30`、`intGrowth = 0`、`baseStats.maxMana = 100`、`growth.maxMana = 35`）：

| 等級 | 最大魔力 | JASS 的 7%／秒 | 今天出貨／秒 | 倍率 |
|---|---:|---:|---:|---:|
| LV1 | 550 | **38.5** | 0.07 | **550×** |
| LV30 | 1,565 | **109.6** | 0.07 | **≈1,566×** |

⚠️ 這是**照法則做的算術**，⛔ 不是跑過 `finalizeStat`（道具／增益／夾子沒算進去）。
⭐ 但根因是結構性的、不受那些影響：**匯入把 `0.07` 抄成 flat，把 `Hab2 加權百分比` 這一格丟掉了。**
⭐ 巧合到值得記一筆：`intToManaRegen` **也剛好是 0.07** ⇒
今天這一招給的東西**逐位元等於「+1 點智力的回魔」**。

⛔ ⭐ 而這同時是**第一·五守則的違規**：卡面逐字寫著「每秒獲得 7% 的瑪那回復」，
而玩家拿到的是 0.07 點 —— **一句說了但不會發生的話**。

---

## 3. ⛔ 翻不過去的：引擎缺什麼（⛔ 我沒有用現有參數湊）

⚠️ 下面每一條都**讀了出貨程式才寫**，⛔ 不是「grep 不到所以沒有」。
⭐ 其中兩條我第一次判斷錯，逐行讀之後**推翻了自己**（記在 §3-⑤）。

| # | 缺的機制 | 證據（出貨檔:行） | 擋住幾支 |
|---|---|---|---|
| ① | **召喚物身上帶光環／帶被動** | `sim/summons.ts:163` `spawnSummon` 寫死 `passiveSlot: null`，四格技能 rank 全 0 ⇒ ⭐ **召喚出來的身體不套用它 champion doc 的任何被動或光環** | 木乃香 passive（式神之火）· 14 支用到 `summon` 的技能全部受限 |
| ② | **「對我方每一隻召喚物逐一觸發」的條件葉／效果** | `content/schema/effects/` 46 個 kind 裡沒有；`summon.ts` 只管生成與壽命，沒有「引爆我的召喚物」的反向入口 | 木乃香 E（整支技能就是這個） |
| ③ | **逐階的係數**（`ratios[].coeff`） | `schema/common.ts:611` `coeff: z.number()`（純量）；`attrRatios[].coeff` 同樣純量；⭐ 而 `slotRankRatios` **只乘基礎值** —— `sim/effects/effect.ts` 的 `resolveScaling` 裡 `v *= m` 在 `for (const r of sc.ratios) v += stat*coeff` **之前** | 木乃香 Q（`150 + (階+1)×INT`）。⛔ 拿固定 coeff 去逼近＝近似，不做 |
| ④ | **屍體資源**（R 的「周圍死去的亡靈」） | GGD 沒有屍體；`summon` 無條件生成 | 木乃香 R。⭐ 但這一格**該問 owner**：階梯第 1 層對她是空的，而「要不要引入屍體」是設計題不是翻譯題 |
| ⑤ | **回復量＝最大資源的百分比／秒** | `zStatModifier` 的 `op` 沒有「佔最大值百分比」的語意；`manaRegen` 是點數／秒。`restore.manaPct` 有 `0..1 of max mana`（`schema/effects/restore.ts`）⭐ **但它是一次性的 effect，需要一個每秒的驅動器** | 木乃香 EX。⭐ 最小的解：`restore.manaPct` ＋ 一個週期 hook，或替 `manaRegen` 加一個 `pctOfMax` op |

### ⭐ 我推翻自己的兩條（⛔ 記下來，因為它們正是「我查的那條路上沒有 ≠ 它不存在」）

- **「對範圍內友軍治療」—— ⭐ 今天就做得到，⛔ 不缺機制。**
  第一次讀 `sim/effects/heal.ts`（只有 `applyTo: self|target`）與 `templates/expand.ts:3175` 的
  `tpl-heal`（只收 `self`／`ally`）⇒ 我判成「缺」。
  ⭐ 再讀 `sim/abilities/abilitySystem.ts:257-269` `groundAoeTargets()`：
  `castType:"ground"` ＋ `targetsEnemies:false` ⇒ 走 `bodiesInCircle(..., { side:"allies" })`，
  **而且把施法者自己收進圈內**（同一支函式的註解逐字舉的例子就是 `range: 0` 的 53-03 破法對咒）。
  ⇒ Q 寫成 `castType:"ground"` ＋ `range: 0` ＋ `targetsEnemies:false` ＋ `radiusTier` ＋
  `effects:[{heal…},{dispel, shape:"circle", side:"allies"…}]` **今天就成立**。
  ⚠️ 只有 `tpl-heal` 這一族少一個 `target:"area"` 分支 —— 而隔壁 `ally-shield`（`expand.ts:3047`）
  **已經有 self/ally/area 三選一**，照抄即可（⛔ 這是補模板，不是補機制）。
- **「友軍光環」—— ⭐ 今天就做得到。** `schema/effect.ts:136` 的 `affects: z.enum(["enemy","ally","all"])`，
  `sim/aura/aura.ts` 逐 tick 對帳；`schema/ability.ts:232` 明列被動 payload 收 `auras`。
  ⇒ W 只要從「自己的 `modifiers`」改寫成「半徑 7.33 的 `auras`」，⛔ 不缺機制。

### 順帶量到的母體（給 owner 排序用，⛔ 不是這張票的範圍）

| 問題 | 數 | 分母／量法 |
|---|---:|---|
| 卡面宣稱「對友軍回復」**且**文件裡真的有 heal 節點 | **26** | 1,130 份 `content/abilities/*.json`；描述先剝掉 `「…」` 台詞（第〇·六守則） |
| ↑ 其中 `castType:"self"`（＝結構上碰不到友軍） | **3** | `b2-boxxo.r` · `b2-fushi.w` · `godie-etyr.q` |
| 用到 `summon` 的技能 | **14** | 全部受 §3-① 限制 |
| 卡面宣稱「友軍 ＋ 範圍／光環」 | **8** | 含 `godie-etyr.q`／`.w` |

⚠️ 「26」是**上界**：22 支是 `castType:"targeted"`（單體指定友軍今天就通），
真正說謊的只有那 3 支 self ——⛔ 要逐支對照才算得準，我沒有做。

---

## 4. 其他讀到、但不在本 lane 柵欄內的

| 發現 | 位置 |
|---|---|
| ⭐ 她的 `role` 是 **`"marksman"`**，而 `origin` 是 **`軟輔`**、稱號是「治癒系公主」 | `content/champions/godie-etyr.json` —— 定位標籤自己就自相矛盾 |
| Q 的 `manaCost` 出貨是 **`[0,0,0]`**，w3a 是 120/170/220 | 一支零耗魔的團隊治療 |
| R 的 `range: 8`／`rangeTier:"大"` 與 `tpl-summon-agent{body:"self"}`（原地施放）打架 | 票文 Scope 5 第一格，✅ 證實 |
| `templatize-ledger.json` 存著 `passive`／`q` **轉模板前**的樣子 | 重寫時那是既有的 `--revert` 退路，⛔ 改完要一起更新（`templatize.py --revert`） |

---

## 5. 落地建議（給重派的那條 lane）

1. **六格全部重寫**，照 §2 的表；傷害／冷卻／耗魔／距離／範圍**只寫級別名**，
   數值交給 `tiers:apply`／`castderive:build:raw`（⛔ 不手挑，票文 Non-goals 第 3 條）。
2. **今天就做得到的**（⛔ 不必等機制）：**Q 的範圍治療＋淨化**、**W 的友軍光環**、
   **R 的 `count` 逐階 1/2/3 ＋ 8 秒**、**六格的冷卻／耗魔改回 w3a**。
3. **要機制的**（§3 的五條）⇒ 各開一張票，按「擋住幾支」排序：
   ①召喚物帶光環（14 支）> ②引爆我方召喚物（1 支＋整個家族）> ③逐階係數 > ⑤%最大值回復 > ④屍體（先問 owner）。
4. **承重守衛＋突變**（票文 Test criteria）：載入出貨內容施放 E，驗
   **「場上沒有式神 ⇒ 零傷害零回魔」** 與 **「場上有 N 隻 ⇒ N 發、每發以那隻為圓心、回 N×150 魔」**
   兩個方向；突變＝拿掉「逐一遍歷我方式神」那個條件 ⇒ 守衛要紅。
5. **卡面**：Q 的「範圍 300」改 `{{radius}}`（實際 325 方形半邊）、EX 的「7%」要嘛做出來要嘛改掉
   （⛔ 不可以留著 —— 第一·五守則），R 的「0 秒冷卻」照 w3a **是真的**、保留。
   被取代的原作文案另存 `docs/legacy/_w3x-fidelity-superseded.md`（第一·五守則「另存，⛔ 不壓縮取代」）。
6. **rollback 開關**：翻譯結果預設啟動，⛔ 但開關要寫得出 config id ＋ 欄位名 ＋ **消費端哪一檔哪一行讀它**
   （第一守則的第四個住處）。

---

## 6. 誠實的邊界（⛔ 我沒有做到的）

- ⛔ **沒有改任何 `content/` 檔**（柵欄外，見 §0）。
- ⛔ **沒有跑 vitest**（本 lane 沒有需要驗的程式改動）、⛔ 沒有跑 `skills:sync`／`content:build`。
- ⚠️ **`u00P` 與 `n01D` 是被誰生出來的，JASS 裡找不到**：
  `grep -n "u00P\|u00Q" war3map.j` 只有 3 個命中（下令跟隨 ×1、E 遍歷 ×2），
  **零個 `CreateNUnitsAtLoc`**；`n01D` 在 `war3map.j` 裡**一次都沒出現**。
  而 `A0JJ` 的 w3a `data` 是空的、`A0SS.Rai3/Rai4`（部隊類型一／二）`authored: false`
  ⇒ ⭐ **召喚的單位型別欄位沒有被匯入工具抽出來。**
  我用三件事把它補上（⛔ 不是猜）：① `u00P` 只被下令與殺死、從不被建立，而唯一叫「召喚式神」的技能是 A0JJ；
  ② `n01D` 名叫「聖夜」、唯一技能 `A0T3` 名叫「14-041聖夜」，而唯一叫「聖夜降臨」的是 A0SS；
  ③ `Rai2`(1/2/3) × `A0T3` 的 200 ＝ 卡面的 200/400/600。
  ⚠️ ⭐ 但它仍然是**推論**，⛔ 不是讀到欄位 —— 要釘死得補抽 `Arai` 的 `Rai3/Rai4` 與 `ACtn`/summon 的單位型別欄位。
- ⚠️ 票裡「LV30 回魔 106，全母體最高」我**沒有重現**：照 `attributes.ts` 的法則
  （`0.1 + 0.1926×29 + 0.07×30`）我算到 **7.8**，與 106 差一個量級。
  ⛔ 我不知道那個 106 的分母是什麼（每 5 秒？含道具？）—— ⭐ 這一格要原量測者說明量法，
  在那之前 §2-6 的兩倍率表請以「結構性根因（`Hab2` 被丟掉）」為準，⛔ 不要引用絕對值。
- ⛔ **沒有重做票裡的母體對照**（驗收標準第 5 點）—— 那要等翻譯真的落地之後才量得到「前後」。
