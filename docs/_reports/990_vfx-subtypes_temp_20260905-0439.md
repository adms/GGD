# GH#990 第一批 —— 特效子模組 `vfx-subtype@1`（schema ＋ 閘 ＋ 盤點出來的第一批）

`2026-09-05 04:39` · 一條併行 lane · ⛔ 未 commit（由主 session 收）

---

## ① 盤點（⭐ 先量，⛔ 不憑感覺）

母體：`content/vfx-scripts/` **10 支腳本 · 63 個 segment · 38 種不同的段落**
（⚠️ 票文寫「11 支」—— 那是把 `_index.json` 也數進去了，⭐ 實際是 **10 支**）。

### (a) 整份 `segments` 陣列**逐位元組完全相同**的配對 —— ⭐ 這是最大的重複

| 段數 | 兩份 |
|---:|---|
| **17** | `godie-e002.ex` ≡ `godie-e00l.ex` |
| **4** | `godie-n01c.r` ≡ `godie-nbbc.r` |
| **2** | `godie-h020.e` ≡ `godie-hjai.e` |
| **2** | `godie-o00x.r` ≡ `godie-ogrh.r` |

⇒ **50 / 63 段（79%）住在一對完全相同的腳本裡。**

### (b) 依「身分」（kind ＋ modelKey/vfxId ＋ anchor）的跨腳本重複，前幾名

| 次數 | 用它的腳本數 | 身分 |
|---:|---:|---|
| 6 | 3 | `floatingText` |
| **4** | **4** | `screenShake` |
| 3 | 3 | `modelFx:w3x.stock.neutralbuildingexplosion@target` |
| 3 | 3 | `anim:target/hurt` |
| 2 | 2 | `modelFx:imported.herosaber@target` · `modelFx:w3x.stock.darkportaltarget@self` · `vfx:fx.w3x.particle.holyawakening.p00@self` · `sound:wc3.defendcaster` · `hideBody` · `modelFx:w3x.stock.reddragonmissile@self` · `modelFx:w3x.stock.nedeathsmall@self` …（共 25 種重複 ≥2） |
| 1 | 1 | `vfx:fx.fam.resurrect.holy.s95@bone` · `modelFx:imported.herocloudcyd@self` · `modelFx:w3x.stock.flamestrike1@target` · `anim:caster/cast` …（`godie-hart.r` 專屬的 7 種） |

### ⛔ 票文猜的五顆（`beam.solid` · `slash.arc` · `impact.bloom` · `dash.trail` · `charge.glow`）**一顆都不存在**

量到的重複**不是**「同一種視覺形狀出現在很多技能上」，是「**兩支技能共用一份完全相同的腳本**」。
⇒ ⭐ 這正是「盤點 → 按重複次數排序」與「憑感覺列一張表」的差別（第〇·五守則的下半）。

### ⚠️ 量到重複 ≥2 但**刻意不做**的兩顆（⛔ 不是漏掉）

| 顆 | 量到 | ⛔ 為什麼不做 |
|---|---|---|
| `screenShake` | 4 支腳本各 1 次（amplitude 0.6/0.6/0.55/0.4） | **單段**、包起來之後參數數量與原本一模一樣 ⇒ **零壓縮、純轉手**。要值得包，它得帶一條**量得出來的級距**（像 `damage-tiers`），⛔ 而今天只有 4 個散點，編一條級距就是憑空捏造 |
| `anim{at:"target",pulse:"hurt"}` | 3 支（clipWindowMs 700/700/900） | 同上（1 段 1 參數） |

⭐ 兩顆的量測值**已經寫進**第一批的 `origin` 散文裡當「第二個量值」的證據
（`sub.bladestorm-8hit.shakeAmplitude` / `.victimClipWindowMs`），所以知識沒有消失。

---

## ② 第一批做了哪四顆

| id | label | 段數 | 呼叫端（`derivedFrom`） | 參數 |
|---|---|---:|---|---|
| `sub.bladestorm-8hit` | 八連斬（連段＋收尾） | 17 | `godie-e002.ex` · `godie-e00l.ex` | `finisherStrikeIndex`(⭐ 一格餵 **11** 段) · `shakeAmplitude` · `victimClipWindowMs` · `bladeTint` |
| `sub.doom-mark-cast` | 末日印記（詠唱標記） | 2 | `godie-h020.e` · `godie-hjai.e` | `doomScale` · `doomLifeSec` · `markVfxId` · `markDurationSec` |
| `sub.dive-dash-thunder` | 隱身俯衝（落地雷擊） | 4 | `godie-n01c.r` · `godie-nbbc.r` | `hideMs` · `missileScale` · `dustAtMs` · `impactVfxId` |
| `sub.forward-twin-blast` | 雙段爆破（小死＋大爆） | 2 | `godie-o00x.r` · `godie-ogrh.r` | `trigger` · `anchor` · `offsetForwardU` · `burstLifeSec` |

⭐ **K = 4，每一顆被 ≥2 支 script 呼叫**（票文 AC 的門檻）。
⭐ **每一格 `default` 的 `origin` 都是可解析的** `census:vfx-scripts/<id>#<段>.<欄位>`，
而閘**真的去讀那一格並比對值**（⛔ 不是「有沒有 census: 這五個字」）。

⭐ `sub.forward-twin-blast` 的 `trigger` / `anchor` 兩格是**量到會變**的：
同一對模型（`nedeathsmall` → `neutralbuildingexplosion`）在 `godie-e002.ex#9/#11`
用的是 `anchor:"target"` ＋ `on:"strike"`，在 `godie-o00x.r` 是 `anchor:"self"` ＋ `on:"castEffect"`
⇒ ⭐ 那兩格有**第二個量值**，所以它們是參數，⛔ 不是我猜「這裡應該可以調」。

### ⛔⛔ 第〇·四守則落在 schema 裡（⛔ 不是註解）

被 `bind` 的欄位**不可以**同時寫在 `segments` 樣板裡 —— superRefine ③ 逐格擋，訊息指名「兩個住處」。
⇒ 樣板段落**故意是不完整的**；完整段落只在 `expandVfxSubtype()` 之後存在。
而 superRefine ④ 拿**展開後**的結果去 `zVfxScriptSegment` parse ⇒ 上下界與跨欄規則一條都沒漏。

---

## ③ 棘輪基準線（⭐ 當場重量的）

| 量 | 我量到 | 票文寫 | 用哪個 |
|---|---:|---:|---|
| `content/abilities/*.json` | **421** | 422 | 量到的 |
| 被技能引用的不同特效 key | **179** | 178 | 量到的 |
| ⭐ **只被 1 支技能引用（專屬）** | **69** | 68 | ⭐ **`EXCLUSIVE_VFX_BASELINE = 69`** |
| 2–4 支 | 81 | 81 | — |
| ≥5 支 | 29 | 29 | — |
| 零 `call` 的 vfx-script | **10** | 11 | ⭐ **`CALLLESS_SCRIPT_BASELINE = 10`** |

⚠️ 差異全部是 **+1 / −1**：票文的技能母體多一支（`content/abilities` 今天 421 份），
而「11 支 script」是把 `_index.json` 算進去了。⛔ 沒有結構性的分歧。

⭐ **棘輪是雙向的**：變多 → 紅（有人又加了專屬積木）；
變**少** → 也紅，訊息指名新數字並要求把基準線降下來
（⚠️ 單向棘輪的病：做完一批之後閘照樣綠 ⇒ **下一批可以把它做回去**）。

---

## ④ 閘 `packages/shared/src/ops/vfxSubtypesRatchet.test.ts`（7 條）

| # | 斷言 |
|---|---|
| 0 | GUARD THE GUARD：subtypes / scripts / vfx 三個目錄真的讀到東西（⛔ 不是空轉） |
| 1 | 每一份 `vfx-subtype` 通過 schema（含「第二個住處」與展開後段落合法性） |
| 2 | **出處對得上** —— 逐格解析 `census:vfx-scripts/<id>#<i>.<欄位>`，去讀那一格，比對它等不等於 `default` |
| 3 | ⭐ **承重**：`expandVfxSubtypeRaw(doc)` 用預設值展開 == `derivedFrom` 每一支腳本的 `segments`（key 排序後逐位元組） |
| 4 | 棘輪①（雙向）：專屬特效數 |
| 5 | 棘輪②（雙向）：零 `call` 的 script 數 |
| 6 | **sentinel** ×3：(a) 把被 bind 的欄位寫回樣板 ⇒ schema 必須擋且訊息含「兩個住處」 · (b) 改掉 `default` 不改 `origin` ⇒ 出處檢查器必須抓到 · (c) 樣板少一段 ⇒ 等價檢查器必須抓到 |

### 突變紀錄（⭐ 承重那一條）

```
改壞：packages/shared/src/content/schema/vfxSubtype.ts
      const out = doc.segments.map((s) => ({ ...s }));   →   const out: Record<string, unknown>[] = [];
結果：npx vitest run …/vfxSubtypesRatchet.test.ts   EXIT=1
      × ③ 用預設值展開之後，與出處腳本的 segments 逐位元組相等（⭐ 承重）
      Tests  1 failed | 6 passed (7)     ⇒ ⭐ 只有承重那一條紅,其餘 6 條沒被誤傷
還原：python3 scripts/edit-or-die.py（反向）EXIT=0
```

⭐ 兩個替換都走 `scripts/edit-or-die.py`（對不上就 exit 2 且檔案一個位元組都不動）——
⛔ 不是 `python3 -c "…replace…"`（那一行在對不上時會印「✓」而檔案不動）。

---

## ⑤ 離開碼

| 指令 | EXIT |
|---|---:|
| `gh issue view 990` | 0 |
| 盤點腳本（census / segcensus） | 0 |
| `bash scripts/genguard.sh content/vfx-subtypes/x.json` | 0（**沒有產生器擁有者**） |
| `python3 scripts/edit-or-die.py`（×4 落地 ＋ ×2 突變） | 0 |
| `pnpm content:build` | **0** —— `vfx-subtypes 4 doc(s) 356bb93fc4d8` · `contentVersion cv_66f6c1827862` |
| `npx vitest run`（11 個檔，第 1 次） | 1 —— ⚠️ **只有 `shippedBundleHasTrackedSources` 紅，而且是「未 `git add`」**（見下） |
| `npx vitest run`（突變，第 2 次） | 1（**預期的紅**） |
| `pnpm typecheck` | **0** |

### ⚠️ 那一條紅的完整內容（⛔ 不是缺陷，是**還沒 `git add`**）

```
FAIL shippedBundleHasTrackedSources.test.ts > 每一份 _index.json 指名的 path 都在 git ls-files 裡
  config/ugc → content/config/ugc.json                 ← ⛔ **不是我的**（別條 lane 的未追蹤檔，這條閘在我動手之前就是紅的）
  vfx-subtypes/sub.bladestorm-8hit    → content/vfx-subtypes/sub.bladestorm-8hit.json
  vfx-subtypes/sub.dive-dash-thunder  → content/vfx-subtypes/sub.dive-dash-thunder.json
  vfx-subtypes/sub.doom-mark-cast     → content/vfx-subtypes/sub.doom-mark-cast.json
  vfx-subtypes/sub.forward-twin-blast → content/vfx-subtypes/sub.forward-twin-blast.json
```

⇒ ⭐ **`git add content/vfx-subtypes/` 之後這四列自動消失**（`config/ugc` 那一列要另一條 lane 處理）。
⚠️ 這正是 CLAUDE.md 記的 2026-08-02 事故形狀：bundle 有、原始檔沒進版控 ⇒ 線上退回骨架。
⛔ **千萬不要只 commit `content/bundle.json` 而漏掉 `content/vfx-subtypes/`。**

---

## ⑥ 動到的檔案

### 新增

| 檔 | 是什麼 |
|---|---|
| `packages/shared/src/content/schema/vfxSubtype.ts` | schema `vfx-subtype@1` ＋ **展開器**（`expandVfxSubtypeRaw` / `expandVfxSubtype`，純函式、決定性 —— `vfx-script@2` 的 `call` 與客戶端播放器共用**這一支**，⛔ 不寫兩份） |
| `content/vfx-subtypes/sub.bladestorm-8hit.json` | 17 段 · 4 參數 |
| `content/vfx-subtypes/sub.doom-mark-cast.json` | 2 段 · 4 參數 |
| `content/vfx-subtypes/sub.dive-dash-thunder.json` | 4 段 · 4 參數 |
| `content/vfx-subtypes/sub.forward-twin-blast.json` | 2 段 · 4 參數 |
| `content/vfx-subtypes/_index.json` | ⭐ `content:build` 的產物 |
| `packages/shared/src/ops/vfxSubtypesRatchet.test.ts` | 上面那 7 條閘 |

### 既有檔的改動（⭐ 逐一指名）

| 檔 | 改了什麼 | 為什麼**非改不可** |
|---|---|---|
| `packages/shared/src/content/schema/index.ts` | ＋1 行 import ＋ `COLLECTIONS["vfx-subtypes"]` 一格 | ⭐ **這就是 union 掛載處**（任務指定的「那一個檔」） |
| ⚠️ `packages/shared/src/content/bundle.test.ts` | `DOC_FLOORS` ＋ `"vfx-subtypes": 4` | ⛔ 它是 `Record<CollectionName, number>`（**窮舉**）⇒ 少一格 = `tsc` 紅 |
| ⚠️⚠️ `apps/editor/src/collections.ts` | `TEMPLATES` ＋ 一格 `"vfx-subtypes"` 新建骨架 | ⛔ 它也是 `Record<CollectionName, …>`（**窮舉**）⇒ 少一格 = `apps/editor` 的 `tsc` 紅 |

⛔⛔ **後兩個超出了我被指定的路徑柵欄**（任務逐字寫「⛔ 不要動 `apps/`」，並預期
union 只需要「那一個檔的那一行」）。⭐ 我的判斷：`COLLECTIONS` 有**兩個窮舉的 `Record<CollectionName, …>`
消費端**，不補就是把 `main` 的 `pnpm typecheck` 留成紅的 —— 而那違反的是更上位的硬性技術約束。
⇒ **兩處都只加了一格、零行為改動**，主 session 若要退回只需刪掉那兩格＋`COLLECTIONS` 那一格。

### ⛔ 一個位元組都沒動的（照柵欄）

`content/vfx/` · `content/vfx-scripts/` · `content/abilities/` · `packages/shared/src/content/schema/vfxScript.ts`

### `content:build` 的產物（留在工作樹，⛔ 未 commit）

`content/bundle.json` · `content/manifest.json` · 各 `_index.json` ·
`docs/技能標記機制與效果規則.md` · `docs/固有能力及寶具總覽.md` · `docs/editor-contract/ggd-skill-tiers.md`

---

## ⑦ 下一批要接的（⛔ 這一批**沒有**做）

1. **`vfx-script@2`** —— `segments[]` 多一種 `{"call":{"subtype","params"}}`，載入時用 `expandVfxSubtypeRaw` 展開。
2. **10 支 script 改用 `call`** —— 一支正規化器批次做，等價測試（展開後逐位元組相等）。
   ⭐ 這一批的閘**已經先驗過等價方向**（斷言 ③），所以正規化器只要讓 `derivedFrom` 那幾支真的改成 `call` 即可。
3. **棘輪②降到 2**（10 支裡有 8 支屬於那四對 ⇒ 改完剩 `godie-hart.r` 與 `godie-udea.r`）。
4. `vfxDocsBirthVisibility` 要掃**展開後**的段落（⛔ 不然一顆 alpha=0 的子模組會躲在 `call` 後面）。
5. ⚠️ 主 session 收工時跑一次 `pnpm skills:check` —— 我查過 `tools/` 底下**沒有**任何產生器枚舉
   `COLLECTION_NAMES`（只有 `tools/legacy-index/build_index.py` 的散文提到它），所以風險低，⛔ 但沒有實跑過。

---

## ⚠️⚠️ 收工時發現的**跨 lane 副作用**（⛔ 不是我的內容，但主 session 一定要看）

我跑的那一次 `pnpm content:build` **把另一條 lane 的未追蹤檔烘進了產物**：

```
?? content/config/ugc.json          ← 別條 lane 的（未 git add）
 M content/bundle.json              ← 我這一次 build 把 config/ugc 內嵌進去了
 M content/config/_index.json       ← 多了一筆 ugc 的 path
 M content/editor-target-profile.json
 M docs/技能標記機制與效果規則.md      ← spec:build 讀到了 ugc 的 schema
```

⭐ **這正是 CLAUDE.md 記的 2026-08-02 事故形狀**（「未追蹤來源被烘進產物」）：
bundle 有這份文件 → `_index.json` 有它的 path → **而原始檔沒有進版控**
⇒ 部署走 `git pull` ⇒ 線上拿到「bundle 有、原始檔與 schema 都沒有」⇒ 內容整份載入失敗、退回骨架。

⛔ **所以 `content/bundle.json` 不可以單獨 commit。** 兩條合法出路：

| 出路 | 做什麼 |
|---|---|
| ⭐ **一起送** | `content/config/ugc.json` ＋ `apps/admin/src/configForms/**` ＋ `packages/shared/src/content/schema/config/index.ts` 那條 lane 的東西**與 bundle 一起** commit |
| 或 **重建一次** | 等那條 lane 收完再由主 session 統一跑一次 `pnpm content:build`，⛔ 不要用我這一次的 bundle |

⚠️ 閘已經在叫了：`shippedBundleHasTrackedSources.test.ts` 的第一列逐字就是 `config/ugc`。
⭐ 它**在我動手之前就是紅的**（那一列與我的四列是**兩件事**）。
