# FIXTS — TS 原始碼註解誤導修正（TSCAN 29 筆的落地）

> 2026-08-25 20:30 · 依 `docs/_reports/TSCAN_temp_20260825-2000.md` 逐筆修 · 只動註解/字串，零邏輯改動
> 未 commit（lane 約束）；閘 `guardProseNamesTheGenerator.test.ts` 4/4 綠。
> GH#706 = 預留票號（主 session 開：缺守衛清單見下）。

## A 類（5/5 修）—— 幽靈守衛改成「尚無守衛（GH#706）」或改指真名

| 檔 | 做了什麼 |
|---|---|
| `apps/client/src/ui/platform/announcements.ts` + `announcements.test.ts` | publicFeedReaders.test.ts 標明從未落地；現有守衛只有 announcements.test.ts 兩條 |
| `packages/shared/src/content/schema/config/index.ts` | configFacadeSurface.test.ts → 「count 閘尚未存在（GH#706）」 |
| `packages/shared/src/sim/effects/revive.ts` | 兩個幽靈 pin → 「NO guard yet (GH#706)」 |
| `packages/shared/src/content/statNormalization.ts` | :18 改指 statNormalization.test.ts；:496 → JSON↔DEFAULT drift 尚無守衛（GH#706） |
| `packages/shared/src/content/aoeTiers.ts` | configDrift.test.ts → 尚無守衛（GH#706，附兩支現有測試為何不算） |

## D 類（1/1 修）

- `packages/shared/src/content/editModel.ts` —— 權威方向改正：runtime 以 **standalone** 為權威（registries.ts STRICT 模型）；雙寫理由改為 raw-doc 消費端（codex browser／admin 內容頁／editor preview）。

## B 類（22/22 修）—— 幽靈名→量到的真名（各一行）

B1 shippedContent.ts(×3)→shippedContent.test.ts · B2 condition.ts(×2)→condition.test.ts ·
B3 itemSets.ts+item.ts:423→sim/economy/itemSets.test.ts · B4 item.ts:573→legendaryClaims.test.ts ·
B5 mobs.ts+EntityViewRegistry.ts→mobBossNoClip.test.ts · B6 arenaRules.mobWaves.ts→mobBossNoClip+mobGroundRing ·
B7 bossIntro.ts→bossIntro.test.ts · B8 ambientVfx.ts→abilityToggleWiring.test.ts(TOGGLE_MASK_SLOTS) ·
B9 vfxCleanup.ts→vfxCleanupConfig.test.ts · B10 champion.ts→championAttributes.test.ts ·
B11 replayPolicy.ts→replayPolicyShipped.test.ts · B12 deathWard.ts補（現名 deathWard.test.ts）·
B13 contentFields.ts→apps/game-server/src/ai/buildPath.test.ts · B14 App.tsx+shellLayout.ts→shellScroll.test.ts ·
B15 VoxelBarcodePage.tsx→voxelBarcodeSave.test.ts · B16 progressChartGeometry.ts→progressChartRender.test.ts ·
B17 itemCardTheme.ts→itemCardSurfaces.test.ts · B18 w3xAbilityArt.ts→familyArtCoverage.test.ts ·
B19 modelFxRig.ts→modelFxRig.test.ts · B20 localRenderPose.ts→predictionAttackFacing.test.ts。

**B21**（draftCardStyle.ts:95）：查證 draftCardStyle.test.ts **只釘既有階級字面值、不從 config 推導覆蓋**
⇒ 依報告歸 A 類處理：「這條覆蓋尚無守衛（GH#706）」。
**B22**（bake.ts:23）：查證 `tools/voxel-gen/gen.test.ts:114` 真的做 bakeLook(lookFromArchetype) ↔ bakeArchetype
逐位元對比（每個 archetype）⇒ 改名指它。

## C 類（5/5 修）

- C1 `HeroReactionBubble.tsx` + `purchaseLines.ts` → `_purchase-lines.json`＋「底線刻意、fsStore 跳過 `_` 檔」（fetch 程式碼未動）
- C2 `mapSpecDoc.ts` 三住處第 3 行 → `apps/admin/src/configForms.ts` 的 `MAP_SPEC_SPEC`
- C3 `audioMap.ts`(×2)·`vfxSound.ts`·`sfxLayerCap.ts` → `content/config/vfx-families.json`；audioMap 兩處加「pitch:build 的產物」
- C4 `arenaRules.ts:925` → `content/_legacy/loot-tables/`＋後台可編輯半句改過去式
- C5 `voxelSkin.ts`(×2)·`beatDance.ts`(×3)·`texture.ts` → `packages/shared/src/voxel/boxman.ts`；texture.ts 改指 `pngWrite.ts`（並拿掉已不成立的 `Buffer.from` 括號 —— 現行 encodePng 收 Uint8Array）

## E 類（2/2 修）

- E1 `mapReportDoc.ts`·`map.ts`·`mapSpecDoc.ts:202`·`ambientVfx.ts:113` → `pnpm --filter @ggd/anime-arena-map map:gen`（mapReportDoc 加註兩支 script 的住處）
- E2 `castTimeFormula.ts` → `pnpm --filter @ggd/shared exec tsx scripts/deriveCastTimes.ts --write`；`expand.ts` 檔名補全路徑

## 另修（lane 清單第 8 項）

- `packages/shared/src/content/editorCapabilities.ts:1298` evidence 字串：`build_vfx_sound_bindings.py` → `` `bash scripts/genrun.sh pitch:build` ``。⚠️ **caps:export／spec:build 未跑**（全域鎖），主 session 收。

## Skipped（帶理由）

1. `tools/anime-arena-map/gen.ts:152,174,194,205` 的同病指令名 —— **tools/ 不在本 lane 範圍**（TSCAN E1 明說留主 session）。
2. `packages/shared/src/content/schema/ability.ts:549` 的 `conditionRng` —— 概念性引用（機制名，非檔案存在宣稱），不在報告 29 筆內，未動。
3. A 類的「當場補真閘」選項 —— 一律選「改標尚無守衛＋GH#706」；補閘要寫測試（動邏輯層面），留給票。
4. `castTimeFormula.ts:131`「castTimeCoverage.test asserts it」—— 不在報告內、未驗證，未動。

## GH#706 該收的缺閘清單（開票用）

① 全 public route 的 reader 對帳（announcements）② config facade export-surface count
③ revive fallback↔arena-rules drift ④ stat-normalization JSON↔DEFAULT drift
⑤ aoe-tiers JSON↔DEFAULT drift ⑥ draftCardStyle weapon-tier label 覆蓋（從 config 推導）
＋ 報告第 5 點的結構性建議：註解裡的測試名掃描器 → ops 測試＋棘輪（現值 32）。

改動檔案共 47 個（`git status` 可列）；vitest 1 次（閘綠）、無其他測試/typecheck（純註解字串改動）。
