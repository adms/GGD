# #547 投射物特效 —— 第三輪：量完之後，⛔ 這不是綁定的洞

> 2026-08-22 · lane: 投射物 · 柵欄 `content/projectiles/**` · `tools/vfx-bind/**` · `content/config/ability-vfx-bindings.json`
> owner 第三次提：「一堆攻擊投射物都沒移植 請儘快從 w3x 補上」

---

## 1. 量到的（⛔ 不是印象）

| | |
|---|---:|
| 活著的技能 | **420** |
| 其中**真的射出投射物**的 | **22** |
| 出貨的 `content/projectiles/*.json` | **21** 份 |
| 其中美術指向原作（`fx.w3x.*`）的 | **1** 份（`imported.thrust.arcane`） |
| 原作地圖裡**作者自己設過** `art:missile` 的活技能 | **76** |
| ⭐ 有飛彈美術意圖、卻**根本沒射投射物** | **68** |

### ⭐ 最重要的一個發現

**缺的不是綁定，是投射物本身沒被移植。**

420 支活技能只有 **22 支**會產生 `spawnProjectile`。原作裡作者親手替 **76 支**技能設了飛彈美術，
其中 **68 支**在 GGD 裡連投射物都不存在 —— 綁定推導**永遠碰不到它們**，因為
`_projectile_users()` 找不到任何人射那顆子彈。

⇒ 前兩輪把 #547 當成「綁定漏接」在修，⛔ 而那個方向的頭寸只剩 **1 顆**（見 §2）。

### #394「32 支投射物」的美術那一半 —— 查證結果

⛔ **沒有 32 支。** 出貨的投射物文件從頭到尾只有 **21 份**（含 `_index.json` 之外）。
`projectile@1.flight`（#394 的彈道姿態）有 **7 份**填了，`meshShape` 21 份都有 ——
**彈道那一半接上了，美術那一半只有 1/21。**

---

## 2. 剩下的綁定頭寸：**1 顆**，而且卡在別人身上

20 顆沒接的投射物，逐顆歸因（每顆都帶一句能被反駁的理由，`--report` 印得出來）：

| 閘 | 顆數 | 是什麼 |
|---|---:|---|
| 閘1 專屬 —— 沒有任何來源射它 | **7** | `basic-attack` · `imported.bolt{,.arcane,.ki,.lightning,.void}` · `imported.wave.lightning`。⭐ 全 repo 零引用（只剩 `content/_legacy/`）＝ **死重** |
| 閘1 專屬 —— 多個來源共用 | **5** | `imported.wave`(5) · `.ice`(3) · `.ki`(3) · `.physical`(3) · `imported.bolt.nature`(2) |
| 閘2 證據 —— GGD 原創，不在 w3x provenance | **5** | `sela.q.bolt` · `thorne.e.thorn` · `imported.wave.arcane` · `grail.projection-bolt` · `grail.tracking-bolt` |
| 閘3 美術 —— 抽不到 emitter | **3** | `imported.wave.earth`(shockwavemissile) · `.fire`(reddragonmissile) · `.void`(tinkerrocketmissile) |

⭐ **這 3 顆裡只有 1 顆是真頭寸**：`imported.wave.fire` ← `godie-n01c.e` ← `reddragonmissile`，
provenance 是 **w3a-override（作者意圖）**。另兩顆是 `stock-inherited` ⇒ ⛔ 依 provenance 契約不算意圖，接了不代表原作長那樣。

⇒ ⭐ **跑 `extract_stock_vfx.py` 收 `reddragonmissile` 一個 stem，`imported.wave.fire` 就自己接上**
（本輪已把這條路做進 `derive_projectiles`，見 §3，且已用合成 fixture 驗過會發生）。

---

## 3. 這一輪落地了什麼（`tools/vfx-bind/scan.py`，⛔ 沒有動任何出貨內容）

### ① 源 C 多一條路：零售 stock 家族（第〇·四守則）

`art:missile` 抽不到地圖自帶模型時，改試 `fx.w3x.stock.<stem>.p*` —— 與源 B **同一條命名規則**，
⛔ 不是一張手抄清單。⇒ `extract_stock_vfx.py` 哪天多收一個飛彈模型，覆蓋率**自己長出來**，
⛔ 不必回來改這支腳本。

**分支驗證（合成 fixture，temp 樹，⛔ 沒碰 repo）**：
放一份 `fx.w3x.stock.reddragonmissile.p00.json` 進 `content/vfx/` →
`投射物 1 → 2 顆接上原作飛彈`，`PROJ imported.wave.fire → fx.w3x.stock.reddragonmissile.p00`。
**突變**：把 stock fallback 那 8 行拿掉 → 掉回 1 顆、`imported.wave.fire` 消失 ⇒ 這段是承重的。

### ② 閘 1 看得到增益卡（第三守則 —— 一句自洽的假話比沒有話更貴）

`_projectile_users()` 在此之前只走 `content/abilities/`，於是
`grail.projection-bolt` / `grail.tracking-bolt` 被判成
**「沒有任何活著的技能射這顆投射物（它是備用原型）」—— ⛔ 而那句話是假的**：
`grail-c-20` / `grail-c-16` 正在射它們。現在理由說真正的那一個（GGD 原創，不在 provenance）。

⛔ **champion 刻意不算第二個使用者**：`content/champions/*.json` 內嵌的是同一支技能的鏡像
（mirror authority model），數進去會讓每顆投射物都變「共用」⇒ 閘 1 永遠不過 ——
一個看起來很嚴謹、實際上把功能整個關掉的閘。

### ③ 閘 3 的理由從「死路」改成**採購清單**

原本：「抽不出 emitter 文件」。現在：
「⇒ 跑 `extract_stock_vfx.py` 收 `reddragonmissile`，產出 `fx.w3x.stock.reddragonmissile.p00`，
這顆子彈**自己就會接上**」。

### ④ `--report` 新增 `WANT` 段 —— 那 68 支的採購清單

依模型 stem 聚合、依支數排序，分兩類：

| 類 | stem 數 | 技能支數 | 收件人 |
|---|---:|---:|---|
| **READY** 資產已在 repo | **10** | **16** | 內容側：缺的只是投射物文件 + 技能去射它 |
| **EXTRACT** 暴雪零售模型 | **28** | **52** | 先跑 `tools/w3x-import/extract_stock_vfx.py` 收這個 stem |

⚠️ **`need` 這一格差點說謊**：第一版照 `assetStatus` 分類，把**已經抽好的**
`warstompcaster` / `thunderclapcaster` 報成「還要去抽」——
因為 provenance 檔是 IMMUTABLE ARCHAEOLOGY，⛔ 它不知道 `extract_stock_vfx.py` 後來抽了什麼。
⇒ 改成問**出貨的 `content/vfx/`**，那是唯一的事實。

**READY 前段（可以今天就做）**：`crescent`(3) · `earthtornado2`(2) · `fireblast`(2) ·
`lightningtornado`(2) · `warstompcaster`(2) · `minitypeflame`(1) · `tectonicfury`(1) ·
`heronarutos4effect`(1) · `doom`(1) · `thunderclapcaster`(1)

**EXTRACT 前段（擋最多支）**：`mirrorimagecaster`(6) · `aiiltarget`(4) ·
`markofchaostarget`(4) · `reddragonmissile`(4) · `ailbspecialart`(2) · `avatarcaster`(2) ·
`boltimpact`(2) · `darkritualtarget`(2) · `obsidianregenaura`(2) · `parasitemissile`(2) ·
`resurrectcaster`(2) · `shockwavemissile`(2) · `steamtankimpact`(2) · `villagerkid1`(2)

---

## 4. 守衛

⛔ **沒有新增第二條同型守衛**（照 lane 指示）。沿用既有的
`packages/shared/src/content/vfxBindings.test.ts` 第 4 條 —— 它 `spawnSync` 跑
`scan.py --check`，逐位元組比對綁定表 + `content/projectiles/` 漂移雙向閘。

- `python3 tools/vfx-bind/scan.py --check` → **EXIT 0**，`✅ 綁定表與證據一致`
- `npx vitest run packages/shared/src/content/vfxBindings.test.ts` → **4 passed**
- 出貨內容 **0 位元變動**（`content/projectiles/` 與 `content/config/ability-vfx-bindings.json` 都沒動）
- ⛔ 沒跑 `content:build` / `skills:sync`（全域鎖）

---

## 5. 需要主 session 接線 / 升級給 owner

| # | 是什麼 | 為什麼不在這條 lane 做 | 大小 |
|---|---|---|---|
| **A** | 跑 `extract_stock_vfx.py` 收 `reddragonmissile` | `tools/w3x-import/**` 在柵欄外 | 1 個 stem ⇒ 投射物 1→2 |
| **B** | ⭐ **拆共用投射物**：`imported.wave`/`.ice`/`.ki`/`.physical`/`imported.bolt.nature` 一支一顆 | 要改 `content/abilities/*.json` 的 `projectileId`，而那些是 `tools/skill-remake/heroes/*.py` **產生的** | 5 顆 → 15 支技能 |
| **C** | ⭐⭐ **那 68 支根本沒有投射物** —— 這才是 owner 說的洞 | 同 B（要動產生器） | READY 16 支可以先做 |
| **D** | 7 顆**零引用**投射物（`basic-attack` / `imported.bolt.*` / `imported.wave.lightning`）要留還是退休 | 是 owner 的排序權（第零守則⑧） | 決策，不是工。⛔ 我沒有自己刪 |
| **E** | `IN_REPO_MESH_ONLY` 的飛彈（`crescent` 3 支 · `doom` 1 支）**網格在 repo、但沒有 emitter** | `projectile@1` 只有 `meshShape`（bolt/orb/shard 三選一），要吃真模型得加欄位 → `packages/shared` 在柵欄外，且 `schema/config.ts` 被別的 lane 鎖住 | 新 schema 欄位 + client render |

⚠️ **E 的決策點**（第一守則）：「投射物要不要能指定真的 w3x 網格」是一個決策，
⛔ 不該由我挑。欄位規格建議：`projectile@1.meshKey?: string`（soft ref 到模型），
省略 = 現行 `meshShape` 行為一位元不差。
