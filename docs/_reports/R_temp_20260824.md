# Lane R 報告 — 2026-08-24

## ① GH#681 練習靶 5 秒重生 — ✅ 落地（commit `67ad6c84`）

owner 逐字：「被打死過五秒會重生」。

| 檔 | 改了什麼 |
|---|---|
| `packages/shared/src/content/schema/practiceDoc.ts` | `dummyRespawnSec`（min 0 / max 60）＋ `PracticeRules` 欄位 ＋ `DEFAULT_PRACTICE_RULES` = 5（他的數字）＋ `resolvePracticeRules` 映射 |
| `content/config/practice.json` | `"dummyRespawnSec": 5` ＋ note 補 GH#681 一句 |
| `apps/admin/src/configForms.ts` | PRACTICE_SPEC 加「靶子重生秒數」一格（⚠️ **未 commit**，見下） |
| `apps/game-server/src/match/MatchController.ts` | `sustainPractice` 拆兩半：玩家照 `autoRevive`（GH#343 原樣）；靶子記**絕對 tick**，+`dummyRespawnSec` 才走**同一支** `restoreForNextRound`（既有復活路，⛔ 沒寫新的）。call site 閘 `practice?.autoRevive` → `practice` |
| `apps/game-server/src/match/practiceDummies.test.ts` | 守衛 ⑤：打死靶 → 留屍 ≥`dummyRespawnSec`（從 config 推導）→ 原地滿血復活 |

**我做的裁決（owner 不在，各留一格 rollback）**：
1. 靶子重生**不吃 `autoRevive`**（那格 describe 寫的是「**自己**倒下」＝玩家；關掉它測死亡表演時靶子照樣要回來，不然練到後面沒東西打）。rollback：`dummyRespawnSec` 本身。
2. `dummyRespawnSec: 0` ＝ 死掉下一 tick 站起來 ＝ 舊行為，一鍵 rollback。

**突變驗證**：拿掉靶子分支的 `restoreForNextRound` ⇒ ⑤ 紅（`expected false to be true` on `hp.alive`），①〜④ 仍綠。已改回。
**測試帳**：vitest 3 次（practice 三支 20/20 綠 · admin 撞別條 lane 的紅 · 突變）＋ tsc 1 次。

## ② GH#645 查證 — ✅ 真的在，已留言

大廳左欄排行榜面板「英雄」分頁：伺服器端按 `Picks` 降冪（`championusage.go:140-148`），勝率獨立一欄（`LeaderboardPanel.tsx:434-437`），公開 API 免登入免選英雄。缺口（已留言，不修）：外層分頁預設「玩家」要點一下；客戶端側零測試。

## ③ GH#655 查證 — 機制通了；字樣已改，已留言

兩顆按鈕在 `FriendsPanel.tsx:127-145`，但原字樣「同隊/對面」→ **已改成 owner 的「友方/敵對」**（只動兩行按鈕文字，commit `79a66efd`）。⚠️ 現狀是兩顆一鍵動作鈕，⛔ 不是帶選取狀態的「切換按鈕」——差異已留言請 owner 裁決。`side` payload → `PlanSeats()` 落座、入房徽章（沒滿足轉 DANGER 色）都在；受邀 toast 刻意不帶 side（wire format 凍結）。

## ⚠️ 留給主 session 的三件事

1. **`content/config/practice.json` 已改** ⇒ `bundle.json` stale。本 lane 禁跑全域鎖指令，請主 session 收尾統一 `pnpm skills:sync` ＋ `git add content/`。
2. **`apps/admin/src/configForms.ts` 未 commit**：我的 dummyRespawnSec 欄位與 weather lane 的 rainChance 欄位共檔，整檔 commit 會把他們未完成的工作掃進我的票 ⇒ 留在工作樹，由主 session 收。
3. **兩個不是我的紅**（都在別條 lane 的柵欄裡，未動）：
   - `packages/shared/src/content/schema/config/weather.ts:412` `DEFAULT_RAIN_CHANCE is not defined` ⇒ **admin 整包 vitest 進不了門**（schema index import 炸掉）。我的 admin 欄位測試因此沒驗到，weather lane 落地後要補跑 `configForms.test.ts` / `configDocCoverage.test.ts` / `configFormsShippedProse.test.ts`。
   - `apps/client/src/render/vfx/w3xRigHonoursDissipateCap.test.ts(68,69)` TS2554 ⇒ `pnpm typecheck` 唯一的紅，client vfx lane 的。
