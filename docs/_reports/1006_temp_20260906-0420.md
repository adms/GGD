# #1006 —— 「兩個併發第一次註冊都變成管理員」的修法，補一條**決定性**守衛

lane 報告 · 2026-09-06 · 票：GH#1006 · 修法本體：`fc6c620e7`（#979）

## 挑了哪條路：**B**（注入 seam 讓時序變成參數）

| 層 | 落點 | 是什麼 |
|---|---|---|
| seam | `apps/platform/internal/auth/bootstrap.go` | `OwnerBootstrap.adminsGate`（**unexported**）＋ `Service.gateAdmins()`；`claimOwnership` 的**兩次**耐久閘讀改走它，nil 時原路 `accounts.Admins`。⛔ 語意零改動（diff +25/−2，只換兩個呼叫點） |
| setter | `apps/platform/internal/auth/bootstrap_export_test.go`（新，`package auth`） | `auth.ScriptAdminsGate(s, fn)` —— 住在 `_test.go`，`go build` **永遠不編它** ⇒ 出貨 binary 結構上沒有這個門 |
| 夾具 | `apps/platform/internal/testutil/testutil.go` | `NewFreshDeployWith(t, before func(*server.Server), mutate…)` —— hook 在 `srv.Start` / `httptest` **之前**跑，seam 的寫入 happens-before 每一個 handler goroutine（race-clean，不靠 `-race` 沒開） |
| 守衛 | `apps/platform/internal/auth/bootstrap_test.go` | 下面兩條 |

### 守衛 ①：`TestClaimRechecksDurableGateAfterTakingTheClaim`（兩個子測）

腳本化的閘：第 1 次讀（claim 前）回空；第 2 次讀（claim 後）
· 子測 a：回一位 admin（**逐字重現 #979 的交錯**：A 在 B 等 claim 時把 owner 寫進去了）
· 子測 b：回 error（**fail-closed**，AC-3）
之後腳本用完 → 落回真的 store。

斷言（走真的 HTTP `POST /api/v1/auth/register`，真的 miniredis SetNX，真的 Repo）：
1. 201（拒絕授權**不可以**讓註冊失敗）
2. 閘被讀了 **2 次**（訊息直接說「沒有再讀一次」）
3. 回應與耐久檔的 `roles` 都**沒有** `admin`
4. `bootstrap:owner` 已釋放（⛔ 不可以把下一位卡 TTL 兩分鐘）
5. 下一位註冊者**仍然**拿到 owner（拒絕的代價不是永久的）

### 守衛 ②：`TestOwnerBootstrapSeamIsTestOnly`（票的 Known risks）

用 `go/parser` 讀本套件**非測試**原始碼（⛔ 不是 grep 字串）：
· 校準：必須**看得到** `adminsGate` 的宣告（改名不會讓它變成掃空氣）
· 斷言：沒有任何非測試檔對它做 `AssignStmt` / `KeyValueExpr`

## 量到的

| 項 | 結果 |
|---|---|
| `go vet ./internal/auth/... ./internal/testutil/...` | EXIT 0 |
| `go test -count=1 ./internal/auth/... ./internal/testutil/...`（第 1 次，完整） | `ok internal/auth 12.3s`（testutil 無測試檔） |
| **突變 A（AC-1）**：把 `claimOwnership` 的 post-claim 再讀整段刪掉 | ⭐ **兩個子測都紅**：`calls 1 ≠ 2`、`roles = [admin]`、耐久檔 `[admin]`；守衛 ② 照樣綠（它守的是 seam 不是修法）。改回來 |
| **突變 B（seam 守衛）**：在 `SetOwnerBootstrap` 加一行 `s.ownerBootstrap.adminsGate = nil` | ⭐ **紅**：「shipped (non-test) code must never set the owner-gate seam」。改回來 |
| AC-2（還原 ⇒ 綠） | 還原後的碼 ＝ 第 1 次完整跑的碼（綠）；依 CLAUDE.md 突變改回來不再跑第三次確認 |
| 測試次數 | vitest 等價：go test **3 次**（完整 1 · 突變 2，第 2、3 次都 `-run` 單支）· vet 1 次 |

## AC 對照

| AC | 狀態 |
|---|---|
| 1 把雙重檢查中和掉 ⇒ 新守衛紅 | ✅（整段刪除，比票裡 `> 99999` 更狠，因為連 error 分支都不在了） |
| 2 還原 ⇒ 綠 | ✅ |
| 3 `Admins()` 回 error 的路徑有斷言 | ✅ 子測 b |
| Known risk：seam 不可在正式站被打開 | ✅ 結構（`_test.go`）＋ 守衛 ②（突變驗過） |

## 誠實列出

- **測試/實作行數 ≈ 4×**（實作 +25/−2 ＋ testutil +18/−4；測試 +~130）。這張票**就是**測試票（修法已在 `fc6c620e7`），守的是「誰是管理員」（靈魂層／安全）；兩條守衛已壓到最薄（一個 table-driven 測試 + 一條 ~35 行的 AST 掃描）。
- **`docs/todo/auth.md` 沒加列**（不在柵欄內）：兩個新 `testkit.Cover` id（`auth-first-owner-claim-recheck` · `auth-first-owner-seam-test-only`）在 `todo:runtime` 會出現 **orphan beacon 警告**（只 warn，不擋）。⇒ 主 session 收工時補兩列即可（auth-46 / auth-47，Category security）。
- `gofmt -l` 對 `testutil.go` 本來（HEAD）就會叫（WSWait 那段註解的 doc-comment 格式）—— 我沒順手改它，柵欄外的東西不動。
- 守衛 ② 是 AST 掃描：它驗的是**程式形狀**（「出貨碼裡沒有任何寫入端」），這種性質沒有 runtime 行為可觀察（seam 在出貨 binary 裡根本不存在），所以 AST 是對的量尺，⛔ 不是形態⑥。

## SWITCH: none

這張票沒有任何玩家看得到的決策：出貨路徑零語意改動（`adminsGate` 在每一條出貨路徑上都是 nil），
seam 結構上不進 binary。沒有東西需要 rollback 開關。
