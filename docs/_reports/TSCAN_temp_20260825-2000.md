# TSCAN — 非測試 TS/TSX 原始碼註解誤導稽核

> 產出於 2026-08-25 20:00 · 唯讀 lane · 範圍：`packages/**/src/**/*.{ts,tsx}` ＋ `apps/**/src/**/*.{ts,tsx}`，排除 `*.test.ts*`
> 每一筆都**驗證過為假**（指令附在方法欄），⛔ 不收「語氣可疑」。
> 已修的 34 處（`docs/_reports/product-edit-misinfo_20260825.md`）不重報。

## 方法

1. 兩輪掃描：關鍵詞 grep（1,587 條註解候選）→ 路徑/測試名/指令名 token 抽取 → 對 `git ls-files` 全表 suffix 比對 + 檔案系統 + `package.json` scripts + `sync-io.json` owners 驗證。
2. 每一筆逐一開檔讀上下文，再找「等效測試是否以別的名字存在」（用該守衛宣稱斷言的符號 grep 測試檔）。
3. 自我修正過的註解（vfx.ts:1217、victoryCrown.ts:24、iconStyleDoc.ts:205、generateFamilyContent.ts:37 —— 都逐字寫著「那個檔案不存在」）**不算誤導**，跳過。誠實揭露（battlefieldIntel.ts:65「目前線上沒有人餵這個 JSON」、StatsHoverPanel.tsx:62、hudBottomCluster.ts:71、valhallaSandboxRules.ts:141「給 integrator 抄的清單」）也跳過。

## 總量

| 類 | 筆數 |
|---|---:|
| A · 宣稱的守衛測試檔**不存在、且找不到等效測試** | 5 |
| B · 宣稱的守衛測試檔不存在，**等效測試以別名存在**（改名即可） | 17 |
| C · 指向錯的檔名/路徑（照做會靜默無效） | 5 |
| D · 已被推翻的權威宣稱（category ④） | 1 |
| E · 指名從 repo 根打不出來的指令 | 2 |

---

## A 類 · 宣稱的守衛不存在，也沒有等效測試 —— 「有守衛」這句話本身是虛構的

⭐ 這一類與 mobTint 前例同型（第三守則檔頭第一例），但更貴：它們宣稱的守衛正是該檔自己描述的失敗形態的「永久防線」。

### A1 `apps/client/src/ui/platform/announcements.ts:17-20`
> "the permanent guard for it is ./publicFeedReaders.test.ts, which derives every public route from the Go source and fails when one has no client/admin reader."

- **驗證**：`git ls-files '*publicFeedReaders*'` → 0；全 repo 無任何測試從 Go 原始碼推導 public route 清單（`grep -rln "api/v1/public" --include='*.test.ts'` 只命中 announcements.test.ts，而它的檔頭:33 也引用同一個幽靈檔）。
- **為什麼貴**：這句話宣稱失敗形態 S6（後端做完、前端沒入口）已有**全路由**的永久閘 —— 下一個加 public route 的人會信「有東西會替我叫」。
- **修法**：改成「⚠️ 全路由的 reader 對帳守衛**尚未存在**（原本點名的 publicFeedReaders.test.ts 從未落地）；現有守衛只有 ./announcements.test.ts 的兩條（URL 有被請求、真的 LobbyScreen 有渲染文字）」，並開票補真的閘。announcements.test.ts:33 同句一起改。

### A2 `packages/shared/src/content/schema/config/index.ts:149-151`
> 「拆檔前 `export * from "./config"` 拿得到的每一個名字，拆檔後**一個不少**地從這裡出去（`configFacadeSurface.test.ts` 在數）」

- **驗證**：該檔不存在；grep 「從這裡出去/facadeSurface/exportSurface」in tests → 0。
- **為什麼貴**：這正是第〇·七守則拆檔三條件的第 2 條（閘）—— 宣稱有閘而沒有，等於 config.ts 9,162 行拆檔的門面**沒有守衛**。
- **修法**：把括號改成「⚠️ 這個count 閘尚未存在 —— 開票補」或當場補一條 30 行的 export-surface 快照測試。

### A3 `packages/shared/src/sim/effects/revive.ts:72-73`
> "`arenaRules.reviveHpDefaults.test.ts`-style drift is pinned by `sim/effects/reviveEffect.test.ts`."

- **驗證**：兩個檔名都不存在（`git ls-files '*revive*'` 只有 client 的 reviveCircleDeathFx / reviveBanner 與 game-server 的 revive.test.ts —— 都不是 sim 層 fallback↔arena-rules 的 drift）。
- **修法**：改成「⚠️ 這條 drift 目前沒有守衛」或補一條；⛔ 不要留一個具名的假 pin。

### A4 `packages/shared/src/content/statNormalization.ts:496` ＋ `:18`（同檔兩個幽靈名）
> :496「`configDrift.test.ts` 那一族在守（第一守則的三個住處）」 · :18「這是一條**不變量**（`statNormalizationInvariant.test.ts` 在守）」

- **驗證**：兩個檔名都不存在；且**沒有任何測試讀 `content/config/stat-normalization.json`**（`grep -rln "stat-normalization\.json" --include='*.test.ts'` → 0）⇒ JSON↔DEFAULT 的 drift 對這份 config 無人在守。statNormalization.test.ts 只用 DEFAULT_ 推導，不比對出貨 JSON。
- **修法**：:18 改指 `statNormalization.test.ts`（它有例外槽那一條）；:496 改成「⚠️ 這份 config 的 JSON↔DEFAULT drift 尚無守衛」並開票（其他 config 的同族守衛範本：`replayPolicyShipped.test.ts` / `itemCardShipped.test.ts`）。

### A5 `packages/shared/src/content/aoeTiers.ts:75`
> 「這五個數字與 `content/config/aoe-tiers.json` 必須一致，`configDrift.test.ts` 那一族在守」

- **驗證**：configDrift.test.ts 不存在；讀 `aoe-tiers.json` 的測試只有 `ops/skillTiersDocFresh.test.ts`（產生器 freshness，不是三住處 drift）；`aoeTiers.test.ts` 不讀出貨 JSON。
- **修法**：同 A4 —— 具名改掉，缺的閘開票。
- 同句第三處 `packages/shared/src/sim/cooldownRules.ts:85` 較輕：`cooldownFloor.test.ts` 真的讀 `cooldown-rules.json`（4 hits）⇒ 只要改名。

---

## B 類 · 守衛存在但名字是假的 —— 一律「把假名換成量到的真名」

| # | 檔:行 | 幽靈名（逐字出現） | 驗證到的真守衛（符號證據） |
|---|---|---|---|
| B1 | `packages/shared/src/content/__fixtures__/shippedContent.ts:59,167,330` | `shippedContentFixture.test.ts`（「逐份比對兩條路」） | 同目錄 `shippedContent.test.ts`（引用 `__resetShippedContentCache`/`shippedBundleIsFresh`） |
| B2 | `packages/shared/src/sim/content/condition.ts:62,1071` | `conditionRng.test.ts`（"Exported for the guard … calls it directly"） | `condition.test.ts:556`「drawChances 的次數 == conditionChanceCount」 |
| B3 | `packages/shared/src/content/schema/item.ts:423` ＋ `sim/economy/itemSets.ts:168` | `content/legendaryItemSets.test.ts` | `sim/economy/itemSets.test.ts`（requiredPieces/repeat 13 hits） |
| B4 | `packages/shared/src/content/schema/item.ts:573` | `content/legendaryCritStrike.test.ts`（「逐件釘死」） | `sim/economy/legendaryClaims.test.ts`（critStrike 16 hits） |
| B5 | `packages/shared/src/sim/mobs.ts:447` ＋ `apps/client/src/render/EntityViewRegistry.ts:271` | `sim/mobBossRing.test.ts` | `sim/mobBossNoClip.test.ts`（mobGroundRingDiameter/radius 19 hits） |
| B6 | `packages/shared/src/content/schema/config/arenaRules.mobWaves.ts:352` | `sim/mobRingIndependence.test.ts` | 同上 ＋ `apps/client/src/render/views/mobGroundRing.test.ts` |
| B7 | `packages/shared/src/content/schema/config/bossIntro.ts:148` | `apps/client/src/ui/hud/bossIntroShipped.test.ts` | `apps/client/src/ui/hud/bossIntro.test.ts`（boss-intro.json 3 hits） |
| B8 | `packages/shared/src/content/schema/config/ambientVfx.ts:22` | `apps/client/src/vfx/ambientToggleGate.test.ts` 的「第一條」 | `apps/client/src/ui/abilityToggleWiring.test.ts:145` `expect(TOGGLE_MASK_SLOTS).toBe(CASTABLE_SLOTS.length)` |
| B9 | `packages/shared/src/content/schema/config/vfxCleanup.ts:481` | `vfxCleanupPolicy.test.ts` | `content/vfxCleanupConfig.test.ts`（vfx-cleanup.json 2 hits） |
| B10 | `packages/shared/src/content/schema/champion.ts:219` | `championGrowthLayers.test.ts`（"pins the three-layer sum"） | `content/championAttributes.test.ts`（growth 55 hits） |
| B11 | `packages/shared/src/content/replayPolicy.ts:57` | `shippedReplayPolicy.test.ts` | `content/replayPolicyShipped.test.ts`（名字前後顛倒了） |
| B12 | `packages/shared/src/sim/deathWard.ts:101` | `nightPact.test.ts`（歷史敘述） | 現名 `sim/deathWard.test.ts` —— 敘述句補「（現名 deathWard.test.ts）」 |
| B13 | `apps/admin/src/contentFields.ts:106` | `botBuildPriorityRetired.test.ts`（「會在任何一份重新長出非空梯子時變紅」） | `apps/game-server/src/ai/buildPath.test.ts`（「這幾位又長出推薦出裝了…`.toEqual([])`」—— 宣稱的行為逐字存在，只是名字與住處全錯） |
| B14 | `apps/admin/src/ui/App.tsx:895` ＋ `apps/admin/src/ui/shellLayout.ts:31` | `shellLayout.test.ts` | `apps/admin/src/shellScroll.test.ts`（renderToString/`<main>` 3 hits） |
| B15 | `apps/admin/src/ui/VoxelBarcodePage.tsx:523` | `voxelBarcodeRender.test.ts`（"reads backgroundColor and height off this node"） | `apps/admin/src/voxelBarcodeSave.test.ts`（backgroundColor/height 17 hits） |
| B16 | `apps/client/src/ui/panels/progressChartGeometry.ts:9` | `progressChartGeometry.test.ts` | `progressChartRender.test.ts`（viewBox 8 hits） |
| B17 | `apps/client/src/ui/components/itemCardTheme.ts:11` | `itemCardWiring.test.ts`（「斷言的是那一行呼叫」） | `itemCardSurfaces.test.ts`（applyItemCardDoc 6 hits） |
| B18 | `apps/client/src/render/vfx/w3xAbilityArt.ts:197` | `familyArtIntegration.test.ts`（"asserts against w3xArtFor"） | `familyArtCoverage.test.ts`（w3xArtFor 12 hits） |
| B19 | `apps/client/src/render/modelFxRig.ts:295` | `modelFxRoundGrowth.test.ts`（「的前身探針」） | `modelFxRig.test.ts` |
| B20 | `apps/client/src/predict/localRenderPose.ts:7` | `localRenderPose.test.ts`（「直接對它做行為斷言」） | `predictionAttackFacing.test.ts:33,224+`（import 並逐模式斷言） |
| B21 | `apps/client/src/ui/panels/draftCardStyle.ts:95` | `weaponTierLabels.test.ts`（「operator 新增一階要補一行 —— 在守」） | `draftCardStyle.test.ts` 存在但**查無 label 覆蓋斷言**（"weapon:ex-release"/WEAPON 0 hits）⇒ 半個 A 類：改名之外要確認/補那一條 |
| B22 | `packages/shared/src/voxel/bake.ts:23` | `bake.test.ts`（"asserts the two produce identical bytes for all five archetypes"） | 最接近：`tools/voxel-gen/gen.test.ts` 的五個 sha256 pins（pngWrite.ts 檔頭自述）＋ `voxel/look.test.ts` 的 byte-determinism —— studio↔CLI 五 archetype 逐位元對比**找不到逐字等效**，改名時要驗 gen.test.ts 是否涵蓋，不涵蓋歸 A 類 |

修法統一：把幽靈名換成右欄真名（一行 Edit）。⚠️ 換名時順手確認右欄那條真的斷言註解宣稱的行為（B21/B22 兩筆已標不確定）。

---

## C 類 · 指向錯的檔名/路徑 —— 照做會靜默無效

### C1 `apps/client/src/ui/HeroReactionBubble.tsx:15` ＋ `apps/client/src/render/intermission/purchaseLines.ts:10`
> "The lines come from `content/config/purchase-lines.json`" / "they ship as the plain static asset `content/config/purchase-lines.json`"

- **驗證**：出貨檔是 **`content/config/_purchase-lines.json`**（`git ls-files` 唯一命中），fetch URL 同檔 :36 逐字 `"/content/config/_purchase-lines.json"`。無底線版不存在。
- **後果**：照註解去編（或建）`purchase-lines.json` → 檔案永遠不被讀，而 fetch 是 404-tolerant ⇒ 零報錯（失敗形態②）。
- **修法**：兩處補上底線並加一句「底線開頭是刻意的 —— fsStore 跳過 `_` 檔，所以它不進 content bundle」。

### C2 `packages/shared/src/content/schema/mapSpecDoc.ts:9`
> 「3. `apps/admin/src/mapSpec.ts`（`SHIPPED_MAP_SPEC` + 欄位 union + 標籤）」

- **驗證**：該檔不存在；`SHIPPED_MAP_SPEC` 全 admin 0 hits。真住處：`apps/admin/src/configForms.ts:3878` 的 `MAP_SPEC_SPEC`。
- **後果**：三住處清單指向幽靈檔 ⇒ 下一個人會**新建**它 ⇒ 第四個住處。
- **修法**：第 3 行改「`apps/admin/src/configForms.ts` 的 `MAP_SPEC_SPEC`」。

### C3 `content/vfx-families.json`（4 處）：`apps/client/src/audio/sfxLayerCap.ts:13` · `apps/client/src/audio/vfxSound.ts:188` · `packages/shared/src/content/schema/config/audioMap.ts:46,65`
- **驗證**：真路徑 `content/config/vfx-families.json`（且它是 `pitch:build` 的產物 —— sync-io 認領）。無 config 前綴的路徑不存在。
- **修法**：四處補 `config/`；audioMap 那兩處順手加半句「（`pitch:build` 的產物）」—— 這正是既有稽核 C4 點名的沉默產物。

### C4 `packages/shared/src/content/schema/config/arenaRules.ts:925`
> 「`content/loot-tables/quest-rewards.json` 同時是 `starter.go` 的 DRAFT 白名單面…以及後台三選一抽獎池分頁的一個可編輯文件」

- **驗證**：該路徑已不存在 —— `starter.go:255` 逐字「2026-08-18: quest-rewards and round-reward **moved to content/_legacy/loot-tables/** entirely」；實測 `content/_legacy/loot-tables/quest-rewards.json` 在。
- **修法**：路徑改 `content/_legacy/loot-tables/`，「後台可編輯文件」半句刪除或改成過去式（retirement 之後那一頁不再編它）。

### C5 `tools/voxel-gen/boxman.ts`（現在時態引用）：`apps/client/src/render/views/voxelSkin.ts:51`（"must match `tools/voxel-gen/boxman.ts` TEX_EDGE"）· `apps/client/src/render/beatDance.ts:12,54,97` · `packages/shared/src/voxel/texture.ts:18`（"`tools/voxel-gen/png.ts` wraps the result"）
- **驗證**：`tools/voxel-gen/` 現存檔為 barcodeExtract/build/extract/gen/pngRead/testImages —— boxman.ts、png.ts、clips.ts 都已 re-home 到 `packages/shared/src/voxel/`（各檔頭自述"Re-homed"；TEX_EDGE 實住 `packages/shared/src/voxel/boxman.ts:88`）。
- **注意**：`voxel/clips.ts:34`、`pngWrite.ts:4`、`glbWrite.ts:4`、`archetypes.ts:22` 的 "Re-homed from …" 是**過去式的出處註記，不算誤導，不改**。要改的只有上列**現在時態**的五處。
- **修法**：路徑換成 `packages/shared/src/voxel/boxman.ts`（voxelSkin.ts:51 那句是可驗證約束，最優先）。

---

## D 類 · 已被推翻的權威宣稱（category ④）

### D1 `packages/shared/src/content/editModel.ts:15-18` ⭐
> "The SIM reads the EMBEDDED copy (sim/content/registry.ts registers `def.abilities[slot]`), so an editor that saved only the standalone doc would look like it worked and change nothing in game."

- **驗證**：與現行權威模型**方向相反** —— `content/registries.ts:138`「Since `registerChampion` made the standalone doc **authoritative**」、:127-129「standalone — the one that now **wins at runtime** / embedded — **ignored** unless the standalone omits it」、:186-190「standalone abilities go in BEFORE champions, and registerChampion will not overwrite」。owner 裁決的 STRICT 模型也是 standalone→embedded（記憶 ggd-mirror-authority-model）。
- **後果**：讀這段的人會把 drift 往**反方向**修（copy embedded→standalone）—— 正是鏡像規則要防的動作。`writePlan()` 雙寫本身仍正確，錯的是理由句。
- **修法**：改成「Runtime 以 **standalone 為權威**（registerChampion 先註冊 standalone、不覆寫；embedded 只填 standalone 省略的欄位）。仍然雙寫的理由：raw-doc 消費端（codex browser、admin content page）讀的是未經註冊的 champion doc（見 registries.ts auditAbilityMirrorDrift 檔頭）。」

---

## E 類 · 指名從 repo 根打不出來的指令

### E1 `pnpm map:gen` / `pnpm map:check`（4 處）：`packages/shared/src/content/schema/mapReportDoc.ts:4` · `schema/map.ts:8` · `schema/mapSpecDoc.ts:202` · `schema/config/ambientVfx.ts:113`
- **驗證**：root `package.json`、`packages/shared/package.json` 都沒有 map:gen/map:check（`grep` 0 hits）；它們只住在 `tools/anime-arena-map/package.json`。從 repo 根 `pnpm map:gen` → Command not found。這與 prose:apply 是同型（幽靈指令名），差別只在它在某個子包裡存在。⚠️ 它也不在 `skills:sync` / `sync-io.json` 的 38 步之內。
- **修法**：四處改成 `pnpm --filter @ggd/anime-arena-map map:gen`（或在 tools/anime-arena-map/ 內跑）。⛔ 產生器自己的訊息（tools/anime-arena-map/gen.ts:152,174,194,205）同病，但 tools/ 不在本 lane 範圍，列這裡供主 session 一起改。

### E2 `scripts/deriveCastTimes.ts --write`（2 處）：`packages/shared/src/content/castTimeFormula.ts:132` · `content/templates/expand.ts:2344`
- **驗證**：repo 根沒有 `scripts/deriveCastTimes.ts` —— 它住 `packages/shared/scripts/`。同檔案的 `contentValidate.ts:183` 印的是正確全形：`pnpm --filter @ggd/shared exec tsx scripts/deriveCastTimes.ts --write`。
- **修法**：補 `--filter @ggd/shared exec tsx` 前綴（低優先：檔案確實存在，只是路徑是 package-relative）。

---

## 不報的（查過、為真或非誤導）

- `schema/config/match.ts:255` exportContentToJson 宣稱 —— 腳本在 `packages/shared/scripts/`，其 match 字面值確實無 boss 區塊 ⇒ 宣稱為真。
- `sim/navRoute.ts` 檔頭的「明令禁止 content:build」—— lane 鎖歷史敘述，為真。
- `content/config/battlefield-intel / hud-stats / hud-layout / pad-hud-focus` 缺檔 —— 註解**自己誠實揭露**是待交接。
- `w3xFamilies.ts:13` —— `content/assets/vfx/w3x-families.json` 實存；`_w3x-families.json` 是明標的歷史名。
- `HudBoundaryGroup.tsx:30` 寫 `.test.tsx` 實為 `.test.ts` —— 副檔名級瑣碎，不列。
- `tierSnap.ts:69` 等「文件 id 與檔名對齊」句 —— 描述命名慣例，非存在宣稱。

## 給主 session 的修序建議

1. **A 類 5 筆**先修 —— 每一筆都要二選一：補真閘，或把「在守」改成「尚無守衛＋票號」。⛔ 不可只刪句子（那是把缺閘藏起來）。
2. D1（editModel 反向權威）單獨一個 commit —— 它會改變讀者的修復方向。
3. B 類 17 筆是機械改名，可以一個 commit 掃完；B21/B22 改名前先確認右欄斷言。
4. C/E 類路徑與指令替換一個 commit。
5. ⭐ 結構性建議：`guardMessagesNameTheGenerator.test.ts` 只管**測試的失敗訊息**；本稽核的 32 個幽靈測試名全部住在**非測試原始碼的註解**裡，沒有閘。同一支掃描器（token 抽取 + git ls-files suffix 比對）可以直接變成一條 ops 測試 ＋ 棘輪（現值 32、只准變短）—— 這正是把這份報告變成「不會再長回來」的方法。
