# GH#1005 —— 7 支技能 `castTimeSec` 變 undefined：⛔ 前提是假的，載入器一個位元組都沒吃

lane：castTime 載入路徑 · 2026-09-06 · ⛔ 未 commit（主 session 收）

## TL;DR

| | |
|---|---|
| **票文主張** | 「全新 clone 的載入器在某個環境下把 `castTimeSec` 吃掉；剩下的差異只有作業系統」 |
| ⭐ **量到的** | 那 7 支 **正好** = `batch1.py` 擁有的 91 支 ∩ `castTimeSec > 1`（`lists.json` 的 95 列裡 batch1 擁有的就是這 7 列，一支不多一支不少） |
| ⭐ **根因那一行** | `tools/skill-remake/batch1.py:441` 照 RETIRED **先把 `castTimeSec` 丟掉**寫進磁碟（含英雄卡鏡射 :454），`deriveCastTimes.ts --write` 之後才補回 ⇒ ⭐ 磁碟上有一個「**這 91 支沒有 `castTimeSec`**」的窗 |
| **誰在窗裡讀樹** | 2026-09-05 的 `unit` job：`apps/content-api/src/editorSourceSurvivesSync.test.ts` 第一版在**出貨樹**上跑真的 `skillremake:json`（逾時被殺 ⇒ `finally` 跑不完 ⇒ 68 份掉 `castTimeSec`，GH#1002 檔頭逐字），而同一個 job 裡 `apps/admin/src/skillLists.test.ts ③` 跑 `speedlists:check` ⇒ 讀到那棵樹 ⇒ 印出「7 支 undefined」 |
| **與 OS 的關係** | ⛔ **零**。`git show origin/main:<檔>` 有值是對的 —— 但 CI 那一刻的**工作樹**已經不是 origin/main |
| **今天還會不會發生** | 寫者已進 mkdtemp 沙盒（`915581369`，#1002）· 讀者已退休（`449d8f807`）· 內容樹鎖（#950）· `test-tree-clean-check.sh` 在 `ci.yml:167` ⇒ 這條路今天關著。⚠️ 但 **`contract` job 從沒跑到 `speedlists:check`** —— 它死在更前面的 `board:check`（見「柵欄外」） |
| **玩家看得到嗎** | ⛔ 看不到。正式站載入的是 commit 的樹；本機 `loadContentCached`（miss 路徑）對 7 支回 `2 / 1.633 / 1.433 / 1.233 / 1.033 ×3`，與 JSON 相同 ⇒ 票文「Known risks」那句**不成立** |

## 二分（票文 Scope 2）—— 逐層都是綠的

| 層 | 問的 | 答 |
|---|---|---|
| `ContentLoader.load()` → Zod | `castTimeSec: z.number().min(0).optional()`（`schema/ability.ts:855`）會不會丟 | ⛔ 不會；quarantined = 0 |
| `withTiersCore` → `resolveCastTimeTierOnDoc` | `castTimeTier` 缺席時回什麼 | `return def`（原物件，`castTimeTiers.ts:93`）⇒ 不動 |
| `applyCastTimeRules` | 夾到上界時回什麼 | 回夾後的**數字**（`castTimeRules.ts`），⛔ 而且它根本不在載入路徑上 —— 只在 `{{cast}}` 渲染與施法當下用 |
| `withApCoeff` / `withProse` | 會不會重建物件掉欄位 | 都是 `{ ...d, … }` spread |
| `registerChampion.fillGaps` | 會不會蓋掉 standalone | 只**補**缺格；standalone 贏 |
| ⭐ 磁碟 | 那一刻磁碟上有沒有 | ⛔ **沒有** —— 見下面的重現 |

## 重現（沙盒，⛔ 出貨樹一個位元組沒動）

沙盒 = `git archive HEAD content/abilities` + 工作樹 `content/`（不含 assets）+ `tools/skill-remake` + `skill-tag-manifest.json`，
與 `apps/content-api/src/testSourceSandbox.ts` 同一個形狀。載入端一律是**出貨的**
`ContentLoader(new FsContentSource(root))` + `registerAll`。

| 階段 | 動作 | 登錄表裡有 `castTimeSec` 的支數 | `> 1s` 的列 | 7 支 |
|---|---|---:|---:|---|
| ⓪ 出貨樹 | —— | 340 / 421 | 145 | `2 / 1.633 / 1.433 / 1.233 / 1.033 / 1.033 / 1.033` |
| ① 窗 | `python3 tools/skill-remake/batch1.py --no-build` | **291** / 421（掉 49） | **138** | ⭐ **全部 undefined，而技能仍在登錄表裡** —— 與 CI 診斷逐字相同 |
| ② 補回 | `buildIndexes.ts` → `deriveCastTimes.ts --write`（1 趟，`WROTE 50 ability docs, 14 champion docs`） | 340 | 145 | 全部回到 ⓪ 的值 |

⓪ − ① 的 `> 1s` 集合差 = `{godie-e00s.r, godie-e00w.r, godie-edem.ex, godie-edem.q, godie-emfr.ex, godie-emns.r, godie-h02v.r}`
== 票文的七支（`== True`）。

⇒ ⭐ 「7 支共同點 castTimeSec > 1」是 `CAST_LIST_MIN_SEC = 1` 這條**門檻的選擇效應**，
⛔ 不是 `castTimeMaxSec = 1.0` 的夾子在載入時作用 —— 母體其實是「batch1 擁有的 91 支」。

## 落地

| 檔 | 做了什麼 |
|---|---|
| ⭐ 新 `packages/shared/src/content/castTimeShippedRegistry.test.ts` | 承重守衛：出貨樹 × 出貨載入路徑，**兩個方向**（磁碟有 ⇒ 登錄表要有且同一個數 · 磁碟沒有 ⇒ 不可憑空長出）＋ 英雄卡影子與 `Abilities` 同一個數 ＋ sentinel 兩向校準（拿掉一格 ⇒ 指名它；憑空一格 ⇒ 指名它）。母體從磁碟數，⛔ 不釘 340 |
| `docs/_reports/1005_temp_20260906-1541.md` | 本報告 |
| ⛔ 載入器／`castTimeTiers.ts`／`castTimeRules.ts`／`registry.ts` | **零改動** —— 沒有缺陷可修。改了才是在替一個假前提背書 |

### 測試（≤ 3 次 vitest：用了 2 次）

| # | 指令 | 結果 |
|---|---|---|
| 1 | `npx vitest run src/content/castTimeShippedRegistry.test.ts` | ✅ 3/3（1.08s） |
| 2 | **突變**：`castTimeTiers.ts:93` `return def` → `return { ...def, castTimeSec: undefined }`（`edit-or-die.py`，對不上就 exit 2） | 🔴 2/3 紅，訊息逐支 `godie-e001.e: 磁碟 0.567 → 登錄表 undefined` … ；`edit-or-die.py` 還原，`git diff` 空 |
| — | `pnpm -s speedlists:check`（dev 樹，read lock） | EXIT 0，3.8s |

## 驗收對票

| AC | 狀態 |
|---|---|
| 1 乾淨 **Linux** clone 上 7 支 = JSON | ⚠️ 本 lane 無 docker（指示「本機重現即可」）。⭐ 機制已在沙盒逐步重現且與 OS 無關；Linux 那一次由 `contract` job 的 `speedlists:check` 驗 —— **但它今天跑不到那一步**（`board:check` 先紅，見下） |
| 2 `speedlists:check` EXIT 0 | ✅ 本機；CI 待 `board:check` 解 |
| 3 拿掉任一支 ⇒ 紅並指名 | ✅ 守衛 + 突變（上表 #2） |

## 柵欄外 —— 請主 session 決定

1. ⚠️ **`contract` job 今天死在 `board:check`**（`ggd-board.html is STALE`，run 34013011989 05:04Z），
   所以 `speedlists:check` 及其後 ~30 支 `--check` 在 CI 上**一支都沒跑**。與 #1005 無關，但它讓 AC-1/2 的 CI 證據拿不到。
2. `tools/skill-lists/gen.mjs` 的第三層診斷可以再加**第四層**：印 `git status --porcelain content/` ——
   「磁碟沒有」與「載入器吃掉」在它今天的輸出裡長得一模一樣；一行 dirty 清單就能把這張票省掉（⛔ 不在我的柵欄：`tools/`）。
3. `batch1.py` 的兩段式寫入（先丟 `castTimeSec` 再 derive）是**設計**（RETIRED + 收斂迴圈），⛔ 我沒動；
   但任何「在窗裡讀樹」的讀者都會看到同一件事 —— `content-tree-lock.py` 只擋走 `genrun.sh`/`gencheck.sh` 的人。
4. 票文「Known risks：七支在正式站上的吟唱時間不是內容寫的值」**不成立**，建議關票時更正（更正也要過同一道證據門檻 —— 證據就是上面 ⓪ 那一列）。
