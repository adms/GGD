# GH#1042 鋼鐵尾巴（`godie-ofar.w` 58-02）被動只活在英雄卡內嵌副本 —— lane 報告

> lane：併行（主工作樹）· 2026-09-06 · ⛔ 未 commit、未跑任何產生器（由主 session 收工）

## TL;DR

| | |
|---|---|
| 權威 | ⭐ **英雄卡內嵌版是對的**（卡面 ＋ 原作 w3a `A04U` 三者一致），⛔ 獨立檔漏了 |
| 修 | `content/abilities/godie-ofar.w.json` 補上 `passive.ranks[5].hooks[]`（逐位元等於內嵌版，鏡射後零差異） |
| 閘 | 新 `packages/shared/src/content/abilityMirrorSemantic.test.ts`：帶機制欄位**單邊**⇒紅（兩個方向）＋ 出貨普攻路徑的行為守衛 |
| 同型掃描 | ⭐ **全庫只有 1 支**（就是它）。`content/_legacy/` 0 支 |
| 突變 | M1 拔掉 standalone 的 `passive` ⇒ 閘紅並指名 `godie-ofar.w passive: embedded-only`；⚠️ 行為守衛**仍綠**（`fillGaps` 撐著）—— 這正是閘存在的理由 |

## ① 權威判定（卡面 ＋ JASS/w3a）

| 來源 | 說什麼 |
|---|---|
| 卡面（兩份副本同文） | 「攻擊時有 **10%** 機率增加 **75** 點破壞力，並有機會將敵人**震昏 0.01 秒**」 |
| w3a `A04U`（`tools/w3x-import/out/GoDieEX22s/ABILITY_W3A.json`） | base **`AHbh`（重擊）**：`Hbh1` 機率 **10/10/10/10** · `Hbh3` 傷害加成 **75/150/225/300** · `duration`/`hero_duration` **0.01** · buff `B03D` |
| 英雄卡內嵌 `abilities.W.passive` | `onBasicAttack` · `chance 0.1` · `damage flat 75/150/225/300/300` · `applyStatus burnstun 0.067s stun:true` |
| ⛔ 獨立檔（修之前） | 只有 `effects: applyBuff as +0.25/3s`，零 `passive` |

⇒ 內嵌版是 w3a 的逐欄翻譯（第 5 階夾到最後一欄 300；0.067 s = 30 Hz 下 2 tick，模擬能交付的最短窗口，`3e9634e87` 的卡面當時也這樣寫）。⛔ 沒有改任何數值。

## ② 根因：⭐ 這是 2026-08-23 的**回歸**，不是「從來沒有」

| commit | 對 standalone 做了什麼 | 對英雄卡做了什麼 |
|---|---|---|
| `3e9634e87` 2026-07-31（owner 七條技能修正） | ⭐ **加上** passive（+31 行） | 加上同一份 passive |
| `d58df591c` 2026-08-23（「收斂七對變身鏡射到產生器側」） | ⛔ **整份重寫，passive 被拔掉**（−122 行）、描述改回原句 | 只動描述 1 行 —— passive 留著 |
| `apply_tiers.py` GH#789 2026-08-27 | —— | 發現單邊，**刻意**把 `passive` 放進 `MIRRORED_UPDATE_ONLY`（只覆寫不刪）保住它 |

### ⚠️ 票文前提的更正（⭐ 走完那條路驗過，⛔ 不只是推翻）

票文：「下一次同步會用空的獨立檔覆蓋英雄卡」。
**今天的 `apply_tiers._mirror()` 不會**：`MIRRORED_UPDATE_ONLY` 那一段逐字「沒有 else：內嵌版獨有的分支留著」。
而 runtime **從來沒壞**：`registerChampion` 的 `fillGaps` 從內嵌版補上 standalone 沒有的 `passive` —— M1 突變下行為守衛仍量到 13 次追擊／13 tick 震昏就是證據。

⭐ 真正的風險是**任何整份重寫 standalone 的路徑**（`d58df591c` 就是一次）、編輯器存檔的鏡射、或有一天把 `passive` 從 `MIRRORED_UPDATE_ONLY` 挪進 `MIRRORED`。四種都是「schema 全綠而機制消失」（失敗形態②）——閘要在**資料層**響，⛔ 不能靠 runtime 的 fillGaps 撐。

## ③ 改了哪些檔

| 檔 | 改動 | genguard |
|---|---|---|
| `content/abilities/godie-ofar.w.json` | +120 行：`passive` 插在 `vfxKey` 之後（與內嵌版位置一致）。⭐ `json` 語意逐位元等於內嵌版 | 「不擋你」（只有正規化器 `castderive:build:raw` 就地改欄位）；`grep -rl godie-ofar tools/` 只命中普查／匯出資料，⛔ 沒有 `heroes/*.py` 來源 |
| `packages/shared/src/content/abilityMirrorSemantic.test.ts` | 新，155 行 / 4 條 | —— |

⛔ 沒動：`content/champions/*.json`（主 session `tiers:apply` 鏡射；這一次鏡射會是 no-op，因為兩份已相同）、卡面、同步方向、任何數值、`godie-u034.*`／`godie-ucrl.*`（小傑 lane）。

## ④ 閘的形狀

**① 語意鏡射**：`SEMANTIC = passive · effects · template · castType · targetsEnemies · cooldown · manaCost · range · radius · maxRank`（帶機制 ＋ `apply_tiers.MIRRORED` 上的欄位）。單邊 ⇒ 紅，訊息分兩個方向：
- `embedded-only ⛔ 機制只靠 fillGaps 活著，重寫 standalone 就會拔走`（本票的形狀）
- `standalone-only ⚠️ 英雄卡鏡射落後 —— python3 tools/skill-remake/apply_tiers.py`

⚠️ **鏡射前紅是預期的**：併行 lane 只准編 standalone，主 session 收工跑 `tiers:apply` 之後綠。這一次兩份已相同 ⇒ 鏡射前後都綠。
豁免表 `EXEMPT` 今天為空；加豁免要指到存在的技能（測試會驗）並寫理由。
「兩邊都有而值不同」仍由 `abilityMirror.test.ts` 管，⛔ 沒抄第二份。
Sentinel：餵一對假的 embedded-only 進純函式 `oneSided()`，抓不到就紅（掃描器自證不瞎）。

**② 行為守衛（AC#3）**：出貨內容（bundle/樹）→ `registerAll` → `spawnChampion(godie-ofar)` ×2（遠程 8.2，靶距 2）→ 每 tick 靶回滿血、釘原地、`nav.attackTarget` → `world.step`（⛔ 不手呼 `fireHooks`；遠程走 `ProjectileSystem` 落地那一格的 `fireHooks("onBasicAttack")`）。種子 1042 · 6000 tick：
- W 1 階：`damage` 事件中 `origin !== "basic"` 且 source=皮卡 **13 次**、靶 `stun && expiresAtTick > tick` **13 tick**
- W 0 階：**0 / 0**（兩個方向）
**③ 卡面↔獨立檔互釘**：正則從 `description` 撈「N%機率增加M點」，對 standalone 的 hook `chance`／`flat`／`stun` —— ⛔ 數字不住在測試裡。

## ⑤ 測試結果（vitest 2 次，≤3）

| 次 | 狀態 | 結果 |
|---|---|---|
| run 1 | 修好之後 | **4/4 綠**（0.98 s） |
| run 2 = **M1** | `edit-or-die.py` 拔掉 standalone 的 `passive`（exit 0，替換 1 處） | **2 紅 / 2 綠**：① 出貨零支 → 紅 `godie-ofar.w passive: embedded-only`；③ 互釘 → 紅「standalone 沒有 onBasicAttack 被動」；② 行為 → ⚠️ **綠**（fillGaps；probe 13/13） |
| 還原 | `edit-or-die.py` 反向（exit 0）；`passive` 與內嵌版 `json` 相同、鍵序不變 | ⛔ 沒有 `git checkout`；還原後不再跑（改壞前已綠） |

⚠️ 行數：測試 155 vs 實作 120（1.3×）—— 高於「≤ 實作行數」。理由：閘涵蓋的是**全庫 284 對**而不只這一支；檔頭 22 行是根因記錄。要再砍就砍檔頭，⛔ 不砍 ② 的兩個方向。

## ⑥ 同型掃描（全庫）

| 母體 | 掃法 | 結果 |
|---|---|---|
| `content/champions` 71 位 × Q/W/E/R = 284 對 | 上述 10 個語意欄位單邊 | ⭐ **1 支：`godie-ofar.w passive`（本票）**；修後 **0** |
| `content/_legacy/**` | 同上（`passive`／`effects`） | **0** |
| 兩邊都有 passive | —— | 41 對（32 對帶 hooks）⇒ 掃描器有東西可看 |

### 順手看到、⛔ 沒修（Non-goals：其餘開票由 owner 排）

- ⭐ **`godie-o02l.w`（神騎寶貝 - 皮卡丘，同 join key 58-02）**：卡面同一句「10% 機率 +75 · 震昏」，⛔ **兩份副本都沒有 passive**（只有 `applyBuff as +0.25`）⇒ 語意閘綠（兩邊一致），但卡面說了不會發生的事（第一·五守則）。w3a 的 `ggd.shipped.abilityId` 指的正是 `godie-o02l.w`。建議開票：把同一份 `AHbh` 翻譯給它（⭐ N 個同型 = 同一個模板）。

## ⑦ 需主 session 做的

1. `python3 tools/skill-remake/apply_tiers.py`（`tiers:apply` 鏡射）—— 預期 **0 份**英雄卡變動（兩份已相同）；跑完 `abilityMirror.test.ts` ＋ `abilityMirrorSemantic.test.ts` 都應綠 ⇒ ⭐ 這就是 AC#2「同步之後被動還在」的證據
2. genguard 提示：手改後跑一次 `pnpm castderive:build:raw`（級距／換算欄位重算；這一次不該改到任何位元組）
3. `pnpm content:build` → `bundle.json` / `manifest.json` / `_index.json` 一起 commit（AC#6）
4. commit（pathspec）：`content/abilities/godie-ofar.w.json` ＋ `packages/shared/src/content/abilityMirrorSemantic.test.ts` ＋ 本報告
5. 決定要不要為 `godie-o02l.w` 開票
