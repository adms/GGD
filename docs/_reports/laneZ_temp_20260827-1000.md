# lane Z — HUD / 輸入修復（2026-08-27）

commits `846695eb`（#509 #759 #800）· `046befa4`（#330）

| 票 | 狀態 |
|---|---|
| **#509** K6 裸 div onClick | ✅ **關** |
| **#800** 觸控 HUD 真的在疊 | 🔶 頭條 88×38 **歸零**；殘量另立帳本，**不關**（開關要三住處） |
| **#759** enemy-team 侵入 6px | 🔶 程式面完成（356 → **350**）；**不關**，只差一行 docs（柵欄外） |
| **#330** 沒加點沒回饋 | 🔶 「看得出來」那一半完成；自動加點開關要三住處，**不關** |
| **#502** 手把 epic | 🔁 重新評估（十個根因票**全關**，剩端到端走查） |

---

## #509 —— ⭐ 真的只差一組 JSX 屬性

兩個缺口今天只剩一個：**ErrorToast 那一半在 GH#513 就修掉了**
（`LobbyScreen.tsx` 的 `ErrorToast` 檔頭逐字記著，關閉是真的 `<button>` 帶 `data-pad-back`）。

`MerchantShop.CatalogueRow` 收起軌（原 `:1392` 裸 `<div onClick={onToggle}>`）：

```
data-pad-focusable=""  tabIndex={0}  role="button"
aria-expanded={expanded}  aria-label={`${displayName} ${price} g`}
onKeyDown={Enter/Space → onToggle()}
```

沿用 `StoreScreen` #516 同一份契約；⛔ 沒有放寬 `FOCUSABLE_SELECTOR`。
守衛 `panels/merchantRowPadReach.test.ts` —— 「可聚焦」的定義**從 `PadFocusNav.tsx` 的
真 `FOCUSABLE_SELECTOR` 正則抓出來比對**，⛔ 不重打一份。
突變：拿掉 `data-pad-focusable=""` → 紅。

---

## #800 —— 88×38 歸零，⛔ 但**沒有**全清

### 量到的（`touchControlsRect` × `hudSlotRect`）

| viewport | attack × equipment 之前 | 之後 |
|---|---|---|
| 844×390 | **88×38**（＝`028aa3bf`） | **0** |
| 852×393 | 88×35 | **0** |
| 780×360 | 88×48 | **0** |
| 375×667 | 0 | 0 |

### 根因（比票 body 更狠一階）

⛔ 不是「宣告錯角落」——**欄位預算**：觸控 top-right 那一疊 **300px**，
而叢集最上面的 `recall` 上緣在 **y≈94** ⇒ ⭐ **右欄真正空著只有 84px，放得下一個 slot。**

### 做了什麼（只改排序，⛔ 沒改高度、⛔ 沒改量尺）

- `equipment.touchOrder` **5 → 0** —— 它 150 寬，與攻擊鈕的水平交集是**滿的 88**
  （公式 `min(w−30, 88)`）⇒ 唯一絕對不能排下面的。
- 其餘照**窮舉 720 種排法**的結果重排（先最小化與攻擊鈕、再與整叢集的交集面積）：
  `equipment · scoreboard · audio-toggle · cheats · leave · settings`（最窄的 44px 排最後）。

| | 與攻擊鈕 | 與整叢集 |
|---|---:|---:|
| 之前 | **11,368 px²** | 18,822 px² |
| 之後 | **2,438 px²**（−79%） | 14,594 px²（−22%） |

### 誠實的那一半 —— `CLUSTER_RESIDUAL`（新增，15 列）

`KNOWN` 空了 ⛔ 不等於乾淨。新帳本量 **top-right 整疊 × 逐顆按鈕**
（⭐ 這一半從來沒被量過 —— 舊守衛只比對 `equipment`）：

```
844×390  settings×attack 14×38 · cheats×R 45×43 · audio-toggle×recall 44×20 · scoreboard×recall 44×16 · audio-toggle×R 55×7
852×393  settings×attack 14×35 · cheats×R 45×44 · audio-toggle×recall 44×23 · scoreboard×recall 44×13 · audio-toggle×R 55×4
780×360  settings×attack 14×44 · leave×attack 50×16 · audio-toggle×R 55×37 · scoreboard×recall 44×42 · cheats×R 45×13
375×667  （空）
```

⇒ 歸零＝手機上**少一顆控制項** ＝ owner 看得到的取捨 ⇒ 三住處開關 ⇒ ⛔ 不是本 lane 做得完的。

### 突變（本批唯一一條，最承重）

`equipment.touchOrder` 0 → 5 ⇒ **兩本帳同時紅**，逐字指名
`780x360/equipment×attack = 88×16` · `844x390/equipment×R = 58×13`。`cp` 還原即綠。

### ⚠️ AC2 未做

`TOUCH_PLATE_KNOWN`（375×667 直立 W/E 壓資源條）—— 票自陳「可能是直立就不該用這個排法」，
且 `touchControlsRect.ts` 唯讀 ⇒ 與上面同一個 owner 取捨。

---

## #759 —— 修的是**縮得下去**的那一個

⛔ **沒有**把 `enemy-team.touchHeight` 66 偷改成 60（票與守衛註解都逐字禁止）。
改 `minimap` 觸控 **116 → 110**（高寬一起 —— 正方形），而 `Minimap.tsx:462`
就是拿 `hudSlotWidth("minimap", touch)` 當畫布邊長 ⇒ 那 6px 是**真的變小的地圖**。
（`minimap` 也是這一疊裡唯一「內容等比縮」的 slot；其餘三個都是文字列。）

```
780×360 top-left: 10+44+8+44+8+110+8+44+8+66 = 350 = 360 − HUD_STAMP_BAND ⇒ 侵入 0
```

`GUTTER_INTRUDERS` 清空。⚠️ 帳本空了會讓原本兩段變**空跑** ⇒ 補一條**自證**：
`enemy-team` 下緣 `=== 360 − HUD_STAMP_BAND`（剛好貼齊，再多 1px 就紅）。

**⚠️ 唯一未做**：`docs/todo/latency-visibility.md:115` 的 `pc-09` 仍寫 `in-progress`
（本 lane 柵欄不含 `docs/`）。改完那一行就能關 #759。

---

## #330 —— 理由**用畫的**

判準⛔不是「有沒有文案」（`castFeedback.ts` 那行早就在），是「**沒讀到那行的人看不看得出來**」
—— owner 本人 2026-08-14 就沒讀到，他回報成「沒有任何效果、甚至沒有進入 CD 冷卻」
（`rank:0` ⇒ `castAbility` 回 `"not-learned"` ⇒ 不進冷卻/不播特效/不套 buff/不變身，四症一次全中）。

`AbilityBar` 新增 `UnlearnedMark`：

| 狀態 | 畫什麼 | 為什麼 |
|---|---|---|
| `unspentPoints > 0` | **「＋ 未學習」**（金框） | 他現在就能解決 |
| `unspentPoints === 0` | 「未學習」（灰框） | ⛔ 對沒有點數的人喊「去加點」是做不到的指示 |
| 被動 | ⛔ 不畫 | 它不用學，那三個字會變謊話 |

守衛 `components/abilityUnlearnedMark.test.ts` —— ⛔ **不掃原始碼字串**
（⚠️ 同目錄下已有兩條是掃字串的）：渲染真的 `AbilityBar` ＋ 真的 HUD store，
**看得見的字**與 `aria-label` 兩個方向一起讀。
突變：拿掉那一行 → 紅。

**⚠️ 未做**：AC3 自動加點開關（三住處）· AC2 toast 加響（⭐ 我認為 AC1 已覆蓋它，⛔ 但沒替 owner 決定）。

---

## #502 —— 重新評估

⭐ **十個根因子票全數關閉，#502 是唯一還開著的手把票。**
⇒ 剩下的**不是缺口，是驗收**：AC 第一句是「純手把走完一整圈」，
而每一段都有一條守衛、**沒有一條走完一整圈**（段與段之間的接縫是唯一沒有守衛的地方，
而 GH#634 的教訓正是「每一頁都活著」≠「走得過去」）。

建議：擴 `ui/padFocusNavReach.test.ts`（jsdom ＋ 出貨 `<PadFocusNav/>` ＋ 假手把 ＋ 實機 rect）
成「14 段的**接縫**」走查，每段只問「上一段的終點按下去之後，這一段接不接得住焦點」。
⚠️ 跨 `ui/` `ui/platform/` `input/` `net/` ⇒ 下一輪的專門 lane。

---

## 驗證（兩次 commit 各一次）

```
npx tsc  --noEmit -p apps/client        EXIT=0
npx vitest run --root apps/client       EXIT=0   →  640 檔 / 5,796 passed / 1 skipped / 0 failed
```

⚠️ 全程沒有跑任何被禁的指令（`content:build` / `skills:sync` / `pnpm test` …），
⛔ 沒有 push，⛔ 沒有碰柵欄外的檔（`GameApp.ts` 一個字都沒動 ⇒ 行數棘輪 4,000 不受影響）。

## 👁 用詞紀律

- **HUD 幾何**：有**算得出矩形**的終端證據（上表逐格）⇒ 那幾個數字是量到的。
- ⛔ 但 `pointerEvents` / z 序**不是幾何** ⇒ 「觸控真的收得到」那一半**未驗收**。
- **商店軌 / 未學習 chip**：只有 markup 證據 ⇒ **鏈路已接上，實機未驗收**。
