# GH#1074 —— 07-01 臨兵鬥零效果？07-02／07-03 的 1 秒窗口按不按得出來？07-03 的 EX 分岔

> lane 報告（第二波，2026-09-06 23:28）。⛔ 未 commit、⛔ 未跑任何 `*:build`／`skills:sync` —— 主 session 統一跑。

## 一句話
**兩條「1 秒內」窗口在出貨設定下都按不出來**（量到的，⛔ 不是推測）：`recentCast`／`comboBonus` 在**解算** tick 求值、紀錄在**提交** tick，而 W/E 的吟唱正好是 1.0 s。07-01 放得出來也記得進 castLedger（⛔ 不是零效果 —— 它被 `tpl-buff-self` 展開成一份卡面沒說的 6 秒 +移速）；07-03 的 EX 分岔已改讀 `learned EX`；07-01 的法術護盾缺一次性標籤 ⇒ 子票。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `packages/shared/src/sim/moonComboChain.test.ts`（新） | 出貨內容、真的 world／`castAbility`／`CastResolveSystem`。6 條：① Q 放得出來且 `lastCastTickInSlot(Q)` 當下有紀錄 ② Q→W 超過窗口 ⇒ 傷害 = 基準 ③ **`it.fails`** Q→W 吟唱一結束就按 W ⇒ 傷害該 > 基準（今天不含）④ W→E 超過窗口 ⇒ = 基準 ⑤ **`it.fails`** W 解算後立刻按 E ⇒ 該 > 基準（今天不含）⑥ 07-03 兩段 `when`：EX 未解鎖 `[true,false]`、`exSlot.rank=1` 後 `[false,true]` |
| `content/abilities/godie-hpb1.e.json` | comboBonus 兩條 ratio 的 `when`：`stat level < 30` → `{not:{learned EX}}`、`stat level >= 30` → `{learned EX}`（JASS `Trig_Jump_Start_Func017Func001C` 讀的就是 `udg_EX_Mode`；與 #1058 的 W 一致）。⭐ `conditionTreeTier()` 對 `stat`／`learned`／`not` 都回預設級 ⇒ AP 六維公式的條件維不變 ⇒ **係數逐位元不變**（runtime 探針：兩段仍是 0.3728）。genguard：castderive 正規化器就地改欄位（我沒動 castTimeSec）—— 主 session 跑 `pnpm castderive:build:raw` 即可 |
| `content/abilities/godie-hpb1.q.json` | ⛔ **沒動**（理由見下：翻不過去，⛔ 不湊） |

## 量到什麼（runtime 探針 `scratchpad/probe1074*.mts` ＋ 測試）

### ① 07-01 不是零效果技能
- 磁碟 `effects: []`，註冊時 `template: tpl-buff-self{duration 6, ms pctAdd 中}` 展開成 `applyBuff{ms +0.5, 6 s}`；施放後 hero sources 多一份 `buff:ability:godie-hpb1.q#2`。
- 來源：`tools/w3x-import/w3xlib/drafts.py:366` `"ANss": ("spirit walk?", _t_ms_buff(2))` —— **帶問號的猜測**。w3a base `ANss` = stock **Spell Shield**（擋一次負面法術，`Cool1 40`；本圖 3 級 65/55/45、`data:{}`、`duration:{}`）。
- `isPassiveOnly(def)` = `passive!==undefined && effects.length===0` ⇒ false ⇒ 通過每一道閘；`noteAbilityCast`（`abilitySystem.ts:812`）在提交點記到 ⇒ ⭐ 07-02 的 `recentCast Q` **讀得到**（測試①綠）。

### ② ⛔⛔ Q→W 的 1 秒窗口按不出來
| | tick |
|---|---:|
| Q 按下（提交、進 ledger） | 1 |
| Q 吟唱 0.067 s = 2 tick（`if (ab.cast) return "cooldown"` 擋住 W） | |
| W 按下（**press 差 2 tick**，遠在 30 tick 窗口內） | 3 |
| W 傷害落地（吟唱 1.0 s ＋ 傷害管線）；`recentCast` **在這一 tick 求值** | 39 |
| 39 − 1 = **38 > 30** ⇒ 條件不成立 | |

傷害：只放 W 1761.89 · Q 之後立刻 W **1761.89** · Q 之後 3 秒 W 1761.89 ⇒ 零加成。⭐ 而且是**結構性**的：窗口 1.0 s、W 吟唱 1.0 s（`castTimeSec 1`，`castTimeMaxSec` 夾也是 1）⇒ 任何時序都不成立。

### ③ ⛔ W→E 同型
W 解算 tick 37 掛 `moon-combo`（expiresAt **66**）→ E 同一 tick 按（press 差 0）→ E 的 `comboBonus` 在 E **解算**（≈ tick 67）烘焙（`effectRunner.ts::bakeCastTimeConditionals`，它修過 leap 飛行那一半，⛔ 吟唱不在它眼裡）→ `hasStatus` 要 `expiresAtTick > tick` ⇒ 不成立。傷害：只放 E 1211.22 · W 之後立刻 E **1211.22**。

⚠️ 這不是 #1058／#937 翻錯：`recentCast slot Q withinSec 1` 與 `applyStatus moon-combo 1.0 s` 都逐字對 JASS；錯的是**紀錄點 ↔ 求值點**兩個時間點的組合。原作 `udg_MoonCombo` 在 Q/W/E 的 SPELL_EFFECT（同一種時間點）各讀各寫。

### ④ 07-01「可抵擋對方負性魔法」—— 翻不過去
| 候選 | 為什麼不是 |
|---|---|
| `invulnerable{blocksDamage:"none", blocksControl:true}`（`sim/effects/invulnerable.ts` 檔頭把 07-01 當成這一格的存在證明） | `durationSec` 必填；w3a `duration:{}`、stock `Dur1 0` ⇒ 任何秒數都是**近似**；而且只擋 stun/root/slow/missChance，⛔ 不是「負性魔法」 |
| `applyBuff{statusImmunity:{tags:["debuff"]}}`（`godie-e00r.ex` 前例；30 份狀態帶 `debuff` tag、polarity=debuff 的 31 份只有 `devour-cooldown` 沒帶） | 同樣要 `duration`／`permanent`；沒有「擋**一次**就消耗」的語意 |
⇒ 缺的標籤：**`statusImmunity.charges`（一次性法術護盾）**。盤點 w3a base=`ANss`：07-01 · 45-00 寫輪眼（卡面沒提）· 04-00 翔封界（owner 卡面逐字「翔封界不再是『擋一次負面魔法』了」⇒ 刻意退掉）· 37-04 · A0H5 19-02 · A0TJ 32-002 ⇒ **擋住 1 支**。子票 ↓。

### ⑤ 順帶量到（⛔ 沒動）
- AP 六維公式（GH#942）把 W 的 0.8／1.5 與 E 的 1.3／2.5 都解析成**同一個**係數（1.9856／0.3728）—— 條件維只看「有沒有條件」，`learned EX` 與 `not learned EX` 同級 ⇒ 卡面「30 級之後可增加 (X)」與「(Y)」印出來會是同一個數。公式是 owner 的（⛔ 不提案改），只記在這裡。
- `content/champions/godie-hpb1.json` 的內嵌 E 仍是 `level` 版 ⇒ `content/abilityMirror.test.ts` 紅到 `skills:sync`（#1058 的 W 也是這樣被同步的：內嵌 W 今天已含 `recentCast`）。

## 突變紀錄
`content/abilities/godie-hpb1.e.json` 第二條 ratio 的 `when` 改回 `{kind:"stat",…level >= 30}` ⇒ 6 條裡 **1 紅**：「07-03 的 EX 分岔讀 learned EX」`expected [false,false] to equal [false,true]`（scratchpad `run2-mutation.log`）。用 Edit 改回來（⛔ 沒有 checkout）。
⛔ 引擎側的突變（拿掉 `noteAbilityCast`）**沒跑**：既有 `conditionRecentCast.test.ts` 已釘那一行，一批一條。

## 跑了什麼
- `npx vitest run src/sim/moonComboChain.test.ts` ×2（初跑 6/6 綠、突變 1 紅）；tsx 探針 ×2（不計 vitest）
- `pnpm typecheck` ×1 ⇒ **EXIT=1**，2 個錯全在 `apps/editor/src/forge/ConditionEditor.tsx:792/867`（`ClauseKind` 少一個 kind）—— ⚠️ **不是我的**：`packages/shared/src/content/schema/condition.ts`（+22 行）與 `sim/content/condition.ts` 在工作樹裡被**另一條 lane** 改著（未 commit），`ConditionEditor.tsx` 本身沒改 ⇒ 那條 lane 加了新葉子沒接到編輯器。`packages/shared typecheck: Done`。

## ⛔ 沒做到的
- 07-01 的法術護盾（缺標籤，柵欄禁 schema）⇒ 子票 A
- 兩條窗口的引擎修法（`condition.ts`／`effectCommon.ts`／`CastResolveSystem.ts` 都在柵欄外，而且是決策點要留開關）⇒ 子票 B；修好後把 `moonComboChain.test.ts` 的兩條 `it.fails` 改回 `it`
- **「玩家看得到」**：07-02／07-03 的連段加成今天按不出來，這條 lane 之後玩家看到的也一樣 ⇒ 狀態 **鏈路已接上，⛔ 未驗收**

## 需要主 session 動的（柵欄外）
1. `pnpm skills:sync`：同步 `content/champions/godie-hpb1.json` 內嵌 E（`abilityMirror.test.ts` 才綠）＋ castderive 正規化。
2. **`packages/shared/src/content/comboWindowClaims.test.ts`** 會紅：第二條 `it` 斷言 `when.stat === "level"`、第三條斷言 ops `["<30", ">=30"]`。改法：第二條改成「一段是 `{not:{kind:"learned",slot:"EX"}}`、另一段是 `{kind:"learned",slot:"EX"}`」；第三條（門檻接得起來）改成「兩段互為否定」或刪掉（`moonComboChain.test.ts` ⑥ 已用真的 world 驗兩段對調）。⚠️ 檔頭「兩段都帶著等級條件」那句也過期了。
3. `packages/shared/src/sim/leapJassFidelity.test.ts:662/673` 的註解與 `w["stat"] === "level"` 過濾：**不會紅**（夾具不餵 AP ⇒ 那一項本來就是 0），但註解「夾具是 1 級 ⇒ 成立的是 level<30 那一段」過期 —— 順手改成 `learned`。
4. 另一條 lane 的 `condition.ts` 改動要接到 `apps/editor/src/forge/ConditionEditor.tsx`（typecheck 紅），⛔ 不是我這條的。

## 順手發現（開票）
- 子票 A：**GH#1085** —— `statusImmunity.charges`（一次性法術護盾）＋ 07-01 接上、拆掉 `drafts.py:366` 的 spirit-walk 猜測
- 子票 B：**GH#1086** —— 兩條窗口的紀錄點↔求值點不對稱（決策點：相對提交 tick 量；留開關）；修好後把 `moonComboChain.test.ts` 的兩條 `it.fails` 改回 `it`
