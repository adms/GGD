# 深度分析證據收集 —— GH#629（1000 隻殭屍 perf）· GH#650（BlockGrant 特效軸）

> 唯讀查證，2026-08-28 00:38。所有行號以當下工作樹為準（HEAD = d5de6e6a 之後、含未 commit 的 vfx audition 改動，但本報告引用的檔案均無未 commit 改動）。

---

## A. GH#629 —— 1000 隻殭屍 perf

### A1. MobSystem.ts 的 world.team 全掃 —— ⭐ 票文的 :209 **零漂移，仍然精確**

`packages/shared/src/sim/systems/MobSystem.ts` —— `mobSystem(world)` 每 tick 跑（:92），
第 3 段 AI TARGET PICK（:149 起）對**每一隻活著的小怪**、**每一個 tick** 做一次全掃：

```ts
// :152
for (const [mobId, mob] of world.mob) {
  …
  // :197
  const myTeam = world.team.get(mobId)?.teamId ?? MONSTER_TEAM;
  // :205-206（GH#577 王優先打真人 ⇒ 兩趟）
  const preferHumans = kingPrefersHumans(rules, mob.kind);
  for (const pass of preferHumans ? [true, false] : [false]) {
  if (pass === false && target !== -1) break; // 第一趟已經找到真人了
  // :209  ← 票文指的那一行，仍在原位
  for (const [cid, cteam] of world.team) {
    if (cteam.teamId === myTeam) continue; // never target its own side
    if (pass && !isHumanSeatId(rules, cteam.seatId)) continue;
    if (!isMobTargetable(world, cid, mobId)) continue;
    const chp = world.health.get(cid);
    const ct = world.transform.get(cid);
    if (!chp?.alive || !ct || ct.zone !== mob.zone) continue;
    const d2 = distSq(mt2.pos, ct.pos);
    if (d2 < bestD2) { bestD2 = d2; target = cid; }
  }
  }
```

- **現況＝全掃，不走索引。** 沒有 target 快取、沒有 repick 間隔閘 —— 每 tick 每隻小怪
  從 `target = -1; bestD2 = Infinity` 重掃一次（settled zone 例外，:160 直接 continue）。
- 王的 `preferHumans` 兩趟設計在最壞情況（無真人）是 **2× 全掃**。
- ⭐ **整支 MobSystem.ts 零次使用 spatial grid**（`grep -n "grid"` 無 match）。
- 同檔還有兩處 `world.team` 全掃（都是**每次施法**跑，不是每 tick）：
  - :724（`lowestHealthEnemyChampion` :715 —— 王 [leap吸血] 目標，owner 規格「全場血量最少」本來就不看距離）
  - :889（`kingCastTarget` :825 的 situational aiming —— ⚠️ 迴圈體對每個候選再叫
    `groundAoeTargets(...)`（:896），那裡面又是一次 overlap 查詢 ⇒ O(team × overlap)）

### A2. SpatialHash —— ⭐ 仍是 **Map 版**，⛔ 不是 flat-array+stamp

`packages/shared/src/sim/SimWorld.ts:580`：`readonly grid = new SpatialHash(4);`

`packages/shared/src/sim/collision/spatialHash.ts`（全檔 90 行）：

```ts
// :11-13
export class SpatialHash {
  private cells = new Map<number, EntityId[]>();
  private entityBounds = new Map<EntityId, AABB>();
// :62-84 queryAABB —— 每次查詢配置一個新 Set，回傳新排序陣列
  queryAABB(min: Vec2, max: Vec2): EntityId[] {
    …
    const seen = new Set<EntityId>();
    …
    return [...seen].sort((a, b) => a - b);
  }
```

- 每 tick 重建：`SimWorld.rebuildGrid()`（:1559-1584）`grid.clear()` 後逐實體
  `insertCircle`（排除 reviveCircle / coin / auraCarrier / deathWard —— 結構性不可選目標）。
- **每次查詢都是新配置**（Set + spread + sort）⇒ E4 報告說的 flat-array + generation
  stamp 佈局**尚未發生**。

### A3. git 歷史 —— ⭐ #629 **零實作 commit**

- `git log --all --grep="629"`：只有兩筆**開票規格**commit（c87cbafe V3_c2 六節回補、
  b03240bc 五件回補），其餘（f98fc92b/b8ad66c1/…）是 grep 誤中（commit 內文含 629 字樣）。
- MobSystem.ts 最近 3 筆：b9cc7fee（#577/#602 王技能）、a1bf7d0e（#602/#577 leap吸血）、
  25853f24（#358/#355 EX∅）—— 全是功能，無 perf 改動。
- ⭐ **spatialHash.ts 自 baseline（2ead09ab initial commit）之後一個 commit 都沒有。**

### A4. spatialHash 查詢呼叫端 × 持有模式（flat-array 共用緩衝遷移的踩雷盤點）

直接呼叫 `grid.queryAABB / queryCircle` 的非測試呼叫端**只有 4 個**：

| 呼叫端 | 行 | 持有模式 | 依據 |
|---|---|---|---|
| `sim/collision/queries.ts`（`queryOverlap`） | :24 `queryAABB` | ⭐ 當場用完 | candidates 在同函式迴圈耗盡；迴圈體是純幾何（circleVsCircle 等）；回傳的是**自配的 `out` 陣列**，與 grid 緩衝無關 |
| `sim/stuckEscape.ts`（`overlappingLiveUnit`） | :95 `queryCircle` | ⭐ 當場用完 | 全檔唯一一次 grid 呼叫；迴圈體只讀 world stores |
| `sim/systems/MovementSystem.ts`（分離 pass） | :397 `queryCircle` | ⭐ 當場用完 | 全檔唯一一次 grid 呼叫；內圈 `separatePair` 不碰 grid |
| `sim/systems/ProjectileSystem.ts`（掃掠命中） | :45 `queryCircle` | ⭐ 當場用完 | `candidates` 最後一次引用在 :53 的迴圈；迴圈體只有 `sweptCircleVsCircle`（純幾何）；命中後的效果處理在迴圈**之後**且不再讀 candidates |

⇒ **今天零個「存起來跨查詢」的呼叫端** —— 4 個都在下一次 grid 查詢發生前耗盡結果。
flat-array 共用緩衝遷移在**現有**呼叫端下沒有踩雷點；風險在**未來**呼叫端（契約要寫下來）。
`queryOverlap` 的 9 個上游呼叫端（targeting.ts:571 · fear.ts:147 · abilitySystem.ts:73,130 ·
GuardianSystem.ts:476,556 · aura/aura.ts:305,386 · effects/damageLine.ts:122）持有的是
queryOverlap 自配的 `out` 陣列 ⇒ 即使巢狀（aura.ts 305/386）也不受 grid 緩衝遷移影響。
⚠️ E4 報告 :232 自己也把「回傳值是共用緩衝」列為選項 1 的風險欄。

### A5. 產物存在性

- `tools/go/spatialhash/` **在，且 git 已追蹤**：spatialhash.go(8.3K) · spatialhash_test.go ·
  teamscan.go · teamscan_test.go · trace.go · tsref/ · testdata/（2026-08-23 19:30）。
- `docs/_reports/E4_temp_20260823d.md` **在，git 已追蹤**（11,722 bytes）。結論摘錄：
  - ① 1000 隻：⛔ 雲端撐不住（42–56 ms/tick vs 33.3 ms 預算；4 vCPU、單場單執行緒）。
  - ② Go 重寫：⛔ 不值得（同演算法只快 **1.05×**）。
  - ⭐ 真槓桿在 TS 換資料結構（:106-153）：flat-array+stamp **0.88-0.93 → 0.49 ms（1.88×）**；
    「敵對可選目標」索引 **→ 0.038 ms（80×）**，且該索引 O(1) 維護（spawn/death/陣營轉換各動一次）。
  - 兩個選項（:232-233）：①SpatialHash 換 flat-array+stamp（風險＝共用緩衝對呼叫端，
    本報告 A4 已逐端盤點）②world 加「非 MONSTER_TEAM 可選目標」索引、MobSystem 改走它（風險小，80× 的浪費就在 A1 那一行）。

---

## B. GH#650 —— BlockGrant 擋下特效軸

### B1. BlockGrant 定義 —— ⭐ **沒有任何 vfxId / vfxScale / tint 欄位**

Zod：`packages/shared/src/content/schema/effects/_shared.ts:380-427`（`.strict()`）：

```ts
export const zBlockGrant = z.object({
  damageTypes: z.array(zDamageType).min(1)…,   // 擋哪些傷害型別
  chance: z.number().positive().max(1)…,        // 觸發機率
  fraction: z.number().positive().max(1)…,      // 擋掉幾成
  lethalOnly: z.boolean().optional()…,          // 只擋致命一擊
  lethalBasis: z.enum(["hp","hpAndShields"]).optional()…,
  internalCooldown: z.number().min(0).max(300).optional()…,
}).strict();
```

- `zItemBlockGrant` 是它的**別名**（schema/item.ts:355），道具 `passive.block` 掛在 item.ts:543。
- sim 側鏡像 `interface BlockGrant`（`sim/combat/block.ts:219-254`）：同六軸，同樣零特效欄。
- `.strict()` ⇒ 內容側今天**寫不進**任何特效欄位（多一個 key 就載入紅）。

### B2. 格擋分支（damage.ts）—— 刻意**不發新事件**，騎既有 hitImpact

`packages/shared/src/sim/combat/damage.ts:1043-1070`（護盾之前、mitigate 之後）：

```ts
// :1053-1058（逐字）
// 格擋 STOPS one that arrived — which is byte-for-byte the shield-ate-it-all
// case the client already draws: `blocked: true` → `combatText` 「guard」 +
// `hitFeel`'s `sparkKind: "block"` + GameApp's `playContextualVoice(blocker,"block")`.
// So this needs no new event and no `net/eventFanout.ts` entry.
const blockCut = blockCutFor(world, pkt.target, pkt.type, impact, hp.hp, shieldBefore); // :1062
let dmg = impact - blockCut;                                                            // :1070
```

事件出口只有一個：hitFeel 的 `hitImpact`（damage.ts:560-562）——
`{ x, z, source, target, dmgType, amount, blocked, crit, killingBlow, profile }`，
其中 `profile.isBlock = blocked`（:553）。⭐ **鏈路上沒有任何一格能載「這一次格擋來自哪個來源
/ 該放哪顆特效」** —— blockCutFor 選中的那份 BlockGrant 來源 id 在 :1070 之後就丟了。

### B3. 客戶端泛用格擋演出（今天有兩個通道，都不是內容驅動）

1. **接觸點火花**：`apps/client/src/vfx/VfxSystem.ts:449-457` `sparkStyleFor` ——
   `case "block": return { tint: IMPACT_TINTS.guardBreak, intensity: "light" };`（冷白鋼色，泛用）。
2. **身體閃光**（GH#741，commit 28194ed5 **已落地**）：`apps/client/src/render/combatFeedback.ts:509-515`
   —— `if (profile.isBlock && blockFlash !== "damage")` 改閃深鋼藍（`BLOCK_FLASH_RGB`），
   `blockFlash` 開關 seam 在 :200-213（⚠️ laneFEEL 報告自承只落了 client 這一個住處）。
3. 另有 combatText「guard」與 `playContextualVoice(blocker,"block")`（B2 引文點名）。

### B4. laneFEEL 報告（20260827-1600，commit d5de6e6a）—— ⭐ #650 **明確未做**

`docs/_reports/laneFEEL_temp_20260827-1600.md`：
- :22 `| **#650** BlockGrant 特效軸 | ⛔ **柵欄外，零改動** | ①schema ②sim 都在 packages/shared；③client 沒有①②可消費 |`
- :205 複驗（與本報告 B1-B3 一致）：zBlockGrant 沒有 vfxId；damage.ts:553 只塞 isBlock；client 泛用冷白火花。
- :207-208：#741 的鋼藍閃是「擋下的那一下」的**第二個可辨識通道**，⛔ **不是 #650 要的**
  （#650 要「每一個格擋來源可以指定自己的特效」）。⇒ **#650 沒有被部分做掉**（相鄰的 #741 做完了）。
- ⚠️ :241 列 #650 的預計檔案清單時**沿用了票文的 `godie-e002.e.json`** —— 見 B5，那個路徑是錯的。

### B5. ⭐⭐ 「初號機 AT 力場」的 JSON —— 票文那一行**寫錯檔**

| 檔 | name 欄（逐字） | 是誰 |
|---|---|---|
| `content/abilities/godie-e00r.e.json` | **「59-03 AT力場」** | ⭐ **初號機（champion 59，godie-e00r）—— 這才是正解** |
| `content/abilities/godie-e002.e.json` | 「20-03 約束與勝利之劍」 | Saber 的 E（光束砲） |
| `content/abilities/godie-e002.passive.json` | 「20-00 銀色甲胄」（30% 格擋 100% 魔法） | Saber 的天生技 |

- `grep -rln "AT力場|初號機" content/abilities/ content/champions/` 只中三個檔：
  godie-e00r.e.json · godie-e00r.q.json · champions/godie-e00r.json。
- 誤植成因推測：**兩位英雄都有格擋被動**（e002.passive 是 schema 註解裡點名的例子），
  票文把「有格擋的 e00x」記串了。laneFEEL :241 又複述了一次 ⇒ 修票時兩處都要更正。
- AT力場的 block 數值（e00r.e `passive.ranks[0..3].block`）：
  `{damageTypes:["physical"], chance:0.10/0.15/0.20/0.25, fraction:0.5}` —— 逐 rank 四份。

### B6. 出貨內容裡的 BlockGrant 引用 —— **10 檔 / 15 個 block 物件**

| 檔 | 路徑 | 參數（觸發頻率風險） |
|---|---|---|
| items/godie-i00s（黃金聖鬥衣） | `passive.block` | magic 50%、無 ICD ⇒ **每二發魔傷一次**（高頻） |
| items/godie-i00j（奇門盾甲） | `passive.block` | phys+magic 50%、無 ICD ⇒ **最高頻**來源 |
| items/godie-i016（晨曦之光） | `passive.block` | 30% lethalOnly + ICD ⇒ 低頻 |
| items/godie-i06g（殺豬刀） | `passive.block` | 30% lethalOnly + ICD ⇒ 低頻 |
| abilities/godie-e002.passive（20-00 銀色甲胄） | `passive.ranks[0].block` | magic 30% ⇒ 中頻 |
| abilities/godie-e00r.e（59-03 AT力場） | `passive.ranks[0..3].block` ×4 | phys 10–25%、fraction 0.5 ⇒ 中頻 |
| abilities/godie-h01n.ex（79-002 虛化） | `passive.ranks[0].block` | phys 30% ⇒ 中頻 |
| champions/godie-e00r（鏡射） | `abilities.E.passive.ranks[0..3].block` ×4 | e00r.e 的內嵌鏡像（⛔ 不另計來源） |
| augments/grail-a-03 | `block` | chance 1.0 lethalOnly ⇒ 低頻 |
| augments/grail-ex-07 | `block` | chance 1.0 lethalOnly ⇒ 低頻 |

⇒ 有效**來源** 9 個（champion 檔是鏡射）。若加 per-source VFX，頻率上限由兩件 50% 無 ICD
平擋道具決定 —— 特效預算要按「每二發命中一次」設計，⛔ 不是按保命型的每場幾次。

---

*查證方法：grep/sed 唯讀；git log/ls-files 唯讀；零檔案改動（本報告除外）。*
