# GH#1087 — castabilityVerdict 補 `summons` 頻道（lane 報告，2026-09-06 23:54）

> 📅 `HEAD=1de2bd31f` 工作樹（⛔ 未 commit，主 session 統一 commit）。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `packages/shared/src/sim/castabilityVerdict.ts` | `ChannelSnapshot.summons: number`；`snapshotChannels()` 填 `world.summon.size`（唯一寫入者 `sim/summons.ts::spawnSummon`，⛔ 不掃字串、⛔ 不讀 `summonFailed` 事件）；`classifyCastOutcome()` 多一格 `channel = "summon"`（排在 `gold` 之後、`dash` 之前，`vfx` 之上）；`anyState` 也看它 |
| `packages/shared/src/sim/castabilitySummonChannel.test.ts` ⭐ 新增 | 兩個方向的校準（見下）。⚠️ 票文要的夾具住處 `castabilityVfxOnly.test.ts` **在這條 lane 的柵欄外**，所以放新檔（同一個目錄） |
| `packages/shared/src/content/templates/templateDefaultsCast.test.ts` | 刪 `EXTRA_CHANNELS` 補丁（常數、`extraBefore`、迴圈裡的第二判定）；檔頭那段改成「已刪」的紀錄；`rows.some(channel === "summon")` 那條斷言**留著**（現在由出貨判定餵） |
| `docs/_castability-128.md` | 跑了一次 sweep ⇒ **逐位元組不變**（`git diff` 空） |
| `castabilitySweep.test.ts` 的 `KNOWN_GAPS` | **一列都沒動**（見「量到什麼」） |

## 量到什麼

- **首發名單上沒有純召喚格**：`content/abilities/` 裡帶 `summon` 的只有 `godie-e00s.r` 與 `godie-e010.r`，而且都**巢在 `randomArea` 底下**（同一棵樹還有 `damageArea`）。e00s 在名單上、R 今天 PASS 24/24 走 `damage`；e010 不在名單上。⇒ sweep 的唯一 ❌（`godie-ogld|EX`）是 randomArea 擲骰，⛔ 與召喚無關 ⇒ `KNOWN_GAPS` 多一格少一格都不必，docs 產物不變，頻道分佈表今天沒有 `summon` 列。
- `summon` 頻道今天**唯一**的讀者是 `templateDefaultsCast.test.ts`（`tpl-summon-agent` 的預設展開），它的「召喚指針一次都沒量到」斷言現在由出貨判定餵，仍然綠。
- 校準（`castabilitySummonChannel.test.ts`，真的註冊、真的施放、出貨判定）：
  - ① 只有 `summon`（身體＝已註冊的 SELA、count 1）⇒ **PASS / channel `summon`** ✅
  - ② 同一個 kind、`championId` 指向未註冊的身體（handler 走 `summonFailed`，零具）⇒ **不是 PASS** ✅ —— 證明指針讀的是**具數**，⛔ 不是效果樹裡的 kind、也不是事件。

## 突變紀錄（一條，柵欄內）

`castabilityVerdict.ts` 的 `anyState` 拿掉 `after.summons > before.summons` ⇒ 🔴 `castabilitySummonChannel` ①：
`expected 'damage' to be 'summon'` —— 沒有那根指針時，這一格要等召喚出來的身體**下一 tick 揮拳**才被記到 `damage` 上，
召喚本身在儀器上是隱形的。用 Edit 改回來。（票文說量測儀器不必突變；這一條在柵欄內、一次 vitest，就做了。）

## 測試

- run 1：`castabilitySummonChannel` · `castabilityVfxOnly` · `templateDefaultsCast` · `visualProofScript` · `castabilitySweep` ⇒ 5 檔 13 條全綠（sweep 2.5 s）。
- run 2（突變）：兩張票的承重線同時改壞 ⇒ 兩條各紅一次。
- `pnpm typecheck` ⇒ EXIT=0（一次）。
- 測試／實作行數：實作 +21（大半是檔內註解）／測試 +87 新檔 −6 補丁 —— 超過 1×，理由是判定本身只有三行，而校準必須兩個方向各一條。

## ⛔ 沒做到／需要主 session 動的柵欄外檔案

1. `castabilitySweep.test.ts` 第 4 行與「PASS 觸發頻道分佈」那段的**散文優先序**（`傷害＞投射物＞補血＞補魔＞護盾＞狀態＞buff＞位移＞特效`）是頻道順序的**第二個住處**，早在 taunt／gold／resourceSwap／championForm 時就過期，今天再少 `summon` 一格。柵欄只准動 `KNOWN_GAPS` ⇒ 開票 **GH#1088**，⛔ 沒動。
2. 票文 Scope 要的 `castabilityVfxOnly.test.ts` 夾具 —— 在柵欄外，改放新檔 `castabilitySummonChannel.test.ts`；主 session 若要併回去，把那兩條 `it` 搬過去即可（同一套 helper）。
3. `templateDefaultsCast.test.ts` 在工作樹裡是 **untracked**（GH#1078 lane 的新檔）—— 我的刪補丁改動在同一份檔上，commit 時一起進。

## 順手發現但沒動的

- 上面第 1 點（散文優先序過期）。
- 沒有 rule-slip。
