# GH#1030 —— 四個假前提 ⇒ 三支會紅的閘（lane 報告，2026-09-06 15:46）

> owner 2026-09-06 逐字：「我也把之前查錯的都更正進簡報了 **請開票**」

⛔ 本 lane 不 commit、不 sync、不 build；⛔ 不動 `content/config/arena-rules.json`（只讀）、⛔ 不動 CLAUDE.md（段落在 §7）。

## 0. 基線（動手之前量到的，⛔ 不是票文）

| 假前提 | 今天的出貨現況（量到的） |
|---|---|
| #1「ex-release / ex-origin 拿不到、寶具 30 件」 | `content/loot-tables/` 三張表 30／39／15；`weaponTiers`：ex-release `minRound 1 · basePct 15`、ex-origin `10..10 · 8% · guaranteeAtD 0.6`；`MatchController` 2211 行走 `pickWeaponTable → offerItems`。⭐ 沒有任何一條既有測試把「取得管道」聯集起來 |
| #2「技能是 1 級」 | 共用入口 `sim/effects/effect.ts::resolveScaling(finalStats, sc, rank, attrs)`：`rank` **必填、無預設**（✅ 今天已是對的形狀），`perRank[rank-1]` 在它裡面；出貨呼叫點 14 站（sim 9 ＋ editor 5）全部傳變數。⭐ 但沒有閘守著這個形狀 —— 加一個 `= 1` 不會有東西紅 |
| #3「致盲沒有實作」 | `sim/combat/evasion.ts:431 missChanceOf()` 活的；**#1041** 已開（免疫控場擋不住致盲／詛咒，`statusTagImmunity.ts:15`）⇒ 驗收 #6 成立 |
| #4「只有技能／普攻兩桶」 | sim 非測試原始碼的 `origin:` 產生端 **18 處、10 個家族**：`ability:` `basic` `hook:` `mark:` `lifesteal` `fireRing` `flower` `guardian` `guardian-heir` `mob`。⛔ 沒有任何住處宣告「桶」；#1015 的排行榜自己另有一套口徑 |
| 污染紀錄 | `docs/_reports/` 底下**沒有**一份報告寫著 30 件／1 級／48.5%（grep 全掃）—— 污染住在 **#1017 body Non-goals**（48.5%）、#928 討論串、簡報（owner 已更正）、其他 session 的 scratchpad |

## 1. 做了什麼（四個各一行）

1. **#1 拿得到 ≠ 貨架** ⇒ 閘① `packages/shared/src/sim/economy/obtainableItems.test.ts`：跑出貨函式（`buyItem` 逐件 · `legendaryPool` · `eligibleItemPool` × `rounds[*].weaponLootTable` × `weaponTiers[*].table` · legacy gacha · quest-rewards）取聯集；兩個方向（宣告的表要存在或已退場 ⇄ 登錄表每一張表都要有管道引用）；斷言聯集 ≠ 任何單一管道、兩階獎池每一件都在聯集裡。⭐ **印出 84 件是斷言的產物**（`探針=godie-e001/godie-e007；shop-shelf=30 · orb=30 · round-2/5=30 · round-10=39 · tier:ex-origin=15 · tier:ex-release=39 · gacha=0 · quest=0`）。
2. **#2 rank 不可假設 1** ⇒ 閘② `packages/shared/src/ops/skillDamageRankExplicit.test.ts`：①簽章 `rank: number` 無預設無 `?`；②掃 apps/packages/tools 非測試原始碼每一個 `resolveScaling(` 呼叫的第 3 個引數不是字面值（跨行也切得對，註解剝掉）；③沒有 `perRank[0]` 手算；量尺自證（合成字面值抓得到、`ctx.rank` 放行、真掃描 ≥ 5 站）。
3. **#3 致盲** ⇒ ⛔ 不開發；只更正紀錄；確認 **#1041** 已單獨記下相鄰真缺口（驗收 #6 ✅）。
4. **#4 三個桶** ⇒ 閘③ `packages/shared/src/ops/damageOriginBuckets.test.ts`：桶宣告 `ability / basic / passive(hook:) / mark / world / restore(lifesteal，非傷害)`；靜態掃 sim 每一個 `origin:` 產生端都落桶 ⇄ 每桶都有產生端；動態跑一場真對局（`godie-h00l` LV30 Q–R maxRank ＋ `cleaver-of-the-warden` vs `godie-hart`）：每一發 damage 落桶、Σ桶 = Σ全部、⭐ **技能／普攻／被動三桶各自非空**（量到 `basic 2,472 · ability 748 · passive 48,104` —— 被動桶存在的證據，⛔ 不是平衡結論）。

另外三個「假前提的住處」在柵欄內的註解接縫改掉（只改註解，⛔ 零玩法數值）：
- `shopShelf.ts` 檔頭「exactly two doors」→ 標 CORRECTED，列今天的五扇門並指向閘①；`legendaryShelfIds()` 加「回答的是貨架，⛔ 不是拿得到」。
- `itemTiers.ts`「the only ways to it are the round-5 card and the orb」→ 標 CORRECTED（過期兩次：貨架開了、weaponTiers 抽得到）。
- `legendaryOrb.ts`「You never buy a legendary」→ 標 CORRECTED（2026-08-17 起直購）。

## 2. 最重要的發現

⭐ **「拿得到」的母體是看英雄的。** 第一版閘①用 `godie-h020`（遠程）當探針就紅了：`endless-edge`（ex-release）拿不到 —— 因為 6 件寶具帶 `requiresAttackType`（近戰 5：`endless-edge` `godie-i00f` `gravity-sword-black-rod` `cleaver-of-the-warden` `godie-i000` `godie-i04d`；遠程 1：`godie-i012`），`itemOfferableTo()` 會照英雄攻擊型別過濾。⇒ 閘①改成**一近一遠兩個探針取聯集**並把探針印進 manifest。⚠️ 這正是票裡 Known risks 的形狀：**一個看起來很精確的數字，分母沒說清楚就是謊話** —— 84 對「某一位英雄」不成立，只對「全體英雄的聯集」成立。

次要：閘②量到今天出貨形狀已經是對的（`rank` 必填），假前提 #2 住在**算式版分析**（scratchpad `full.ts` 直接讀 `flat`）而不在出貨程式 —— 所以閘的形狀是「守住入口不退化 ＋ 擋住 `perRank[0]` 手算」，⛔ 不是 runtime 斷言（票文的 B 方案只在沒有共用入口時才用）。

## 3. 改了哪些檔（逐檔）

| 檔 | 動作 |
|---|---|
| `packages/shared/src/sim/economy/obtainableItems.test.ts` | 新增（閘①，113 行） |
| `packages/shared/src/ops/skillDamageRankExplicit.test.ts` | 新增（閘②，89 行） |
| `packages/shared/src/ops/damageOriginBuckets.test.ts` | 新增（閘③，125 行） |
| `packages/shared/src/sim/economy/shopShelf.ts` | 註解接縫 ×2（檔頭 · `legendaryShelfIds` docstring） |
| `packages/shared/src/sim/economy/itemTiers.ts` | 註解接縫 ×1 |
| `packages/shared/src/sim/economy/legendaryOrb.ts` | 註解接縫 ×1 |
| `docs/_reports/1030_temp_20260906-1546.md` | 本報告 |
| GH#928 · GH#1017 | 更正留言（§5） |

⚠️ `packages/shared/src/sim/effects/damageArea.ts` 只在突變期間被改過，已用 `edit-or-die` 還原，`git diff` 為 0 行。

## 4. 測試結果（含突變）

`npx vitest run`（packages/shared）三次：① 首跑 → 閘①紅（探針問題，見 §2）② 修探針後 **7/7 綠** ③ 三個突變同時放進去 → **4 紅 / 3 綠**，各自指名：

| 閘 | 突變（放回假前提） | 紅的訊息指名 |
|---|---|---|
| ① | 聯集改成只讀 `legendary-orb` 那一張表（`channels[1]`） | 「只讀「shop-shelf（buyItem）」會漏掉 0 件 —— 這不是母體」＋ manifest 全列 |
| ② | `sim/effects/damageArea.ts` 的 `ctx.rank` → `1` | 「`packages/shared/src/sim/effects/damageArea.ts ← rank=1`」 |
| ③ | 從 BUCKETS 刪掉 `passive` | 靜態：`hooks.ts` 的 `hook:…` 沒人宣告；動態：`hook:abilityPassive:godie-h00l.passive` · `hook:item:cleaver-of-the-warden#0` · `hook:abilityPassive:godie-hart.passive` 三個 origin 落不進桶 |

三個突變全部用 `python3 scripts/edit-or-die.py` 施加與還原（對不上 ⇒ exit 2）。
`tsc --noEmit -p packages/shared/tsconfig.json`：**2 個錯，全在 `src/ops/enabledSwitchesHaveConsumers.ts`**（另一條 lane 的新檔，#1043），本 lane 六個檔零錯。
測試行數：閘① 113 / 閘② 89 / 閘③ 125 —— 票文的「≤ 80 行」沒守到；理由：三支閘的**產出就是測試本身**（沒有實作行數可比），而每支都要兩個方向＋量尺自證，砍到 80 行會砍掉反方向那一半（形態⑫）。要再瘦的話，閘③的動態治具可抽到 testkit（柵欄外）。

## 5. 紀錄更正（驗收 #5）

- **GH#928**：留言一則，四個假前提逐條「當時說的 / 實際 / 下一次怎麼擋」（本檔 §1 的表）。
- **GH#1017**：Non-goals 引用「技能只佔 48.5%」⇒ 留言標注已被 126 場推翻（13%–100%，第三桶）。
- `docs/_reports/`：grep 全掃**沒有**一份引用 30 件／1 級／48.5%（污染不在這裡）⇒ 無需改。
- `docs/固有能力及寶具總覽.md`（`content:build` 產物）今天已經寫著「84 件寶具」與 30/39/15 三節、「取得方式那一欄就是階級的來源」⇒ 「下一輪讀得到的地方」已存在，⛔ 不必手改。

## 6. 需主 session 套的柵欄外改動

1. **`arena-rules.json`：不需要任何改動**（閘①只讀它）。
2. **可選（柵欄外）**：把閘③的 `BUCKETS` 宣告搬成 `packages/shared/src/sim/combat/damageOrigin.ts::bucketOfOrigin()`，讓 **#1015 的排行榜**與本閘 import 同一份 —— 今天分桶宣告只有測試這一個住處，排行榜的口徑仍是另一套。
3. **可選**：`resolveScaling` 加 runtime 斷言 `Number.isInteger(rank) && rank >= 1`（`sim/effects/effect.ts`，柵欄外）—— 閘②的靜態掃描擋得住字面值，擋不住執行期算出 0 的 rank。
4. **守則犯錯表**：本 lane 沒有新的犯錯要記（三個突變都一次紅）；主 session 若要把「四個假前提」本身記進 `docs/守則犯錯.md`，建議代號＝「它沒有在跑 vs 在哪個環境沒跑」那一條、成因＝`沒有閘`。

## 7. 建議寫進 CLAUDE.md 的段落（逐字，由主 session 套）

```
### ⛔⛔ 「我查的那條路上沒有」≠「它不存在」—— 一天之內四次，四個都有閘了

2026-09-05～06 量到（GH#1030）：

| 我證明了什麼 | ⛔ 我說成了什麼 | 閘 |
|---|---|---|
| 「它不在商店貨架上」（`legendaryShelfIds()` 只讀一張表） | 「玩家拿不到」（寶具 30 件） | `sim/economy/obtainableItems.test.ts`：出貨管道聯集（今天 84 件）≠ 任何單一來源 |
| 「係數表上寫這個數」（讀 `flat`） | 「玩家打出來是這個數」（技能 1 級） | `ops/skillDamageRankExplicit.test.ts`：`resolveScaling` 的 rank 必填、呼叫點不可字面值、⛔ 沒有 `perRank[0]` |
| 「`grep "blind" sim/` 是 0」 | 「引擎沒有實作致盲」 | ⛔ 不開發（`missChanceOf()` 是活的）；相鄰真缺口 #1041 |
| 「這兩桶加起來 100%」 | 「只有這兩桶」（技能 48.5%） | `ops/damageOriginBuckets.test.ts`：每一發 damage 落進宣告過的桶、Σ桶 = Σ全部、第三桶 `hook:*` 非空 |

⭐ 判準：寫下「X 不存在」之前，先問「**我掃的是哪一條路？還有別條嗎？分母是誰？**」
⚠️ 而閘①第一版自己就再犯一次：拿一位遠程英雄當探針 ⇒ 6 件 `requiresAttackType` 的寶具「拿不到」⇒ 84 只對**全體英雄的聯集**成立。
⇒ ⭐ 一個統計要印出**分母與探針**，⛔ 不是只回一個數字。
```
