# GH#1083 — visual-proof.sh 空殼標記 liar-scan 的分母改成「全 repo 帶標記的測試檔」（lane 報告，2026-09-06 23:54）

> 📅 `HEAD=1de2bd31f` 工作樹（⛔ 未 commit，主 session 統一 commit）。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `scripts/visual-proof.sh` | `LIARS` 段的分母從 `find apps/client/src/vfx apps/client/src/render -name '*.test.ts'` 改成 **從 grep 推導**：`git ls-files -z --cached --others --exclude-standard -- '*.test.ts' '*.test.tsx' ':(exclude)docs/legacy' \| xargs -0 -r grep -l '@visual-proof'`（tracked ＋ untracked、gitignore 的不算、`.test.tsx` 也算）。判準 `VOCAB_RE` **一個字沒放寬**；`TOUCHED`／`VISUAL_RE` 沒碰 |
| `packages/shared/src/ops/visualProofScript.test.ts` | 新一條 `it`：`apps/client/src/ui/` 底下一支只有標記、零詞彙的 `*.test.ts` ⇒ 閘 exit≠0 **並指名**；同一檔補上 `alpha` ⇒ 不再被指名；`docs/legacy/…` 底下同樣的複本 ⇒ 不被指名。⭐ 夾具的標記改用 `MARK = ["@visual","proof"].join("-")` 拼出來 —— 因為**這個守衛檔自己現在也在分母裡**，逐字寫會讓它被自己的閘指名 |

## 量到什麼

- 新分母今天 **28 個檔**：`apps/client/src/{vfx,render}` 21 ＋ `ui/floatingTextRenders` ＋ `game/commsWheelVisualProof`（票文點名的兩支）＋ ⭐ **`packages/shared/src/content/locust{Crescent,PentaFamilies,QuadFamilies,TailFamilies,TornadoElemental}.test.ts` 5 支** —— 它們一直帶著標記而從來不在舊分母裡；五支都含 `alpha` ⇒ 綠，⛔ 不是缺陷，但在此之前沒有人量過。
- `docs/legacy/_overwrites/` 底下有 **30+ 份未追蹤的測試複本**（preserve hook 的覆蓋前留底），其中 **2 份是 `visualProofScript.test.ts` 的舊版**：帶逐字標記、零詞彙 ⇒ 不排除退休區的話這支閘在真 repo 上**立刻紅**，而那兩份沒有人該去「修」。⇒ `:(exclude)docs/legacy` 是承重的，理由寫在腳本註解裡（同 `temp-sweep.sh` 的「⛔ 不掃 docs/legacy/」）。⚠️ 這是**唯一**的排除，⛔ 不是路徑清單。
- 改完在真 repo 跑 `bash scripts/visual-proof.sh` ⇒ EXIT=0（liar-scan 零指名；這次改動沒碰畫面層）。

## 突變紀錄（一條）

`MARKED_TESTS` 改回 `find apps/client/src/vfx apps/client/src/render …` ⇒ 🔴 新 `it`：
`expected '⛔ visual-proof：這次改動碰了畫面層…' to contain 'apps/client/src/ui/shell.test.ts'`（ui/ 的空殼在舊分母裡結構上綠）。用 Edit 改回來。

## 決策點

- **`docs/legacy/` 排除**：我挑的。它不是「後台會轉的開關」（只有 CI／作者會轉 ⇒ 不進三個住處，同 2026-09-06「開關的住處看誰會轉」的裁決）；要改就改腳本那一個 pathspec。
- DOM 級詞彙（`display`／`opacity`／`visibility`／`getComputedStyle`）**沒有**加進 `VOCAB_RE`（票文：先不放寬，紅了再說；今天兩支 DOM 級測試都含 `alpha`）。

## 測試

- 與 GH#1087 同一批：run 1 全綠（`visualProofScript` 3 條 5.8 s）；run 2 突變紅；`pnpm typecheck` EXIT=0。
- 測試／實作：腳本 +15/−6，守衛 +28/−2。

## ⛔ 沒做到／需要主 session 動的

- 無柵欄外改動。
- 沒有 rule-slip。
