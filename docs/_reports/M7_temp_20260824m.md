# M7 · 21 張 open 票的證據複驗（⛔ 唯讀）

> 基準 HEAD `94914bef`（複驗過程中由 `73a70071` 前進，兩者對本報告的結論無差異，
> 例外只有 #560/#561 —— 那條 lane **正在寫**，見下）。
> ⛔ 全程零 gh 寫入、零出貨檔改動。判準：**沒有證據就標「未動」**，
> 「看起來做完了」不算；commit 訊息**不算證據**，只有檔:行 / 量到的數字算。

## 結論表

| 票 | 判定 | 一句話 |
|---|---|---|
| #565 骨頭錨點 | **部分** | ⛔ **推翻**「`90fb4167` 已做完」——機制到位，**11 支內容 0 支接上** |
| #558 三筆清理 | **部分** | ③ union 閘已修（`b7f05033`）；①② 未動 |
| #417 同編號漂移 | **已完成** | 棘輪出貨中，龍破斬兩份都是 8.0，剩 274 筆由棘輪管 |
| #325 spawns 契約 | **未動** | schema 仍 `.min(1)`，兩個 `!` 原封不動 |
| #327 匯入包合法性 | **未動** | 3 strict vs **19** passthrough，tokenizer 不存在，MD5 政策不存在 |
| #348 隱藏英雄開關 | **未動（且已升級）** | 開關沒有，而 `hiddenChampions` **已經不是空的**（4 位） |
| #354 18 事件 + 21 action | **部分** | 事件半邊 **全到齊**（`e92a1713`）；21 個 action 只進了合約，引擎仍缺 12 |
| #368 模型尺寸/站姿 | **已完成** | `3a7fd486`；`modelSizing.test.ts` 三條路徑同高 + 血泥四場景都藏 |
| #372 Codex 契約整理 | **已完成** | 六項全在產生的三份文件裡；`「」` 剝除進第〇章；`orient` 缺口已補 |
| #382 六場景 VRAM | **未動（惡化）** | 「已接受」的 scene vramBytes 從 6 條變 **13 條** |
| #401 沿線推進剩 6 支 | **未動** | 3 格 `inert` 還在、`DELAYED_MAX_COUNT` 仍 32、迴力鏢機制不存在 |
| #409 colosseum 餘裕 | **未動（本來就不必動）** | `nav.headroom` 出貨 1.5，柱子仍 r=2 —— 票自己的結論是「現況綠」 |
| #425 wave.* 共用拖尾 | **未動** | 今天重量：**同樣那 5 份**共用，一份都沒分家 |
| #441 語音普查 | **部分** | 三個缺陷全修；**2 位英雄仍沒有戰鬥語音包** |
| #444 級距錨點作廢 | **未動** | `DUEL_ZONE_RADIUS_REF = 24` × `LADDER_FRACTIONS` 原封不動，零 commit |
| #473 上架自動稽核 | **未動** | 後台沒有任何「啟用當下跑稽核」的路徑 |
| #529 89% 綁通用原型 | **部分** | 音效白名單搬進 JSON 了；三個頭條數字**一個都沒改變** |
| #538 大混戰名單慢 | **未動** | #537 好友那半做了，大混戰名單沒有預取、沒有共用預載器、沒有旋鈕 |
| #560 三個清理邊界 | **⏳ 進行中（未提交）** | 另一條 lane **此刻正在寫**：`CleanupEdge` 四個邊界已在工作樹 |
| #561 地面貼圖上限 | **⏳ 進行中（未提交）** | 三住處 + LRU 全在工作樹，HEAD **一個位元都沒有** |
| #588 一帳號一房 | **部分** | ① 一人一房做完（`b5b171fe`）；② owner 的「30 分鐘」裁決未動 |

⇒ **可關 3 張**（#417 · #368 · #372）· 部分 6 張 · 未動 10 張 · 進行中 2 張。

---

## 逐張證據

### #565 骨頭錨點 —— **部分**（⛔ 推翻「已做完」）

**做了的（機制）**：`90fb4167`
- `packages/shared/src/content/schema/effects/spawnVfx.ts:24` `at: z.enum([… ,"bone"])`；
  `:36` `attach`；`:51/:58` refine 把兩者綁成成對出現
- sim/client 兩側接縫 + `VfxSpawnEvent` 型別；commit 訊息自陳守衛走出貨鏈

**⛔ 沒做的（內容，＝這張票的交付物）**：
```
grep -rl '"at": *"bone"'  content/   →  0 個檔
```
票上點名的 11 支逐支查，`content/abilities/<id>.json` 裡 `"bone"` 出現次數**全部是 0**
（godie-hart.r / hart.q / h01n.w / h01n.e / osam.ex / u00j.q / u00v.r / nsjs.w / u00j.w /
u034.passive / h00l.r）。
前置檔 `content/models/imported.bloodbreathstream.json` **仍然不存在**
（`content/models/` 129 個檔裡沒有它；glb `content/assets/models/imported/bloodbreathstream.glb` 在）。
⚠️ `90fb4167` 的訊息自己就寫著「⛔ **一支內容都沒改**（43 支是第 2 批）」。

⇒ **⛔ 不可以關。** 機制那一半可以在票上打勾，11 支內容是剩下的工作。
（`51abf9b9` 的 Saber 金粉走的是 `persistentVfx`，⛔ 不是 `at:"bone"`，不算這 11 支。）

---

### #558 三筆清理 —— **部分（1/3）**

**③ union 覆蓋率 —— ✅ 已修**，commit `b7f05033`
`packages/shared/src/content/schema/config/configUnionCoversDirectory.test.ts:41` 起
改成 `const ROOT = join(DIR, "..")` + `walk()` 遞迴整棵 `schema/` 樹，
並且逐名釘住三個住上一層的檔（`mitigationDoc.ts` / `ownerKnobsDoc.ts` / `castApproachDoc.ts`）
——「閘又縮回單一資料夾了」會紅。

**① `ModelFxRig.dispose()` —— ⛔ 未動**
`apps/client/src/render/modelFxRig.ts:351`
```ts
for (const c of this.containers.values()) c?.dispose();
```
而那些 container 來自 `GameApp.ts:903 loadModelContainer: (glbPath) => this.assets.load(glbPath)`
→ `apps/client/src/render/AssetManager.ts:169-176`，那是一份 **per-Scene、以 resolved path 為鍵的共用快取**。
⇒ rig 仍在 dispose 它不擁有的東西。

**② `E00S.glb` prim3/prim4 —— ⛔ 未動**
`content/models/_overlay-hidden-geometry.json` 的 `models` 有 **17 筆**，
逐鍵列出來沒有 `E00S.glb`（有 E00R / E00U / E00V / Efur / Ekee / H021 / Hapm / Hblm /
O02O / Obla / Ogld / Othr / U01F / Ubal / Ucrl / Umal / Usyl）。

---

### #417 同編號技能數值不一致 —— **已完成（建議關）**

- 守衛出貨中：`packages/shared/src/content/abilityCodeParity.ts`（檔頭逐字寫著界線：
  `COSMETIC_FIELDS` 可以不一樣、`slot` 刻意不算、其餘負面表列必須一樣）
  ＋ `abilityCodeParity.test.ts`（**棘輪**：新漂移紅、修好了還留在 baseline 也紅）
- 票裡點名的那一組**已修好**：`godie-h020.e` 與 `godie-hjai.e` 的 `radius` 今天量到**兩份都是 8.0**，
  而 `packages/shared/src/content/abilityCodeParity.baseline/04.json` 只剩
  `04-002|cooldownTier` / `04-002|effects` / `04-02|effects` / `04-02|template`
  —— **`04-03` 已經不在名單上**。
- 基準線按英雄分片（`234d96b9`），20 個分片共 **274** 個 `<編號>|<欄位>` 鍵 ——
  那是棘輪在管的餘額，⛔ 不是「沒做」。

**可貼進 gh 的關票註解**
> ✅ 交付物三項齊：①掃描器 `packages/shared/src/content/abilityCodeParity.ts`
> ②橫向棘輪 `abilityCodeParity.test.ts`（⛔ 不是白名單：修好了卻留在 baseline 也會紅）
> ③界線寫在 `abilityCodeParity.ts` 檔頭（`slot` / 文案 / vfx 可以不同，其餘必須相同）。
> 票裡點名的 04-03 龍破斬今天量到 `godie-h020.e` 與 `godie-hjai.e` 的 `radius` **都是 8.0**，
> 且 `abilityCodeParity.baseline/04.json` 裡已經沒有 `04-03` 的任何鍵。
> 剩餘 274 筆已知漂移由棘輪逐步收斂（只准降不准升），⛔ 不需要這張票繼續開著。

---

### #325 `zZoneDef.spawns` 契約缺口 —— **未動**

- `packages/shared/src/content/schema/arena.ts:59`
  `spawns: z.tuple([z.array(zVec2).min(1), z.array(zVec2).min(1)])` —— **仍是 `.min(1)`**
- `arena.ts` 全檔 `TEAM_SIZE` 出現 **0 次**；兩個 `superRefine`（:198 / :372）
  只驗「出生點在邊界內」，⛔ 沒有驗個數
- 消費端兩個非空斷言原封不動：
  `apps/game-server/src/match/MatchController.ts:2191` `zoneDef.spawns[side]![slot % TEAM_SIZE]!`
  · `:2393` `zoneDef.spawns[0]![slot % TEAM_SIZE]!`

---

### #327 匯入包合法性 —— **未動**

| 票上的三項 | 量到的 |
|---|---|
| strict-by-default | `packages/shared/src/content/import/packageSchema.ts`：`.strict()` **3** vs `.passthrough()` **19** ⇒ 方向沒有翻，而且 passthrough 比開票時（8+）更多 |
| `[token]` tokenizer | `packages/shared/src/content/import/` 只有 `diagnostics/digest/jcs/packageSchema/targetProfile/zipSafety` —— **沒有 tokenizer 這個檔，全目錄零命中** |
| checksum 政策 | `import/` 與 `docs/editor-contract/` 內 `md5` 命中 **0** |

---

### #348 隱藏英雄開關 —— **未動，而且風險已經從「今天不會發生」升級成「今天會」**

- `packages/shared/src/content/schema/config/roster.ts` 的 `.strict()` 物件只有
  `retiredChampions` 與 `hiddenChampions` 兩格 —— **沒有任何「殭屍避開彩蛋」的開關**
- 兩條路仍共用同一個池：`MatchController.ts:1370 mobChampionPicker()` → `:1373 this.randomChampionPool()`；
  `:1410` `autoPickAndSpawn` 也是 `this.randomChampionPool()`
- ⚠️ **前提過期**：票寫「出貨的 `hiddenChampions` 是空陣列，所以今天不會發生」。
  `content/config/roster.json:18` 現在有 **4 位**（GH#469，owner 2026-08-20 指定：
  死之王 / 飛鼠先生 / 喪標麥可 / 黑人牙膏），而
  `apps/game-server/src/curation/hiddenChampionsShipped.test.ts:80` 正在斷言它 **> 0**。
  ⇒ 玩家**現在**就可能先在殭屍臉上看到彩蛋。

---

### #354 18 事件 + 21 action —— **部分（事件全到齊，action 只進合約）**

**事件 ✅**（`e92a1713`「13 個新 hook 事件 —— owner 那張清單的事件半邊全部到齊」）
`packages/shared/src/content/schema/effects/_hook.ts:22` 起的 `zHookEvent` 含
`onUltimateCast` `onUltimateHit` `onCrowdControlApplied` `onCrowdControlReceived`
`onHeal` `onOverheal` `onAllyDamaged` `onProjectileExpire` `onBoundaryTouch`
`onDashOrBlink` `onLethalDamage` `onRoundStart` `onRoundEnd`；
發射端逐列在 `packages/shared/src/sim/systems/WorldHookSystem.ts:243/277/282/292/301/308/313`
（⇒ ⛔ 不是「下拉裡有、引擎沒有」的 `onLevelUp` 形狀）。

**Action ⛔**：`createZone` / `redirectDamage` / `storeDamage` / `swapPosition` /
`copyAbility` / `delayDeath` 在 `schema/` 與 `sim/` 全樹命中 **0**。
`11aab03f` 做的是**把 21 列寫進對外合約 `PLANNED_CAPABILITIES`**（5 supported /
4 partial / 12 完全沒有），⭐ 那解掉的是「對方以為不需要」，⛔ 不是引擎缺口。

---

### #368 模型尺寸/站姿 —— **已完成（建議關；驗收仍需看畫面）**

commit `3a7fd486`（逐字：「`doc.scale` 從來不是尺寸…**70/78 英雄**在預覽場景跟競技場不一樣大
（多拉A夢 **5.70×**）。站姿歪斜的真因是**血泥 geoset**，只有競技場在藏。」）

- ① 尺寸：`apps/client/src/render/views/modelSizing.ts` 是**共用取尺**；
  守衛 `modelSizing.test.ts` 檔頭明說「⛔ 不驗函式存在」，
  而是斷言 `StorePreview` / `IntermissionScene` / `ChampionView` **三條出貨路徑對同一具網格算出同一個世界高度**
  （夾具刻意做成「身體 + 腳下血泥」兩片圖元 —— 任何一條退回 `doc.scale` 或漏掉
  `hiddenPrimitives` 就紅 ⇒ 直接對著失敗形態⑤設計）
- ② 站姿：血泥宣告一起送到非戰鬥場景 ——
  `GameApp.ts:3546/3568`（`hiddenPrimitives` 進攤位酬載）·
  `IntermissionScene.ts:1005/1020/1051/1074` · `StorePreviewCanvas.tsx:79/184` · `ui/actions.ts:45/47`

**可貼進 gh 的關票註解**
> ✅ 兩半都落地（commit `3a7fd486`）。① 四條預覽路徑改走**同一個**取尺函式
> `apps/client/src/render/views/modelSizing.ts`（原因是 `doc.scale` 從來不是尺寸，overlay 一律 1；
> 量到 70/78 英雄在預覽與競技場不一樣大，多拉A夢 5.70×）。
> ② 「下半身傾斜」的真因是**血泥 geoset**（只有競技場在藏），修法是把
> `model@1.hiddenPrimitives` 一起送進商店／英靈殿／頒獎台
> （`GameApp.ts:3568` · `IntermissionScene.ts:1020` · `StorePreviewCanvas.tsx:184`）。
> 閘 `apps/client/src/render/views/modelSizing.test.ts` 斷言三條**出貨**路徑對同一具網格
> 算出同一個世界高度，夾具是「身體 + 腳下血泥」兩片圖元 ⇒ 任一條退回 `doc.scale`
> 或漏掉 `hiddenPrimitives` 就紅。⚠️ 最終驗收仍是看畫面，若還有個別英雄歪斜請另開單支票。

---

### #372 Codex 合約整理 —— **已完成（建議關）**

`docs/技能標記機制與效果規則.md`（`pnpm spec:build`）與
`docs/editor-contract/ggd-runtime-capabilities.md`（`pnpm caps:export`）今天的命中數：

| 票上點名 | 規則書 | 能力契約 |
|---|---:|---:|
| `convertTeam` | 3 | 2 |
| `item@1.marks` | 5 | 9 |
| `dispel.polarity` | 6 | 2 |
| `augment@1.icon` | 6 | 5 |
| `ENTITY_FLAG` uint32 | 2 | — |
| `orient`（`3a7fd486` 自陳的**已知缺口**） | **7** | 3 |

- `orient` 的缺口成因是「`gen_spec.ts` 不讀 `schema/vfx.ts`」——
  今天 `tools/skill-spec/gen_spec.ts:83` 就是 `from "../../packages/shared/src/content/schema/vfx"`，缺口已補
- 「`「」` 剝除規則進第〇章」：`docs/技能編輯器引擎須知 20260811.md:155-167`
  （逐字「**必須先剝掉整段 `「…」` 再開始找機制**」+ 三支誤讀案例 + 「含跨行、含行中」的但書）
- ⚠️ 票上的 `revive.condition` 是**過期項**：`schema/effects/revive.ts` 只有
  `hpPct` / `manaPct` / `side` / `teamCharge`，`condition` 這個欄位**從來不存在** ⇒ 沒有東西要記錄

**可貼進 gh 的關票註解**
> ✅ 六項裡五項已在**產生的**三份文件裡（convertTeam / item@1.marks / dispel.polarity /
> augment@1.icon / ENTITY_FLAG uint32），`「」` 剝除規則在
> `docs/技能編輯器引擎須知 20260811.md:155-167`（含「整段、含跨行、含行中」的但書與三支誤讀案例）。
> `3a7fd486` 自陳的唯一已知缺口（`orient` 在產生的合約裡是 0，因為 `gen_spec.ts` 不讀 `schema/vfx.ts`）
> 已補：`tools/skill-spec/gen_spec.ts:83` 現在直接 import `schema/vfx`，規則書命中 7 處。
> ⚠️ 票上的 `revive.condition` 是過期項 —— `schema/effects/revive.ts` 只有
> hpPct / manaPct / side / teamCharge，`condition` 這個欄位從來不存在。
> 三份文件都有**逐位元組 `--check` 閘**（skillSpecFresh / caps / skillRemakeDocsFresh），
> ⇒ 「合約落後於引擎」這個形狀已經由閘常態擋著，⛔ 不需要一張常開的整理票。

---

### #382 六場景 VRAM —— **未動，而且惡化**

`tools/model-budget/baseline.json`（`updatedAt: 2026-08-18`）的 `accepted` 今天有
**13 條 `scene:*/vramBytes`**：champ-select · combat-castle · **combat-colosseum** ·
**combat-dota** · combat-frieren · **combat-heavens-arena** · combat-infinity-castle ·
combat-nazarick · combat-shiganshina · combat-skeleton · combat-world-tree ·
intermission · settlement。
票開的時候是 **6 條**。⇒ ⛔ 票裡預言的「加第 7 個的成本是一行 JSON」已經發生了七次。
票建議的出路 2（替地標開一個自己的 role，因為「單件 × 擺放數 50」的分母本來就錯）
在 `tools/model-budget/` 找不到任何實作。

---

### #401 沿線推進剩 6 支 —— **未動**

| 票上三段 | 量到的 |
|---|---|
| ① `tpl-line-sweep` 的 3 格 `inert` | `content/ability-templates/tpl-line-sweep.json:15/23/31` **三格都在**，字串仍是「分段時序未支援：6 段掃擊折算為單發穿透投射」 |
| ② `DELAYED_MAX_COUNT = 32` | `packages/shared/src/sim/effects/kindLimits.ts:155` **仍是 32**（而 `COMBO_MAX_STRIKES` 與 `MODEL_FX_MAX_TOUCH_SAMPLES` 綁定同值 ⇒ 放寬是三處連動，⛔ 不是改一個數字） |
| ③ 曲線/迴力鏢機制 | `boomerang` / `迴力鏢` / `returnPath` 在 `sim/` 與 `schema/` **零命中** |

---

### #409 colosseum 通道餘裕 —— **未動（票本身的結論就是「不必動」）**

- `content/config/map-spec.json` → `nav.headroom = 1.5`（＝票裡說「現況在出貨的 1.5 下是綠的」的那個值）
- `content/arenas/arena.colosseum.json` 兩個 zone 各 20 顆 circle 障礙，半徑集合仍是
  `[0.5, 0.7, 1, 1.4, 2]` ⇒ **柱環還是 r=2**，三個選項一個都沒被採用
⇒ 這是一張**等 owner 取捨**的票（要不要把柱子變細換 headroom），
⛔ 不是一個待修缺陷。要關的話理由是「現況綠，取捨已記錄」，⛔ 不是「做完了」。

---

### #425 `wave.*` 共用拖尾 —— **未動（今天重量，逐份相同）**

| 彈道 | `vfxKey` | 同 key 的其他彈道 |
|---|---|---|
| `imported.wave.arcane` | `fx.prim.arcane.bolt` | `grail.projection-bolt` · `grail.tracking-bolt` · `imported.bolt.arcane` |
| `imported.wave.ki` | `fx.prim.ki.bolt` | `imported.bolt.ki` |
| `imported.wave.lightning` | `fx.prim.lightning.bolt` | `imported.bolt.lightning` |
| `imported.wave.void` | `fx.prim.void.bolt` | `imported.bolt.void` |
| `imported.wave`（base） | `fx.thorn` | `thorne.e.thorn` |
| earth / fire / ice / physical | 各自獨佔 | —（乾淨的 4 份，與票一致） |

⇒ 5 共用 / 4 乾淨，**與開票當天逐項相同**。

---

### #441 語音普查 —— **部分（三個缺陷全修，兩位英雄仍缺包）**

**✅ 缺陷① `spatialPolicy` 對 6 個類別說謊 —— 修了**
`apps/client/src/audio/spatialPolicy.ts:220/221/271/290/291/292`：
`taunt` `charge` `free-move` `thanks` `thumbs-up` `watch` **六格全部 `dispatched: true`**，
reason 逐字寫「GH#441 —— shopPerformVoice … 已在播」。

**✅ 缺陷② 模組零出貨引用 —— 修了**
`apps/client/src/GameApp.ts:217` import + `:2896` 註解逐字「類別交給政策表
（`VOICE_CATEGORY_POLICY`）。少了它，那份表全 repo 沒有任何出貨呼叫端（失敗形態③）」；
另有 `apps/client/src/audio/vfxSound.ts:37` 用 `spatialSourceFor`。

**✅ 缺陷③ 喪標麥可名言放錯層 —— 修了**
`content/assets/audio/voices/quotes/quotes.json` 的 `quotes`（115 筆）**含** `godie-zombiex`，
且 `clip: assets/audio/voices/quotes/godie-zombiex.mp3` **檔案存在**。

**⛔ 未動：2 位英雄沒有戰鬥語音包**
`content/assets/audio/voices/champions/MANIFEST.json` → `champions` **51 筆**，
`godie-e00s`（白木老樹精）與 `godie-ucrl`（傑·富力士）**都不在裡面**。
⇒ 票的標題第一項仍然成立。（「11 類無觸發點」這一半本次未逐類複驗，
判定保守留在「部分」。）

---

### #444 級距錨點作廢 —— **未動**

- `git log --grep="#444"` **零個 commit**
- `packages/shared/src/content/skillTiers.ts:70` `DUEL_ZONE_RADIUS_REF = 24`、
  `:118` `LADDER_FRACTIONS = [1/12, 1/8, 3/16, 1/4, 1/3, 1/2]`、
  `:141` `zoneRadius * LADDER_FRACTIONS[…]` —— **推導鏈一個字都沒改**
- 檔頭 `:61-67` 仍寫「R = 24 是參考值…真值從 `Arenas` 推導，守衛在對帳」
  ⇒ 那條守衛守的是「24 對不對」，⛔ 不是 owner 說的「**不能再拿場地大小當比例尺**」

---

### #473 上架自動稽核 —— **未動**

- `git log --grep="#473"` 只命中一份**唯讀複驗報告**（`3d1dafb9`），零實作
- `apps/admin/src` 全樹找不到「白名單/貨架存檔 → 跑 `noOpModifierClaims` / castability /
  描述↔JSON」的任何路徑；`QuickApprovalPage.tsx` 是**玩家帳號**審核（approvals.ts），
  ⛔ 不是內容稽核

---

### #529 89% 綁通用原型 —— **部分（只有音效白名單那一小格動了）**

**✅ 動了的**：`6f3ad504`「技能施法音的 cue 名單從 TS 搬進 JSON —— ⛔ TS 那份真的刪掉了」。
`apps/client/src/audio/combatSfx.ts:175` 現在寫「這裡**曾經**住著 `WC3_ABILITY_SFX`：
一個 52 個字面值的 TypeScript…」⇒ 第〇·五守則說的「特效音效綁定要住 JSON」那一項付掉了。

**⛔ 頭條數字今天重量，一個都沒改變**：

| | 開票時 | **今天** |
|---|---:|---:|
| 技能總數 | 420 | 421 |
| 有 `vfxKey` | 384 | 385 |
| 綁 `fx.prim.*`（通用原型） | 342（89%） | **343（89%）** |
| 綁 `fx.w3x.*`（原作特效） | 24（6%） | **24（6%）** |
| `fx.w3x.*` 文件總數 / 被引用 | 120 / 10 | **120 / 18** |
| 有 `sfxKey` | 72（17%） | **72（17%）** |

⇒ 「原作特效匯進來了但沒接到技能上」**原封不動**。

---

### #538 大混戰名單慢 —— **未動**

- `#537` 的**好友**那一半做完了：`6e622f36`「好友清單在**商店那段時間**就抓好」
- 大混戰那一半：`apps/client/src/ui/platform/store.ts` 全檔 `royale` 命中 **0**；
  預取只有 `refreshFriends`（:502 / :806 / :829 / :840 / :849 / :858 / :1397）
- 票要的「**一支共用的預載器**（好友 · 線上玩家 · 大混戰名單 · 排行榜）」與
  「後台一格（要不要預載 · 快取多久）」——`prefetch`/`預載` 在該目錄零命中，config 也沒有那一格

---

### #560 三個清理邊界 —— **⏳ 進行中（未提交），⛔ 不要當成未動也不要當成做完**

⚠️ 複驗過程中這幾個檔**在我眼前改變了**（先讀到 `RoundEdge = "enter" | "leave"`，
數分鐘後同一行變成 `Extract<CleanupEdge, "enter" | "leave">`）。⇒ 另一條 lane 正在寫。

工作樹（`git status` = `M`，**HEAD `94914bef` 沒有**）：
- `apps/client/src/render/roundVfxLifecycle.ts:54` `export type CleanupEdge = (typeof CLEANUP_EDGES)[number];`
  · `:65` `RoundEdge = Extract<CleanupEdge,"enter"|"leave">` · `:75` `resetForRound(edge: CleanupEdge)`
- `apps/client/src/render/roundFxRegistry.ts:48` `const ALL_EDGES: readonly CleanupEdge[] = CLEANUP_EDGES;`
- `content/config/vfx-cleanup.json` 的 note 已寫入逐字說明：「邊界從 enter/leave 兩個變成
  entry（進場組裝）／enter（開打）／leave（進商店）／exit（離場）四個，`registry.add()`
  的預設是四個邊界全跑，只在某幾個邊界跑的那一列要走 `addScoped()` 並且寫得出理由」

**HEAD 上的狀態**（＝真正可以拿去對票的那一份）：
- 邊界仍只有 enter/leave；`GameApp.dispose()` 與回合邊界仍是兩份清單
- 殘留守衛 `apps/client/src/vfx/roundGrowthIsBounded.test.ts:73-76` 只數 **4 個**計數器
  （`modelfx-*` transformNodes / particleSystems / meshes / materials）——
  票要的六個（多 texture 與骨架）不齊，且它只跑**回合**邊界，⛔ 不涵蓋出口/入口

⇒ **⛔ 不要關。** 等那條 lane commit 之後再複驗一次；
⚠️ 特別要驗「config note 已經宣告四個邊界」與「程式真的有四個邊界」是否同時落地
——note 領先程式的那個窗口正是第三守則說的謊話形狀。

---

### #561 地面貼圖快取上限 —— **⏳ 進行中（未提交）**

同一條 lane。工作樹裡四個住處都在，HEAD **一個位元都沒有**：
- `content/config/vfx-cleanup.json:34/35` `groundTextureCacheMax: 8` · `groundTextureCacheMaxMobile: 4`（`M`，未提交）
- `packages/shared/src/content/schema/config/vfxCleanup.ts:381/392` + `:475` `DEFAULT_*`（`M`）
- `apps/admin/src/configForms.ts:660/665` 兩格欄位＋長說明（`M`）
- `apps/client/src/render/groundTextureCache.ts:69-85`（`M`）：上限讀
  `groundTextureCacheMax`（from `../vfx/vfxCleanupPolicy`），`evictTo` 裡明文
  「剛拿到的那一組永遠不會被這一次淘汰掉」，預熱不淘汰
- 說明文字有把「調小＝省 VRAM 但換圖平色 / 調大＝無縫但吃記憶體」寫出來，
  也有「兩格拉到 64 ＝止血閥」的 rollback

⇒ 交付物看起來會齊，**但今天不可關** —— HEAD 上不存在的東西不算做完。

---

### #588 一帳號一房 —— **部分**

**✅ ① 一人一房 —— 做完了**，commit `b5b171fe`
「feat(server)(#588)(#595)(#593): 一人一房 + ⛔ 離開不交給 AI —— owner 的裁決落地」
- `apps/game-server/src/rooms/accountRooms.ts`（新的跨房登記表）
- `apps/game-server/src/rooms/accountSingleRoom.test.ts` —— 檔頭逐字寫著突變點
  「拿掉 `MatchRoom.onJoin` 裡的 `previousRoom.evictAccount(accountId)` ⇒ 房間數曲線變成 1,2,3,4,5,6,7」，
  而且斷言讀的是**出貨登記表** `roomRegistry.active`、真的把房走到 `_dispose()`
  （⛔ 不是掃 presence 的 key，避開失敗形態⑦）
- `MatchRoom.ts:987` 註解記著「在此之前這裡是 `seat.setDriver(new AIDriver())`，
  而那正是幽靈房**活著**的機制」⇒ 離開不再交給 AI

**⛔ ② owner 2026-08-23 的裁決（body 最上面那一塊）—— 未動**
> 「每間房間存活時間只要開始進入戰鬥後，存活時間最多 30 分鐘，避免幽靈房間」

- 練習房的相位機**仍然**永遠停在 combat：
  `apps/game-server/src/match/MatchController.ts:4052` `if (this.practice?.endlessCombat) break;`
- 唯一的主動收房路徑仍是 `MatchRoom.ts:1224-1225` 的 `finishMatch()` 後
  `clock.setTimeout(() => this.disconnect(), …)`（那個秒數在 `06ff70ce` 由寫死 10 秒
  改成後台 120 秒，那是 **#651**，⛔ 不是這一條）
- 全 repo 找不到「進入戰鬥後 N 分鐘一律收房」的閘或旋鈕
  （`combatMaxSec` 的上界 1800 是**回合相位**的硬底線，
  `packages/shared/src/content/schema/config/match.ts:485`，而練習房正是繞過相位推進的那一個）

⇒ **⛔ 不可以關。** 建議把票縮成單一交付物：「戰鬥開始後的房間硬壽命（後台一格，出貨 30 分鐘），
⛔ 不管相位機走不走得到 `matchEnd`」。

---

## ⚠️ 三個複驗過程本身的發現（⛔ 不在票上）

1. **#348 的前提已經過期**：票寫「今天不會發生」是因為 `hiddenChampions` 是空的，
   而 GH#469 之後它有 4 位，`hiddenChampionsShipped.test.ts:80` 還在斷言它 > 0。
   ⇒ 這張票的**嚴重度該升**，⛔ 不是維持原狀。
2. **#382 的棘輪正在被當成垃圾桶用**：6 → 13 條「已接受」，而票開的理由正是
   「加第 7 個的成本是一行 JSON」。
3. **一條 lane 的 config note 領先它自己的程式**（#560）：
   `content/config/vfx-cleanup.json` 已經逐字宣告四個清理邊界，
   而 `roundVfxLifecycle.ts` 在我第一次讀時只有兩個。
   ⇒ 那條 lane commit 前要自己確認兩邊同時落地，否則就是第三守則的「註解會說謊」。
