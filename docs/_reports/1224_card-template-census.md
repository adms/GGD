# 🧮 英雄卡屬性共用普查（GH#1224）

> ⚙️ **這一份是量出來的，⛔ 不要手改。**
>
> ```bash
> npx tsx tools/balance-anchors/card_template_census.ts          # 重量
> npx tsx tools/balance-anchors/card_template_census.ts --check  # 唯讀；過期回非零
> ```
>
> ⛔ 這一支**一個數值都沒有挑** —— owner 2026-08-22 逐字：「公式已定好，只要公式本身自洽，
> **我們只調系統倍率**」。下面每一個數字都是**出貨函式**算出來的。

---

## 🔎 三句話（給 owner 挑的那張表在第 5 節）

1. **共用比票上寫的更嚴重**：票上說 74/132，今天量到 **155/167 位（93%）的血量曲線與別人一模一樣** —— ⭐ 逐點相同，⛔ 不是「差不多」。
2. ⭐⭐ **真正危險的不是共用，是刻度有階梯**：中位數落在 `(2800, 3800]` 這一整段裡得到同一張表，而今天的錨點 **2838 離下緣只有 38 點（1.3%）** ⇒ ⭐ 中位掉 38 點，全遊戲傷害 **−20%**。
3. ⭐ **有一個「零改動」的選項**：只算三圍寫著 `w3x`（＝引用得到出處）的那 48 位，中位數**三格都與今天的出貨值逐位元組相同** ⇒ 換這個分母，五級距**一個數字都不用動**，而且新上架的模板預設卡**再也搬不動它**。

⛔ 上面第 3 點**不是提案** —— 我沒有挑（第一守則：這一格是 owner 的）。第 5 節四個選項並列。

---

## 0. 先看這一格：今天的傷害刻度**沒有**掛在卡面中位上

`content/config/balance-anchors.json` 的 `enabled` 是 **true** ⇒ 錨點用的是那份設定檔裡的**固定值**（`baseHp` LV30 2838 · LV50 4438 · LV99 8358）。

⇒ ⭐ 卡面中位今天只在**兩條路**上生效：

| 路 | 什麼時候 |
|---|---|
| **rollback** | `balance-anchors.enabled:false` ⇒ 分母交還給名單（＝下面的 Ⓑ） |
| **偏差警告** | `pnpm anchors:check` 差 >10% 印一行，⛔ 不改值 |

⛔ 所以這份報告**不是**「玩家今天打出來的傷害」，⭐ 它是「**如果**分母交還給名單，會得到什麼」。

---

## 1. 母體 —— **167 位**對戰可選英雄

> 來源：`apps/platform/internal/curation/starter.go 的 starterChampions（對戰可選名單）− content/config/roster.json 的 retiredChampions − 變身態（英雄卡的 transform.role === "alternate"）`
>
> ⛔ **不是** `content/champions/` 的檔案數（含變身態與 fail-open 骨架佔位）。

| 三圍出處欄（`attributes.source`） | 幾位 | 佔比 |
|---|---:|---:|
| `authored` | 119 | 71.3% |
| `w3x` | 48 | 28.7% |

⚠️ ⭐ `w3x` 是唯一**引用得到出處**的值（`tools/w3x-import/apply_attributes_to_content.py`
從 `RESOLVED_HERO_STATS.json` 寫進去的）。⛔ `authored` **不是**一個出處 ——
它只說「不是從 w3x 來的」，⛔ 沒有說是誰、依據什麼挑的。

---

## 2. 共用了什麼 —— 逐層量

| 層 | 不同的值有幾種 | 最大一群 | 落在「≥2 張共用」的卡 | 佔母體 |
|---|---:|---:|---:|---:|
| **全套**（baseStats ＋ growth ＋ 三圍） | 76 | 11 | **117** | 70.1% |
| `baseStats`（15 格） | 59 | 16 | **125** | 74.9% |
| `growth`（每級成長） | 74 | 11 | **118** | 70.7% |
| `attributes`（三圍） | 54 | 28 | **126** | 75.4% |
| ⭐ **決定血量的四格**（base ＋ 每級 ＋ STR ＋ STR 每級） | 38 | 28 | **155** | 92.8% |
| ⭐ **決定魔力的四格**（base ＋ 每級 ＋ INT ＋ INT 每級） | 37 | 22 | **153** | 91.6% |

⭐ **要看的是倒數第二欄**（「≥2 張共用」），⛔ 不是「最大一群」——
票上的「74 張共用同一組」問的是**有多少張卡的屬性不是自己的**。

⚠️ 血量那一列與 `baseStats` 那一列**不會**相等：`maxHealth(L)` 只吃四格
（`championStatBase` ＝ `baseStats.maxHealth` ＋ `growth.maxHealth`×(L−1) ＋ `strToMaxHealth`×STR(L)），
⇒ 兩張卡在別的欄位不同、而血量曲線**逐點相同**，是常見的。

---

## 3. 最大的幾群（血量四件組）—— 逐張列名

### 28 張：base `150` ＋ 每級 `80` ＋ STR `18`（每級 `0`） ⇒ LV30 純基礎 **2884**

- 出身：鬥士 20 · 法鬥 8
- 三圍出處：`authored` 24 · `w3x` 4
- 成員：`b2-albus`、`b2-bojji`、`b2-goblin`、`b2-kaede`、`b2-keyaru`、`b2-matthias`、`b2-ned`、`b2-orphen`、`b2-sinbad`、`b2-touka`、`community-review-02-20260907`、`community-review-03-20260907`、`community-review-04-20260907`、`community-review-08-20260907`、`community-review-11-20260907`、`community-review-12-20260907`、`community-review-19-20260907`、`community-review-23-20260907`、`community-review-28-20260907`、`community-review-31-20260907`、`community-review-32-20260907`、`community-review-35-20260907`、`godie-e00w`、`godie-ewar`、`godie-ogrh`、`godie-u00n`、`lol-leesin`、`lol-yasuo`

### 15 張：base `150` ＋ 每級 `80` ＋ STR `13`（每級 `0`） ⇒ LV30 純基礎 **2769**

- 出身：法師 15
- 三圍出處：`authored` 15
- 成員：`b2-aladdin`、`b2-makoto`、`b2-misery`、`b2-popp`、`b2-rin`、`b2-uncle`、`b2-yogiri`、`community-review-01-20260907`、`community-review-16-20260907`、`community-review-17-20260907`、`community-review-20-20260907`、`community-review-29-20260907`、`community-review-30-20260907`、`lol-karthus`、`lol-xerath`

### 11 張：base `541` ＋ 每級 `45.833673469387755` ＋ STR `13`（每級 `0`） ⇒ LV30 純基礎 **2169.2**

- 出身：法師 11
- 三圍出處：`authored` 11
- 成員：`acquired-astralym`、`acquired-beatrice`、`acquired-emilia`、`acquired-lord-nightmares`、`acquired-mewtwo`、`acquired-pokemon-trainer`、`acquired-ram`、`acquired-saya`、`lol-ahri`、`lol-fiddlesticks`、`lol-velkoz`

### 11 張：base `541` ＋ 每級 `35.000204081632646` ＋ STR `18`（每級 `0`） ⇒ LV30 純基礎 **1970**

- 出身：鬥士 11
- 三圍出處：`authored` 11
- 成員：`acquired-cattiva`、`acquired-inuyasha`、`acquired-mario`、`acquired-minecraft`、`acquired-morgiana`、`acquired-naruto`、`acquired-ryu`、`acquired-wargreymon`、`acquired-zero`、`lol-garen`、`lol-sett`

### 11 張：base `150` ＋ 每級 `60` ＋ STR `21`（每級 `0`） ⇒ LV30 純基礎 **2373**

- 出身：軟輔 10 · 狂戰 1
- 三圍出處：`authored` 10 · `w3x` 1
- 成員：`b2-haga`、`b2-kaiji`、`b2-maomao`、`b2-shinchan`、`b2-takopi`、`community-review-09-20260907`、`community-review-10-20260907`、`community-review-21-20260907`、`community-review-27-20260907`、`godie-e00r`、`lol-lux`

### 7 張：base `150` ＋ 每級 `100` ＋ STR `25`（每級 `0`） ⇒ LV30 純基礎 **3625**

- 出身：硬輔 7
- 三圍出處：`authored` 7
- 成員：`b2-boxxo`、`b2-fushi`、`b2-naofumi`、`b2-nube`、`community-review-15-20260907`、`community-review-22-20260907`、`community-review-37-20260907`

⋯另有 **20** 群（各 7–2 張，合計 72 張）。

---

## 4. `growth.maxHealth` 逐張量（票上 Scope 2：掉下來的是**成長**那一半）

不同的每級成長只有 **11** 種（母體 167 位）：

| 每級成長 | 幾位 | 那幾位的出身 |
|---:|---:|---|
| 80 | 67 | 鬥士 30 · 法師 21 · 法鬥 16 |
| 60 | 32 | 軟輔 14 · 狂戰 11 · 射手 7 |
| 100 | 19 | 坦克 10 · 硬輔 9 |
| 40 | 12 | 法刺 10 · 砲手 2 |
| 35.000204081632646 | 11 | 鬥士 11 |
| 45.833673469387755 | 11 | 法師 11 |
| 27.67734693877551 | 5 | 坦克 5 |
| 10.305102040816323 | 4 | 軟輔 4 |
| 0.8563265306122456 | 2 | 狂戰 2 |
| 6.100816326530613 | 2 | 法刺 2 |
| 29.202653061224485 | 2 | 射手 2 |

⚠️ ⭐ 這一欄**今天沒有執行期讀者**（GH#1224 的第二則留言量過：註冊表被出身表覆寫，153/153 張）——
⛔ 但它**被這支統計讀**，而這支統計是 rollback 路徑上的分母。

---

## 5. ⭐ 換一個分母，五級距會變成什麼（全部走出貨函式）

推導鏈（`gen.ts`，⛔ 一個字面值都沒有）：`（純基礎中位 LV30 ＋ 初始加成 1200）÷ 20 發` → 進位 ⇒ 極小 ⇒ 其餘四格 ＝ 極小 × 單體冷卻比。

| 分母 | 人數 | 純基礎 HP 中位 LV30 | 純基礎 HP 中位 LV50 | 純基礎 HP 中位 LV99 | ⭐ 中位那一張（探針） | 極小 | 傷害五級距 | vs 出貨 | 耗魔五級距 |
|---|---:|---:|---:|---:|---|---:|---|---:|---|
| ⭐ **Ⓐ 今天出貨**（固定錨點） | 167 | **2838** | **4438** | **8358** | （⛔ 不取中位，讀設定檔） | **250** | **250 / 625 / 1250 / 1875 / 2500** | — | 75 / 150 / 300 / 600 / 1200 |
| Ⓑ 全母體取中位（＝ `enabled:false` 的 rollback 路徑） | 167 | 2769 | 4369 | 8289 | `b2-misery` | 200 | 200 / 500 / 1000 / 1500 / 2000 | **-20%** | 75 / 150 / 300 / 600 / 1200 |
| Ⓒ 排除「血量四件組與別人一模一樣」的卡 | 12 | 3068 | 4668 | 8588 | `godie-h01u` | 250 | 250 / 625 / 1250 / 1875 / 2500 | **+0%** | 50 / 100 / 200 / 400 / 800 |
| Ⓓ 只留三圍寫著 `source: "w3x"` 的卡 | 48 | 2838 ⭐同 | 4438 ⭐同 | 8358 ⭐同 | `godie-u00k` | 250 | 250 / 625 / 1250 / 1875 / 2500 | **+0%** | 75 / 150 / 300 / 600 / 1200 |
| Ⓔ 排除 `b2-*` 與 `community-review-*`（票上點名的兩群） | 93 | 2281 | 3481 | 6421 | `godie-e001` | 200 | 200 / 500 / 1000 / 1500 / 2000 | **-20%** | 50 / 100 / 200 / 400 / 800 |

- **Ⓑ ** 今天的名單，一張不排除
- **Ⓒ ** 共用者**全部**出局（含被共用的那一張）
- **Ⓓ ** ⭐ 唯一「引用得到出處」的欄位
- **Ⓔ ** 照 id 前綴排除，⛔ 不是照證據

⚠️ ⭐ 「vs 出貨」比的是**極小**那一格 —— 其餘四格與它嚴格成正比，所以同一個百分比。
⭐ 「同」＝ 那一格與 `balance-anchors.json` 的固定值**逐位元組相同**。
⭐ **探針**那一欄是「中位數是哪一張卡給的」—— ⛔ 一個不印探針的中位數驗證不了。

⭐⭐ **Ⓓ 與今天的出貨值三格全同**，而那個值 2838 正好是 **5 張卡**的 LV30 血量：`godie-hjai`（w3x）、`godie-nsjs`（w3x）、`godie-u00j`（w3x）、`godie-u00k`（w3x）、`godie-ubal`（w3x）。

⚠️ ⛔ **這只是量到的相等，⛔ 不是我查到的因果** —— `content/config/owner-knobs.json` 裡**沒有** `balance-anchors.baseHp` 這一格，⇒ 那三個數字引用不到 owner 的任何一句原話（它們是 2026-09-12 的一次量測快照）。⭐ 能說的只有：**照 Ⓓ 換分母，今天的五級距一個數字都不用動。**

---

## 5.5 ⭐⭐ 刻度是一條**階梯**，而今天的錨點站在階邊上

`anchorFloorFrom` 是 `ceil((中位 ＋ 1200) ÷ 20 ÷ 50) × 50` ⇒ ⭐ **中位數落在一整段 1000 點寬的區間裡，得到的是同一張表**；跨過邊界，整張表跳一格。

| 分母 | 中位 LV30 | 它落在哪一段 | 離下緣 | 離上緣 | 極小 |
|---|---:|---|---:|---:|---:|
| ⭐ **Ⓐ 今天出貨**（固定錨點） | 2838 | `(2800, 3800]` | **38**（1.3%） | 962（33.9%） | 250 |
| Ⓑ  | 2769 | `(1800, 2800]` | **969**（35.0%） | 31（1.1%） | 200 |
| Ⓒ  | 3068 | `(2800, 3800]` | **268**（8.7%） | 732（23.9%） | 250 |
| Ⓓ  | 2838 | `(2800, 3800]` | **38**（1.3%） | 962（33.9%） | 250 |
| Ⓔ  | 2281 | `(1800, 2800]` | **481**（21.1%） | 519（22.8%） | 200 |

⇒ ⭐⭐ **今天的錨點 2838 離下緣只有 38 點** —— 中位血量的 **1.3%**。

⭐ 說白一點：中位再掉 38 點，極小就從 **250 → 200**，＝ **全遊戲每一支技能的傷害 −20%**。⭐ 而那個觸發條件**一張卡就做得到**（上面第 6 節逐群量過）。

⚠️ ⭐ 這不是「74 張卡共用預設」那個病，⭐ 是它的**放大器**：
共用的預設讓中位數**可以被一批新卡整段搬走**，而階梯讓「搬 1%」變成「傷害動 20%」。
⛔ 兩件事要分開看 —— 換分母治前者，⛔ 治不了後者。

---

## 6. 逐群敏感度：**只**拔掉這一群，中位會走多遠

| 拔掉哪一群 | 幾張 | 剩幾位 | 中位 LV30 | 中位動了 | 極小 | 傷害動了 |
|---|---:|---:|---:|---:|---:|---:|
| （不拔，全母體） | 0 | 167 | 2769 | — | 200 | — |
| base 150 ＋每級 80 ＋STR 18 | 28 | 139 | 2373 | -14.3% | 200 | +0% |
| base 150 ＋每級 80 ＋STR 13 | 15 | 152 | 2465 | -11.0% | 200 | +0% |
| base 541 ＋每級 45.833673469387755 ＋STR 13 | 11 | 156 | 2769 | +0.0% | 200 | +0% |
| base 541 ＋每級 35.000204081632646 ＋STR 18 | 11 | 156 | 2769 | +0.0% | 200 | +0% |
| base 150 ＋每級 60 ＋STR 21 | 11 | 156 | 2769 | +0.0% | 200 | +0% |
| base 150 ＋每級 100 ＋STR 25 | 7 | 160 | 2488 | -10.1% | 200 | +0% |

⭐ 這張表回答的是票上那句「**它還會再動**」：一群同型卡上架（或下架），中位就往它那一格靠。

⚠️ ⭐ **「傷害動了」那一欄多半是 0%，⛔ 而那不是安全** —— 它只是說
「這一次沒跨過階梯的邊界」。⭐ 中位那一欄才是真正在動的東西（上面 5.5）。
⛔ 拿「傷害沒動」當成「母體沒問題」的證據，就是把**階梯的量化**讀成**穩定性**。

---

## 7. 三個錨點的達成率（每一個分母各算一次）

門檻 20 發（owner Q1「20 次以內一定要能殺死對方」），分母是**純基礎＋加成**。

| 分母 | LV30（hard limit（一定要滿足）） | LV50（soft limit（能滿足比較好）） | LV99（極限（不要求）） |
|---|---:|---:|---:|
| ⭐ **Ⓐ 今天出貨** | 16.2 發 ✅ | 22.6 發 ❌ | 38.2 發 ❌ |
| Ⓑ  | 19.8 發 ✅ | 27.8 發 ❌ | 47.4 發 ❌ |
| Ⓒ  | 17.1 發 ✅ | 23.5 發 ❌ | 39.2 發 ❌ |
| Ⓓ  | 16.2 發 ✅ | 22.6 發 ❌ | 38.2 發 ❌ |
| Ⓔ  | 17.4 發 ✅ | 23.4 發 ❌ | 38.1 發 ❌ |

---

## 8. ⛔ 這張票還缺什麼（⛔ 不要讀成做完了）

| 驗收條件 | 狀態 |
|---|---|
| 那些卡的每一格屬性要嘛引用得到出處、要嘛被標成「模板預設，未設計」 | ⚠️ **量到了，⛔ 還沒標**：今天唯一的出處欄是 `attributes.source`，而它只有 `w3x` 一種值算出處 |
| 錨點母體的定義寫成一條會紅的閘 | ⛔ **還沒有** —— 這一支沒有被 `skills:check` 收（`package.json` 在這條 lane 的柵欄外） |
| owner 決定：未設計的卡算不算在分母裡 | ⛔ **等 owner** —— 上面第 5 節就是那張選項表 |
| `anchors:check` 綠 | ✅ 綠（這份普查**不動**任何產物） |

⭐ **接線那一行**（留給主線）：`package.json` 加 `cardcensus:check` 並收進 `skills:check`，
或把這份普查的母體不變量寫成 `packages/shared/src/ops/` 底下一條測試。
⛔ 在那之前，這份報告會過期而**沒有任何東西會紅**（失敗形態 ⑨：一個沒有人看它綠過的閘）。

### ⛔⛔ 而那條閘**不能**是 `anchors:check` —— 突變量過了

把 `shippedInputs.ts` 的 `median()` 改壞（上中位 → 下中位）再跑兩支閘：

| 閘 | 結果 |
|---|---|
| `anchors:check`（`tsx tools/balance-anchors/gen.ts --check`） | ⛔ **還是綠的**（exit 0） |
| 這份普查的 `--check` | ✅ **紅**（exit 1，指名報告過期） |

⭐ 為什麼：`balance-anchors.enabled:true` 之後，三份產物都是從**固定值**寫出來的 ——
量測只餵那一行 advisory，⛔ 而 advisory **不是產物**。
⇒ ⭐ **今天「母體變了」不會讓任何一份產物過期**，所以票上要的那條閘
⛔ 不可能由 `anchors:check` 兼任，⭐ 它得是**另一條**（這一支，或 `ops/` 底下同型的一條）。

⚠️ 同一次突變也證明了 `gen.ts` 真的在吃 `shippedInputs.ts`：改 `shippedBaseBonus()`
⇒ `anchors:check` 三份產物全部判過期（`balanceAnchorsDerived.ts` · `damage-tiers.json` · `平衡錨點量測.md`）。
