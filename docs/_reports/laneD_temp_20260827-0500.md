# 🛠 修復 lane D —— #758 · #774 · #757 · #795(Scope④) 明細（2026-08-27 05:00）

> 柵欄：`docs/**`（⛔ 不含 `_review/` `_daily/` `_release/`）· `packages/shared/src/ops/**`。
> ⛔ 全程沒有跑任何全域鎖指令（`skills:sync` / `content:build` / `ship:check` …）。

---

## ① #774 — `docs/kill-bounty.md` 的 SUPERSEDED 標頭有閘了 ✅ 關

**新檔** `packages/shared/src/ops/killBountyDocSuperseded.test.ts`（**79 行**，體驗層上限 80 ✅）。

它做的事只有一件：把標頭裡**每一組** `` `<key>`(<n>) `` / `` `<key>: <n>` `` 形式的金額宣稱
撈出來，逐項比對 `GOLD_REWARDS`（**import 進來的，⛔ 不抄字面值** —— 第〇·四守則）。

| | |
|---|---|
| 錨 | 標頭 = 檔案開頭那一段連續的 `>` 引言塊（#735 掛上去的那一段），⛔ 不做全文比對 |
| 夾具前提 | 撈不到 ≥2 條宣稱 ⇒ **紅**（⛔ 不讓它退化成永遠綠的空殼，失敗形態③） |
| 訊息 | 指名 `docs/kill-bounty.md:<行>` ＋「doc 說 X / 出貨是 Y」 |

**突變驗證（兩個方向都做了，⛔ 兩個都在我的柵欄內，⛔ 沒有動 `progression.ts`）：**

| 方向 | 做法 | 結果 |
|---|---|---|
| **doc 側** | `` `killBounty`(100) `` → `(150)` | 🔴 `docs/kill-bounty.md:15 說 GOLD_REWARDS.killBounty = 150，而出貨是 100` |
| **出貨常數側** | 在測試裡把 import 影子覆寫成 `{...GOLD_REWARDS, killBounty:150}` | 🔴 **兩行**（:15 與 :25）`說 …= 100，而出貨是 150` |

⇒ 兩側都真的被讀 —— ⛔ 不是把 100 硬寫在測試裡。兩次都用 `cp` 備份還原（⛔ 不用 `git checkout`）。

---

## ② #758 — `docs/_attribute-derivation-248.md` 的 superseded 標頭 ✅ 關

`git diff --numstat` = **56 插入 / 0 刪除** ⇒ AC4（§1–§7 的 w3x 派生係數一個字都沒被刪）✅。
`git status --porcelain content/` 空 ⇒ AC5 ✅。

三處插入：

1. **檔頭標頭**（第 3 行起，AC1「前 10 行內」✅）——逐點寫了 AC2 要的四件：
   ① 已被**五級距傷害公式體系**取代（`damage-tiers.json` · `KILL_CASTS_REF` · `echoloop:check`）
   ② ⛔ §0 的 14 列不要再拿去問 owner，附 **owner 2026-08-22 逐字原話**引用
   ③ 出貨 `critDamage` = **1.75**、**71/71 份**（今日實查：遞迴掃 71 份 champion doc，`{1.75: 71}`）
   ④ 114/111 分母已過期（今天 71 份）⇒ ⛔ 由它推導的百分比一律不可引用
   ⭐ 另加一張「**哪一部分仍然有效 / 哪一部分是失效的提案**」表 —— 這就是「知識另存不刪」那一半。
2. **§0 表格上方**的狀態註記：空白決策欄是**刻意的最終狀態**，⛔ 不是「還沒填的待辦」；
   第 13 列（攻速上限 2.5）⭐ **已由別的機制解決**（`stat-caps.json` 的 `as {base:4, unlocked:10}`，今日實查）。
3. **§6 標題下**的狀態註記：`critDamage` 今天仍是 1.75、71/71 一致 ⇒ 「歸零」**從沒發生**；分母 114 已過期。

⛔ 零測試（票自己指定，比照 #735）。⛔ 沒有動任何出貨數值。

---

## ③ #757 — ⛔ **跳過（整張票在柵欄外）**

票的 Scope 兩條出路都落在我碰不到的路徑上：

| 出路 | 要動的檔 | 我的柵欄 |
|---|---|---|
| A 拆掉 | `packages/shared/src/content/schema/ability.ts` · `packages/shared/src/content/fieldAdoption.test.ts` · `apps/client/src/ui/**`（5 檔）· `tools/w3x-import/w3xlib/wts.py` | ⛔ 全部不准碰（我只有 `packages/shared/src/ops/**` 與 `docs/**`） |
| B 餵它 | 同上 ＋ `tools/skill-remake` 來源 ＋ `pnpm skills:sync`（全域鎖） | ⛔ 同上，且 `skills:sync` 併行禁跑 |

⇒ ⛔ **沒有可以在柵欄內完成的子集**（連「選 A 還是 B」都要落地才算數）。票留開，已留言說明。

---

## ④ #795 Scope④ — 25 份報告的 HEAD 覆蓋率盤點

⭐ **權威數字用 `scanSequences()` 自己跑出來的，⛔ 不是我 grep 的**
（我第一版用 bash `grep -r` 撈，**BSD grep 沒有檔案參數時會去掃當前目錄** ⇒ 誤報兩份，
已改用 `node -e "import('./tools/review/features.mjs')"` 直接問那支函式）。

### ⛔ 票 body 的「2/25」在我動手之前其實是 **1/25**

`evidenceHeadOf()` 實跑：**25 個 sequence，只有 1 個**（`dragonslave_visual-proof_20260827-0030`
= `cdd8fe54 工作樹`）撈得到 HEAD。⇒ 票上那個 2 是散文，而它已經開始說謊（第三守則的形狀）。

### 補得了：**1 份**（覆蓋率 1/25 → **2/25**）

`docs/_reports/invprim_visual-proof_20260826-1700/frames.md` ← 加了一段 `HEAD=e44bf446`（**工作樹**）。

⭐ **它為什麼算「查證得到」而不是「編的」——兩個獨立出處：**

| 出處 | 內容 |
|---|---|
| ① **作者自己寫的** | 同目錄 `frames_reverify.md:41`：「**修後**（工作樹＝`e44bf446`）」 |
| ② **git 佐證** | `e44bf446` = 08-26 **13:53**；這批 PNG 的 mtime **14:10–14:37**；進版控於 `470cb1fd` 08-26 **14:49**。⭐ `e44bf446` 與 `470cb1fd` 之間**沒有第三個 commit** ⇒ 拍攝當下的 HEAD 只能是 `e44bf446` |

⚠️ ⭐ **順帶量到一個結構性的洞，寫進那份報告了**：#767 的 TeamGlow 修復當時**只在工作樹上**
（它到 `470cb1fd` 才進歷史）⇒ `evidenceOrder()` 只比 commit 的祖孫關係，
**結構上分不出「拍攝時修復已在工作樹」與「拍攝時根本還沒修」**。
⇒ `HEAD=<sha> 工作樹` 這個形狀**在最需要它的地方是二義的** —— 這是 #795 的下一個缺口。

### ⛔ 永遠判不了：**23 份**

⭐ **理由是同一個，⛔ 所以不逐份抄 23 遍**（`N 個同型 = K 個模板`）：
報告正文沒有任何作者寫下的 HEAD/commit，而 git **只給得出上界**
（「這批 PNG 進版控於 `<sha>`」）—— ⛔ 那**不是**拍攝時的 HEAD，因為擷圖幾乎都發生在
**工作樹**上（修復尚未 commit）。用上界回填 = 一個看起來精確、實際上可能把「判不了」
變成**錯誤結論**的標記 ⇒ ⛔ **那是捏造證據**（票的 Implementation constraints 逐字禁止）。

**⛔ 連 .md 都沒有（2）—— 沒有標題、沒有逐格表、沒有量尺自證紀錄：**

- `dragonslave_visual-proof_20260827`（進版控 `8bab9aa1` 08-27 01:02）
- `dragonslave_visual-proof_20260827-fixed`（同上）

**⛔ 有 .md 但撈不到 HEAD（21）：**

`beam_20260824-2240` · `beamtruth_20260826-1200` · `beamverify_20260826-1200` ·
`classic_avalon_20260825` · `classic_dragonslave_20260825` · `classic_endworld_20260825` ·
`classic_omnislash_20260825` · `crescent_20260825-2130` · `dagger_20260825` ·
`firering_20260825` · `goku_20260825-0200` · `kenshiro_20260827` · `mark_20260826-0100` ·
`o00e_20260825-1200` · `penta_20260825-2300` · `quad_20260825-2200` ·
`stockglow_20260826-1520` · `stuckescape_20260825` · `tail_20260826-0000` ·
`tornado_20260825-2100` · `weather_rain_20260825`

⚠️ `o00e_20260825-1200` 是**唯一**正文帶了合法 sha 的一份（`15f108e8`），⛔ 但那句話是
「#689（`15f108e8`）**落地之後補上**」—— 講的是**別人的修復**，⛔ 不是拍攝點。⇒ ⛔ 不採用。

### ⚠️ 另外兩個「看起來像證據包但不在佇列裡」

| | 為什麼看不到 |
|---|---|
| `docs/_reports/beam_visual-proof_20260824-2311.md` | ⛔ 是一個 **.md 檔**，⛔ 不是目錄 ⇒ `scanSequences` 只收目錄 |
| `docs/_reports/fxtint_visual-proof_20260825/` | ⛔ 目錄裡**零張 PNG** ⇒ `scanSequences` 的 `pngs.length === 0` 直接 skip |

⇒ 它們的檔名長得跟證據包一模一樣，而批核頁上**永遠不會出現**（第二守則失敗形態③的變體：
可以整包刪掉而沒有任何東西變紅）。

### ⛔ 為什麼 #795 **不能關**

Scope ③（`scripts/visual-proof.sh` 產出報告時自動蓋 `HEAD=<sha>`）落在 `scripts/` ——
**在我的柵欄外**。⇒ 票留開，只留言回報 Scope ④ 的結果。

---

## ⑤ 順手發現、⛔ 沒有當場修（第零守則⑧）

⭐ **`invprim_visual-proof_20260826-1700` 這一批登記的「修復 commit」是錯的。**

| | |
|---|---|
| 帳本登記 | `commit: e44bf446` —— 而那是一支 **ops** commit（「帳本寫入點自解鎖＋戰情表每日輪替閘」） |
| 真正的修復 | `470cb1fd` —— 「fix(vfx)(#767): TeamGlow 轉檔缺口修復＋rollback 可達性閘」 |

⇒ ⭐ **#795 那條祖孫比對是拿錯的錨在比** —— 錨錯了，「證據比修復早」這個判斷就整個不成立。
⛔ 修不了（`docs/_review/material/batches.json` 與 `tools/review/` 都在柵欄外）⇒ 已開票。
