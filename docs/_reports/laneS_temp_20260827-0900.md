# lane S — 前提查證（#325–#529，17 張）· 2026-08-27

> 任務：⛔ 不是「修票」，是查證每張票**主張的事實**還成不成立，該關的關掉。

## 結論：**關 0 張** · 留言 3 張（#327 · #473 · #502）

⚠️ **關 0 張是量出來的結果，⛔ 不是沒查。** 根因是一個 lane 開工前不知道的事實：

⭐ **這 17 張在過去 24–48 小時內全部已被逐張讀碼稽核過**
（16 張帶 `2026-08-26 全票稽核（#300–#799，100 張逐張讀碼）` 的留言，
#325/#401/#425/#441/#529 另有 08-26 晚間至 08-27 的 lane 留言）。
⇒ 「做完了沒」這一軸**已經被掃過**，而 17 張沒有一張的驗收條件是滿的。
⇒ 本 lane 的產出只可能在**另一軸**：票文主張的**現況**是不是假的。那一軸找到 3 筆。

---

## ① 三筆真的假前提（已留言）

### #473 —— ⛔ 「三支稽核只在人想到時手動跑」是**假的**

| 票文點名 | 實際住處 | 自動跑的時機 |
|---|---|---|
| `noOpModifierClaims` | `packages/shared/src/content/noOpModifierClaims.test.ts` | 普通 vitest ⇒ `pnpm test`（`package.json:16`）＋ `ship:check` 每一包 vitest（`tools/parallel-gates/ship.mjs:20`）|
| castability 普查 | `packages/shared/src/sim/castabilityVfxOnly.test.ts` | 同上 |
| 描述↔JSON 一致性 | `abilityProse.test.ts` ＋ `pnpm prose:check`（`tools/card-prose/apply_placeholders.ts --check`）| ⭐ 逐字在 `skills:check` 的 36 支裡（`package.json:100`）|

⭐ 順帶 `pnpm audit:check`（`tools/skill-audit/audit.py --check`，421 支三軸量尺）**也**在 `skills:check`。
⇒ 出貨前跑的稽核**比票文假設的多**，⛔ 不是少。

**對 AC 的影響**：票的經濟論證「不啟用就不花錢，啟用就一定驗」的**前半句不成立** ——
出貨內容每次 ship 閘就付過那筆錢了。⭐ 真缺口只剩**營運當下**（後台按白名單那一刻是 runtime 變更，
⛔ 不經過任何 ship 閘）⇒ **AC 第 1 條要重寫成「補 runtime 那一段」**。
08-26 指出的真結（判準只活在 `.test.ts`、無可程式呼叫匯出）仍是第一個動作，⛔ 但理由變了：
不是「讓稽核跑得起來」，是「讓 runtime 叫得到**同一個**函式」。

### #327 —— ⚠️ 缺口②「沒有任何東西在剝 HTML/CSS/URL/script」是**假的**

| 主張 | 量到的 |
|---|---|
| 「`[token]` tokenizer 不存在」 | ⚠️ **半真** —— `import/` 底下沒有，⭐ 但 `packages/shared/src/content/itemCardText.ts` 就是一支 allowlist tokenizer：`ItemCardToken = text｜tag｜value`（`:56-63`），`tag` 依內建 `ItemCardCategory` palette 著色 ⇒ 逐條對上計畫 §3.6；檔頭第 1 行「一個字都不改原文」＝ §3.6「不得寫回 authoring JSON」也成立 |
| 「沒有任何東西在剝 HTML/…」 | ⛔ **假** —— `dangerouslySetInnerHTML` 全 `apps/{client,admin,editor}/src` 命中 **0**；description 一律以 React text node 落地 ⇒ **結構上不可能注入標記**。三處 `.innerHTML` 全餵內建常數（`PadCursor.ts:52` `ARROW_SVG`／`RoundWinnerStage.ts:667,672`）；⭐ `WorldAnchorLayer.tsx:95` 註解記著同型的洞**已被關過一次** |

**對 AC 的影響**：②不是「做一支 tokenizer」而是「接線 ＋ 補 §3.6 五條守衛」
（`itemCardText.ts` 今天 `「`／`【`／`GLADIARIA`／`[直線]`／冪等**一條都沒有**）
⇒ ⭐ **三項裡最貴的其實是 ①（strict-by-default，19 格 passthrough ＋ 全量 strict 試跑）**，
⇒ 建議實作順序倒過來：②（便宜、且它是五條守衛的住處）→ ① → ③。
⚠️ 但書：「結構上不可能注入」只涵蓋**今天的**渲染端 ⇒ ②該有一條會紅的守衛
（description 不得到達任何 HTML sink），⛔ 不只是一支 tokenizer。

### #502 —— ⚠️ 08-26 分診的**三個理由裡一個已過期**

| 理由 | 今天 |
|---|---|
| ① #509 商品軌仍是裸 div | ⛔ 仍成立（`MerchantShop.tsx:1400-1401`，`data-pad-focusable`/`tabIndex`/`role` 命中 0）|
| ② 沒有端到端純手把走查紀錄 | ⛔ 仍成立 |
| ③ 手感層 #712 仍 OPEN | ⭐ **已過期 —— #712 今天 CLOSED** |

⭐ 十個根因子票 **9 關 1 開**（#503–#508 #510 #511 #512 全 CLOSED，只剩 #509）；
逐點票 #513–#524 ＋ #634 #652 #712 也全 CLOSED。
而 #509 自己兩個缺口**也只剩一個**（②ErrorToast 已由 #513 帶掉，守衛 `couchPadLane.test.ts:70-79`）。
⇒ epic 卡在 **一個 JSX 屬性 ＋ 一次走查紀錄**，⛔ 不是十個根因。

---

## ② 逐張查證明細（其餘 14 張：前提成立、⛔ 沒做完 ⇒ 不動）

| 票 | 票主張的事實 | 本 lane 實查 | 判定 |
|---|---|---|---|
| **#325** | schema 每側 ≥1 而消費端取 `[slot % TEAM_SIZE]` | ✅ AC1 已落（`schema/arena.ts:205-256` superRefine，import `TEAM_SIZE`；commit `a91c47c6`＋`aecf1bc5`）；⛔ AC2 未落 —— `MatchController.ts:2329` 與 `:2543` 的兩個 `!` 原封不動 | 前提成立 · 半完成 · **已有 2 則留言記錄** ⇒ 不動 |
| **#327** | ①19 passthrough ②無 tokenizer ③無 md5 政策 | ①✅ 實測 19 `.passthrough()` / 3 `.strict()`、`extensions` 0 筆 ③✅ `import/` 全 grep 無 md5 ②⚠️ **見上** | ①③ 成立 · ② 半假 ⇒ **留言** |
| **#330** | 未加點回饋不夠響 · 無自動加點開關 | ⛔ `autoSpend`/`autoLearnPoints`/`自動加點` 全 `content/config`＋`schema`＋`admin` 零命中 ⇒ 三個住處一個都沒有 | 前提成立 · 未做 ⇒ 不動 |
| **#382** | 「**6 個**場景 VRAM 超編」 | ⚠️ 今天 `baseline.json`（`updatedAt` 停在 2026-08-18）accepted 20 筆，其中 `scene:*/vramBytes` 是 **13 筆**（champ-select · castle · colosseum · dota · frieren · heavens-arena · infinity-castle · nazarick · shiganshina · skeleton · world-tree · intermission · settlement）⇒ 票文的 6 已翻倍 | 前提**低估**（08-26 已記「13 筆是真債」）⇒ 不重複留言 |
| **#401** | 「4 支 line-sweep」「⛔ 不需要新引擎工作」 | ⚠️ `grep -rl tpl-line-sweep content/abilities/` 只有 **2 支**（`godie-hgam.r`＝90-04 陽光烈焰 · `godie-n00b.q`＝57-01 空氣砲）；3 格 `inert` 仍在 `content/ability-templates/tpl-line-sweep.json`；`DELAYED_MAX_COUNT` 仍是 32 | 前提**數字錯 ＋「純內容變更」錯**（08-26 22:46 已逐條留言）⇒ 不重複 |
| **#423** | summon 引擎在、內容零 | ✅ `grep -rl '"kind": *"summon"' content/abilities/` ⇒ **0** | 前提成立 · 未做 ⇒ 不動 |
| **#425** | 9 份 wave 有 5 份與別的彈道共用 vfxKey | ⭐ **缺陷本體已修**（commit `b4d2f65a`）：9 份全部指 `fx.wave.*`，跨家族碰撞 0；守衛 `laneKWaveTrailFamily.test.ts` 3 條＋突變。⛔ 但 AC 明列「帶終端視覺證據」而 visual-proof 不存在 | 已修·**未驗收** ⇒ 依用詞紀律 ⛔ 不關（08-26 22:45 已留言） |
| **#441** | 2 位無戰鬥語音包 · 11 類無觸發點 · spatialPolicy 零引用 | 主項目✅仍成立（`combatVoiceCoverage.test.ts:112` `VOICE_GAP = ["godie-e00s","godie-ucrl"]`，兩者不在 `champions/MANIFEST.json` 的 51 位裡）；⚠️ 另兩項已假（`spatialPolicy.ts` 六列全 `dispatched:true` 且逐列帶 GH#441 理由；dormant 今天剩 **9** 列不是 11） | 主項目成立 ⇒ 不動（08-27 00:04 lane P 已記） |
| **#443** | 四層閘全未動 · 1305/1529 被版權擋 | ✅ 四層皆未動。⚠️ 順帶量到票文的解封估計**過樂觀**：`w3xAbilityArt.ts` 檔頭寫明閘是**三道**，filter#1 是 `IN_REPO_*`（地圖匯入），而「做真正工作的是 filter#3（每個 emitter 必須錨在 model root）」 ⇒ 解除版權理由**不會**把零售位元組放進 repo | 前提成立 · 未做 ⇒ 不動（WarStompCaster 已走 `extract_stock_vfx.py` 落地，08-26 已記） |
| **#448** | 引擎沒有「瞬移到帶標記的單位」 | ✅ `markedUnit` 全 repo（packages/apps/content）**零命中** | 前提成立 · 未做 ⇒ 不動 |
| **#452** | 13 張圖場地之外全黑 | ⚠️ 08-26 已更正為 9/13 有 GH#324 程序 backdrop、AC1 佔比已答；本 lane 覆核 `content/assets/arena-bg/` 三張 webp 仍**零引用** | 前提已被更正 ⇒ 不重複 |
| **#453** | 貼圖/物件/背景待跟進 · 需一份共同 token | ✅ `content/config/` 底下只有 `icon-style.json`，無共同 art token 檔；prompt.py 的 Fate PREFIX/NEGATIVE 確已落 | 前提成立 · 未做 ⇒ 不動 |
| **#457** | LoRA 零載入路徑 · 無 SDXL 類別 | ⭐ 票文**已被自己的後續留言推翻**且本 lane 覆核屬實：`local/pipeline.py:299 load_lora_weights` · `:302 set_adapters` · `:160-168`+`:326` 從 unet `cross_attention_dim` 判 SDXL；`icon-style.json:7 "loras": []`；構圖契約 `prompt.py:137-167`＋`CHARACTER_FAMILIES` | 前提已假但**已記錄兩次** · 剩量測/帳本/後台清單 ⇒ 不重複 |
| **#509** | 兩處裸 div onClick | ⛔ 缺口①仍在（`MerchantShop.tsx:1400-1401`，全檔 focusable 標記 0）；✅ 缺口②已由 #513 修（`LobbyScreen.tsx` 真 `<button>` ＋ `data-pad-back`） | 半完成 ⇒ 不動（08-26 已記） |
| **#529** | 89% 綁通用原型 · 110 份沒人用 | 08-26 19:57 已重量：`fx.prim.*` **346**（票文 342）· `fx.w3x.*` **25**（24）· 未引用 **101/126** ⇒ 落差**在擴大** | 前提成立且惡化 ⇒ 不動 |

---

## ③ ⭐ 最值得記的一個假前提

**#473：「三支稽核只在人想到時手動跑」** ——
三支**全部**已經是自動閘（兩支是 `packages/shared` 的普通 vitest ⇒ 進 `pnpm test` 與
`ship:check`；第三支的 `prose:check` 逐字在 `skills:check` 的 36 支裡）。

⭐ 這個假前提的形狀值得單獨記：**票文把「我沒有在 admin 裡看到它」寫成了「它沒有在跑」。**
兩者的差別是**執行環境**，⛔ 不是「有沒有做」——
而它會讓下一個實作者去**再做一次已經存在的東西**（把稽核自動化），
⛔ 而真正的缺口在另一個地方（runtime 的白名單切換不經過任何 ship 閘）。

⇒ 判準：票文寫「**現況是 X 沒有在跑**」時，要問的是「**在哪一個環境沒有在跑**」，
⛔ 不是「它存不存在」。

---

## ④ 一個給下一輪的方法論觀察

本 lane 的 17 張**全部**在 24–48 小時內被稽核過 ⇒ 「做完了沒」這一軸的邊際產出已經**趨近零**。
⭐ 而「前提真不真」這一軸在同一批票上仍找到 **3 筆**（#327 #473 #502），
其中 #473 與 #327 都**改變了 AC 本身**（一條要重寫、一條要調換實作順序）。

⇒ 建議：下一次全票掃描**不要再問「做完了沒」**，直接問
「**票文主張的現況，今天在哪一個環境下還成立？**」

---

*lane S · 唯讀 · ⛔ 零程式碼改動 · 寫入僅 3 則 `gh issue comment`*
