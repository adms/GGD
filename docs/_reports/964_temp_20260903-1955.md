# GH#964 · 驗收批6 **變身與鏡像**（3 份 ＋ 26 對）

量測日 2026-09-03 · 守衛 `packages/shared/src/ops/acceptanceN6.test.ts`（7 條，全綠）

---

## ⭐ 前提回驗：「26 對」今天**驗得到的只有 20 對**

`CHAMPION_FORM_PAIRS` **宣告** 26 對是對的（w3x `Eme1`/`Emeu`，票文沒說錯），
⛔ 但 `splitFormPairsByShipping()` 量到：

| | 數 | 說明 |
|---|---:|---|
| 宣告 | **26** | `championForms.ts` |
| ⭐ **出貨（驗得到）** | **20** | 兩半都在 `content/champions/` |
| 下架 | **6** | 兩半都在 `content/_legacy/`（owner 2026-08-13 的下架）—— 引擎看不到、玩家碰不到 |
| ⛔ 只搬一半 | **0** | 有的話會在每 tick 的 snapshot 裡擲例外 |

下架的 6 對：19 `godie-e00k` · 26 `godie-harf` · 40 `godie-nman` · 61 `godie-u012` ·
81 `godie-o01z` · 87 `godie-o02n`。

⇒ ⭐ 守衛**跑 `shipped`，⛔ 不寫死 26 也不寫死 20**：一位英雄重新上架，它自己長回去。
（斷言 `shipped + archived === 26` 把這個關係釘住。）

---

## ⭐ 逐份判定（3 份，⛔ 沒有一份空白）

### ① `godie-e001.r` — 22-04 雛見澤症候群L5 ⇒ **⛔ 阻塞於 #943 ＋ 一句卡面做不到**

| 卡面逐字 | 實作 | 判定 |
|---|---|---|
| 強化**攻擊 55 點** | `applyBuff{ad, flat, 55, duration 7}` | ✅ |
| 強化**移動速度** | ⭐ 由 `championForm → godie-e00n` 的**身體**給：`ms 5.8 → 7.7`（+33%） | ✅（在**變身態身體**那個住處） |
| 暫時**減少生命最大值 150 點** | ⛔ **零個節點** —— buff 沒有 `maxHealth` modifier，`godie-e00n.baseStats.maxHealth` 也是 **150（與本體相同）** | ⛔ **卡面說了但不會發生**（第一·五守則） |
| 結束後全部恢復，⛔ 不留永久屬性 | `championForm 7.0` ＝ `applyBuff.duration 7` ⇒ ⭐ **同時結束** | ✅（守衛 ⑤ 釘住） |
| 五級距 | `cooldownTier 中`（變身表 ⇒ **60s**，陣列 60 ✅）· ⛔ 無 `rangeTier`（self）· ⛔ 無 `conditionTier` | ⛔ **阻塞於 #943** |

⚠️ 「減少最大生命 150」在 GGD 的血量尺度下等於**歸零**（基礎 maxHealth 就是 150）
⇒ ⛔ 這不是「補一條 modifier」就結束的，它需要 owner 決定要換成什麼。
⭐ 三條出路照第一·五守則的順序：換等效機制 / 把描述改成只講真的會發生的 / 升級成 owner 的決定。

### ② `godie-h01n.r` — 79-04 卍解 ⇒ **⛔ 阻塞於 #943 ＋ ⭐ Main 的 primitive 缺口**

| 卡面逐字 | 實作 | 判定 |
|---|---|---|
| [攻擊速度] 提升 100/150/200% | `applyBuff{as, pctAdd}` `perRank 1.0/1.5/2.0`，`duration 8` | ✅ |
| ⭐ 只縮短**瞬步**冷卻 50% | `modifyCooldown{slot:"Q", mode:"reduce", amount:0.5}` —— ⭐ 而 `godie-h01n.q` 逐字就是 **79-01 瞬步 / slot Q** | ✅ 指對了那一格 |
| 持續 8 秒後**冷卻規則與外觀一起恢復** | 外觀 ✅（`championForm 8.0`）；⛔ **冷卻規則沒有「恢復」這回事** | ⛔ **做不出來** |
| 五級距 | `cooldownTier 大`（變身表 ⇒ **90s**，陣列 90 ✅）· ⛔ 無 `conditionTier` | ⛔ **阻塞於 #943** |

⭐⭐ **為什麼「做不出來」而不是「寫錯」**（逐行讀 `sim/effects/modifyCooldown.ts`）：

```
basis 預設 "remaining" ⇒ cut = round(cooldownRemainingTicks × 0.5)
inst.cooldownRemainingTicks = max(0, inst.cooldownRemainingTicks - cut)
```

⇒ 它是**一次性**的位移，⛔ **不是**一條「這 8 秒內 Q 的冷卻減半」的規則。兩個後果：

1. ⭐ 施放當下 **瞬步不在冷卻中 ⇒ 這一發一格都不會動**（reduce 50% of 0 = 0）。
2. ⭐ 8 秒後**沒有任何規則要被收回** —— 卡面的「持續 8 秒」對這一半不成立。

⇒ ⭐ 這是 **Main 的 primitive 缺口**（缺「限時的、逐槽位的冷卻規則」＝ 一格帶 duration 的
`cdr{slot}` modifier），⛔ **不是這份 JSON 寫錯** ——
照票文 Known risks：**建議開一張最小 primitive 票**（⛔ 本輪未開，等 owner 說「開票」）。

⚠️ 順帶抓到一句過期散文（第三守則）：`content/schema/effects/modifyCooldown.ts:67,197`
逐字寫著「**出貨 0 份 `modifyCooldown`**」—— ⭐ 今天出貨 **1 份**，就是這一支。

### ③ `godie-emfr.e` — 15-03 獄炎煉我 ⇒ **⛔ 阻塞於 #943**（機制本身 ✅ 全過）

| 票文要點 | 實作 | 判定 |
|---|---|---|
| 變身期間普攻附火傷 | `hook{on:"onBasicAttack"} → damage{magic, 極小, ap 0.4}` | ✅ |
| 技能命中產生爆炎並附燃燒 | `hook{on:"onDamageDealt", damageSource:"ability"} → damageArea + applyStatus{burn,3s}` | ✅ |
| 移速減半 | `applyBuff{ms, pctMult, -0.5, duration 12}` | ✅ |
| ⛔ **事件來源不可互相誤觸** | ⭐ **結構上分得開**：`hooks.ts:543` 給觸發器排出的效果打 `origin:"hook:<srcId>"`，而 `damageSource:"ability"` 走 `originInScope(origin,"ability")` ⇒ ⭐ **`hook:` 不在 `"ability"` 裡** ⇒ 普攻火傷叫不起爆炎、爆炎也叫不起自己 | ✅（守衛 ⑦ 兩頭都釘：JSON 的過濾 ＋ 引擎的 origin 標記） |
| [變身] 為唯一狀態不可疊加 | `exclusiveGroup:"emfr-form"` | ✅ |
| 五級距 | `cooldownTier 小`（變身表 ⇒ **45s**，陣列 45 ✅）· `manaCostTier 小` · `radiusTier 小` · ⛔ 無 `conditionTier` | ⛔ **阻塞於 #943** |

⚠️ 順手量到（⛔ 不在本批範圍，登記不修）：`damageArea` 同時寫了 `radius: 4.5` **和**
`radiusTier: "小"` —— 第〇·四守則說同一個節點不可以同時有級別與算好的值。
⭐ 全庫 **47 個節點 / 39 份文件**是這個形狀。

### ⭐ 共同規則 #4 的分母（⛔ 不是為了好看而放寬）

`conditionTier` 在 **421 份出貨技能裡覆蓋 0 份** ⇒ ⭐ **3/3 全部判「阻塞於 #943」**，
⛔ 這是真實現況，⛔ 不是這三份寫得不好。

---

## ⭐⭐ 鏡像逐欄比較（共同規則 #13，3 對 → **26 對宣告 / 20 對出貨**）

⭐ **兩頭都走**（CLAUDE.md 失敗形態⑫）。一條只從本體那一頭走的掃描
（`for (const code of base.keys())`）對「**變身態獨有**的編號」**結構上失明**，而且會一直是綠的。

| 軸 | 量到 | 守衛形狀 |
|---|---:|---|
| 出貨對子 / 技能編號 | 20 / **120** | 分母 |
| ⭐ **變身態獨有**（反方向） | **0** | ⭐ **硬零** —— ⛔ 只有反方向走得到 |
| 本體獨有（正方向） | **1**（70-002，紮根是 toggle 型） | 棘輪 ≤ 1 |
| ⭐ 兩邊都有而**機制欄位分歧**的編號 | **56 / 120（47%）** | 棘輪 ≤ 56 |
| ⭐ (編號, 欄位) 分歧筆數 | **217** | 棘輪 ≤ 217 |

**分歧欄位直方圖（前 10）**：`effects` 47 · `cooldownTier` 24 · `targetsEnemies` 22 ·
`castTimeSec` 18 · `template` 16 · `passive` 13 · `castType` 12 · `manaCost` 12 ·
`cooldown` 12 · `manaCostTier` 11。

⭐ **主導的形狀是一個**：本體被 owner 規格重寫過，**變身態停在 w3x 匯入值**。
量到的簽名很乾淨 —— `cooldownTier` 「中 vs 極大」出現 **11 次**，而 `極大` 正是匯入預設。

⚠️ ⭐ **而它直接踩到共同規則 #5（實際生效值）**，就在本批的兩個對子裡：

| 技能 | `cooldownTier` | 變身表解析 | 陣列寫 | |
|---|---|---:|---:|---|
| `godie-h01n.r`（本體） | 大 | **90** | 90 | ✅ |
| `godie-h01o.r`（變身態） | 極大 | **120** | **60** | ⛔ **差 2×** |
| `godie-e001.r`（本體） | 中 | **60** | 60 | ✅ |
| `godie-e00n.r`（變身態） | 極大 | **120** | **60** | ⛔ **差 2×** |

⇒ ⭐ 與 #953 規則 B 點名的 `godie-nbbc.r` **完全同一個形狀**，
⛔ 只是它住在鏡像的另一半，所以在此之前沒有任何東西問過它。

⭐ **為什麼這一格重要**：一位有變身的英雄在內容樹裡是**兩份完整的技能檔**。
改了本體、忘了變身態 ⇒ **玩家變身之後用的是舊的那一份，而全套測試會全綠**。

⛔ 守衛**不裁決**哪一邊是對的（第〇·六守則）——它只說「這兩份不同步了」。
⚠️ 而 #944 教過的那一課在這裡是**加倍**重要：join key（技能編號）自己漂掉時，
「照 key 同步」會把整支技能覆蓋掉 ⇒ ⛔ **這 217 筆不可以拿同步器一鍵抹平。**

---

## 🧪 守衛 `packages/shared/src/ops/acceptanceN6.test.ts`（7 條，一套治具）

| # | 斷言 | 形狀 |
|---|---|---|
| ① | 量尺自證：讀得到 >300 份技能 · 出貨對子 >0 · 只搬一半 = 0 · 出貨+下架 = 26 · ⭐ **兩個方向各校準一次**（造一組合成對子：變身態獨有的編號**要**被抓到；兩邊逐位元組相同時**不可**報漂移） | calibrate 兩方向 |
| ② | **變身態獨有**的技能編號 ⇒ **硬零** | 反方向 |
| ③ | **本體獨有**的技能編號 ⇒ 棘輪 ≤ 1 | 正方向 |
| ④ | 鏡像逐欄漂移 ⇒ 棘輪 ≤ 56 編號 / ≤ 217 筆，訊息列出最嚴重 6 筆 | 棘輪 |
| ⑤ | 變身期間的增減益要**與變身同時結束** ⇒ 棘輪 ≤ 3 | 棘輪 |
| ⑥ | `championForm` ＋ `modifyCooldown` 同時出現的 ⇒ 登記（今天 1 支，`godie-h01n.r`） | 登記 primitive 缺口 |
| ⑦ | 事件來源不可互相誤觸 ⇒ **硬零**，⭐ 並連引擎一起驗（`originInScope("hook:…","ability") === false` 且 `("ability:…") === true`） | 兩個名詞的關係 |

**⑤ 的既有 3 筆**（⛔ 可能是刻意的，所以是棘輪不是硬零）：
`godie-hgam.ex` buff 6 / form 18 · `godie-hjai.ex` buff 6 / form 20 · `godie-n003.ex` buff 6 / form 7。
⚠️ `hjai.ex` 那一筆的 6 秒對得上 #944 的 `recentCast{withinSec:6}` 增幅窗 ⇒ ⭐ 那個 6 是有出處的。

### ⛔ 這一支**不讀** `abilityCodeParityForms.baseline.json`

每一個數字都是**當場量的**（⛔ 沒有第二個住處要維護），
所以它與主 session 在那份基準線上的柵欄**完全獨立**。
「卡面說增幅而傷害節點逐位元組相同」由 `formAmplifyClaims.test.ts`（#944）管，⛔ 這裡不重寫第二份。

### ⛔ 本批的 3 個 id **沒有抄進測試**（第〇·四守則）

⭐ 四條規則都是**全庫掃描**，那 3 份是被**推導**進分母的：
`godie-e001.r` / `godie-h01n.r` 有 `championForm`（23 支之二）、
`godie-emfr.e` 有兩條會打人的觸發器（全庫只有 2 份，另一份是 `godie-emfr.r`）。

⚠️ ⭐ **順帶更正一個前提**：票文說 id 清單「只有一個住處（#953 定案的 #838 body）」——
⛔ 量到 **#838 的 body 今天查不到那三個 id**（`gh issue view 838 --json body` 全文 grep 零命中），
而機器讀的那一份 `docs/editor-contract/ggd-acceptance-eight.json` 只裝**八招**，⛔ 沒有 46 份清單。
⇒ ⭐ 46 份那一張表今天**沒有任何一個住處** —— 所以「不要抄進測試」是對的，
⛔ 而「去那個住處讀」今天做不到。本輪的解法是**從內容推導**，⛔ 不新開第二個住處。

### 🧬 突變紀錄（實跑，`scripts/edit-or-die.py`，改壞 → 🔴 → 還原）

```
M1  content/abilities/godie-h01n.r.json  "durationSec": 8.0 → 6.0
    ⇒ 🔴 ⑤「變身期間的增減益」expected 4 to be less than or equal to 3
    ⇒ 訊息逐字指名：godie-h01n.r buff=8/8/8 form=6/6/6
    還原後 git status 乾淨（逐位元組回到 HEAD）
```

---

## 📋 收尾

- `npx vitest run packages/shared/src/ops/acceptanceN6.test.ts` ⇒ **7 passed**
- `packages/shared` tsc：本檔 **0 error**。
  ⚠️ 該套件仍有 **5 個 error，全部在 `src/ops/acceptanceN3.test.ts`**（另一條並行 lane 的未追蹤檔，⛔ 不是本輪的）。
- ⛔ 全程沒跑 `pnpm skills:sync`（全域鎖）· ⛔ 沒動 `VfxSystem.ts` · ⛔ 沒動
  `abilityCodeParityForms.baseline.json` · ⛔ 沒開分支 · ⛔ 沒動 `owner-knobs.json` · ⛔ 沒碰 #838。
- ⛔ `content/` **一個位元組都沒有留下改動**（突變已還原）。

### ⭐ 建議開票（⛔ 本輪未開，排序是 owner 的權力）

1. **[最小 primitive]** 限時的逐槽位冷卻規則（帶 `duration` 的 `cdr{slot}` modifier）——
   ⭐ 沒有它，79-04 卍解的「[瞬步] 冷卻縮短 50%，持續 8 秒」**寫不出來**。
   ⛔ 那張票**不可以**要求 Main 幫忙拼完整技能（owner 的角色分工）。
2. **[fix]** `godie-e001.r` 卡面的「減少生命最大值 150 點」是一句做不到的宣稱（第一·五守則）。
3. **[fix]** 鏡像的 **217 筆**機制欄位分歧 —— ⚠️ ⭐ **不可以拿同步器一鍵抹平**（#944 的教訓）。
4. **[docs]** `content/schema/effects/modifyCooldown.ts` 的「出貨 0 份 `modifyCooldown`」已過期。
5. **[docs]** 46 份驗收清單今天**沒有住處** ⇒ #953 的 Scope 第 1 條沒有落地。
