# GH#1041 免疫控場擋不住【致盲】與【詛咒】—— lane 報告（2026-09-06 15:36）

> 併行 lane，⛔ 未 commit（主 session 統一收）。柵欄：`packages/shared/src/sim/effects/applyStatus.ts`
>（`isCc` 真正的住處，⛔ 不是票文寫的 `invulnerable.ts`）· 新守衛 · `docs/_reports/`。

## 量到的基線（動手之前，⛔ 不是複述票文）

- `isCc` 住在 `packages/shared/src/sim/effects/applyStatus.ts:64`（`grep -rn isCc packages/shared/src/sim`），
  讀 `stun / root / feared / disarmed / moveSpeedMult<1` 五格。
- **出貨 48 份 `content/status-effects/*.json` 沒有一份帶 `missChance`** —— 那個數字住在**施加它的 applyStatus 節點**上。
  ⇒ 「帶 missChance 的狀態」的母體是 applyStatus 站點，⛔ 不是 status 文件。
- 通用走樹（⛔ 不列子鍵名單）量到 **12 個站點**（abilities 10 · items 2；champions 是 abilities 的鏡射，不重複算）：
  `godie-efur.q`(blind) · `godie-emns.q`(curse) · `godie-h02k.passive/.q×4/.w`(blind/curse) ·
  `godie-h02v.passive/.r`(blind) · `ultimate-mod-shiranui`×2(blind/curse)。0 個 `applyTo:"self"`。
- 修之前跑新守衛：**3/3 紅**，逐字 —
  `免控中仍被 curse 弄到失手: expected 0.5 to be +0` · `免控中仍被 blind 弄到失手: expected 0.5 to be +0` ·
  `⛔ 這 12/12 處帶 missChance 的狀態穿過了免控（isCc 不認得它們）：` ＋ 12 行站點（AC #6 ✅）。
- ⭐ 順帶量到：`blind.json` / `curse.json` 的 `tags` **早就**含 `cc` 與 `soft-cc` —— 資料兩邊都說它們是控場，只有 `isCc` 沒讀。

## 做了什麼

| 檔 | 改動 |
|---|---|
| `packages/shared/src/sim/effects/applyStatus.ts` | `isCc` 加一句 `(missChance !== undefined && missChance > 0)`（+11/−2，含註解）。⛔ 沒有 `"blind"`/`"curse"` 字串比對（AC #3）。`> 0` 而非 `!== undefined`：`missChance: 0` 的文件沒拿走任何東西（同 `condition.ts` 的 `miss` tag 判準）。同檔 :113 那句「而那一行漏掉詛咒與致盲」改成「GH#1041 之前漏掉…2026-09-06 補上」 |
| `packages/shared/src/sim/effects/ccImmunityCoversMissChance.test.ts`（新，157 行） | ① 承重：出貨 44-01 死神之眼（頂層 applyStatus→curse）與 92-04 馬勒戈壁（`damageArea.onHitTargets`→blind），免疫用出貨 74-01 獄門的 `invulnerable{blocksControl:true}` 由受害者施給自己；兩個方向：沒免疫 `missChanceOf>0`、免疫 `missChanceOf===0` 且 `immuneControl` 帶那個 statusId。② 閘：通用走 `Abilities/Items/Augments.all()` 整棵 JSON，任何 `applyStatus{missChance>0}` 節點剝掉 `condition` 後打到免控身體上，掛得上就紅並指名（含站點路徑）。內建 sentinel：掃到 0 站點 ⇒ 紅（閘不能是空的） |

⚠️ 副作用（刻意、與繳械同構）：致盲／詛咒的秒數現在也記進 `ccAppliedTicks` 戰績（`recordCc` 在同一個 `isCc` 上）。

## 測試

| 次 | 指令 | 結果 |
|---|---|---|
| 1 | `npx vitest run ccImmunityCoversMissChance.test.ts`（修之前） | ⛔ 3/3 紅，指名 12 站點（見上） |
| 2 | 同上 ＋ `invulnerable.test.ts` ＋ `conditionDerivedStatusTag.test.ts` ＋ `statusTagImmunity.test.ts` ＋ `sim/purity.test.ts`（一個指令） | ✅ 5 檔 52/52 綠 —— 暈眩／纏繞／恐懼／繳械／減速的既有免疫行為零改變（AC #4） |
| 3 | 突變：`scripts/edit-or-die.py` 把 `(missChance !== undefined && missChance > 0)` 換成 `false` | ⛔ 3/3 紅，訊息與修之前逐字相同（12/12）。改回來用 `Edit`，⛔ 沒 `git checkout` |
| tsc | `tsc --noEmit -p packages/shared`（跑兩次：第一次紅在我自己的測試檔 —— `store.add` 的 collection 要 `as const`；修完第二次 0 錯） | ✅ EXIT=0 |

⚠️ 測試行數 157 vs 實作 11：超過「靈魂層測試 ≤ 實作」的比。理由：票的 Test criteria 明點要「承重守衛（兩方向）＋會紅的閘」兩件事；
守衛是一檔三個 `it`，沒有第二條角度。若主 session 要砍，先砍檔頭註解（17 行），⛔ 不砍閘。

## 決策點 —— 我判斷**不是**要留開關的那一種，理由寫在這裡讓主 session 可以反駁

- owner 2026-08-24 逐字：「殭屍王**免疫負面狀態** 包含**暈眩 緩慢 詛咒 致盲**」—— 他自己把詛咒／致盲跟暈眩／緩慢放在同一列；
  出貨 `blind.json` / `curse.json` 的 `tags` 也都含 `cc`。⇒ 這是「`isCc` 沒讀資料」的缺陷，⛔ 不是我挑的設計。
- 票是 owner 說「開票」之後開的，修法（`missChance>0` ⇒ CC）是票文指定的 ⇒ 沒有一格是「我挑的」。
- 若主 session 仍要 rollback 開關：它要落三個住處而全部在柵欄外 —— `sim/combatFeel.ts`（新 `controlImmunity.blocksMissChance`, DEFAULT true）·
  `content/schema/config/combatFeel.ts` · `content/config/combat-feel.json` · `apps/admin/src/combatFeel.ts` + `ui/CombatFeelPage.tsx`。
  在 `applyStatus.ts` 只要把那一句改成 `(world.combatFeel.controlImmunity?.blocksMissChance ?? true) && missChance > 0`。

## ⛔ 柵欄外、需主 session 套的改動（AC #5 在這裡）

1. **`packages/shared/src/sim/statusTagImmunity.ts:15`**（票文 AC #5，柵欄禁我動）—— 那一格現在是過期散文。建議改成：
   `⛔ 它是**一段時間**的授予（durationSec 必填），而 owner 要的是這具身體的**常駐身分**；（它的 isCc 判準在 GH#1041 之前漏掉詛咒與致盲 —— 2026-09-06 起 missChance>0 也算 CC，見 effects/applyStatus.ts）`
2. **`packages/shared/src/sim/stats/modifiers.ts:1152`** —— 同一句過期散文的第三個住處（票文沒點名）：
   「而且它的 `isCc` 判準漏掉詛咒與致盲（那兩支走 `missChance`）」⇒ 刪掉那半句或改成「（GH#1041 之後已涵蓋 missChance）」。
3. **`packages/shared/src/sim/content/condition.ts` `STATUS_FIELD_TAGS.missChance`** ＋ **`conditionDerivedStatusTag.test.ts` `CC_PROBES`**（⭐ 兩個要一起改，改一個那條測試就紅）：
   推導表的 `cc` 家族今天沒有 `missChance` 這一列，而檔頭逐字說「`cc` 的成員必須跟 isCc 一致」——現在是單向一致（cc ⊆ isCc）。
   加 `{ tag: "cc", when: (s) => s.missChance !== undefined && s.missChance > DERIVED_NO_MISS_CHANCE }` 到 `missChance:` 那組，並在 `CC_PROBES` 加 `{ label: "miss", patch: { missChance: 0.5 } }`。
   ⚠️ 這會讓 `condition.status@1{tag:"cc"}` 的條件葉對致盲／詛咒的目標開始回 true（`blind.json` 文件 tag 本來就含 cc，所以文件那一路早就 true；差別只在**旗標推導**那一路）。

## 給玩家的一句

免疫控場現在也擋得住致盲與詛咒（被擋時畫面上會出現「免疫」）。
