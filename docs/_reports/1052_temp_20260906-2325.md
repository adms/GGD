# GH#1052 —— V 鍵通訊輪盤的終端證據（lane 報告，2026-09-06 23:25）

> 上一輪（`1051-1053_temp_20260906-1756.md`）把 `resolveUiCues()` 掉在地上的 `commsWheel` 補回來，標記停在「鏈路已接上未驗收」。
> 這一輪只做**證據**，⛔ 沒有再接一次線、⛔ 沒動 `commsWheel.ts`／`uiCues.ts`／`commsWheelRunner.ts`（突變改壞→改回，diff 空）。
> ⛔ 未 commit（主 session 統一）。

## 做了什麼

| 檔 | 動作 |
|---|---|
| `apps/client/src/game/commsWheelVisualProof.test.ts`（**新**，141 行） | `@visual-proof` · jsdom。出貨 `ui-cues.json` → `Configs` → `uiCues()`/`resolveUiCues` → `commsWheelRunner` → **出貨 `InputCapture`** 收真的 `pointermove`／`keydown KeyV` → `recordCommsWheel` → `hudStore` → **出貨 `<CommsWheelOverlay/>`** → DOM。① 出貨 config：輪盤釘在指標那一點、五格出貨文字逐字、字級/顏色/底色不透明、祖先鏈沒有 display:none／alpha 0、五格在 5 個相異座標、指向第 0 格會亮框、放開就收。② `enabled:false` ⇒ 同一下 V **量不到**；翻回 ⇒ 又量得到 |
| `docs/_reports/1052_visual-proof_20260906-2325.md`（**新**） | 終端證據：台子、逐段鏈路（哪一段是出貨的）、兩個方向、看到什麼／關掉時看到什麼、誠實界線。檔頭帶 `HEAD=1de2bd31 工作樹`（GH#795 格式，`evidenceHeadOf()` 撈得到） |
| 本檔 | lane 報告 |

## 最重要的一個發現

**證據不需要 WebGL**：輪盤是 React HUD（`CommsWheelOverlay.tsx`），玩家看到的終端就是那棵 DOM，repo 既有的同型台子是 `ui/floatingTextRenders.test.ts`（GH#701）。所以「沒有截圖 harness」⛔ 不是這張票的阻礙 —— 阻礙只在「有沒有人把出貨的 `InputCapture` 與出貨的 overlay 接在同一支測試裡」。接上之後，#1043 那個「三個住處齊全但沒人讀」的洞從畫面那一端也量得到（`enabled:false` ⇒ 零節點）。

## 量到的 / 跑過的

| | |
|---|---|
| vitest run ① | `commsWheelVisualProof.test.ts` 2/2 綠，436ms |
| vitest run ②（突變） | `commsWheelRunner.ts:38` `uiCues().commsWheel ?? FALLBACK` → 只回 `FALLBACK`（`edit-or-die.py` 改壞→跑→改回，同一個 Bash）⇒ ① 紅「⛔ 按下 V 之後畫面上沒有輪盤 —— 鏈路某一段是斷的」、② 紅「翻回 enabled=true 之後輪盤沒有回來」。`git diff --stat commsWheelRunner.ts` 空 |
| `bash scripts/visual-proof.sh` | 綠（它只看 `vfx/render`；見順手發現） |
| `pnpm typecheck`（1 次） | **EXIT=1，⛔ 不是我的**：`apps/client typecheck: Done`；紅在 `apps/editor/src/forge/ConditionEditor.tsx:792/:867`（`"form"` 不在 `ClauseKind`、`FormLeaf` 不是 `StatLeaf`）—— 來源是另一條 lane 工作樹裡的 `packages/shared/src/content/schema/condition.ts`／`sim/content/condition.ts`（新加 `FormLeaf`，editor 端還沒跟上）。⛔ 沒動 |
| 實作／測試行數 | 實作 0（證據票）／測試 141（含 20 行檔頭）—— 超過體驗層 80 行；理由：終端證據那一族，兩個方向＋指向＋收起各一段，沒有重複斷言 |

## ⛔ 沒做到的 / 誠實聲明

- jsdom **不 raster**：字體、遮擋、品質階梯、與 canvas 的 z 序驗不到。這是 DOM 級證據，⛔ 不是像素級；對 React HUD 這是它的終端，對 WebGL 才需要 audition。
- 台子**沒接**的兩行：`GameApp.ts:1102` 的 `...this.comms.inputDeps` 攤進 `InputCapture`、`HudRoot.tsx:222` 把 overlay 掛進 HUD 樹 —— 測試直接用同一個 runner 物件／同一個元件，⛔ 沒起 `GameApp`／`HudRoot` 本人。兩行都是一眼看得到的字面接線。
- 語音（`playContextualVoice`）刻意不驗（`champId` 給 null）—— 那是 GH#731 的事。

## 需要主 session 動的（柵欄外）

1. **commit**（逐檔 pathspec）：`apps/client/src/game/commsWheelVisualProof.test.ts` · `docs/_reports/1052_visual-proof_20260906-2325.md` · `docs/_reports/1052_temp_20260906-2325.md`；然後 `ticket-progress.sh write 1052 --commit <sha>` 補 sha。
2. 上一輪三檔（`uiCues.ts`／`uiCues.test.ts`／exemptions）仍在工作樹 —— 這份證據的 HEAD 行寫了「工作樹」，commit 之後證據與修復才在同一個祖先鏈上。
3. #1052 可關（AC：後台那格到得了畫面 ✅ 守衛；玩家看得到 ✅ 本證據）。

## 順手發現但沒動

- **GH#1083**（新開）：`scripts/visual-proof.sh` v2 的空殼標記 liar-scan 只 `find apps/client/src/vfx apps/client/src/render` ⇒ `ui/`／`game/` 底下的 `@visual-proof` 測試不在分母裡（形態⑫）。本檔含 `alpha` 詞彙 3 處，所以就算掃到也綠。
- `pnpm typecheck` 的 editor 紅（上表）—— 另一條 lane 的 in-flight，它們自己會收；⛔ 沒開票。
