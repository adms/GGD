# GH#982 ＋ GH#1031 —— `regression` job 的 `todo:runtime` 紅 ／ docs/todo 零信標 26 列（2026-09-06）

> lane 柵欄：`docs/todo/*.md` · 補 `cover()` 的既有測試檔 · `todoDoneBeaconsHaveEmitters.test.ts`。
> ⛔ 本 lane 沒有 commit／sync／genrun；`.github/workflows/ci.yml` **沒有動**（見 §5，artifact 合併是對的）。

## 1. 基線（動手之前量到的，⛔ 不是票文的 61）

| 量到的 | 值 | 出處 |
|---|---|---|
| `regression` job 在 main 上的狀態 | 最近 8 次 run 裡 **7 次 `skipped`**（`if:` 要 `unit`＋`go-platform`＋`vuln` 全綠，而 `unit` 一直紅）| `gh run view … --json jobs` |
| 唯一真的跑過的一次 | run **33985736831**（2026-09-05 19:31Z，`unit` 綠）→ `TODO runtime gate` **紅 31 列**、2975 beacons、1590 orphan | 該 job 的 log |
| 31 列的組成 | **26** 列在原始碼裡零個 `cover()`（`todoDoneBeaconsHaveEmitters` 的 `KNOWN_UNEMITTED`）＋ **5** 列有發射器但 CI 沒跑到 | 逐列比對 |
| 本機 `todoDoneBeaconsHaveEmitters.test.ts` | 綠（26 列住在帳本裡）| `npx vitest run`（1/3）|

⇒ #982 的前提「`regression` 紅」要更正成：**它幾乎從來沒機會跑；跑過的那一次紅 31 列，⛔ 不是 61**。

## 2. 26 列的分類與修法（三種病三種藥）

| 病 | 列數 | 藥 |
|---|---:|---|
| ① 一格多個 id（`a / b`、`a, b`）| 5 | 拆列。⚠️ **量到票文沒說的事**：其中 3 個 id（`juice-shake` · `client-model-normalized` · `client-standin-override`）本來就是**別列**的 Test ID —— 多 id 的格子躲過了 `todo:check` 的唯一性檢查。拆出來的列各自拿到**新 id**＋斷言自己那句話的守衛 |
| ② 守衛在，只是沒發射 | 16 | 補 `cover("<id>")`（⛔ 每一條都先讀過它斷言的是不是那一列說的事）|
| ② 守衛在，但發的是別的名字 | 2 | 行文描述的形狀已被取代 ⇒ Test ID 改指到真的在發射的守衛，原句以 ~~刪除線~~ 留史 |
| ③ 守衛不存在／需求已被取代 | 3 | 不再是 `done`，理由寫在列上 |

### 逐列

| 檔:行 | ID | 原 Test ID | 病 | 做了什麼 |
|---|---|---|:-:|---|
| ability-vfx.md:351 | av-18 | vfx-census-status | ② | `vfxCensus.test.ts` 六個 status `it` 補 beacon |
| ability-vfx.md:352 | av-19 | vfx-census-trueport | ② | 同檔 TRUE-PORT 單元 case ＋ 出貨表「every promoted reads TRUE-PORT」「no MIS-BOUND」（beacon 放在 `if (!provenance) return;` **之後**）|
| ability-vfx.md:353 | av-20 | vfx-census-extras | ② | 同檔 ledger 三條 |
| ability-vfx.md:354 | av-21 | vfx-census-ownernotes | ② | 同檔 owner-note 那一條 |
| audio.md:62 | au-20 | audio-fx-pcm-wav → **audio-fx-mp3-ceiling** | ②改指 | fx 音檔在 task #158 就地轉成 MP3（`audioAssets.test.ts` 檔頭逐字寫著），PCM WAV 這句從那天起是假的 |
| client-hud.md:39 | client-28 | hud-desc-role-colour | ③ | **withdrawn**（GH#757：`descriptionRoles` 是 `superseded`，餵它會把 `{{cd}}` 印上 338 張卡；知識另存 `docs/legacy/_semantic-role-markup-superseded.md`）|
| combat-juice.md:174 | cj-c17 | vfx-spark-nonfinite | ② | `VfxSystem.impact.test.ts` 兩條 non-finite `it` |
| combat-juice.md:175 | cj-c18 | juice-shake / juice-shake-directional | ① | 拆成 cj-c18 **`juice-shake-crisp`**（`combatFeedback.test.ts` CRISP 兩條）＋ cj-c18d `juice-shake-directional`（已有發射）|
| economy.md:79 | stat-02 | econ-stat-roll-parity | ③ | **withdrawn**（#260 把九格隨機池換成 力/敏/智 三選一，`statPath.ts` 已無那個池）|
| economy.md:124 | aug-pool-01 | eco-augment-pool-variety | ② | `draft.test.ts`「never under-fill」＋ `loader.test.ts` 池全數註冊 |
| economy.md:125 | aug-pool-02 | eco-augment-all-fire | ② | `noOpModifierClaims.test.ts`（無結構性無效 modifier／假狀態）＋ `loader.test.ts`（嚴格 schema）|
| guardian.md:43 | guard-wired | guardian-match-wired | ② | `roundPacing.test.ts` guardian 三條（列自己就指名這個檔）|
| models.md:69 | mdl-150b | client-model-normalized / client-standin-override | ① | 拆成 mdl-150b **`client-model-relative-scale`**（`ChampionView.test.ts`「DELIBERATELY smaller」＋ `standInOverridesWiring.test.ts` 小叮噹/張飛）＋ mdl-150b-override **`client-standin-relative-scale`**（`EntityViewRegistry.test.ts` relativeScaleOf 預設 1.0 ＋ wiring 三條）|
| models.md:70 | mdl-150c | client-model-degenerate-fallback | ② | `ChampionView.test.ts` degenerate 那一條 |
| models.md:82/83 | mdl-73-01/02 | model-herohehi-… / model-linkstik-… | ② | `modelGeosetAlpha.test.ts` 各自的 describe（3＋4 條）|
| models.md:85 | mdl-68-01 | model-orientation-noop → **model-facing-measured** | ②改指 | 稽核結論「0 outliers」被 GH#216 推翻（linkstik 量到 180°）；活的守衛是 `modelFacing.test.ts` 逐顆重量 |
| models.md:100/101 | mdl-162 / -sweep | model-idle-grounded / -sweep | ② | `modelIdleGrounding.test.ts`（5 條 ＋ 全名單 sweep 51 顆）|
| restart-cheats.md:55 | rc-31 | cheat-practice-tabs | ② | `practiceMount.test.ts` 最後一條（列自己指名）|
| restart-cheats.md:56 | rc-32 | cheat-practice-server-gate | ② | `practiceCheatGate.test.ts` 三條（列自己指名）|
| security-infra.md:34/38/42 | sec-infra-05/11/07 | 三個 `a, b` | ① | 各拆成 -05b / -11b / -07b，每個 id 對到 `private_deploy_test.go` / `approval_console_test.go` 既有的 `Cover` |
| security.md:52 | sec-154-19 | sec-154-jwt-aud-iss | ② | `token_purpose_test.go` 的 stamped / refused 兩條（後者逐字刪 aud/iss、換錯 aud/iss ⇒ 拒）|
| w3x-tint-263.md:143 | t263-07 | tint263-capture | ③ | **deferred**：截圖 diff 是瀏覽器 e2e，CI `e2e` 是 `if: false`，`docs/_reports/` 也沒有它的 visual-proof（只有 fxtint 的）—— 這列一直在沒證據的情況下讀成 done |

## 3. 5 列「有發射器但 CI 沒跑到」（#982 的第三種病；GH#1031 明寫另開票，⛔ 本 lane 沒修）

| 列 | 發射器 | 量到的原因 |
|---|---|---|
| client-hud.md:36 `build-stamp-env` | `apps/client/src/buildstamp/buildStamp.test.ts` ×9，**沒有 skip 閘** | 09-05 那次 artifact 裡沒有它；原因**這一輪沒定案**（那個時期 apps/client 有 GH#1014 的 worker RPC 逾時殺檔）。下一次 `unit` 綠再量 |
| icons.md:176–179 `icon-gen-*` ×4 | `tools/icon-gen/test/icon-gen.test.ts` | ⭐ **`tools/icon-gen/` 沒有 `package.json`** ⇒ 不是 pnpm workspace 成員 ⇒ `pnpm -r test` **從來沒有任何 runner 跑過它**。⛔ 不是環境閘，是**沒有 runner** |

## 4. 驗證（vitest 3/3 用完，tsc 1 次；⛔ 體驗層不做突變 —— 票文明寫）

| 什麼 | 結果 |
|---|---|
| `pnpm todo:check`（static）| 第一次 **紅 3 條 duplicate Test ID** ⇒ 抓到 §2 那三個借來的 id ⇒ 改新 id 後綠：1514 items（done 1413 · withdrawn 2→4 · deferred 31→32）|
| `packages/shared` 6 檔（含帳本閘）| **94/94 綠**；`GGD_COVERAGE_FILE` 真的發出：herohehi ×3 · linkstik ×4 · idle-grounded ×5 · sweep ×51 · eco-augment-pool ×2 · all-fire ×3 |
| `apps/client` 7 檔 | **134/134 綠**；發出：vfx-census-status ×6 · trueport ×3 · extras ×3 · ownernotes ×1 · spark-nonfinite ×2 · degenerate ×1 · cheat-practice-tabs ×1 · juice-shake-crisp ×2 · relative-scale ×2 · standin-relative ×4 |
| `apps/platform` `go test -run` 兩條 | ok；`sec-154-jwt-aud-iss` ×2 |
| `tsc --noEmit -p apps/game-server` | EXIT 0（`practiceCheatGate` 的新 import 解析得到）|
| gofmt | 乾淨 |
| ⭐ 合併重量：CI 09-05 的 artifact（7825 行）＋本機三份 ⇒ `pnpm todo:runtime` | **31 → 9 列**：5 環境閘住（§3）＋ 2 game-server（`guardian-match-wired` · `cheat-practice-server-gate`：beacon 已放、**本機沒跑那兩個檔**，額度用完）＋ 2 `auth-first-owner-*`（auth.md:54-55，別的 lane 09-05 之後才加的列，發射器存在、帳本閘綠，只是不在舊 artifact）|

## 5. `.github/workflows/ci.yml` —— 沒有要改的

`regression` 的 `download-artifact pattern: coverage-*` ＋ `find … -exec cat` 是對的：09-05 那次合併集裡 Go 的 435 條都在（`coverage-platform.ndjson`）。#982 猜的「Go beacon 沒合併」**不成立**。

## 6. 需要主 session 套的柵欄外改動

1. **`tools/icon-gen/package.json`（新檔）** —— 讓那 4 列有 runner：
   `{ "name": "@ggd/icon-gen", "private": true, "type": "module", "scripts": { "test": "vitest run" }, "devDependencies": { "vitest": "<跟其他包同版>" } }`
   （或把 `tools/icon-gen/test/icon-gen.test.ts` 搬進某個既有 package）。加完 `pnpm install` 會動 lockfile。
2. **branch protection（#983）**：`regression` 在那 5 列清掉之前**暫不 required**（票文第 3 條；一格可回頭的設定）。
3. 收工後 `pnpm skills:sync --check` 那一族不受本 lane 影響（只動了測試檔與 docs/todo）。

## 7. 改了哪些檔（23，＋86 −43）

`apps/client/src/ui/assets/vfxCensus.test.ts` · `apps/client/src/vfx/VfxSystem.impact.test.ts` · `apps/client/src/render/views/ChampionView.test.ts` · `apps/client/src/ui/practiceMount.test.ts` · `apps/client/src/render/combatFeedback.test.ts` · `apps/client/src/render/standInOverridesWiring.test.ts` · `apps/client/src/render/EntityViewRegistry.test.ts` · `apps/game-server/src/match/roundPacing.test.ts` · `apps/game-server/src/match/practiceCheatGate.test.ts` · `apps/platform/internal/auth/token_purpose_test.go` · `packages/shared/src/content/modelGeosetAlpha.test.ts` · `packages/shared/src/content/modelIdleGrounding.test.ts` · `packages/shared/src/content/loader.test.ts` · `packages/shared/src/content/noOpModifierClaims.test.ts` · `packages/shared/src/sim/economy/draft.test.ts` · `packages/shared/src/ops/todoDoneBeaconsHaveEmitters.test.ts`（帳本清空）· `docs/todo/{combat-juice,models,security-infra,audio,client-hud,economy,w3x-tint-263}.md`

## 8. 下一個人從哪裡接

- 等下一次 `unit` 綠的 main run：`regression` 應只剩 §3 的 5 列（＋若 game-server 兩條沒發出就是 7 —— 那代表 roundPacing/practiceCheatGate 在 CI 沒跑到，去查 vitest 裁決帳）。
- 那 5 列：另開票（icon-gen 是 runner 缺席，build-stamp 要先重量）。
- #982 AC①「本機 `todo:runtime` EXIT 0」**沒達到**（5 列還在，而且本機沒跑全套）；AC② 等 CI。
