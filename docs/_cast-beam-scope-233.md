# #233 向天光束 — 可以幫到哪些技能，幫不到哪些

> 這份表由 `apps/client/src/vfx/castBeamScope.test.ts` 每次跑測試時從
> `content/abilities/*.json` 重新推導，**不是手抄的**。改了施法前搖，這裡就會變。

反應預算 = 施法時間 − 光柱自己的淡入(14%) − `INTERP_DELAY_MS`(66 ms) − 人類反應(250 ms) − 一個 tick(33 ms)

| 施法前搖 | 技能數 | 反應預算 | 判定 | 光束會做什麼 |
|---|---:|---:|---|---|
| 3.0 s | 2 | 2231 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.6 s | 1 | 1915 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.2 s | 5 | 1571 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.0 s | 2 | 1399 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.0 s | 13 | 1371 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.8 s | 3 | 1227 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.6 s | 7 | 1055 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.5 s | 9 | 941 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.4 s | 16 | 883 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.2 s | 28 | 711 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.2 s | 3 | 683 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 53 | 539 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 8 | 511 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 19 | 367 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 7 | 339 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 12 | 310 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.7 s | 63 | 224 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.6 s | 1 | 167 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 4 | 81 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 12 | 52 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.4 s | 1 | -5 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 19 | -91 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 35 | -120 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 1 | -177 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 27 | -292 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 瞬發 | 69 | — | `instant` | **不出現**（沒有施法窗口可以預告） |

## 彙總

- `reactable` — **252** 支（60.0 %）
- `marginal` — **16** 支（3.8 %）
- `notice` — **83** 支（19.8 %）
- `instant` — **69** 支（16.4 %）

**結論，寫成一句話**：這個功能對 **0.6–0.9 秒** 的技能是真的「來得及閃」；
對 **0.5 秒** 是勉強；對 **0.3–0.4 秒**（超過一半的技能）它只是「你被鎖定了」的通知，
不是閃避機會 —— 要讓那些技能可閃，唯一的辦法是把它們的 `castTimeSec` 拉長，那是內容決策，不是特效決策。
