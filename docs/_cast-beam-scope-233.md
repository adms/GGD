# #233 向天光束 — 可以幫到哪些技能，幫不到哪些

> 這份表由 `apps/client/src/vfx/castBeamScope.test.ts` 每次跑測試時從
> `content/abilities/*.json` 重新推導，**不是手抄的**。改了施法前搖，這裡就會變。

反應預算 = 施法時間 − 光柱自己的淡入(14%) − `INTERP_DELAY_MS`(66 ms) − 人類反應(250 ms) − 一個 tick(33 ms)

| 施法前搖 | 技能數 | 反應預算 | 判定 | 光束會做什麼 |
|---|---:|---:|---|---|
| 3.0 s | 2 | 2231 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.0 s | 9 | 1371 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.6 s | 2 | 1055 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.5 s | 10 | 941 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.4 s | 5 | 883 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.2 s | 9 | 711 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.1 s | 4 | 625 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 55 | 539 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 6 | 511 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 23 | 367 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 6 | 339 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 31 | 310 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.7 s | 27 | 224 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.6 s | 1 | 167 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 3 | 81 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 53 | 52 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.4 s | 21 | -34 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 3 | -91 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 27 | -120 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 1 | -149 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 1 | -177 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 27 | -206 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 1 | -263 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 27 | -292 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 瞬發 | 66 | — | `instant` | **不出現**（沒有施法窗口可以預告） |

## 彙總

- `reactable` — **190** 支（45.2 %）
- `marginal` — **56** 支（13.3 %）
- `notice` — **108** 支（25.7 %）
- `instant` — **66** 支（15.7 %）

**結論，寫成一句話**：這個功能對 **0.6–0.9 秒** 的技能是真的「來得及閃」；
對 **0.5 秒** 是勉強；對 **0.3–0.4 秒**（超過一半的技能）它只是「你被鎖定了」的通知，
不是閃避機會 —— 要讓那些技能可閃，唯一的辦法是把它們的 `castTimeSec` 拉長，那是內容決策，不是特效決策。
