# #233 向天光束 — 可以幫到哪些技能，幫不到哪些

> 這份表由 `apps/client/src/vfx/castBeamScope.test.ts` 每次跑測試時從
> `content/abilities/*.json` 重新推導，**不是手抄的**。改了施法前搖，這裡就會變。

反應預算 = 施法時間 − 光柱自己的淡入(14%) − `INTERP_DELAY_MS`(66 ms) − 人類反應(250 ms) − 一個 tick(33 ms)

| 施法前搖 | 技能數 | 反應預算 | 判定 | 光束會做什麼 |
|---|---:|---:|---|---|
| 3.0 s | 2 | 2231 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 2.0 s | 9 | 1371 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.8 s | 2 | 1170 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.6 s | 5 | 1055 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.5 s | 30 | 941 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.4 s | 4 | 883 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.4 s | 2 | 826 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.3 s | 1 | 740 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.2 s | 7 | 711 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.1 s | 10 | 625 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 57 | 539 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 1.0 s | 11 | 511 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.9 s | 10 | 396 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 13 | 367 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 7 | 339 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.8 s | 11 | 310 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.7 s | 29 | 224 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.6 s | 22 | 195 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.6 s | 1 | 167 ms | `reactable` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 6 | 81 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.5 s | 22 | 52 ms | `marginal` | 出現 + **下墜倒數**，光點落地＝技能生效 |
| 0.4 s | 4 | -34 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 24 | -91 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.3 s | 53 | -120 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 1 | -149 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 4 | -177 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.2 s | 1 | -206 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 2 | -235 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 1 | -263 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 0.1 s | 38 | -292 ms | `notice` | 出現，但**不畫下墜倒數**（沒有可閃的窗口，倒數就是騙人） |
| 瞬發 | 72 | — | `instant` | **不出現**（沒有施法窗口可以預告） |

## 彙總

- `reactable` — **233** 支（50.5 %）
- `marginal` — **28** 支（6.1 %）
- `notice` — **128** 支（27.8 %）
- `instant` — **72** 支（15.6 %）

**結論，寫成一句話**：這個功能對 **0.6–0.9 秒** 的技能是真的「來得及閃」；
對 **0.5 秒** 是勉強；對 **0.3–0.4 秒**（超過一半的技能）它只是「你被鎖定了」的通知，
不是閃避機會 —— 要讓那些技能可閃，唯一的辦法是把它們的 `castTimeSec` 拉長，那是內容決策，不是特效決策。
