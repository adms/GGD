# 未合併改動 —— 逐項決策表（2026-08-30）

> owner 逐字：「**現在的 main 為主**⋯看懂有什麼**功能邏輯是優於目前的**列成一張表來判斷是否合併，
> 這種**手術級的合併**，動手之前都要跟我**詳細逐項確認**才執行」

⛔ **這份是決策材料，⛔ 不是一次合併。我一項都沒動。**

## 📊 總計：**97 項**比對（12 個 agent，含對抗式複驗）

| 判定 | 項 |
|---|---:|
| 🟢 **分支較優** | **46** |
| 🟡 **兩邊各一半**（合併要保留兩者）| **7** |
| 🔴 **main 較優**（⛔ 合過來是退步）| **38** |
| ⚪ 一樣 | 1 |
| ❔ 判不出來 | 5 |

⇒ ⭐⭐ **近四成的改動合過來會讓 main 變差。** ⛔ 這不是一次「合併」，是一次**挑揀**。


---

# 🔴 ⛔ **不可以合**的（main 較優）—— 先看這一節


### ⛔ POST /validate|/apply|/rollback 等未實作路由的 501 回應外殼

| | |
|---|---|
| 位置 | `apps/content-api/src/importRoutes.ts:151-176（main）／分支同檔 :157-186` |
| ⭐ **main 今天** | 回獨立的 `ggd-content-import-error@1`（IMPORT_ERROR_SCHEMA），code 取自 IMPORT_DIAGNOSTICS.OPERATION_NOT_IMPLEMENTED、retryable:false、plannedStage/implementedStage 齊全。檔頭逐字記錄了 2026-08-14 修掉的三處違規 |
| 分支 | 回 `ggd-content-import-result@1`（IMPORT_RESULT_SCHEMA）＋ operationId:null ＋ code:"unsupported-operation"（不在診斷登錄表裡）＋ severity:"blocker"（登錄表用的是 error）——＝ main 修好之前的那一版 |
| ⭐ **為什麼 main 較優** | main 的檔頭逐字寫了為什麼：「對面用 schema tag 決定用哪一個 parser。一個宣稱是 result 卻不合 result schema 的東西，讓對方分不出『我解析錯了』與『你們還沒做』——而那兩者的處置完全相反」。分支不是提了另一種設計，它只是基線停在 2026-08-14 之前。要反駁必須證明外部 importer 真的偏好 result 外殼——票文與規格 §12 都指向相反方向 |
| 出處 | `git diff main origin/feat/ability-review-authoring -- apps/content-api/src/importRoutes.ts 顯示 main 的 `const envelope: ImportErrorEnvelope = { schema: IMPORT_ERROR_SCHEMA, ... }` 整段被 `-`，換成 `` |

### ⛔ GET <prefix>/authoring-rules 的資料來源

| | |
|---|---|
| 位置 | `apps/content-api/src/importRoutes.ts:226-227（main）／分支 :150-158, 197-205` |
| ⭐ **main 今天** | `buildAuthoringRules((id) => Configs.tryGet(id))` —— 讀執行期 Configs 登錄表，含後台 override。註解逐字：「owner 在後台改一格，這個端點下一秒就變，對方不用改一行程式」 |
| 分支 | `readAuthoringRules(root)` ＋ `readConfigDoc(root,"aoe-tiers")` —— 直接 readFile(content/config/*.json)，繞過 Configs |
| ⭐ **為什麼 main 較優** | 後台 override 會蓋掉 content/ 的檔案（第一守則末段、memory ggd-admin-override-shadows-content）。分支那條路讀的是磁碟上的出貨值 ⇒ owner 在後台改了規則，這個端點不會變，而外部編輯器會照著過期的規則產 JSON。要反駁得證明 authoring-rules 刻意不吃 override——main 的註解說的正好相反 |
| 出處 | `main importRoutes.ts:227 `reply.send(buildAuthoringRules((id) => Configs.tryGet(id)))`；分支同一段被 `-`，改成 :201-204 `buildAuthoringRules(await readAuthoringRules(root), now().toISOString(), await ` |

### ⛔ `ggd-authoring-rules@1` 的 payload 形狀與推導鏈（packages/shared 側的 buildAuthoringRules）

| | |
|---|---|
| 位置 | `packages/shared/src/content/authoringRules.ts（main，634 行）／分支 packages/shared/src/content/authoring/authoringRu` |
| ⭐ **main 今天** | 回 `{schema, hard[], principle[], manaIsDerived:true, …}` 兩層界（硬界擋下／原則界警告放行），從 Zod 界＋cast-time／cooldown-rules／aoe-tiers／stat-caps＋proportionality(GH#465)＋maxTiersAboveMin(GH#616)＋lowDamageCells(GH#445) 推導；每條規則帶 `source` |
| 分支 | 回 `{schema, enabled, rules[], fingerprint}` 單層，從一份手寫 config 推導；另有 mana.singleTargetBase/roundTo/rangeMultiplier 的耗魔公式 |
| ⭐ **為什麼 main 較優** | 同一個 schema tag 兩種不相容的 payload。main 那份的規則數與推導來源都是分支的超集，且每條帶出處（正是它自己檔頭說「沒有這一格就會長出手寫規則」的那條）。分支的 mana 三格看似 main 沒有，但 main 另有整套 content/config/mana-tiers.json + manaTiers.ts 級距系統，並用 manaIsDerived:true 告訴編輯器把輸入框關掉——那是更完整的答案 |
| 出處 | `main authoringRules.ts:96-109 定義 AuthoringRulesManifest{hard,principle,manaIsDerived}；分支檔開頭定義 zAuthoringRulesConfigDoc{enabled,mana,cooldown,duelRadius}。`ls content/config/ ／ grep -i mana` →` |

### ⛔ content/config/authoring-rules.json 這份出貨設定文件

| | |
|---|---|
| 位置 | `content/config/authoring-rules.json（兩邊同 id 同 schema，欄位集合幾乎不相交）` |
| ⭐ **main 今天** | `{singleTargetCooldown{min:5,max:30}, aoeCooldown{min:30,max:120}, transformCooldownMin:120, proportionality{enabled,model:"formula",expectedHits,aimRiskMult,minDamageTier 15格,maxTiersAboveMin:1}}`，note 逐字記著 owner 2026-08-20 的公式與 GH#616 的上限裁決 |
| 分支 | `{enabled:true, mana{singleTargetBase:50,roundTo:5,rangeMultiplier}, cooldown{targeted,area,transformMinimum,longDurationThreshold:6}, duelRadius:24}` |
| ⭐ **為什麼 main 較優** | 分支那份覆蓋過去會抹掉 GH#465 相稱性模型（四個 model、expectedHits、aimRiskMult、十五格 minDamageTier）與 GH#616 的 maxTiersAboveMin——那些都是 owner 逐字裁決的產物（note 裡有引言）。分支那份是 2026-08-14 之前的簡化版。要反駁得指出 main 那份哪一格是錯的；量不出來 |
| 出處 | `cat content/config/authoring-rules.json（main，note 逐字引 owner 2026-08-20「30/6秒=5…再除2…約等於2.5倍」）vs git show origin/feat/ability-review-authoring:content/config/authoring-rules.json（note 只有一句「編輯器` |

### ⛔ /validate 的 501 說明文字（why 欄位）

| | |
|---|---|
| 位置 | `apps/content-api/src/importRoutes.ts:86-92` |
| ⭐ **main 今天** | 「需要 bounded transport（zipSafety 已有）+ Zod／capability／authoring-rules 三道驗證。⚠️ owner 2026-08-15 砍掉了『遊戲端重編 + 逐位元比對』那一段 —— 見 target profile 的 authoringModel：編輯器直接產 ability@1，所以沒有第二個編譯器可以漂移」 |
| 分支 | 「需要 transport safety、staging tree、compiler 與 compiled compare —— 一項都還沒有」 |
| ⭐ **為什麼 main 較優** | 分支那句還在講「compiler 與 compiled compare」，而 owner 2026-08-15 已經砍掉那條路（main 的 commit 5406c4ce7『砍掉編譯器那一層 —— 編輯器直接產 ability@1』）⇒ 分支的文字是一句過期的散文，而它會被外部 importer 讀進 501 診斷訊息裡 |
| 出處 | `git log --oneline 9e5cc378..main -- apps/content-api/src/importRoutes.ts → 5406c4ce7 / 91502b5f3 / 5f1c3ed7b 三個 commit 都在分岔之後；main:88-92 的 why 引用了 owner 2026-08-15` |

### ⛔ editor-desktop 的 build:renderer 用 VITE_DESKTOP=1 覆寫 apps/editor/dist

| | |
|---|---|
| 位置 | `分支 apps/editor-desktop/package.json 的 build:renderer / build；耦合 apps/editor/src/api/client.ts:33-34` |
| ⭐ **main 今天** | apps/editor/src/api/client.ts:22 是 `Boolean(env?.DEV)` —— 正式 vite build 一律 dead-fold 成 WRITES_ENABLED=false。docker/edge.Dockerfile:14-44 逐字解釋這道鎖為什麼在（apps/editor/dist 會被烘進正式映像、服務於 /editor/） |
| 分支 | client.ts 改成 `Boolean(env?.DEV ／／ env?.VITE_DESKTOP === "1")`；editor-desktop 的 build:renderer = `VITE_DESKTOP=1 pnpm --filter @ggd/editor build`，輸出仍在 apps/editor/dist |
| ⭐ **為什麼 main 較優** | root 的 build 是 `pnpm -r --if-present build` ⇒ 加進 workspace 之後，一次 root build 就會讓 apps/editor/dist 最後留下的是「寫入 UI 開著」的桌面版——而那正是 edge.Dockerfile 會 COPY 的目錄。docker 自己那條路今天安全（它跑的是 `pnpm --filter @ggd/editor build`，沒設 VITE_DESKTOP），危險的是任何先 root-build 再 COPY dist 的流程。反駁點：只要沒有人這樣做就沒事——那是判準不是閘 |
| 出處 | `root package.json `"build": "pnpm -r --if-present build"`；分支 apps/editor-desktop/package.json `"build": "pnpm build:renderer && pnpm build:main"`；docker/edge.Dockerfile:22-27「apps/editor/src` |

### ⛔ pnpm-lock.yaml（+2,252 行）

| | |
|---|---|
| 位置 | `分支 pnpm-lock.yaml` |
| ⭐ **main 今天** | 17 個 workspace importer，含 tools/anime-arena-map（分岔之後在 main 上新增的） |
| 分支 | 也是 17 個：加了 apps/editor-desktop，⛔ 少了 tools/anime-arena-map |
| ⭐ **為什麼 main 較優** | 分支的 lock 是「舊 main 的 lock + editor-desktop」，它不知道 tools/anime-arena-map 存在。直接合併 ⇒ pnpm-workspace.yaml 列了 tools/* 而 lock 裡沒有那個 importer ⇒ `pnpm install --frozen-lockfile` 失敗 ⇒ docker/edge.Dockerfile:60 當場死 ⇒ 部署失敗。⛔ lock 檔本來就不該被合併，該重生成 |
| 出處 | `comm -23 <(grep -oE "^  (apps／packages／tools)/[a-z0-9-]+" pnpm-lock.yaml ／ sort -u) <(git show 'origin/feat/ability-review-authoring:pnpm-lock.yaml' ／ grep -oE ... ／ sort -u) → 只有一行：tools/an` |

### ⛔ 稽核結果的嚴重度接進 errorCount ⇒ 直接 disable save 按鈕

| | |
|---|---|
| 位置 | `apps/editor/src/views/EditorView.tsx:75-76, 114（分支）vs main:views/EditorView.tsx:57, 91` |
| ⭐ **main 今天** | main 的 errorCount 只算 Zod inline errors + server errors（EditorView.tsx:57）。語意警示走另一條路 render 成 <ul class="author-warnings">，authorWarnings.ts 檔頭逐字寫著「⛔ 它不擋存檔（owner 2026-08-12：只是個警告標記，並不會擋）」。 |
| 分支 | const auditErrorCount = abilityIssues.filter(i => i.severity === "error").length; const errorCount = Object.keys(errors).length + auditErrorCount; → save 按鈕 disabled={!dirty ／／ errorCount > 0} |
| ⭐ **為什麼 main 較優** | 這條線把「內容慣例沒對齊」升級成「存不進去」。實測：把分支的 auditSkillDescription 跑過 main 今天出貨的 421 支技能，264 支（62.7%）至少有一個 error ⇒ 改一格無關的欄位（例如特效顏色）也會因為第一行寫著 [主動攻擊] 而存不下去。這正是 CLAUDE.md 說的「一條會誤報的閘會被下一個人放寬，而放寬過的閘等於沒有閘」。 |
| 出處 | `npx tsx /private/tmp/ggdcmp/run.ts → "docs with >=1 ERROR (branch blocks save): 264 (62.7%)"；main:apps/editor/src/authorWarnings.ts:22-23 引 owner 2026-08-12 原話` |

### ⛔ OWNER_PROSE_CHANGED —— 偵測 resolved 說明是否改動了 Owner 原始文案（只允許補數值與修標籤，不可改寫幽默文案）

| | |
|---|---|
| 位置 | `apps/editor/src/abilityReviewRecord.ts:16-28（ownerTextSkeleton/ownerTextPreserved）· abilityAudit.ts:56-62 · Ed` |
| ⭐ **main 今天** | ⛔ main 沒有這條規則，也沒有 ability-review-record 這個概念。 |
| 分支 | ownerTextSkeleton 砍掉第一行標籤、把數字換成 #、去空白與標點後比對。基準線 source.description 由 initialAbilityReview(original) 產生，而 original = 第一次在編輯器打開這支技能時磁碟上的內容。severity=error ⇒ 擋存檔。 |
| ⭐ **為什麼 main 較優** | 概念有價值，但基準線是自己生成的快照，卻被貼上「Owner 原始文字」的標籤。第一次打開時那份說明如果本來就過期／有錯，作者從此再也不能透過編輯器修好它——而錯誤訊息會指著錯的方向（說是你改了 owner 的字）。⭐ 這與 CLAUDE.md「我的推測會變成他的需求」是同一個病的第四個載體。⇒ 今天 main 的『什麼都沒有』比較安全。 |
| 出處 | `apps/editor/src/views/EditorView.tsx:41-47 `if (... ／／ abilityReview.data) return; void api.createAbilityReview(docId, initialAbilityReview(original))`；abilityReviewRecord.ts:34 `const descr` |

### ⛔ 效果模板工作台：JSON 定義 effect-template@1 → 建立帶參數的 Product → 排成效果鏈 → 即時編譯出 abilityPatch/itemPatch/effects + 

| | |
|---|---|
| 位置 | `apps/editor/src/forge/EffectGraphWorkbench.tsx（296，新增）+ packages/shared/src/content/authoring/effectGraph.ts（4` |
| ⭐ **main 今天** | ⭐ main 的 ForgeStudio.tsx（964）+ expand.ts（2,490）已經是同一條管線且更強：AbilityTemplateCard{ref,params} = Product；cards[] = 效果鏈；TemplateConflictPolicy(reject/lastWins) = onConflict；expandStack → ExpandStackTrace = compile+trace。而且 ① 參數表單走 paramsSchemaFor(tpl)→walkZod→真正的 FormRenderer widget（夾住上下界的 NumberField、En |
| 分支 | 整份工作台住 localStorage（STORAGE_KEY="ggd-editor:effect-graph-workspace@1"），⛔ 零寫回：沒有 api.put、沒有 planForgeWrite，編譯結果只印在 <pre> 裡。參數輸入是裸 <input> + event.target.value.split("/").map(Number)，⛔ 沒有型別化 widget、沒有上下界夾制。 |
| ⭐ **為什麼 main 較優** | 扣掉三個真正的新概念（下三列）之後，剩下的是一個功能較少、且做完什麼都存不下去的版本。⛔『行數多=功能多』在這裡是反的：296 行對上 964+2,490 行。要反駁我，得指出 EffectGraphWorkbench 能做而 ForgeStudio 做不到的第四件事。 |
| 出處 | `分支 EffectGraphWorkbench.tsx 全文無 api. 呼叫（只有 window.localStorage.setItem）；main:apps/editor/src/forge/ForgeStudio.tsx:56 import planForgeWrite/runForgeWrite；main:packages/shared/src/content/tem` |

### ⛔ 鑄技工坊的落地頁：兩個模式頁籤，預設進哪一個

| | |
|---|---|
| 位置 | `apps/editor/src/forge/ForgePage.tsx:20（分支）vs main:forge/ForgePage.tsx:21` |
| ⭐ **main 今天** | 點 🔨 鑄技工坊 → ForgeGallery 選模板 → ForgeStudio（964 行，能填參數、疊卡、選衝突政策、看展開來源表、真 sim 試放、一鍵寫回技能文件）。 |
| 分支 | const [mode] = useState<"legacy"／"graph">("graph") ⇒ ⭐ 預設落在 EffectGraphWorkbench，ForgeStudio 被降級成叫做「既有模板卡」的第二個頁籤。 |
| ⭐ **為什麼 main 較優** | 預設落點是一個 localStorage-only、零寫回的頁面：作者定義模板、建 Product、排鏈、看到「✓ 編譯成功」，然後沒有任何按鈕能把它存進任何技能。⭐ 把『做完什麼都存不下去的頁』設成預設，比工具不存在更糟——它會消耗一整輪的作業時間才被發現。 |
| 出處 | `分支 forge/ForgePage.tsx:20 useState<"legacy" ／ "graph">("graph")；分支 EffectGraphWorkbench.tsx 全文零個 api. 呼叫` |

### ⛔ 工坊第 3 步的 3D 舞台 + 「真的放一次」（把這一發包成 IntentFrame 丟進 world.step()，走玩家那條路）

| | |
|---|---|
| 位置 | `main:apps/editor/src/preview/BabylonPreviewController.ts（295）· forge/ForgeStudio.tsx:73-77, 245-252, 447-483 ·` |
| ⭐ **main 今天** | ⭐ GH#174 已落地：ForgeStudio 掛 createBabylonPreviewController()，有 <canvas> 舞台（地面格線+英雄模型+走與遊戲同一支 toParticleSystem 的粒子），有「真的放一次」按鈕回報 sim 收下/拒絕、魔力變化、冷卻 tick、事件筆數。 |
| 分支 | ⛔ 全部刪除：BabylonPreviewController.ts 整檔消失（-295）、forgeRealCast.test.ts 整檔消失（-83）、ForgeStudio 改回 createSimPreviewController()、canvas 與按鈕與 trace state 全拿掉，文案改成「3D 預覽仍是 P2」。 |
| ⭐ **為什麼 main 較優** | 這不是設計選擇，是分支的 base（2026-08-14）比 GH#174 早。⭐ 而「真的放一次」正是 CLAUDE.md 第二守則要的那種守衛：它走出貨的那條路，所以『編輯器放得出來、遊戲裡按下去沒反應』不可能發生。刪掉它等於刪掉一條承重守衛。 |
| 出處 | `git diff --stat main origin/feat/ability-review-authoring -- apps/editor → "preview/BabylonPreviewController.ts ／ 295 -----" 與 "preview/forgeRealCast.test.ts ／ 83 ---"；git diff ... -- forge/` |

### ⛔ content/config/authoring-rules.json —— 創作規則的出貨值

| | |
|---|---|
| 位置 | `content/config/authoring-rules.json（main 52 行 / 分支 24 行）· packages/shared/src/content/schema/config/authoringR` |
| ⭐ **main 今天** | ⭐ 52 行，欄位是 singleTargetCooldown / aoeCooldown / transformCooldownMin / proportionality（GH#465 四個推導模型 + minDamageTier 十五格由公式現推 + GH#616 maxTiersAboveMin）。Zod 是 .strict()。 |
| 分支 | ⭐ 同一個 id、同一個 schema tag（config.authoring-rules@1），⛔ 但欄位完全不同：enabled / mana{singleTargetBase,roundTo,rangeMultiplier} / cooldown{targeted,area,transformMinimum,longDurationThreshold} / duelRadius。main 的四個欄位一個都不在。 |
| ⭐ **為什麼 main 較優** | 這不是「哪一版比較好」的問題——兩份文件用同一個 schema tag 宣稱自己是同一份設定。合併分支那一版，main 的 .strict() Zod 會拒絕它 ⇒ 內容驗證整份失敗 ⇒ fail-open 退回 2 隻骨架英雄，而網站看起來完全正常。⭐ 這逐字就是 CLAUDE.md 記載的 2026-08-02 生產故障的形狀。 |
| 出處 | `git show main:content/config/authoring-rules.json ／ wc -l → 52；git show origin/feat/ability-review-authoring:content/config/authoring-rules.json ／ wc -l → 24；main:packages/shared/src/content` |

### ⛔ /content-import/authoring-rules 端點與 buildAuthoringRules() 的資料來源

| | |
|---|---|
| 位置 | `main:apps/content-api/src/importRoutes.ts:226 · packages/shared/src/content/authoringRules.ts:368；分支:importRou` |
| ⭐ **main 今天** | ⭐ 讀執行期的 Configs 登錄表：buildAuthoringRules((id) => Configs.tryGet(id))。註解逐字寫著「owner 在後台改一格，這個端點下一秒就變，對方不用改一行程式」（GH#327）。回傳 AuthoringRulesManifest（rules[] + RuleSource + LowDamageCellRule + NewHeroCheckRule + StatTuning + proportionality）。 |
| 分支 | 換成讀磁碟：buildAuthoringRules(await readAuthoringRules(root), now().toISOString(), await readConfigDoc(root,"aoe-tiers"))。回傳 AuthoringRulesDocument（mana/cooldown/aoeTiers/fingerprint），並在 /content-api/authoring-rules 多開一個別名路由 |
| ⭐ **為什麼 main 較優** | 分支那版看不到後台 override —— 直接違反 CLAUDE.md「後台的 override 會蓋掉 content/ 的檔案，改 config 前先確認線上有沒有存過 override」。⚠️ 而且兩邊 export 了一模一樣的三個名字（AUTHORING_RULES_SCHEMA / AUTHORING_RULES_DOC_ID / buildAuthoringRules）卻是不相容的型別 ⇒ 這是名字碰撞不是升級。另外 generatedAt: now().toISOString() 會讓任何逐位元組 --check 永遠不相等（CLAUDE.md 記過這個坑）。 |
| 出處 | `main:apps/content-api/src/importRoutes.ts:226-228 `buildAuthoringRules((id) => Configs.tryGet(id))`；分支同檔:124-128 `buildAuthoringRules(await readAuthoringRules(root), now().toISOString(), ...` |

⭐ 另有 **24 項**同類。


---

# 🟡 **兩邊各一半** —— ⛔ 二選一兩邊都虧


### 🟡 /content-api/authoring-rules 相容別名路由（不帶 /content-import 的短路徑）

| | |
|---|---|
| 位置 | `分支 apps/content-api/src/importRoutes.ts:274-280` |
| main | 只有 `<prefix>/authoring-rules`，prefix 是 /api/v1/content-import 與 /content-api/content-import 兩個 ⇒ 短路徑 /content-api/authoring-rules 是 404 |
| 分支 | 當 prefixes 含 /content-api/content-import 時，額外註冊裸的 /content-api/authoring-rules |
| ⭐ 為什麼兩邊都要 | 路由本身值得收（交接文件當初寫的就是短路徑，main 的 DEFAULT_IMPORT_PREFIXES 註解自己說「兩個都註冊，成本是零」——同一個理由適用第三條）；但它掛的 builder 是分支那版（讀檔、繞過 Configs）⇒ 要收就用 main 的 buildAuthoringRules(Configs.tryGet) 掛上去，兩件事要拆開 |
| ⚠️ 風險 | 低。收的時候要注意分支的 server.test.ts 那條斷言（fingerprint 比對 canonical 與 alias 相同、rules[] 含 cooldown-must-not-rise-per-rank）是照分支的 payload 形狀寫的，用 main 的 builder 會紅 ⇒ 那條測試要重寫，⛔ 不是照抄 |
| 工作量 | 小 |

### 🟡 存檔當下的七條語意警示：六欄留白 · 超出出貨 Zod 上下界 · 冷卻原則區間 · 說明數字↔JSON 對不上 · 說了但不會發生（第一·五守則）· 機制數字寫進「」台詞 · 傷害相對冷卻偏低（GH

| | |
|---|---|
| 位置 | `main:apps/editor/src/authorWarnings.ts（44 行）+ packages/shared/src/content/newHeroChecks.ts；分支把該檔整份刪除` |
| main | ⭐ 七條全部在跑，而且是三個住處齊全的後台開關（content/config/new-hero-checks.json + DEFAULT_NEW_HERO_CHECKS + admin NEW_HERO_CHECKS_SPEC）。每一條都重用既有判斷：descriptionClaims.scanAbility / abilityNoOpEffects.analyseAbility / lowDamageCells / 出貨 Zod。這是 GH#480，owner 2026-08-20 點名的「生成代入與檢查跳警示 |
| 分支 | ⛔ authorWarnings.ts 整檔刪除（-44），EditorView 不再 import、不再 render <ul class="author-warnings">。分支的 abilityAudit 沒有 claim-mismatch / no-op-effect / dialogue-claim / low-damage-cell 任何一條的對應物。 |
| ⭐ 為什麼兩邊都要 | 兩邊管的是不相交的兩族：main 管「數字對不對、會不會發生」，分支管「第一行的標籤對不對」。分支不是取代它，是沒看過它（GH#480 是 2026-08-20 落地，晚於 merge-base 2026-08-14）。二選一兩邊都虧。 |
| ⚠️ 風險 | ⭐ 這是「靜默回歸」的典型：刪掉之後 tsc 是綠的、測試是綠的（main 沒有 authorWarnings 的專屬測試），而編輯器對七族問題重新變成沉默。⛔ 合併時必須明確保留 authorWarnings 的 import 與 render。 |
| 工作量 | 小 |

### 🟡 EditorView 本身（衝突檔）——兩邊各自加了一半的作者輔助

| | |
|---|---|
| 位置 | `apps/editor/src/views/EditorView.tsx（main 113 行 / 分支 +55 −? 兩點 diff 55 行變動）` |
| main | import authorWarnings → const warnings = useMemo(...) → 渲染 <ul class="author-warnings">，⛔ 不進 errorCount。 |
| 分支 | 改為 import auditAbilityDocument + useAuthoringPolicy + AbilityReviewPanel + abilityReviewRecord，三個 useQuery（runtime-contract / authoring-rules / ability-review），一個 useEffect 在開啟技能時 POST 建立 review record，abilityIssues 進 errorCount。⛔ 沒有 authorWarnings。 |
| ⭐ 為什麼兩邊都要 | 這正是簡報裡點名的第③個陷阱：main 有 authorWarnings（GH#480）、分支有 auditAbilityDocument，兩個都是真功能。正解是在 EditorView 裡同時掛上兩條，共用一個不擋存檔的警示區。 |
| ⚠️ 風險 | 合併時這個檔一定會衝突。⭐ 逐行手工合併，⛔ 不可以用 --ours/--theirs（任一邊都會吃掉對方的整條功能）。 |
| 工作量 | 小 |

### 🟡 依英雄分組的技能導覽（六格 PASSIVE/Q/W/E/R/EX、缺件灰顯、四個 scope 過濾、搜尋框）

| | |
|---|---|
| 位置 | `apps/editor/src/abilityNavigation.ts（65）· views/AbilityDocList.tsx（127）· App.tsx:44-46` |
| main | ⛔ 平坦清單：views/Sidebar.tsx 的 DocList 直接 data.entries.map(...)，今天 content/abilities 是 421 筆，⛔ 沒有搜尋、沒有分組。⭐ 但它帶著 + new / ⧉ duplicate / 🗑 delete 三個 CRUD 工具。 |
| 分支 | abilities 集合改用 AbilityDocList（純函式 groupAbilitiesByHero + 兩個 filter，有 35 行測試）。⛔ 但 AbilityDocList 只有 open()，⛔ 沒有 new/duplicate/delete。 |
| ⭐ 為什麼兩邊都要 | 導覽這一半分支明顯較優（421 筆平坦清單實務上不可用）；CRUD 這一半 main 較優，而分支只換掉 abilities 一個集合 ⇒ 技能會變成唯一失去 CRUD 的集合。兩半都要。 |
| ⚠️ 風險 | ⚠️ loadHeroMetadata() 對 champions 索引的每一筆各發一次 api.doc("champions", id) ⇒ 首次展開技能清單會同時打約 119 個請求（有 staleTime:Infinity 快取，是一次性的）。⭐ 正解是讓 _index 帶上 name/provenance，⛔ 不是 N+1。 |
| 工作量 | 小 |

### 🟡 合併機制:PreviewController.ts 4 個衝突區塊 + BabylonPreviewController 要補兩行轉發

| | |
|---|---|
| 位置 | `apps/editor/src/preview/PreviewController.ts(衝突)· apps/editor/src/preview/BabylonPreviewController.ts:256-268(` |
| main | PreviewController 介面有 mount/dispose/previewChampion/previewAbility/**castAbility**/previewItem/previewAugment/spawnVfx/stepFixed;BabylonPreviewController 逐支轉發(:256 previewAbility、:265 castAbility)。 |
| 分支 | 介面追加 previewStandaloneAbility(:110)與 verifyAbility(:112);⛔ **分支沒有動 BabylonPreviewController.ts**(它是 8/14 之後 main 才建的檔)。 |
| ⭐ 為什麼兩邊都要 | ⭐ castAbility(main)與 verifyAbility(分支)是**兩支不同的方法**,不是同一件事的兩個版本 —— 前者是工坊的快速試放(30 tick、回冷卻),後者是編輯頁的深度驗收(360 tick、雙方 before/after、digest)。四個衝突區塊經 `git merge-file --diff3` 檢視,三個都是「兩邊各自新增、位置相鄰」⇒ **取聯集即可**,⛔ 不需要任何取捨。 |
| ⚠️ 風險 | ⚠️ 忘記補 BabylonPreviewController 的兩行轉發 ⇒ **tsc 紅**(介面缺方法)。⭐ 這是好風險 —— 它會紅,不是靜默。⛔ 但依 CLAUDE.md 併行紀律,合併 commit 要 `git commit -F msg -- <逐檔 pathspec>`,⛔ 不 add 不 amend。 |
| 工作量 | 小 |

### 🟡 編輯器側 sidecar：source.authority 五層階梯 + sourceSha256 凍住 owner 原文 + ownerIssues[] 中文稽核軌跡 + resolved.edit

| | |
|---|---|
| 位置 | `packages/shared/src/content/authoring/abilityAuthoring.ts:17-95 · itemAuthoring.ts:10-27` |
| main | schema/ability.ts:618 provenance: z.enum(["owner-spec","w3x-import"]).optional() —— 只有 2 個成員，且同段註解逐字說明第 3 個成員被刻意拿掉：「第一版有，而 fieldAdoption.test.ts 當場擋下來：0 份文件用它」。守衛 content/abilityProvenance.test.ts 要求每份出貨文件宣告它。⛔ main 今天沒有任何地方表達得出「這段字來自 JASS」vs「來自 w3x 文案」，也沒有 im |
| 分支 | authority 5 成員（owner-spec / editor-json / jass-runtime / w3x-tooltip / w3x-raw）＝第〇·六守則五層逐字；superRefine 重算 sourceSha256 比對（改了原文就拒絕）；resolved.description 與 source.description 不同而 edits 為空 ⇒ 拒絕；每筆 edit 的 issueCode 必須對到一則 summaryZh 為中文的 ownerIssue |
| ⭐ 為什麼兩邊都要 | ⭐ 分支這一組是 CLAUDE.md 兩條散文守則的機器化：五層階梯（第〇·六）＋「被取代的東西要另存，知識不可以無聲消失」。main 表達不出階梯第 3 層 vs 第 4 層的差別，而 CLAUDE.md 說它們打架時可能是刻意的隱藏機制、要問 owner。⛔ 但 main 的 2 成員不是疏漏，是量過的決定 ⇒ 建議只收「immutable source + 逐筆中文理由」的機制，authority 成員照 main 的規矩「有第一份證據才加」 |
| ⚠️ 風險 | ⚠️ sidecar 住在 schema/ 之外，而 fieldAdoption.test.ts 走的是 schema/ 樹 ⇒ 那 5 個 authority 值今天 0 採用而沒有任何閘會說話。⇒ 這是繞過那道閘，⛔ 不是通過它。合併時要把 sidecar 掛進普查範圍 |
| 工作量 | 中 |

### 🟡 Stat.AbilityAmp + ENTITY_FLAG.EMPOWERED —— 四拍增幅的 caster 側視窗與它的上線旗標

| | |
|---|---|
| 位置 | `分支的 packages/shared/src/sim/stats/statTypes.ts + protocol/schema.ts` |
| main | 有一個泛用的近親、沒有這一格：main 的 Stat 有 OutputDamagePct（0 = ×1，statTypes.ts:70），⛔ 沒有 per-effect-kind 的明列放大表，也沒有把「現在被強化中」送到客戶端的旗標。 |
| 分支 | Stat.AbilityAmp（effectRunner 一個讀取點 + 明列的 per-kind 放大表）＋ ENTITY_FLAG.EMPOWERED 上線給玩家看 |
| ⭐ 為什麼兩邊都要 | OutputDamagePct 蓋得住「全部輸出 ×N」，⛔ 蓋不住「只放大技能的某幾種 effect kind」；反過來，AbilityAmp 也取代不了 OutputDamagePct（後者已經被出貨內容用著）。⇒ 兩個都要，⛔ 不是二選一。⚠️ 但 EMPOWERED 那一半有硬成本，見風險欄。 |
| ⚠️ 風險 | 高，而且不可逆：ENTITY_FLAG 是 Colyseus defineTypes 的 APPEND-ONLY 欄位，高半部只剩 11 格，⛔ 加錯回不去，而且部署當下開著舊分頁的玩家會整格錯位（不報錯，狀態亂掉）。⇒ ⭐ 這一格要 owner 拍板「值不值得為一隻英雄吃掉 11 分之 1」，⛔ 不是我可以自己決定的。 |
| 工作量 | 中 |

---

# 🟢 **分支較優**（可以考慮合）


⭐ 共 **46 項**，按工作量由小到大。


| # | 功能 | 工作量 | ⚠️ 風險 |
|---:|---|---|---|
| 1 | remoteAsset.test.ts（新守衛，76 行） | 很小 | 低。⛔ 但它沒有反向守衛：沒有一條驗「remoteAssets 未設定時，缺檔仍然是 404 而不是對外連線」，也沒有驗 redirect 被拒。收的時候補這兩條（各 3–5 行） |
| 2 | EX 槽的「真的放一次」—— main 上永遠是 not-learned（沒有人呼叫 learnEx） | 很小 | ⭐ 低但不是零：learnEx 會 emit `exUnlock`,事件清單多一筆,任何按事件數斷言的東西要跟著調。⚠️ 而且 learnEx 走 syncAbilityPassives ⇒ 一支「Cool=0 的被動型 |
| 3 | R(大招)的「真的放一次」—— main 上永遠是 not-learned(沒設 ultGateOverride,而按鈕又寫死 level:1) | 很小 | ⚠️ ultGateOverride 是 **host 武裝的 world state**(SimWorld.ts:723),在預覽裡開它是合理的,但它讓預覽與「一場真比賽的第 N 回合」不再是同一個世界 —— 要在 U |
| 4 | PreviewPanel.tsx / previewLeap.test.ts / styles.css / AbilityCombatPanel.tsx | 很小 | ⚠️ 分支的 PreviewPanel 是 8/14 的基準 ⇒ **整檔覆蓋會吃掉 `case "maps"`**(地圖預覽會多貼一份 raw JSON 把報告推出捲軸)。⭐ 用 merge。⚠️ 另外分支那 5 條測 |
| 5 | contentSha256 的 wire format：zSha256Hex 正則加上 `sha256:` 前綴（⭐ 分支唯一的真修正） | 很小 | ⚠️ 對外 wire format 的 breaking change。合併要同時：① 在 Codex 契約文件寫一行 ② 票用 [breaking change] tag ③ ⛔ 不可以用 --content-only |
| 6 | .dockerignore 的 `**/coverage` 吃掉了 testrunner 自己的 Go 套件 ⇒ docker/testrunner.D | 很小 | ⭐ 低但**不是零**：.dockerignore 是**每一支** Dockerfile 共用的 build context 規則，改它之後要重跑一次完整 `docker compose build`，確認沒有別的東西 |
| 7 | zip bomb 壓縮比防線在 uint64 溢位時**靜默關閉** | 很小 | ⭐ 低。改動只在 OpenReaderAt 的驗證段，且是**收緊**（多拒不多收）。⚠️ 唯一要驗的是別把合法的大匯入檔一起擋掉 —— 跑一次 platformarchive 的既有 Go 測試 |
| 8 | 觀戰橫幅 WATCHING 狀態的正文是**空字串**，而註解說它「逐字保留 owner 的話」，守衛也是空轉的 | 很小 | ⭐ 低，但⚠️ 兩點要 owner 點頭：① 這一句 owner **從沒逐字給過**（第一守則：引用不到原話就是我加的）⇒ 字要他定 ② 填字之後那塊 HUD 版位會變寬，`hudSurfaces` 的 COMPACT |
| 9 | .dockerignore 的 `**/coverage` 吞掉一個 Go 原始碼套件 ⇒ testrunner 映像根本建不起來 | 很小 | 近乎零：只加一個 re-include。⚠️ 唯一要驗的是那一行不會把別處產生的 coverage/ 輸出目錄也放進 build context（分支刻意用例外行而不是改通則，正是為了這件事）。 |
| 10 | zip bomb 壓縮比防線的 int64 溢位 —— 攻擊者可以用 ZIP 標頭靜默關掉它 | 很小 | 近乎零（只增加一條拒絕路徑，正常檔案碰不到）。⚠️ 但要和「掃描器釘版本」一起做：gosec v2.28.0 才有的 G703 規則正是抓這一族，只取一半會讓 CI 在下一次 @latest 漂移時紅。 |
| 11 | CI 的 gosec / govulncheck 用 @latest ⇒ 一道 verdict 隨日曆改變的閘 | 很小 | ⚠️ 釘到 v2.28.0 之後第一次跑會紅：它會抓到上一項的 G703（DiscardStage 的路徑）與那批 G115。⇒ 這一項與 zip-bomb 那一項是同一批，⛔ 不要只取一半。分支同時附了 4 個 #no |
| 12 | findPython() 的 `arch` 探針在 Linux 上永遠回答 yes ⇒ 「乾淨跳過」失效、真因被吞掉 | 很小 | 近乎零。⚠️ 副作用是誠實的：在沒有 python 的 Linux CI 上，那兩支 suite 會從「18 條紅」變成「乾淨跳過」—— ⛔ 如果現在有人靠那個紅當訊號，會少一個（但那個訊號本來就指著錯方向）。 |
| 13 | celebrationGate —— 商店的「停止繪製」要等煙火播完 | 很小 | 低，但有一個真的失敗模式：等待條件寫錯 ⇒ 轉場被無限期擋住。⇒ 那個 2.5 s 硬上限不可以省，而且它應該是後台一格（第一守則），⛔ 不是寫死的常數。 |
| 14 | content-api.Dockerfile 沒有 COPY tsconfig.base.json | 很小 | 近乎零，但也近乎零收益。⚠️ 真正的風險是把它當成「修好了」—— 那支映像仍然建不起來，⇒ 修一半會製造一個「看起來已經處理過」的假前提。 |
| 15 | `duelRadius`（決鬥區半徑）與 `longDurationThreshold`（長持續門檻）兩格參數 | 小 | 要補進 main 就得落三個住處（config JSON + Zod DEFAULT_* + admin SHIPPED_*），並確認 24 不是別處已有的數字（第〇·四守則）。⛔ 不可以只補 JSON |
| 16 | `allowedWriteOrigins` —— 寫入守衛可由呼叫端追加白名單 origin | 小 | ⛔ 兩個真缺口：① additional 沒有被驗證是 loopback ⇒ 傳 https://evil.com 進去就是白名單，而 guard.ts 檔頭第 3 條逐字說明的攻擊（dev 機器上的瀏覽器＝loopba |
| 17 | GET /content-api/editor-source —— 回報這個編輯器現在讀的是本機還是遠端 | 小 | 低。純唯讀端點，不傳選項時回本機預設。唯一要看的是它會 readFileSync(manifest.json) —— 每次請求同步讀檔（沒有快取），高頻輪詢時是同步 I/O |
| 18 | 新增 electron / electron-builder / esbuild 工具鏈到 monorepo | 小 | ⚠️ 對既有 build 的實測影響：`pnpm -r --if-present test` 與 ship:check 的 ALL_SUITES（packagesWithVitest 是掃出來的）會自動納入 editor |
| 19 | 技能階數與被動成本規則：Q/W/E 固定 4 級 R 固定 3 級 · cooldown/manaCost 陣列長度要對得上 maxRank（passi | 小 | 低。⭐ 但正確住處是 packages/shared/newHeroChecks.ts（那裡已經有三個住處的逐條開關），⛔ 不是 apps/editor —— 放在 editor 等於後台那一頁看不到也關不掉。 |
| 20 | assertHostOutputKind —— 技能鏈不可輸出 itemPatch、道具鏈不可輸出 effects/abilityPatch | 小 | 低——但它只有在『道具也能用模板鏈』這件事成立時才有意義。今天 main 沒有那條路，所以撈它等於先答應要做那條路。⭐ 建議跟著上一列一起決定，⛔ 不要單獨撈。 |
| 21 | 變身技(championForm)：main 的預覽**綠著騙人** —— 寫「sim 收下了」而變身根本沒發生 | 小 | ⛔⛔ **全域 registry 污染**：relatedChampions 由呼叫端傳整份英雄清單(PreviewPanel 傳 `champions as ChampionDef[]`),而 registerCham |
| 22 | standalone PASSIVE / EX 技能文件的效果預覽(137/421 = 32.5% 的技能今天在編輯器裡是空白) | 小 | ⚠️ registerChampion **不註冊** passiveAbility/exAbility(registry.ts:146-153 只跑 CORE_SLOTS)⇒ 分支靠 `Abilities.regist |
| 23 | abilities 集合的 3D 對戰驗收場(敵我兩具模型 + 施法預告 + 真粒子) | 小 | ⚠️ ①**繞過 which.ts**：分支直接在 PreviewPanel 另開一個 lazy import ⇒ `COLLECTIONS_WITH_3D` 從此不描述現實(第三守則,而且不會有東西紅)。②每開一份 a |
| 24 | 驗收情境：三具身體(含友方)+ 血魔預先布置 + before/after 全面量測 | 小 | ⛔ **第三具身體不可以灌回共用 sandbox**：main 的 sandbox 註解逐字寫著「任何額外實體都會讓 previewItem/previewAugment 的 tick 內容改變」—— 那兩支量的是純數值 |
| 25 | 可重現性：seed 欄位 + 逐 tick digest + 逐幀時間軸 UI | 小 | ⚠️ ticks 上限 1800 × 每 tick 一次 digest() + 事件深拷貝 ⇒ 推估最壞 ~0.8s 同步阻塞,UI 會頓一下(使用者觸發,可接受)。⭐ 但 CAST_PREVIEW_TICKS(main |
| 26 | 主動 / 被動的第三態(status: "passive") | 小 | ⛔⛔ **第三條判準是啟發式而且會綠著騙人**：「EX + cooldown 全 0 + 沒有 abilityCast」也正好是「一支應該能放、但因為某個閘擋住而沒放出去」的樣子 ⇒ 它會被標成「✓ 被動已掛載」。⭐ 合 |
| 27 | CI 的兩支安全掃描器用 `@latest` —— 一道判決會隨日期改變的閘 | 小 | ⭐ **真有風險，⛔ 不要單獨做**：pin 到 v2.28.0 會多出 G703（路徑穿越、taint 分析）那一條，而 main 沒有對應修正也沒有 #nosec 理由 ⇒ pin 完 CI 可能當場變紅。⇒ 要跟下 |
| 28 | testrunner 映像沒有 python3 ⇒ 兩支已註冊的 suite 在容器裡永遠跑不了 | 小 | 低但不是零：會長映像大小、多兩個相依（py3-pillow 從 apk、mpyq 從 pip）。⚠️ 這是 CI/dev 映像的相依變更，值得在 release note 講一句。⛔ 也要先做上一項的 .dockerig |
| 29 | 分頁切到背景 ⇒ rAF 整個停 ⇒ 回來第一格把整場排隊的爆炸一次放出來 | 小 | 低—中：新檔零衝突，GameApp.ts 只需加 30 行、0 刪除。⚠️ 真正的風險是語意選擇（回來不補播），那是一個會被玩家感知的決定 ⇒ 照第〇·六守則要做成一格後台開關並預設啟動。⚠️ 分支自承「仍然沒有實機試玩 |
| 30 | VfxSystem 的 pooled ParticleSystem free-list 沒有任何 reaper | 小 | 中：收得太積極會把還在用的 emitter 砍掉（畫面上特效中途消失），而那種缺陷在 NullEngine 測試環境裡量不到。⇒ 必須跟著 vfxLedger 的「只收擁有者不在的」規則一起搬，⛔ 不要只搬 reaper |
| 31 | setHealthPct effect —— 「引爆後自身生命固定變成最大生命的 1%」 | 小 | 低—中：機制本身很小（一個 effect kind + 一個 runner 分支），⛔ 但要一併決定它跟 invulnerable / manaBarrier / shield 的互動順序（設成 1% 的那一刻護盾算不算 |
| 32 | `targetProfileOverride` —— capabilities / active/target-profile / health 三條改 | 中 | ⛔ 量到一個缺陷：main 出貨的 editor-target-profile.json 沒有 `generatedAt` 鍵（keys 逐字列過，17 個裡沒有它），而 adaptTargetProfile() 寫 ` |
| 33 | editor-authoring 側車：GET/POST/PUT /content-api/editor-authoring/abilities/:id | 中 | ⛔ 硬阻擋：zAbilityReviewRecord 來自 packages/shared/src/content/authoring/abilityAuthoring.ts，main 上不存在（git grep 確認） |
| 34 | 遠端素材讀穿快取：/content-api/assets/* 在本機缺檔時去 contentBaseUrl 抓並落地 | 中 | ⛔ 三個：① contentBaseUrl 在 buildServer 這層沒有白名單（桌面版靠 normalizeRemoteSource 擋，但 content-api 是共用服務）⇒ 建議白名單下移；② 這讓一個  |
| 35 | remoteWorkspace.ts —— 遠端唯讀 Base 的下載／驗證／三方合併（676 行） | 中 | ⛔ 三個寫死的決策點違反第一守則：MAX_BUNDLE_BYTES=64MB、REQUEST_TIMEOUT_MS=20_000、以及白名單主機 "ggd.adms.ai" 直接寫在程式裡（換一台 staging 就要改 |
| 36 | Electron 殼本體：main.ts(300) + preload.ts(8) + package.json + tsconfig + deskto | 中 | ⛔ 兩個：① `allowProduction: true` 刻意繞過 content-api「NODE_ENV=production 拒絕啟動」的第一道鎖（打包後的 Electron 本來就是 production，所 |
| 37 | 第一行方括號標籤的機械稽核（未知標籤／重複／blocked 狀態／[主動][被動] 互斥／[輔助] 前提／[指定]vs[指向]／直線技必須有[指向][範 | 中 | ⭐ 規則本身安全，危險的是它接到哪裡（見下一列）。另外 TAG_ALIASES 只有兩筆，main 出貨的 [主動攻擊](129 支)/[主動傷害](11)/[變化](4)/[靈氣](4) 全部不在 manifest ⇒ |
| 38 | 模板／Product 的精確引用鎖：引用帶 {id, revision, contentSha256}，改模板後一鍵『同步精確引用』 | 中 | ⭐ 正解是把 revision+sha 加到 main 既有的 AbilityTemplateCard（可選欄位，向後相容），⛔ 不是為此引進整套第二模板系統。加上去要同時處理 denormalizeTemplateBi |
| 39 | 編輯決策面板：衝突來源（五層權威/Editor JSON/既有 Runtime）· 近似預覽 · Product 預設 scope · 原則警告處理（w | 中 | ⛔ 三個住處只有零個：住 localStorage ⇒ owner 在後台看不到也改不到，而且每個瀏覽器各有一份（第〇·四守則的反面）。⚠️ principleHandling:"strict" 會把 principle |
| 40 | auditAbilityPricing()：把四條硬界裡的三條「關係型」檢查真的實作出來（升階冷卻不可上升、MP 不可超過英雄魔力池、逐階陣列長度須為  | 中 | ⚠️ 接上去之後會對出貨內容吐出既有違規 —— 要先量一次母體再決定 hard 是否真的擋（main 的 authoring-rules.json note 逐字：「違反只警告不擋（owner 2026-08-12 的原 |
| 41 | 商店期畫面停繪 ⇒ 粒子與排隊的 burst 被冷凍，下一回合第一格用上一回合的世界座標滿亮度炸出來 | 中 | 中：8 個衝突檔全在 apps/client/src/vfx/* 與 GameApp.ts，而 main 這一個月大改過 VfxSystem.ts / Telegraph.ts / vfxPresets.ts。⭐ 新檔（ |
| 42 | applyBuff.valueFrom + stacksAreCount —— 「吃掉敵方英雄最大生命的 10%」這種可變單次數值 | 中 | 中：這一格會被寫進 StatsComp 的來源累加管線，而 main 的 modifiers.ts 這一個月大改過（grantRank、aura、hookLastFired 作廢規則）。⛔ 不要直接搬那個 diff —— |
| 43 | aimEnvelope 守衛 —— 每次跑測試自己重量一次滑鼠真正點得到的地面包絡線 | 中 | ⚠️ 那個 56 不是缺陷數 —— 包絡線本身要在 dolly 18 下重量，會大很多。⇒ ⭐ 建議不合併、改開一張票「在今天的鏡頭與乘數下重量包絡線並補上這道閘」，⛔ 並且明講「這張票的產出是一道閘，⛔ 不是一批改過的 |
| 44 | Product scope：成品可在『技能專用（host-local）』與『共用（shared）』之間雙向 promote / demote，且 pro | 大 | ⭐ 要落到 main 得先回答一個 owner 層級的問題：共用 Product 住在哪個 collection？今天沒有 effect-product@1 這個集合，也沒有 content:build 認得它 ⇒ 這是 |
| 45 | 後台改內容「存檔即下一場生效」——game-server 訂閱 content-overlay 並在無比賽時重載內容登錄表 | 大 | ⭐ 中高，三點：① ⛔ **不可以直接搬 contentLoad.ts** —— 它是 2026-07-27 從當時 index.ts 抽的，而 main 的 index.ts:366-497 之後長出了「帶 overl |
| 46 | HookDef.empower —— 「下一發普攻強化」在 resolveAttack 內結算，保住 origin:"basic" | 大 | 中—高：它動的是 resolveAttack 這條全遊戲最熱的路徑，而 main 這一個月在同一條路上加了 knockback 反轉、hitstop、facingLock、combo-strikes。⛔ 不要搬 diff |


---

# ⭐ 我的建議（⛔ 你逐項確認才執行）

## 🔴 第一：**⛔ 不要整包合**

近四成合過來會退步 ⇒ ⭐ 正確做法是**逐項 cherry-pick**，⛔ 不是 `git merge`。

## 🟡 第二：七項「兩邊各一半」要**手工合**

⛔ 一律**不可以** `--ours` / `--theirs` —— 任一邊都會吃掉對方的整條功能。

## 🟢 第三：分支較優的，**先做工作量「很小／小」的**

⚠️ ⭐ 每一項都要先看風險欄 —— 有幾項帶著**誤報**
（例：標籤稽核對 421 支出貨技能產生 **161 筆誤報**，因為別名表過期）。

---

# ⚠️ 誠實的界線

· 這 97 項是**六個 agent 分工比對 ＋ 六個對抗複驗**，⚠️ 複驗推翻了 **11 條**
· ⛔ 我**沒有逐項自己重驗** —— 你點頭要合的那幾項，我會在動手前**再驗一次**
· ❔ **5 項判不出來** —— 它們需要跑起來才知道

