# Client HUD & netcode — TODO

`apps/client`. Babylon imperative loop; React HUD via Zustand; prediction/interpolation; voxel
models with capsule fallback.

| ID | Item | Test ID | Category | Status |
| --- | --- | --- | --- | --- |
| client-01 | Local prediction reuses shared movement step | client-predict-shared | unit | done |
| client-02 | Reconciliation replays unacked inputs | client-reconcile | unit | done |
| client-03 | Remote entities interpolate ~100ms behind | client-interp | unit | done |
| client-04 | screenToGround maps cursor to (x,z) on y=0 | client-screen-to-ground | unit | done |
| client-05 | Input maps to IntentFrame order/command shape | client-intent-mapping | unit | done |
| client-06 | Capsule fallback renders before model loads | client-model-fallback | integration | done |
| client-07 | Animation state driven by authoritative flags | client-anim-authority | integration | done |
| client-08 | HUD reads only discrete-rate schema (no per-frame) | client-hud-discrete | unit | done |
| client-09 | E2E: join match → move → cast → see result | client-e2e-match | e2e | pending |
| client-10 | Reconnect within grace restores control | client-reconnect | e2e | pending |
| client-11 | Gamepad twin-stick maps to IntentFrame shapes | client-gamepad-mapping | unit | done |
| client-12 | Gamepad deadzone + button edge detection | client-gamepad-edge | unit | done |
| client-13 | Vfx docs map onto Babylon particle systems | client-vfx-doc-mapping | unit | done |
| client-14 | Clip playback: clone-prefix-tolerant clipMap resolution, event-driven pulse windows (cast/wind-up), run-rate ground-speed sync | client-anim-clip-playback | unit | done |
| client-15 | Ability bar strips the hero/skill number tag from the displayed NAME (`19-01 斷未`→`斷未`, `22-002 …`→`…`; un-tagged names unchanged; never emptied) while the tooltip keeps the full numbered name | hud-strip-ability-number | unit | done |
| client-16 | Floating ability/item tooltip placement: offset onto one side so the box never covers the anchor/cursor, edge-flips top↔bottom, and clamps horizontally inside the viewport (pure math) | hud-tooltip-placement | unit | done |
| client-17 | Augment-draft `resolveChoice` surfaces an ABILITY choice's w3x icon + description (item/weapon icons kept, icon-less choices fall back to the text card) | hud-draft-choice-icon | unit | done |
| client-18 | Minimap world→map projection: zone-union bounds, aspect-preserving scale, camera-yaw alignment (up-on-map = up-on-screen), team→dot color mapping, self-marker selection | hud-minimap | unit | done |
| client-19 | HUD corner layout is DECLARED, not hard-coded: every piece of corner chrome claims a `corner + order` slot in `ui/hud/hudLayout`, offsets/gaps/z-order + >=44px coarse-pointer targets come from the registry, panels dock past `hudStackEnd`, and guards reject a duplicated corner+order or a re-introduced literal corner pin | hud-corner-layout | regression | done |
| client-20 | Minimap PLACEMENT is registry-declared per pointer type: desktop bottom-right (LoL ~200-260px), touch top-left under the ☰/team stack (Wild Rift — bottom-right is the ability arc). Slots reserve a WIDTH as well as a height, and the guard proves the minimap rect stays inside the viewport and disjoint from every other slot at 667x375 / 812x375 / 852x393 / 1280x720 | hud-minimap-placement | regression | done |
| client-21 | Minimap camera VIEWPORT BOX: the real frustum intersected with the ground plane, derived from the rig's live target/dolly/pitch/yaw/fov/aspect (published by CameraRig.groundView, cross-checked against Babylon's own picking rays), tracking pan/zoom/yaw and degrading safely above the horizon | hud-minimap-camera | unit | done |
| client-22 | Minimap click→world: `mapToWorld` is the exact inverse of `worldToMap` at any yaw (left-click looks at that spot) and a right-click order clamps into the nearest zone instead of the void | hud-minimap-interact | unit | done |
| client-23 | Minimap TERRAIN background: zone discs + obstacles + spawn pads projected with the same transform as the markers, sub-unit props stay visible, and the bake is cached — repainted exactly on a map/size/dpr/yaw change and never per frame | hud-minimap-terrain | unit | done |
| client-24 | Minimap champion PORTRAITS: w3x icons resolved through the shared `championIconUrl`, loaded once, with stock-art/404/unknown champions settling permanently on the team-coloured dot fallback (no per-frame retry, never a blank marker) | hud-minimap-portrait | unit | done |
| client-25 | Minimap MARKER states: self is the largest marker with a white ring (isLocal fallback before the entity id arrives), allies/enemies wear the team ring, dead champions dim to a hollow ring and then stop drawing, and every size scales with the map | hud-minimap-markers | unit | done |
| client-26 | HUD panels DECLARE the edge they occupy (`HUD_PANELS` in `ui/hud/hudLayout`), so a docked panel and the chrome it covers are no longer invisible to each other: the left shop owns its edge and every covered slot yields per a `displaced` policy (hide / relocate), portal + overlay chrome are the only exemptions, and a guard proves no managed slot is left painting under the shop on 6 viewports × both pointer types (failing on the pre-fix layout — the FPS pill among the offenders) | hud-panel-cover | regression | done |
| client-27 | Minimap shows ONLY the LOCAL player's own duel zone (task #67): `zoneIndexAt` assigns a world point to its containing disc (nearest by rim gap in the void), `boundsForZone` narrows the mapped rect to that single zone, and `inLocalZone` filters entities so only the local 3v3's champions/flowers/revive-circles/terrain paint — a null local zone (spectating / pre-spawn) degrades bounds and filter alike to the whole-arena view | hud-minimap-zone | unit | done |
| client-29 | Build-stamped version badge (task #66): vite `define` bakes `VITE_BUILD_STAMP` (short git sha + date, no runtime git) and `VersionBadge` renders it `position:fixed` bottom-centre, low z-index, `pointer-events:none`, mounted via `GlobalChrome` so it shows on login AND in-match (screenshot-traceable) | version-badge | unit | done |
| client-33 | The build stamp SURVIVES THE CONTAINER (task #66, defect P0-6(a)): `dev/buildStamp.resolveBuildStamp` takes the stamp from `GGD_BUILD_STAMP`/`VITE_BUILD_STAMP` FIRST (the only source an image has — `.dockerignore` drops `.git` and node:22-alpine has no git binary, which is why every image ever built baked the plausible literal `"dev"` and two deploys were indistinguishable), then host git (with a `-dirty` marker, so two builds of one commit differ), and when nothing identifies the build the stamp is a loud `UNSTAMPED-BUILD` plus a stderr banner — never a believable label; `GGD_REQUIRE_BUILD_STAMP=1` makes it fatal | build-stamp-env | unit | done |
| client-34 | Global chrome exists in EVERY render tree (defect P0-6(b), shape S9): `main.tsx` renders `<AppRoot/>` normally but `<ReplayApp/>` on `#replay=`, and the audio quick-toggle (#14) and version badge (#66) were hand-mounted inside AppRoot only — so the replay page, the one screen the owner screenshots for playtest feedback, had no mute and no build identity; both are declared once in `ui/GlobalChrome` and the guard walks every `root.render(` site in main.tsx, failing on any tree that does not render it (or any unknown new tree) | global-chrome-every-tree | unit | done |
| client-30 | 3-choose-1 draft card is never a blind pick (task #110): a legendary WEAPON choice states what it DOES via `weaponEffectDescription`, which reads the SAME item content the shop shelf does (`itemStats.buildItemRow`) and joins the ✦ mechanical effect line with the merged, labelled stat bonuses — non-item choices (augment/ability, already carrying their own description) and bare items (no effect/stat → kept cost) fall through to `resolveChoice`'s text; the card renders name + this non-empty description + the real icon or the id-seeded glyph fallback | hud-draft-card-style | unit | done |
| client-28 | Description colour markup → SEMANTIC ROLES (task #114): every distinct w3x `cAARRGGBB` colour code in the source maps to a KNOWN role (damage/physical/duration/heal/mana/magic/generic — never "unknown") via `classifyRole` (override table + hue rule, byte-synced with the importer's `w3xlib/wts.py`), `parseRoleMarkup` splits `[c=role]…[/c]` into coloured/plain runs (unknown tag degrades to plain, no-markup string is a single plain run), each role renders as ONE normalised colour, and `docDescription` prefers the `descriptionRoles` field so game tooltips / codex / editor speak one palette | hud-desc-role-colour | unit | done |
| client-32 | Persistent in-match equipment bar (task #44): the LOCAL champion's owned items as a fixed six-cell row so the LoL slot CAP is visible (`equipmentCap` → `filled / cap` + `full`), unique items are FLAGGED and carry a 唯一 no-duplicate feedback chip, and each filled cell exposes the SAME detailed hover tooltip the shop shows (✦ effect + WC3 原文 claims + lore via the shared `itemStats.buildItemRow`, not a HUD re-derivation) through the shared `<Tooltip>`; it claims the `equipment` HUD slot (bottom-right desktop / top-right on the touch ability-arc pointer, task #107) so the safe-area guard proves the left-docked shop never covers it | hud-equipment-bar | unit | done |
| client-31 | Displayed numbers are the POST-MULTIPLIER final, not the base (task #125, 數字可信): `ui/displayFinal` is the ONE canonical helper — `displayFinal(base, factor, env)` returns `base × combat-env[factor]` (friendly aliases: damage→damageDealt, hp/health→maxHealth, mana→maxMana, regen→healthRegen, ad/ap; `"none"`/unknown → unscaled), `displayFinalText` formats + units it, and `useDisplayEnv` reads the live `MatchState.combatEnvJson` off the HUD store so every final tracks an operator's edit without a reload; `<Tooltip>` renders a meta chip carrying `{base, factor}` as the live final (冷卻 base 35s @ `cooldown:0.25` → 8.75s, never 35s — the 皮卡丘 十萬伏特 playtest bug), and `abilityText.abilityMetaChips` is the shared row builder both the ability bar and the champ-select skill profile feed so a cooldown can't disagree between them or with the sim (mana COST stays a literal — no env key scales a cost) | hud-display-final | unit | done |
| client-35 | Anything claiming to be GLOBAL is on EVERY surface (audit shape S9, generalised from defect P0-6(b)): `ui/surfaceParity.test.ts` DERIVES both sides at test time — surfaces from every `.render(<X/>)` in every `createRoot` file, every mutually-exclusive boolean HUD-variant branch pair in `HudRoot`, and every store action that sets `screen: "match"`; the global set from whatever `ui/GlobalChrome.tsx` renders; the claims from component docblocks that promise "on EVERY screen" — then asserts the cross-product, so a third entry point / fourth HUD variant / new match-entry action owes every ubiquity contract with no edit to the test. Still-open instances (phone + couch `CastNoticeLine`, 2 of 3 match-entry paths ungated by the #74 handoff) are a two-way KNOWN_GAPS ratchet: an unlisted violation fails AND a listed gap that got fixed fails ("delete the row"), so an allow-list can never go stale | ubiquitous-surface-parity | regression | done |
| client-36 | Ability COOLDOWN is readable at a glance on EVERY HUD surface (task #219, playtest 「技能冷卻進度不容易從圖示上看到」): the ROOT CAUSE was arithmetic, not paint — the sim charges `authored × (1-cdr) × combatEnv.cooldown` seconds and the shipped table has `cooldown: 0.2`, while all five inline copies of the sweep divided the REMAINING seconds by the RAW AUTHORED cooldown, so the progress fraction was mathematically capped at 0.20 (a ~10px band at the bottom of a 52px tile, inside the strip the ability-name scrim already owns — the indicator never left the scrim). One pure `ui/cooldownView` now owns ticks→seconds→fraction and is fed the ENV-SCALED max via the same `displayFinal(base,"cooldown",env)` seam the 冷卻 tooltip chip uses (#125), and one `<CooldownChrome/>` paints it: a conic-gradient RADIAL WIPE + flat dim (rotational + shrinking, so it can never be confused with the cast fill's bottom-anchored linear RISE, which the old rect shared geometry with), a shadowed/stroked `tabular-nums` number with one decimal under 3s (the old `Math.ceil` froze a "1" for a full second), and a 340ms ready bloom on a CHILD element (the tile's transform/filter stay the press + deny-shake channel). Parity is DERIVED from HudRoot's imports, not listed, so a 4th HUD variant owes the contract with no test edit — this is what caught the touch EX + touch 天生技 tiles shipping the dark rect with NO countdown number and the couch chip with NO progress fill at all | cooldown-legibility | regression | done |
| client-216 | The world-anchored overlay FOLLOWS THE ARENA (task #216, 「回到商店時…看得到戰場上的血條」): HP bars / names / cast bars / revive rings / floating numbers are DOM painted at projected world positions, so they only mean anything while the arena canvas under them is drawn. `GameApp` published `frameBus` every frame regardless of `setArenaRenderSuppressed`, leaving battle bars over the shop with nothing behind them. Both now run off one switch — no arena render ⇒ `clearWorldAnchors()`, which drops champions / reviveCircles / localCast / combat text while KEEPING the arena description (projection, zones, arenaId) so the minimap survives and combat re-entry is not a blank frame; idempotent and non-wedging | world-anchor-teardown | regression | done |
