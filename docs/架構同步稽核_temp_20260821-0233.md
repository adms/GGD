# 架構同步稽核（唯讀）—— 2026-08-21 02:33 CST

> owner 2026-08-21：「**我們架構已經改過了 你要同步開最大限度工作流來檢查跟更新**」
>
> ⛔ 這一份**只稽核，不修**（第零守則⑧：排序是 owner 的權力）。
> 每一條都給 **檔:行 + 影響**；零命中的軸也逐一寫出「查了幾個點」。

## 這一份是對著什麼量的

| # | 這一版改了什麼 | 稽核基準 |
|---:|---|---|
| 1 | 平衡母體 71 檔 → **49 位可選本體** | `packages/shared/testkit/balancePopulation.ts` |
| 2 | 上架名單 59（含 10 變身態）→ **49** | `apps/platform/internal/curation/starter.go` |
| 3 | 傷害錨 Lv18/LV50 → **LV30**，⛔ 不含 HP 倍率與魔抗減傷 | `balanceAnchors.ts` · `balanceAnchorsDerived.ts` |
| 4 | 傷害五級距 → **600/1500/3000/4500/6000** | `content/config/damage-tiers.json` |
| 5 | 屬性上限 → **基礎空間推導、讀取時乘一次 env** | `sim/statCapsDerived.ts` · `sim/statCaps.ts::capCeiling` |
| 6 | 級距靠攏在**註冊時**（tierSnap），內容 JSON 不動 | `packages/shared/src/content/tierSnap.ts`（⚠️ **未追蹤**） |
| 7 | pitchDeg 兩支產生器 → **單一寫入者** `build_pitch.py` | `packages/shared/src/ops/pitchDerived.test.ts` |

### ⚠️ 稽核當下的工作樹狀態（會影響你怎麼讀這一份）

另一條 workflow **正在飛**，稽核期間工作樹從「2 個異動」長到 **24 個**。
被它獨佔的路徑（`content/abilities` · `content/champions` · `tools/skill-remake` ·
`damageTiers/cooldownTiers/tierSnap/manaTiers*.ts`）我**一個都沒碰**。

⚠️ **所有 `content:build` 系的新鮮度閘現在跑起來都會反映那條 lane 的中途狀態，
⛔ 不是真的過期。** 實例：稽核中途 `pnpm mana:audit:check` 曾以
`ReferenceError: liveDepsFromConfigs is not defined`（`registries.ts:312`）失敗，
兩分鐘後同一條指令 **✅ 通過** —— 那是 lane 存檔到一半，⛔ **不是缺陷，我沒有把它列進發現**。

⇒ 我因此把可驗的閘限縮在**那條 lane 沒碰的**幾支，全部實跑過：

```
roster:check ✅   anchors:check ✅   statcaps:check ✅
lowdmg:check ✅   newhero:check ✅   pitch:check ✅   overview:check ✅
```

⭐ 母體本身我**從三份來源自己重算過一次**，不是讀報告：
`starterChampions 49 − retired 7(皆不在名單內) − alternate 20` ⇒ **49**；
`content/champions` 71 張卡 = 49 本體 + 20 變身態 + 2 骨架（sela/thorne）。**逐位元對得上。**

---

## ① 還在用舊母體的

**查了 72 個「掃英雄全體」的點**（`readdirSync(champions)` / `Champions.all()` /
`readCollection("champions")` / python `listdir|glob`），其中 43 個是測試與 probe 腳本、
7 個是 `tools/w3x-import` 的抽取器（讀 w3x 不讀名單，不在此列）。

### 🔴 1-A　`statNormalization` 的 30 個級距錨點，是**舊母體**量的，而且**沒有任何產生器或閘**

| | |
|---|---|
| 檔:行 | `packages/shared/src/content/statNormalization.ts:524`（母體宣告）· `:555-570`（11 個手寫錨點）· `:313` · `:572` |
| 現況 | 檔案自己寫著「**53 位本體**、作者原值、等級 99」；`content/config/stat-normalization.json` 的 note 也寫「統計母體從 **73** 縮到 **53**」 |
| 事實 | 母體現在是 **49**。`e75231a1` 的 commit message 逐字列出改成走 `balancePopulationIds` 的**六支**：`anchors · stat-caps · mana-audit · low-damage · newhero · hero-archetypes` —— **`statNormalization` 不在裡面** |

⭐ **證據（⛔ 不是推論）**：兩個檔宣稱在量**同一個量**（出貨母體在 L99 的純基礎中位數），

```
maxHealth   statNormalization.ts:559  8149.2   ==  balanceAnchorsDerived MEDIAN_BASE_HP[99]   8149.2   ✅ 逐位元相等
maxMana     statNormalization.ts:560  4985.5   !=  balanceAnchorsDerived MEDIAN_BASE_MANA[99] 4940.5   ❌ 差 45.0
```

血量那一格逐位元相等 ⇒ **兩者的量法是同一個**，所以魔力那一格的差**不是方法差異，是過期**。

**影響**：`bands` 驅動 `appliesTo` 的 **10 項屬性**（`ms/mr/armor/maxHealth/maxMana/ad/ap/healthRegen/manaRegen/range`）
的成長反解 —— ⭐ **每一位英雄的成長值都是從這 11 個錨點長出來的**。
它是「屬性級距」的最大消費端，而 owner 的裁決正是「**查所有屬性級距等 都是不考慮變身態的**」。
⛔ 而且**沒有 `statnorm:build` / `statnorm:check`** —— 母體再變一次，這 30 個數字照樣不會動，也照樣不會紅。

### ⚠️ 1-B　`gen_overview.ts` 拿 71 張卡當「出貨英雄總數」

| | |
|---|---|
| 檔:行 | `tools/innate-legendary/gen_overview.ts:581`（`champTotal = readCollection("champions").length`）· `:589` · `:591` |
| 印出去的 | 「出貨英雄總數（`content/champions/`）**71**」與「沒有天生技的英雄 = 71 − N」 |

**影響**：`overview:check` 是**綠的**（它只驗「重生成一次還一樣」），但那張統計表把
**20 個變身態 + 2 張骨架**算成英雄，「沒有天生技的英雄」這一格因此**被灌水**。
標籤誠實（有寫是 `content/champions/` 的檔案數），⛔ 但讀報告的人拿它當英雄數。

### ⚠️ 1-C　`statCapsAreFences.test.ts` 的母體是 71 張卡

`packages/shared/src/sim/statCapsAreFences.test.ts:24-35`（`readdirSync` 全收）· `:67`（`Champions.all()`）。
**這一條我判定是可接受的**：它驗的是「圍欄夾不到任何一張**出貨**的卡」，而變身態確實會上場 ⇒ 母體該是全體。
列出來只是為了讓 owner 知道它**刻意**不是 49。

### ✅ 已經跟上的（逐一確認過）

`tools/balance-anchors/gen.ts:115` · `tools/stat-caps/gen_stat_caps.ts:94` ·
`tools/mana-audit/gen_mana_audit.ts:139` · `tools/hero-archetypes/build.ts:90` ·
`tools/low-damage/gen.ts:28` · `tools/newhero/gen.ts:38` —— 六支全部走 `balancePopulation*`。
`docs/hero-archetypes.json` 實測 `champions` 長度 = **49** ✅。
`tools/roster-guard/check.ts` 從 5 條長成 11 條，其中 6 條就在守母體 ✅。

---

## ② 還在用舊數字的

| 目標數字 | 查的範圍 | 命中 |
|---|---|---|
| `1150 / 2875 / 5750 / 8625 / 11500` | 3,315 個 live 檔（`packages/ apps/ tools/ scripts/ content/config/ docs/editor-contract/ README CLAUDE package.json`，⛔ 排除 `docs/_daily/` `docs/legacy/` `tools/w3x-import/out/`） | **與級距有關的：0** |
| `13927 / 9048 / 3442 / 2792` | 同上 | **1**，且是**正確標成「舊的」**（`damageTiers.ts:18`） |
| `71 / 53 / 59` 當英雄數 | 同上 | **3**（見下） |
| `700` 當初始生命加成 | 同上 | **0**（出貨是 650，三個住處一致） |

⭐ 五級距重錨這件事本身**做得很乾淨**：`1150…` 這一族在 live 面上**零殘留**，
唯二提到它的兩處都寫成「2026-08-21 重錨把五格從 1150… 改成 600…」的歷史句
（`content/config/authoring-rules.json` 的 note · `damageTiers.ts:18`）—— 那是對的寫法。

### 🔴 2-A　後台欄位說明寫著「等級 18」與「73 位」

| | |
|---|---|
| 檔:行 | `apps/admin/src/configForms.ts:1567`（HEAD；工作樹現為 ~1608）與 `:1568`（~1609） |
| 原文 | 「⚠️ 這是**等級 18 的最終總值**」／「錨點 = **73 位**可達英雄在**等級 18** 的中位數」 |
| 事實 | `referenceLevel` 出貨是 **99**（owner 2026-08-13 逐字改的），母體是 **49** |

**影響**：這兩句是**操作者面前的欄位說明**，而且同一個檔在 `:1623` 自己寫著
「而實際出貨的是 **99**（owner 2026-08-13「我不要用等級 18 作為終值假設」）」——
**同一頁自相矛盾**。operator 照著「等級 18」去理解「裝甲 · 中 = 90.09」會差一個數量級
（L18 是 25.39，L99 是 131.36 —— 檔案自己在 `statNormalization.ts:415` 列著這兩組）。

### ⚠️ 2-B　出貨 config 的 note 還在說「53 / 73」

`content/config/stat-normalization.json` 的 `note`（三處）：
「統計母體從 **73** 縮到 **53**」「**53 位本體**在 L99 的攻速中位數是 12.2」「重量 **53 位可選本體**」。
**影響**：與 1-A 同源 —— 這是 operator 與 Codex 都讀得到的出貨文字。

### ⚠️ 2-C　`manaEconomy.ts` 的量測現況還錨在 Lv18

`packages/shared/src/sim/manaEconomy.ts:58`：「量到的現況（**Lv18** 中位，GH#446）：魔力池 **1,746** · 回魔 **36.6/s**」。
錨點已經是 LV30/50/99，而 `MEDIAN_BASE_MANA[30] = 1725.3`（× `MANA_ENV_MULT 1` + `600` = 2325.3）。
**影響**：只是散文（`refillSeconds` 的兩個讀者都不讀它），但它是這條規則**唯一的來歷說明**，
下一輪要重判斷「15 秒夠不夠」時會拿一個錯的池子去算。

### ⚠️ 2-D　`champSelectFilter.ts` 還在描述「白名單手動勾了 10 個變身態」

`apps/client/src/ui/panels/champSelectFilter.ts:184`：
「The operator's saved whitelist enables **TEN** alternate-form ids by hand」。
owner 裁決①之後那 10 個已經從白名單拔掉（59 → 49）。
**影響**：程式碼本身**是對的**（它把 alternate id **解析回本體**而不是丟掉，所以拔不拔都安全），
壞掉的只有那段說明 —— 而它正是下一個人判斷「這段還要不要留」的唯一依據。

---

## ③ 說了但不會發生的（第一·五守則）

### 🔴 3-A　傷害五級距與冷卻五級距，出貨內容裡**一支技能都沒在用**

在 **HEAD**（⛔ 不是被 lane 改動中的工作樹）掃 `content/{abilities,items,champions}`：

| 級距欄位 | 用到它的出貨文件數 |
|---|---:|
| `damageTier`（#447，五格 600/1500/3000/4500/6000） | **0** / 421 支技能 |
| `cooldownTier`（#445，15 格） | **0** |
| `distanceTier`（位移） | 5 |
| `radiusTier`（AoE） | 50 |
| `rangeTier`（施法距離） | 252 |

⭐ 兩條的整條鏈都**存在而且正確**：出貨 config ✅、Zod schema ✅、後台頁
（`configForms.ts:1307 DAMAGE_TIERS_SPEC`，說明全部從 `DEFAULT_DAMAGE_TIERS` 推導 ✅）、
唯一解析處 `registries.ts:265-279 withTiers` ✅。**只有「有沒有人填」那一格是空的。**

**影響**：這正是 configForms 檔頭第 1 條的形狀 —— 後台存得起來、卡片上印得出來、
`anchors:check` 綠、`content:build` 綠，而**遊戲裡一個數字都不會變**。

⚠️ **但這幾乎確定是「正在做」而不是「漏了」**：同一時間工作樹出現了兩支未追蹤的產生器
`tools/skill-remake/tierize.py` 與 `tools/skill-remake/apply_tiers.py`，它們就是在做這件事。
⇒ ⭐ **請 owner 在收 lane 的時候確認這兩個數字從 0 變成非 0**；如果 lane 沒收，這一條就是 🔴。

### 🔴 3-B　`tierSnap`（註冊時靠攏）目前**完全沒有接線**

| 檢查 | 結果 |
|---|---|
| `content/config/tier-snap.json` | **不存在**（`content/config/` 只有 aoe/cooldown/damage/displacement/range 五份 tiers） |
| `config.tier-snap@1` 在 Zod union | **0 命中**（`schema/config.ts` 完全沒有它） |
| 後台頁 | **沒有** |
| production 呼叫端 | **0** —— `registries.ts:265-279` 的 `withTiers` 只串了 radius/range/displacement/damage/cooldown，⛔ 沒有 `applyTierSnap` |
| git 狀態 | `?? packages/shared/src/content/tierSnap.ts`（**未追蹤**） |

**影響**：架構事實 #6 說「級距靠攏發生在註冊時」——**今天它不發生**。
⚠️ 同時這是 2026-08-02 事故的形狀之預告：`tierSnap.ts` 是**未追蹤**的來源檔，
一旦有人在它落版控之前跑 `content:build` 並 commit 產物，就會重演「產物有、來源沒有」。
⇒ ⭐ **這是另一條 lane 的在飛工作，⛔ 我沒有動它。** 列在這裡是為了讓 owner 在收 lane 時確認它真的接上。

### ✅ 其餘新級距欄位：逐格查過消費端，**零孤兒**

| 欄位 | 消費端 |
|---|---|
| `damage-tiers.enabled` · `.damage.{五格}` | `damageTiers.ts:326 resolveDamageTier` ← `registries.ts:265` |
| `cooldown-tiers.enabled` · `.autoShape` · `.seconds.{單體,範圍,變身}×5` | `cooldownTiers.ts:184 cooldownShapeOf` · `:208 resolveCooldownTier` |
| `mana-economy.refillSeconds` | 兩個讀者，⭐ 而且**寫在 schema 裡**（`schema/config.ts:5276`）：`enforceFloor` 開著時的地板算式 + `mana:audit` 的門檻。出貨 `enforceFloor:false` 是 owner 2026-08-20 親自降級的 |
| `authoring-rules.proportionality.{model,expectedHits,aimRiskMult,minDamageTier}` | `proportionality.ts:278/287/303` 四個 model 各讀一組；`authoringRules.ts:443-445` 接進去 |
| `stat-caps.caps.*` | `statCaps.ts:257 capCeiling`（⭐ 讀取時才乘 env，基礎空間存） |

⚠️ 一個**已知且已記錄**的舊例外（⛔ 不是這一版造成的）：`config/round-grade.json` 的
`roundGradeFromDoc` 在 production 沒有呼叫端 —— configForms 檔頭第 1 條自己寫著它，
所以它**刻意沒有後台頁**。狀態未變。

---

## ④ 產生器沒接進聚合閘的

### ✅ `*:check` 這一軸：**零命中**

掃 root + 所有子專案的 `package.json`（`git ls-files package.json **/package.json`），
共 **28 支** `*:check`。逐支比對 `skills:check` 與
`skillsSyncCoversGenerators.test.ts` 的 `EXEMPT` 表：

```
不在 skills:check 也不在豁免表的：(none)
```

⭐ 那條閘是有效的。

### 🔴 4-A　但閘只問「每一支 check 有沒有被跑」，⛔ 沒問「每一支產生器有沒有 check」

`archetypes:build`（`tools/hero-archetypes/build.ts`）在 `skills:sync` 裡，
產出 **`docs/hero-archetypes.json` + `docs/hero-archetypes.md`** ——
而 **`archetypes:check` 根本不存在**。

| | |
|---|---|
| 檔:行 | `package.json:84`（`archetypes:build`，⛔ 沒有配對的 `:check`）· `skillsSyncCoversGenerators.test.ts:78`（只枚舉 `k.endsWith(":check")`） |
| 為什麼閘看不到 | 它列舉的是 **check**。一支「有 build、沒有 check」的產生器對它是**不存在的** —— 正是它自己檔頭在防的那個形狀 |
| 現況 | `docs/hero-archetypes.json` 實測 `champions` = 49 ✅（**這一刻是新鮮的**），而且 `roster:check:390` 有守它印出來的母體數 |
| 缺口 | 母體數以外的每一格（出身判定、初始/成長、卡面倍率）**改了不重生成，沒有任何東西會紅** |

⭐ `tools/hero-archetypes/build.ts:13` 自己剛承認過同型的謊：
「⚠️ 這裡在 2026-08-21 之前寫著『守衛 `tools/hero-archetypes/build.test.ts` 會重跑這支⋯』」——
⇒ 舊的假守衛被拔掉了，**但沒有補上真的那一個**。

（`content:build`／`items:csv:export`／`champions:csv:export` 同樣沒有配對 `:check`，
但前者有 `shippedBundleIsCurrent.test.ts` 逐位元組守著，後兩者是 owner 手動匯出入口 ⇒ ⛔ 不列。）

---

## ⑤ 註解說謊的（第三守則）

方法：掃全 repo（⛔ 排除 `docs/_daily/` `docs/legacy/` `tools/w3x-import/out/` `.claude/`）
所有形如 `xxx.test.ts` 的引用，逐個比對 `git ls-files`；再對每個「不存在」的名字做
token 相似度比對，把「其實只是改名」與「真的沒有」分開。
**共 54 個死引用**，其中 **12 個連相似名字都沒有**。

### 🔴 5-A　`statNormalization.ts:18` 宣稱有一條不變量守衛，⛔ 而那個檔不存在，⭐ 而且真正的測試**明文否認**它在驗

```
statNormalization.ts:17  ③ **佔用例外槽要付代價**：任一屬性落在極大 ⇒ 至少有一項落在極小。
statNormalization.ts:18     這是一條**不變量**（`statNormalizationInvariant.test.ts` 在守），不是一份觀察清單。
```

- `statNormalizationInvariant.test.ts` —— **不存在**。
- 唯一相似的 `packages/shared/src/content/statNormalization.test.ts:9` **逐字寫著**：
  「⛔ 也**不驗人數分佈** —— owner 2026-08-12：『極大極小就是為了極端例外而誕生⋯』」

**影響**：這是四層自洽地一起錯的那個形狀 —— 規格說它是不變量、註解說有守衛、
守衛檔名看起來很合理、而實際的測試明文說它不驗這個。⛔ 那條規則今天**沒有任何東西在守**。

### 🔴 5-B　`statNormalization.ts:537` 宣稱上限鏡射由 `statCapsAreFences.test.ts` 守著，⛔ 它不驗這個 —— 而鏡射**確實已經過期**

```
statNormalization.ts:536  // ⚠️ 第二個參數是那條屬性的**系統上限**（`config.stat-caps@1` 的 base）。
statNormalization.ts:537  //   ⛔ 這是一份鏡射，而 `statCapsAreFences.test.ts` 對照真的那張表 —— 說謊就紅。
```

`packages/shared/src/sim/statCapsAreFences.test.ts` **存在**，但它驗的是
「每一張出貨英雄卡都在圍欄內」（檔頭 `:5` 逐字：「⛔ 這裡**不驗那九個數字是多少**」）——
它**從來沒有 import `statNormalization`**。

**鏡射的實際狀態（`statNormalization.ts:557-570` vs `content/config/stat-caps.json`）：**

| 屬性 | 鏡射寫的 | 出貨 `stat-caps.json` base | |
|---|---:|---:|---|
| `mr` | 15344 | **12560** | ❌ |
| `armor` | 5078 | **7939** | ❌ |
| `maxHealth` | 375960 | **561720** | ❌ |
| `maxMana` | 232150 | **345050** | ❌ |
| `ad` | 21200 | **20948** | ❌ |
| `ap` | 100000 | **500000** | ❌ |
| `healthRegen` | 744 | **1134** | ❌ |
| `manaRegen` | 926 | **3271** | ❌ |
| `as` | 4 | 4 | ✅ |

**影響（⚠️ 誠實的版本）**：**今天沒有一格數字因此算錯** —— `bandsFromMedian` 只在
`中位 × 2 > cap` 時才動用 cap，而九項裡只有 `as`（7.34 × 2 = 14.68 > 4）會踩到，
而 `as` 那一格剛好是對的。⇒ 這是**一顆被拔掉引信的地雷**：
`statcaps:build` 下次重量（母體或倍率一動就會）如果把某條上限壓到中位數 ×2 以下，
它會**靜靜地**用一個過期的柵欄去錨梯子，而註解指著的那條守衛不會紅。

### ⚠️ 5-C　`configDrift.test.ts` —— 三處引用，檔案不存在

`packages/shared/src/content/statNormalization.ts:476` ·
`packages/shared/src/content/aoeTiers.ts:75` ·
`packages/shared/src/sim/cooldownRules.ts:55`
（三處都寫「`configDrift.test.ts` 那一族在守（第一守則的三個住處）」）

**影響**：三個住處的漂移**確實**有守（每份 config 各有自己的 shipped 測試），
所以⛔ 不是機制缺失；壞的是**指路**。三處指向同一個不存在的名字 ⇒ 下一個人會以為沒有守衛。
⭐ 我實測過三個住處**目前一致**（`stat-normalization.json` 的 30 個 band 與
`DEFAULT_STAT_NORMALIZATION` 逐格相等）。

### ⚠️ 5-D　其餘 9 個「連相似名字都沒有」的死引用（⛔ 與這次架構改版無關，列出來給 owner 排）

| 引用的檔（不存在） | 宣稱它的地方 |
|---|---|
| `arenaRules.reviveHpDefaults.test.ts` | `packages/shared/src/sim/effects/revive.ts:72` |
| `championGrowthLayers.test.ts` | `packages/shared/src/content/schema/champion.ts:215` |
| `content/legendaryItemSets.test.ts` | `packages/shared/src/content/schema/item.ts:423` · `sim/economy/itemSets.ts:168` |
| `content/legendaryCritStrike.test.ts` | `packages/shared/src/content/schema/item.ts:573` |
| `nightPact.test.ts` | `packages/shared/src/sim/deathWard.ts:101` |
| `shippedReplayPolicy.test.ts` | `packages/shared/src/content/replayPolicy.ts:41` |
| `victoryCrown.test.ts` | `apps/client/src/render/victoryCrown.ts:24` |
| `localRenderPose.test.ts` | `apps/client/src/predict/localRenderPose.ts:7` |
| `familyContent.test.ts` | `apps/client/src/render/vfx/generateFamilyContent.ts:37` |
| `bake.test.ts` | `packages/shared/src/voxel/bake.ts:23` |
| `botBuildPriorityRetired.test.ts` | `apps/admin/src/contentFields.ts:106` |
| `publicFeedReaders.test.ts` | `apps/client/src/ui/platform/announcements.ts:17` |

另有 ~30 個是**改名**（例：`familyTuning.test.ts` → `familyTuningDegrade.test.ts`、
`voxelBarcodeRender.test.ts` → `voxelBarcode.test.ts`、`sim/effects/reviveEffect.test.ts` → `sim/revive.test.ts`），
守衛還在、只是名字對不上。

### ✅ 查過但**不是**謊的（⛔ 免得誤傷）

- `packages/shared/src/ops/pitchDerived.test.ts:4-5` 提到 `vfxOrientDerived.test.ts` 與
  `slashPitchFromAnimFresh.test.ts`（兩個都不存在）—— ⭐ 但它寫的是「這一支**取代**了那兩個檔」，
  **歷史敘述是對的**。架構事實 #7（單一寫入者）實測 `pitch:check` ✅。
- `damageTiers.ts:18` 的 `500/1250/2500/3750/5000` 與 `9048` 都明寫成「舊的」。
- `content/config/authoring-rules.json` 的 note 提到 `1150…` 時明寫成「2026-08-21 重錨把五格從 1150… 改成 600…」。

---

## ⑥ ⭐ 分級：哪幾個是 🔴（現在就在造成錯誤數字／現在就有事沒發生）

| # | 發現 | 檔:行 | 為什麼是 🔴 |
|---|---|---|---|
| 🔴1 | **`statNormalization` 的 30 個級距錨點是舊母體量的，且無產生器無閘** | `statNormalization.ts:524,555-570` | 它是「屬性級距」最大的消費端（10 項 × 每位英雄的成長反解），而 `maxMana 4985.5` 已經與新母體的 `4940.5` 對不上。owner 的裁決字面就是「查所有屬性級距等 都是不考慮變身態的」。⛔ 六支被修的量測**沒有包含它** |
| 🔴2 | **傷害／冷卻五級距在出貨內容裡 0 使用** | HEAD `content/{abilities,items}`：`damageTier` 0/421 · `cooldownTier` 0 | 整條鏈都在，只有「有沒有人填」是空的 ⇒ 後台改五格、卡片印五格、遊戲裡零效果。⚠️ **另一條 lane 正在補（`tools/skill-remake/tierize.py`）—— 收 lane 時務必確認它從 0 變成非 0** |
| 🔴3 | **`tierSnap` 完全沒接線，而且來源檔未追蹤** | `packages/shared/src/content/tierSnap.ts`（`??`）· `registries.ts:265-279` 沒有它 | 架構事實 #6 說「靠攏發生在註冊時」，今天不發生。⚠️ 未追蹤來源 + `content:build` = 2026-08-02 事故的形狀。⭐ 同樣是在飛的 lane，收 lane 時確認 |
| 🔴4 | **後台欄位說明寫「等級 18 / 73 位」，同一個檔 55 行後自己說是 99** | `configForms.ts:1567-1568`（HEAD） | operator 面前的文字，且**自相矛盾**；照它理解「裝甲 · 中 = 90.09」會差一個數量級（`statNormalization.ts:415`：L18=25.39 / L99=131.36） |
| 🔴5 | **`statNormalizationInvariant.test.ts` 不存在，而真正的測試明文否認在驗它** | `statNormalization.ts:18` ↔ `statNormalization.test.ts:9` | 一條被當成「不變量」的規則，今天**零守衛**，而四層文字互相背書說有 |
| 🔴6 | **上限鏡射 8/9 格過期，指著的守衛不驗它** | `statNormalization.ts:536-537,557-570` vs `content/config/stat-caps.json` | 今天不改變任何數字（只有 `as` 會踩到 cap，而它是對的），⛔ 但下一次 `statcaps:build` 重量就會靜默錯，且不會紅 |
| 🔴7 | **`archetypes:build` 沒有 `:check`，聚合閘結構上看不到它** | `package.json:84` · `skillsSyncCoversGenerators.test.ts:78` | 閘問的是「每支 check 有沒有被跑」，⛔ 不是「每支產生器有沒有 check」。⭐ 那支檔案自己剛拔掉一條假守衛（`build.ts:13`）卻沒補真的 |

### 🟡 其餘（散文／指路，⛔ 不影響任何一場比賽）

`stat-normalization.json` note 的「53/73」· `manaEconomy.ts:58` 的 Lv18 池子 ·
`champSelectFilter.ts:184` 的「TEN alternate ids」· `gen_overview.ts:581` 的 71 ·
`configDrift.test.ts` ×3 · 其餘 ~40 個死引用。

---

## ⭐ 一句話結論

**級距重錨（#447）與母體修正（#485）本身做得很乾淨** —— 舊數字在 live 面上零殘留，
六支量測全部改成走同一個母體模組，`roster:check` 從 5 條長成 11 條在守它。

**沒跟上的是同一個母體的第七個消費端：`statNormalization`。**
它握著十項屬性的級距錨點、驅動每一位英雄的成長反解，
而它 ① 用舊母體、② 沒有產生器、③ 沒有閘、④ 註解指著兩條不存在或不驗它的守衛、
⑤ 後台說明還停在「等級 18 / 73 位」。**五個問題是同一個根：它從來沒有被納入那六支。**

再加上兩件「在飛」的（傷害/冷卻級距 0 使用、tierSnap 沒接線）——
⭐ **那兩件請在收 lane 的時候當成驗收條件，⛔ 不要當成已經完成的架構事實。**
