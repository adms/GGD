# BGM 版本備份 20260822-0235

owner 2026-08-22：「**舊的歌不要刪除，移到 legacy 備份就好 不要直接取代**」

這一份是 owner 逐句指定名言（芙莉蓮／天空鬥技場／無限城／納薩力克／世界樹核心）
**之前**的狀態，在重算覆蓋前留的。

| | |
|---|---|
| `bgm/` | 13 首場地曲的當時版本（mp3，逐位元組） |
| `vox/` | 當時的 CosyVoice 名言 wav + `lines.json` |

⛔ 這不是「以防萬一」的雜物：`render.py` 寫 `content/assets/audio/bgm/<id>.mp3` 是
**直接覆蓋**，而 PreToolUse 的覆蓋備份 hook 只攔 Write 工具與 bash 的 `>` 重導向 ——
⛔ 攔不到 Python 自己開檔寫入。所以這一層必須是 render.py 自己做的事，見
`tools/bgm-gen/src/render.py` 的 `archive_previous()`。

當時的名言（全部被 owner 指定的新句取代）：
- 芙莉蓮「人を殺す魔法」· 天空鬥技場「纏 絶 練」· 無限城「全集中の呼吸」
- 納薩力克「至高の御方」· 世界樹核心「リンク・スタート」
- 希干希納「心臓を捧げよ」與大聖杯「令呪をもって命ずる」**未變**
