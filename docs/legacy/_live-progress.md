# GGD 即時進度看板

> 每有工作流回報就更新這一份。**最後更新:2026-07-26**
> 主線:執行批次計畫 + 鑄技工坊 + 掃描對齊技能效果/特效 + 編輯器 → 充實內容與玩法

## 🚀 已上線 ggd.adms.ai
| 版本 | 內容 | 狀態 |
|---|---|---|
| v0.5.10 | #215 肉鴿殭屍房間開關(預設開)、51 英雄語音、揍敵客、名言、移速、後台內容管理 | 🚀 已部署 |

## ✅ 已推送、待部署
| 版本 | 內容 |
|---|---|
| v0.5.12 | **content-bus 修復** — 後台改內容終於即時生效(game 容器沒設 REDIS_ADDR,一直連到自己) |
| v0.5.13 | 大廳 BGM −30%、**全體 BGM −20%**;技能特效對齊**修 12 支錯綁** |
| (未 tag) | **語音 Tier 1** — 防污染(同句不重疊+隨機+跳過不燒冷卻)+ 9 類語音接線 |

## 📊 兩個「以為做完、其實沒有」的實測真相

### 1. VFX 真實引用(#230)
| | 數量 |
|---|---|
| 已抽取的**原作特效** `fx.w3x.*` | 118(粒子 66 / 球體 40 / 蝗蟲群 12) |
| 其中真的被技能引用 | **12** |
| 🔴 抽出來卻**完全閒置** | **106(90%)** |
| 技能綁**自製通用替身** `fx.prim.*` | **578(93%)** |
| 技能綁**原作真貨** | 25(4%) |

→ 抽取(#182/#183)成功了,但**「把技能指回它原本那顆特效」從沒做**。

### 2. 技能預告特效(#228)
| castType | 格數 | 現況 |
|---|---|---|
| ground | 44 | 43 OK |
| **targeted** | **93** | 🔴 **畫出會騙人的圈**(單體技卻畫範圍,教錯誤閃避) |
| self | 90 | ⚪ 地上全無 |
| skillshot | 22 | ⚪ 全無(投射物還要等施法完才生成) |
| dash | 6 | ⚪ 全無 |

→ **誠實覆蓋率 43/255 = 16.9%**。且敵我預告**同一個琥珀色**,和自己的範圍預覽撞色 —— 這就是「不明顯」的直接原因。

## 🔎 工作流挖到的根因(已定位、修復進行中)

| # | 真正的根因 |
|---|---|
| **#222 手把框看不見** | 選取框**一直都在**(`PadFocusNav.tsx` 注入 `outline:3px solid #7aa2ff` + 外發光),但 `.ggd-btn` 的 **`clip-path` 缺角造型把 outline 與外發光整個裁掉**。修法必須畫在**邊框內側**(inset 陰影 + 遮罩漸層偽元素),不能用 outline。 |
| **#228 預告特效** | 誠實覆蓋率 **43/255 = 16.9%**;**93 格單體技畫出「會騙人的範圍圈」**;敵我預告**同一個琥珀色**、還跟自己的範圍預覽撞色。`Telegraph.ts` 目前**只會畫圓**,且**不聽 castInterrupt**(被打斷還在填、還會放落地特效)。 |
| **#216 商店殘留** | 🔴 **三個症狀同一個根因,而且比回報更嚴重**。實測(真 MatchController × 5 seeds):商店階段本身乾淨(0 掉血)。真兇在前一階段且是**分區**問題——`combatActive` 是**全域**旗標,只有「所有分區對決都結束」才關;所以**你打完後火圈還在燒你**(實測存活者 161→39 HP 燒死),而對面那區還在打。加上 `shopGate` 刻意讓「本回合已倒下者」**在戰鬥中就開商店** → 商店蓋在活的戰鬥上 → 火圈聲響、血條浮、血還在扣。<br>另外火圈音效是 **60 秒的 one-shot 且 AudioSystem 根本沒有停止 SFX 的 API**(只有 stopBed 給 BGM);`isLoopableSfx` 是**死的中繼資料、零呼叫點**。修法:把「這一區已分勝負」升級成**被記錄的 sim 狀態**(進 replay digest)才不破壞重播。 |
| **#225 水晶發放** | 現行「M幣 發放」頁的端點**完全沒寫稽核**(另一條 PlayersPage 路徑才有);水晶**只有絕對值設定、沒有累加函式**(naive 實作 = 競態);`BackfillWelcomeCrystals` 在正式機會**發給 0 個帳號**(冪等規則是「已有 walletmeta 就跳過」)。 |
| **#229 體素工坊** | 後台**已能寫 models 集合**(server 端 COLLECTIONS 有 model@1),缺的只是 client 端 `EditCollection` 沒列 models;另 `model@1` **硬性要求 glbPath**,程序化模型要處理。 → **已解**:`EditCollection` 加 `models`;glbPath **不放寬**,改成由 id 推導 `assets/models/voxel/<id>.glb` 並由離線 `pnpm voxel:gen` 烘焙——後台只寫參數 JSON,**零二進位寫入**,content-api 圖片白名單不必動。 |
| **鑄技工坊 P1** | 關鍵換算定出來了:**1 GGD 單位 ≈ 54.5 WC3 單位**(三次交叉驗證);JASS 的實測參數是**文字**(如 "AoE 480; 傷害 200+150×lvl"),模板產生器必須**解析字串**而非讀數值欄位。 |

## 🔄 進行中的工作流
| # | 項目 | 階段 |
|---|---|---|
| 鑄技工坊 P1 | 技能模板編輯器 #141 | 建置 |
| 試玩修復 6 軌 | #216 商店殘留 · #217 喪標麥可 · #218 柱子 · #219 冷卻UI · #220 升天 · #221 自動攻擊 | 修復/驗證 |
| #222 | 手把選取炫光 | 進行 |
| #224 | 天生技 48 支空效果 | 進行 |
| #225 | 後台發水晶(單筆+一鍵全帳號) | 進行 |
| #226 | 方塊人替換 4 個高面數角色 | 進行 |
| #229 | 體素角色生成器網頁「鑄形工坊」 | **核心+頁面已完成**(feat/voxel-character-studio)；待 #226 merge 對帳 |
| #230 | VFX 真實引用普查 + 回綁 | 進行 |
| #227/#228 | 商店名稱幣別 + 預告特效 | **重開**(前次撞額度上限) |

## 🎯 已定案的規則
- **英雄解鎖 = 水晶 ◆ / 造型 = M幣 Ⓜ**(owner 2026-07-26)。目前大廳商店英雄標成 `Ⓜ300` 是**幣別接錯**。
- 語音防污染:隨機發、**同一句不同時播**。
- 方塊人:**不得使用任何 Mojang 素材**,自產幾何與貼圖。
- 部署仍由 owner 本人確認;每次 push 都要 release note(有部署要標記)。

## 📋 待辦編號
#216 商店殘留 · #217 喪標麥可 · #218 柱子 · #219 冷卻UI · #220 升天動畫 · #221 自動攻擊 · #222 手把炫光 · #223 敵人受傷/死亡語音 · #224 天生技落地 · #225 水晶發放 · #226 方塊人 · #227 商店名稱幣別 · #228 預告特效 · #229 體素工坊 · #230 VFX 真實引用
