# GH#1054 `chanceFrom` 補常數項 `flat` —— lane 報告（2026-09-06）

> 狀態：**完成（機制）／鏈路已接上，⛔ 未驗收（玩家側）** —— 96 令狐沖今天停在 `content/_legacy/`（owner 2026-08-13 下架未開放英雄），所以玩家在任何一場裡都碰不到華山劍法；機制本身由承重守衛 `packages/shared/src/sim/effects/hookChanceFlat.test.ts` 3/3 綠、突變紅（§4）證明。
> ⛔ 本 lane 沒有 commit／沒有跑任何 `*:build`／沒有動 `content/champions/**`。

## 1. 最重要的一個發現

⭐ **票文的兩個「客戶」今天一個在封存區、一個已經不是這個 key 的客戶。**

| 票文說 | 量到 |
|---|---|
| 「`content/abilities/godie-o02w.q.json`」 | ⛔ **營運樹裡不存在**。整位英雄 96 令狐沖（`godie-o02w`，w3x `O02W` 笑傲江湖）在 `content/_legacy/{champions,abilities}/`（41 位下架英雄之一）。營運樹 72 位／422 支裡 `grep -l chanceFrom content/abilities/` = **0**；全 content 唯一用 `chanceFrom` 的是道具 `content/items/godie-i018.json`（朗基努斯之槍） |
| 「06-04 傑桑變化的 hook 機率改用 flat」 | ⛔ **不做，而且做了會錯**：#1020 逐行讀 JASS（j:27155–27295 `Trig_XHunter`）量到 hook 本身是 `GetRandomInt(1,5)==1`＝固定 20%（`chance: 0.2`），「(5+敏捷/10)%」是**出石頭那一支的權重**（j:27215 `Trig_XHunter_Func005C`），已以 `weightedBranch.weightFrom` 落地。改成 `chanceFrom.flat` 會把**兩次擲骰翻成一次** |

⇒ 機制照票做（`flat` 進 schema／型別／`hookProcChance`）；華山劍法改在**封存區那一份**，並成為 fieldAdoption 那一列的 `legacy-parked` witness。

## 2. 做了什麼（逐檔）

**柵欄內**
- `packages/shared/src/content/schema/effects/_hook.ts`：`chanceFrom.flat`（選填，0..1，`.describe("@zh …")`）；`refineHookDamageContext` 加一條 `flat > max ⇒ 拒絕`（與 `min > max` 同族：係數永遠沒作用的靜默 typo）；`refineHookDamageContext` 的參數型別補 `flat?`；JSDoc「沒有常數項」那一段改寫（它已經不是事實）。⛔ `min`/`max` 語意沒動，仍夾整個和。
- `packages/shared/src/sim/effects/hooks.ts::hookProcChance`：`clamp((from.flat ?? 0) + live × coeff, min, max)`。⭐ 缺席逐位元等於今天：`flat` 缺席 ⇒ `+0`；非英雄身體（讀不到三圍）以前回 `min`，現在 `clamp(0 + 0)` = `min`（`min ≤ max`、`min ≥ 0` 由 schema 保證）。抽籤的次數與位置一個位元都沒動。
- `content/_legacy/abilities/godie-o02w.q.json`（genguard ✓ 沒有擁有者）：從「12 秒冷卻／60 魔的**主動**傷害技 ＋ armor+20 自身 buff」重寫成 JASS 逐行翻譯（§3）；`provenance: "jass-verified"`；`zAbilityDoc.parse` 通過（strict）。
- `packages/shared/src/content/fieldAdoption.exemptions.json`：`field:abilities.effects[]#applyBuff.hooks[].chanceFrom` 那一列 `debt` → **`legacy-parked`**，`witness: content/_legacy/abilities/godie-o02w.q.json`；`why` 重寫（含傑桑不再是客戶的理由）。⚠️ census 現在多印一列 `…chanceFrom.flat ZERO 0/1`，reach 1 低於閘的門檻 ⇒ **不需要**豁免列（同 `chanceFrom.attr=int` 那幾列）。
- `packages/shared/src/sim/effects/hookChanceFlat.test.ts`（新，≈125 行；實作側新增 ≈96 行 —— 比值 1.3×，超出「測試 ≤ 實作」一點，多的是 21 行 import 與 registry boot；⚠️ 主 session commit 前自檢那一行會把它算進去）。

**⚠️ 柵欄外但型別鏡射必要（主 session 請過目）**
- `packages/shared/src/sim/stats/modifiers.ts`：`HookDef.chanceFrom` 補 `flat?: number`（+6 行，含 JSDoc）。`HookDef` 是手寫型別、⛔ 不是從 Zod 推導 —— 沒有這 6 行 `hooks.ts` 讀 `from.flat` 就是 tsc 紅；用 `as` 繞過會是失敗形態⑧的那種靜默洞，所以我動了它。

## 3. JASS 出處（逐字，⛔ 不是近似）

`tools/w3x-import/out/GoDieEX22s-src/raw/war3map.j` **j:44815** `Trig_HuashanSword_Actions`（事件 `EVENT_PLAYER_UNIT_ATTACKED`，j:44829）：

| JASS | JSON |
|---|---|
| `set udg_LHC_RandRang = ( 5 + ( GetHeroStatBJ(bj_HEROSTAT_AGI, udg_LHC_Hero, true) / 15 ) )`，`GetRandomInt(1, 100) <= udg_LHC_RandRang`（j:44809） | `chanceFrom{attr:"agi", basis:"total", flat:0.05, coeff:0.00066667 (=1/1500), min:0, max:1}` |
| `UnitDamageTargetBJ(…, 10.00 × I2R(GetUnitAbilityLevelSwapped('A0XS',…)) + I2R(GetHeroStatBJ(AGI,…,true)), ATTACK_TYPE_NORMAL, DAMAGE_TYPE_MAGIC)` | `damage{damageType:"magic", amount:{perRank:[10,20,30,40], attrRatios:[{attr:"agi", basis:"total", coeff:1}]}}` |
| w3a `A0XS` base `Asth`（被動，4 級） | `effects: []`＋`passive.ranks[0].hooks[0].on:"onBasicAttack"`（G4：payload 以 `grantRank` 讀 `perRank`，一個 rank 區塊夠） |

⚠️ 誠實留白：JASS 的 `/ 15` 是**整數除法**（floor），卡面寫的是連續的「敏捷/15」。第〇·六守則第 1 層（owner 卡面）> 第 3 層（JASS）⇒ 用連續係數；差最大 1 個百分點以內。
⚠️ 被取代的舊文件值（`cooldown 12`、`manaCost 60`、`applyBuff armor+20 8s`、`ratios ap×0.03`）在 JASS 與 w3a 裡**都沒有出處** —— 是早期匯入器的模板預設（CLAUDE.md「推測寫進模板家族預設」那一族），⛔ 不進 superseded（那份存的是**原作**數值）。舊檔副本在 `docs/legacy/_overwrites/` 的自動留底。

## 4. 測試與突變

- `hookChanceFlat.test.ts` 3/3 ✓：① 華山劍法走出貨 `syncAbilityPassives` 掛上，真 `fireHooks` 各 400 發 × 0／75／150 敏，每一發拿引擎當下的 `rng.state` 重算「照 `clamp(flat + 敏×coeff)` 該不該中」逐發相等（⇒ 門檻對**且**抽籤位置沒動）；公式從 JSON 推導 ② 朗基努斯之槍（沒 flat）逐發等於 #1054 之前那條 `clamp(敏×coeff)` ③ schema：`flat 1.5` 拒、`flat 0.5 > max 0.3` 拒、缺席不注入預設。
- ⭐ **突變**：`hooks.ts` 拿掉 `(from.flat ?? 0) +` ⇒ ① 紅：`第 14 發：門檻 0.05: expected false to be true`（0 敏那一點引擎門檻變 0）⇒ 用 Edit 改回。
- 同批綠：`fieldAdoption`（含 STALE／witness 檢查）· `onHitTerms.shipped`（§2C 朗基努斯）· `legendaryOwnerRulings` · `acceptanceN3` · `editorCapabilities` · `compat` · `killTriggerSchema` · `purity`。
- `pnpm typecheck` EXIT=0（跑 1 次）。vitest 跑 3 次（批次／突變／修剪後確認）。
- **紅、但⛔ 不是本 lane 該修的**：`ops/skillSpecFresh.test.ts` —— `docs/技能標記機制與效果規則.md` 是產物，schema 多了一格 ⇒ 主 session `pnpm spec:build`（或 `skills:sync`）後 `git add docs/`。

## 5. 需要主 session 做的

1. `pnpm spec:build`／`skills:sync`（產物：`docs/技能標記機制與效果規則.md`、`docs/editor-contract/*`、`content/editor-target-profile.json` 都會因為 `chanceFrom.flat` 而變）。
2. ⚠️ `content/_legacy/champions/godie-o02w.json` 的 `abilities.Q` 嵌入版**還是舊的**（主動傷害技那份）—— 封存區沒有任何 sync 工具鏡射；若要一致，手動把 `content/_legacy/abilities/godie-o02w.q.json` 的內容抄進去（本 lane 柵欄禁動 champions）。營運樹的 `abilityMirror` 不掃 `_legacy`，所以今天沒有閘會紅。
3. 過目 `modifiers.ts` 那 6 行（§2）。
4. 關票前逐條念驗收：AC「華山劍法在 0／75／150 敏三點 = 卡面公式」在**封存區文件**上成立（守衛①）；AC「既有 chanceFrom 使用者逐位元不變」成立（守衛②）。⚠️ 玩家側要等 96 令狐沖重新上架 —— 到時 `fieldAdoption` 那一列會 STALE 紅 ⇒ **刪列**，⛔ 不要改狀態。

## 6. 順手發現、⛔ 沒動（沒開票，理由各一句）

- 華山劍法卡面「額外 10+敏捷×1 點傷害」只在 rank 1 為真（rank 2–4 是 20/30/40）—— 該走 `{{dmg}}` 佔位，但那要 `tiers:apply`／`prose:build` 鏈，而封存區文件不在那條鏈上；令狐沖上架那一天由 #214 新英雄 SOP 一起收。⛔ 不開票：那是 roster 決定（owner 2026-08-13），不是缺陷。
- `content/_legacy/` 底下其他 235 支技能很可能有同一種「早期匯入器模板預設」（沒出處的冷卻／耗魔／自身 buff）—— 本 lane 只讀了 o02w 六份，⛔ 沒普查、沒量分母，不寫成結論。
