# GH#993 Scope 3 · 4 · 5 · 6 —— templatize 正規化器 · 等價閘 · 子票 · 棘輪 231

> ⚠️ 暫存報告（`{用途}_temp_{timestamp}`）。量測時間 **2026-09-06 18:15**，主工作樹 `main`（第二波 lane，⛔ 未 commit）。
> 上一輪（Scope 1 · 2 · 6 的盤點與判定）在 `docs/_reports/993_handwritten-abilities_temp_20260905-0430.md`，本檔**不重抄**那 99 列形狀表。

---

## 0. 一句話

`tools/skill-remake/templatize.py`（新）把 **26 支**逐位元等價的手寫技能接上 5 份既有模板（**257 → 231**），
每一支由 `packages/shared/src/content/templatizeEquivalence.test.ts`（新）用**出貨的**展開路徑重新展開比對（突變驗過會紅）。
⛔ AC 的「≤ 145」**在這條 lane 的柵欄裡達不到**：剩下的 231 支裡，形狀對得上模板的只有 **25 支**，而它們每一支都差在一格 —— 差在**同一個** `mergeExpansion` 迴圈的 52 支是最大宗（#1065）。

---

## 1. 量到的（⭐ 兩個方向都對過）

| 格 | 動手前 | 動手後 | 怎麼量 |
|---|---:|---:|---|
| 手寫（非模板）技能 | 257 | **231** | 與 `tools/brick-census/gen.ts::demandShapes()` 逐行相同的算式獨立重算；26 ＝ 帳本筆數 |
| 零採用模板（非 terminal） | 28 | **26** | `tpl-blink-strike`（godie-n01c.w）、`tpl-proxy-fanout`（godie-u01u.w）有客戶了 |
| 等價閘 | — | 26/26 綠 | 出貨 `resolveTemplateExpansion` → `mergeExpansion` → `zAbilityDoc`，行為欄位 deep-equal（effects 忽略順序） |
| 突變 | — | 🔴 指名 1 支 | `godie-e00n.ex` 的 `params.duration` 6.0 → 8.0 ⇒ 「1/26 支 … godie-e00n.ex: 展開 ≠ 轉換前」；已用 Edit 改回 |
| `pnpm typecheck` | — | EXIT 1，**42 個錯全部在別的檔**（`vfx-forge/*` 27、`compat.test.ts`、`mobs.*.test.ts`…，多為別條 lane 的 dirty 檔） | `grep` 我的三支測試檔：0 命中 |
| `pnpm bricks:check` | 綠 | 🔴 過期（預期：產物要 `bricks:build`） | 唯讀 |

⚠️ **棘輪（`handWrittenAbilitiesRatchet.test.ts`）現在是紅的** —— 基準線已降到 231，而它讀的是 `ggd-brick-census.json` 這份**產物**（仍寫 257）。主 session 跑 `pnpm skills:sync` 之後轉綠。訊息已補一行「先跑 `pnpm bricks:check`」，⛔ 免得下一輪把「產物過期」讀成「有人手刻了 26 支」。

---

## 2. Scope 3 —— 正規化器 `tools/skill-remake/templatize.py`（新，676 行）

### 它守的三條規矩

| 規矩 | 落地 |
|---|---|
| ① **先驗鑰匙**（第〇·六守則） | `id` ＝ 檔名、全樹唯一；`name` 前綴的 JASS 編號尾碼合法、**同一隻英雄不可兩格同號**。⚠️ 「編號 ↔ 槽位」**不擋**（owner 2026-07-30：技能↔槽位是設計偏好；鑰匙是編號↔技能）—— 只印 ℹ️ 給人看 |
| ② **產物不動** | `skillremake:json` 的 90 份從 `sync-io.json` 的 writes 讀（⛔ 不手寫名單）⇒ 50 支手寫產物列在「改 `batch1.py` 的來源列」桶 |
| ③ **只轉逐位元等價的** | 7 個 matcher（buff-self · single-strike · instant-blast · proxy-fanout · blink-strike · drain-leech · leap-strike · life-manipulate）各自是**提案**；差一格就回一句「差在哪」進「差一格」桶。裁判是 TS 那邊 |

`--apply` 寫檔 ＋ 帳本 `tools/skill-remake/templatize-ledger.json`（26 筆：`ref` / `params` / `before` / `order`）；`--revert <id>` 依帳本連鍵序一起還原（diff 為零）。

### 轉了哪 26 支（按模板）

| 模板 | 支 | 技能 |
|---|---:|---|
| `tpl-buff-self` | **16** | e00n.ex · e00n.q · e00n.r · e00x.ex · h01o.r · h020.ex · h02r.ex · n00p.ex · n01g.ex · o00x.ex · o02l.ex · ogrh.ex · u00l.ex · u00o.ex · u010.r · u01u.q |
| `tpl-single-strike` | **7** | e007.e · e00x.e · h02r.e · n00p.e · n00p.q · o00x.e · o02l.r |
| `tpl-blink-strike` | 1 | n01c.w（⭐ 它本來就是這份模板的 exemplar） |
| `tpl-instant-blast` | 1 | u00l.e（radius 6.0 → `wc3u` 327.27，`toLen` 逐位元回 6.0；與既有 12 位採用者同一個慣例：文件保留 `radiusTier`） |
| `tpl-proxy-fanout` | 1 | u01u.w（burnstun 0.8、radius 3.0 → 163.64） |

`castTimeSec` 沿用出貨 71/79 位採用者的慣例（文件與 `params` 同一個值，`castderive:build:raw` 兩邊一起蓋）。

### 排除（⛔ 沒動）

| 桶 | 支 | 為什麼 |
|---|---:|---|
| 別條 lane 的檔 | 6 | `godie-o02w.q` · `o00x.r` · `ogrh.r` · `u01u.e` · `udre.e` · `hpb1.w`（`--exclude`） |
| fail-open 骨架的孿生 | 8 | `sela.*` · `thorne.*` —— `packages/shared/src/sim/content/skeleton.ts:108` 逐字說它們的值要與 content 對齊、`loader.test.ts` 是那條 drift 守衛；⭐ `sela.r` 形狀其實對得上 proxy-fanout，留給主 session 決定（收 1 支） |
| 產物 | 50 | 見下面「需要主 session 動的」 |
| 鑰匙驗不過 | 1 | `godie-e00r.ex` 的 name 前綴是 **`xx-001`**（尾碼不是 00/01–04/002）—— 順手發現，⛔ 沒動 |

---

## 3. 「差一格」—— 形狀對得上模板、⛔ 逐位元對不上的 25 支（⭐ 這張表就是缺的模板參數）

| 差在哪 | 支 | 技能 | 正解住在 |
|---|---:|---|---|
| **`innateKind` 被 merge 刪掉 ＋ schema 要求 active 的 effects 非空** | 12 | buff-self 形狀 9：n003／n01g／osam／u00h／u00l／u00o／u01u／udre／umal `.passive` · drain-leech 形狀 2：h02r／hgam `.passive` · instant-blast 形狀 1：etyr.passive | **#1065**（mergeExpansion／zAbilityDoc 三個洞；全樹 34 支 PASSIVE＋active 都卡在這） |
| 文件層 `passive` 與 effects 並存，merge 會整格刪 `passive` | 3 | o02l.w · o030.ex · ofar.w | **#1065**（全樹 18 支） |
| `applyBuff` 帶 `perRank` 逐階表，buff-self 沒這一格 | 3 | e001.q · e008.w · hpb1.r（＋產物 hapm.q） | ⛔ 沒開票（4 支 < 5）；tpl-buff-self 加 optional `perRank` |
| `applyBuff` 帶 `hooks／maxStacks／stackKey／applyTo` | 1 | hvsh.e | 單例（N=1 不模板化，GH#916） |
| proxy-fanout 的 `statusId` 下拉只有 root／burnstun／slow30，文件用 slow40 | 3 | n01g.q · u010.w · uvng.w（＋sela.e／thorne.r） | **#1066**（status 槽帶機制欄位） |
| `restore` 沒寫 `applyTo`，life-manipulate **永遠**發 applyTo（必填槽有預設） | 1 | o02p.ex | 豁免表理由已改成這一句 |
| leap 的 `onLand` 帶 `comboBonus` | 1 | hpb1.e | 單例 |
| blink-strike 的 `onArrive` 不是 [spawnVfx, damage] | 1 | u00v.r | 單例（大組合） |

⭐ **判讀**：25 支裡 **15 支**（60%）卡在**同一個** `for (k of EXPANDED_KEYS) delete out[k]` 迴圈 —— 那不是「缺模板」，是載入路徑的一個洞（#1065）。修好它，這條 lane 的正規化器**零改動**就再收 15 支（`_common_reject` 放寬一行）。

---

## 4. 沒有模板發這個形狀（231 支的其餘）—— 按擋住幾支排序，⭐ 已開子票

| # | 缺口 | 擋住 | 子票 | 優先級 |
|---:|---|---:|---|---|
| 1 | `mergeExpansion` 刪 innateKind（34）／刪 passive（18）／`range` 沒寫回 | **52** | **#1065** | [重要][fix] |
| 2 | 單體／範圍打擊＋上狀態：single-strike 沒有 status 槽（22）＋ proxy-fanout 下拉太窄（5） | **27** | **#1066** | [重要][feature] |
| 3 | 變身家族 `championForm`（9）＋ `applyBuff+championForm`（11）＋ 混合（4）—— ⭐ owner 2026-09-06「英雄層級有十出身十一屬性可以作為模板」 | **24** | **#1067** | [重要][feature] |
| 4 | 投射物：35 個家族沒有一個發 `spawnProjectile` | **19** | **#1068** | [優先][feature] |
| 5 | 純位移 `blink to:point`（7 支 effects 逐位元相同）；tpl-teleport 發 leap | **7** | **#1069** | [優先][feature] |
| 6 | 只上一個狀態（applyStatus-only） | **7** | **#1071** | [一般][feature] |
| 7 | 只回血（heal ≠ restore） | **5** | **#1072** | [一般][feature] |
| 8 | 打一下＋dot：drain-leech 的 `leechFlat` 必填 ⇒ 永遠帶 heal | **5** | **#1073** | [一般][feature] |

（#1070 是別人在中間開的，⛔ 不是本 lane。）八張都過 `scripts/ticket-lint.sh --body-file`（五件齊、六節齊）；標題帶 `[自動]`。
⚠️ 票文裡 owner 的話一律 `> 引言`＋日期；「擋住幾支」是 `templatize.py` 的桶 ＋ 本檔 §3 的重算，⛔ 不是估的。

⚠️ 票文（2026-09-05）說「引擎缺的 10 個機制」指的是 10 份 `status:draft` 且 `isExpandable=false` 的模板（barrier-domain … team-synergy）。⭐ 那 10 份**不是**需求側量到的缺口：GH#916 已把其中 6 份量成 N≤2（豁免表逐列有理由），而**真的擋住技能的**是上表這 8 個 —— 子票照**擋住幾支**開，⛔ 不照那 10 個名字開（第〇·五守則下半：按擋住的支數做機制）。

---

## 5. Scope 4 —— 等價閘 `packages/shared/src/content/templatizeEquivalence.test.ts`（新，149 行）

| | |
|---|---|
| 讀什麼 | 帳本 26 筆 ＋ `content/abilities/<id>.json` ＋ `content/ability-templates/*.json`（`zTemplateDoc.parse`） |
| 怎麼比 | `resolveTemplateExpansion(doc)` → `zAbilityDoc.safeParse(merged)`；「轉換前」＝ 文件去掉 template、行為欄位換成帳本的 `before` → 同一個 `zAbilityDoc`；比 `castType / effects / radius / range / castTimeSec / targetsEnemies / innateKind / passive / marks`，深層鍵排序、頂層 effects 忽略順序 |
| 三條 `it` | ①量尺自證（帳本非空、模板讀得到）②★ 一條收齊 26 支（⛔ 不是一支一測試）③sentinel：清空一支的 params ⇒ 抓得到；沒動的那一支 ⇒ null |
| 突變 | `godie-e00n.ex` duration 6.0→8.0 ⇒ 🔴 指名它並印出兩邊的行為字串；Edit 改回 ⇒ 未重跑（改壞前已綠，第零守則） |
| 有意改動時 | 訊息說明兩條路：修回去 / 同一 commit 更新帳本 `before`（或從帳本刪掉那一筆） |

測試行數 149 vs 實作 676 ⇒ 0.22×。

---

## 6. Scope 2 追加 —— 豁免表（`templateFamiliesAreAdopted.test.ts`）

- **刪** `tpl-blink-strike`、`tpl-proxy-fanout`（有內容引用了；不刪第二條 `it` 會紅）。
- **理由改成量到的差在哪一格**（⛔ 不再是「內容未套用」）：teleport（需求側 7 支發 blink）· lock-combo（差的不只 dot：invulnerable 兩格旗標、leap 弧高、onLand 帶 applyStatus）· leap-strike（2 支：h00l.w 是產物、hpb1.e 帶 comboBonus）· pull-throw（0 支 `applyTo:"target"` 客戶）· drain-leech（2 支對得上但卡 PASSIVE＋active）· life-manipulate（o02p.ex 差 applyTo）· ground-nova（range 0 的 damage-only 手寫 0 支）· random-barrage（需求側沒有 dot-only）· 4 份 passive 家族註明「需求側量尺看不到 effects 為空的 77 支」。
- 三條 `it` 全綠。

---

## 7. ⛔ 沒做到的（誠實）

| AC | 狀態 |
|---|---|
| 手寫 263 → ≤ 145 | ⛔ **231**。柵欄內可逐位元轉的就是這 26 支；再往下要 #1065（+15 零改動）與 #1066/#1067/#1068 的新槽／新家族，全部在 `expand.ts`／`ability-templates/`（本 lane ⛔ 不動） |
| 零採用 36 → 0 | 28 → **26**；其餘每一份在豁免表帶「差在哪一格」的理由（可被反駁） |
| 等價閘綠 · `skills:check` 綠 · `content:build` EXIT 0 | 等價閘 ✅；`skills:check`／`content:build` **⛔ 本 lane 不准跑**（`*:build` 禁令）—— `bricks:check` 唯讀跑過：過期（預期） |
| 10 個引擎缺口各一張票 | ✅ **8 張**（#1065–#1069、#1071–#1073），按擋住支數排；⚠️ 不是票文那 10 個 draft 名字（§4 說明） |
| Scope 7 說明佔位 | ⛔ 沒碰 description（`prose:build` 主 session 跑；被轉的 26 支說明本來就是 `{{dmg}}`／`{{cd}}` 佔位） |

---

## 8. 需要主 session 動的（柵欄外）

1. **`pnpm skills:sync`**（一次）：`bricks:build`（棘輪 231 轉綠 · 零採用 26）· `content:build`（26 份 `_index.json` hash ＋ `bundle.json`）· 英雄卡鏡射（`content/champions/**` 的 26 個內嵌複本，`abilityMirror.test.ts` 在此之前會紅）· `castderive:build:raw`／`tiers:apply`（genguard 對這 26 份的要求）。
2. **`tools/skill-remake/batch1.py` 的來源列**：50 支手寫產物裡，形狀對得上既有模板的（估：`applyBuff`→buff-self 1、`applyStatus+damage`→#1066 2、`damage+leap`→leap-strike 1（h00l.w）…）—— 本 lane 沒動 batch1.py；`templatize.py --only <id>` 可以列出提案但**不會**寫產物（genguard 會擋）。
3. **`packages/shared/src/content/templates/expand.ts`**（#1057 lane 在裡面）：#1065 的三個洞；修好後 `python3 tools/skill-remake/templatize.py --apply` 再收 15 支。
4. `sela.r` 要不要接 proxy-fanout（骨架孿生；1 支）。
5. 別條 lane 的 5 份 dirty 檔（`hpb1.w` · `o00x.r` · `ogrh.r` · `u01u.e` · `udre.e`）—— 本 lane 沒碰，其中 `udre.e`／`u01u.e` 是 #1066 的客戶。

---

## 9. 順手發現、⛔ 沒動的

| 發現 | 住在 |
|---|---|
| `ExpandResult.range` 不在 `EXPANDED_KEYS` ⇒ `tpl-blink-strike.params.range` 是空宣稱（第一·五守則）；今天綠只因為文件自己也寫 range 6 | **#1065** 洞③ |
| `CC_MECHANIC`（statusId → 機制欄位）是手寫表 ＝ 第〇·四的第二個住處 | **#1066** |
| `godie-e00r.ex` 的 name 編號尾碼 `001` 不合慣例 | 本檔 §2（⛔ 沒開票；一格資料） |
| 17 份文件的 JASS 編號與槽位不一致（e002 q/w · e008 q/w · e00l q/w · etyr e/w · h02u e/w · h02v e/w · n00b e/w · ubal e/w · e00r.ex）—— ⭐ 依 owner 2026-07-30 是設計偏好，⛔ 不是缺陷；`templatize.py` 印 ℹ️ 不擋 | 本檔（記錄用） |
| `tpl-drain-leech` 的 2 支出貨客戶 heal 節點缺 `applyTo`（GH#1046 已記） | GH#1046 |

---

## 10. 動到的檔案

| 檔 | |
|---|---|
| `tools/skill-remake/templatize.py` | **新**（361 行） |
| `tools/skill-remake/templatize-ledger.json` | **新**（26 筆） |
| `packages/shared/src/content/templatizeEquivalence.test.ts` | **新**（147 行） |
| `content/abilities/*.json` × 26 | `effects: []` ＋ `template:{ref,params}`（§2 名單；diff 213+/174−） |
| `packages/shared/src/ops/handWrittenAbilitiesRatchet.test.ts` | 基準線 257 → 231 ＋ 訊息補「先跑 bricks:check」 |
| `packages/shared/src/content/templateFamiliesAreAdopted.test.ts` | 刪 2 列、改 12 列理由 |
| `docs/_reports/993_templatize_temp_20260906-1815.md` | **新**（本檔） |

⛔ 沒碰：`content/champions/**` · `content/ability-templates/*` · `expand.ts` · `batch1.py` · 任何 `*:build` · git。
vitest 跑了 3 次（①等價閘 ②三支一起 ③突變），`pnpm typecheck` 1 次。
