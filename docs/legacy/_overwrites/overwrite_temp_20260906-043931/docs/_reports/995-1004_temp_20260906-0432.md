# #995 ＋ #1004 —— 產生器的輸出不再取決於 git 外的營運狀態（lane 5，2026-09-06）

> owner 逐字（2026-09-06）：「盡量把所有票解決完我要去睡了 有問題做成開關 不要卡在我這裡 我醒了你可以回我開關列表」
> ⇒ ⭐ 兩張都**自己選了路＋留開關**，⛔ 沒有停下來問。

commit：**`00216792e`**（程式＋產物＋守衛；13 檔）· 本報告另一個 commit。

## 🔀 開關列表（owner 醒來要看的那一格）

| 開關 | 預設 | 另一邊 | 我為什麼選這個預設 |
|---|---|---|---|
| `GGD_REFERENCE_CURATION`（環境變數，`pnpm docs:readme` 讀） | **`snapshot`** —— README／docs 的開放名單從**進版控的快照** `docs/reference/_curation-snapshot.json` 讀 | `placeholder` —— 文件**完全不含**開放名單：旗標整欄不印、README 名冊改印全部 71 名、來源句改成固定文字指向 `GET /api/v1/curation/whitelist` | 票的 Non-goal 逐字是「⛔ 不改 owner 看得到的 README 版面結構（他要看得到開放名單這件事不變）」—— 而 README 的 roster 區塊**整段**就是開放名單（49 名＋六條技能）。走 A／C 那條「產出不取決於白名單」等於把那 450 行變成 0 名或 71 名 ⇒ 版面必變。⭐ 快照是票裡的 **B 路**：兩台機器同一份位元組，而且它是**有閘的**第二住處（`--check` 比對快照↔白名單、表頭印 updatedAt）。 |
| `GGD_EDITOR_PROFILE_CURATION`（環境變數，`content:build` 讀） | **`placeholder`** —— `curation.*` 四格一律 null ＋ `unavailable[]` 說明（帶 GH#1004 出處），`liveEndpoint` 固定文字 | `live` —— 烘進**這台機器**的 `data/curation/whitelist.json` digest（championCount 49…）。⚠️ 代價：契約位元組跟著機器走，`shippedEditorProfileIsCurrent` 只在同一台機器上綠 | 你的 lane 指令逐字：「實作路 **C** 當預設…留一個產生器旗標讓他翻回去」。它是**對外契約**，外部編輯器看不到我們的 `data/` ⇒ 一份跟著機器走的契約就是在對他們說謊。 |
| `GGD_CURATION_WHITELIST`（環境變數，兩支產生器同名） | 未設 ＝ `data/curation/whitelist.json` | 任何路徑（測試指到不存在的路徑 ＝ 模擬「這台機器沒有白名單」） | ⛔ 不是給人翻的，是**測試接縫** —— 承重守衛靠它在同一台機器上跑出「在／不在」兩種環境。列在這裡是因為它也是環境變數。 |

⚠️ 三個都是**環境變數**，⛔ 不是後台三個住處的欄位 —— 它們管的是**產生器在哪台機器怎麼產**，不是遊戲行為；後台那一層沒有對應物（同 GH#979 的 `hashAssetTree` 那一族）。

## #995 —— `docs:readme:check` 在 CI 結構上不可能綠

### 基線（動手前量到的）
- `tools/reference/gen_readme_lists.py:1311` 逐字：白名單不在 ⇒ 印「沒驗到」⇒ `return`（exit 0）。⇒ CI **從來沒驗過** README／docs 的內容漂移。
- 本機（有白名單）：`--check` 綠；README roster 區塊 = 49 名 OPEN；`docs/reference/*.md` 表頭印 `data/curation/whitelist.json（updatedAt …）`。
- 產出依賴白名單的地方（量到，⛔ 不是猜）：README roster 區塊的**成員**（49/71）＋ 計數 4 處；abilities 區塊計數 2 處；items 區塊 `開放` 欄逐列；docs roster.md §1/§2 分節 ＋ `開放` 欄；abilities.md／items.md `開放` 欄；三份表頭的來源句。

### 做了什麼
| 檔 | 改動 |
|---|---|
| `tools/reference/gen_reference.py` | 新 `load_curation()`：`snapshot` 模式只讀 `docs/reference/_curation-snapshot.json`；白名單**在**時逐項比對（三張名單 sorted-unique ＋ updatedAt），寫模式重刷快照、`--check` 回報 stale；白名單**不在**照樣從快照產出同一份位元組。`placeholder` 模式：三張名單皆空、`curation_flags=False`。ctx 裡**刻意沒有**「這台機器有沒有白名單」這種欄位。三份 docs 的 `開放` 欄改成 `open_flag_cells()`（placeholder 整欄不印）。 |
| `tools/reference/gen_readme_lists.py` | 那段 skip **整段刪掉**（AC-3）；`stale` 起手就帶 `ctx["curation_stale"]`；寫模式 `build_context(refresh_snapshot=True)`；roster／abilities／items 三個區塊各長一個 placeholder 分支；`# ggd:writes docs/reference/_curation-snapshot.json`（reconcile.mjs 認得，`genrun docs:readme` 對帳綠）。 |
| `docs/reference/_curation-snapshot.json`（新，產物） | `ggd-curation-snapshot@1`：source／updatedAt（`2026-08-20T16:58:56Z`）／digest／champions 49／items 101／abilities 245。 |
| README／docs/reference | ⭐ **版面一字不動**：只有「來源：」那一句從 `data/curation/whitelist.json（updatedAt）` 變成「快照 `docs/reference/_curation-snapshot.json`（whitelist updatedAt …；英雄 49 · 道具 101 · 技能 245）；即時名單 `GET /api/v1/curation/whitelist`」。 |

### AC
| AC | 結果 |
|---|---|
| 1 沒有白名單的樹上 `--check` ⇒ EXIT 0 且不是 skip | ✅ 乾淨 HEAD worktree（`data/` 不存在）：`up to date`；另以 `GGD_CURATION_WHITELIST=/nonexistent` 複驗同結果 |
| 2 有白名單的機器 ⇒ 也 EXIT 0，同一份位元組 | ✅ 本機 `up to date`（同一份 README／docs） |
| 3 skip 整段刪掉 | ✅ |
| 4 ops 改了白名單而文件沒重生成 ⇒ 有東西紅 | ✅ 在**有白名單的機器**上 `--check` 印 `docs/reference/_curation-snapshot.json(白名單 updatedAt X ≠ 快照 Y)` 並 exit 1。⚠️ 誠實：在**沒有**白名單的機器（CI）這一條驗不到 —— 那是營運狀態的本質，快照的 updatedAt 就是給人看「它多舊」的。 |

### 我沒做（不在柵欄／要 owner 決定）
- **`curation:snapshot` 從正式站拉**：快照的來源是**跑產生器那台機器**的 `data/curation/whitelist.json`（今天 README 本來就是這樣烘的）。要從 Mac mini 拉，要一支 package.json 腳本 ⇒ 不在柵欄。
- `README.md:696` 手寫散文仍寫「fresh clone 唯一缺的是 gitignored 的 whitelist：它不在時開放清單是空的」—— 現在是假的（它會從快照印 49 名）。柵欄只准動 marker 區塊 ⇒ 沒改，⭐ 要一句手改。
- `packages/shared/src/ops/readmeListsFresh.test.ts` 的 ② 分支（「沒驗到」那條路）現在是死碼（永遠不會走到）；測試仍綠。不在柵欄 ⇒ 沒動。
- `tools/parallel-gates/sync-io.json` 沒手寫（⛔ 規則）；新產物靠 `# ggd:writes` 宣告，`genguard` 對它回「戶籍無主但檔頭寫著它是產生的」。下一次重量測會補上。

## #1004 —— `editor-target-profile.json` 把機器狀態烘進對外契約

### 基線
- 出貨檔：`curation.championCount 49 / itemCount 101 / championDigest e39f1147b9e7`，`unavailable` 只有 `deltaExportAllowed` 一筆，266,425 B（票上的 213,250 B 是 09-05 的；內容長了）。
- `shippedEditorProfileIsCurrent.test.ts:51-59`：`HAS_WHITELIST ? it : it.skip`。

### 做了什麼
- `buildEditorTargetProfile.ts` ⑥ 段：`curationMode = env ?? "placeholder"`；不認得的值 throw（⛔ 不靜默退回）；placeholder ⇒ 不讀白名單、四格 null、`unavailable.push({field:"curation.*", reason:"⭐ 刻意不烘進（GH#1004）…"})`；live ⇒ 讀 `GGD_CURATION_WHITELIST ?? data/curation/whitelist.json`。
- 產物 diff vs HEAD：**只有**四格 → null、多一筆 unavailable、`profileDigest cc7e6d6f6e41 → c0632ea50165`；`content.contentVersion` 不變（`cv_f97908667d9b`）。
- 測試：skip 整段刪掉；第一條加「`GGD_CURATION_WHITELIST` 指到不存在的路徑再算一次 ⇒ 位元組相同」＋「fresh 的 `curation.*` unavailable 帶 GH#1004」（後者讓「預設翻成 live」在**沒有**白名單的機器上也會紅）。

### AC
| AC | 結果 |
|---|---|
| 1 沒有白名單的乾淨 worktree 重生成 ⇒ 位元組與 commit 相同 | ✅ `git worktree add --detach … HEAD` ＋ 我的產生器 ⇒ `cmp` 相同 |
| 2 有的機器重生成 ⇒ 也相同 | ✅ 本機（測試第一條） |
| 3 skip 整段刪掉，兩條都真的跑 | ✅ 2 tests passed |

## 測試／突變（vitest 3 次：全綠 · 全綠（對 HEAD 匯出）· 突變）
- `readmeListsIgnoreRuntimeState.test.ts`（新，體驗層一條薄的，56 行）· `shippedEditorProfileIsCurrent.test.ts` · 既有 `readmeListsFresh.test.ts` —— 5/5 綠。
- 突變 ①（TS 預設 `"placeholder"`→`"live"`）⇒ 契約比對紅（championCount 49 ≠ null）；突變 ②（python 白名單缺席時 note 多一句）⇒ absent 那次 `--check` stale 紅。兩條都紅、都還原。
- `tsc -p packages/shared`：唯一的錯在**別條 lane 未追蹤**的 `releaseNotePlayersRange.test.ts`（TS2322／TS2532），⛔ 不是這裡。

## ⚠️ 併行 lane 的實際狀況（誠實記下，⛔ 不是抱怨）
- 另一條 lane 未提交的 `content/config/ugc.json`（`digestRecompute`）、`godie-h02u/u034/ucrl` 的技能與英雄卡、`spec:build` 後的 `docs/editor-contract/ggd-ability-prose.json`，會被主樹的 `content:build`／`docs:readme` 烘進 bundle／manifest／profile／README（cv 變 `cv_a1ae1bdcfc97` → 後來 `cv_5d84e9b225f9`）。⇒ 我的產物一律以 **HEAD 的 content 匯出**（`GGD_CONTENT_DIR`）產出：README／docs 走 `genrun docs:readme`；profile 由產生器自己的 `writeProduct` 透過 symlink 寫回工作樹（⛔ 沒有手改任何位元組）。收尾腳本在乾淨 HEAD worktree 複驗後才 commit。
- `content/bundle.json`／`manifest.json`／`config/_index.json` 在工作樹是髒的（那條 lane 的），⛔ 不在我的 commit 裡。
