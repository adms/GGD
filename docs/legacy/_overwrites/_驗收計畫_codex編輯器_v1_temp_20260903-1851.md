# 驗收計畫：Codex 特效編輯器（2026-09-02）

> ⭐ **驗收標準不是「八招好不好看」** —— owner 2026-09-01 逐字：
> 「**八個驗收技能特效是用來驗收編輯器是否能做出對應技能，不是直接套用回去遊戲主程式中**，
>  所有技能效果機制動畫特效由 AI 來調整變更都要**經過後台一頁批核審查頁 通過才能套用**，
>  因為目前 **AI 產特效的正確性太差了且太不穩定了**（肉眼 **0–4/10** · 視覺擷圖自動審查 **2–6/10**）」
>
> ⇒ ⭐ 所以本計畫問的是「**編輯器拿不拿得到、拼不拼得出來**」，
> ⛔ 視覺對錯**不在機器驗收裡**，它走 HITL 批核頁（第 5 層）。

---

## ⛔ 0. 先回驗前提（⭐ 沒做完這一層，底下每一層都可能在驗錯的東西）

⚠️ 這一層是必要的，因為**已經量到三個前提有問題**：

| # | 問題 | ⭐ 量到的 |
|---|---|---|
| **0a** | ⛔⛔ **驗收清單到底幾招？三個數字並存** | owner 05:06「**三招**」（超究武神霸斬·龍破斬·理想鄉EX）· owner 05:17「**八個**驗收技能特效」· #838 body 引的 owner 原話是「**六個**全動畫特效」（＋龜派氣功·阿邦快速劍X·天譴）⇒ ⭐ **驗收標準本身沒有單一住處** |
| **0b** | ⛔ #838 票文的前提**已過期** | 票文 :221 寫「`VfxSystem` **零 case** ⇒ 超究武神霸斬的每一斬在客戶端沒有錨點」——⭐ 而今天 `VfxSystem.ts:2495` 有 `case "modelFxSpawn"`、`:2614` 有 `case "vfxArc"` |
| **0c** | ⚠️ 編輯器對的是**哪一版契約**？ | 契約帶兩個指紋：`fingerprint: fb36db5a72c7` · `capabilityFingerprint: 18d7999e` ⇒ ⭐ 編輯器要說得出它建置時看到的是哪一組 |

**⭐ 這一層的產出：一張「驗收清單定案表」**（幾招、哪幾招、每一招的 JASS 出處），
⛔ 而且它要有**唯一住處**（建議 #838 的 body），其他地方一律引用它。

---

## 1. 契約對帳（⭐ 機器，零人力，幾分鐘）

**分母（量到的）**：`ggd-editor-coverage.json` `_total` = **5,137 格**

| 類 | 數 | ⭐ 驗什麼 |
|---|---:|---|
| `required` | **5,137** | 編輯器**暴露了幾格**？未暴露的逐格列名 |
| `notRequired` | **15** | ⛔ 引擎今天做不到 ⇒ ⭐ **編輯器做出控制項就是紅**（做出來的內容上線是死的）|
| `ownerOnly` | **39** | ⭐ 可唯讀顯示，⛔ **做成可調控制項就是紅** |

⭐⭐ **⛔ 只從一頭掃會結構上失明**（本 repo 的綠燈假象⑫）——**兩頭都要走**：

| 方向 | 問題 | ⛔ 漏掉什麼 |
|---|---|---|
| 契約 → 編輯器 | 每一格**被暴露**了嗎？ | 漏掉「編輯器有而契約沒有」的 |
| ⭐ 編輯器 → 契約 | 每一個控制項**在契約裡查得到**嗎？ | ⭐ 查不到 ＝ **編輯器自己猜了一個欄位** ⇒ 它產的 JSON 會被引擎拒絕 |

**指令**
```bash
pnpm editorcov:build          # 先把契約重生成（⛔ 對舊契約驗＝驗了一個過期的分母）
pnpm caps:export              # runtime-capabilities 同步
# 然後由編輯器側輸出一份「我暴露了哪些欄位」的清單，兩邊做集合差
```

---

## 2. ⭐⭐ 往返驗證（**最重要的一層**，機器）

⭐ **這是唯一問「兩個名詞的關係」的一層**，其餘各層問的都是名詞。

```
編輯器產出的 JSON
   → 出貨的 ContentLoader（嚴格 Zod）
   → 出貨的 sim（真的跑一場）
   → 斷言「機制真的發生了」
```

| ⛔ 不夠 | ⭐ 要有 |
|---|---|
| 「JSON 通過 schema」 | schema 過**不代表**引擎會做那件事（本 repo 的失敗形態⑧：case 存在但第一行讀零寫入端的欄位就 break） |
| 「編輯器預覽看起來對」 | 預覽是**編輯器自己畫的**，⛔ 不是遊戲畫的 |
| 「檔案存得下來」 | 出貨的 bundle 要**真的載得進去**（2026-08-02 的事故就是這一格） |

**⭐ 驗收條件**：把六（八）招的編輯器輸出丟進 `pnpm content:build` ＋ 跑真的 sim ⇒
每一招**列名的機制**逐條被觀測到（傷害有發、狀態有掛、模型有生成事件）。

⚠️ ⭐ **量尺要雙向校準**（`calibrate()`）：
先拿一份**已知會動**的技能量到「有」，再拿一份**已知壞掉**的量到「沒有」。
⛔ 只驗「應該有」那一邊的量尺，會在它最該說話的時候沉默。

---

## 3. ⚠️ `partial` 那 30 個 —— ⭐ 最危險的一格

**量到的**：`ggd-runtime-capabilities.json` 宣告 **supported 16 · ⭐ partial 30 · unsupported 15**

⇒ ⭐⭐ **半數的能力是「一半能用」** —— 而編輯器最容易在這裡騙人：
控制項有、拖得動、存得下去，⛔ **而引擎只做了其中一半**。

**⭐ 每一個 partial 要問兩題：**
1. 編輯器有沒有**告訴使用者哪一半不能用**？（⛔ 沉默 ＝ 使用者會做出上線即死的內容）
2. 那個「不能用的一半」在往返驗證（第 2 層）裡**會不會紅**？

⛔ 若兩題都是否 ⇒ ⭐ **那不是編輯器的缺陷，是契約說謊** ⇒ 回頭修 `caps:export` 的宣告。

---

## 4. 六（八）招的**可做性**拆解（⭐ 機器＋一次人工比對）

⭐ **⛔ 不問「做得像不像」，問「做不做得出來」。**
每一招拆成它需要的**積木清單**，逐塊問「編輯器拿得到嗎」：

| 招 | JASS 出處（#838 已整理） | 需要的積木（例） |
|---|---|---|
| ① 01-04 超究武神霸斬 | A077 · j:33759–33944 | 施法者紅染半透明 · 無敵 · 幻影升降 · **每刀瞬移**（M1）· 掛骨特效雨（⚠️ **一半掛受擊者**）· 三人升空急墜（M3） |
| ② 04-03 龍破斬 | A04R · j:29886–30160 | CAST 詠唱 5 句（0.2s 隔）· 聚氣陣 230% · **0.96s 延遲** · 直線 1500u/s ＋ 每 tick 拖尾 · 18 具環形 WarStomp |
| ③ 理想鄉EX 鏈 | A0CT j:32374–32466 ＋ j:32475–32668 | 反射窗 · 逐敵腳下柱 ＋ 鏈鎖閃電 · 雙方定身無敵 · **七刀拖行**（隨機刀距 0.05–0.30s）· 30% 慢動作 · 前方 900 掃蕩 |
| ④⑤⑥ 龜派氣功 · 阿邦快速劍X · 天譴 | ⏸ **待 0a 定案後補** | — |

⭐ **判準（owner 的角色分工）**：
· 積木**拿得到** ⇒ ✅ 編輯器的事
· 積木**在 JSON 語意上根本表達不了** ⇒ ⭐ 開一張**最小 primitive 票**給 Main，
  ⛔ 而那張票**不可以要求 Main 幫忙拼完整技能**

⚠️ ⭐ **這一層的產出是一張缺口表**，⛔ 不是通過/不通過 —— 缺口就是 Main 的下一批票。

---

## 5. HITL 視覺驗收（⭐ 人，**最後且最小**）

> owner：AI 產特效的正確性 **肉眼 0–4/10 · 視覺擷圖自動審查 2–6/10** ⇒ ⛔ **不可以直接上線**

⭐ 照本 repo 既有的**分層漏斗**（#664，Tier 0/1/1.5 已建、**Tier 2 從來沒被用過**）：

| Tier | 誰 | 驗什麼 | 結果 |
|---|---|---|---|
| **0** | 機器 | 必不可見／必無聲：alpha 0 · additive 全黑 · 退化 UV · 零像素 glb | ⭐ **自動擋下**，⛔ 不進人類佇列 |
| **1** | 機器 | 可量界線：fade ≤0.5s · 粒子/VRAM 預算 · 時長 | 界內自動過 |
| **1.5** | ⭐ Claude 看圖 | 接觸表逐格：明顯空白／全黑／破圖 ⇒ 自動 fail | ⭐ 縮小人類佇列，⛔ **不得**當高風險項的終審 |
| ⭐ **2** | **owner** | 「**像不像**」—— 機器判不了的語意題 | ⭐ **一頁批次打勾**，⭐ **左右並排原作** |

⭐ **三個把人力變成一次性的乘數**（本 repo 已設計，⛔ 尚未啟用）：
① **感知基準線** —— 核准一次 ⇒ 存決定性種子的參考影格 ⇒ 之後每版重渲染比對（perceptual hash），漂移超閾值才**重新**進佇列 ⇒ ⭐ **人審一次，機器守永遠**
② **A/B 並排原作** —— 機器判不了相似度，⛔ 但機器可以把並排擺好
③ **接觸表進 release note** —— 引用最多的前 20 個特效自動渲染成一張圖 ⇒ owner 十秒掃完

---

## 6. ⛔ 這份計畫**驗不到**的（誠實列出來）

| 驗不到 | 為什麼 |
|---|---|
| 「編輯器好不好用」 | ⭐ 那是 owner 自己操作才知道的（#838 逐字：「**你自己操作**」）⇒ ⛔ 機器測不出拖拉手感 |
| 「特效像不像原作」 | ⭐ 第 5 層 Tier 2 的事，⛔ 不在機器驗收裡 |
| 動態才發生的不可見 | 被遮擋 · 鏡頭外 · AdaptiveQuality 砍掉 ⇒ 要 audition，⛔ 靜態掃描看不到 |
| ⭐ **「這一版之後不會再壞」** | ⛔ 沒有任何一次驗收能保證這個 —— 保證它的是**閘**（第 1/2 層要留成 CI 測試，⛔ 不是一次性腳本） |

---

## 7. 執行順序與成本

| 層 | 誰 | 成本 | ⛔ 阻塞誰 |
|---|---|---|---|
| **0 前提回驗** | 我 | ⭐ 1 小時 | ⛔ **擋住其餘全部**（清單沒定案就是在驗錯的東西）|
| **1 契約對帳** | 機器 | 幾分鐘 | — |
| **2 往返驗證** | 機器 | 半天（要寫那條測試） | ⭐ 最有價值 |
| **3 partial 30** | 機器＋判斷 | 半天 | — |
| **4 六招可做性** | 機器＋一次人工 | 一天 | ⛔ 等 0a |
| **5 HITL** | ⭐ **owner** | ⭐ 一頁打勾，十分鐘 | ⛔ 等 1–4 全綠 |

⭐ **建議：0 → 1 ＋ 2（並行）→ 3 ＋ 4（並行）→ 5**
⛔ **不要先跑第 5 層** —— 那會把 owner 變成人肉測試機，而前四層本來就會擋掉大部分。
