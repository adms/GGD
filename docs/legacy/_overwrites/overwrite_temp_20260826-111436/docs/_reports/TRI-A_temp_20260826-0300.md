# TRI-A 分診報告 —— #300–#559 的 open issue

日期 2026-08-26 · lane TRI-A · 範圍：`gh issue list --state open` 落在 #300–#559 的 **23 張**
（⚠️ 舊票 <#300 不在範圍內）

---

## 一句話結果

**23 張全部逐張驗過並留下帶證據的註解** ⇒ **關 3 張**（1 張真做完 · 2 張自裁）、**留開 20 張**。
⛔ 沒有一張是「看起來做了就關」——每一張的判定都指得出檔案:行 / 測試名 / commit hash / 量到的數字。

| 動作 | 張數 | 票 |
|---|---:|---|
| ✅ 已完成 → 關 | 1 | #511 |
| ⛔ 不再適用 / 自裁 → 關 | 2 | #444 · #409 |
| ◐ 部分完成 → 留開＋註解 | 8 | #330 · #382 · #441 · #453 · #457 · #473 · #509 · #558 |
| ⛔ 未完成 → 留開＋註解 | 12 | #325 · #327 · #401 · #423 · #425 · #443 · #448 · #452 · #502 · #529 · #538 · #547 |

---

## 一、關掉的三張

### ✅ #511 K8 B 鍵沒有明確 `data-pad-back` —— 真的做完了（本 lane 補上最後一處）

| AC | 證據 |
|---|---|
| StoreScreen「太好了」 | `apps/client/src/ui/platform/StoreScreen.tsx:138` `<Btn padBack kind="primary" onClick={cancel}>太好了</Btn>`（`:118` / `:149` 另兩相位也有）；守衛 `apps/client/src/ui/platform/purchaseDialogPadBack.test.ts`（渲染出貨 `PurchaseDialog` 讀最終 markup，⛔ 不 grep 原始碼） |
| LeaveSettlementOverlay「繼續觀戰」 | `apps/client/src/ui/panels/LeaveSettlementOverlay.tsx:280` |
| MatchEndPanel 收合鍵 | ⭐ **本 lane 補上** |

**根因（為什麼它到今天還是死鍵）**：那顆按鈕的字面是「▴ 收到最小」/「▾ 展開戰績」，
而 `apps/client/src/input/padFocusNav.ts:224` 的
`BACK_ALLOW_RE = /取消|關閉|收起|返回|back|close|cancel|dismiss|✕|×|╳/i`
有「收**起**」**沒有**「收**到**」；底部的 `返回大廳` 又被 `BACK_VETO_RE`（`:236`）明著擋掉
⇒ B 在整個結算畫面什麼都不做。

**改了什麼（兩個檔，都在工作樹）**
- `apps/client/src/ui/panels/MatchEndPanel.tsx` —— `MatchEndCollapseToggle` 改成 `export`、
  加一行 `data-pad-back` ＋ 一段說明為什麼⛔不去擴那條正則的註解（+13/−1 行）
- `apps/client/src/ui/panels/matchEndPadBack.test.ts` —— **新檔 44 行**（體驗層上限 80 內）

**守衛的形狀**：真的渲染出貨的 `MatchEndCollapseToggle` 讀最終 markup（⛔ 不 grep 原始碼 = 避開失敗形態⑥），
第二條斷言把根因釘住：兩個 `aria-label` 餵進**出貨的** `BACK_ALLOW_RE`/`BACK_VETO_RE` 都是 `false`
⇒ 證明標籤掃描這條退路接不住它，`data-pad-back` ⛔ 不是裝飾。

**突變驗證（做了）**：拿掉那行 `data-pad-back` → 紅，訊息指名
`collapsed=false: expected '<div style="display:flex;justify-cont…' to contain 'data-pad-back'`；已改回。
`apps/client` 的 `tsc --noEmit` **EXIT=0**。

⚠️ **誠實的但書**：本 lane 不做 git 寫入 ⇒ 這兩個檔在**工作樹**等主 session commit。
沒帶上就請重開 #511（已寫進關票留言）。

---

### ⛔ #444 級距錨點作廢 —— **不再適用**

三條理由（都寫進票裡，都可以被反駁）：

1. **票文擔心的傷害在出貨程式碼裡不存在。** 五級距**從來沒有** per-arena 重算過 ——
   `ladderWindow()` 在出貨側的每一個呼叫點都餵同一個編譯期常數：
   `aoeTiers.ts:84` · `displacementTiers.ts:178,179` · `abilityProse.ts:368`（全部 `DUEL_ZONE_RADIUS_REF`）。
   ⇒ 級距在 royale（R=42）與 skeleton（R=24）**逐位元組相同**，「同一個字＝同一件事」沒有破。
2. **「理由死掉」那一半已經被改寫而且有閘。** `skillTiers.ts:61-70` 現在明說「R = 24 是**參考值**，不是真理」，
   `skillTierLadder.test.ts:30` `Math.min(...Arenas.all()…boundaryRadius)` 從內容推導錨。
   ⚠️ 誠實標注：這段在 commit `bc695daa`（2026-08-19 14:21 +0800），**早於本票**（17:27）
   ⇒ ⛔ 不是為了回應本票才寫的。
3. **第一條 AC 已被更後面的常設裁決取消。** AC 說「拿給 owner 確認」，而 owner 2026-08-22：
   「不要再叫我調整了，公式已定好…我們只調系統倍率」⇒ 依第〇·六守則，那個動作現在被禁止。

**推翻條件**：出貨側出現把當前場地半徑餵進 `ladderWindow()` 的呼叫點，或 owner 明說他要身位／移速×時間那種錨。

---

### ⛔ #409 colosseum 通道餘裕 1.89× —— **自裁：維持現況**

本票唯一的 AC 是「owner 從三選項勾選」，而 owner 2026-08-23 常設指令
（「沒做完以前別問我了自己判斷 但是留後台開關可以簡易 rollback」）讓那個動作不該存在。

| 題 | 我挑的 | 為什麼 |
|---|---|---|
| 柱徑 | **維持 16 根 r=2（1.89×）** | 票文自己量到現況在出貨 `navHeadroom` 1.5 下**是綠的** ⇒ 不是缺陷；r=1.0 的代價是「掩體砍半」＝拿玩家看得見的損失換一個沒人在要求的天花板 |
| 瓶頸數 | **(a) 接受 1 個** | (c) 被票文自己排除（天花板掉到 1.83）；(b) 只是改標準不改地形，會讓 13 張圖的 soft warn 語意一起漂 |

**rollback 在哪**（這條指令的成立條件）：`navHeadroom` 已是後台一格
（`content/config/map-spec.json` → `packages/shared/src/map/spec.ts:84`）；柱環幾何住
`content/arenas/arena.colosseum.json`（74 件 decor）。兩條都是「改一格 / 改一份內容」。

---

## 二、留開的二十張 —— 逐張的一句話證據

| # | 判定 | 最硬的一條證據 |
|---|---|---|
| **325** | ⛔ 未完成 | `schema/arena.ts:59` 仍 `.min(1)`，整份**零個 `TEAM_SIZE``；`MatchController.ts:2254` 與 `:2468` 兩個 `!` 原封不動 |
| **327** | ⛔ 未完成 | `import/` 目錄**沒有 tokenizer**（只有 diagnostics/digest/jcs/packageSchema/targetProfile/zipSafety）；`packageSchema.ts` `passthrough` **22** vs `strict()` **3** |
| **330** | ◐ 部分 | 視覺分層早在初版 commit `2ead09ab` 就在（`AbilityBar.tsx:649/666` 灰階＋壓暗）⇒ ⭐ owner 2026-08-14 誤報時它**已經在畫面上**；缺的是自動加點開關（三住處**零命中**） |
| **382** | ◐ 部分 | `arena-decor-cc0` role 已落地（`limits.ts:249`，commit `18722c1c`）⛔ **但沒人重跑 baseline** —— `baseline.json` `updatedAt` 停在 2026-08-18，`accepted` 從票文的 6–7 筆**漲到 13 筆**；「新增越界要有東西叫」= `guard.ts` 零實作 |
| **401** | ⛔ 未完成 | `expand.ts:752-764` line-sweep 仍吐**一顆** `spawnProjectile`；`kindLimits.ts:155` `DELAYED_MAX_COUNT = 32` 未動。⭐ 對比：同檔 `:785+` 的 `traveling-wave` **已經**是 `delayed`+`advance` ⇒ ①真的只是內容變更 |
| **423** | ⛔ 未完成 | 掃 421 支 ability：帶 `summon` 的 = **0**。⭐ 新發現：`content/champions/godie-e010.json` 有 `"immobile": true` ⇒ 形狀有一半先例；⚠️ 但它 `modelKey` 是 `champ.sela`（借來的替身）—— 那正是 AC 裡 @visual-proof 要擋的形狀 |
| **425** | ⛔ 未完成 | 9 份 `imported.wave.*` 逐檔讀 `vfxKey`：**沒有一份有「波」的形狀，全部指著 `fx.prim.*.bolt`**；5 份與別的彈道逐字相同（arcane 那一組共 4 份）。⭐ 缺口比票文寫的還大 |
| **441** | ◐ 部分 | ✅ 缺陷①（6 列 `dispatched:true`，`spatialPolicy.ts:220/221/271/290/291/292`）· ✅ 缺陷②（今天 4 個出貨引用端）· ◐ 缺陷③（zombiex 已在 `quotes` 內，mp3 未驗）· ⛔ **兩位英雄仍不在 `MANIFEST.json` 的 51 位裡** |
| **443** | ⛔ 未完成 | 四層閘**一層都沒撤**：`w3xAbilityArt.ts:30-31` 仍排除 1305/1529；README `:31` 仍「models ⛔ 維持原狀」；`blizzardOverlayGate.test.ts:36-37/147` 仍只守音訊。⭐ **這是 #452/#453 的地基**——①被擋著時「要生成幾張」一定算高 |
| **448** | ⛔ 未完成 | `content/abilities/godie-o030.passive.json` 的 description 逐字仍是舊的「30 秒真視自我增益」；四處點名的改動零項落地 |
| **452** | ⛔ 未完成 | 3 張 webp 在磁碟、`grep arena-bg apps/client/src/render/` **零命中** ⇒ ⭐ 票文自己寫的 Known risk（失敗形態⑧）**已經成真**。建議先接那 3 張才量得到佔比 |
| **453** | ◐ 部分 | ✅ `prompt.py:105` 已是 Fate/Type-Moon/ufotable，衝突逐條說明保留在 `:83-100`；⛔ 貼圖／地圖物件／背景三面沒動。（票文的 digest 已過期：現在是 `bbb3c3097260f170`） |
| **457** | ◐ 部分 | ⭐ **失敗形態⑧的乾淨樣本**：`loras` 三住處齊了兩個（`content/config/icon-style.json` ＋ `iconStyleDoc.ts:76,234`），而 `tools/icon-gen/src/*.py` 的 `load_lora_weights` / `StableDiffusionXL` **零命中** ⇒ 可以填一個合法、驗證全綠、而產圖器逐位元組忽略的值 |
| **473** | ◐ 部分 | 容器在（`ApprovalsPage` / `QuickApprovalPage` / `AuditPage` / `approvals.ts`），⛔ 觸發不在（`CurationPage.tsx` 零個 audit）。⭐ 真正的工作量是把三支稽核從 `.test.ts` 抽成可呼叫函式 |
| **502** | ⛔ epic 留開 | 十個根因只剩 **#509** 一個；#513–#524 · #634 · #652 全 CLOSED。⛔ 但驗收是端到端宣稱，⛔ 不是十個根因的算術和 ⇒ 用詞紀律：「鏈路已接上，⛔ 未驗收」 |
| **509** | ◐ 部分 | ✅ ErrorToast（`LobbyScreen.tsx:112-124`，GH#513 帶掉）· ⛔ **商店商品列軌仍是裸 div**（`MerchantShop.tsx` 的 `data-pad-focusable` 計數 = **0**）—— 而那是兩個缺口裡的 🟠 major |
| **529** | ⛔ 未完成 | 重量 421 支：`fx.prim.*` **343** · `fx.w3x.*` **24**（⛔ 一支沒增加）· `sfxKey` **72/421**（⛔ 一支沒增加）；而 `fx.w3x.*` 文件從 120 漲到 **126** ⇒ ⭐ **落差反而拉大** |
| **538** | ⛔ 未完成 | `store.ts` 的 `prefetch|preload|預載` **零命中**；`royale|大混戰` 也零命中。⭐ 落點很乾淨：`store.ts:506-519` 的 landing fan-out 就是要被提前的那張表 |
| **547** | ⛔ 未完成 | 同 #529 的量測 ⇒ 零進展。⭐ 建議把 **#425 當第一步**（證據最硬、範圍最小、⛔ 不需要新 JASS 證據） |
| **558** | ◐ 部分 | ✅ ③（`configUnionCoversDirectory.test.ts:38` `ROOT = join(DIR,"..")` ＋ 遞迴 walk，檔頭記著 2026-08-23 修）· ⛔ ①（`GameApp.ts:905` → `VfxSystem.ts:750` → `modelFxRig.ts:570` 三段鏈完整，而 `AssetManager.ts:13` 自己寫著那是 per-Scene 共用快取）· ⛔ ②（`_overlay-hidden-geometry.json` 的 `e00s` 零命中，只差兩列） |

---

## 三、分診過程中量到、⛔ 原票裡沒有的三件事

1. **#425 的缺口比票文寫的大。** 票文說 earth/fire/ice/physical 四份「乾淨」——
   實際上它們**同樣指著 `fx.prim.*.bolt`**，乾淨只是因為對面沒有同名的 `bolt.earth/fire/ice/physical`。
   ⇒ **9 份 wave 沒有一份有「波」的形狀。**
2. **#457 是一個現成的失敗形態⑧樣本。** `loras` 欄位三住處齊了兩個而唯一的消費端一個字都沒讀
   ⇒ 「實作的第一件事是接消費端，⛔ 不是再加欄位」。
3. **#443 是 #452 / #453 的地基，而順序目前是反的。** 兩張票都寫「多半是③自己生成」——
   ⭐ 那句話**只在①仍被擋著時才是真的**（`w3xAbilityArt.ts:30-31` 今天仍排除 1305/1529）。
   ⇒ 先撤 #443 的四層閘，再回頭盤點要生成幾張。

## 四、本 lane 的自我限制（誠實列出「我沒驗到什麼」）

- ⛔ 沒跑任何全域鎖指令（`content:build` / `skills:sync` / `ship:check` …）
  ⇒ #382 的「重跑 baseline 之後越界剩幾筆」**沒有量到**，只確認了沒人跑過。
- ⛔ 沒碰正式站 ⇒ #441 的「線上真正開著哪 49 位」仍是票文標明的**假設**，本 lane 沿用。
- vitest 只跑 2 次（新守衛綠 ＋ 突變紅），`tsc` 1 次（`apps/client`，EXIT=0）—— 在第零守則⏱的額度內。
- ⛔ 沒做 git 寫入 ⇒ #511 的兩個檔在工作樹待 commit（已寫進關票留言，沒帶上就重開）。
