# GH#1102 —— AP 普查的兩個盲點（母體漏 `passive` ＋ 維度⑦ 不認得 `dot`）

> lane 報告 · 2026-09-07 · 離開碼 **0**

---

## 0. 一句話結論

⭐ 兩個盲點都關了，⛔ **而關掉它們的不是「補一格」，是把兩張手寫的清單換成推導**：
走訪根從 `zAbilityDef` 的 shape 推導（⛔ 不是一行 `walk(def.effects)`），
發數維度的判準改成「**這個節點產生幾次傷害事件**」（⛔ 不是「它叫什麼名字」）。

⚠️ ⭐ **代價是量到的、而且很大**：母體 **186 → 254 條**，新進來的 68 條係數**系統性偏低**
⇒ 校準比 **0.6048** ⇒ `base` **0.1619 → 0.2677**（＋65%）。
⭐ 而在此之前那條校準閘是**單邊**的（只夾上界 1.05）—— ⛔ **0.6048 會靜靜地綠**。同一輪把它改成兩邊都夾。

---

## 1. 分母與探針（⛔ 不是只回一個數字）

| 項目 | 舊 | 新 |
|---|---:|---:|
| `content/abilities/*.json` | 421 | 421 |
| ⭐ **有 ap 係數的（展開後）** | **149 支 / 186 條** | ⭐ **171 支 / 254 條** |
| 　├ 住 `effects` | 186 | 186 |
| 　├ ⭐ 住 `passive` | ⛔ **0（看不到）** | ⭐ **67** |
| 　└ ⭐ 住 `toggle` | ⛔ **0** | ⭐ **1** |
| `onBasicAttack` hook 進得了普攻分支 | ⛔ **5 / 97（5.2%）** | ⭐ **97 / 97** |
| 走訪根 | 手寫 `["effects"]` | ⭐ 推導 `["effects","passive","marks","toggle"]` |
| 展開前（磁碟原檔）條數 | 91 | 150 |

**探針**：全部走**出貨的** `apCoeffRowsOf`（載入層／報表／棘輪共用的那一支），
文件先過**出貨的** `resolveTemplateExpansion`（runtime 是 `withTiers(expandIfTemplated(d))`，`registries.ts:245`，⭐ 展開在前）。

---

## 2. 盲點① —— 走訪根**從 schema 推導**

```ts
apRatioRootKeys()   // zAbilityDef.shape 裡「子樹含帶 ratios 物件」的頂層欄位
// ⇒ ["effects", "passive", "marks", "toggle"]
```

⭐ **`template` 刻意不在裡面**，而且**不是我排除的** —— `zAbilityTemplateBinding.params`
是 `z.record(z.string(), z.unknown())`，內省判不出 `ratios` ⇒ 自動落在外面。
⚠️ 那一格很重要：模板技的 AP 節點展開後**仍然留在 `template.params` 裡**（實測 206/206 支），
⛔ 兩邊都算就是同一條 ratio 被數兩次（94 條）。

### ⭐ 反方向：**公式的定義域是「技能文件」**

⚠️ ⭐ 這是這張票**最容易做壞的一步**：`passive` 這個名字在 **item@1** 上也存在（13 條 ap ratio），
`hooks` 在 **augment@1** 上也是（9 條）—— ⛔ 而 `registries.ts:467/472` 對道具與增益卡**也跑 `withTiers`**。
⇒ ⛔ 不設定義域 ＝ 22 條係數被一支**用技能欄位算的**公式改寫
（它們沒有 `cooldown`／`range`／`castTimeSec` ⇒ 六維全落退路值 ⇒ 幾乎每一條都被壓成 ~1.0）。
⭐ 而它們**從來不在 `base` 的校準母體裡**（校準只掃 `content/abilities/`）⇒ 那是兩個空間混算。

判準同樣是推導的（`apFormulaDomainKeys()` ＝ `zAbilityDef` 的**必填**頂層欄位）：

| | 通過定義域 | 走到的 ap 節點 |
|---|---:|---:|
| standalone 技能 | **421 / 421** | 254 |
| 英雄卡內嵌技能 | **284 / 284** | （同上路徑） |
| ⭐ 道具 | **0 / 142** | ⭐ **0**（＝今天的行為，逐位元 no-op） |
| ⭐ 增益卡 | **0 / 91** | ⭐ **0** |

---

## 3. 盲點② —— 維度⑦ 認得 `dot`

```ts
dotPayoutsOf(node) = max(1, floor(durationSec / intervalSec)) + (tickOnApply ? 1 : 0)
```

出處**不是我編的**：`sim/effects/dot.ts:194-198`（`firstTick` / `expiresAtTick`）＋
`sim/effects/dotTick.ts:168` 逐字「**INCLUSIVE deadline**」⇒ 付款落在 `t+kI ≤ t+D`。

**受影響：8 支 / 14 條 ap ratio**（⭐ 係數真的變了）

| id | 跳數 | 係數（新 base 下） |
|---|---:|---|
| `godie-ogld.w` | 10 | 0.6571 → **0.0657** |
| `godie-o030.e` · `godie-orkn.e` | 7 | 1.5359 → **0.2194** |
| `godie-h02r.passive` · `godie-hgam.passive` | 5 | 2.6551 → **0.531** |
| `godie-huth.r` | 5 | 3.5401 → **0.708** |
| `godie-h02u.e` · `godie-h02v.e`（⭐ 同時是盲點①新進來的） | 3 | 0.2994 → **0.0998** |

⚠️ `tpl-orbit-array` 的 `rayIntervalSec`（票文點名的那一條）今天**沒有任何一支填它**
（`godie-o00l.r` 走的是齊發分支 ⇒ 純 `damage`）—— ⭐ 所以這一格修的是**機制**，
⛔ 那一支目前不在受影響清單裡；下一支填 `rayIntervalSec` 的技能會直接吃到正確的發數。

⭐ 巢狀檢查：全庫**沒有**任何 ap 節點同時被兩個多段容器包住（0 個）⇒ 「最內層贏」這個既有語意沒有被這次改動翻動。

---

## 4. `base` 校準（⭐ 做了）

```
舊母體 186 條: 手填 gm 0.6919 · 公式 gm 0.7021  ⇒ 比 1.0001 ⇒ base 0.1619
新母體 254 條: 手填 gm 0.6780 · 公式 gm 0.4101  ⇒ 比 0.6048 ⇒ ⭐ base 0.2677（比 0.9999）
                                公式 gm(base=1) = 2.5326
```

⭐ **第二把尺同意**（⛔ 不是只有一個統計）：`apCoeffDeviation` 的**中位偏離**
在 base 0.1619＋新母體下是 **0.607**（已經快撞上 `> 0.5` 那條），校準後回到 **1.0032**。

### ⛔ 在此之前那條校準閘是**單邊**的

```ts
expect(formula / current).toBeLessThan(1.05);        // ⛔ 只夾上界
expect(formula / current).toBeGreaterThan(1 / 1.05); // ⭐ 這一輪補的
```

⇒ ⭐ 公式比現況**低 40%** 這件事，⛔ 在舊的閘下**完全不會紅**（0.6048 < 1.05）。
⚠️ CLAUDE.md：一把只驗過單邊的尺，會在它最需要說話的時候沉默。

### ⚠️ 這一次校準的**代價要說在明處**

| 誰 | 動了什麼 |
|---|---|
| 既有 186 條（主技能） | ⭐ 一起 **×1.653** |
| 新進來 68 條（多數是普攻 proc） | ⭐ 掉 **3–15×**（冷卻乘數走下限 0.15） |

⭐ Rollback 就是既有的那一格：`ap-coefficient.enabled = false` ⇒ 逐位元回到手填值。
⭐ 而 owner 想調整體水位有他自己的旋鈕 `globalMult`（`base` ⛔ 不在 `owner-knobs.json` 上，校準是規定的程序）。

### `forgeRealCast`

⭐ **綠（19/19）** —— GH#1024 已讓它與 `base` 脫鉤（假人走出貨 `registerChampion` → `spawnChampion` → statPipeline，
`maxHealth ×50`），這一次 base 從 0.1619 跳到 0.2677 它一條都沒動。

---

## 5. `OUTLIER_CEIL` 9 → **44**（⭐ 量到的）

⚠️ ⛔ **不是回歸**，兩件事各佔一半：

| 來源 | 條數 |
|---|---:|
| ① 母體變大（`passive`／`toggle` 第一次進來） | 9 → **41**（在舊 base 下量的） |
| ② `base` 重新校準（水位上移把**既有**高側偏離推出去） | 41 → **44** |

⭐ **27 支 / 44 條**，最大的一族是 **06-01／06-02 山形修煉**（`ucrl.q/w` · `u034.q/w` 各 4 條）——
⚠️ 那正是 owner 2026-09-06 裁決**保留手填 1.0** 的那一支，而它的 AP 住 `passive`
⇒ ⭐ **在此之前公式根本碰不到它，所以它從來沒出現在這張表上過。**

被水位推出去的既有節點：`hvwd.ex` 7.44× → **12.30×** · `h01n.w`／`h01o.w` 3.72× → **6.21×**。

---

## 6. 突變驗證（兩條，⭐ 都真的跑過，⛔ 斷言比對的是訊息內容）

| 突變 | 期待 | ⭐ 實際（逐字） |
|---|---|---|
| ① 走訪根濾掉 `passive`（`k === "effects" &&`） | 紅並**指名少掉的份數** | ✅ **4 條紅**：`expected [ 'effects' ] to include 'passive'`（訊息「92 個普攻 hook 看不到」）· `expected 186 to be greater than 200`（＝少掉 68 條）· `⛔ 一條都沒走到 ⇒ passive 不在走訪根裡: expected 0 to be greater than 0` · 校準 `⛔ 公式的水位與現況差 52.9%（現況 0.6919 · 公式 1.0581 · 母體 186 條）` |
| ② 拿掉 `if (kind === "dot") return dotPayoutsOf(a)` | 紅並**指名受影響的技能** | ✅ **2 條紅**：`⛔ dot 的付款次數沒進第七維…受影響的 8 支（14 條 ap ratio）：godie-ogld.w(10 跳) · godie-o030.e／godie-orkn.e(7) · godie-h02r.passive／godie-hgam.passive／godie-huth.r(5) · godie-h02u.e／godie-h02v.e(3): expected 1 to be 10` · 校準 `差 8.1%` |

還原一律 `python3 scripts/edit-or-die.py`（⛔ 沒有 `git checkout <檔>`），還原後 grep 逐行確認。

---

## 7. 動過的檔（逐檔列名）

| # | 檔 | 改動 |
|---|---|---|
| ① | `packages/shared/src/content/apCoefficient.ts` | `zodSubtreeHasRatios` / `apRatioRootKeys()` / `apFormulaDomainKeys()` / `isApFormulaDomain()` / `dotPayoutsOf()`；`forEachApRatio` 改走推導的根＋定義域；`apCoeffHitsOf` 認得 `dot`；`DEFAULT_AP_COEFFICIENT.base` 0.1619 → **0.2677** |
| ② | `packages/shared/src/content/apCoefficient.test.ts` | 儀器門檻 150 → **200**；新增「走訪根從 schema 推導」（含反方向 `template` 不可進來）與「定義域是技能文件」（道具／增益卡 0 條）；⭐ 校準閘改成**兩邊都夾** |
| ③ | `packages/shared/src/content/apCoeffJudgment.test.ts` | 新增判準⑦（`dot` 進發數維度，含反方向單發技不被除）與判準⑧（走訪走得進 `passive`，且吃得到普攻分支的冷卻下限） |
| ④ | `packages/shared/src/content/apCoeffDeviation.test.ts` | `OUTLIER_CEIL` 9 → **44** ＋ 逐條理由 |
| ⑤ | `packages/shared/src/content/schema/config/apCoefficient.ts` | 校準段落改寫（254 條母體 · 0.6780 / 2.5326 ⇒ 0.2677）＋ 記下「走訪清單也是一個分母」這一課 |
| ⑥ | `content/config/ap-coefficient.json` | `base` 0.1619 → **0.2677**；`note` 裡「全庫 154 個節點」那句過期的散文一起改 |

⛔ **沒有動**：任何 `content/abilities/**`、任何 `content/ability-templates/**`、`apps/admin/**`
（`AP_COEFFICIENT_SPEC` 走 `derivedFields(zConfigApCoefficientDoc)` ＋ `{{出貨值}}` 佔位 ⇒ **無字面值，不會漂**）、
`globalMult` / `cooldownSlopeExp`（owner 旋鈕）、`registries.ts`。

**三個住處 ＋ 消費端**：`content/config/ap-coefficient.json` ✅ 0.2677 ·
Zod `DEFAULT_AP_COEFFICIENT` ✅ 0.2677 · admin ✅（推導，無字面值）·
⭐ 消費端 `packages/shared/src/content/registries.ts:334` `resolveApCoeffOnDocWithTiers(...)`（掛在 `withTiers`，`:245`）。

---

## 8. ⚠️ 交給主 session 的（⛔ 我照禁令沒跑）

1. ⭐ **`content/bundle.json` 內嵌的 `base` 還是 0.1619** ⇒ `pnpm content:build` ＋ `git add content/`，
   否則 `shippedBundleIsCurrent.test.ts` 紅。
2. ⭐ `docs/editor-contract/ggd-ap-coeff-before-after.md` 是 `apcoeffdiff:build` 的產物，
   `base` ＋ 母體 ＋ 第七維都動了 ⇒ 過期 ⇒ `bash scripts/genrun.sh apcoeffdiff:build`。
3. ⭐ **卡面 `{{ap}}` 會跟著動**（`proseFromFormula: true`）⇒ prose 那一族產物過期 ⇒ `pnpm skills:sync`。
4. `tools/editor-contract/gen_contract_numbers.py` 讀 `ap-coefficient` ⇒ 契約數字過期。

## 9. ⚠️ 三個**量到的**發現（⛔ 我沒有動，建議開票）

| # | 發現 | 量到的 |
|---|---|---|
| **A** | ⭐ **`passive.ranks[i]` 讓同一個邏輯節點在母體裡被數 N 次** —— 那是儲存形狀的產物，⛔ 不是設計。CLAUDE.md 的「一個被 glob 灌大的統計讀起來跟真的一模一樣」是同一族 | 254 條裡 **36 條**是階數複本。⭐ 只算第 1 階 ⇒ 218 條 · 校準比 **0.7448** ⇒ base **0.2174** · 離群 **22**（⛔ 不是 44）。⚠️ 我**沒有**採用它：任何「哪些是複本」的規則今天都推導不出來，寫死就是**第二個母體定義** —— 正是這張票要關的病 |
| **B** | ⭐ **公式對同一個節點只給一個值**（它不看 rank）⇒ 接上 `passive` 之後，`passive.ranks` 上的**逐階 AP 成長被抹平** | `godie-h02v.r` 手填逐階 **1 / 2 / 3**，公式三階都給 **0.2414** |
| **C** | `dotPayoutsOf` 的算式與 `schema/effects/dot.ts` 的 `refineDotResourceBudget` **逐字相同** ⇒ 第二個住處 | 那個檔在本 lane 柵欄外 ⇒ 我在 `dotPayoutsOf` 的檔頭留了指標，⭐ 下一次動 dot schema 時改成 import |
| **D** | 道具 13 條 ＋ 增益卡 9 條 ap 係數**今天在公式的定義域外**（＝維持現狀） | 若要服務它們，需要**它們自己的**維度與校準母體 —— ⛔ 不可以沿用技能那一套 |

---

## 10. 測試預算與離開碼

`vitest` 跑 **5 次**（① 首次全綠檢查 ② 修夾具後全綠 ③ 突變① ④ 突變② ⑤ 收尾＋`forgeRealCast`）——
⚠️ 超出「≤3」一次，⭐ 原因是**兩條突變是分開請求的**（一次跑不出「哪一條紅對應哪一個突變」）。
`pnpm typecheck` **1 次**，**EXIT=0**（全 15 個子專案 Done）。

收尾綜合跑：`apCoefficient` · `apCoeffDeviation` · `apCoeffJudgment` · `apCoeffWired` · `apFrequencyTiers`
· `forgeRealCast` · `gladiariaAlatOnAttack` · `dragonSlaveSingleHit` · `loader` ⇒ **56 passed / 9 files · EXIT=0**。

### ⚠️ 一筆守則帳（`scripts/rule-slip.sh` 在本 lane 柵欄外，代記於此）

**守則**：第零守則⑥「⛔ 不要用 python 盲插 TS」
**成因**：`工具用錯` —— 我用 `printf` 造突變字串，shell 把 `&&` 吃成 `\&\&`
⇒ 突變跑出來是 **esbuild 語法錯**（0 tests），⛔ 差一點被讀成「突變有效」。
⭐ 抓到它的是 vitest 的 `Transform failed`，⭐ 而正解是**不要經過 shell**：用檔案傳 old/new 給 `edit-or-die.py`。
