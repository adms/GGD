# GGD 效果標籤詞彙表 v2 —— ⛔ 已退役（2026-08-16）

> **這份文件的內容已經合併進 [`docs/技能標記機制與效果規則.md`](技能標記機制與效果規則.md)，
> 而那一份是產生出來的。**
>
> ```bash
> pnpm spec:build     # 重新產生（`pnpm content:build` 已經連帶跑它）
> ```

---

## 為什麼退役

它**已經在說謊了**，而且沒有任何東西叫。逐字引用它自己的第 241 行：

> 「**今天真的存在的 kind 有 24 個**」

實測引擎現在有 **37 個**。同一份表裡還有：

| 它寫的 | 實際 |
|---|---|
| 【淨化 / 驅散】`dispel` 🔨 這一輪要建 | **已出貨**，`dispel` 有處理器 |
| 【延遲落地】`scheduleEffects` 🔨 要建 | **已出貨**，叫 `delayed` |
| `onReflect` | 2026-08-08 更名 `onReflectSuccess`，⛔ 舊名寫下去會被拒絕 |
| `onLevelUp` | 2026-08-05 從 enum **刪掉**（它從進 enum 那天起就零發射點） |

這正是 `editorCapabilities.ts` 檔頭記錄過的那一句：

> a flag defended by prose outlives the prose's expiry date and **nothing goes red**

⚠️ 一份**手寫**的能力清單，它的宣稱不會過期，被它宣稱的事實會。
外部編輯器（Codex）看不到我們的 registry，**沒有辦法發現我們在說謊**，
於是它會照著一份舊清單做出一整批上線就是死的內容。

---

## 內容去了哪裡

| 這份的哪一段 | 去哪 | 現在誰負責 |
|---|---|---|
| 層 1 效果種類（中文標籤 ↔ token） | §2 · §5 | 中文名住在 `tools/skill-spec/curated.json`，**逐一與引擎對帳**；參數／上下界由 Zod schema 推導 |
| 層 2 狀態軸（五根） | §7.3 | 同上 |
| 層 3＋4 謂詞池 | §4 | 由 `buildCapabilityManifest().conditionLeafKinds` 推導 |
| 層 5 授權格 | §6 | 由 `SOURCE_GRANT_SHAPE` 推導 |
| 結構事實（殭屍／火圈／`ENTITY_FLAG`） | §8 | 手寫在 `curated.json`，⭐ **但每一條都帶一個對原始碼跑的斷言** |
| 🔨 待建 / owner 裁決不做的標籤 | §9 | `curated.json` 的 `retiredTokens` —— 知識不可以無聲消失 |

## 現在它為什麼不會再說謊

`tools/skill-spec/gen_spec.ts` 把**兩個方向**都關起來（突變都驗過會紅）：

- `curated.json` 有、引擎沒有的 token → **產生器回非零**，指名是哪一個
- 引擎有、`curated.json` 沒有中文名 → 文件上印「**待命名**」（看得見，不是無聲）
- 結構事實的前提不再成立 → **回非零**，指名是哪一條、要去改哪一格

守衛：`packages/shared/src/ops/skillSpecFresh.test.ts`（真的把產生器用 `--check` 跑起來）。

⛔ **不要在這裡補新內容。** 要加中文標籤改 `tools/skill-spec/curated.json`；
要加機制改引擎，文件會自己跟上。

歷史版本在 git；v1 在 [`docs/legacy/效果標籤詞彙表.md`](legacy/效果標籤詞彙表.md)。
