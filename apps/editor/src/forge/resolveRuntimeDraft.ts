// Compatibility surface: registration, editor preview and hero imports use the
// same resolver order, including AP coefficients and per-rank growth.
export { resolveRuntimeDraft } from "@ggd/shared/content/runtimeResolver";
