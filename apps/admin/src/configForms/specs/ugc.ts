/**
 * 設定文件的**標籤資料**（玩家自製內容 UGC 的提交閘）—— GH#991。
 *
 * ⚠️ 這裡只有**語意**（中文名稱、「它影響什麼」、上下界）。結構仍然從 Zod 走出來
 * （`../engine`），⛔ 這裡一個欄位型別都不重打 —— 見門面 `../../configForms.ts` 的檔頭。
 * ⛔ 新增一份設定要**同時**把它的 spec 掛進門面的 `CONFIG_DOC_SPECS`：忘了掛
 * ⇒ `configDocCoverage.test.ts` 紅並指名那份 `content/config/*.json`（閘從出貨的東西推導）。
 */
import {
  // ⭐ UGC 提交閘（GH#991）—— 走 barrel（`schema/config/index.ts` 有 re-export），
  //    ⛔ 不走深路徑：深路徑那條沒有守衛在看，遲早會指到搬走的檔案。
  zConfigUgcDoc,
} from "@ggd/shared/content";
import type { ConfigDocSpec } from "../engine";

// ── 🧑‍🎨 玩家自製內容（config/ugc）—— GH#991 ────────────────────────────────
export const UGC_SPEC: ConfigDocSpec<"ugc"> = {
  page: "ugc",
  collection: "config",
  docId: "ugc",
  schemaTag: "config.ugc@1",
  zod: zConfigUgcDoc,
  title: "玩家自製內容（UGC）",
  intro: [
    "owner 2026-09-05：「**開放讓玩家自己設計 英雄、技能、特效**，不是靠 AI 無止境的逼近太沒效率」。這一頁管的是**那條路開不開、開多大**，⛔ 不管內容長什麼樣。",
    "⭐ 流程：玩家在編輯器做出 JSON → 提交（格式沿用既有的 `ggd-ai-authoring-operation@1`，`author: \"player:<id>\"`，⛔ 不是第二種格式）→ **機器閘**（嚴格 Zod／空效果宣稱／出生就看不見的特效）→ **人審**（批次審查頁一頁打勾）→ 上架到 `content/ugc/`。",
    "⛔⛔ **這條路今天是關著的，而那是刻意的。** 落地的只有這一頁（開關本身）—— 身分檢查、逐份機器閘、配額計數**一個都還沒有**。⇒ 打開總開關等於開一條**沒有任何守衛的公開寫入路**。⭐ 判斷「可以打開了嗎」不要靠感覺：`packages/shared/src/ops/ugcGateIsArmed.test.ts` 會在提交端點出現而沒綁齊身分＋這一頁的開關時**紅並逐條列出缺什麼**。",
    "⚠️ ⭐ 關掉總開關**不會**動到任何已經上架的 UGC 內容 —— 那要清白名單，是另一個動作。這一格答的是「還收不收新的」，⛔ 不是「已經在的還算不算數」。",
    "⚠️ 存檔寫進的是耐久覆蓋層（data/），**覆蓋層會蓋掉 `content/config/ugc.json`**。線上存過一次之後，再去改 repo 裡那個檔案不會有任何效果。",
  ],
  consumer:
    "packages/shared/src/content/schema/config/ugc.ts 的 `resolveUgc()`（唯一知道這六格怎麼作用的地方）← ⚠️ ⭐ **今天零個執行期呼叫端** —— 提交端點還沒做（GH#991 第二批）。⇒ 這一頁現在是**規格**，而 `ugcGateIsArmed.test.ts` 是它會不會被繞過去的閘。",
  effect:
    "**下一次讀取設定就生效**（提交端點做好之後，它每一次請求都重讀，⛔ 不快取）。⛔ 不必重新部署、⛔ 不必重啟。",
  fields: [
    {
      path: "enabled",
      zh: "收不收玩家提交（總開關）",
      note:
        "⭐⭐ **這一格就是整個 UGC 的一鍵 rollback。** 出貨 **{{出貨值}}**。" +
        "關掉時，玩家提交會被**明確拒絕**（診斷碼 `UGC_DISABLED`，HTTP 403）——" +
        "⭐ 而不是靜靜地收下再丟掉，⚠️ 因為「靜靜丟掉」會讓玩家以為自己的作品在排隊。" +
        "⛔⛔ **打開它之前先看下面三格**：票文的風險段逐字寫著「配額 ＋ 位元組上限 ＋ 嚴格 Zod " +
        "是最低配，**缺一個就不要打開**」—— 提交端點是一個**對外的寫入面**，" +
        "而它今天連身分都還沒有。" +
        "⚠️ 關掉它**不會**下架任何已經上線的玩家作品（那要去清白名單）。",
    },
    {
      path: "requireAuth",
      zh: "提交必須帶玩家身分",
      note:
        "出貨 **{{出貨值}}**（fail-closed）。" +
        "⛔⛔ 關掉它的後果比「少一道登入」嚴重得多：下面兩格配額是**按玩家**算的，" +
        "沒有身分就沒有配額主體 ⇒ ⭐ **三格限制一起失效**，" +
        "⚠️ 而畫面上看起來只是「不用登入比較方便」。" +
        "⭐ 它也是「退回原因」寄得回去的唯一理由 —— 匿名提交被退，沒有人收得到那句話。",
    },
    {
      path: "maxPendingPerPlayer",
      zh: "一個玩家同時可以有幾份待審",
      note:
        "出貨 **{{出貨值}}** 份。⭐ 這一格擋的是**人審佇列** —— 漏斗最窄的一段是" +
        "「有人一份一份看」，⛔ 不是機器。" +
        "⚠️ 上界 200：再高就等於沒有擋 —— 一個人塞滿 200 份，那一天的批次審查頁" +
        "就只有他的東西，其他玩家的作品**看起來像沒送到**。" +
        "⚠️ 它只數**待審**的：被退回或被上架之後那個名額就還回去了。",
    },
    {
      path: "quotaPerPlayerPerDay",
      zh: "一個玩家一天送得出幾份",
      note:
        "出貨 **{{出貨值}}** 份／天（不論後來被退還是被收）。" +
        "⚠️ ⭐ 它與上面那格**不是同一件事**：待審深度擋得住「一次塞爆佇列」，" +
        "⛔ 擋不住「送一份、被退、立刻再送一份」那種**磨佇列**的節奏 ——" +
        "後者的待審深度永遠是 1，而它可以整天佔著審查者的注意力。" +
        "⇒ 兩格分別回答「**同時**多少」與「**多快**」。",
    },
    {
      path: "maxBytes",
      zh: "一份提交最大幾 byte",
      note:
        "出貨 **{{出貨值}}**（256 KiB）。⭐ 這個數字是**量出來的**：" +
        "出貨最大的一份技能 JSON 是 57,748 byte、英雄 25,688 byte ⇒ 4.5 倍餘裕。" +
        "⚠️ ⭐ 它擋的是**記憶體**，⛔ 不是磁碟 —— 提交要整份 parse 成 JSON 才驗得動，" +
        "而 JSON.parse 的峰值是位元組數的好幾倍。上界 4 MiB。" +
        "⚠️ 下界 4 KiB（比最小的出貨文件還大）是刻意的：填得比那更低等於整條路關掉，" +
        "⛔ 而那應該用**總開關**表達，不是用一個看起來像調整的數字。",
    },
    {
      path: "autoPromote",
      zh: "過了機器閘就直接上架",
      note:
        "出貨 **{{出貨值}}**。⭐ 關著＝**一律過人審**（票文逐字「上架一律過 HITL」）。" +
        "⛔⛔ 打開它的後果要看清楚：機器閘答得出「這份 JSON 合不合法」、" +
        "「這條效果會不會什麼都不做」、「這個特效是不是出生就全透明」，" +
        "⚠️ 答不出「這支技能**像不像**它的名字」「這個特效在戰鬥中**讀不讀得出來**」——" +
        "⭐ 那是 owner 2026-08-24 分層漏斗裡的 Tier2 語意題，而 Tier2 只有人做得到。" +
        "⇒ 打開它等於把 Tier2 整層拿掉，而**沒有任何東西會紅**。",
    },
    {
      path: "digestRecompute",
      zh: "投稿的內容指紋由伺服器重算比對",
      note:
        "出貨 **{{出貨值}}**（GH#1022）。⭐ 開著時，每一份投稿的 `packageDigest` 由 **platform 送去 " +
        "content-api 重算**（TS 側唯一那份 `packageDigest()`），與客戶端宣稱的比對：" +
        "對不上 ⇒ **400** 並指名是哪一份文件對不上；content-api 沒設定（`GGD_CONTENT_API_URL`）" +
        "或連不上 ⇒ **503**（⛔ 不會退回「當成通過」）。" +
        "⛔⛔ 關掉它等於回到「digest 是客戶端自己說的」—— 一份改了內容卻沿用舊 digest 的投稿，" +
        "**舊核准會繼續有效**（審核被繞過，而畫面上完全看不出來）。" +
        "⭐ 這一格存在是為了一鍵回頭（例：content-api 暫時掛了而投稿必須開著），⛔ 不是為了觀望。" +
        "⚠️ 後台存檔**當下**生效（platform 每一次投稿都重讀），⛔ 不必重啟。",
    },
  ],
  // 六格純量，沒有不編輯的分支要原封帶走。
  preserved: [],
};
