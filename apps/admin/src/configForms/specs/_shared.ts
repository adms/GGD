/**
 * 標籤表之間共用的驗證常數 —— GH#626 拆檔時**唯一**跨組被引用的東西。
 *
 * ⚠️ 它原本定義在傷害配色那一段，而道具卡與介面用語兩組也在用它。拆檔最常見的失敗
 * 是把這種東西複製成兩份然後各自漂（第〇·七守則的「唯一性」）⇒ 這裡是它的單一住處。
 */

/** `#rrggbb`，和 shared 的 `zColorHex` 同一條規則。 */
export const HEX6 = /^#[0-9A-Fa-f]{6}$/;
export const HEX6_ERROR = "顏色要寫成 #rrggbb 六位十六進位（例如 #FF5900），不能寫顏色名稱";
